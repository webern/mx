/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_lyric_language.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxLyricLanguage *mx_lyric_language_parse(xmlNodePtr el) {
    MxLyricLanguage *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_strdup(s);
        } else if (strcmp(aname, "name") == 0) {
            m->has_name = true;
            m->name = mx_strdup(s);
        } else if (strcmp(aname, "xml:lang") == 0) {
            m->has_xml_lang = true;
            m->xml_lang = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_lyric_language_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_lyric_language_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_lyric_language_serialize(const MxLyricLanguage *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_number) {
        xmlSetProp(el, BAD_CAST "number", BAD_CAST m->number);
    }
    if (m->has_name) {
        xmlSetProp(el, BAD_CAST "name", BAD_CAST m->name);
    }
    if (m->has_xml_lang) {
        xmlSetProp(el, BAD_CAST "xml:lang", BAD_CAST m->xml_lang);
    }
    return el;
}

void mx_lyric_language_free(MxLyricLanguage *m) {
    if (!m)
        return;
    if (m->has_number)
        free(m->number);
    if (m->has_name)
        free(m->name);
    if (m->has_xml_lang)
        free(m->xml_lang);
    free(m);
}
