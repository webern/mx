/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_system_dividers.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxSystemDividers *mx_system_dividers_parse(xmlNodePtr el) {
    MxSystemDividers *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_system_dividers_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxSystemDividersChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "left-divider") == 0) {
            ch->left_divider = mx_empty_print_object_style_align_parse(c);
            if (!ch->left_divider) {
                mx_system_dividers_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "right-divider") == 0) {
            ch->right_divider = mx_empty_print_object_style_align_parse(c);
            if (!ch->right_divider) {
                mx_system_dividers_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_system_dividers_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_system_dividers_serialize(const MxSystemDividers *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxSystemDividersChild *ch = &m->children[i];
        if (ch->left_divider) {
            mx_empty_print_object_style_align_serialize(ch->left_divider, el, "left-divider");
        } else if (ch->right_divider) {
            mx_empty_print_object_style_align_serialize(ch->right_divider, el, "right-divider");
        }
    }
    return el;
}

void mx_system_dividers_free(MxSystemDividers *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxSystemDividersChild *ch = &m->children[i];
        if (ch->left_divider)
            mx_empty_print_object_style_align_free(ch->left_divider);
        if (ch->right_divider)
            mx_empty_print_object_style_align_free(ch->right_divider);
    }
    free(m->children);
    free(m);
}
