/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_STEM_H_INCLUDED
#define MX_STEM_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_stem_value.h"
#include "mx_tenths.h"

/*
 * Stems can be down, up, none, or double. For down and up stems, the position attributes can be
 * used to specify stem length. The relative values specify the end of the stem relative to the
 * program default. Default values specify an absolute end stem position. Negative values of
 * relative-y that would flip a stem instead of shortening it are ignored. A stem element associated
 * with a rest refers to a stemlet.
 */
typedef struct {
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_color;
    MxColor color; /* attribute color */
    MxStemValue value; /* text content */
} MxStem;

/* NULL on error; the message is in mx_error(). */
MxStem *mx_stem_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_stem_serialize(const MxStem *m, xmlNodePtr parent, const char *tag);
void mx_stem_free(MxStem *m);

#endif /* MX_STEM_H_INCLUDED */
