/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TUPLET_PORTION_H_INCLUDED
#define MX_TUPLET_PORTION_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_tuplet_dot.h"
#include "mx_tuplet_number.h"
#include "mx_tuplet_type.h"

/*
 * The tuplet-portion type provides optional full control over tuplet specifications. It allows the
 * number and note type (including dots) to be set for the actual and normal portions of a single
 * tuplet. If any of these elements are absent, their values are based on the time-modification
 * element.
 */
/* One child element of MxTupletPortion: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTupletNumber *tuplet_number;
    MxTupletType *tuplet_type;
    MxTupletDot *tuplet_dot;
} MxTupletPortionChild;

typedef struct {
    MxTupletPortionChild *children; /* child elements in document order */
    size_t children_count;
} MxTupletPortion;

/* NULL on error; the message is in mx_error(). */
MxTupletPortion *mx_tuplet_portion_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_tuplet_portion_serialize(const MxTupletPortion *m, xmlNodePtr parent, const char *tag);
void mx_tuplet_portion_free(MxTupletPortion *m);

#endif /* MX_TUPLET_PORTION_H_INCLUDED */
