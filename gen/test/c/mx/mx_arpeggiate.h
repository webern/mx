/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ARPEGGIATE_H_INCLUDED
#define MX_ARPEGGIATE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_color.h"
#include "mx_number_level.h"
#include "mx_tenths.h"
#include "mx_up_down.h"

/*
 * The arpeggiate type indicates that this note is part of an arpeggiated chord. The number
 * attribute can be used to distinguish between two simultaneous chords arpeggiated separately
 * (different numbers) or together (same number). The up-down attribute is used if there is an arrow
 * on the arpeggio sign. By default, arpeggios go from the lowest to highest note.
 */
typedef struct {
    bool has_number;
    MxNumberLevel number; /* attribute number */
    bool has_direction;
    MxUpDown direction; /* attribute direction */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_id;
    char *id; /* attribute id */
} MxArpeggiate;

/* NULL on error; the message is in mx_error(). */
MxArpeggiate *mx_arpeggiate_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_arpeggiate_serialize(const MxArpeggiate *m, xmlNodePtr parent, const char *tag);
void mx_arpeggiate_free(MxArpeggiate *m);

#endif /* MX_ARPEGGIATE_H_INCLUDED */
