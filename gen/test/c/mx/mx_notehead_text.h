/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_NOTEHEAD_TEXT_H_INCLUDED
#define MX_NOTEHEAD_TEXT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_accidental_text.h"
#include "mx_formatted_text.h"

/*
 * The notehead-text type represents text that is displayed inside a notehead, as is done in some
 * educational music. It is not needed for the numbers used in tablature or jianpu notation. The
 * presence of a TAB or jianpu clefs is sufficient to indicate that numbers are used. The
 * display-text and accidental-text elements allow display of fully formatted text and accidentals.
 */
/* One child element of MxNoteheadText: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxFormattedText *display_text;
    MxAccidentalText *accidental_text;
} MxNoteheadTextChild;

typedef struct {
    MxNoteheadTextChild *children; /* child elements in document order */
    size_t children_count;
} MxNoteheadText;

/* NULL on error; the message is in mx_error(). */
MxNoteheadText *mx_notehead_text_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_notehead_text_serialize(const MxNoteheadText *m, xmlNodePtr parent, const char *tag);
void mx_notehead_text_free(MxNoteheadText *m);

#endif /* MX_NOTEHEAD_TEXT_H_INCLUDED */
