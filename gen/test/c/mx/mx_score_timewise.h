/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SCORE_TIMEWISE_H_INCLUDED
#define MX_SCORE_TIMEWISE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_credit.h"
#include "mx_defaults.h"
#include "mx_identification.h"
#include "mx_part_list.h"
#include "mx_timewise_measure.h"
#include "mx_work.h"

/* One child element of MxScoreTimewise: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxWork *work;
    char *movement_number;
    char *movement_title;
    MxIdentification *identification;
    MxDefaults *defaults;
    MxCredit *credit;
    MxPartList *part_list;
    MxTimewiseMeasure *measure;
} MxScoreTimewiseChild;

typedef struct {
    bool has_version;
    char *version; /* attribute version */
    MxScoreTimewiseChild *children; /* child elements in document order */
    size_t children_count;
} MxScoreTimewise;

/* NULL on error; the message is in mx_error(). */
MxScoreTimewise *mx_score_timewise_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_score_timewise_serialize(const MxScoreTimewise *m, xmlNodePtr parent, const char *tag);
void mx_score_timewise_free(MxScoreTimewise *m);

#endif /* MX_SCORE_TIMEWISE_H_INCLUDED */
