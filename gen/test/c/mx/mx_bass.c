/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_bass.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxBass *mx_bass_parse(xmlNodePtr el) {
    MxBass *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_bass_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxBassChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "bass-step") == 0) {
            ch->bass_step = mx_bass_step_parse(c);
            if (!ch->bass_step) {
                mx_bass_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "bass-alter") == 0) {
            ch->bass_alter = mx_bass_alter_parse(c);
            if (!ch->bass_alter) {
                mx_bass_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_bass_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_bass_serialize(const MxBass *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxBassChild *ch = &m->children[i];
        if (ch->bass_step) {
            mx_bass_step_serialize(ch->bass_step, el, "bass-step");
        } else if (ch->bass_alter) {
            mx_bass_alter_serialize(ch->bass_alter, el, "bass-alter");
        }
    }
    return el;
}

void mx_bass_free(MxBass *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxBassChild *ch = &m->children[i];
        if (ch->bass_step)
            mx_bass_step_free(ch->bass_step);
        if (ch->bass_alter)
            mx_bass_alter_free(ch->bass_alter);
    }
    free(m->children);
    free(m);
}
