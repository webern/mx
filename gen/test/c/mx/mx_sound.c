/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_sound.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxSound *mx_sound_parse(xmlNodePtr el) {
    MxSound *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "tempo") == 0) {
            m->has_tempo = true;
            m->tempo = mx_non_negative_decimal_parse(s);
        } else if (strcmp(aname, "dynamics") == 0) {
            m->has_dynamics = true;
            m->dynamics = mx_non_negative_decimal_parse(s);
        } else if (strcmp(aname, "dacapo") == 0) {
            m->has_dacapo = true;
            m->dacapo = mx_yes_no_parse(s);
        } else if (strcmp(aname, "segno") == 0) {
            m->has_segno = true;
            m->segno = mx_strdup(s);
        } else if (strcmp(aname, "dalsegno") == 0) {
            m->has_dalsegno = true;
            m->dalsegno = mx_strdup(s);
        } else if (strcmp(aname, "coda") == 0) {
            m->has_coda = true;
            m->coda = mx_strdup(s);
        } else if (strcmp(aname, "tocoda") == 0) {
            m->has_tocoda = true;
            m->tocoda = mx_strdup(s);
        } else if (strcmp(aname, "divisions") == 0) {
            m->has_divisions = true;
            m->divisions = mx_divisions_parse(s);
        } else if (strcmp(aname, "forward-repeat") == 0) {
            m->has_forward_repeat = true;
            m->forward_repeat = mx_yes_no_parse(s);
        } else if (strcmp(aname, "fine") == 0) {
            m->has_fine = true;
            m->fine = mx_strdup(s);
        } else if (strcmp(aname, "time-only") == 0) {
            m->has_time_only = true;
            m->time_only = mx_time_only_parse(s);
        } else if (strcmp(aname, "pizzicato") == 0) {
            m->has_pizzicato = true;
            m->pizzicato = mx_yes_no_parse(s);
        } else if (strcmp(aname, "pan") == 0) {
            m->has_pan = true;
            m->pan = mx_rotation_degrees_parse(s);
        } else if (strcmp(aname, "elevation") == 0) {
            m->has_elevation = true;
            m->elevation = mx_rotation_degrees_parse(s);
        } else if (strcmp(aname, "damper-pedal") == 0) {
            m->has_damper_pedal = true;
            m->damper_pedal = mx_yes_no_number_parse(s);
        } else if (strcmp(aname, "soft-pedal") == 0) {
            m->has_soft_pedal = true;
            m->soft_pedal = mx_yes_no_number_parse(s);
        } else if (strcmp(aname, "sostenuto-pedal") == 0) {
            m->has_sostenuto_pedal = true;
            m->sostenuto_pedal = mx_yes_no_number_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_sound_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxSoundChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "midi-device") == 0) {
            ch->midi_device = mx_midi_device_parse(c);
            if (!ch->midi_device) {
                mx_sound_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "midi-instrument") == 0) {
            ch->midi_instrument = mx_midi_instrument_parse(c);
            if (!ch->midi_instrument) {
                mx_sound_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "play") == 0) {
            ch->play = mx_play_parse(c);
            if (!ch->play) {
                mx_sound_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "offset") == 0) {
            ch->offset = mx_offset_parse(c);
            if (!ch->offset) {
                mx_sound_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_sound_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_sound_serialize(const MxSound *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_tempo) {
        char *s = mx_non_negative_decimal_to_string(m->tempo);
        xmlSetProp(el, BAD_CAST "tempo", BAD_CAST s);
        free(s);
    }
    if (m->has_dynamics) {
        char *s = mx_non_negative_decimal_to_string(m->dynamics);
        xmlSetProp(el, BAD_CAST "dynamics", BAD_CAST s);
        free(s);
    }
    if (m->has_dacapo) {
        xmlSetProp(el, BAD_CAST "dacapo", BAD_CAST mx_yes_no_to_string(m->dacapo));
    }
    if (m->has_segno) {
        xmlSetProp(el, BAD_CAST "segno", BAD_CAST m->segno);
    }
    if (m->has_dalsegno) {
        xmlSetProp(el, BAD_CAST "dalsegno", BAD_CAST m->dalsegno);
    }
    if (m->has_coda) {
        xmlSetProp(el, BAD_CAST "coda", BAD_CAST m->coda);
    }
    if (m->has_tocoda) {
        xmlSetProp(el, BAD_CAST "tocoda", BAD_CAST m->tocoda);
    }
    if (m->has_divisions) {
        char *s = mx_divisions_to_string(m->divisions);
        xmlSetProp(el, BAD_CAST "divisions", BAD_CAST s);
        free(s);
    }
    if (m->has_forward_repeat) {
        xmlSetProp(el, BAD_CAST "forward-repeat", BAD_CAST mx_yes_no_to_string(m->forward_repeat));
    }
    if (m->has_fine) {
        xmlSetProp(el, BAD_CAST "fine", BAD_CAST m->fine);
    }
    if (m->has_time_only) {
        xmlSetProp(el, BAD_CAST "time-only", BAD_CAST m->time_only);
    }
    if (m->has_pizzicato) {
        xmlSetProp(el, BAD_CAST "pizzicato", BAD_CAST mx_yes_no_to_string(m->pizzicato));
    }
    if (m->has_pan) {
        char *s = mx_rotation_degrees_to_string(m->pan);
        xmlSetProp(el, BAD_CAST "pan", BAD_CAST s);
        free(s);
    }
    if (m->has_elevation) {
        char *s = mx_rotation_degrees_to_string(m->elevation);
        xmlSetProp(el, BAD_CAST "elevation", BAD_CAST s);
        free(s);
    }
    if (m->has_damper_pedal) {
        char *s = mx_yes_no_number_to_string(m->damper_pedal);
        xmlSetProp(el, BAD_CAST "damper-pedal", BAD_CAST s);
        free(s);
    }
    if (m->has_soft_pedal) {
        char *s = mx_yes_no_number_to_string(m->soft_pedal);
        xmlSetProp(el, BAD_CAST "soft-pedal", BAD_CAST s);
        free(s);
    }
    if (m->has_sostenuto_pedal) {
        char *s = mx_yes_no_number_to_string(m->sostenuto_pedal);
        xmlSetProp(el, BAD_CAST "sostenuto-pedal", BAD_CAST s);
        free(s);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxSoundChild *ch = &m->children[i];
        if (ch->midi_device) {
            mx_midi_device_serialize(ch->midi_device, el, "midi-device");
        } else if (ch->midi_instrument) {
            mx_midi_instrument_serialize(ch->midi_instrument, el, "midi-instrument");
        } else if (ch->play) {
            mx_play_serialize(ch->play, el, "play");
        } else if (ch->offset) {
            mx_offset_serialize(ch->offset, el, "offset");
        }
    }
    return el;
}

void mx_sound_free(MxSound *m) {
    if (!m)
        return;
    if (m->has_segno)
        free(m->segno);
    if (m->has_dalsegno)
        free(m->dalsegno);
    if (m->has_coda)
        free(m->coda);
    if (m->has_tocoda)
        free(m->tocoda);
    if (m->has_fine)
        free(m->fine);
    if (m->has_time_only)
        free(m->time_only);
    if (m->has_damper_pedal)
        mx_yes_no_number_free(&m->damper_pedal);
    if (m->has_soft_pedal)
        mx_yes_no_number_free(&m->soft_pedal);
    if (m->has_sostenuto_pedal)
        mx_yes_no_number_free(&m->sostenuto_pedal);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxSoundChild *ch = &m->children[i];
        if (ch->midi_device)
            mx_midi_device_free(ch->midi_device);
        if (ch->midi_instrument)
            mx_midi_instrument_free(ch->midi_instrument);
        if (ch->play)
            mx_play_free(ch->play);
        if (ch->offset)
            mx_offset_free(ch->offset);
    }
    free(m->children);
    free(m);
}
