/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_KEY_OCTAVE_H_INCLUDED
#define MX_KEY_OCTAVE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_octave.h"
#include "mx_yes_no.h"

/*
 * The key-octave element specifies in which octave an element of a key signature appears. The
 * content specifies the octave value using the same values as the display-octave element. The
 * number attribute is a positive integer that refers to the key signature element in left-to-right
 * order. If the cancel attribute is set to yes, then this number refers to the canceling key
 * signature specified by the cancel element in the parent key element. The cancel attribute cannot
 * be set to yes if there is no corresponding cancel element within the parent key element. It is no
 * by default.
 */
typedef struct {
    bool has_number;
    long number; /* attribute number */
    bool has_cancel;
    MxYesNo cancel; /* attribute cancel */
    MxOctave value; /* text content */
} MxKeyOctave;

/* NULL on error; the message is in mx_error(). */
MxKeyOctave *mx_key_octave_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_key_octave_serialize(const MxKeyOctave *m, xmlNodePtr parent, const char *tag);
void mx_key_octave_free(MxKeyOctave *m);

#endif /* MX_KEY_OCTAVE_H_INCLUDED */
