/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_part_group.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPartGroup *mx_part_group_parse(xmlNodePtr el) {
    MxPartGroup *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "type") == 0) {
            m->has_type = true;
            m->type = mx_start_stop_parse(s);
        } else if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_part_group_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPartGroupChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "group-name") == 0) {
            ch->group_name = mx_group_name_parse(c);
            if (!ch->group_name) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group-name-display") == 0) {
            ch->group_name_display = mx_name_display_parse(c);
            if (!ch->group_name_display) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group-abbreviation") == 0) {
            ch->group_abbreviation = mx_group_name_parse(c);
            if (!ch->group_abbreviation) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group-abbreviation-display") == 0) {
            ch->group_abbreviation_display = mx_name_display_parse(c);
            if (!ch->group_abbreviation_display) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group-symbol") == 0) {
            ch->group_symbol = mx_group_symbol_parse(c);
            if (!ch->group_symbol) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group-barline") == 0) {
            ch->group_barline = mx_group_barline_parse(c);
            if (!ch->group_barline) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group-time") == 0) {
            ch->group_time = mx_empty_parse(c);
            if (!ch->group_time) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_part_group_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_part_group_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_part_group_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_part_group_serialize(const MxPartGroup *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_type) {
        xmlSetProp(el, BAD_CAST "type", BAD_CAST mx_start_stop_to_string(m->type));
    }
    if (m->has_number) {
        xmlSetProp(el, BAD_CAST "number", BAD_CAST m->number);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPartGroupChild *ch = &m->children[i];
        if (ch->group_name) {
            mx_group_name_serialize(ch->group_name, el, "group-name");
        } else if (ch->group_name_display) {
            mx_name_display_serialize(ch->group_name_display, el, "group-name-display");
        } else if (ch->group_abbreviation) {
            mx_group_name_serialize(ch->group_abbreviation, el, "group-abbreviation");
        } else if (ch->group_abbreviation_display) {
            mx_name_display_serialize(ch->group_abbreviation_display, el, "group-abbreviation-display");
        } else if (ch->group_symbol) {
            mx_group_symbol_serialize(ch->group_symbol, el, "group-symbol");
        } else if (ch->group_barline) {
            mx_group_barline_serialize(ch->group_barline, el, "group-barline");
        } else if (ch->group_time) {
            mx_empty_serialize(ch->group_time, el, "group-time");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        }
    }
    return el;
}

void mx_part_group_free(MxPartGroup *m) {
    if (!m)
        return;
    if (m->has_number)
        free(m->number);
    for (size_t i = 0; i < m->children_count; i++) {
        MxPartGroupChild *ch = &m->children[i];
        if (ch->group_name)
            mx_group_name_free(ch->group_name);
        if (ch->group_name_display)
            mx_name_display_free(ch->group_name_display);
        if (ch->group_abbreviation)
            mx_group_name_free(ch->group_abbreviation);
        if (ch->group_abbreviation_display)
            mx_name_display_free(ch->group_abbreviation_display);
        if (ch->group_symbol)
            mx_group_symbol_free(ch->group_symbol);
        if (ch->group_barline)
            mx_group_barline_free(ch->group_barline);
        if (ch->group_time)
            mx_empty_free(ch->group_time);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
    }
    free(m->children);
    free(m);
}
