/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_midi_instrument.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMIDIInstrument *mx_midi_instrument_parse(xmlNodePtr el) {
    MxMIDIInstrument *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_midi_instrument_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxMIDIInstrumentChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "midi-channel") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->midi_channel = malloc(sizeof(*ch->midi_channel));
            if (!ch->midi_channel)
                abort();
            *ch->midi_channel = mx_midi_16_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "midi-name") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->midi_name = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "midi-bank") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->midi_bank = malloc(sizeof(*ch->midi_bank));
            if (!ch->midi_bank)
                abort();
            *ch->midi_bank = mx_midi_16384_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "midi-program") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->midi_program = malloc(sizeof(*ch->midi_program));
            if (!ch->midi_program)
                abort();
            *ch->midi_program = mx_midi_128_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "midi-unpitched") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->midi_unpitched = malloc(sizeof(*ch->midi_unpitched));
            if (!ch->midi_unpitched)
                abort();
            *ch->midi_unpitched = mx_midi_128_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "volume") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->volume = malloc(sizeof(*ch->volume));
            if (!ch->volume)
                abort();
            *ch->volume = mx_percent_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "pan") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->pan = malloc(sizeof(*ch->pan));
            if (!ch->pan)
                abort();
            *ch->pan = mx_rotation_degrees_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "elevation") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->elevation = malloc(sizeof(*ch->elevation));
            if (!ch->elevation)
                abort();
            *ch->elevation = mx_rotation_degrees_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_midi_instrument_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_midi_instrument_serialize(const MxMIDIInstrument *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxMIDIInstrumentChild *ch = &m->children[i];
        if (ch->midi_channel) {
            char *s = mx_midi_16_to_string((*ch->midi_channel));
            xmlNewTextChild(el, NULL, BAD_CAST "midi-channel", BAD_CAST s);
            free(s);
        } else if (ch->midi_name) {
            xmlNewTextChild(el, NULL, BAD_CAST "midi-name", BAD_CAST ch->midi_name);
        } else if (ch->midi_bank) {
            char *s = mx_midi_16384_to_string((*ch->midi_bank));
            xmlNewTextChild(el, NULL, BAD_CAST "midi-bank", BAD_CAST s);
            free(s);
        } else if (ch->midi_program) {
            char *s = mx_midi_128_to_string((*ch->midi_program));
            xmlNewTextChild(el, NULL, BAD_CAST "midi-program", BAD_CAST s);
            free(s);
        } else if (ch->midi_unpitched) {
            char *s = mx_midi_128_to_string((*ch->midi_unpitched));
            xmlNewTextChild(el, NULL, BAD_CAST "midi-unpitched", BAD_CAST s);
            free(s);
        } else if (ch->volume) {
            char *s = mx_percent_to_string((*ch->volume));
            xmlNewTextChild(el, NULL, BAD_CAST "volume", BAD_CAST s);
            free(s);
        } else if (ch->pan) {
            char *s = mx_rotation_degrees_to_string((*ch->pan));
            xmlNewTextChild(el, NULL, BAD_CAST "pan", BAD_CAST s);
            free(s);
        } else if (ch->elevation) {
            char *s = mx_rotation_degrees_to_string((*ch->elevation));
            xmlNewTextChild(el, NULL, BAD_CAST "elevation", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_midi_instrument_free(MxMIDIInstrument *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxMIDIInstrumentChild *ch = &m->children[i];
        free(ch->midi_channel);
        free(ch->midi_name);
        free(ch->midi_bank);
        free(ch->midi_program);
        free(ch->midi_unpitched);
        free(ch->volume);
        free(ch->pan);
        free(ch->elevation);
    }
    free(m->children);
    free(m);
}
