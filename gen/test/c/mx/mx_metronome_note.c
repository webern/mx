/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_metronome_note.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMetronomeNote *mx_metronome_note_parse(xmlNodePtr el) {
    MxMetronomeNote *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_metronome_note_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxMetronomeNoteChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "metronome-type") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->metronome_type = malloc(sizeof(*ch->metronome_type));
            if (!ch->metronome_type)
                abort();
            *ch->metronome_type = mx_note_type_value_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "metronome-dot") == 0) {
            ch->metronome_dot = mx_empty_parse(c);
            if (!ch->metronome_dot) {
                mx_metronome_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome-beam") == 0) {
            ch->metronome_beam = mx_metronome_beam_parse(c);
            if (!ch->metronome_beam) {
                mx_metronome_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome-tied") == 0) {
            ch->metronome_tied = mx_metronome_tied_parse(c);
            if (!ch->metronome_tied) {
                mx_metronome_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome-tuplet") == 0) {
            ch->metronome_tuplet = mx_metronome_tuplet_parse(c);
            if (!ch->metronome_tuplet) {
                mx_metronome_note_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_metronome_note_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_metronome_note_serialize(const MxMetronomeNote *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxMetronomeNoteChild *ch = &m->children[i];
        if (ch->metronome_type) {
            xmlNewTextChild(el, NULL, BAD_CAST "metronome-type", BAD_CAST mx_note_type_value_to_string((*ch->metronome_type)));
        } else if (ch->metronome_dot) {
            mx_empty_serialize(ch->metronome_dot, el, "metronome-dot");
        } else if (ch->metronome_beam) {
            mx_metronome_beam_serialize(ch->metronome_beam, el, "metronome-beam");
        } else if (ch->metronome_tied) {
            mx_metronome_tied_serialize(ch->metronome_tied, el, "metronome-tied");
        } else if (ch->metronome_tuplet) {
            mx_metronome_tuplet_serialize(ch->metronome_tuplet, el, "metronome-tuplet");
        }
    }
    return el;
}

void mx_metronome_note_free(MxMetronomeNote *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxMetronomeNoteChild *ch = &m->children[i];
        free(ch->metronome_type);
        if (ch->metronome_dot)
            mx_empty_free(ch->metronome_dot);
        if (ch->metronome_beam)
            mx_metronome_beam_free(ch->metronome_beam);
        if (ch->metronome_tied)
            mx_metronome_tied_free(ch->metronome_tied);
        if (ch->metronome_tuplet)
            mx_metronome_tuplet_free(ch->metronome_tuplet);
    }
    free(m->children);
    free(m);
}
