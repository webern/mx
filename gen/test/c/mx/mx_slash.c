/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_slash.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxSlash *mx_slash_parse(xmlNodePtr el) {
    MxSlash *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "type") == 0) {
            m->has_type = true;
            m->type = mx_start_stop_parse(s);
        } else if (strcmp(aname, "use-dots") == 0) {
            m->has_use_dots = true;
            m->use_dots = mx_yes_no_parse(s);
        } else if (strcmp(aname, "use-stems") == 0) {
            m->has_use_stems = true;
            m->use_stems = mx_yes_no_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_slash_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxSlashChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "slash-type") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->slash_type = malloc(sizeof(*ch->slash_type));
            if (!ch->slash_type)
                abort();
            *ch->slash_type = mx_note_type_value_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "slash-dot") == 0) {
            ch->slash_dot = mx_empty_parse(c);
            if (!ch->slash_dot) {
                mx_slash_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "except-voice") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->except_voice = mx_strdup(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_slash_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_slash_serialize(const MxSlash *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_type) {
        xmlSetProp(el, BAD_CAST "type", BAD_CAST mx_start_stop_to_string(m->type));
    }
    if (m->has_use_dots) {
        xmlSetProp(el, BAD_CAST "use-dots", BAD_CAST mx_yes_no_to_string(m->use_dots));
    }
    if (m->has_use_stems) {
        xmlSetProp(el, BAD_CAST "use-stems", BAD_CAST mx_yes_no_to_string(m->use_stems));
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxSlashChild *ch = &m->children[i];
        if (ch->slash_type) {
            xmlNewTextChild(el, NULL, BAD_CAST "slash-type", BAD_CAST mx_note_type_value_to_string((*ch->slash_type)));
        } else if (ch->slash_dot) {
            mx_empty_serialize(ch->slash_dot, el, "slash-dot");
        } else if (ch->except_voice) {
            xmlNewTextChild(el, NULL, BAD_CAST "except-voice", BAD_CAST ch->except_voice);
        }
    }
    return el;
}

void mx_slash_free(MxSlash *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxSlashChild *ch = &m->children[i];
        free(ch->slash_type);
        if (ch->slash_dot)
            mx_empty_free(ch->slash_dot);
        free(ch->except_voice);
    }
    free(m->children);
    free(m);
}
