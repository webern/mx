/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_part_list.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPartList *mx_part_list_parse(xmlNodePtr el) {
    MxPartList *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_part_list_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPartListChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "part-group") == 0) {
            ch->part_group = mx_part_group_parse(c);
            if (!ch->part_group) {
                mx_part_list_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "score-part") == 0) {
            ch->score_part = mx_score_part_parse(c);
            if (!ch->score_part) {
                mx_part_list_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_part_list_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_part_list_serialize(const MxPartList *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPartListChild *ch = &m->children[i];
        if (ch->part_group) {
            mx_part_group_serialize(ch->part_group, el, "part-group");
        } else if (ch->score_part) {
            mx_score_part_serialize(ch->score_part, el, "score-part");
        }
    }
    return el;
}

void mx_part_list_free(MxPartList *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxPartListChild *ch = &m->children[i];
        if (ch->part_group)
            mx_part_group_free(ch->part_group);
        if (ch->score_part)
            mx_score_part_free(ch->score_part);
    }
    free(m->children);
    free(m);
}
