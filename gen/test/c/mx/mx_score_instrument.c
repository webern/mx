/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_score_instrument.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxScoreInstrument *mx_score_instrument_parse(xmlNodePtr el) {
    MxScoreInstrument *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_score_instrument_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxScoreInstrumentChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "instrument-name") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->instrument_name = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "instrument-abbreviation") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->instrument_abbreviation = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "instrument-sound") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->instrument_sound = malloc(sizeof(*ch->instrument_sound));
            if (!ch->instrument_sound)
                abort();
            *ch->instrument_sound = mx_instrument_sound_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "solo") == 0) {
            ch->solo = mx_empty_parse(c);
            if (!ch->solo) {
                mx_score_instrument_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ensemble") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->ensemble = malloc(sizeof(*ch->ensemble));
            if (!ch->ensemble)
                abort();
            *ch->ensemble = mx_positive_integer_or_empty_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "virtual-instrument") == 0) {
            ch->virtual_instrument = mx_virtual_instrument_parse(c);
            if (!ch->virtual_instrument) {
                mx_score_instrument_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_score_instrument_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_score_instrument_serialize(const MxScoreInstrument *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxScoreInstrumentChild *ch = &m->children[i];
        if (ch->instrument_name) {
            xmlNewTextChild(el, NULL, BAD_CAST "instrument-name", BAD_CAST ch->instrument_name);
        } else if (ch->instrument_abbreviation) {
            xmlNewTextChild(el, NULL, BAD_CAST "instrument-abbreviation", BAD_CAST ch->instrument_abbreviation);
        } else if (ch->instrument_sound) {
            char *s = mx_instrument_sound_to_string((*ch->instrument_sound));
            xmlNewTextChild(el, NULL, BAD_CAST "instrument-sound", BAD_CAST s);
            free(s);
        } else if (ch->solo) {
            mx_empty_serialize(ch->solo, el, "solo");
        } else if (ch->ensemble) {
            char *s = mx_positive_integer_or_empty_to_string((*ch->ensemble));
            xmlNewTextChild(el, NULL, BAD_CAST "ensemble", BAD_CAST s);
            free(s);
        } else if (ch->virtual_instrument) {
            mx_virtual_instrument_serialize(ch->virtual_instrument, el, "virtual-instrument");
        }
    }
    return el;
}

void mx_score_instrument_free(MxScoreInstrument *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxScoreInstrumentChild *ch = &m->children[i];
        free(ch->instrument_name);
        free(ch->instrument_abbreviation);
        if (ch->instrument_sound) {
            mx_instrument_sound_free(ch->instrument_sound);
            free(ch->instrument_sound);
        }
        if (ch->solo)
            mx_empty_free(ch->solo);
        if (ch->ensemble) {
            mx_positive_integer_or_empty_free(ch->ensemble);
            free(ch->ensemble);
        }
        if (ch->virtual_instrument)
            mx_virtual_instrument_free(ch->virtual_instrument);
    }
    free(m->children);
    free(m);
}
