/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_GROUPING_H_INCLUDED
#define MX_GROUPING_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_feature.h"
#include "mx_start_stop_single.h"

/*
 * The grouping type is used for musical analysis. When the type attribute is "start" or "single",
 * it usually contains one or more feature elements. The number attribute is used for distinguishing
 * between overlapping and hierarchical groupings. The member-of attribute allows for easy
 * distinguishing of what grouping elements are in what hierarchy. Feature elements contained within
 * a "stop" type of grouping may be ignored. This element is flexible to allow for different types
 * of analyses. Future versions of the MusicXML format may add elements that can represent more
 * standardized categories of analysis data, allowing for easier data sharing.
 */
/* One child element of MxGrouping: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxFeature *feature;
} MxGroupingChild;

typedef struct {
    bool has_type;
    MxStartStopSingle type; /* attribute type */
    bool has_number;
    char *number; /* attribute number */
    bool has_member_of;
    char *member_of; /* attribute member-of */
    bool has_id;
    char *id; /* attribute id */
    MxGroupingChild *children; /* child elements in document order */
    size_t children_count;
} MxGrouping;

/* NULL on error; the message is in mx_error(). */
MxGrouping *mx_grouping_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_grouping_serialize(const MxGrouping *m, xmlNodePtr parent, const char *tag);
void mx_grouping_free(MxGrouping *m);

#endif /* MX_GROUPING_H_INCLUDED */
