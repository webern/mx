/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BEAT_UNIT_TIED_H_INCLUDED
#define MX_BEAT_UNIT_TIED_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_note_type_value.h"

/*
 * The beat-unit-tied type indicates a beat-unit within a metronome mark that is tied to the
 * preceding beat-unit. This allows or two or more tied notes to be associated with a per-minute
 * value in a metronome mark, whereas the metronome-tied element is restricted to metric
 * relationship marks.
 */
/* One child element of MxBeatUnitTied: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxNoteTypeValue *beat_unit;
    MxEmpty *beat_unit_dot;
} MxBeatUnitTiedChild;

typedef struct {
    MxBeatUnitTiedChild *children; /* child elements in document order */
    size_t children_count;
} MxBeatUnitTied;

/* NULL on error; the message is in mx_error(). */
MxBeatUnitTied *mx_beat_unit_tied_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_beat_unit_tied_serialize(const MxBeatUnitTied *m, xmlNodePtr parent, const char *tag);
void mx_beat_unit_tied_free(MxBeatUnitTied *m);

#endif /* MX_BEAT_UNIT_TIED_H_INCLUDED */
