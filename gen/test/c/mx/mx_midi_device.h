/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_MIDI_DEVICE_H_INCLUDED
#define MX_MIDI_DEVICE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_midi_16.h"

/*
 * The midi-device type corresponds to the DeviceName meta event in Standard MIDI Files. The
 * optional port attribute is a number from 1 to 16 that can be used with the unofficial MIDI port
 * (or cable) meta event. Unlike the DeviceName meta event, there can be multiple midi-device
 * elements per MusicXML part starting in MusicXML 3.0. The optional id attribute refers to the
 * score-instrument assigned to this device. If missing, the device assignment affects all
 * score-instrument elements in the score-part.
 */
typedef struct {
    bool has_port;
    MxMIDI16 port; /* attribute port */
    bool has_id;
    char *id; /* attribute id */
    char *value; /* text content */
} MxMIDIDevice;

/* NULL on error; the message is in mx_error(). */
MxMIDIDevice *mx_midi_device_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_midi_device_serialize(const MxMIDIDevice *m, xmlNodePtr parent, const char *tag);
void mx_midi_device_free(MxMIDIDevice *m);

#endif /* MX_MIDI_DEVICE_H_INCLUDED */
