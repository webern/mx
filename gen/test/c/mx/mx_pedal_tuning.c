/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_pedal_tuning.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPedalTuning *mx_pedal_tuning_parse(xmlNodePtr el) {
    MxPedalTuning *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_pedal_tuning_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPedalTuningChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "pedal-step") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->pedal_step = malloc(sizeof(*ch->pedal_step));
            if (!ch->pedal_step)
                abort();
            *ch->pedal_step = mx_step_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "pedal-alter") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->pedal_alter = malloc(sizeof(*ch->pedal_alter));
            if (!ch->pedal_alter)
                abort();
            *ch->pedal_alter = mx_semitones_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_pedal_tuning_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_pedal_tuning_serialize(const MxPedalTuning *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPedalTuningChild *ch = &m->children[i];
        if (ch->pedal_step) {
            xmlNewTextChild(el, NULL, BAD_CAST "pedal-step", BAD_CAST mx_step_to_string((*ch->pedal_step)));
        } else if (ch->pedal_alter) {
            char *s = mx_semitones_to_string((*ch->pedal_alter));
            xmlNewTextChild(el, NULL, BAD_CAST "pedal-alter", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_pedal_tuning_free(MxPedalTuning *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxPedalTuningChild *ch = &m->children[i];
        free(ch->pedal_step);
        free(ch->pedal_alter);
    }
    free(m->children);
    free(m);
}
