/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_technical.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxTechnical *mx_technical_parse(xmlNodePtr el) {
    MxTechnical *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_technical_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxTechnicalChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "up-bow") == 0) {
            ch->up_bow = mx_empty_placement_parse(c);
            if (!ch->up_bow) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "down-bow") == 0) {
            ch->down_bow = mx_empty_placement_parse(c);
            if (!ch->down_bow) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "harmonic") == 0) {
            ch->harmonic = mx_harmonic_parse(c);
            if (!ch->harmonic) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "open-string") == 0) {
            ch->open_string = mx_empty_placement_parse(c);
            if (!ch->open_string) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "thumb-position") == 0) {
            ch->thumb_position = mx_empty_placement_parse(c);
            if (!ch->thumb_position) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fingering") == 0) {
            ch->fingering = mx_fingering_parse(c);
            if (!ch->fingering) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pluck") == 0) {
            ch->pluck = mx_placement_text_parse(c);
            if (!ch->pluck) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "double-tongue") == 0) {
            ch->double_tongue = mx_empty_placement_parse(c);
            if (!ch->double_tongue) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "triple-tongue") == 0) {
            ch->triple_tongue = mx_empty_placement_parse(c);
            if (!ch->triple_tongue) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "stopped") == 0) {
            ch->stopped = mx_empty_placement_smufl_parse(c);
            if (!ch->stopped) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "snap-pizzicato") == 0) {
            ch->snap_pizzicato = mx_empty_placement_parse(c);
            if (!ch->snap_pizzicato) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fret") == 0) {
            ch->fret = mx_fret_parse(c);
            if (!ch->fret) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "string") == 0) {
            ch->string = mx_string_parse(c);
            if (!ch->string) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "hammer-on") == 0) {
            ch->hammer_on = mx_hammer_on_pull_off_parse(c);
            if (!ch->hammer_on) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pull-off") == 0) {
            ch->pull_off = mx_hammer_on_pull_off_parse(c);
            if (!ch->pull_off) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "bend") == 0) {
            ch->bend = mx_bend_parse(c);
            if (!ch->bend) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tap") == 0) {
            ch->tap = mx_tap_parse(c);
            if (!ch->tap) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "heel") == 0) {
            ch->heel = mx_heel_toe_parse(c);
            if (!ch->heel) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "toe") == 0) {
            ch->toe = mx_heel_toe_parse(c);
            if (!ch->toe) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fingernails") == 0) {
            ch->fingernails = mx_empty_placement_parse(c);
            if (!ch->fingernails) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "hole") == 0) {
            ch->hole = mx_hole_parse(c);
            if (!ch->hole) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "arrow") == 0) {
            ch->arrow = mx_arrow_parse(c);
            if (!ch->arrow) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "handbell") == 0) {
            ch->handbell = mx_handbell_parse(c);
            if (!ch->handbell) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "brass-bend") == 0) {
            ch->brass_bend = mx_empty_placement_parse(c);
            if (!ch->brass_bend) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "flip") == 0) {
            ch->flip = mx_empty_placement_parse(c);
            if (!ch->flip) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "smear") == 0) {
            ch->smear = mx_empty_placement_parse(c);
            if (!ch->smear) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "open") == 0) {
            ch->open = mx_empty_placement_smufl_parse(c);
            if (!ch->open) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "half-muted") == 0) {
            ch->half_muted = mx_empty_placement_smufl_parse(c);
            if (!ch->half_muted) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "harmon-mute") == 0) {
            ch->harmon_mute = mx_harmon_mute_parse(c);
            if (!ch->harmon_mute) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "golpe") == 0) {
            ch->golpe = mx_empty_placement_parse(c);
            if (!ch->golpe) {
                mx_technical_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-technical") == 0) {
            ch->other_technical = mx_other_placement_text_parse(c);
            if (!ch->other_technical) {
                mx_technical_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_technical_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_technical_serialize(const MxTechnical *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxTechnicalChild *ch = &m->children[i];
        if (ch->up_bow) {
            mx_empty_placement_serialize(ch->up_bow, el, "up-bow");
        } else if (ch->down_bow) {
            mx_empty_placement_serialize(ch->down_bow, el, "down-bow");
        } else if (ch->harmonic) {
            mx_harmonic_serialize(ch->harmonic, el, "harmonic");
        } else if (ch->open_string) {
            mx_empty_placement_serialize(ch->open_string, el, "open-string");
        } else if (ch->thumb_position) {
            mx_empty_placement_serialize(ch->thumb_position, el, "thumb-position");
        } else if (ch->fingering) {
            mx_fingering_serialize(ch->fingering, el, "fingering");
        } else if (ch->pluck) {
            mx_placement_text_serialize(ch->pluck, el, "pluck");
        } else if (ch->double_tongue) {
            mx_empty_placement_serialize(ch->double_tongue, el, "double-tongue");
        } else if (ch->triple_tongue) {
            mx_empty_placement_serialize(ch->triple_tongue, el, "triple-tongue");
        } else if (ch->stopped) {
            mx_empty_placement_smufl_serialize(ch->stopped, el, "stopped");
        } else if (ch->snap_pizzicato) {
            mx_empty_placement_serialize(ch->snap_pizzicato, el, "snap-pizzicato");
        } else if (ch->fret) {
            mx_fret_serialize(ch->fret, el, "fret");
        } else if (ch->string) {
            mx_string_serialize(ch->string, el, "string");
        } else if (ch->hammer_on) {
            mx_hammer_on_pull_off_serialize(ch->hammer_on, el, "hammer-on");
        } else if (ch->pull_off) {
            mx_hammer_on_pull_off_serialize(ch->pull_off, el, "pull-off");
        } else if (ch->bend) {
            mx_bend_serialize(ch->bend, el, "bend");
        } else if (ch->tap) {
            mx_tap_serialize(ch->tap, el, "tap");
        } else if (ch->heel) {
            mx_heel_toe_serialize(ch->heel, el, "heel");
        } else if (ch->toe) {
            mx_heel_toe_serialize(ch->toe, el, "toe");
        } else if (ch->fingernails) {
            mx_empty_placement_serialize(ch->fingernails, el, "fingernails");
        } else if (ch->hole) {
            mx_hole_serialize(ch->hole, el, "hole");
        } else if (ch->arrow) {
            mx_arrow_serialize(ch->arrow, el, "arrow");
        } else if (ch->handbell) {
            mx_handbell_serialize(ch->handbell, el, "handbell");
        } else if (ch->brass_bend) {
            mx_empty_placement_serialize(ch->brass_bend, el, "brass-bend");
        } else if (ch->flip) {
            mx_empty_placement_serialize(ch->flip, el, "flip");
        } else if (ch->smear) {
            mx_empty_placement_serialize(ch->smear, el, "smear");
        } else if (ch->open) {
            mx_empty_placement_smufl_serialize(ch->open, el, "open");
        } else if (ch->half_muted) {
            mx_empty_placement_smufl_serialize(ch->half_muted, el, "half-muted");
        } else if (ch->harmon_mute) {
            mx_harmon_mute_serialize(ch->harmon_mute, el, "harmon-mute");
        } else if (ch->golpe) {
            mx_empty_placement_serialize(ch->golpe, el, "golpe");
        } else if (ch->other_technical) {
            mx_other_placement_text_serialize(ch->other_technical, el, "other-technical");
        }
    }
    return el;
}

void mx_technical_free(MxTechnical *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxTechnicalChild *ch = &m->children[i];
        if (ch->up_bow)
            mx_empty_placement_free(ch->up_bow);
        if (ch->down_bow)
            mx_empty_placement_free(ch->down_bow);
        if (ch->harmonic)
            mx_harmonic_free(ch->harmonic);
        if (ch->open_string)
            mx_empty_placement_free(ch->open_string);
        if (ch->thumb_position)
            mx_empty_placement_free(ch->thumb_position);
        if (ch->fingering)
            mx_fingering_free(ch->fingering);
        if (ch->pluck)
            mx_placement_text_free(ch->pluck);
        if (ch->double_tongue)
            mx_empty_placement_free(ch->double_tongue);
        if (ch->triple_tongue)
            mx_empty_placement_free(ch->triple_tongue);
        if (ch->stopped)
            mx_empty_placement_smufl_free(ch->stopped);
        if (ch->snap_pizzicato)
            mx_empty_placement_free(ch->snap_pizzicato);
        if (ch->fret)
            mx_fret_free(ch->fret);
        if (ch->string)
            mx_string_free(ch->string);
        if (ch->hammer_on)
            mx_hammer_on_pull_off_free(ch->hammer_on);
        if (ch->pull_off)
            mx_hammer_on_pull_off_free(ch->pull_off);
        if (ch->bend)
            mx_bend_free(ch->bend);
        if (ch->tap)
            mx_tap_free(ch->tap);
        if (ch->heel)
            mx_heel_toe_free(ch->heel);
        if (ch->toe)
            mx_heel_toe_free(ch->toe);
        if (ch->fingernails)
            mx_empty_placement_free(ch->fingernails);
        if (ch->hole)
            mx_hole_free(ch->hole);
        if (ch->arrow)
            mx_arrow_free(ch->arrow);
        if (ch->handbell)
            mx_handbell_free(ch->handbell);
        if (ch->brass_bend)
            mx_empty_placement_free(ch->brass_bend);
        if (ch->flip)
            mx_empty_placement_free(ch->flip);
        if (ch->smear)
            mx_empty_placement_free(ch->smear);
        if (ch->open)
            mx_empty_placement_smufl_free(ch->open);
        if (ch->half_muted)
            mx_empty_placement_smufl_free(ch->half_muted);
        if (ch->harmon_mute)
            mx_harmon_mute_free(ch->harmon_mute);
        if (ch->golpe)
            mx_empty_placement_free(ch->golpe);
        if (ch->other_technical)
            mx_other_placement_text_free(ch->other_technical);
    }
    free(m->children);
    free(m);
}
