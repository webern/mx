/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_percussion.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPercussion *mx_percussion_parse(xmlNodePtr el) {
    MxPercussion *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "halign") == 0) {
            m->has_halign = true;
            m->halign = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "valign") == 0) {
            m->has_valign = true;
            m->valign = mx_valign_parse(s);
        } else if (strcmp(aname, "enclosure") == 0) {
            m->has_enclosure = true;
            m->enclosure = mx_enclosure_shape_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_percussion_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPercussionChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "glass") == 0) {
            ch->glass = mx_glass_parse(c);
            if (!ch->glass) {
                mx_percussion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metal") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->metal = malloc(sizeof(*ch->metal));
            if (!ch->metal)
                abort();
            *ch->metal = mx_metal_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "wood") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->wood = malloc(sizeof(*ch->wood));
            if (!ch->wood)
                abort();
            *ch->wood = mx_wood_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "pitched") == 0) {
            ch->pitched = mx_pitched_parse(c);
            if (!ch->pitched) {
                mx_percussion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "membrane") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->membrane = malloc(sizeof(*ch->membrane));
            if (!ch->membrane)
                abort();
            *ch->membrane = mx_membrane_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "effect") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->effect = malloc(sizeof(*ch->effect));
            if (!ch->effect)
                abort();
            *ch->effect = mx_effect_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "timpani") == 0) {
            ch->timpani = mx_empty_parse(c);
            if (!ch->timpani) {
                mx_percussion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "beater") == 0) {
            ch->beater = mx_beater_parse(c);
            if (!ch->beater) {
                mx_percussion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "stick") == 0) {
            ch->stick = mx_stick_parse(c);
            if (!ch->stick) {
                mx_percussion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "stick-location") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->stick_location = malloc(sizeof(*ch->stick_location));
            if (!ch->stick_location)
                abort();
            *ch->stick_location = mx_stick_location_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "other-percussion") == 0) {
            ch->other_percussion = mx_other_text_parse(c);
            if (!ch->other_percussion) {
                mx_percussion_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_percussion_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_percussion_serialize(const MxPercussion *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_halign) {
        xmlSetProp(el, BAD_CAST "halign", BAD_CAST mx_left_center_right_to_string(m->halign));
    }
    if (m->has_valign) {
        xmlSetProp(el, BAD_CAST "valign", BAD_CAST mx_valign_to_string(m->valign));
    }
    if (m->has_enclosure) {
        xmlSetProp(el, BAD_CAST "enclosure", BAD_CAST mx_enclosure_shape_to_string(m->enclosure));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPercussionChild *ch = &m->children[i];
        if (ch->glass) {
            mx_glass_serialize(ch->glass, el, "glass");
        } else if (ch->metal) {
            xmlNewTextChild(el, NULL, BAD_CAST "metal", BAD_CAST mx_metal_to_string((*ch->metal)));
        } else if (ch->wood) {
            xmlNewTextChild(el, NULL, BAD_CAST "wood", BAD_CAST mx_wood_to_string((*ch->wood)));
        } else if (ch->pitched) {
            mx_pitched_serialize(ch->pitched, el, "pitched");
        } else if (ch->membrane) {
            xmlNewTextChild(el, NULL, BAD_CAST "membrane", BAD_CAST mx_membrane_to_string((*ch->membrane)));
        } else if (ch->effect) {
            xmlNewTextChild(el, NULL, BAD_CAST "effect", BAD_CAST mx_effect_to_string((*ch->effect)));
        } else if (ch->timpani) {
            mx_empty_serialize(ch->timpani, el, "timpani");
        } else if (ch->beater) {
            mx_beater_serialize(ch->beater, el, "beater");
        } else if (ch->stick) {
            mx_stick_serialize(ch->stick, el, "stick");
        } else if (ch->stick_location) {
            xmlNewTextChild(el, NULL, BAD_CAST "stick-location", BAD_CAST mx_stick_location_to_string((*ch->stick_location)));
        } else if (ch->other_percussion) {
            mx_other_text_serialize(ch->other_percussion, el, "other-percussion");
        }
    }
    return el;
}

void mx_percussion_free(MxPercussion *m) {
    if (!m)
        return;
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxPercussionChild *ch = &m->children[i];
        if (ch->glass)
            mx_glass_free(ch->glass);
        free(ch->metal);
        free(ch->wood);
        if (ch->pitched)
            mx_pitched_free(ch->pitched);
        free(ch->membrane);
        free(ch->effect);
        if (ch->timpani)
            mx_empty_free(ch->timpani);
        if (ch->beater)
            mx_beater_free(ch->beater);
        if (ch->stick)
            mx_stick_free(ch->stick);
        free(ch->stick_location);
        if (ch->other_percussion)
            mx_other_text_free(ch->other_percussion);
    }
    free(m->children);
    free(m);
}
