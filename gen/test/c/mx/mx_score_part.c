/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_score_part.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxScorePart *mx_score_part_parse(xmlNodePtr el) {
    MxScorePart *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_score_part_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxScorePartChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "identification") == 0) {
            ch->identification = mx_identification_parse(c);
            if (!ch->identification) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-name") == 0) {
            ch->part_name = mx_part_name_parse(c);
            if (!ch->part_name) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-name-display") == 0) {
            ch->part_name_display = mx_name_display_parse(c);
            if (!ch->part_name_display) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-abbreviation") == 0) {
            ch->part_abbreviation = mx_part_name_parse(c);
            if (!ch->part_abbreviation) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-abbreviation-display") == 0) {
            ch->part_abbreviation_display = mx_name_display_parse(c);
            if (!ch->part_abbreviation_display) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "group") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->group = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "score-instrument") == 0) {
            ch->score_instrument = mx_score_instrument_parse(c);
            if (!ch->score_instrument) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "midi-device") == 0) {
            ch->midi_device = mx_midi_device_parse(c);
            if (!ch->midi_device) {
                mx_score_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "midi-instrument") == 0) {
            ch->midi_instrument = mx_midi_instrument_parse(c);
            if (!ch->midi_instrument) {
                mx_score_part_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_score_part_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_score_part_serialize(const MxScorePart *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxScorePartChild *ch = &m->children[i];
        if (ch->identification) {
            mx_identification_serialize(ch->identification, el, "identification");
        } else if (ch->part_name) {
            mx_part_name_serialize(ch->part_name, el, "part-name");
        } else if (ch->part_name_display) {
            mx_name_display_serialize(ch->part_name_display, el, "part-name-display");
        } else if (ch->part_abbreviation) {
            mx_part_name_serialize(ch->part_abbreviation, el, "part-abbreviation");
        } else if (ch->part_abbreviation_display) {
            mx_name_display_serialize(ch->part_abbreviation_display, el, "part-abbreviation-display");
        } else if (ch->group) {
            xmlNewTextChild(el, NULL, BAD_CAST "group", BAD_CAST ch->group);
        } else if (ch->score_instrument) {
            mx_score_instrument_serialize(ch->score_instrument, el, "score-instrument");
        } else if (ch->midi_device) {
            mx_midi_device_serialize(ch->midi_device, el, "midi-device");
        } else if (ch->midi_instrument) {
            mx_midi_instrument_serialize(ch->midi_instrument, el, "midi-instrument");
        }
    }
    return el;
}

void mx_score_part_free(MxScorePart *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxScorePartChild *ch = &m->children[i];
        if (ch->identification)
            mx_identification_free(ch->identification);
        if (ch->part_name)
            mx_part_name_free(ch->part_name);
        if (ch->part_name_display)
            mx_name_display_free(ch->part_name_display);
        if (ch->part_abbreviation)
            mx_part_name_free(ch->part_abbreviation);
        if (ch->part_abbreviation_display)
            mx_name_display_free(ch->part_abbreviation_display);
        free(ch->group);
        if (ch->score_instrument)
            mx_score_instrument_free(ch->score_instrument);
        if (ch->midi_device)
            mx_midi_device_free(ch->midi_device);
        if (ch->midi_instrument)
            mx_midi_instrument_free(ch->midi_instrument);
    }
    free(m->children);
    free(m);
}
