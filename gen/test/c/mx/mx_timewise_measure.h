/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TIMEWISE_MEASURE_H_INCLUDED
#define MX_TIMEWISE_MEASURE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_measure_text.h"
#include "mx_tenths.h"
#include "mx_timewise_part.h"
#include "mx_yes_no.h"

/* One child element of MxTimewiseMeasure: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTimewisePart *part;
} MxTimewiseMeasureChild;

typedef struct {
    bool has_number;
    char *number; /* attribute number */
    bool has_text;
    MxMeasureText text; /* attribute text */
    bool has_implicit;
    MxYesNo implicit; /* attribute implicit */
    bool has_non_controlling;
    MxYesNo non_controlling; /* attribute non-controlling */
    bool has_width;
    MxTenths width; /* attribute width */
    bool has_id;
    char *id; /* attribute id */
    MxTimewiseMeasureChild *children; /* child elements in document order */
    size_t children_count;
} MxTimewiseMeasure;

/* NULL on error; the message is in mx_error(). */
MxTimewiseMeasure *mx_timewise_measure_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_timewise_measure_serialize(const MxTimewiseMeasure *m, xmlNodePtr parent, const char *tag);
void mx_timewise_measure_free(MxTimewiseMeasure *m);

#endif /* MX_TIMEWISE_MEASURE_H_INCLUDED */
