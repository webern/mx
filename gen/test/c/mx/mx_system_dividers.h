/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SYSTEM_DIVIDERS_H_INCLUDED
#define MX_SYSTEM_DIVIDERS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty_print_object_style_align.h"

/*
 * The system-dividers element indicates the presence or absence of system dividers (also known as
 * system separation marks) between systems displayed on the same page. Dividers on the left and
 * right side of the page are controlled by the left-divider and right-divider elements
 * respectively. The default vertical position is half the system-distance value from the top of the
 * system that is below the divider. The default horizontal position is the left and right system
 * margin, respectively. When used in the print element, the system-dividers element affects the
 * dividers that would appear between the current system and the previous system.
 */
/* One child element of MxSystemDividers: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxEmptyPrintObjectStyleAlign *left_divider;
    MxEmptyPrintObjectStyleAlign *right_divider;
} MxSystemDividersChild;

typedef struct {
    MxSystemDividersChild *children; /* child elements in document order */
    size_t children_count;
} MxSystemDividers;

/* NULL on error; the message is in mx_error(). */
MxSystemDividers *mx_system_dividers_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_system_dividers_serialize(const MxSystemDividers *m, xmlNodePtr parent, const char *tag);
void mx_system_dividers_free(MxSystemDividers *m);

#endif /* MX_SYSTEM_DIVIDERS_H_INCLUDED */
