/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PAGE_LAYOUT_H_INCLUDED
#define MX_PAGE_LAYOUT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_page_margins.h"
#include "mx_tenths.h"

/*
 * Page layout can be defined both in score-wide defaults and in the print element. Page margins are
 * specified either for both even and odd pages, or via separate odd and even page number values.
 * The type is not needed when used as part of a print element. If omitted when used in the defaults
 * element, "both" is the default.
 */
/* One child element of MxPageLayout: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTenths *page_height;
    MxTenths *page_width;
    MxPageMargins *page_margins;
} MxPageLayoutChild;

typedef struct {
    MxPageLayoutChild *children; /* child elements in document order */
    size_t children_count;
} MxPageLayout;

/* NULL on error; the message is in mx_error(). */
MxPageLayout *mx_page_layout_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_page_layout_serialize(const MxPageLayout *m, xmlNodePtr parent, const char *tag);
void mx_page_layout_free(MxPageLayout *m);

#endif /* MX_PAGE_LAYOUT_H_INCLUDED */
