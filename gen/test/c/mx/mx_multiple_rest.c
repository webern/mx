/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_multiple_rest.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMultipleRest *mx_multiple_rest_parse(xmlNodePtr el) {
    MxMultipleRest *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "use-symbols") == 0) {
            m->has_use_symbols = true;
            m->use_symbols = mx_yes_no_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_multiple_rest_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    {
        xmlChar *text = xmlNodeGetContent(el);
        const char *s = text ? (const char *)text : "";
        m->value = mx_positive_integer_or_empty_parse(s);
        xmlFree(text);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_multiple_rest_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_multiple_rest_serialize(const MxMultipleRest *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    {
        char *s = mx_positive_integer_or_empty_to_string(m->value);
        xmlAddChild(el, xmlNewText(BAD_CAST s));
        free(s);
    }
    if (m->has_use_symbols) {
        xmlSetProp(el, BAD_CAST "use-symbols", BAD_CAST mx_yes_no_to_string(m->use_symbols));
    }
    return el;
}

void mx_multiple_rest_free(MxMultipleRest *m) {
    if (!m)
        return;
    mx_positive_integer_or_empty_free(&m->value);
    free(m);
}
