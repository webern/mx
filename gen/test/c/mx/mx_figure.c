/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_figure.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxFigure *mx_figure_parse(xmlNodePtr el) {
    MxFigure *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_figure_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxFigureChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "prefix") == 0) {
            ch->prefix = mx_style_text_parse(c);
            if (!ch->prefix) {
                mx_figure_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "figure-number") == 0) {
            ch->figure_number = mx_style_text_parse(c);
            if (!ch->figure_number) {
                mx_figure_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "suffix") == 0) {
            ch->suffix = mx_style_text_parse(c);
            if (!ch->suffix) {
                mx_figure_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "extend") == 0) {
            ch->extend = mx_extend_parse(c);
            if (!ch->extend) {
                mx_figure_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_figure_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_figure_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_figure_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_figure_serialize(const MxFigure *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxFigureChild *ch = &m->children[i];
        if (ch->prefix) {
            mx_style_text_serialize(ch->prefix, el, "prefix");
        } else if (ch->figure_number) {
            mx_style_text_serialize(ch->figure_number, el, "figure-number");
        } else if (ch->suffix) {
            mx_style_text_serialize(ch->suffix, el, "suffix");
        } else if (ch->extend) {
            mx_extend_serialize(ch->extend, el, "extend");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        }
    }
    return el;
}

void mx_figure_free(MxFigure *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxFigureChild *ch = &m->children[i];
        if (ch->prefix)
            mx_style_text_free(ch->prefix);
        if (ch->figure_number)
            mx_style_text_free(ch->figure_number);
        if (ch->suffix)
            mx_style_text_free(ch->suffix);
        if (ch->extend)
            mx_extend_free(ch->extend);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
    }
    free(m->children);
    free(m);
}
