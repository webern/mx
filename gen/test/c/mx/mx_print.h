/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PRINT_H_INCLUDED
#define MX_PRINT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_measure_layout.h"
#include "mx_measure_numbering.h"
#include "mx_name_display.h"
#include "mx_page_layout.h"
#include "mx_staff_layout.h"
#include "mx_system_layout.h"
#include "mx_tenths.h"
#include "mx_yes_no.h"

/*
 * The print type contains general printing parameters, including the layout elements defined in the
 * layout.mod file. The part-name-display and part-abbreviation-display elements used in the
 * score.mod file may also be used here to change how a part name or abbreviation is displayed over
 * the course of a piece. They take effect when the current measure or a succeeding measure starts a
 * new system. Layout elements in a print statement only apply to the current page, system, staff,
 * or measure. Music that follows continues to take the default values from the layout included in
 * the defaults element.
 */
/* One child element of MxPrint: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxPageLayout *page_layout;
    MxSystemLayout *system_layout;
    MxStaffLayout *staff_layout;
    MxMeasureLayout *measure_layout;
    MxMeasureNumbering *measure_numbering;
    MxNameDisplay *part_name_display;
    MxNameDisplay *part_abbreviation_display;
} MxPrintChild;

typedef struct {
    bool has_staff_spacing;
    MxTenths staff_spacing; /* attribute staff-spacing */
    bool has_new_system;
    MxYesNo new_system; /* attribute new-system */
    bool has_new_page;
    MxYesNo new_page; /* attribute new-page */
    bool has_blank_page;
    long blank_page; /* attribute blank-page */
    bool has_page_number;
    char *page_number; /* attribute page-number */
    bool has_id;
    char *id; /* attribute id */
    MxPrintChild *children; /* child elements in document order */
    size_t children_count;
} MxPrint;

/* NULL on error; the message is in mx_error(). */
MxPrint *mx_print_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_print_serialize(const MxPrint *m, xmlNodePtr parent, const char *tag);
void mx_print_free(MxPrint *m);

#endif /* MX_PRINT_H_INCLUDED */
