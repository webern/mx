/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TIE_H_INCLUDED
#define MX_TIE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_start_stop.h"
#include "mx_time_only.h"

/*
 * The tie element indicates that a tie begins or ends with this note. If the tie element applies
 * only particular times through a repeat, the time-only attribute indicates which times to apply
 * it. The tie element indicates sound; the tied element indicates notation.
 */
typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_time_only;
    MxTimeOnly time_only; /* attribute time-only */
} MxTie;

/* NULL on error; the message is in mx_error(). */
MxTie *mx_tie_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_tie_serialize(const MxTie *m, xmlNodePtr parent, const char *tag);
void mx_tie_free(MxTie *m);

#endif /* MX_TIE_H_INCLUDED */
