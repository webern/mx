/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PITCH_H_INCLUDED
#define MX_PITCH_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_octave.h"
#include "mx_semitones.h"
#include "mx_step.h"

/*
 * Pitch is represented as a combination of the step of the diatonic scale, the chromatic
 * alteration, and the octave.
 */
/* One child element of MxPitch: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStep *step;
    MxSemitones *alter;
    MxOctave *octave;
} MxPitchChild;

typedef struct {
    MxPitchChild *children; /* child elements in document order */
    size_t children_count;
} MxPitch;

/* NULL on error; the message is in mx_error(). */
MxPitch *mx_pitch_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_pitch_serialize(const MxPitch *m, xmlNodePtr parent, const char *tag);
void mx_pitch_free(MxPitch *m);

#endif /* MX_PITCH_H_INCLUDED */
