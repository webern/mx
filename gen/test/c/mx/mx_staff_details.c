/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_staff_details.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxStaffDetails *mx_staff_details_parse(xmlNodePtr el) {
    MxStaffDetails *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_staff_number_parse(s);
        } else if (strcmp(aname, "show-frets") == 0) {
            m->has_show_frets = true;
            m->show_frets = mx_show_frets_parse(s);
        } else if (strcmp(aname, "print-object") == 0) {
            m->has_print_object = true;
            m->print_object = mx_yes_no_parse(s);
        } else if (strcmp(aname, "print-spacing") == 0) {
            m->has_print_spacing = true;
            m->print_spacing = mx_yes_no_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_staff_details_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxStaffDetailsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "staff-type") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staff_type = malloc(sizeof(*ch->staff_type));
            if (!ch->staff_type)
                abort();
            *ch->staff_type = mx_staff_type_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "staff-lines") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staff_lines = malloc(sizeof(*ch->staff_lines));
            if (!ch->staff_lines)
                abort();
            *ch->staff_lines = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "staff-tuning") == 0) {
            ch->staff_tuning = mx_staff_tuning_parse(c);
            if (!ch->staff_tuning) {
                mx_staff_details_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "capo") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->capo = malloc(sizeof(*ch->capo));
            if (!ch->capo)
                abort();
            *ch->capo = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "staff-size") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staff_size = malloc(sizeof(*ch->staff_size));
            if (!ch->staff_size)
                abort();
            *ch->staff_size = mx_non_negative_decimal_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_staff_details_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_staff_details_serialize(const MxStaffDetails *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_number) {
        char *s = mx_staff_number_to_string(m->number);
        xmlSetProp(el, BAD_CAST "number", BAD_CAST s);
        free(s);
    }
    if (m->has_show_frets) {
        xmlSetProp(el, BAD_CAST "show-frets", BAD_CAST mx_show_frets_to_string(m->show_frets));
    }
    if (m->has_print_object) {
        xmlSetProp(el, BAD_CAST "print-object", BAD_CAST mx_yes_no_to_string(m->print_object));
    }
    if (m->has_print_spacing) {
        xmlSetProp(el, BAD_CAST "print-spacing", BAD_CAST mx_yes_no_to_string(m->print_spacing));
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxStaffDetailsChild *ch = &m->children[i];
        if (ch->staff_type) {
            xmlNewTextChild(el, NULL, BAD_CAST "staff-type", BAD_CAST mx_staff_type_to_string((*ch->staff_type)));
        } else if (ch->staff_lines) {
            char *s = mx_format_int((*ch->staff_lines));
            xmlNewTextChild(el, NULL, BAD_CAST "staff-lines", BAD_CAST s);
            free(s);
        } else if (ch->staff_tuning) {
            mx_staff_tuning_serialize(ch->staff_tuning, el, "staff-tuning");
        } else if (ch->capo) {
            char *s = mx_format_int((*ch->capo));
            xmlNewTextChild(el, NULL, BAD_CAST "capo", BAD_CAST s);
            free(s);
        } else if (ch->staff_size) {
            char *s = mx_non_negative_decimal_to_string((*ch->staff_size));
            xmlNewTextChild(el, NULL, BAD_CAST "staff-size", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_staff_details_free(MxStaffDetails *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxStaffDetailsChild *ch = &m->children[i];
        free(ch->staff_type);
        free(ch->staff_lines);
        if (ch->staff_tuning)
            mx_staff_tuning_free(ch->staff_tuning);
        free(ch->capo);
        free(ch->staff_size);
    }
    free(m->children);
    free(m);
}
