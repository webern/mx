/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_system_layout.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxSystemLayout *mx_system_layout_parse(xmlNodePtr el) {
    MxSystemLayout *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_system_layout_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxSystemLayoutChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "system-margins") == 0) {
            ch->system_margins = mx_system_margins_parse(c);
            if (!ch->system_margins) {
                mx_system_layout_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "system-distance") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->system_distance = malloc(sizeof(*ch->system_distance));
            if (!ch->system_distance)
                abort();
            *ch->system_distance = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "top-system-distance") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->top_system_distance = malloc(sizeof(*ch->top_system_distance));
            if (!ch->top_system_distance)
                abort();
            *ch->top_system_distance = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "system-dividers") == 0) {
            ch->system_dividers = mx_system_dividers_parse(c);
            if (!ch->system_dividers) {
                mx_system_layout_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_system_layout_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_system_layout_serialize(const MxSystemLayout *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxSystemLayoutChild *ch = &m->children[i];
        if (ch->system_margins) {
            mx_system_margins_serialize(ch->system_margins, el, "system-margins");
        } else if (ch->system_distance) {
            char *s = mx_tenths_to_string((*ch->system_distance));
            xmlNewTextChild(el, NULL, BAD_CAST "system-distance", BAD_CAST s);
            free(s);
        } else if (ch->top_system_distance) {
            char *s = mx_tenths_to_string((*ch->top_system_distance));
            xmlNewTextChild(el, NULL, BAD_CAST "top-system-distance", BAD_CAST s);
            free(s);
        } else if (ch->system_dividers) {
            mx_system_dividers_serialize(ch->system_dividers, el, "system-dividers");
        }
    }
    return el;
}

void mx_system_layout_free(MxSystemLayout *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxSystemLayoutChild *ch = &m->children[i];
        if (ch->system_margins)
            mx_system_margins_free(ch->system_margins);
        free(ch->system_distance);
        free(ch->top_system_distance);
        if (ch->system_dividers)
            mx_system_dividers_free(ch->system_dividers);
    }
    free(m->children);
    free(m);
}
