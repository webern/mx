/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TIME_SYMBOL_H_INCLUDED
#define MX_TIME_SYMBOL_H_INCLUDED

#include <stdbool.h>

/*
 * The time-symbol type indicates how to display a time signature. The normal value is the usual
 * fractional display, and is the implied symbol type if none is specified. Other options are the
 * common and cut time symbols, as well as a single number with an implied denominator. The note
 * symbol indicates that the beat-type should be represented with the corresponding downstem note
 * rather than a number. The dotted-note symbol indicates that the beat-type should be represented
 * with a dotted downstem note that corresponds to three times the beat-type value, and a numerator
 * that is one third the beats value.
 */
typedef enum {
    MX_TIME_SYMBOL_COMMON = 0,
    MX_TIME_SYMBOL_CUT = 1,
    MX_TIME_SYMBOL_SINGLE_NUMBER = 2,
    MX_TIME_SYMBOL_NOTE = 3,
    MX_TIME_SYMBOL_DOTTED_NOTE = 4,
    MX_TIME_SYMBOL_NORMAL = 5
} MxTimeSymbol;

bool mx_time_symbol_try_parse(const char *s, MxTimeSymbol *out);
/* Lenient: unknown input falls back to the first variant. */
MxTimeSymbol mx_time_symbol_parse(const char *s);
/* Returns the wire literal (static storage; do not free). */
const char *mx_time_symbol_to_string(MxTimeSymbol v);

#endif /* MX_TIME_SYMBOL_H_INCLUDED */
