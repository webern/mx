/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PARTWISE_PART_H_INCLUDED
#define MX_PARTWISE_PART_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_partwise_measure.h"

/* One child element of MxPartwisePart: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxPartwiseMeasure *measure;
} MxPartwisePartChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxPartwisePartChild *children; /* child elements in document order */
    size_t children_count;
} MxPartwisePart;

/* NULL on error; the message is in mx_error(). */
MxPartwisePart *mx_partwise_part_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_partwise_part_serialize(const MxPartwisePart *m, xmlNodePtr parent, const char *tag);
void mx_partwise_part_free(MxPartwisePart *m);

#endif /* MX_PARTWISE_PART_H_INCLUDED */
