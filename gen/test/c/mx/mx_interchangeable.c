/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_interchangeable.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxInterchangeable *mx_interchangeable_parse(xmlNodePtr el) {
    MxInterchangeable *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "symbol") == 0) {
            m->has_symbol = true;
            m->symbol = mx_time_symbol_parse(s);
        } else if (strcmp(aname, "separator") == 0) {
            m->has_separator = true;
            m->separator = mx_time_separator_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_interchangeable_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxInterchangeableChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "time-relation") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->time_relation = malloc(sizeof(*ch->time_relation));
            if (!ch->time_relation)
                abort();
            *ch->time_relation = mx_time_relation_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "beats") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->beats = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "beat-type") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->beat_type = mx_strdup(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_interchangeable_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_interchangeable_serialize(const MxInterchangeable *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_symbol) {
        xmlSetProp(el, BAD_CAST "symbol", BAD_CAST mx_time_symbol_to_string(m->symbol));
    }
    if (m->has_separator) {
        xmlSetProp(el, BAD_CAST "separator", BAD_CAST mx_time_separator_to_string(m->separator));
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxInterchangeableChild *ch = &m->children[i];
        if (ch->time_relation) {
            xmlNewTextChild(el, NULL, BAD_CAST "time-relation", BAD_CAST mx_time_relation_to_string((*ch->time_relation)));
        } else if (ch->beats) {
            xmlNewTextChild(el, NULL, BAD_CAST "beats", BAD_CAST ch->beats);
        } else if (ch->beat_type) {
            xmlNewTextChild(el, NULL, BAD_CAST "beat-type", BAD_CAST ch->beat_type);
        }
    }
    return el;
}

void mx_interchangeable_free(MxInterchangeable *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxInterchangeableChild *ch = &m->children[i];
        free(ch->time_relation);
        free(ch->beats);
        free(ch->beat_type);
    }
    free(m->children);
    free(m);
}
