/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_HARP_PEDALS_H_INCLUDED
#define MX_HARP_PEDALS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_left_center_right.h"
#include "mx_pedal_tuning.h"
#include "mx_tenths.h"
#include "mx_valign.h"

/*
 * The harp-pedals type is used to create harp pedal diagrams. The pedal-step and pedal-alter
 * elements use the same values as the step and alter elements. For easiest reading, the
 * pedal-tuning elements should follow standard harp pedal order, with pedal-step values of D, C, B,
 * E, F, G, and A.
 */
/* One child element of MxHarpPedals: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxPedalTuning *pedal_tuning;
} MxHarpPedalsChild;

typedef struct {
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValign valign; /* attribute valign */
    bool has_id;
    char *id; /* attribute id */
    MxHarpPedalsChild *children; /* child elements in document order */
    size_t children_count;
} MxHarpPedals;

/* NULL on error; the message is in mx_error(). */
MxHarpPedals *mx_harp_pedals_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_harp_pedals_serialize(const MxHarpPedals *m, xmlNodePtr parent, const char *tag);
void mx_harp_pedals_free(MxHarpPedals *m);

#endif /* MX_HARP_PEDALS_H_INCLUDED */
