/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_METRONOME_NOTE_H_INCLUDED
#define MX_METRONOME_NOTE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_metronome_beam.h"
#include "mx_metronome_tied.h"
#include "mx_metronome_tuplet.h"
#include "mx_note_type_value.h"

/*
 * The metronome-note type defines the appearance of a note within a metric relationship mark.
 */
/* One child element of MxMetronomeNote: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxNoteTypeValue *metronome_type;
    MxEmpty *metronome_dot;
    MxMetronomeBeam *metronome_beam;
    MxMetronomeTied *metronome_tied;
    MxMetronomeTuplet *metronome_tuplet;
} MxMetronomeNoteChild;

typedef struct {
    MxMetronomeNoteChild *children; /* child elements in document order */
    size_t children_count;
} MxMetronomeNote;

/* NULL on error; the message is in mx_error(). */
MxMetronomeNote *mx_metronome_note_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_metronome_note_serialize(const MxMetronomeNote *m, xmlNodePtr parent, const char *tag);
void mx_metronome_note_free(MxMetronomeNote *m);

#endif /* MX_METRONOME_NOTE_H_INCLUDED */
