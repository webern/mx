/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SCORE_INSTRUMENT_H_INCLUDED
#define MX_SCORE_INSTRUMENT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_instrument_sound.h"
#include "mx_positive_integer_or_empty.h"
#include "mx_virtual_instrument.h"

/*
 * The score-instrument type represents a single instrument within a score-part. As with the
 * score-part type, each score-instrument has a required ID attribute, a name, and an optional
 * abbreviation. A score-instrument type is also required if the score specifies MIDI 1.0 channels,
 * banks, or programs. An initial midi-instrument assignment can also be made here. MusicXML
 * software should be able to automatically assign reasonable channels and instruments without these
 * elements in simple cases, such as where part names match General MIDI instrument names.
 */
/* One child element of MxScoreInstrument: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    char *instrument_name;
    char *instrument_abbreviation;
    MxInstrumentSound *instrument_sound;
    MxEmpty *solo;
    MxPositiveIntegerOrEmpty *ensemble;
    MxVirtualInstrument *virtual_instrument;
} MxScoreInstrumentChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxScoreInstrumentChild *children; /* child elements in document order */
    size_t children_count;
} MxScoreInstrument;

/* NULL on error; the message is in mx_error(). */
MxScoreInstrument *mx_score_instrument_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_score_instrument_serialize(const MxScoreInstrument *m, xmlNodePtr parent, const char *tag);
void mx_score_instrument_free(MxScoreInstrument *m);

#endif /* MX_SCORE_INSTRUMENT_H_INCLUDED */
