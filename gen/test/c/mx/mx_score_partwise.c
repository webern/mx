/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_score_partwise.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxScorePartwise *mx_score_partwise_parse(xmlNodePtr el) {
    MxScorePartwise *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "version") == 0) {
            m->has_version = true;
            m->version = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_score_partwise_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxScorePartwiseChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "work") == 0) {
            ch->work = mx_work_parse(c);
            if (!ch->work) {
                mx_score_partwise_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "movement-number") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->movement_number = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "movement-title") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->movement_title = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "identification") == 0) {
            ch->identification = mx_identification_parse(c);
            if (!ch->identification) {
                mx_score_partwise_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "defaults") == 0) {
            ch->defaults = mx_defaults_parse(c);
            if (!ch->defaults) {
                mx_score_partwise_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "credit") == 0) {
            ch->credit = mx_credit_parse(c);
            if (!ch->credit) {
                mx_score_partwise_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-list") == 0) {
            ch->part_list = mx_part_list_parse(c);
            if (!ch->part_list) {
                mx_score_partwise_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part") == 0) {
            ch->part = mx_partwise_part_parse(c);
            if (!ch->part) {
                mx_score_partwise_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_score_partwise_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_score_partwise_serialize(const MxScorePartwise *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_version) {
        xmlSetProp(el, BAD_CAST "version", BAD_CAST m->version);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxScorePartwiseChild *ch = &m->children[i];
        if (ch->work) {
            mx_work_serialize(ch->work, el, "work");
        } else if (ch->movement_number) {
            xmlNewTextChild(el, NULL, BAD_CAST "movement-number", BAD_CAST ch->movement_number);
        } else if (ch->movement_title) {
            xmlNewTextChild(el, NULL, BAD_CAST "movement-title", BAD_CAST ch->movement_title);
        } else if (ch->identification) {
            mx_identification_serialize(ch->identification, el, "identification");
        } else if (ch->defaults) {
            mx_defaults_serialize(ch->defaults, el, "defaults");
        } else if (ch->credit) {
            mx_credit_serialize(ch->credit, el, "credit");
        } else if (ch->part_list) {
            mx_part_list_serialize(ch->part_list, el, "part-list");
        } else if (ch->part) {
            mx_partwise_part_serialize(ch->part, el, "part");
        }
    }
    return el;
}

void mx_score_partwise_free(MxScorePartwise *m) {
    if (!m)
        return;
    if (m->has_version)
        free(m->version);
    for (size_t i = 0; i < m->children_count; i++) {
        MxScorePartwiseChild *ch = &m->children[i];
        if (ch->work)
            mx_work_free(ch->work);
        free(ch->movement_number);
        free(ch->movement_title);
        if (ch->identification)
            mx_identification_free(ch->identification);
        if (ch->defaults)
            mx_defaults_free(ch->defaults);
        if (ch->credit)
            mx_credit_free(ch->credit);
        if (ch->part_list)
            mx_part_list_free(ch->part_list);
        if (ch->part)
            mx_partwise_part_free(ch->part);
    }
    free(m->children);
    free(m);
}
