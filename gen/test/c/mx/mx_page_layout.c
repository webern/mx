/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_page_layout.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPageLayout *mx_page_layout_parse(xmlNodePtr el) {
    MxPageLayout *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_page_layout_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPageLayoutChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "page-height") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->page_height = malloc(sizeof(*ch->page_height));
            if (!ch->page_height)
                abort();
            *ch->page_height = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "page-width") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->page_width = malloc(sizeof(*ch->page_width));
            if (!ch->page_width)
                abort();
            *ch->page_width = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "page-margins") == 0) {
            ch->page_margins = mx_page_margins_parse(c);
            if (!ch->page_margins) {
                mx_page_layout_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_page_layout_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_page_layout_serialize(const MxPageLayout *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPageLayoutChild *ch = &m->children[i];
        if (ch->page_height) {
            char *s = mx_tenths_to_string((*ch->page_height));
            xmlNewTextChild(el, NULL, BAD_CAST "page-height", BAD_CAST s);
            free(s);
        } else if (ch->page_width) {
            char *s = mx_tenths_to_string((*ch->page_width));
            xmlNewTextChild(el, NULL, BAD_CAST "page-width", BAD_CAST s);
            free(s);
        } else if (ch->page_margins) {
            mx_page_margins_serialize(ch->page_margins, el, "page-margins");
        }
    }
    return el;
}

void mx_page_layout_free(MxPageLayout *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxPageLayoutChild *ch = &m->children[i];
        free(ch->page_height);
        free(ch->page_width);
        if (ch->page_margins)
            mx_page_margins_free(ch->page_margins);
    }
    free(m->children);
    free(m);
}
