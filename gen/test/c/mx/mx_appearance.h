/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_APPEARANCE_H_INCLUDED
#define MX_APPEARANCE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_distance.h"
#include "mx_glyph.h"
#include "mx_line_width.h"
#include "mx_note_size.h"
#include "mx_other_appearance.h"

/*
 * The appearance type controls general graphical settings for the music's final form appearance on
 * a printed page of display. This includes support for line widths, definitions for note sizes, and
 * standard distances between notation elements, plus an extension element for other aspects of
 * appearance.
 */
/* One child element of MxAppearance: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxLineWidth *line_width;
    MxNoteSize *note_size;
    MxDistance *distance;
    MxGlyph *glyph;
    MxOtherAppearance *other_appearance;
} MxAppearanceChild;

typedef struct {
    MxAppearanceChild *children; /* child elements in document order */
    size_t children_count;
} MxAppearance;

/* NULL on error; the message is in mx_error(). */
MxAppearance *mx_appearance_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_appearance_serialize(const MxAppearance *m, xmlNodePtr parent, const char *tag);
void mx_appearance_free(MxAppearance *m);

#endif /* MX_APPEARANCE_H_INCLUDED */
