/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SYSTEM_MARGINS_H_INCLUDED
#define MX_SYSTEM_MARGINS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_tenths.h"

/*
 * System margins are relative to the page margins. Positive values indent and negative values
 * reduce the margin size.
 */
/* One child element of MxSystemMargins: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTenths *left_margin;
    MxTenths *right_margin;
} MxSystemMarginsChild;

typedef struct {
    MxSystemMarginsChild *children; /* child elements in document order */
    size_t children_count;
} MxSystemMargins;

/* NULL on error; the message is in mx_error(). */
MxSystemMargins *mx_system_margins_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_system_margins_serialize(const MxSystemMargins *m, xmlNodePtr parent, const char *tag);
void mx_system_margins_free(MxSystemMargins *m);

#endif /* MX_SYSTEM_MARGINS_H_INCLUDED */
