/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_bend.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxBend *mx_bend_parse(xmlNodePtr el) {
    MxBend *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "accelerate") == 0) {
            m->has_accelerate = true;
            m->accelerate = mx_yes_no_parse(s);
        } else if (strcmp(aname, "beats") == 0) {
            m->has_beats = true;
            m->beats = mx_trill_beats_parse(s);
        } else if (strcmp(aname, "first-beat") == 0) {
            m->has_first_beat = true;
            m->first_beat = mx_percent_parse(s);
        } else if (strcmp(aname, "last-beat") == 0) {
            m->has_last_beat = true;
            m->last_beat = mx_percent_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_bend_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxBendChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "bend-alter") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->bend_alter = malloc(sizeof(*ch->bend_alter));
            if (!ch->bend_alter)
                abort();
            *ch->bend_alter = mx_semitones_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "pre-bend") == 0) {
            ch->pre_bend = mx_empty_parse(c);
            if (!ch->pre_bend) {
                mx_bend_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "release") == 0) {
            ch->release = mx_empty_parse(c);
            if (!ch->release) {
                mx_bend_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "with-bar") == 0) {
            ch->with_bar = mx_placement_text_parse(c);
            if (!ch->with_bar) {
                mx_bend_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_bend_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_bend_serialize(const MxBend *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_accelerate) {
        xmlSetProp(el, BAD_CAST "accelerate", BAD_CAST mx_yes_no_to_string(m->accelerate));
    }
    if (m->has_beats) {
        char *s = mx_trill_beats_to_string(m->beats);
        xmlSetProp(el, BAD_CAST "beats", BAD_CAST s);
        free(s);
    }
    if (m->has_first_beat) {
        char *s = mx_percent_to_string(m->first_beat);
        xmlSetProp(el, BAD_CAST "first-beat", BAD_CAST s);
        free(s);
    }
    if (m->has_last_beat) {
        char *s = mx_percent_to_string(m->last_beat);
        xmlSetProp(el, BAD_CAST "last-beat", BAD_CAST s);
        free(s);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxBendChild *ch = &m->children[i];
        if (ch->bend_alter) {
            char *s = mx_semitones_to_string((*ch->bend_alter));
            xmlNewTextChild(el, NULL, BAD_CAST "bend-alter", BAD_CAST s);
            free(s);
        } else if (ch->pre_bend) {
            mx_empty_serialize(ch->pre_bend, el, "pre-bend");
        } else if (ch->release) {
            mx_empty_serialize(ch->release, el, "release");
        } else if (ch->with_bar) {
            mx_placement_text_serialize(ch->with_bar, el, "with-bar");
        }
    }
    return el;
}

void mx_bend_free(MxBend *m) {
    if (!m)
        return;
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    for (size_t i = 0; i < m->children_count; i++) {
        MxBendChild *ch = &m->children[i];
        free(ch->bend_alter);
        if (ch->pre_bend)
            mx_empty_free(ch->pre_bend);
        if (ch->release)
            mx_empty_free(ch->release);
        if (ch->with_bar)
            mx_placement_text_free(ch->with_bar);
    }
    free(m->children);
    free(m);
}
