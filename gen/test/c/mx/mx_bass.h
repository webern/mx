/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BASS_H_INCLUDED
#define MX_BASS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_bass_alter.h"
#include "mx_bass_step.h"

/*
 * The bass type is used to indicate a bass note in popular music chord symbols, e.g. G/C. It is
 * generally not used in functional harmony, as inversion is generally not used in pop chord
 * symbols. As with root, it is divided into step and alter elements, similar to pitches.
 */
/* One child element of MxBass: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxBassStep *bass_step;
    MxBassAlter *bass_alter;
} MxBassChild;

typedef struct {
    MxBassChild *children; /* child elements in document order */
    size_t children_count;
} MxBass;

/* NULL on error; the message is in mx_error(). */
MxBass *mx_bass_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_bass_serialize(const MxBass *m, xmlNodePtr parent, const char *tag);
void mx_bass_free(MxBass *m);

#endif /* MX_BASS_H_INCLUDED */
