/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ENCODING_H_INCLUDED
#define MX_ENCODING_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_supports.h"
#include "mx_typed_text.h"
#include "mx_yyyy_mm_dd.h"

/*
 * The encoding element contains information about who did the digital encoding, when, with what
 * software, and in what aspects. Standard type values for the encoder element are music, words, and
 * arrangement, but other types may be used. The type attribute is only needed when there are
 * multiple encoder elements.
 */
/* One child element of MxEncoding: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxYyyyMmDd encoding_date;
    MxTypedText *encoder;
    char *software;
    char *encoding_description;
    MxSupports *supports;
} MxEncodingChild;

typedef struct {
    MxEncodingChild *children; /* child elements in document order */
    size_t children_count;
} MxEncoding;

/* NULL on error; the message is in mx_error(). */
MxEncoding *mx_encoding_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_encoding_serialize(const MxEncoding *m, xmlNodePtr parent, const char *tag);
void mx_encoding_free(MxEncoding *m);

#endif /* MX_ENCODING_H_INCLUDED */
