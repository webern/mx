/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TREMOLO_H_INCLUDED
#define MX_TREMOLO_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_smufl_glyph_name.h"
#include "mx_tenths.h"
#include "mx_tremolo_marks.h"
#include "mx_tremolo_type.h"

/*
 * The tremolo ornament can be used to indicate single-note, double-note, or unmeasured tremolos.
 * Single-note tremolos use the single type, double-note tremolos use the start and stop types, and
 * unmeasured tremolos use the unmeasured type. The default is "single" for compatibility with
 * Version 1.1. The text of the element indicates the number of tremolo marks and is an integer from
 * 0 to 8. Note that the number of attached beams is not included in this value, but is represented
 * separately using the beam element. The value should be 0 for unmeasured tremolos. When using
 * double-note tremolos, the duration of each note in the tremolo should correspond to half of the
 * notated type value. A time-modification element should also be added with an actual-notes value
 * of 2 and a normal-notes value of 1. If used within a tuplet, this 2/1 ratio should be multiplied
 * by the existing tuplet ratio. The smufl attribute specifies the glyph to use from the SMuFL
 * tremolos range for an unmeasured tremolo. It is ignored for other tremolo types. The SMuFL
 * buzzRoll glyph is used by default if the attribute is missing. Using repeater beams for
 * indicating tremolos is deprecated as of MusicXML 3.0.
 */
typedef struct {
    bool has_type;
    MxTremoloType type; /* attribute type */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_smufl;
    MxSMUFLGlyphName smufl; /* attribute smufl */
    MxTremoloMarks value; /* text content */
} MxTremolo;

/* NULL on error; the message is in mx_error(). */
MxTremolo *mx_tremolo_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_tremolo_serialize(const MxTremolo *m, xmlNodePtr parent, const char *tag);
void mx_tremolo_free(MxTremolo *m);

#endif /* MX_TREMOLO_H_INCLUDED */
