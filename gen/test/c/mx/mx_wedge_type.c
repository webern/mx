/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_wedge_type.h"

#include <string.h>

static const char *const mx_wedge_type_values[] = {
    "crescendo",
    "diminuendo",
    "stop",
    "continue",
};

bool mx_wedge_type_try_parse(const char *s, MxWedgeType *out) {
    if (!s)
        s = "";
    for (size_t i = 0; i < sizeof(mx_wedge_type_values) / sizeof(mx_wedge_type_values[0]); i++) {
        if (strcmp(s, mx_wedge_type_values[i]) == 0) {
            *out = (MxWedgeType)i;
            return true;
        }
    }
    *out = (MxWedgeType)0;
    return false;
}

MxWedgeType mx_wedge_type_parse(const char *s) {
    MxWedgeType v;
    mx_wedge_type_try_parse(s, &v);
    return v;
}

const char *mx_wedge_type_to_string(MxWedgeType v) {
    if ((size_t)v >= sizeof(mx_wedge_type_values) / sizeof(mx_wedge_type_values[0]))
        return mx_wedge_type_values[0];
    return mx_wedge_type_values[v];
}
