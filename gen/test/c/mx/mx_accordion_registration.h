/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ACCORDION_REGISTRATION_H_INCLUDED
#define MX_ACCORDION_REGISTRATION_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_accordion_middle.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_empty.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_left_center_right.h"
#include "mx_tenths.h"
#include "mx_valign.h"

/*
 * The accordion-registration type is use for accordion registration symbols. These are circular
 * symbols divided horizontally into high, middle, and low sections that correspond to 4', 8', and
 * 16' pipes. Each accordion-high, accordion-middle, and accordion-low element represents the
 * presence of one or more dots in the registration diagram. An accordion-registration element needs
 * to have at least one of the child elements present.
 */
/* One child element of MxAccordionRegistration: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxEmpty *accordion_high;
    MxAccordionMiddle *accordion_middle;
    MxEmpty *accordion_low;
} MxAccordionRegistrationChild;

typedef struct {
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValign valign; /* attribute valign */
    bool has_id;
    char *id; /* attribute id */
    MxAccordionRegistrationChild *children; /* child elements in document order */
    size_t children_count;
} MxAccordionRegistration;

/* NULL on error; the message is in mx_error(). */
MxAccordionRegistration *mx_accordion_registration_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_accordion_registration_serialize(const MxAccordionRegistration *m, xmlNodePtr parent, const char *tag);
void mx_accordion_registration_free(MxAccordionRegistration *m);

#endif /* MX_ACCORDION_REGISTRATION_H_INCLUDED */
