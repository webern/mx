/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ATTRIBUTES_H_INCLUDED
#define MX_ATTRIBUTES_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_clef.h"
#include "mx_directive.h"
#include "mx_formatted_text.h"
#include "mx_key.h"
#include "mx_level.h"
#include "mx_measure_style.h"
#include "mx_part_symbol.h"
#include "mx_positive_divisions.h"
#include "mx_staff_details.h"
#include "mx_time.h"
#include "mx_transpose.h"

/*
 * The attributes element contains musical information that typically changes on measure boundaries.
 * This includes key and time signatures, clefs, transpositions, and staving. When attributes are
 * changed mid-measure, it affects the music in score order, not in MusicXML document order.
 */
/* One child element of MxAttributes: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxFormattedText *footnote;
    MxLevel *level;
    MxPositiveDivisions *divisions;
    MxKey *key;
    MxTime *time;
    long *staves;
    MxPartSymbol *part_symbol;
    long *instruments;
    MxClef *clef;
    MxStaffDetails *staff_details;
    MxTranspose *transpose;
    MxDirective *directive;
    MxMeasureStyle *measure_style;
} MxAttributesChild;

typedef struct {
    MxAttributesChild *children; /* child elements in document order */
    size_t children_count;
} MxAttributes;

/* NULL on error; the message is in mx_error(). */
MxAttributes *mx_attributes_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_attributes_serialize(const MxAttributes *m, xmlNodePtr parent, const char *tag);
void mx_attributes_free(MxAttributes *m);

#endif /* MX_ATTRIBUTES_H_INCLUDED */
