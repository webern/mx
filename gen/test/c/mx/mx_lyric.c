/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_lyric.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxLyric *mx_lyric_parse(xmlNodePtr el) {
    MxLyric *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_strdup(s);
        } else if (strcmp(aname, "name") == 0) {
            m->has_name = true;
            m->name = mx_strdup(s);
        } else if (strcmp(aname, "time-only") == 0) {
            m->has_time_only = true;
            m->time_only = mx_time_only_parse(s);
        } else if (strcmp(aname, "justify") == 0) {
            m->has_justify = true;
            m->justify = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "placement") == 0) {
            m->has_placement = true;
            m->placement = mx_above_below_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "print-object") == 0) {
            m->has_print_object = true;
            m->print_object = mx_yes_no_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_lyric_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxLyricChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "syllabic") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->syllabic = malloc(sizeof(*ch->syllabic));
            if (!ch->syllabic)
                abort();
            *ch->syllabic = mx_syllabic_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "text") == 0) {
            ch->text = mx_text_element_data_parse(c);
            if (!ch->text) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "elision") == 0) {
            ch->elision = mx_elision_parse(c);
            if (!ch->elision) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "extend") == 0) {
            ch->extend = mx_extend_parse(c);
            if (!ch->extend) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "laughing") == 0) {
            ch->laughing = mx_empty_parse(c);
            if (!ch->laughing) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "humming") == 0) {
            ch->humming = mx_empty_parse(c);
            if (!ch->humming) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "end-line") == 0) {
            ch->end_line = mx_empty_parse(c);
            if (!ch->end_line) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "end-paragraph") == 0) {
            ch->end_paragraph = mx_empty_parse(c);
            if (!ch->end_paragraph) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_lyric_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_lyric_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_lyric_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_lyric_serialize(const MxLyric *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_number) {
        xmlSetProp(el, BAD_CAST "number", BAD_CAST m->number);
    }
    if (m->has_name) {
        xmlSetProp(el, BAD_CAST "name", BAD_CAST m->name);
    }
    if (m->has_time_only) {
        xmlSetProp(el, BAD_CAST "time-only", BAD_CAST m->time_only);
    }
    if (m->has_justify) {
        xmlSetProp(el, BAD_CAST "justify", BAD_CAST mx_left_center_right_to_string(m->justify));
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_placement) {
        xmlSetProp(el, BAD_CAST "placement", BAD_CAST mx_above_below_to_string(m->placement));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_print_object) {
        xmlSetProp(el, BAD_CAST "print-object", BAD_CAST mx_yes_no_to_string(m->print_object));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxLyricChild *ch = &m->children[i];
        if (ch->syllabic) {
            xmlNewTextChild(el, NULL, BAD_CAST "syllabic", BAD_CAST mx_syllabic_to_string((*ch->syllabic)));
        } else if (ch->text) {
            mx_text_element_data_serialize(ch->text, el, "text");
        } else if (ch->elision) {
            mx_elision_serialize(ch->elision, el, "elision");
        } else if (ch->extend) {
            mx_extend_serialize(ch->extend, el, "extend");
        } else if (ch->laughing) {
            mx_empty_serialize(ch->laughing, el, "laughing");
        } else if (ch->humming) {
            mx_empty_serialize(ch->humming, el, "humming");
        } else if (ch->end_line) {
            mx_empty_serialize(ch->end_line, el, "end-line");
        } else if (ch->end_paragraph) {
            mx_empty_serialize(ch->end_paragraph, el, "end-paragraph");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        }
    }
    return el;
}

void mx_lyric_free(MxLyric *m) {
    if (!m)
        return;
    if (m->has_number)
        free(m->number);
    if (m->has_name)
        free(m->name);
    if (m->has_time_only)
        free(m->time_only);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxLyricChild *ch = &m->children[i];
        free(ch->syllabic);
        if (ch->text)
            mx_text_element_data_free(ch->text);
        if (ch->elision)
            mx_elision_free(ch->elision);
        if (ch->extend)
            mx_extend_free(ch->extend);
        if (ch->laughing)
            mx_empty_free(ch->laughing);
        if (ch->humming)
            mx_empty_free(ch->humming);
        if (ch->end_line)
            mx_empty_free(ch->end_line);
        if (ch->end_paragraph)
            mx_empty_free(ch->end_paragraph);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
    }
    free(m->children);
    free(m);
}
