/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_IDENTIFICATION_H_INCLUDED
#define MX_IDENTIFICATION_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_encoding.h"
#include "mx_miscellaneous.h"
#include "mx_typed_text.h"

/*
 * Identification contains basic metadata about the score. It includes the information in MuseData
 * headers that may apply at a score-wide, movement-wide, or part-wide level. The creator, rights,
 * source, and relation elements are based on Dublin Core.
 */
/* One child element of MxIdentification: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTypedText *creator;
    MxTypedText *rights;
    MxEncoding *encoding;
    char *source;
    MxTypedText *relation;
    MxMiscellaneous *miscellaneous;
} MxIdentificationChild;

typedef struct {
    MxIdentificationChild *children; /* child elements in document order */
    size_t children_count;
} MxIdentification;

/* NULL on error; the message is in mx_error(). */
MxIdentification *mx_identification_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_identification_serialize(const MxIdentification *m, xmlNodePtr parent, const char *tag);
void mx_identification_free(MxIdentification *m);

#endif /* MX_IDENTIFICATION_H_INCLUDED */
