/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PEDAL_H_INCLUDED
#define MX_PEDAL_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_left_center_right.h"
#include "mx_number_level.h"
#include "mx_pedal_type.h"
#include "mx_tenths.h"
#include "mx_valign.h"
#include "mx_yes_no.h"

/*
 * The pedal type represents piano pedal marks. In MusicXML 3.1 this includes sostenuto as well as
 * damper pedal marks. The line attribute is yes if pedal lines are used. The sign attribute is yes
 * if Ped, Sost, and * signs are used. For MusicXML 2.0 compatibility, the sign attribute is yes by
 * default if the line attribute is no, and is no by default if the line attribute is yes. If the
 * sign attribute is set to yes and the type is start or sostenuto, the abbreviated attribute is yes
 * if the short P and S signs are used, and no if the full Ped and Sost signs are used. It is no by
 * default. Otherwise the abbreviated attribute is ignored. The change and continue types are used
 * when the line attribute is yes. The change type indicates a pedal lift and retake indicated with
 * an inverted V marking. The continue type allows more precise formatting across system breaks and
 * for more complex pedaling lines. The alignment attributes are ignored if the line attribute is
 * yes.
 */
typedef struct {
    bool has_type;
    MxPedalType type; /* attribute type */
    bool has_number;
    MxNumberLevel number; /* attribute number */
    bool has_line;
    MxYesNo line; /* attribute line */
    bool has_sign;
    MxYesNo sign; /* attribute sign */
    bool has_abbreviated;
    MxYesNo abbreviated; /* attribute abbreviated */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValign valign; /* attribute valign */
    bool has_id;
    char *id; /* attribute id */
} MxPedal;

/* NULL on error; the message is in mx_error(). */
MxPedal *mx_pedal_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_pedal_serialize(const MxPedal *m, xmlNodePtr parent, const char *tag);
void mx_pedal_free(MxPedal *m);

#endif /* MX_PEDAL_H_INCLUDED */
