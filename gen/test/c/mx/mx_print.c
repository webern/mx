/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_print.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPrint *mx_print_parse(xmlNodePtr el) {
    MxPrint *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "staff-spacing") == 0) {
            m->has_staff_spacing = true;
            m->staff_spacing = mx_tenths_parse(s);
        } else if (strcmp(aname, "new-system") == 0) {
            m->has_new_system = true;
            m->new_system = mx_yes_no_parse(s);
        } else if (strcmp(aname, "new-page") == 0) {
            m->has_new_page = true;
            m->new_page = mx_yes_no_parse(s);
        } else if (strcmp(aname, "blank-page") == 0) {
            m->has_blank_page = true;
            m->blank_page = mx_parse_int(s);
        } else if (strcmp(aname, "page-number") == 0) {
            m->has_page_number = true;
            m->page_number = mx_strdup(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_print_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPrintChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "page-layout") == 0) {
            ch->page_layout = mx_page_layout_parse(c);
            if (!ch->page_layout) {
                mx_print_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "system-layout") == 0) {
            ch->system_layout = mx_system_layout_parse(c);
            if (!ch->system_layout) {
                mx_print_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staff-layout") == 0) {
            ch->staff_layout = mx_staff_layout_parse(c);
            if (!ch->staff_layout) {
                mx_print_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "measure-layout") == 0) {
            ch->measure_layout = mx_measure_layout_parse(c);
            if (!ch->measure_layout) {
                mx_print_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "measure-numbering") == 0) {
            ch->measure_numbering = mx_measure_numbering_parse(c);
            if (!ch->measure_numbering) {
                mx_print_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-name-display") == 0) {
            ch->part_name_display = mx_name_display_parse(c);
            if (!ch->part_name_display) {
                mx_print_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "part-abbreviation-display") == 0) {
            ch->part_abbreviation_display = mx_name_display_parse(c);
            if (!ch->part_abbreviation_display) {
                mx_print_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_print_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_print_serialize(const MxPrint *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_staff_spacing) {
        char *s = mx_tenths_to_string(m->staff_spacing);
        xmlSetProp(el, BAD_CAST "staff-spacing", BAD_CAST s);
        free(s);
    }
    if (m->has_new_system) {
        xmlSetProp(el, BAD_CAST "new-system", BAD_CAST mx_yes_no_to_string(m->new_system));
    }
    if (m->has_new_page) {
        xmlSetProp(el, BAD_CAST "new-page", BAD_CAST mx_yes_no_to_string(m->new_page));
    }
    if (m->has_blank_page) {
        char *s = mx_format_int(m->blank_page);
        xmlSetProp(el, BAD_CAST "blank-page", BAD_CAST s);
        free(s);
    }
    if (m->has_page_number) {
        xmlSetProp(el, BAD_CAST "page-number", BAD_CAST m->page_number);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPrintChild *ch = &m->children[i];
        if (ch->page_layout) {
            mx_page_layout_serialize(ch->page_layout, el, "page-layout");
        } else if (ch->system_layout) {
            mx_system_layout_serialize(ch->system_layout, el, "system-layout");
        } else if (ch->staff_layout) {
            mx_staff_layout_serialize(ch->staff_layout, el, "staff-layout");
        } else if (ch->measure_layout) {
            mx_measure_layout_serialize(ch->measure_layout, el, "measure-layout");
        } else if (ch->measure_numbering) {
            mx_measure_numbering_serialize(ch->measure_numbering, el, "measure-numbering");
        } else if (ch->part_name_display) {
            mx_name_display_serialize(ch->part_name_display, el, "part-name-display");
        } else if (ch->part_abbreviation_display) {
            mx_name_display_serialize(ch->part_abbreviation_display, el, "part-abbreviation-display");
        }
    }
    return el;
}

void mx_print_free(MxPrint *m) {
    if (!m)
        return;
    if (m->has_page_number)
        free(m->page_number);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxPrintChild *ch = &m->children[i];
        if (ch->page_layout)
            mx_page_layout_free(ch->page_layout);
        if (ch->system_layout)
            mx_system_layout_free(ch->system_layout);
        if (ch->staff_layout)
            mx_staff_layout_free(ch->staff_layout);
        if (ch->measure_layout)
            mx_measure_layout_free(ch->measure_layout);
        if (ch->measure_numbering)
            mx_measure_numbering_free(ch->measure_numbering);
        if (ch->part_name_display)
            mx_name_display_free(ch->part_name_display);
        if (ch->part_abbreviation_display)
            mx_name_display_free(ch->part_abbreviation_display);
    }
    free(m->children);
    free(m);
}
