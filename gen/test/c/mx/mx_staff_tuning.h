/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_STAFF_TUNING_H_INCLUDED
#define MX_STAFF_TUNING_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_octave.h"
#include "mx_semitones.h"
#include "mx_staff_line.h"
#include "mx_step.h"

/*
 * The staff-tuning type specifies the open, non-capo tuning of the lines on a tablature staff.
 */
/* One child element of MxStaffTuning: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStep *tuning_step;
    MxSemitones *tuning_alter;
    MxOctave *tuning_octave;
} MxStaffTuningChild;

typedef struct {
    bool has_line;
    MxStaffLine line; /* attribute line */
    MxStaffTuningChild *children; /* child elements in document order */
    size_t children_count;
} MxStaffTuning;

/* NULL on error; the message is in mx_error(). */
MxStaffTuning *mx_staff_tuning_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_staff_tuning_serialize(const MxStaffTuning *m, xmlNodePtr parent, const char *tag);
void mx_staff_tuning_free(MxStaffTuning *m);

#endif /* MX_STAFF_TUNING_H_INCLUDED */
