/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_miscellaneous_field.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMiscellaneousField *mx_miscellaneous_field_parse(xmlNodePtr el) {
    MxMiscellaneousField *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "name") == 0) {
            m->has_name = true;
            m->name = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_miscellaneous_field_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    {
        xmlChar *text = xmlNodeGetContent(el);
        const char *s = text ? (const char *)text : "";
        m->value = mx_strdup(s);
        xmlFree(text);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_miscellaneous_field_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_miscellaneous_field_serialize(const MxMiscellaneousField *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    xmlAddChild(el, xmlNewText(BAD_CAST m->value));
    if (m->has_name) {
        xmlSetProp(el, BAD_CAST "name", BAD_CAST m->name);
    }
    return el;
}

void mx_miscellaneous_field_free(MxMiscellaneousField *m) {
    if (!m)
        return;
    if (m->has_name)
        free(m->name);
    free(m->value);
    free(m);
}
