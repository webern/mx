/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_barline.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxBarline *mx_barline_parse(xmlNodePtr el) {
    MxBarline *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "location") == 0) {
            m->has_location = true;
            m->location = mx_right_left_middle_parse(s);
        } else if (strcmp(aname, "segno") == 0) {
            m->has_segno_sound = true;
            m->segno_sound = mx_strdup(s);
        } else if (strcmp(aname, "coda") == 0) {
            m->has_coda_sound = true;
            m->coda_sound = mx_strdup(s);
        } else if (strcmp(aname, "divisions") == 0) {
            m->has_divisions = true;
            m->divisions = mx_divisions_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_barline_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxBarlineChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "bar-style") == 0) {
            ch->bar_style = mx_bar_style_color_parse(c);
            if (!ch->bar_style) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "wavy-line") == 0) {
            ch->wavy_line = mx_wavy_line_parse(c);
            if (!ch->wavy_line) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "segno") == 0) {
            ch->segno = mx_segno_parse(c);
            if (!ch->segno) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "coda") == 0) {
            ch->coda = mx_coda_parse(c);
            if (!ch->coda) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fermata") == 0) {
            ch->fermata = mx_fermata_parse(c);
            if (!ch->fermata) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ending") == 0) {
            ch->ending = mx_ending_parse(c);
            if (!ch->ending) {
                mx_barline_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "repeat") == 0) {
            ch->repeat = mx_repeat_parse(c);
            if (!ch->repeat) {
                mx_barline_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_barline_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_barline_serialize(const MxBarline *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_location) {
        xmlSetProp(el, BAD_CAST "location", BAD_CAST mx_right_left_middle_to_string(m->location));
    }
    if (m->has_segno_sound) {
        xmlSetProp(el, BAD_CAST "segno", BAD_CAST m->segno_sound);
    }
    if (m->has_coda_sound) {
        xmlSetProp(el, BAD_CAST "coda", BAD_CAST m->coda_sound);
    }
    if (m->has_divisions) {
        char *s = mx_divisions_to_string(m->divisions);
        xmlSetProp(el, BAD_CAST "divisions", BAD_CAST s);
        free(s);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxBarlineChild *ch = &m->children[i];
        if (ch->bar_style) {
            mx_bar_style_color_serialize(ch->bar_style, el, "bar-style");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        } else if (ch->wavy_line) {
            mx_wavy_line_serialize(ch->wavy_line, el, "wavy-line");
        } else if (ch->segno) {
            mx_segno_serialize(ch->segno, el, "segno");
        } else if (ch->coda) {
            mx_coda_serialize(ch->coda, el, "coda");
        } else if (ch->fermata) {
            mx_fermata_serialize(ch->fermata, el, "fermata");
        } else if (ch->ending) {
            mx_ending_serialize(ch->ending, el, "ending");
        } else if (ch->repeat) {
            mx_repeat_serialize(ch->repeat, el, "repeat");
        }
    }
    return el;
}

void mx_barline_free(MxBarline *m) {
    if (!m)
        return;
    if (m->has_segno_sound)
        free(m->segno_sound);
    if (m->has_coda_sound)
        free(m->coda_sound);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxBarlineChild *ch = &m->children[i];
        if (ch->bar_style)
            mx_bar_style_color_free(ch->bar_style);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
        if (ch->wavy_line)
            mx_wavy_line_free(ch->wavy_line);
        if (ch->segno)
            mx_segno_free(ch->segno);
        if (ch->coda)
            mx_coda_free(ch->coda);
        if (ch->fermata)
            mx_fermata_free(ch->fermata);
        if (ch->ending)
            mx_ending_free(ch->ending);
        if (ch->repeat)
            mx_repeat_free(ch->repeat);
    }
    free(m->children);
    free(m);
}
