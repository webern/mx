/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_encoding.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxEncoding *mx_encoding_parse(xmlNodePtr el) {
    MxEncoding *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_encoding_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxEncodingChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "encoding-date") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->encoding_date = mx_yyyy_mm_dd_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "encoder") == 0) {
            ch->encoder = mx_typed_text_parse(c);
            if (!ch->encoder) {
                mx_encoding_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "software") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->software = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "encoding-description") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->encoding_description = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "supports") == 0) {
            ch->supports = mx_supports_parse(c);
            if (!ch->supports) {
                mx_encoding_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_encoding_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_encoding_serialize(const MxEncoding *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxEncodingChild *ch = &m->children[i];
        if (ch->encoding_date) {
            xmlNewTextChild(el, NULL, BAD_CAST "encoding-date", BAD_CAST ch->encoding_date);
        } else if (ch->encoder) {
            mx_typed_text_serialize(ch->encoder, el, "encoder");
        } else if (ch->software) {
            xmlNewTextChild(el, NULL, BAD_CAST "software", BAD_CAST ch->software);
        } else if (ch->encoding_description) {
            xmlNewTextChild(el, NULL, BAD_CAST "encoding-description", BAD_CAST ch->encoding_description);
        } else if (ch->supports) {
            mx_supports_serialize(ch->supports, el, "supports");
        }
    }
    return el;
}

void mx_encoding_free(MxEncoding *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxEncodingChild *ch = &m->children[i];
        free(ch->encoding_date);
        if (ch->encoder)
            mx_typed_text_free(ch->encoder);
        free(ch->software);
        free(ch->encoding_description);
        if (ch->supports)
            mx_supports_free(ch->supports);
    }
    free(m->children);
    free(m);
}
