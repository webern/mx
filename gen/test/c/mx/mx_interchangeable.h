/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_INTERCHANGEABLE_H_INCLUDED
#define MX_INTERCHANGEABLE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_time_relation.h"
#include "mx_time_separator.h"
#include "mx_time_symbol.h"

/*
 * The interchangeable type is used to represent the second in a pair of interchangeable dual time
 * signatures, such as the 6/8 in 3/4 (6/8). A separate symbol attribute value is available compared
 * to the time element's symbol attribute, which applies to the first of the dual time signatures.
 */
/* One child element of MxInterchangeable: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTimeRelation *time_relation;
    char *beats;
    char *beat_type;
} MxInterchangeableChild;

typedef struct {
    bool has_symbol;
    MxTimeSymbol symbol; /* attribute symbol */
    bool has_separator;
    MxTimeSeparator separator; /* attribute separator */
    MxInterchangeableChild *children; /* child elements in document order */
    size_t children_count;
} MxInterchangeable;

/* NULL on error; the message is in mx_error(). */
MxInterchangeable *mx_interchangeable_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_interchangeable_serialize(const MxInterchangeable *m, xmlNodePtr parent, const char *tag);
void mx_interchangeable_free(MxInterchangeable *m);

#endif /* MX_INTERCHANGEABLE_H_INCLUDED */
