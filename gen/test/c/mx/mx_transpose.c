/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_transpose.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxTranspose *mx_transpose_parse(xmlNodePtr el) {
    MxTranspose *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_staff_number_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_transpose_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxTransposeChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "diatonic") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->diatonic = malloc(sizeof(*ch->diatonic));
            if (!ch->diatonic)
                abort();
            *ch->diatonic = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "chromatic") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->chromatic = malloc(sizeof(*ch->chromatic));
            if (!ch->chromatic)
                abort();
            *ch->chromatic = mx_semitones_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "octave-change") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->octave_change = malloc(sizeof(*ch->octave_change));
            if (!ch->octave_change)
                abort();
            *ch->octave_change = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "double") == 0) {
            ch->double_ = mx_empty_parse(c);
            if (!ch->double_) {
                mx_transpose_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_transpose_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_transpose_serialize(const MxTranspose *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_number) {
        char *s = mx_staff_number_to_string(m->number);
        xmlSetProp(el, BAD_CAST "number", BAD_CAST s);
        free(s);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxTransposeChild *ch = &m->children[i];
        if (ch->diatonic) {
            char *s = mx_format_int((*ch->diatonic));
            xmlNewTextChild(el, NULL, BAD_CAST "diatonic", BAD_CAST s);
            free(s);
        } else if (ch->chromatic) {
            char *s = mx_semitones_to_string((*ch->chromatic));
            xmlNewTextChild(el, NULL, BAD_CAST "chromatic", BAD_CAST s);
            free(s);
        } else if (ch->octave_change) {
            char *s = mx_format_int((*ch->octave_change));
            xmlNewTextChild(el, NULL, BAD_CAST "octave-change", BAD_CAST s);
            free(s);
        } else if (ch->double_) {
            mx_empty_serialize(ch->double_, el, "double");
        }
    }
    return el;
}

void mx_transpose_free(MxTranspose *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxTransposeChild *ch = &m->children[i];
        free(ch->diatonic);
        free(ch->chromatic);
        free(ch->octave_change);
        if (ch->double_)
            mx_empty_free(ch->double_);
    }
    free(m->children);
    free(m);
}
