/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BEND_H_INCLUDED
#define MX_BEND_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_empty.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_percent.h"
#include "mx_placement_text.h"
#include "mx_semitones.h"
#include "mx_tenths.h"
#include "mx_trill_beats.h"
#include "mx_yes_no.h"

/*
 * The bend type is used in guitar and tablature. The bend-alter element indicates the number of
 * steps in the bend, similar to the alter element. As with the alter element, numbers like 0.5 can
 * be used to indicate microtones. Negative numbers indicate pre-bends or releases; the pre-bend and
 * release elements are used to distinguish what is intended. A with-bar element indicates that the
 * bend is to be done at the bridge with a whammy or vibrato bar. The content of the element
 * indicates how this should be notated.
 */
/* One child element of MxBend: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxSemitones *bend_alter;
    MxEmpty *pre_bend;
    MxEmpty *release;
    MxPlacementText *with_bar;
} MxBendChild;

typedef struct {
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_accelerate;
    MxYesNo accelerate; /* attribute accelerate */
    bool has_beats;
    MxTrillBeats beats; /* attribute beats */
    bool has_first_beat;
    MxPercent first_beat; /* attribute first-beat */
    bool has_last_beat;
    MxPercent last_beat; /* attribute last-beat */
    MxBendChild *children; /* child elements in document order */
    size_t children_count;
} MxBend;

/* NULL on error; the message is in mx_error(). */
MxBend *mx_bend_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_bend_serialize(const MxBend *m, xmlNodePtr parent, const char *tag);
void mx_bend_free(MxBend *m);

#endif /* MX_BEND_H_INCLUDED */
