/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_FRAME_H_INCLUDED
#define MX_FRAME_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_first_fret.h"
#include "mx_frame_note.h"
#include "mx_left_center_right.h"
#include "mx_tenths.h"
#include "mx_valign_image.h"

/*
 * The frame type represents a frame or fretboard diagram used together with a chord symbol. The
 * representation is based on the NIFF guitar grid with additional information. The frame type's
 * unplayed attribute indicates what to display above a string that has no associated frame-note
 * element. Typical values are x and the empty string. If the attribute is not present, the display
 * of the unplayed string is application-defined.
 */
/* One child element of MxFrame: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    long *frame_strings;
    long *frame_frets;
    MxFirstFret *first_fret;
    MxFrameNote *frame_note;
} MxFrameChild;

typedef struct {
    bool has_height;
    MxTenths height; /* attribute height */
    bool has_width;
    MxTenths width; /* attribute width */
    bool has_unplayed;
    char *unplayed; /* attribute unplayed */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValignImage valign; /* attribute valign */
    bool has_id;
    char *id; /* attribute id */
    MxFrameChild *children; /* child elements in document order */
    size_t children_count;
} MxFrame;

/* NULL on error; the message is in mx_error(). */
MxFrame *mx_frame_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_frame_serialize(const MxFrame *m, xmlNodePtr parent, const char *tag);
void mx_frame_free(MxFrame *m);

#endif /* MX_FRAME_H_INCLUDED */
