/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_STICK_H_INCLUDED
#define MX_STICK_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_stick_material.h"
#include "mx_stick_type.h"
#include "mx_tip_direction.h"
#include "mx_yes_no.h"

/*
 * The stick type represents pictograms where the material of the stick, mallet, or beater is
 * included.The parentheses and dashed-circle attributes indicate the presence of these marks around
 * the round beater part of a pictogram. Values for these attributes are "no" if not present.
 */
/* One child element of MxStick: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStickType *stick_type;
    MxStickMaterial *stick_material;
} MxStickChild;

typedef struct {
    bool has_tip;
    MxTipDirection tip; /* attribute tip */
    bool has_parentheses;
    MxYesNo parentheses; /* attribute parentheses */
    bool has_dashed_circle;
    MxYesNo dashed_circle; /* attribute dashed-circle */
    MxStickChild *children; /* child elements in document order */
    size_t children_count;
} MxStick;

/* NULL on error; the message is in mx_error(). */
MxStick *mx_stick_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_stick_serialize(const MxStick *m, xmlNodePtr parent, const char *tag);
void mx_stick_free(MxStick *m);

#endif /* MX_STICK_H_INCLUDED */
