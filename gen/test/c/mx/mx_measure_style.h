/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_MEASURE_STYLE_H_INCLUDED
#define MX_MEASURE_STYLE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_beat_repeat.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_measure_repeat.h"
#include "mx_multiple_rest.h"
#include "mx_slash.h"
#include "mx_staff_number.h"

/*
 * A measure-style indicates a special way to print partial to multiple measures within a part. This
 * includes multiple rests over several measures, repeats of beats, single, or multiple measures,
 * and use of slash notation. The multiple-rest and measure-repeat symbols indicate the number of
 * measures covered in the element content. The beat-repeat and slash elements can cover partial
 * measures. All but the multiple-rest element use a type attribute to indicate starting and
 * stopping the use of the style. The optional number attribute specifies the staff number from top
 * to bottom on the system, as with clef.
 */
/* One child element of MxMeasureStyle: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxMultipleRest *multiple_rest;
    MxMeasureRepeat *measure_repeat;
    MxBeatRepeat *beat_repeat;
    MxSlash *slash;
} MxMeasureStyleChild;

typedef struct {
    bool has_number;
    MxStaffNumber number; /* attribute number */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_id;
    char *id; /* attribute id */
    MxMeasureStyleChild *children; /* child elements in document order */
    size_t children_count;
} MxMeasureStyle;

/* NULL on error; the message is in mx_error(). */
MxMeasureStyle *mx_measure_style_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_measure_style_serialize(const MxMeasureStyle *m, xmlNodePtr parent, const char *tag);
void mx_measure_style_free(MxMeasureStyle *m);

#endif /* MX_MEASURE_STYLE_H_INCLUDED */
