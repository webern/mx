/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_ornaments.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxOrnaments *mx_ornaments_parse(xmlNodePtr el) {
    MxOrnaments *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_ornaments_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxOrnamentsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "trill-mark") == 0) {
            ch->trill_mark = mx_empty_trill_sound_parse(c);
            if (!ch->trill_mark) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "turn") == 0) {
            ch->turn = mx_horizontal_turn_parse(c);
            if (!ch->turn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "delayed-turn") == 0) {
            ch->delayed_turn = mx_horizontal_turn_parse(c);
            if (!ch->delayed_turn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "inverted-turn") == 0) {
            ch->inverted_turn = mx_horizontal_turn_parse(c);
            if (!ch->inverted_turn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "delayed-inverted-turn") == 0) {
            ch->delayed_inverted_turn = mx_horizontal_turn_parse(c);
            if (!ch->delayed_inverted_turn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "vertical-turn") == 0) {
            ch->vertical_turn = mx_empty_trill_sound_parse(c);
            if (!ch->vertical_turn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "inverted-vertical-turn") == 0) {
            ch->inverted_vertical_turn = mx_empty_trill_sound_parse(c);
            if (!ch->inverted_vertical_turn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "shake") == 0) {
            ch->shake = mx_empty_trill_sound_parse(c);
            if (!ch->shake) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "wavy-line") == 0) {
            ch->wavy_line = mx_wavy_line_parse(c);
            if (!ch->wavy_line) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "mordent") == 0) {
            ch->mordent = mx_mordent_parse(c);
            if (!ch->mordent) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "inverted-mordent") == 0) {
            ch->inverted_mordent = mx_mordent_parse(c);
            if (!ch->inverted_mordent) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "schleifer") == 0) {
            ch->schleifer = mx_empty_placement_parse(c);
            if (!ch->schleifer) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tremolo") == 0) {
            ch->tremolo = mx_tremolo_parse(c);
            if (!ch->tremolo) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "haydn") == 0) {
            ch->haydn = mx_empty_trill_sound_parse(c);
            if (!ch->haydn) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-ornament") == 0) {
            ch->other_ornament = mx_other_placement_text_parse(c);
            if (!ch->other_ornament) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "accidental-mark") == 0) {
            ch->accidental_mark = mx_accidental_mark_parse(c);
            if (!ch->accidental_mark) {
                mx_ornaments_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_ornaments_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_ornaments_serialize(const MxOrnaments *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxOrnamentsChild *ch = &m->children[i];
        if (ch->trill_mark) {
            mx_empty_trill_sound_serialize(ch->trill_mark, el, "trill-mark");
        } else if (ch->turn) {
            mx_horizontal_turn_serialize(ch->turn, el, "turn");
        } else if (ch->delayed_turn) {
            mx_horizontal_turn_serialize(ch->delayed_turn, el, "delayed-turn");
        } else if (ch->inverted_turn) {
            mx_horizontal_turn_serialize(ch->inverted_turn, el, "inverted-turn");
        } else if (ch->delayed_inverted_turn) {
            mx_horizontal_turn_serialize(ch->delayed_inverted_turn, el, "delayed-inverted-turn");
        } else if (ch->vertical_turn) {
            mx_empty_trill_sound_serialize(ch->vertical_turn, el, "vertical-turn");
        } else if (ch->inverted_vertical_turn) {
            mx_empty_trill_sound_serialize(ch->inverted_vertical_turn, el, "inverted-vertical-turn");
        } else if (ch->shake) {
            mx_empty_trill_sound_serialize(ch->shake, el, "shake");
        } else if (ch->wavy_line) {
            mx_wavy_line_serialize(ch->wavy_line, el, "wavy-line");
        } else if (ch->mordent) {
            mx_mordent_serialize(ch->mordent, el, "mordent");
        } else if (ch->inverted_mordent) {
            mx_mordent_serialize(ch->inverted_mordent, el, "inverted-mordent");
        } else if (ch->schleifer) {
            mx_empty_placement_serialize(ch->schleifer, el, "schleifer");
        } else if (ch->tremolo) {
            mx_tremolo_serialize(ch->tremolo, el, "tremolo");
        } else if (ch->haydn) {
            mx_empty_trill_sound_serialize(ch->haydn, el, "haydn");
        } else if (ch->other_ornament) {
            mx_other_placement_text_serialize(ch->other_ornament, el, "other-ornament");
        } else if (ch->accidental_mark) {
            mx_accidental_mark_serialize(ch->accidental_mark, el, "accidental-mark");
        }
    }
    return el;
}

void mx_ornaments_free(MxOrnaments *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxOrnamentsChild *ch = &m->children[i];
        if (ch->trill_mark)
            mx_empty_trill_sound_free(ch->trill_mark);
        if (ch->turn)
            mx_horizontal_turn_free(ch->turn);
        if (ch->delayed_turn)
            mx_horizontal_turn_free(ch->delayed_turn);
        if (ch->inverted_turn)
            mx_horizontal_turn_free(ch->inverted_turn);
        if (ch->delayed_inverted_turn)
            mx_horizontal_turn_free(ch->delayed_inverted_turn);
        if (ch->vertical_turn)
            mx_empty_trill_sound_free(ch->vertical_turn);
        if (ch->inverted_vertical_turn)
            mx_empty_trill_sound_free(ch->inverted_vertical_turn);
        if (ch->shake)
            mx_empty_trill_sound_free(ch->shake);
        if (ch->wavy_line)
            mx_wavy_line_free(ch->wavy_line);
        if (ch->mordent)
            mx_mordent_free(ch->mordent);
        if (ch->inverted_mordent)
            mx_mordent_free(ch->inverted_mordent);
        if (ch->schleifer)
            mx_empty_placement_free(ch->schleifer);
        if (ch->tremolo)
            mx_tremolo_free(ch->tremolo);
        if (ch->haydn)
            mx_empty_trill_sound_free(ch->haydn);
        if (ch->other_ornament)
            mx_other_placement_text_free(ch->other_ornament);
        if (ch->accidental_mark)
            mx_accidental_mark_free(ch->accidental_mark);
    }
    free(m->children);
    free(m);
}
