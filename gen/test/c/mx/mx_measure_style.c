/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_measure_style.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMeasureStyle *mx_measure_style_parse(xmlNodePtr el) {
    MxMeasureStyle *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_staff_number_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_measure_style_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxMeasureStyleChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "multiple-rest") == 0) {
            ch->multiple_rest = mx_multiple_rest_parse(c);
            if (!ch->multiple_rest) {
                mx_measure_style_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "measure-repeat") == 0) {
            ch->measure_repeat = mx_measure_repeat_parse(c);
            if (!ch->measure_repeat) {
                mx_measure_style_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "beat-repeat") == 0) {
            ch->beat_repeat = mx_beat_repeat_parse(c);
            if (!ch->beat_repeat) {
                mx_measure_style_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "slash") == 0) {
            ch->slash = mx_slash_parse(c);
            if (!ch->slash) {
                mx_measure_style_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_measure_style_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_measure_style_serialize(const MxMeasureStyle *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_number) {
        char *s = mx_staff_number_to_string(m->number);
        xmlSetProp(el, BAD_CAST "number", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxMeasureStyleChild *ch = &m->children[i];
        if (ch->multiple_rest) {
            mx_multiple_rest_serialize(ch->multiple_rest, el, "multiple-rest");
        } else if (ch->measure_repeat) {
            mx_measure_repeat_serialize(ch->measure_repeat, el, "measure-repeat");
        } else if (ch->beat_repeat) {
            mx_beat_repeat_serialize(ch->beat_repeat, el, "beat-repeat");
        } else if (ch->slash) {
            mx_slash_serialize(ch->slash, el, "slash");
        }
    }
    return el;
}

void mx_measure_style_free(MxMeasureStyle *m) {
    if (!m)
        return;
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxMeasureStyleChild *ch = &m->children[i];
        if (ch->multiple_rest)
            mx_multiple_rest_free(ch->multiple_rest);
        if (ch->measure_repeat)
            mx_measure_repeat_free(ch->measure_repeat);
        if (ch->beat_repeat)
            mx_beat_repeat_free(ch->beat_repeat);
        if (ch->slash)
            mx_slash_free(ch->slash);
    }
    free(m->children);
    free(m);
}
