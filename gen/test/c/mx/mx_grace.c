/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_grace.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxGrace *mx_grace_parse(xmlNodePtr el) {
    MxGrace *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "steal-time-previous") == 0) {
            m->has_steal_time_previous = true;
            m->steal_time_previous = mx_percent_parse(s);
        } else if (strcmp(aname, "steal-time-following") == 0) {
            m->has_steal_time_following = true;
            m->steal_time_following = mx_percent_parse(s);
        } else if (strcmp(aname, "make-time") == 0) {
            m->has_make_time = true;
            m->make_time = mx_divisions_parse(s);
        } else if (strcmp(aname, "slash") == 0) {
            m->has_slash = true;
            m->slash = mx_yes_no_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_grace_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_grace_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_grace_serialize(const MxGrace *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_steal_time_previous) {
        char *s = mx_percent_to_string(m->steal_time_previous);
        xmlSetProp(el, BAD_CAST "steal-time-previous", BAD_CAST s);
        free(s);
    }
    if (m->has_steal_time_following) {
        char *s = mx_percent_to_string(m->steal_time_following);
        xmlSetProp(el, BAD_CAST "steal-time-following", BAD_CAST s);
        free(s);
    }
    if (m->has_make_time) {
        char *s = mx_divisions_to_string(m->make_time);
        xmlSetProp(el, BAD_CAST "make-time", BAD_CAST s);
        free(s);
    }
    if (m->has_slash) {
        xmlSetProp(el, BAD_CAST "slash", BAD_CAST mx_yes_no_to_string(m->slash));
    }
    return el;
}

void mx_grace_free(MxGrace *m) {
    if (!m)
        return;
    free(m);
}
