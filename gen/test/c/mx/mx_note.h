/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_NOTE_H_INCLUDED
#define MX_NOTE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_accidental.h"
#include "mx_beam.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_divisions.h"
#include "mx_empty.h"
#include "mx_empty_placement.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_formatted_text.h"
#include "mx_grace.h"
#include "mx_instrument.h"
#include "mx_level.h"
#include "mx_lyric.h"
#include "mx_non_negative_decimal.h"
#include "mx_notations.h"
#include "mx_note_type.h"
#include "mx_notehead.h"
#include "mx_notehead_text.h"
#include "mx_pitch.h"
#include "mx_play.h"
#include "mx_positive_divisions.h"
#include "mx_rest.h"
#include "mx_stem.h"
#include "mx_tenths.h"
#include "mx_tie.h"
#include "mx_time_modification.h"
#include "mx_time_only.h"
#include "mx_unpitched.h"
#include "mx_yes_no.h"

/*
 * Notes are the most common type of MusicXML data. The MusicXML format keeps the MuseData
 * distinction between elements used for sound information and elements used for notation
 * information (e.g., tie is used for sound, tied for notation). Thus grace notes do not have a
 * duration element. Cue notes have a duration element, as do forward elements, but no tie elements.
 * Having these two types of information available can make interchange considerably easier, as some
 * programs handle one type of information much more readily than the other. The print-leger
 * attribute is used to indicate whether leger lines are printed. Notes without leger lines are used
 * to indicate indeterminate high and low notes. By default, it is set to yes. If print-object is
 * set to no, print-leger is interpreted to also be set to no if not present. This attribute is
 * ignored for rests. The dynamics and end-dynamics attributes correspond to MIDI 1.0's Note On and
 * Note Off velocities, respectively. They are expressed in terms of percentages of the default
 * forte value (90 for MIDI 1.0). The attack and release attributes are used to alter the starting
 * and stopping time of the note from when it would otherwise occur based on the flow of durations -
 * information that is specific to a performance. They are expressed in terms of divisions, either
 * positive or negative. A note that starts a tie should not have a release attribute, and a note
 * that stops a tie should not have an attack attribute. The attack and release attributes are
 * independent of each other. The attack attribute only changes the starting time of a note, and the
 * release attribute only changes the stopping time of a note. If a note is played only particular
 * times through a repeat, the time-only attribute shows which times to play the note. The pizzicato
 * attribute is used when just this note is sounded pizzicato, vs. the pizzicato element which
 * changes overall playback between pizzicato and arco.
 */
/* One child element of MxNote: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxGrace *grace;
    MxEmpty *chord;
    MxPitch *pitch;
    MxUnpitched *unpitched;
    MxRest *rest;
    MxTie *tie;
    MxEmpty *cue;
    MxPositiveDivisions *duration;
    MxInstrument *instrument;
    MxFormattedText *footnote;
    MxLevel *level;
    char *voice;
    MxNoteType *type;
    MxEmptyPlacement *dot;
    MxAccidental *accidental;
    MxTimeModification *time_modification;
    MxStem *stem;
    MxNotehead *notehead;
    MxNoteheadText *notehead_text;
    long *staff;
    MxBeam *beam;
    MxNotations *notations;
    MxLyric *lyric;
    MxPlay *play;
} MxNoteChild;

typedef struct {
    bool has_print_leger;
    MxYesNo print_leger; /* attribute print-leger */
    bool has_dynamics;
    MxNonNegativeDecimal dynamics; /* attribute dynamics */
    bool has_end_dynamics;
    MxNonNegativeDecimal end_dynamics; /* attribute end-dynamics */
    bool has_attack;
    MxDivisions attack; /* attribute attack */
    bool has_release;
    MxDivisions release; /* attribute release */
    bool has_time_only;
    MxTimeOnly time_only; /* attribute time-only */
    bool has_pizzicato;
    MxYesNo pizzicato; /* attribute pizzicato */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_print_dot;
    MxYesNo print_dot; /* attribute print-dot */
    bool has_print_lyric;
    MxYesNo print_lyric; /* attribute print-lyric */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_print_spacing;
    MxYesNo print_spacing; /* attribute print-spacing */
    bool has_id;
    char *id; /* attribute id */
    MxNoteChild *children; /* child elements in document order */
    size_t children_count;
} MxNote;

/* NULL on error; the message is in mx_error(). */
MxNote *mx_note_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_note_serialize(const MxNote *m, xmlNodePtr parent, const char *tag);
void mx_note_free(MxNote *m);

#endif /* MX_NOTE_H_INCLUDED */
