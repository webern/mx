/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PAGE_MARGINS_H_INCLUDED
#define MX_PAGE_MARGINS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_margin_type.h"
#include "mx_tenths.h"

/*
 * Page margins are specified either for both even and odd pages, or via separate odd and even page
 * number values. The type attribute is not needed when used as part of a print element. If omitted
 * when the page-margins type is used in the defaults element, "both" is the default value.
 */
/* One child element of MxPageMargins: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTenths *left_margin;
    MxTenths *right_margin;
    MxTenths *top_margin;
    MxTenths *bottom_margin;
} MxPageMarginsChild;

typedef struct {
    bool has_type;
    MxMarginType type; /* attribute type */
    MxPageMarginsChild *children; /* child elements in document order */
    size_t children_count;
} MxPageMargins;

/* NULL on error; the message is in mx_error(). */
MxPageMargins *mx_page_margins_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_page_margins_serialize(const MxPageMargins *m, xmlNodePtr parent, const char *tag);
void mx_page_margins_free(MxPageMargins *m);

#endif /* MX_PAGE_MARGINS_H_INCLUDED */
