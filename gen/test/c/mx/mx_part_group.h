/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PART_GROUP_H_INCLUDED
#define MX_PART_GROUP_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_formatted_text.h"
#include "mx_group_barline.h"
#include "mx_group_name.h"
#include "mx_group_symbol.h"
#include "mx_level.h"
#include "mx_name_display.h"
#include "mx_start_stop.h"

/*
 * The part-group element indicates groupings of parts in the score, usually indicated by braces and
 * brackets. Braces that are used for multi-staff parts should be defined in the attributes element
 * for that part. The part-group start element appears before the first score-part in the group. The
 * part-group stop element appears after the last score-part in the group. The number attribute is
 * used to distinguish overlapping and nested part-groups, not the sequence of groups. As with
 * parts, groups can have a name and abbreviation. Values for the child elements are ignored at the
 * stop of a group. A part-group element is not needed for a single multi-staff part. By default,
 * multi-staff parts include a brace symbol and (if appropriate given the bar-style) common
 * barlines. The symbol formatting for a multi-staff part can be more fully specified using the
 * part-symbol element.
 */
/* One child element of MxPartGroup: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxGroupName *group_name;
    MxNameDisplay *group_name_display;
    MxGroupName *group_abbreviation;
    MxNameDisplay *group_abbreviation_display;
    MxGroupSymbol *group_symbol;
    MxGroupBarline *group_barline;
    MxEmpty *group_time;
    MxFormattedText *footnote;
    MxLevel *level;
} MxPartGroupChild;

typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_number;
    char *number; /* attribute number */
    MxPartGroupChild *children; /* child elements in document order */
    size_t children_count;
} MxPartGroup;

/* NULL on error; the message is in mx_error(). */
MxPartGroup *mx_part_group_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_part_group_serialize(const MxPartGroup *m, xmlNodePtr parent, const char *tag);
void mx_part_group_free(MxPartGroup *m);

#endif /* MX_PART_GROUP_H_INCLUDED */
