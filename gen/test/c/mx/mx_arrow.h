/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ARROW_H_INCLUDED
#define MX_ARROW_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_arrow_direction.h"
#include "mx_arrow_style.h"
#include "mx_circular_arrow.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_empty.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_smufl_glyph_name.h"
#include "mx_tenths.h"

/*
 * The arrow element represents an arrow used for a musical technical indication. It can represent
 * both Unicode and SMuFL arrows. The presence of an arrowhead element indicates that only the
 * arrowhead is displayed, not the arrow stem. The smufl attribute distinguishes different SMuFL
 * glyphs that have an arrow appearance such as arrowBlackUp, guitarStrumUp, or handbellsSwingUp.
 * The specified glyph should match the descriptive representation.
 */
/* One child element of MxArrow: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxArrowDirection *arrow_direction;
    MxArrowStyle *arrow_style;
    MxEmpty *arrowhead;
    MxCircularArrow *circular_arrow;
} MxArrowChild;

typedef struct {
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_smufl;
    MxSMUFLGlyphName smufl; /* attribute smufl */
    MxArrowChild *children; /* child elements in document order */
    size_t children_count;
} MxArrow;

/* NULL on error; the message is in mx_error(). */
MxArrow *mx_arrow_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_arrow_serialize(const MxArrow *m, xmlNodePtr parent, const char *tag);
void mx_arrow_free(MxArrow *m);

#endif /* MX_ARROW_H_INCLUDED */
