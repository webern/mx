/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_VIRTUAL_INSTRUMENT_H_INCLUDED
#define MX_VIRTUAL_INSTRUMENT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>

/*
 * The virtual-instrument element defines a specific virtual instrument used for an instrument
 * sound.
 */
/* One child element of MxVirtualInstrument: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    char *virtual_library;
    char *virtual_name;
} MxVirtualInstrumentChild;

typedef struct {
    MxVirtualInstrumentChild *children; /* child elements in document order */
    size_t children_count;
} MxVirtualInstrument;

/* NULL on error; the message is in mx_error(). */
MxVirtualInstrument *mx_virtual_instrument_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_virtual_instrument_serialize(const MxVirtualInstrument *m, xmlNodePtr parent, const char *tag);
void mx_virtual_instrument_free(MxVirtualInstrument *m);

#endif /* MX_VIRTUAL_INSTRUMENT_H_INCLUDED */
