/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SCALING_H_INCLUDED
#define MX_SCALING_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_millimeters.h"
#include "mx_tenths.h"

/*
 * Margins, page sizes, and distances are all measured in tenths to keep MusicXML data in a
 * consistent coordinate system as much as possible. The translation to absolute units is done with
 * the scaling type, which specifies how many millimeters are equal to how many tenths. For a staff
 * height of 7 mm, millimeters would be set to 7 while tenths is set to 40. The ability to set a
 * formula rather than a single scaling factor helps avoid roundoff errors.
 */
/* One child element of MxScaling: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxMillimeters *millimeters;
    MxTenths *tenths;
} MxScalingChild;

typedef struct {
    MxScalingChild *children; /* child elements in document order */
    size_t children_count;
} MxScaling;

/* NULL on error; the message is in mx_error(). */
MxScaling *mx_scaling_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_scaling_serialize(const MxScaling *m, xmlNodePtr parent, const char *tag);
void mx_scaling_free(MxScaling *m);

#endif /* MX_SCALING_H_INCLUDED */
