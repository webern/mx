/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BACKUP_H_INCLUDED
#define MX_BACKUP_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_formatted_text.h"
#include "mx_level.h"
#include "mx_positive_divisions.h"

/*
 * The backup and forward elements are required to coordinate multiple voices in one part, including
 * music on multiple staves. The backup type is generally used to move between voices and staves.
 * Thus the backup element does not include voice or staff elements. Duration values should always
 * be positive, and should not cross measure boundaries or mid-measure changes in the divisions
 * value.
 */
/* One child element of MxBackup: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxPositiveDivisions *duration;
    MxFormattedText *footnote;
    MxLevel *level;
} MxBackupChild;

typedef struct {
    MxBackupChild *children; /* child elements in document order */
    size_t children_count;
} MxBackup;

/* NULL on error; the message is in mx_error(). */
MxBackup *mx_backup_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_backup_serialize(const MxBackup *m, xmlNodePtr parent, const char *tag);
void mx_backup_free(MxBackup *m);

#endif /* MX_BACKUP_H_INCLUDED */
