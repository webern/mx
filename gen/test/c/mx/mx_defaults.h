/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_DEFAULTS_H_INCLUDED
#define MX_DEFAULTS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_appearance.h"
#include "mx_empty_font.h"
#include "mx_lyric_font.h"
#include "mx_lyric_language.h"
#include "mx_page_layout.h"
#include "mx_scaling.h"
#include "mx_staff_layout.h"
#include "mx_system_layout.h"

/*
 * The defaults type specifies score-wide defaults for scaling, layout, and appearance.
 */
/* One child element of MxDefaults: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxScaling *scaling;
    MxPageLayout *page_layout;
    MxSystemLayout *system_layout;
    MxStaffLayout *staff_layout;
    MxAppearance *appearance;
    MxEmptyFont *music_font;
    MxEmptyFont *word_font;
    MxLyricFont *lyric_font;
    MxLyricLanguage *lyric_language;
} MxDefaultsChild;

typedef struct {
    MxDefaultsChild *children; /* child elements in document order */
    size_t children_count;
} MxDefaults;

/* NULL on error; the message is in mx_error(). */
MxDefaults *mx_defaults_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_defaults_serialize(const MxDefaults *m, xmlNodePtr parent, const char *tag);
void mx_defaults_free(MxDefaults *m);

#endif /* MX_DEFAULTS_H_INCLUDED */
