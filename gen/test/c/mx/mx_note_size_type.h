/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_NOTE_SIZE_TYPE_H_INCLUDED
#define MX_NOTE_SIZE_TYPE_H_INCLUDED

#include <stdbool.h>

/*
 * The note-size-type type indicates the type of note being defined by a note-size element. The
 * grace-cue type is used for notes of grace-cue size. The grace type is used for notes of cue size
 * that include a grace element. The cue type is used for all other notes with cue size, whether
 * defined explicitly or implicitly via a cue element. The large type is used for notes of large
 * size.
 */
typedef enum {
    MX_NOTE_SIZE_TYPE_CUE = 0,
    MX_NOTE_SIZE_TYPE_GRACE = 1,
    MX_NOTE_SIZE_TYPE_GRACE_CUE = 2,
    MX_NOTE_SIZE_TYPE_LARGE = 3
} MxNoteSizeType;

bool mx_note_size_type_try_parse(const char *s, MxNoteSizeType *out);
/* Lenient: unknown input falls back to the first variant. */
MxNoteSizeType mx_note_size_type_parse(const char *s);
/* Returns the wire literal (static storage; do not free). */
const char *mx_note_size_type_to_string(MxNoteSizeType v);

#endif /* MX_NOTE_SIZE_TYPE_H_INCLUDED */
