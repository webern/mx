/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BEAM_LEVEL_H_INCLUDED
#define MX_BEAM_LEVEL_H_INCLUDED

#include <stdbool.h>

/*
 * The MusicXML format supports six levels of beaming, up to 1024th notes. Unlike the number-level
 * type, the beam-level type identifies concurrent beams in a beam group. It does not distinguish
 * overlapping beams such as grace notes within regular notes, or beams used in different voices.
 */
typedef long MxBeamLevel;

/* Lexically strict parse, then clamps into the declared range. */
bool mx_beam_level_try_parse(const char *s, MxBeamLevel *out);
/* Lenient: unparseable input becomes 0, then clamps into the declared range. */
MxBeamLevel mx_beam_level_parse(const char *s);
/* Malloc'd; caller frees. */
char *mx_beam_level_to_string(MxBeamLevel v);

#endif /* MX_BEAM_LEVEL_H_INCLUDED */
