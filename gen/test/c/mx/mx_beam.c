/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_beam.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxBeam *mx_beam_parse(xmlNodePtr el) {
    MxBeam *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_beam_level_parse(s);
        } else if (strcmp(aname, "repeater") == 0) {
            m->has_repeater = true;
            m->repeater = mx_yes_no_parse(s);
        } else if (strcmp(aname, "fan") == 0) {
            m->has_fan = true;
            m->fan = mx_fan_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_beam_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    {
        xmlChar *text = xmlNodeGetContent(el);
        const char *s = text ? (const char *)text : "";
        m->value = mx_beam_value_parse(s);
        xmlFree(text);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_beam_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_beam_serialize(const MxBeam *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    xmlAddChild(el, xmlNewText(BAD_CAST mx_beam_value_to_string(m->value)));
    if (m->has_number) {
        char *s = mx_beam_level_to_string(m->number);
        xmlSetProp(el, BAD_CAST "number", BAD_CAST s);
        free(s);
    }
    if (m->has_repeater) {
        xmlSetProp(el, BAD_CAST "repeater", BAD_CAST mx_yes_no_to_string(m->repeater));
    }
    if (m->has_fan) {
        xmlSetProp(el, BAD_CAST "fan", BAD_CAST mx_fan_to_string(m->fan));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    return el;
}

void mx_beam_free(MxBeam *m) {
    if (!m)
        return;
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    free(m);
}
