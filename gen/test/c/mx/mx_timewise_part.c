/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_timewise_part.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxTimewisePart *mx_timewise_part_parse(xmlNodePtr el) {
    MxTimewisePart *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_timewise_part_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxTimewisePartChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "note") == 0) {
            ch->note = mx_note_parse(c);
            if (!ch->note) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "backup") == 0) {
            ch->backup = mx_backup_parse(c);
            if (!ch->backup) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "forward") == 0) {
            ch->forward = mx_forward_parse(c);
            if (!ch->forward) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "direction") == 0) {
            ch->direction = mx_direction_parse(c);
            if (!ch->direction) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "attributes") == 0) {
            ch->attributes = mx_attributes_parse(c);
            if (!ch->attributes) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "harmony") == 0) {
            ch->harmony = mx_harmony_parse(c);
            if (!ch->harmony) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "figured-bass") == 0) {
            ch->figured_bass = mx_figured_bass_parse(c);
            if (!ch->figured_bass) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "print") == 0) {
            ch->print = mx_print_parse(c);
            if (!ch->print) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sound") == 0) {
            ch->sound = mx_sound_parse(c);
            if (!ch->sound) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "barline") == 0) {
            ch->barline = mx_barline_parse(c);
            if (!ch->barline) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "grouping") == 0) {
            ch->grouping = mx_grouping_parse(c);
            if (!ch->grouping) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "link") == 0) {
            ch->link = mx_link_parse(c);
            if (!ch->link) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "bookmark") == 0) {
            ch->bookmark = mx_bookmark_parse(c);
            if (!ch->bookmark) {
                mx_timewise_part_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_timewise_part_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_timewise_part_serialize(const MxTimewisePart *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxTimewisePartChild *ch = &m->children[i];
        if (ch->note) {
            mx_note_serialize(ch->note, el, "note");
        } else if (ch->backup) {
            mx_backup_serialize(ch->backup, el, "backup");
        } else if (ch->forward) {
            mx_forward_serialize(ch->forward, el, "forward");
        } else if (ch->direction) {
            mx_direction_serialize(ch->direction, el, "direction");
        } else if (ch->attributes) {
            mx_attributes_serialize(ch->attributes, el, "attributes");
        } else if (ch->harmony) {
            mx_harmony_serialize(ch->harmony, el, "harmony");
        } else if (ch->figured_bass) {
            mx_figured_bass_serialize(ch->figured_bass, el, "figured-bass");
        } else if (ch->print) {
            mx_print_serialize(ch->print, el, "print");
        } else if (ch->sound) {
            mx_sound_serialize(ch->sound, el, "sound");
        } else if (ch->barline) {
            mx_barline_serialize(ch->barline, el, "barline");
        } else if (ch->grouping) {
            mx_grouping_serialize(ch->grouping, el, "grouping");
        } else if (ch->link) {
            mx_link_serialize(ch->link, el, "link");
        } else if (ch->bookmark) {
            mx_bookmark_serialize(ch->bookmark, el, "bookmark");
        }
    }
    return el;
}

void mx_timewise_part_free(MxTimewisePart *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxTimewisePartChild *ch = &m->children[i];
        if (ch->note)
            mx_note_free(ch->note);
        if (ch->backup)
            mx_backup_free(ch->backup);
        if (ch->forward)
            mx_forward_free(ch->forward);
        if (ch->direction)
            mx_direction_free(ch->direction);
        if (ch->attributes)
            mx_attributes_free(ch->attributes);
        if (ch->harmony)
            mx_harmony_free(ch->harmony);
        if (ch->figured_bass)
            mx_figured_bass_free(ch->figured_bass);
        if (ch->print)
            mx_print_free(ch->print);
        if (ch->sound)
            mx_sound_free(ch->sound);
        if (ch->barline)
            mx_barline_free(ch->barline);
        if (ch->grouping)
            mx_grouping_free(ch->grouping);
        if (ch->link)
            mx_link_free(ch->link);
        if (ch->bookmark)
            mx_bookmark_free(ch->bookmark);
    }
    free(m->children);
    free(m);
}
