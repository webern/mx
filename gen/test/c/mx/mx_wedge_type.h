/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_WEDGE_TYPE_H_INCLUDED
#define MX_WEDGE_TYPE_H_INCLUDED

#include <stdbool.h>

/*
 * The wedge type is crescendo for the start of a wedge that is closed at the left side, diminuendo
 * for the start of a wedge that is closed on the right side, and stop for the end of a wedge. The
 * continue type is used for formatting wedges over a system break, or for other situations where a
 * single wedge is divided into multiple segments.
 */
typedef enum {
    MX_WEDGE_TYPE_CRESCENDO = 0,
    MX_WEDGE_TYPE_DIMINUENDO = 1,
    MX_WEDGE_TYPE_STOP = 2,
    MX_WEDGE_TYPE_CONTINUE = 3
} MxWedgeType;

bool mx_wedge_type_try_parse(const char *s, MxWedgeType *out);
/* Lenient: unknown input falls back to the first variant. */
MxWedgeType mx_wedge_type_parse(const char *s);
/* Returns the wire literal (static storage; do not free). */
const char *mx_wedge_type_to_string(MxWedgeType v);

#endif /* MX_WEDGE_TYPE_H_INCLUDED */
