/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_INSTRUMENT_SOUND_H_INCLUDED
#define MX_INSTRUMENT_SOUND_H_INCLUDED

#include <stdbool.h>
#include "mx_sound_id.h"

/*
 * The instrument-sound value: one of the standard sound-id identifiers, or any other string. The
 * schema leaves the content open (xs:string), so the string member is intrinsic, not a fallback.
 */
typedef enum {
    MX_INSTRUMENT_SOUND_SOUND_ID = 0,
    MX_INSTRUMENT_SOUND_STRING = 1
} MxInstrumentSoundKind;

typedef struct {
    MxInstrumentSoundKind kind;
    MxSoundID sound_id;
    char *string;
} MxInstrumentSound;

/* Tries each union member in schema order. */
bool mx_instrument_sound_try_parse(const char *s, MxInstrumentSound *out);
/* Lenient: an unmatched input is absorbed by the first member. */
MxInstrumentSound mx_instrument_sound_parse(const char *s);
/* The wire spelling of whichever member is held. Malloc'd. */
char *mx_instrument_sound_to_string(MxInstrumentSound v);
void mx_instrument_sound_free(MxInstrumentSound *v);

#endif /* MX_INSTRUMENT_SOUND_H_INCLUDED */
