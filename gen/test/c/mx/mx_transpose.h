/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TRANSPOSE_H_INCLUDED
#define MX_TRANSPOSE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_semitones.h"
#include "mx_staff_number.h"

/*
 * The transpose type represents what must be added to a written pitch to get a correct sounding
 * pitch. The optional number attribute refers to staff numbers, from top to bottom on the system.
 * If absent, the transposition applies to all staves in the part. Per-staff transposition is most
 * often used in parts that represent multiple instruments.
 */
/* One child element of MxTranspose: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    long *diatonic;
    MxSemitones *chromatic;
    long *octave_change;
    MxEmpty *double_;
} MxTransposeChild;

typedef struct {
    bool has_number;
    MxStaffNumber number; /* attribute number */
    bool has_id;
    char *id; /* attribute id */
    MxTransposeChild *children; /* child elements in document order */
    size_t children_count;
} MxTranspose;

/* NULL on error; the message is in mx_error(). */
MxTranspose *mx_transpose_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_transpose_serialize(const MxTranspose *m, xmlNodePtr parent, const char *tag);
void mx_transpose_free(MxTranspose *m);

#endif /* MX_TRANSPOSE_H_INCLUDED */
