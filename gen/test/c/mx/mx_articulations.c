/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_articulations.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxArticulations *mx_articulations_parse(xmlNodePtr el) {
    MxArticulations *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_articulations_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxArticulationsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "accent") == 0) {
            ch->accent = mx_empty_placement_parse(c);
            if (!ch->accent) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "strong-accent") == 0) {
            ch->strong_accent = mx_strong_accent_parse(c);
            if (!ch->strong_accent) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staccato") == 0) {
            ch->staccato = mx_empty_placement_parse(c);
            if (!ch->staccato) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tenuto") == 0) {
            ch->tenuto = mx_empty_placement_parse(c);
            if (!ch->tenuto) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "detached-legato") == 0) {
            ch->detached_legato = mx_empty_placement_parse(c);
            if (!ch->detached_legato) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staccatissimo") == 0) {
            ch->staccatissimo = mx_empty_placement_parse(c);
            if (!ch->staccatissimo) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "spiccato") == 0) {
            ch->spiccato = mx_empty_placement_parse(c);
            if (!ch->spiccato) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "scoop") == 0) {
            ch->scoop = mx_empty_line_parse(c);
            if (!ch->scoop) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "plop") == 0) {
            ch->plop = mx_empty_line_parse(c);
            if (!ch->plop) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "doit") == 0) {
            ch->doit = mx_empty_line_parse(c);
            if (!ch->doit) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "falloff") == 0) {
            ch->falloff = mx_empty_line_parse(c);
            if (!ch->falloff) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "breath-mark") == 0) {
            ch->breath_mark = mx_breath_mark_parse(c);
            if (!ch->breath_mark) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "caesura") == 0) {
            ch->caesura = mx_caesura_parse(c);
            if (!ch->caesura) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "stress") == 0) {
            ch->stress = mx_empty_placement_parse(c);
            if (!ch->stress) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "unstress") == 0) {
            ch->unstress = mx_empty_placement_parse(c);
            if (!ch->unstress) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "soft-accent") == 0) {
            ch->soft_accent = mx_empty_placement_parse(c);
            if (!ch->soft_accent) {
                mx_articulations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-articulation") == 0) {
            ch->other_articulation = mx_other_placement_text_parse(c);
            if (!ch->other_articulation) {
                mx_articulations_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_articulations_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_articulations_serialize(const MxArticulations *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxArticulationsChild *ch = &m->children[i];
        if (ch->accent) {
            mx_empty_placement_serialize(ch->accent, el, "accent");
        } else if (ch->strong_accent) {
            mx_strong_accent_serialize(ch->strong_accent, el, "strong-accent");
        } else if (ch->staccato) {
            mx_empty_placement_serialize(ch->staccato, el, "staccato");
        } else if (ch->tenuto) {
            mx_empty_placement_serialize(ch->tenuto, el, "tenuto");
        } else if (ch->detached_legato) {
            mx_empty_placement_serialize(ch->detached_legato, el, "detached-legato");
        } else if (ch->staccatissimo) {
            mx_empty_placement_serialize(ch->staccatissimo, el, "staccatissimo");
        } else if (ch->spiccato) {
            mx_empty_placement_serialize(ch->spiccato, el, "spiccato");
        } else if (ch->scoop) {
            mx_empty_line_serialize(ch->scoop, el, "scoop");
        } else if (ch->plop) {
            mx_empty_line_serialize(ch->plop, el, "plop");
        } else if (ch->doit) {
            mx_empty_line_serialize(ch->doit, el, "doit");
        } else if (ch->falloff) {
            mx_empty_line_serialize(ch->falloff, el, "falloff");
        } else if (ch->breath_mark) {
            mx_breath_mark_serialize(ch->breath_mark, el, "breath-mark");
        } else if (ch->caesura) {
            mx_caesura_serialize(ch->caesura, el, "caesura");
        } else if (ch->stress) {
            mx_empty_placement_serialize(ch->stress, el, "stress");
        } else if (ch->unstress) {
            mx_empty_placement_serialize(ch->unstress, el, "unstress");
        } else if (ch->soft_accent) {
            mx_empty_placement_serialize(ch->soft_accent, el, "soft-accent");
        } else if (ch->other_articulation) {
            mx_other_placement_text_serialize(ch->other_articulation, el, "other-articulation");
        }
    }
    return el;
}

void mx_articulations_free(MxArticulations *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxArticulationsChild *ch = &m->children[i];
        if (ch->accent)
            mx_empty_placement_free(ch->accent);
        if (ch->strong_accent)
            mx_strong_accent_free(ch->strong_accent);
        if (ch->staccato)
            mx_empty_placement_free(ch->staccato);
        if (ch->tenuto)
            mx_empty_placement_free(ch->tenuto);
        if (ch->detached_legato)
            mx_empty_placement_free(ch->detached_legato);
        if (ch->staccatissimo)
            mx_empty_placement_free(ch->staccatissimo);
        if (ch->spiccato)
            mx_empty_placement_free(ch->spiccato);
        if (ch->scoop)
            mx_empty_line_free(ch->scoop);
        if (ch->plop)
            mx_empty_line_free(ch->plop);
        if (ch->doit)
            mx_empty_line_free(ch->doit);
        if (ch->falloff)
            mx_empty_line_free(ch->falloff);
        if (ch->breath_mark)
            mx_breath_mark_free(ch->breath_mark);
        if (ch->caesura)
            mx_caesura_free(ch->caesura);
        if (ch->stress)
            mx_empty_placement_free(ch->stress);
        if (ch->unstress)
            mx_empty_placement_free(ch->unstress);
        if (ch->soft_accent)
            mx_empty_placement_free(ch->soft_accent);
        if (ch->other_articulation)
            mx_other_placement_text_free(ch->other_articulation);
    }
    free(m->children);
    free(m);
}
