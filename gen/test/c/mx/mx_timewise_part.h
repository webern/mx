/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TIMEWISE_PART_H_INCLUDED
#define MX_TIMEWISE_PART_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_attributes.h"
#include "mx_backup.h"
#include "mx_barline.h"
#include "mx_bookmark.h"
#include "mx_direction.h"
#include "mx_figured_bass.h"
#include "mx_forward.h"
#include "mx_grouping.h"
#include "mx_harmony.h"
#include "mx_link.h"
#include "mx_note.h"
#include "mx_print.h"
#include "mx_sound.h"

/* One child element of MxTimewisePart: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxNote *note;
    MxBackup *backup;
    MxForward *forward;
    MxDirection *direction;
    MxAttributes *attributes;
    MxHarmony *harmony;
    MxFiguredBass *figured_bass;
    MxPrint *print;
    MxSound *sound;
    MxBarline *barline;
    MxGrouping *grouping;
    MxLink *link;
    MxBookmark *bookmark;
} MxTimewisePartChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxTimewisePartChild *children; /* child elements in document order */
    size_t children_count;
} MxTimewisePart;

/* NULL on error; the message is in mx_error(). */
MxTimewisePart *mx_timewise_part_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_timewise_part_serialize(const MxTimewisePart *m, xmlNodePtr parent, const char *tag);
void mx_timewise_part_free(MxTimewisePart *m);

#endif /* MX_TIMEWISE_PART_H_INCLUDED */
