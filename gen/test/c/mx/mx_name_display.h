/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_NAME_DISPLAY_H_INCLUDED
#define MX_NAME_DISPLAY_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_accidental_text.h"
#include "mx_formatted_text.h"
#include "mx_yes_no.h"

/*
 * The name-display type is used for exact formatting of multi-font text in part and group names to
 * the left of the system. The print-object attribute can be used to determine what, if anything, is
 * printed at the start of each system. Enclosure for the display-text element is none by default.
 * Language for the display-text element is Italian ("it") by default.
 */
/* One child element of MxNameDisplay: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxFormattedText *display_text;
    MxAccidentalText *accidental_text;
} MxNameDisplayChild;

typedef struct {
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    MxNameDisplayChild *children; /* child elements in document order */
    size_t children_count;
} MxNameDisplay;

/* NULL on error; the message is in mx_error(). */
MxNameDisplay *mx_name_display_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_name_display_serialize(const MxNameDisplay *m, xmlNodePtr parent, const char *tag);
void mx_name_display_free(MxNameDisplay *m);

#endif /* MX_NAME_DISPLAY_H_INCLUDED */
