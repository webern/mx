/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PART_LIST_H_INCLUDED
#define MX_PART_LIST_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_part_group.h"
#include "mx_score_part.h"

/*
 * The part-list identifies the different musical parts in this movement. Each part has an ID that
 * is used later within the musical data. Since parts may be encoded separately and combined later,
 * identification elements are present at both the score and score-part levels. There must be at
 * least one score-part, combined as desired with part-group elements that indicate braces and
 * brackets. Parts are ordered from top to bottom in a score based on the order in which they appear
 * in the part-list.
 */
/* One child element of MxPartList: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxPartGroup *part_group;
    MxScorePart *score_part;
} MxPartListChild;

typedef struct {
    MxPartListChild *children; /* child elements in document order */
    size_t children_count;
} MxPartList;

/* NULL on error; the message is in mx_error(). */
MxPartList *mx_part_list_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_part_list_serialize(const MxPartList *m, xmlNodePtr parent, const char *tag);
void mx_part_list_free(MxPartList *m);

#endif /* MX_PART_LIST_H_INCLUDED */
