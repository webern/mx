/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_tuplet.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxTuplet *mx_tuplet_parse(xmlNodePtr el) {
    MxTuplet *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "type") == 0) {
            m->has_type = true;
            m->type = mx_start_stop_parse(s);
        } else if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_number_level_parse(s);
        } else if (strcmp(aname, "bracket") == 0) {
            m->has_bracket = true;
            m->bracket = mx_yes_no_parse(s);
        } else if (strcmp(aname, "show-number") == 0) {
            m->has_show_number = true;
            m->show_number = mx_show_tuplet_parse(s);
        } else if (strcmp(aname, "show-type") == 0) {
            m->has_show_type = true;
            m->show_type = mx_show_tuplet_parse(s);
        } else if (strcmp(aname, "line-shape") == 0) {
            m->has_line_shape = true;
            m->line_shape = mx_line_shape_parse(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "placement") == 0) {
            m->has_placement = true;
            m->placement = mx_above_below_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_tuplet_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxTupletChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "tuplet-actual") == 0) {
            ch->tuplet_actual = mx_tuplet_portion_parse(c);
            if (!ch->tuplet_actual) {
                mx_tuplet_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tuplet-normal") == 0) {
            ch->tuplet_normal = mx_tuplet_portion_parse(c);
            if (!ch->tuplet_normal) {
                mx_tuplet_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_tuplet_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_tuplet_serialize(const MxTuplet *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_type) {
        xmlSetProp(el, BAD_CAST "type", BAD_CAST mx_start_stop_to_string(m->type));
    }
    if (m->has_number) {
        char *s = mx_number_level_to_string(m->number);
        xmlSetProp(el, BAD_CAST "number", BAD_CAST s);
        free(s);
    }
    if (m->has_bracket) {
        xmlSetProp(el, BAD_CAST "bracket", BAD_CAST mx_yes_no_to_string(m->bracket));
    }
    if (m->has_show_number) {
        xmlSetProp(el, BAD_CAST "show-number", BAD_CAST mx_show_tuplet_to_string(m->show_number));
    }
    if (m->has_show_type) {
        xmlSetProp(el, BAD_CAST "show-type", BAD_CAST mx_show_tuplet_to_string(m->show_type));
    }
    if (m->has_line_shape) {
        xmlSetProp(el, BAD_CAST "line-shape", BAD_CAST mx_line_shape_to_string(m->line_shape));
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_placement) {
        xmlSetProp(el, BAD_CAST "placement", BAD_CAST mx_above_below_to_string(m->placement));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxTupletChild *ch = &m->children[i];
        if (ch->tuplet_actual) {
            mx_tuplet_portion_serialize(ch->tuplet_actual, el, "tuplet-actual");
        } else if (ch->tuplet_normal) {
            mx_tuplet_portion_serialize(ch->tuplet_normal, el, "tuplet-normal");
        }
    }
    return el;
}

void mx_tuplet_free(MxTuplet *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxTupletChild *ch = &m->children[i];
        if (ch->tuplet_actual)
            mx_tuplet_portion_free(ch->tuplet_actual);
        if (ch->tuplet_normal)
            mx_tuplet_portion_free(ch->tuplet_normal);
    }
    free(m->children);
    free(m);
}
