/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_NOTE_TYPE_H_INCLUDED
#define MX_NOTE_TYPE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_note_type_value.h"
#include "mx_symbol_size.h"

/*
 * The note-type type indicates the graphic note type. Values range from 1024th to maxima. The size
 * attribute indicates full, cue, grace-cue, or large size. The default is full for regular notes,
 * grace-cue for notes that contain both grace and cue elements, and cue for notes that contain
 * either a cue or a grace element, but not both.
 */
typedef struct {
    bool has_size;
    MxSymbolSize size; /* attribute size */
    MxNoteTypeValue value; /* text content */
} MxNoteType;

/* NULL on error; the message is in mx_error(). */
MxNoteType *mx_note_type_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_note_type_serialize(const MxNoteType *m, xmlNodePtr parent, const char *tag);
void mx_note_type_free(MxNoteType *m);

#endif /* MX_NOTE_TYPE_H_INCLUDED */
