/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_timewise_measure.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxTimewiseMeasure *mx_timewise_measure_parse(xmlNodePtr el) {
    MxTimewiseMeasure *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_strdup(s);
        } else if (strcmp(aname, "text") == 0) {
            m->has_text = true;
            m->text = mx_measure_text_parse(s);
        } else if (strcmp(aname, "implicit") == 0) {
            m->has_implicit = true;
            m->implicit = mx_yes_no_parse(s);
        } else if (strcmp(aname, "non-controlling") == 0) {
            m->has_non_controlling = true;
            m->non_controlling = mx_yes_no_parse(s);
        } else if (strcmp(aname, "width") == 0) {
            m->has_width = true;
            m->width = mx_tenths_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_timewise_measure_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxTimewiseMeasureChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "part") == 0) {
            ch->part = mx_timewise_part_parse(c);
            if (!ch->part) {
                mx_timewise_measure_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_timewise_measure_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_timewise_measure_serialize(const MxTimewiseMeasure *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_number) {
        xmlSetProp(el, BAD_CAST "number", BAD_CAST m->number);
    }
    if (m->has_text) {
        xmlSetProp(el, BAD_CAST "text", BAD_CAST m->text);
    }
    if (m->has_implicit) {
        xmlSetProp(el, BAD_CAST "implicit", BAD_CAST mx_yes_no_to_string(m->implicit));
    }
    if (m->has_non_controlling) {
        xmlSetProp(el, BAD_CAST "non-controlling", BAD_CAST mx_yes_no_to_string(m->non_controlling));
    }
    if (m->has_width) {
        char *s = mx_tenths_to_string(m->width);
        xmlSetProp(el, BAD_CAST "width", BAD_CAST s);
        free(s);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxTimewiseMeasureChild *ch = &m->children[i];
        if (ch->part) {
            mx_timewise_part_serialize(ch->part, el, "part");
        }
    }
    return el;
}

void mx_timewise_measure_free(MxTimewiseMeasure *m) {
    if (!m)
        return;
    if (m->has_number)
        free(m->number);
    if (m->has_text)
        free(m->text);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxTimewiseMeasureChild *ch = &m->children[i];
        if (ch->part)
            mx_timewise_part_free(ch->part);
    }
    free(m->children);
    free(m);
}
