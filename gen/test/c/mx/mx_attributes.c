/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_attributes.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxAttributes *mx_attributes_parse(xmlNodePtr el) {
    MxAttributes *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_attributes_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxAttributesChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "divisions") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->divisions = malloc(sizeof(*ch->divisions));
            if (!ch->divisions)
                abort();
            *ch->divisions = mx_positive_divisions_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "key") == 0) {
            ch->key = mx_key_parse(c);
            if (!ch->key) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "time") == 0) {
            ch->time = mx_time_parse(c);
            if (!ch->time) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staves") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staves = malloc(sizeof(*ch->staves));
            if (!ch->staves)
                abort();
            *ch->staves = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "part-symbol") == 0) {
            ch->part_symbol = mx_part_symbol_parse(c);
            if (!ch->part_symbol) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "instruments") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->instruments = malloc(sizeof(*ch->instruments));
            if (!ch->instruments)
                abort();
            *ch->instruments = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "clef") == 0) {
            ch->clef = mx_clef_parse(c);
            if (!ch->clef) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staff-details") == 0) {
            ch->staff_details = mx_staff_details_parse(c);
            if (!ch->staff_details) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "transpose") == 0) {
            ch->transpose = mx_transpose_parse(c);
            if (!ch->transpose) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "directive") == 0) {
            ch->directive = mx_directive_parse(c);
            if (!ch->directive) {
                mx_attributes_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "measure-style") == 0) {
            ch->measure_style = mx_measure_style_parse(c);
            if (!ch->measure_style) {
                mx_attributes_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_attributes_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_attributes_serialize(const MxAttributes *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxAttributesChild *ch = &m->children[i];
        if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        } else if (ch->divisions) {
            char *s = mx_positive_divisions_to_string((*ch->divisions));
            xmlNewTextChild(el, NULL, BAD_CAST "divisions", BAD_CAST s);
            free(s);
        } else if (ch->key) {
            mx_key_serialize(ch->key, el, "key");
        } else if (ch->time) {
            mx_time_serialize(ch->time, el, "time");
        } else if (ch->staves) {
            char *s = mx_format_int((*ch->staves));
            xmlNewTextChild(el, NULL, BAD_CAST "staves", BAD_CAST s);
            free(s);
        } else if (ch->part_symbol) {
            mx_part_symbol_serialize(ch->part_symbol, el, "part-symbol");
        } else if (ch->instruments) {
            char *s = mx_format_int((*ch->instruments));
            xmlNewTextChild(el, NULL, BAD_CAST "instruments", BAD_CAST s);
            free(s);
        } else if (ch->clef) {
            mx_clef_serialize(ch->clef, el, "clef");
        } else if (ch->staff_details) {
            mx_staff_details_serialize(ch->staff_details, el, "staff-details");
        } else if (ch->transpose) {
            mx_transpose_serialize(ch->transpose, el, "transpose");
        } else if (ch->directive) {
            mx_directive_serialize(ch->directive, el, "directive");
        } else if (ch->measure_style) {
            mx_measure_style_serialize(ch->measure_style, el, "measure-style");
        }
    }
    return el;
}

void mx_attributes_free(MxAttributes *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxAttributesChild *ch = &m->children[i];
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
        free(ch->divisions);
        if (ch->key)
            mx_key_free(ch->key);
        if (ch->time)
            mx_time_free(ch->time);
        free(ch->staves);
        if (ch->part_symbol)
            mx_part_symbol_free(ch->part_symbol);
        free(ch->instruments);
        if (ch->clef)
            mx_clef_free(ch->clef);
        if (ch->staff_details)
            mx_staff_details_free(ch->staff_details);
        if (ch->transpose)
            mx_transpose_free(ch->transpose);
        if (ch->directive)
            mx_directive_free(ch->directive);
        if (ch->measure_style)
            mx_measure_style_free(ch->measure_style);
    }
    free(m->children);
    free(m);
}
