/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_start_note.h"

#include <string.h>

static const char *const mx_start_note_values[] = {
    "upper",
    "main",
    "below",
};

bool mx_start_note_try_parse(const char *s, MxStartNote *out) {
    if (!s)
        s = "";
    for (size_t i = 0; i < sizeof(mx_start_note_values) / sizeof(mx_start_note_values[0]); i++) {
        if (strcmp(s, mx_start_note_values[i]) == 0) {
            *out = (MxStartNote)i;
            return true;
        }
    }
    *out = (MxStartNote)0;
    return false;
}

MxStartNote mx_start_note_parse(const char *s) {
    MxStartNote v;
    mx_start_note_try_parse(s, &v);
    return v;
}

const char *mx_start_note_to_string(MxStartNote v) {
    if ((size_t)v >= sizeof(mx_start_note_values) / sizeof(mx_start_note_values[0]))
        return mx_start_note_values[0];
    return mx_start_note_values[v];
}
