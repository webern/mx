/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TIED_H_INCLUDED
#define MX_TIED_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_color.h"
#include "mx_divisions.h"
#include "mx_line_type.h"
#include "mx_number_level.h"
#include "mx_over_under.h"
#include "mx_tenths.h"
#include "mx_tied_type.h"

/*
 * The tied element represents the notated tie. The tie element represents the tie sound. The number
 * attribute is rarely needed to disambiguate ties, since note pitches will usually suffice. The
 * attribute is implied rather than defaulting to 1 as with most elements. It is available for use
 * in more complex tied notation situations. Ties that join two notes of the same pitch together
 * should be represented with a tied element on the first note with type="start" and a tied element
 * on the second note with type="stop". This can also be done if the two notes being tied are
 * enharmonically equivalent, but have different step values. It is not recommended to use tied
 * elements to join two notes with enharmonically inequivalent pitches. Ties that indicate that an
 * instrument should be undamped are specified with a single tied element with type="let-ring". Ties
 * that are visually attached to only one note, other than undamped ties, should be specified with
 * two tied elements on the same note, first type="start" then type="stop". This can be used to
 * represent ties into or out of repeated sections or codas.
 */
typedef struct {
    bool has_type;
    MxTiedType type; /* attribute type */
    bool has_number;
    MxNumberLevel number; /* attribute number */
    bool has_line_type;
    MxLineType line_type; /* attribute line-type */
    bool has_dash_length;
    MxTenths dash_length; /* attribute dash-length */
    bool has_space_length;
    MxTenths space_length; /* attribute space-length */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_orientation;
    MxOverUnder orientation; /* attribute orientation */
    bool has_bezier_x;
    MxTenths bezier_x; /* attribute bezier-x */
    bool has_bezier_y;
    MxTenths bezier_y; /* attribute bezier-y */
    bool has_bezier_x2;
    MxTenths bezier_x2; /* attribute bezier-x2 */
    bool has_bezier_y2;
    MxTenths bezier_y2; /* attribute bezier-y2 */
    bool has_bezier_offset;
    MxDivisions bezier_offset; /* attribute bezier-offset */
    bool has_bezier_offset2;
    MxDivisions bezier_offset2; /* attribute bezier-offset2 */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_id;
    char *id; /* attribute id */
} MxTied;

/* NULL on error; the message is in mx_error(). */
MxTied *mx_tied_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_tied_serialize(const MxTied *m, xmlNodePtr parent, const char *tag);
void mx_tied_free(MxTied *m);

#endif /* MX_TIED_H_INCLUDED */
