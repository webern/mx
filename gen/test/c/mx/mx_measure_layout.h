/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_MEASURE_LAYOUT_H_INCLUDED
#define MX_MEASURE_LAYOUT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_tenths.h"

/*
 * The measure-layout type includes the horizontal distance from the previous measure.
 */
/* One child element of MxMeasureLayout: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTenths *measure_distance;
} MxMeasureLayoutChild;

typedef struct {
    MxMeasureLayoutChild *children; /* child elements in document order */
    size_t children_count;
} MxMeasureLayout;

/* NULL on error; the message is in mx_error(). */
MxMeasureLayout *mx_measure_layout_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_measure_layout_serialize(const MxMeasureLayout *m, xmlNodePtr parent, const char *tag);
void mx_measure_layout_free(MxMeasureLayout *m);

#endif /* MX_MEASURE_LAYOUT_H_INCLUDED */
