/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_defaults.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxDefaults *mx_defaults_parse(xmlNodePtr el) {
    MxDefaults *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_defaults_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxDefaultsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "scaling") == 0) {
            ch->scaling = mx_scaling_parse(c);
            if (!ch->scaling) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "page-layout") == 0) {
            ch->page_layout = mx_page_layout_parse(c);
            if (!ch->page_layout) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "system-layout") == 0) {
            ch->system_layout = mx_system_layout_parse(c);
            if (!ch->system_layout) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staff-layout") == 0) {
            ch->staff_layout = mx_staff_layout_parse(c);
            if (!ch->staff_layout) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "appearance") == 0) {
            ch->appearance = mx_appearance_parse(c);
            if (!ch->appearance) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "music-font") == 0) {
            ch->music_font = mx_empty_font_parse(c);
            if (!ch->music_font) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "word-font") == 0) {
            ch->word_font = mx_empty_font_parse(c);
            if (!ch->word_font) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "lyric-font") == 0) {
            ch->lyric_font = mx_lyric_font_parse(c);
            if (!ch->lyric_font) {
                mx_defaults_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "lyric-language") == 0) {
            ch->lyric_language = mx_lyric_language_parse(c);
            if (!ch->lyric_language) {
                mx_defaults_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_defaults_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_defaults_serialize(const MxDefaults *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxDefaultsChild *ch = &m->children[i];
        if (ch->scaling) {
            mx_scaling_serialize(ch->scaling, el, "scaling");
        } else if (ch->page_layout) {
            mx_page_layout_serialize(ch->page_layout, el, "page-layout");
        } else if (ch->system_layout) {
            mx_system_layout_serialize(ch->system_layout, el, "system-layout");
        } else if (ch->staff_layout) {
            mx_staff_layout_serialize(ch->staff_layout, el, "staff-layout");
        } else if (ch->appearance) {
            mx_appearance_serialize(ch->appearance, el, "appearance");
        } else if (ch->music_font) {
            mx_empty_font_serialize(ch->music_font, el, "music-font");
        } else if (ch->word_font) {
            mx_empty_font_serialize(ch->word_font, el, "word-font");
        } else if (ch->lyric_font) {
            mx_lyric_font_serialize(ch->lyric_font, el, "lyric-font");
        } else if (ch->lyric_language) {
            mx_lyric_language_serialize(ch->lyric_language, el, "lyric-language");
        }
    }
    return el;
}

void mx_defaults_free(MxDefaults *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxDefaultsChild *ch = &m->children[i];
        if (ch->scaling)
            mx_scaling_free(ch->scaling);
        if (ch->page_layout)
            mx_page_layout_free(ch->page_layout);
        if (ch->system_layout)
            mx_system_layout_free(ch->system_layout);
        if (ch->staff_layout)
            mx_staff_layout_free(ch->staff_layout);
        if (ch->appearance)
            mx_appearance_free(ch->appearance);
        if (ch->music_font)
            mx_empty_font_free(ch->music_font);
        if (ch->word_font)
            mx_empty_font_free(ch->word_font);
        if (ch->lyric_font)
            mx_lyric_font_free(ch->lyric_font);
        if (ch->lyric_language)
            mx_lyric_language_free(ch->lyric_language);
    }
    free(m->children);
    free(m);
}
