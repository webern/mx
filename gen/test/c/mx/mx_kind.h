/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_KIND_H_INCLUDED
#define MX_KIND_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_kind_value.h"
#include "mx_left_center_right.h"
#include "mx_tenths.h"
#include "mx_valign.h"
#include "mx_yes_no.h"

/*
 * Kind indicates the type of chord. Degree elements can then add, subtract, or alter from these
 * starting points The attributes are used to indicate the formatting of the symbol. Since the kind
 * element is the constant in all the harmony-chord groups that can make up a polychord, many
 * formatting attributes are here. The use-symbols attribute is yes if the kind should be
 * represented when possible with harmony symbols rather than letters and numbers. These symbols
 * include: major: a triangle, like Unicode 25B3 minor: -, like Unicode 002D augmented: +, like
 * Unicode 002B diminished: °, like Unicode 00B0 half-diminished: ø, like Unicode 00F8 For the
 * major-minor kind, only the minor symbol is used when use-symbols is yes. The major symbol is set
 * using the symbol attribute in the degree-value element. The corresponding degree-alter value will
 * usually be 0 in this case. The text attribute describes how the kind should be spelled in a
 * score. If use-symbols is yes, the value of the text attribute follows the symbol. The
 * stack-degrees attribute is yes if the degree elements should be stacked above each other. The
 * parentheses-degrees attribute is yes if all the degrees should be in parentheses. The
 * bracket-degrees attribute is yes if all the degrees should be in a bracket. If not specified,
 * these values are implementation-specific. The alignment attributes are for the entire
 * harmony-chord group of which this kind element is a part.
 */
typedef struct {
    bool has_use_symbols;
    MxYesNo use_symbols; /* attribute use-symbols */
    bool has_text;
    char *text; /* attribute text */
    bool has_stack_degrees;
    MxYesNo stack_degrees; /* attribute stack-degrees */
    bool has_parentheses_degrees;
    MxYesNo parentheses_degrees; /* attribute parentheses-degrees */
    bool has_bracket_degrees;
    MxYesNo bracket_degrees; /* attribute bracket-degrees */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValign valign; /* attribute valign */
    MxKindValue value; /* text content */
} MxKind;

/* NULL on error; the message is in mx_error(). */
MxKind *mx_kind_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_kind_serialize(const MxKind *m, xmlNodePtr parent, const char *tag);
void mx_kind_free(MxKind *m);

#endif /* MX_KIND_H_INCLUDED */
