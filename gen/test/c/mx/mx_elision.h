/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ELISION_H_INCLUDED
#define MX_ELISION_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_smufl_lyrics_glyph_name.h"

/*
 * The elision type represents an elision between lyric syllables. The text content specifies the
 * symbol used to display the elision. Common values are a no-break space (Unicode 00A0), an
 * underscore (Unicode 005F), or an undertie (Unicode 203F). If the text content is empty, the smufl
 * attribute is used to specify the symbol to use. Its value is a SMuFL canonical glyph name that
 * starts with lyrics. The SMuFL attribute is ignored if the elision glyph is already specified by
 * the text content. If neither text content nor a smufl attribute are present, the elision glyph is
 * application-specific.
 */
typedef struct {
    bool has_smufl;
    MxSMUFLLyricsGlyphName smufl; /* attribute smufl */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    char *value; /* text content */
} MxElision;

/* NULL on error; the message is in mx_error(). */
MxElision *mx_elision_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_elision_serialize(const MxElision *m, xmlNodePtr parent, const char *tag);
void mx_elision_free(MxElision *m);

#endif /* MX_ELISION_H_INCLUDED */
