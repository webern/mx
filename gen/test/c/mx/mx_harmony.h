/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_HARMONY_H_INCLUDED
#define MX_HARMONY_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_bass.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_degree.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_formatted_text.h"
#include "mx_frame.h"
#include "mx_harmony_type.h"
#include "mx_inversion.h"
#include "mx_kind.h"
#include "mx_level.h"
#include "mx_offset.h"
#include "mx_root.h"
#include "mx_style_text.h"
#include "mx_tenths.h"
#include "mx_yes_no.h"

/*
 * The harmony type is based on Humdrum's **harm encoding, extended to support chord symbols in
 * popular music as well as functional harmony analysis in classical music. If there are alternate
 * harmonies possible, this can be specified using multiple harmony elements differentiated by type.
 * Explicit harmonies have all note present in the music; implied have some notes missing but
 * implied; alternate represents alternate analyses. The harmony object may be used for analysis or
 * for chord symbols. The print-object attribute controls whether or not anything is printed due to
 * the harmony element. The print-frame attribute controls printing of a frame or fretboard diagram.
 * The print-style attribute group sets the default for the harmony, but individual elements can
 * override this with their own print-style values.
 */
/* One child element of MxHarmony: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxRoot *root;
    MxStyleText *function;
    MxKind *kind;
    MxInversion *inversion;
    MxBass *bass;
    MxDegree *degree;
    MxFrame *frame;
    MxOffset *offset;
    MxFormattedText *footnote;
    MxLevel *level;
    long *staff;
} MxHarmonyChild;

typedef struct {
    bool has_type;
    MxHarmonyType type; /* attribute type */
    bool has_print_frame;
    MxYesNo print_frame; /* attribute print-frame */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_id;
    char *id; /* attribute id */
    MxHarmonyChild *children; /* child elements in document order */
    size_t children_count;
} MxHarmony;

/* NULL on error; the message is in mx_error(). */
MxHarmony *mx_harmony_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_harmony_serialize(const MxHarmony *m, xmlNodePtr parent, const char *tag);
void mx_harmony_free(MxHarmony *m);

#endif /* MX_HARMONY_H_INCLUDED */
