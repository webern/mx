/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_measure_layout.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMeasureLayout *mx_measure_layout_parse(xmlNodePtr el) {
    MxMeasureLayout *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_measure_layout_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxMeasureLayoutChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "measure-distance") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->measure_distance = malloc(sizeof(*ch->measure_distance));
            if (!ch->measure_distance)
                abort();
            *ch->measure_distance = mx_tenths_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_measure_layout_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_measure_layout_serialize(const MxMeasureLayout *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxMeasureLayoutChild *ch = &m->children[i];
        if (ch->measure_distance) {
            char *s = mx_tenths_to_string((*ch->measure_distance));
            xmlNewTextChild(el, NULL, BAD_CAST "measure-distance", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_measure_layout_free(MxMeasureLayout *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxMeasureLayoutChild *ch = &m->children[i];
        free(ch->measure_distance);
    }
    free(m->children);
    free(m);
}
