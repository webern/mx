/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_FRAME_NOTE_H_INCLUDED
#define MX_FRAME_NOTE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_barre.h"
#include "mx_fingering.h"
#include "mx_fret.h"
#include "mx_string.h"

/*
 * The frame-note type represents each note included in the frame. An open string will have a fret
 * value of 0, while a muted string will not be associated with a frame-note element.
 */
/* One child element of MxFrameNote: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxString *string;
    MxFret *fret;
    MxFingering *fingering;
    MxBarre *barre;
} MxFrameNoteChild;

typedef struct {
    MxFrameNoteChild *children; /* child elements in document order */
    size_t children_count;
} MxFrameNote;

/* NULL on error; the message is in mx_error(). */
MxFrameNote *mx_frame_note_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_frame_note_serialize(const MxFrameNote *m, xmlNodePtr parent, const char *tag);
void mx_frame_note_free(MxFrameNote *m);

#endif /* MX_FRAME_NOTE_H_INCLUDED */
