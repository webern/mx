/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SCORDATURA_H_INCLUDED
#define MX_SCORDATURA_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_accord.h"

/*
 * Scordatura string tunings are represented by a series of accord elements, similar to the
 * staff-tuning elements. Strings are numbered from high to low.
 */
/* One child element of MxScordatura: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxAccord *accord;
} MxScordaturaChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxScordaturaChild *children; /* child elements in document order */
    size_t children_count;
} MxScordatura;

/* NULL on error; the message is in mx_error(). */
MxScordatura *mx_scordatura_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_scordatura_serialize(const MxScordatura *m, xmlNodePtr parent, const char *tag);
void mx_scordatura_free(MxScordatura *m);

#endif /* MX_SCORDATURA_H_INCLUDED */
