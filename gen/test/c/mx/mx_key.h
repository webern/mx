/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_KEY_H_INCLUDED
#define MX_KEY_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_cancel.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_fifths.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_key_accidental.h"
#include "mx_key_octave.h"
#include "mx_mode.h"
#include "mx_semitones.h"
#include "mx_staff_number.h"
#include "mx_step.h"
#include "mx_tenths.h"
#include "mx_yes_no.h"

/*
 * The key type represents a key signature. Both traditional and non-traditional key signatures are
 * supported. The optional number attribute refers to staff numbers. If absent, the key signature
 * applies to all staves in the part. Key signatures appear at the start of each system unless the
 * print-object attribute has been set to "no".
 */
/* One child element of MxKey: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxCancel *cancel;
    MxFifths *fifths;
    MxMode mode;
    MxStep *key_step;
    MxSemitones *key_alter;
    MxKeyAccidental *key_accidental;
    MxKeyOctave *key_octave;
} MxKeyChild;

typedef struct {
    bool has_number;
    MxStaffNumber number; /* attribute number */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_id;
    char *id; /* attribute id */
    MxKeyChild *children; /* child elements in document order */
    size_t children_count;
} MxKey;

/* NULL on error; the message is in mx_error(). */
MxKey *mx_key_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_key_serialize(const MxKey *m, xmlNodePtr parent, const char *tag);
void mx_key_free(MxKey *m);

#endif /* MX_KEY_H_INCLUDED */
