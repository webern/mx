/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_DYNAMICS_H_INCLUDED
#define MX_DYNAMICS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_empty.h"
#include "mx_enclosure_shape.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_left_center_right.h"
#include "mx_number_of_lines.h"
#include "mx_other_text.h"
#include "mx_tenths.h"
#include "mx_valign.h"

/*
 * Dynamics can be associated either with a note or a general musical direction. To avoid
 * inconsistencies between and amongst the letter abbreviations for dynamics (what is sf vs. sfz,
 * standing alone or with a trailing dynamic that is not always piano), we use the actual letters as
 * the names of these dynamic elements. The other-dynamics element allows other dynamic marks that
 * are not covered here, but many of those should perhaps be included in a more general musical
 * direction element. Dynamics elements may also be combined to create marks not covered by a single
 * element, such as sfmp. These letter dynamic symbols are separated from crescendo, decrescendo,
 * and wedge indications. Dynamic representation is inconsistent in scores. Many things are assumed
 * by the composer and left out, such as returns to original dynamics. Systematic representations
 * are quite complex: for example, Humdrum has at least 3 representation formats related to
 * dynamics. The MusicXML format captures what is in the score, but does not try to be optimal for
 * analysis or synthesis of dynamics.
 */
/* One child element of MxDynamics: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxEmpty *p;
    MxEmpty *pp;
    MxEmpty *ppp;
    MxEmpty *pppp;
    MxEmpty *ppppp;
    MxEmpty *pppppp;
    MxEmpty *f;
    MxEmpty *ff;
    MxEmpty *fff;
    MxEmpty *ffff;
    MxEmpty *fffff;
    MxEmpty *ffffff;
    MxEmpty *mp;
    MxEmpty *mf;
    MxEmpty *sf;
    MxEmpty *sfp;
    MxEmpty *sfpp;
    MxEmpty *fp;
    MxEmpty *rf;
    MxEmpty *rfz;
    MxEmpty *sfz;
    MxEmpty *sffz;
    MxEmpty *fz;
    MxEmpty *n;
    MxEmpty *pf;
    MxEmpty *sfzp;
    MxOtherText *other_dynamics;
} MxDynamicsChild;

typedef struct {
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValign valign; /* attribute valign */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_underline;
    MxNumberOfLines underline; /* attribute underline */
    bool has_overline;
    MxNumberOfLines overline; /* attribute overline */
    bool has_line_through;
    MxNumberOfLines line_through; /* attribute line-through */
    bool has_enclosure;
    MxEnclosureShape enclosure; /* attribute enclosure */
    bool has_id;
    char *id; /* attribute id */
    MxDynamicsChild *children; /* child elements in document order */
    size_t children_count;
} MxDynamics;

/* NULL on error; the message is in mx_error(). */
MxDynamics *mx_dynamics_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_dynamics_serialize(const MxDynamics *m, xmlNodePtr parent, const char *tag);
void mx_dynamics_free(MxDynamics *m);

#endif /* MX_DYNAMICS_H_INCLUDED */
