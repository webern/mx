/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_HARMONIC_H_INCLUDED
#define MX_HARMONIC_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_empty.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_tenths.h"
#include "mx_yes_no.h"

/*
 * The harmonic type indicates natural and artificial harmonics. Allowing the type of pitch to be
 * specified, combined with controls for appearance/playback differences, allows both the notation
 * and the sound to be represented. Artificial harmonics can add a notated touching-pitch;
 * artificial pinch harmonics will usually not notate a touching pitch. The attributes for the
 * harmonic element refer to the use of the circular harmonic symbol, typically but not always used
 * with natural harmonics.
 */
/* One child element of MxHarmonic: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxEmpty *natural;
    MxEmpty *artificial;
    MxEmpty *base_pitch;
    MxEmpty *touching_pitch;
    MxEmpty *sounding_pitch;
} MxHarmonicChild;

typedef struct {
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    MxHarmonicChild *children; /* child elements in document order */
    size_t children_count;
} MxHarmonic;

/* NULL on error; the message is in mx_error(). */
MxHarmonic *mx_harmonic_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_harmonic_serialize(const MxHarmonic *m, xmlNodePtr parent, const char *tag);
void mx_harmonic_free(MxHarmonic *m);

#endif /* MX_HARMONIC_H_INCLUDED */
