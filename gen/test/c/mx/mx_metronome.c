/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_metronome.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxMetronome *mx_metronome_parse(xmlNodePtr el) {
    MxMetronome *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "parentheses") == 0) {
            m->has_parentheses = true;
            m->parentheses = mx_yes_no_parse(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "halign") == 0) {
            m->has_halign = true;
            m->halign = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "valign") == 0) {
            m->has_valign = true;
            m->valign = mx_valign_parse(s);
        } else if (strcmp(aname, "justify") == 0) {
            m->has_justify = true;
            m->justify = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_metronome_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxMetronomeChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "beat-unit") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->beat_unit = malloc(sizeof(*ch->beat_unit));
            if (!ch->beat_unit)
                abort();
            *ch->beat_unit = mx_note_type_value_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "beat-unit-dot") == 0) {
            ch->beat_unit_dot = mx_empty_parse(c);
            if (!ch->beat_unit_dot) {
                mx_metronome_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "beat-unit-tied") == 0) {
            ch->beat_unit_tied = mx_beat_unit_tied_parse(c);
            if (!ch->beat_unit_tied) {
                mx_metronome_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "per-minute") == 0) {
            ch->per_minute = mx_per_minute_parse(c);
            if (!ch->per_minute) {
                mx_metronome_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome-arrows") == 0) {
            ch->metronome_arrows = mx_empty_parse(c);
            if (!ch->metronome_arrows) {
                mx_metronome_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome-note") == 0) {
            ch->metronome_note = mx_metronome_note_parse(c);
            if (!ch->metronome_note) {
                mx_metronome_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome-relation") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->metronome_relation = mx_strdup(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_metronome_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_metronome_serialize(const MxMetronome *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_parentheses) {
        xmlSetProp(el, BAD_CAST "parentheses", BAD_CAST mx_yes_no_to_string(m->parentheses));
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_halign) {
        xmlSetProp(el, BAD_CAST "halign", BAD_CAST mx_left_center_right_to_string(m->halign));
    }
    if (m->has_valign) {
        xmlSetProp(el, BAD_CAST "valign", BAD_CAST mx_valign_to_string(m->valign));
    }
    if (m->has_justify) {
        xmlSetProp(el, BAD_CAST "justify", BAD_CAST mx_left_center_right_to_string(m->justify));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxMetronomeChild *ch = &m->children[i];
        if (ch->beat_unit) {
            xmlNewTextChild(el, NULL, BAD_CAST "beat-unit", BAD_CAST mx_note_type_value_to_string((*ch->beat_unit)));
        } else if (ch->beat_unit_dot) {
            mx_empty_serialize(ch->beat_unit_dot, el, "beat-unit-dot");
        } else if (ch->beat_unit_tied) {
            mx_beat_unit_tied_serialize(ch->beat_unit_tied, el, "beat-unit-tied");
        } else if (ch->per_minute) {
            mx_per_minute_serialize(ch->per_minute, el, "per-minute");
        } else if (ch->metronome_arrows) {
            mx_empty_serialize(ch->metronome_arrows, el, "metronome-arrows");
        } else if (ch->metronome_note) {
            mx_metronome_note_serialize(ch->metronome_note, el, "metronome-note");
        } else if (ch->metronome_relation) {
            xmlNewTextChild(el, NULL, BAD_CAST "metronome-relation", BAD_CAST ch->metronome_relation);
        }
    }
    return el;
}

void mx_metronome_free(MxMetronome *m) {
    if (!m)
        return;
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxMetronomeChild *ch = &m->children[i];
        free(ch->beat_unit);
        if (ch->beat_unit_dot)
            mx_empty_free(ch->beat_unit_dot);
        if (ch->beat_unit_tied)
            mx_beat_unit_tied_free(ch->beat_unit_tied);
        if (ch->per_minute)
            mx_per_minute_free(ch->per_minute);
        if (ch->metronome_arrows)
            mx_empty_free(ch->metronome_arrows);
        if (ch->metronome_note)
            mx_metronome_note_free(ch->metronome_note);
        free(ch->metronome_relation);
    }
    free(m->children);
    free(m);
}
