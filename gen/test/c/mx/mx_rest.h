/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_REST_H_INCLUDED
#define MX_REST_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_octave.h"
#include "mx_step.h"
#include "mx_yes_no.h"

/*
 * The rest element indicates notated rests or silences. Rest elements are usually empty, but
 * placement on the staff can be specified using display-step and display-octave elements. If the
 * measure attribute is set to yes, this indicates this is a complete measure rest.
 */
/* One child element of MxRest: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStep *display_step;
    MxOctave *display_octave;
} MxRestChild;

typedef struct {
    bool has_measure;
    MxYesNo measure; /* attribute measure */
    MxRestChild *children; /* child elements in document order */
    size_t children_count;
} MxRest;

/* NULL on error; the message is in mx_error(). */
MxRest *mx_rest_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_rest_serialize(const MxRest *m, xmlNodePtr parent, const char *tag);
void mx_rest_free(MxRest *m);

#endif /* MX_REST_H_INCLUDED */
