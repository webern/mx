/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PLAY_H_INCLUDED
#define MX_PLAY_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_mute.h"
#include "mx_other_play.h"
#include "mx_semi_pitched.h"

/*
 * The play type, new in Version 3.0, specifies playback techniques to be used in conjunction with
 * the instrument-sound element. When used as part of a sound element, it applies to all notes going
 * forward in score order. In multi-instrument parts, the affected instrument should be specified
 * using the id attribute. When used as part of a note element, it applies to the current note only.
 */
/* One child element of MxPlay: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    char *ipa;
    MxMute *mute;
    MxSemiPitched *semi_pitched;
    MxOtherPlay *other_play;
} MxPlayChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxPlayChild *children; /* child elements in document order */
    size_t children_count;
} MxPlay;

/* NULL on error; the message is in mx_error(). */
MxPlay *mx_play_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_play_serialize(const MxPlay *m, xmlNodePtr parent, const char *tag);
void mx_play_free(MxPlay *m);

#endif /* MX_PLAY_H_INCLUDED */
