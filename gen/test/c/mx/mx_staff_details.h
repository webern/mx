/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_STAFF_DETAILS_H_INCLUDED
#define MX_STAFF_DETAILS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_non_negative_decimal.h"
#include "mx_show_frets.h"
#include "mx_staff_number.h"
#include "mx_staff_tuning.h"
#include "mx_staff_type.h"
#include "mx_yes_no.h"

/*
 * The staff-details element is used to indicate different types of staves. The optional number
 * attribute specifies the staff number from top to bottom on the system, as with clef. The
 * print-object attribute is used to indicate when a staff is not printed in a part, usually in
 * large scores where empty parts are omitted. It is yes by default. If print-spacing is yes while
 * print-object is no, the score is printed in cutaway format where vertical space is left for the
 * empty part.
 */
/* One child element of MxStaffDetails: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStaffType *staff_type;
    long *staff_lines;
    MxStaffTuning *staff_tuning;
    long *capo;
    MxNonNegativeDecimal *staff_size;
} MxStaffDetailsChild;

typedef struct {
    bool has_number;
    MxStaffNumber number; /* attribute number */
    bool has_show_frets;
    MxShowFrets show_frets; /* attribute show-frets */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_print_spacing;
    MxYesNo print_spacing; /* attribute print-spacing */
    MxStaffDetailsChild *children; /* child elements in document order */
    size_t children_count;
} MxStaffDetails;

/* NULL on error; the message is in mx_error(). */
MxStaffDetails *mx_staff_details_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_staff_details_serialize(const MxStaffDetails *m, xmlNodePtr parent, const char *tag);
void mx_staff_details_free(MxStaffDetails *m);

#endif /* MX_STAFF_DETAILS_H_INCLUDED */
