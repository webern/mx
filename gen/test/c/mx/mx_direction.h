/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_DIRECTION_H_INCLUDED
#define MX_DIRECTION_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_direction_type.h"
#include "mx_formatted_text.h"
#include "mx_level.h"
#include "mx_offset.h"
#include "mx_sound.h"
#include "mx_yes_no.h"

/*
 * A direction is a musical indication that is not necessarily attached to a specific note. Two or
 * more may be combined to indicate starts and stops of wedges, dashes, etc. For applications where
 * a specific direction is indeed attached to a specific note, the direction element can be
 * associated with the note element that follows it in score order that is not in a different voice.
 * By default, a series of direction-type elements and a series of child elements of a
 * direction-type within a single direction element follow one another in sequence visually. For a
 * series of direction-type children, non-positional formatting attributes are carried over from the
 * previous element by default.
 */
/* One child element of MxDirection: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxDirectionType *direction_type;
    MxOffset *offset;
    MxFormattedText *footnote;
    MxLevel *level;
    char *voice;
    long *staff;
    MxSound *sound;
} MxDirectionChild;

typedef struct {
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_directive;
    MxYesNo directive; /* attribute directive */
    bool has_id;
    char *id; /* attribute id */
    MxDirectionChild *children; /* child elements in document order */
    size_t children_count;
} MxDirection;

/* NULL on error; the message is in mx_error(). */
MxDirection *mx_direction_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_direction_serialize(const MxDirection *m, xmlNodePtr parent, const char *tag);
void mx_direction_free(MxDirection *m);

#endif /* MX_DIRECTION_H_INCLUDED */
