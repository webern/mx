/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_direction.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxDirection *mx_direction_parse(xmlNodePtr el) {
    MxDirection *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "placement") == 0) {
            m->has_placement = true;
            m->placement = mx_above_below_parse(s);
        } else if (strcmp(aname, "directive") == 0) {
            m->has_directive = true;
            m->directive = mx_yes_no_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_direction_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxDirectionChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "direction-type") == 0) {
            ch->direction_type = mx_direction_type_parse(c);
            if (!ch->direction_type) {
                mx_direction_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "offset") == 0) {
            ch->offset = mx_offset_parse(c);
            if (!ch->offset) {
                mx_direction_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_direction_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_direction_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "voice") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->voice = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "staff") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staff = malloc(sizeof(*ch->staff));
            if (!ch->staff)
                abort();
            *ch->staff = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "sound") == 0) {
            ch->sound = mx_sound_parse(c);
            if (!ch->sound) {
                mx_direction_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_direction_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_direction_serialize(const MxDirection *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_placement) {
        xmlSetProp(el, BAD_CAST "placement", BAD_CAST mx_above_below_to_string(m->placement));
    }
    if (m->has_directive) {
        xmlSetProp(el, BAD_CAST "directive", BAD_CAST mx_yes_no_to_string(m->directive));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxDirectionChild *ch = &m->children[i];
        if (ch->direction_type) {
            mx_direction_type_serialize(ch->direction_type, el, "direction-type");
        } else if (ch->offset) {
            mx_offset_serialize(ch->offset, el, "offset");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        } else if (ch->voice) {
            xmlNewTextChild(el, NULL, BAD_CAST "voice", BAD_CAST ch->voice);
        } else if (ch->staff) {
            char *s = mx_format_int((*ch->staff));
            xmlNewTextChild(el, NULL, BAD_CAST "staff", BAD_CAST s);
            free(s);
        } else if (ch->sound) {
            mx_sound_serialize(ch->sound, el, "sound");
        }
    }
    return el;
}

void mx_direction_free(MxDirection *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxDirectionChild *ch = &m->children[i];
        if (ch->direction_type)
            mx_direction_type_free(ch->direction_type);
        if (ch->offset)
            mx_offset_free(ch->offset);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
        free(ch->voice);
        free(ch->staff);
        if (ch->sound)
            mx_sound_free(ch->sound);
    }
    free(m->children);
    free(m);
}
