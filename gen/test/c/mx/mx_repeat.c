/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_repeat.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxRepeat *mx_repeat_parse(xmlNodePtr el) {
    MxRepeat *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "direction") == 0) {
            m->has_direction = true;
            m->direction = mx_backward_forward_parse(s);
        } else if (strcmp(aname, "times") == 0) {
            m->has_times = true;
            m->times = mx_parse_int(s);
        } else if (strcmp(aname, "winged") == 0) {
            m->has_winged = true;
            m->winged = mx_winged_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_repeat_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_repeat_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_repeat_serialize(const MxRepeat *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_direction) {
        xmlSetProp(el, BAD_CAST "direction", BAD_CAST mx_backward_forward_to_string(m->direction));
    }
    if (m->has_times) {
        char *s = mx_format_int(m->times);
        xmlSetProp(el, BAD_CAST "times", BAD_CAST s);
        free(s);
    }
    if (m->has_winged) {
        xmlSetProp(el, BAD_CAST "winged", BAD_CAST mx_winged_to_string(m->winged));
    }
    return el;
}

void mx_repeat_free(MxRepeat *m) {
    if (!m)
        return;
    free(m);
}
