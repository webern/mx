/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_LYRIC_H_INCLUDED
#define MX_LYRIC_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_above_below.h"
#include "mx_color.h"
#include "mx_elision.h"
#include "mx_empty.h"
#include "mx_extend.h"
#include "mx_formatted_text.h"
#include "mx_left_center_right.h"
#include "mx_level.h"
#include "mx_syllabic.h"
#include "mx_tenths.h"
#include "mx_text_element_data.h"
#include "mx_time_only.h"
#include "mx_yes_no.h"

/*
 * The lyric type represents text underlays for lyrics, based on Humdrum with support for other
 * formats. Two text elements that are not separated by an elision element are part of the same
 * syllable, but may have different text formatting. The MusicXML XSD is more strict than the DTD in
 * enforcing this by disallowing a second syllabic element unless preceded by an elision element.
 * The lyric number indicates multiple lines, though a name can be used as well (as in Finale's
 * verse / chorus / section specification). Justification is center by default; placement is below
 * by default. The print-object attribute can override a note's print-lyric attribute in cases where
 * only some lyrics on a note are printed, as when lyrics for later verses are printed in a block of
 * text rather than with each note. The time-only attribute precisely specifies which lyrics are to
 * be sung which time through a repeated section.
 */
/* One child element of MxLyric: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxSyllabic *syllabic;
    MxTextElementData *text;
    MxElision *elision;
    MxExtend *extend;
    MxEmpty *laughing;
    MxEmpty *humming;
    MxEmpty *end_line;
    MxEmpty *end_paragraph;
    MxFormattedText *footnote;
    MxLevel *level;
} MxLyricChild;

typedef struct {
    bool has_number;
    char *number; /* attribute number */
    bool has_name;
    char *name; /* attribute name */
    bool has_time_only;
    MxTimeOnly time_only; /* attribute time-only */
    bool has_justify;
    MxLeftCenterRight justify; /* attribute justify */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_placement;
    MxAboveBelow placement; /* attribute placement */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_id;
    char *id; /* attribute id */
    MxLyricChild *children; /* child elements in document order */
    size_t children_count;
} MxLyric;

/* NULL on error; the message is in mx_error(). */
MxLyric *mx_lyric_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_lyric_serialize(const MxLyric *m, xmlNodePtr parent, const char *tag);
void mx_lyric_free(MxLyric *m);

#endif /* MX_LYRIC_H_INCLUDED */
