/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_note.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxNote *mx_note_parse(xmlNodePtr el) {
    MxNote *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "print-leger") == 0) {
            m->has_print_leger = true;
            m->print_leger = mx_yes_no_parse(s);
        } else if (strcmp(aname, "dynamics") == 0) {
            m->has_dynamics = true;
            m->dynamics = mx_non_negative_decimal_parse(s);
        } else if (strcmp(aname, "end-dynamics") == 0) {
            m->has_end_dynamics = true;
            m->end_dynamics = mx_non_negative_decimal_parse(s);
        } else if (strcmp(aname, "attack") == 0) {
            m->has_attack = true;
            m->attack = mx_divisions_parse(s);
        } else if (strcmp(aname, "release") == 0) {
            m->has_release = true;
            m->release = mx_divisions_parse(s);
        } else if (strcmp(aname, "time-only") == 0) {
            m->has_time_only = true;
            m->time_only = mx_time_only_parse(s);
        } else if (strcmp(aname, "pizzicato") == 0) {
            m->has_pizzicato = true;
            m->pizzicato = mx_yes_no_parse(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "print-dot") == 0) {
            m->has_print_dot = true;
            m->print_dot = mx_yes_no_parse(s);
        } else if (strcmp(aname, "print-lyric") == 0) {
            m->has_print_lyric = true;
            m->print_lyric = mx_yes_no_parse(s);
        } else if (strcmp(aname, "print-object") == 0) {
            m->has_print_object = true;
            m->print_object = mx_yes_no_parse(s);
        } else if (strcmp(aname, "print-spacing") == 0) {
            m->has_print_spacing = true;
            m->print_spacing = mx_yes_no_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_note_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxNoteChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "grace") == 0) {
            ch->grace = mx_grace_parse(c);
            if (!ch->grace) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "chord") == 0) {
            ch->chord = mx_empty_parse(c);
            if (!ch->chord) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pitch") == 0) {
            ch->pitch = mx_pitch_parse(c);
            if (!ch->pitch) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "unpitched") == 0) {
            ch->unpitched = mx_unpitched_parse(c);
            if (!ch->unpitched) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "rest") == 0) {
            ch->rest = mx_rest_parse(c);
            if (!ch->rest) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tie") == 0) {
            ch->tie = mx_tie_parse(c);
            if (!ch->tie) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "cue") == 0) {
            ch->cue = mx_empty_parse(c);
            if (!ch->cue) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "duration") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->duration = malloc(sizeof(*ch->duration));
            if (!ch->duration)
                abort();
            *ch->duration = mx_positive_divisions_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "instrument") == 0) {
            ch->instrument = mx_instrument_parse(c);
            if (!ch->instrument) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "voice") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->voice = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "type") == 0) {
            ch->type = mx_note_type_parse(c);
            if (!ch->type) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "dot") == 0) {
            ch->dot = mx_empty_placement_parse(c);
            if (!ch->dot) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "accidental") == 0) {
            ch->accidental = mx_accidental_parse(c);
            if (!ch->accidental) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "time-modification") == 0) {
            ch->time_modification = mx_time_modification_parse(c);
            if (!ch->time_modification) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "stem") == 0) {
            ch->stem = mx_stem_parse(c);
            if (!ch->stem) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "notehead") == 0) {
            ch->notehead = mx_notehead_parse(c);
            if (!ch->notehead) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "notehead-text") == 0) {
            ch->notehead_text = mx_notehead_text_parse(c);
            if (!ch->notehead_text) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staff") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staff = malloc(sizeof(*ch->staff));
            if (!ch->staff)
                abort();
            *ch->staff = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "beam") == 0) {
            ch->beam = mx_beam_parse(c);
            if (!ch->beam) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "notations") == 0) {
            ch->notations = mx_notations_parse(c);
            if (!ch->notations) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "lyric") == 0) {
            ch->lyric = mx_lyric_parse(c);
            if (!ch->lyric) {
                mx_note_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "play") == 0) {
            ch->play = mx_play_parse(c);
            if (!ch->play) {
                mx_note_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_note_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_note_serialize(const MxNote *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_print_leger) {
        xmlSetProp(el, BAD_CAST "print-leger", BAD_CAST mx_yes_no_to_string(m->print_leger));
    }
    if (m->has_dynamics) {
        char *s = mx_non_negative_decimal_to_string(m->dynamics);
        xmlSetProp(el, BAD_CAST "dynamics", BAD_CAST s);
        free(s);
    }
    if (m->has_end_dynamics) {
        char *s = mx_non_negative_decimal_to_string(m->end_dynamics);
        xmlSetProp(el, BAD_CAST "end-dynamics", BAD_CAST s);
        free(s);
    }
    if (m->has_attack) {
        char *s = mx_divisions_to_string(m->attack);
        xmlSetProp(el, BAD_CAST "attack", BAD_CAST s);
        free(s);
    }
    if (m->has_release) {
        char *s = mx_divisions_to_string(m->release);
        xmlSetProp(el, BAD_CAST "release", BAD_CAST s);
        free(s);
    }
    if (m->has_time_only) {
        xmlSetProp(el, BAD_CAST "time-only", BAD_CAST m->time_only);
    }
    if (m->has_pizzicato) {
        xmlSetProp(el, BAD_CAST "pizzicato", BAD_CAST mx_yes_no_to_string(m->pizzicato));
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_print_dot) {
        xmlSetProp(el, BAD_CAST "print-dot", BAD_CAST mx_yes_no_to_string(m->print_dot));
    }
    if (m->has_print_lyric) {
        xmlSetProp(el, BAD_CAST "print-lyric", BAD_CAST mx_yes_no_to_string(m->print_lyric));
    }
    if (m->has_print_object) {
        xmlSetProp(el, BAD_CAST "print-object", BAD_CAST mx_yes_no_to_string(m->print_object));
    }
    if (m->has_print_spacing) {
        xmlSetProp(el, BAD_CAST "print-spacing", BAD_CAST mx_yes_no_to_string(m->print_spacing));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxNoteChild *ch = &m->children[i];
        if (ch->grace) {
            mx_grace_serialize(ch->grace, el, "grace");
        } else if (ch->chord) {
            mx_empty_serialize(ch->chord, el, "chord");
        } else if (ch->pitch) {
            mx_pitch_serialize(ch->pitch, el, "pitch");
        } else if (ch->unpitched) {
            mx_unpitched_serialize(ch->unpitched, el, "unpitched");
        } else if (ch->rest) {
            mx_rest_serialize(ch->rest, el, "rest");
        } else if (ch->tie) {
            mx_tie_serialize(ch->tie, el, "tie");
        } else if (ch->cue) {
            mx_empty_serialize(ch->cue, el, "cue");
        } else if (ch->duration) {
            char *s = mx_positive_divisions_to_string((*ch->duration));
            xmlNewTextChild(el, NULL, BAD_CAST "duration", BAD_CAST s);
            free(s);
        } else if (ch->instrument) {
            mx_instrument_serialize(ch->instrument, el, "instrument");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        } else if (ch->voice) {
            xmlNewTextChild(el, NULL, BAD_CAST "voice", BAD_CAST ch->voice);
        } else if (ch->type) {
            mx_note_type_serialize(ch->type, el, "type");
        } else if (ch->dot) {
            mx_empty_placement_serialize(ch->dot, el, "dot");
        } else if (ch->accidental) {
            mx_accidental_serialize(ch->accidental, el, "accidental");
        } else if (ch->time_modification) {
            mx_time_modification_serialize(ch->time_modification, el, "time-modification");
        } else if (ch->stem) {
            mx_stem_serialize(ch->stem, el, "stem");
        } else if (ch->notehead) {
            mx_notehead_serialize(ch->notehead, el, "notehead");
        } else if (ch->notehead_text) {
            mx_notehead_text_serialize(ch->notehead_text, el, "notehead-text");
        } else if (ch->staff) {
            char *s = mx_format_int((*ch->staff));
            xmlNewTextChild(el, NULL, BAD_CAST "staff", BAD_CAST s);
            free(s);
        } else if (ch->beam) {
            mx_beam_serialize(ch->beam, el, "beam");
        } else if (ch->notations) {
            mx_notations_serialize(ch->notations, el, "notations");
        } else if (ch->lyric) {
            mx_lyric_serialize(ch->lyric, el, "lyric");
        } else if (ch->play) {
            mx_play_serialize(ch->play, el, "play");
        }
    }
    return el;
}

void mx_note_free(MxNote *m) {
    if (!m)
        return;
    if (m->has_time_only)
        free(m->time_only);
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxNoteChild *ch = &m->children[i];
        if (ch->grace)
            mx_grace_free(ch->grace);
        if (ch->chord)
            mx_empty_free(ch->chord);
        if (ch->pitch)
            mx_pitch_free(ch->pitch);
        if (ch->unpitched)
            mx_unpitched_free(ch->unpitched);
        if (ch->rest)
            mx_rest_free(ch->rest);
        if (ch->tie)
            mx_tie_free(ch->tie);
        if (ch->cue)
            mx_empty_free(ch->cue);
        free(ch->duration);
        if (ch->instrument)
            mx_instrument_free(ch->instrument);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
        free(ch->voice);
        if (ch->type)
            mx_note_type_free(ch->type);
        if (ch->dot)
            mx_empty_placement_free(ch->dot);
        if (ch->accidental)
            mx_accidental_free(ch->accidental);
        if (ch->time_modification)
            mx_time_modification_free(ch->time_modification);
        if (ch->stem)
            mx_stem_free(ch->stem);
        if (ch->notehead)
            mx_notehead_free(ch->notehead);
        if (ch->notehead_text)
            mx_notehead_text_free(ch->notehead_text);
        free(ch->staff);
        if (ch->beam)
            mx_beam_free(ch->beam);
        if (ch->notations)
            mx_notations_free(ch->notations);
        if (ch->lyric)
            mx_lyric_free(ch->lyric);
        if (ch->play)
            mx_play_free(ch->play);
    }
    free(m->children);
    free(m);
}
