/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_WORK_H_INCLUDED
#define MX_WORK_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_opus.h"

/*
 * Works are optionally identified by number and title. The work type also may indicate a link to
 * the opus document that composes multiple scores into a collection.
 */
/* One child element of MxWork: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    char *work_number;
    char *work_title;
    MxOpus *opus;
} MxWorkChild;

typedef struct {
    MxWorkChild *children; /* child elements in document order */
    size_t children_count;
} MxWork;

/* NULL on error; the message is in mx_error(). */
MxWork *mx_work_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_work_serialize(const MxWork *m, xmlNodePtr parent, const char *tag);
void mx_work_free(MxWork *m);

#endif /* MX_WORK_H_INCLUDED */
