/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_appearance.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxAppearance *mx_appearance_parse(xmlNodePtr el) {
    MxAppearance *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_appearance_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxAppearanceChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "line-width") == 0) {
            ch->line_width = mx_line_width_parse(c);
            if (!ch->line_width) {
                mx_appearance_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "note-size") == 0) {
            ch->note_size = mx_note_size_parse(c);
            if (!ch->note_size) {
                mx_appearance_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "distance") == 0) {
            ch->distance = mx_distance_parse(c);
            if (!ch->distance) {
                mx_appearance_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "glyph") == 0) {
            ch->glyph = mx_glyph_parse(c);
            if (!ch->glyph) {
                mx_appearance_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-appearance") == 0) {
            ch->other_appearance = mx_other_appearance_parse(c);
            if (!ch->other_appearance) {
                mx_appearance_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_appearance_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_appearance_serialize(const MxAppearance *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxAppearanceChild *ch = &m->children[i];
        if (ch->line_width) {
            mx_line_width_serialize(ch->line_width, el, "line-width");
        } else if (ch->note_size) {
            mx_note_size_serialize(ch->note_size, el, "note-size");
        } else if (ch->distance) {
            mx_distance_serialize(ch->distance, el, "distance");
        } else if (ch->glyph) {
            mx_glyph_serialize(ch->glyph, el, "glyph");
        } else if (ch->other_appearance) {
            mx_other_appearance_serialize(ch->other_appearance, el, "other-appearance");
        }
    }
    return el;
}

void mx_appearance_free(MxAppearance *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxAppearanceChild *ch = &m->children[i];
        if (ch->line_width)
            mx_line_width_free(ch->line_width);
        if (ch->note_size)
            mx_note_size_free(ch->note_size);
        if (ch->distance)
            mx_distance_free(ch->distance);
        if (ch->glyph)
            mx_glyph_free(ch->glyph);
        if (ch->other_appearance)
            mx_other_appearance_free(ch->other_appearance);
    }
    free(m->children);
    free(m);
}
