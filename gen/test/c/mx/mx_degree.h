/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_DEGREE_H_INCLUDED
#define MX_DEGREE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_degree_alter.h"
#include "mx_degree_type.h"
#include "mx_degree_value.h"
#include "mx_yes_no.h"

/*
 * The degree type is used to add, alter, or subtract individual notes in the chord. The
 * print-object attribute can be used to keep the degree from printing separately when it has
 * already taken into account in the text attribute of the kind element. The degree-value and
 * degree-type text attributes specify how the value and type of the degree should be displayed. A
 * harmony of kind "other" can be spelled explicitly by using a series of degree elements together
 * with a root.
 */
/* One child element of MxDegree: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxDegreeValue *degree_value;
    MxDegreeAlter *degree_alter;
    MxDegreeType *degree_type;
} MxDegreeChild;

typedef struct {
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    MxDegreeChild *children; /* child elements in document order */
    size_t children_count;
} MxDegree;

/* NULL on error; the message is in mx_error(). */
MxDegree *mx_degree_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_degree_serialize(const MxDegree *m, xmlNodePtr parent, const char *tag);
void mx_degree_free(MxDegree *m);

#endif /* MX_DEGREE_H_INCLUDED */
