/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_stick_location.h"

#include <string.h>

static const char *const mx_stick_location_values[] = {
    "center",
    "rim",
    "cymbal bell",
    "cymbal edge",
};

bool mx_stick_location_try_parse(const char *s, MxStickLocation *out) {
    if (!s)
        s = "";
    for (size_t i = 0; i < sizeof(mx_stick_location_values) / sizeof(mx_stick_location_values[0]); i++) {
        if (strcmp(s, mx_stick_location_values[i]) == 0) {
            *out = (MxStickLocation)i;
            return true;
        }
    }
    *out = (MxStickLocation)0;
    return false;
}

MxStickLocation mx_stick_location_parse(const char *s) {
    MxStickLocation v;
    mx_stick_location_try_parse(s, &v);
    return v;
}

const char *mx_stick_location_to_string(MxStickLocation v) {
    if ((size_t)v >= sizeof(mx_stick_location_values) / sizeof(mx_stick_location_values[0]))
        return mx_stick_location_values[0];
    return mx_stick_location_values[v];
}
