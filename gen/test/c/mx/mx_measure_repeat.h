/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_MEASURE_REPEAT_H_INCLUDED
#define MX_MEASURE_REPEAT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_positive_integer_or_empty.h"
#include "mx_start_stop.h"

/*
 * The measure-repeat type is used for both single and multiple measure repeats. The text of the
 * element indicates the number of measures to be repeated in a single pattern. The slashes
 * attribute specifies the number of slashes to use in the repeat sign. It is 1 if not specified.
 * Both the start and the stop of the measure-repeat must be specified. The text of the element is
 * ignored when the type is stop. The measure-repeat element specifies a notation style for
 * repetitions. The actual music being repeated needs to be repeated within the MusicXML file. This
 * element specifies the notation that indicates the repeat.
 */
typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_slashes;
    long slashes; /* attribute slashes */
    MxPositiveIntegerOrEmpty value; /* text content */
} MxMeasureRepeat;

/* NULL on error; the message is in mx_error(). */
MxMeasureRepeat *mx_measure_repeat_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_measure_repeat_serialize(const MxMeasureRepeat *m, xmlNodePtr parent, const char *tag);
void mx_measure_repeat_free(MxMeasureRepeat *m);

#endif /* MX_MEASURE_REPEAT_H_INCLUDED */
