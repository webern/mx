/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PER_MINUTE_H_INCLUDED
#define MX_PER_MINUTE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"

/*
 * The per-minute type can be a number, or a text description including numbers. If a font is
 * specified, it overrides the font specified for the overall metronome element. This allows
 * separate specification of a music font for the beat-unit and a text font for the numeric value,
 * in cases where a single metronome font is not used.
 */
typedef struct {
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    char *value; /* text content */
} MxPerMinute;

/* NULL on error; the message is in mx_error(). */
MxPerMinute *mx_per_minute_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_per_minute_serialize(const MxPerMinute *m, xmlNodePtr parent, const char *tag);
void mx_per_minute_free(MxPerMinute *m);

#endif /* MX_PER_MINUTE_H_INCLUDED */
