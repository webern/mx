/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_staff_tuning.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxStaffTuning *mx_staff_tuning_parse(xmlNodePtr el) {
    MxStaffTuning *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "line") == 0) {
            m->has_line = true;
            m->line = mx_staff_line_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_staff_tuning_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxStaffTuningChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "tuning-step") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->tuning_step = malloc(sizeof(*ch->tuning_step));
            if (!ch->tuning_step)
                abort();
            *ch->tuning_step = mx_step_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "tuning-alter") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->tuning_alter = malloc(sizeof(*ch->tuning_alter));
            if (!ch->tuning_alter)
                abort();
            *ch->tuning_alter = mx_semitones_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "tuning-octave") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->tuning_octave = malloc(sizeof(*ch->tuning_octave));
            if (!ch->tuning_octave)
                abort();
            *ch->tuning_octave = mx_octave_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_staff_tuning_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_staff_tuning_serialize(const MxStaffTuning *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_line) {
        char *s = mx_staff_line_to_string(m->line);
        xmlSetProp(el, BAD_CAST "line", BAD_CAST s);
        free(s);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxStaffTuningChild *ch = &m->children[i];
        if (ch->tuning_step) {
            xmlNewTextChild(el, NULL, BAD_CAST "tuning-step", BAD_CAST mx_step_to_string((*ch->tuning_step)));
        } else if (ch->tuning_alter) {
            char *s = mx_semitones_to_string((*ch->tuning_alter));
            xmlNewTextChild(el, NULL, BAD_CAST "tuning-alter", BAD_CAST s);
            free(s);
        } else if (ch->tuning_octave) {
            char *s = mx_octave_to_string((*ch->tuning_octave));
            xmlNewTextChild(el, NULL, BAD_CAST "tuning-octave", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_staff_tuning_free(MxStaffTuning *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxStaffTuningChild *ch = &m->children[i];
        free(ch->tuning_step);
        free(ch->tuning_alter);
        free(ch->tuning_octave);
    }
    free(m->children);
    free(m);
}
