/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_STAFF_LAYOUT_H_INCLUDED
#define MX_STAFF_LAYOUT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_staff_number.h"
#include "mx_tenths.h"

/*
 * Staff layout includes the vertical distance from the bottom line of the previous staff in this
 * system to the top line of the staff specified by the number attribute. The optional number
 * attribute refers to staff numbers within the part, from top to bottom on the system. A value of 1
 * is assumed if not present. When used in the defaults element, the values apply to all parts. This
 * value is ignored for the first staff in a system.
 */
/* One child element of MxStaffLayout: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxTenths *staff_distance;
} MxStaffLayoutChild;

typedef struct {
    bool has_number;
    MxStaffNumber number; /* attribute number */
    MxStaffLayoutChild *children; /* child elements in document order */
    size_t children_count;
} MxStaffLayout;

/* NULL on error; the message is in mx_error(). */
MxStaffLayout *mx_staff_layout_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_staff_layout_serialize(const MxStaffLayout *m, xmlNodePtr parent, const char *tag);
void mx_staff_layout_free(MxStaffLayout *m);

#endif /* MX_STAFF_LAYOUT_H_INCLUDED */
