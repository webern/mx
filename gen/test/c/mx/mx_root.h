/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ROOT_H_INCLUDED
#define MX_ROOT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_root_alter.h"
#include "mx_root_step.h"

/*
 * The root type indicates a pitch like C, D, E vs. a function indication like I, II, III. It is
 * used with chord symbols in popular music. The root element has a root-step and optional
 * root-alter element similar to the step and alter elements, but renamed to distinguish the
 * different musical meanings.
 */
/* One child element of MxRoot: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxRootStep *root_step;
    MxRootAlter *root_alter;
} MxRootChild;

typedef struct {
    MxRootChild *children; /* child elements in document order */
    size_t children_count;
} MxRoot;

/* NULL on error; the message is in mx_error(). */
MxRoot *mx_root_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_root_serialize(const MxRoot *m, xmlNodePtr parent, const char *tag);
void mx_root_free(MxRoot *m);

#endif /* MX_ROOT_H_INCLUDED */
