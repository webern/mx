/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_dynamics.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxDynamics *mx_dynamics_parse(xmlNodePtr el) {
    MxDynamics *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "halign") == 0) {
            m->has_halign = true;
            m->halign = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "valign") == 0) {
            m->has_valign = true;
            m->valign = mx_valign_parse(s);
        } else if (strcmp(aname, "placement") == 0) {
            m->has_placement = true;
            m->placement = mx_above_below_parse(s);
        } else if (strcmp(aname, "underline") == 0) {
            m->has_underline = true;
            m->underline = mx_number_of_lines_parse(s);
        } else if (strcmp(aname, "overline") == 0) {
            m->has_overline = true;
            m->overline = mx_number_of_lines_parse(s);
        } else if (strcmp(aname, "line-through") == 0) {
            m->has_line_through = true;
            m->line_through = mx_number_of_lines_parse(s);
        } else if (strcmp(aname, "enclosure") == 0) {
            m->has_enclosure = true;
            m->enclosure = mx_enclosure_shape_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_dynamics_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxDynamicsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "p") == 0) {
            ch->p = mx_empty_parse(c);
            if (!ch->p) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pp") == 0) {
            ch->pp = mx_empty_parse(c);
            if (!ch->pp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ppp") == 0) {
            ch->ppp = mx_empty_parse(c);
            if (!ch->ppp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pppp") == 0) {
            ch->pppp = mx_empty_parse(c);
            if (!ch->pppp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ppppp") == 0) {
            ch->ppppp = mx_empty_parse(c);
            if (!ch->ppppp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pppppp") == 0) {
            ch->pppppp = mx_empty_parse(c);
            if (!ch->pppppp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "f") == 0) {
            ch->f = mx_empty_parse(c);
            if (!ch->f) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ff") == 0) {
            ch->ff = mx_empty_parse(c);
            if (!ch->ff) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fff") == 0) {
            ch->fff = mx_empty_parse(c);
            if (!ch->fff) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ffff") == 0) {
            ch->ffff = mx_empty_parse(c);
            if (!ch->ffff) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fffff") == 0) {
            ch->fffff = mx_empty_parse(c);
            if (!ch->fffff) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ffffff") == 0) {
            ch->ffffff = mx_empty_parse(c);
            if (!ch->ffffff) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "mp") == 0) {
            ch->mp = mx_empty_parse(c);
            if (!ch->mp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "mf") == 0) {
            ch->mf = mx_empty_parse(c);
            if (!ch->mf) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sf") == 0) {
            ch->sf = mx_empty_parse(c);
            if (!ch->sf) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sfp") == 0) {
            ch->sfp = mx_empty_parse(c);
            if (!ch->sfp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sfpp") == 0) {
            ch->sfpp = mx_empty_parse(c);
            if (!ch->sfpp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fp") == 0) {
            ch->fp = mx_empty_parse(c);
            if (!ch->fp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "rf") == 0) {
            ch->rf = mx_empty_parse(c);
            if (!ch->rf) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "rfz") == 0) {
            ch->rfz = mx_empty_parse(c);
            if (!ch->rfz) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sfz") == 0) {
            ch->sfz = mx_empty_parse(c);
            if (!ch->sfz) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sffz") == 0) {
            ch->sffz = mx_empty_parse(c);
            if (!ch->sffz) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fz") == 0) {
            ch->fz = mx_empty_parse(c);
            if (!ch->fz) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "n") == 0) {
            ch->n = mx_empty_parse(c);
            if (!ch->n) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pf") == 0) {
            ch->pf = mx_empty_parse(c);
            if (!ch->pf) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "sfzp") == 0) {
            ch->sfzp = mx_empty_parse(c);
            if (!ch->sfzp) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-dynamics") == 0) {
            ch->other_dynamics = mx_other_text_parse(c);
            if (!ch->other_dynamics) {
                mx_dynamics_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_dynamics_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_dynamics_serialize(const MxDynamics *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_halign) {
        xmlSetProp(el, BAD_CAST "halign", BAD_CAST mx_left_center_right_to_string(m->halign));
    }
    if (m->has_valign) {
        xmlSetProp(el, BAD_CAST "valign", BAD_CAST mx_valign_to_string(m->valign));
    }
    if (m->has_placement) {
        xmlSetProp(el, BAD_CAST "placement", BAD_CAST mx_above_below_to_string(m->placement));
    }
    if (m->has_underline) {
        char *s = mx_number_of_lines_to_string(m->underline);
        xmlSetProp(el, BAD_CAST "underline", BAD_CAST s);
        free(s);
    }
    if (m->has_overline) {
        char *s = mx_number_of_lines_to_string(m->overline);
        xmlSetProp(el, BAD_CAST "overline", BAD_CAST s);
        free(s);
    }
    if (m->has_line_through) {
        char *s = mx_number_of_lines_to_string(m->line_through);
        xmlSetProp(el, BAD_CAST "line-through", BAD_CAST s);
        free(s);
    }
    if (m->has_enclosure) {
        xmlSetProp(el, BAD_CAST "enclosure", BAD_CAST mx_enclosure_shape_to_string(m->enclosure));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxDynamicsChild *ch = &m->children[i];
        if (ch->p) {
            mx_empty_serialize(ch->p, el, "p");
        } else if (ch->pp) {
            mx_empty_serialize(ch->pp, el, "pp");
        } else if (ch->ppp) {
            mx_empty_serialize(ch->ppp, el, "ppp");
        } else if (ch->pppp) {
            mx_empty_serialize(ch->pppp, el, "pppp");
        } else if (ch->ppppp) {
            mx_empty_serialize(ch->ppppp, el, "ppppp");
        } else if (ch->pppppp) {
            mx_empty_serialize(ch->pppppp, el, "pppppp");
        } else if (ch->f) {
            mx_empty_serialize(ch->f, el, "f");
        } else if (ch->ff) {
            mx_empty_serialize(ch->ff, el, "ff");
        } else if (ch->fff) {
            mx_empty_serialize(ch->fff, el, "fff");
        } else if (ch->ffff) {
            mx_empty_serialize(ch->ffff, el, "ffff");
        } else if (ch->fffff) {
            mx_empty_serialize(ch->fffff, el, "fffff");
        } else if (ch->ffffff) {
            mx_empty_serialize(ch->ffffff, el, "ffffff");
        } else if (ch->mp) {
            mx_empty_serialize(ch->mp, el, "mp");
        } else if (ch->mf) {
            mx_empty_serialize(ch->mf, el, "mf");
        } else if (ch->sf) {
            mx_empty_serialize(ch->sf, el, "sf");
        } else if (ch->sfp) {
            mx_empty_serialize(ch->sfp, el, "sfp");
        } else if (ch->sfpp) {
            mx_empty_serialize(ch->sfpp, el, "sfpp");
        } else if (ch->fp) {
            mx_empty_serialize(ch->fp, el, "fp");
        } else if (ch->rf) {
            mx_empty_serialize(ch->rf, el, "rf");
        } else if (ch->rfz) {
            mx_empty_serialize(ch->rfz, el, "rfz");
        } else if (ch->sfz) {
            mx_empty_serialize(ch->sfz, el, "sfz");
        } else if (ch->sffz) {
            mx_empty_serialize(ch->sffz, el, "sffz");
        } else if (ch->fz) {
            mx_empty_serialize(ch->fz, el, "fz");
        } else if (ch->n) {
            mx_empty_serialize(ch->n, el, "n");
        } else if (ch->pf) {
            mx_empty_serialize(ch->pf, el, "pf");
        } else if (ch->sfzp) {
            mx_empty_serialize(ch->sfzp, el, "sfzp");
        } else if (ch->other_dynamics) {
            mx_other_text_serialize(ch->other_dynamics, el, "other-dynamics");
        }
    }
    return el;
}

void mx_dynamics_free(MxDynamics *m) {
    if (!m)
        return;
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxDynamicsChild *ch = &m->children[i];
        if (ch->p)
            mx_empty_free(ch->p);
        if (ch->pp)
            mx_empty_free(ch->pp);
        if (ch->ppp)
            mx_empty_free(ch->ppp);
        if (ch->pppp)
            mx_empty_free(ch->pppp);
        if (ch->ppppp)
            mx_empty_free(ch->ppppp);
        if (ch->pppppp)
            mx_empty_free(ch->pppppp);
        if (ch->f)
            mx_empty_free(ch->f);
        if (ch->ff)
            mx_empty_free(ch->ff);
        if (ch->fff)
            mx_empty_free(ch->fff);
        if (ch->ffff)
            mx_empty_free(ch->ffff);
        if (ch->fffff)
            mx_empty_free(ch->fffff);
        if (ch->ffffff)
            mx_empty_free(ch->ffffff);
        if (ch->mp)
            mx_empty_free(ch->mp);
        if (ch->mf)
            mx_empty_free(ch->mf);
        if (ch->sf)
            mx_empty_free(ch->sf);
        if (ch->sfp)
            mx_empty_free(ch->sfp);
        if (ch->sfpp)
            mx_empty_free(ch->sfpp);
        if (ch->fp)
            mx_empty_free(ch->fp);
        if (ch->rf)
            mx_empty_free(ch->rf);
        if (ch->rfz)
            mx_empty_free(ch->rfz);
        if (ch->sfz)
            mx_empty_free(ch->sfz);
        if (ch->sffz)
            mx_empty_free(ch->sffz);
        if (ch->fz)
            mx_empty_free(ch->fz);
        if (ch->n)
            mx_empty_free(ch->n);
        if (ch->pf)
            mx_empty_free(ch->pf);
        if (ch->sfzp)
            mx_empty_free(ch->sfzp);
        if (ch->other_dynamics)
            mx_other_text_free(ch->other_dynamics);
    }
    free(m->children);
    free(m);
}
