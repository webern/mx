/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_credit.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxCredit *mx_credit_parse(xmlNodePtr el) {
    MxCredit *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "page") == 0) {
            m->has_page = true;
            m->page = mx_parse_int(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_credit_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxCreditChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "credit-type") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->credit_type = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "link") == 0) {
            ch->link = mx_link_parse(c);
            if (!ch->link) {
                mx_credit_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "bookmark") == 0) {
            ch->bookmark = mx_bookmark_parse(c);
            if (!ch->bookmark) {
                mx_credit_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "credit-image") == 0) {
            ch->credit_image = mx_image_parse(c);
            if (!ch->credit_image) {
                mx_credit_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "credit-words") == 0) {
            ch->credit_words = mx_formatted_text_id_parse(c);
            if (!ch->credit_words) {
                mx_credit_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "credit-symbol") == 0) {
            ch->credit_symbol = mx_formatted_symbol_id_parse(c);
            if (!ch->credit_symbol) {
                mx_credit_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_credit_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_credit_serialize(const MxCredit *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_page) {
        char *s = mx_format_int(m->page);
        xmlSetProp(el, BAD_CAST "page", BAD_CAST s);
        free(s);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxCreditChild *ch = &m->children[i];
        if (ch->credit_type) {
            xmlNewTextChild(el, NULL, BAD_CAST "credit-type", BAD_CAST ch->credit_type);
        } else if (ch->link) {
            mx_link_serialize(ch->link, el, "link");
        } else if (ch->bookmark) {
            mx_bookmark_serialize(ch->bookmark, el, "bookmark");
        } else if (ch->credit_image) {
            mx_image_serialize(ch->credit_image, el, "credit-image");
        } else if (ch->credit_words) {
            mx_formatted_text_id_serialize(ch->credit_words, el, "credit-words");
        } else if (ch->credit_symbol) {
            mx_formatted_symbol_id_serialize(ch->credit_symbol, el, "credit-symbol");
        }
    }
    return el;
}

void mx_credit_free(MxCredit *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxCreditChild *ch = &m->children[i];
        free(ch->credit_type);
        if (ch->link)
            mx_link_free(ch->link);
        if (ch->bookmark)
            mx_bookmark_free(ch->bookmark);
        if (ch->credit_image)
            mx_image_free(ch->credit_image);
        if (ch->credit_words)
            mx_formatted_text_id_free(ch->credit_words);
        if (ch->credit_symbol)
            mx_formatted_symbol_id_free(ch->credit_symbol);
    }
    free(m->children);
    free(m);
}
