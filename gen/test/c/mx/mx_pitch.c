/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_pitch.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPitch *mx_pitch_parse(xmlNodePtr el) {
    MxPitch *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_pitch_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPitchChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "step") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->step = malloc(sizeof(*ch->step));
            if (!ch->step)
                abort();
            *ch->step = mx_step_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "alter") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->alter = malloc(sizeof(*ch->alter));
            if (!ch->alter)
                abort();
            *ch->alter = mx_semitones_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "octave") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->octave = malloc(sizeof(*ch->octave));
            if (!ch->octave)
                abort();
            *ch->octave = mx_octave_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_pitch_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_pitch_serialize(const MxPitch *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPitchChild *ch = &m->children[i];
        if (ch->step) {
            xmlNewTextChild(el, NULL, BAD_CAST "step", BAD_CAST mx_step_to_string((*ch->step)));
        } else if (ch->alter) {
            char *s = mx_semitones_to_string((*ch->alter));
            xmlNewTextChild(el, NULL, BAD_CAST "alter", BAD_CAST s);
            free(s);
        } else if (ch->octave) {
            char *s = mx_octave_to_string((*ch->octave));
            xmlNewTextChild(el, NULL, BAD_CAST "octave", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_pitch_free(MxPitch *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxPitchChild *ch = &m->children[i];
        free(ch->step);
        free(ch->alter);
        free(ch->octave);
    }
    free(m->children);
    free(m);
}
