/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_FIGURE_H_INCLUDED
#define MX_FIGURE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_extend.h"
#include "mx_formatted_text.h"
#include "mx_level.h"
#include "mx_style_text.h"

/*
 * The figure type represents a single figure within a figured-bass element.
 */
/* One child element of MxFigure: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStyleText *prefix;
    MxStyleText *figure_number;
    MxStyleText *suffix;
    MxExtend *extend;
    MxFormattedText *footnote;
    MxLevel *level;
} MxFigureChild;

typedef struct {
    MxFigureChild *children; /* child elements in document order */
    size_t children_count;
} MxFigure;

/* NULL on error; the message is in mx_error(). */
MxFigure *mx_figure_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_figure_serialize(const MxFigure *m, xmlNodePtr parent, const char *tag);
void mx_figure_free(MxFigure *m);

#endif /* MX_FIGURE_H_INCLUDED */
