/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_bookmark.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxBookmark *mx_bookmark_parse(xmlNodePtr el) {
    MxBookmark *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else if (strcmp(aname, "name") == 0) {
            m->has_name = true;
            m->name = mx_strdup(s);
        } else if (strcmp(aname, "element") == 0) {
            m->has_element = true;
            m->element = mx_strdup(s);
        } else if (strcmp(aname, "position") == 0) {
            m->has_position = true;
            m->position = mx_parse_int(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_bookmark_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_bookmark_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_bookmark_serialize(const MxBookmark *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    if (m->has_name) {
        xmlSetProp(el, BAD_CAST "name", BAD_CAST m->name);
    }
    if (m->has_element) {
        xmlSetProp(el, BAD_CAST "element", BAD_CAST m->element);
    }
    if (m->has_position) {
        char *s = mx_format_int(m->position);
        xmlSetProp(el, BAD_CAST "position", BAD_CAST s);
        free(s);
    }
    return el;
}

void mx_bookmark_free(MxBookmark *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    if (m->has_name)
        free(m->name);
    if (m->has_element)
        free(m->element);
    free(m);
}
