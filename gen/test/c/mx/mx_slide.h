/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SLIDE_H_INCLUDED
#define MX_SLIDE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_line_type.h"
#include "mx_number_level.h"
#include "mx_percent.h"
#include "mx_start_stop.h"
#include "mx_tenths.h"
#include "mx_trill_beats.h"
#include "mx_yes_no.h"

/*
 * Glissando and slide types both indicate rapidly moving from one pitch to the other so that
 * individual notes are not discerned. The distinction is similar to that between NIFF's glissando
 * and portamento elements. A slide is continuous between two notes and defaults to a solid line.
 * The optional text for a is printed alongside the line.
 */
typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_number;
    MxNumberLevel number; /* attribute number */
    bool has_line_type;
    MxLineType line_type; /* attribute line-type */
    bool has_dash_length;
    MxTenths dash_length; /* attribute dash-length */
    bool has_space_length;
    MxTenths space_length; /* attribute space-length */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_accelerate;
    MxYesNo accelerate; /* attribute accelerate */
    bool has_beats;
    MxTrillBeats beats; /* attribute beats */
    bool has_first_beat;
    MxPercent first_beat; /* attribute first-beat */
    bool has_last_beat;
    MxPercent last_beat; /* attribute last-beat */
    bool has_id;
    char *id; /* attribute id */
    char *value; /* text content */
} MxSlide;

/* NULL on error; the message is in mx_error(). */
MxSlide *mx_slide_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_slide_serialize(const MxSlide *m, xmlNodePtr parent, const char *tag);
void mx_slide_free(MxSlide *m);

#endif /* MX_SLIDE_H_INCLUDED */
