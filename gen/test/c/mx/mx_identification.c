/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_identification.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxIdentification *mx_identification_parse(xmlNodePtr el) {
    MxIdentification *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_identification_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxIdentificationChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "creator") == 0) {
            ch->creator = mx_typed_text_parse(c);
            if (!ch->creator) {
                mx_identification_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "rights") == 0) {
            ch->rights = mx_typed_text_parse(c);
            if (!ch->rights) {
                mx_identification_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "encoding") == 0) {
            ch->encoding = mx_encoding_parse(c);
            if (!ch->encoding) {
                mx_identification_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "source") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->source = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "relation") == 0) {
            ch->relation = mx_typed_text_parse(c);
            if (!ch->relation) {
                mx_identification_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "miscellaneous") == 0) {
            ch->miscellaneous = mx_miscellaneous_parse(c);
            if (!ch->miscellaneous) {
                mx_identification_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_identification_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_identification_serialize(const MxIdentification *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxIdentificationChild *ch = &m->children[i];
        if (ch->creator) {
            mx_typed_text_serialize(ch->creator, el, "creator");
        } else if (ch->rights) {
            mx_typed_text_serialize(ch->rights, el, "rights");
        } else if (ch->encoding) {
            mx_encoding_serialize(ch->encoding, el, "encoding");
        } else if (ch->source) {
            xmlNewTextChild(el, NULL, BAD_CAST "source", BAD_CAST ch->source);
        } else if (ch->relation) {
            mx_typed_text_serialize(ch->relation, el, "relation");
        } else if (ch->miscellaneous) {
            mx_miscellaneous_serialize(ch->miscellaneous, el, "miscellaneous");
        }
    }
    return el;
}

void mx_identification_free(MxIdentification *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxIdentificationChild *ch = &m->children[i];
        if (ch->creator)
            mx_typed_text_free(ch->creator);
        if (ch->rights)
            mx_typed_text_free(ch->rights);
        if (ch->encoding)
            mx_encoding_free(ch->encoding);
        free(ch->source);
        if (ch->relation)
            mx_typed_text_free(ch->relation);
        if (ch->miscellaneous)
            mx_miscellaneous_free(ch->miscellaneous);
    }
    free(m->children);
    free(m);
}
