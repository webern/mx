/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SLASH_H_INCLUDED
#define MX_SLASH_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_note_type_value.h"
#include "mx_start_stop.h"
#include "mx_yes_no.h"

/*
 * The slash type is used to indicate that slash notation is to be used. If the slash is on every
 * beat, use-stems is no (the default). To indicate rhythms but not pitches, use-stems is set to
 * yes. The type attribute indicates whether this is the start or stop of a slash notation style.
 * The use-dots attribute works as for the beat-repeat element, and only has effect if use-stems is
 * no.
 */
/* One child element of MxSlash: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxNoteTypeValue *slash_type;
    MxEmpty *slash_dot;
    char *except_voice;
} MxSlashChild;

typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_use_dots;
    MxYesNo use_dots; /* attribute use-dots */
    bool has_use_stems;
    MxYesNo use_stems; /* attribute use-stems */
    MxSlashChild *children; /* child elements in document order */
    size_t children_count;
} MxSlash;

/* NULL on error; the message is in mx_error(). */
MxSlash *mx_slash_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_slash_serialize(const MxSlash *m, xmlNodePtr parent, const char *tag);
void mx_slash_free(MxSlash *m);

#endif /* MX_SLASH_H_INCLUDED */
