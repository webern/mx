/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_MISCELLANEOUS_H_INCLUDED
#define MX_MISCELLANEOUS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_miscellaneous_field.h"

/*
 * If a program has other metadata not yet supported in the MusicXML format, it can go in the
 * miscellaneous element. The miscellaneous type puts each separate part of metadata into its own
 * miscellaneous-field type.
 */
/* One child element of MxMiscellaneous: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxMiscellaneousField *miscellaneous_field;
} MxMiscellaneousChild;

typedef struct {
    MxMiscellaneousChild *children; /* child elements in document order */
    size_t children_count;
} MxMiscellaneous;

/* NULL on error; the message is in mx_error(). */
MxMiscellaneous *mx_miscellaneous_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_miscellaneous_serialize(const MxMiscellaneous *m, xmlNodePtr parent, const char *tag);
void mx_miscellaneous_free(MxMiscellaneous *m);

#endif /* MX_MISCELLANEOUS_H_INCLUDED */
