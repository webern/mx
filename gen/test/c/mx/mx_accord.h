/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ACCORD_H_INCLUDED
#define MX_ACCORD_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_octave.h"
#include "mx_semitones.h"
#include "mx_step.h"
#include "mx_string_number.h"

/*
 * The accord type represents the tuning of a single string in the scordatura element. It uses the
 * same group of elements as the staff-tuning element. Strings are numbered from high to low.
 */
/* One child element of MxAccord: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStep *tuning_step;
    MxSemitones *tuning_alter;
    MxOctave *tuning_octave;
} MxAccordChild;

typedef struct {
    bool has_string;
    MxStringNumber string; /* attribute string */
    MxAccordChild *children; /* child elements in document order */
    size_t children_count;
} MxAccord;

/* NULL on error; the message is in mx_error(). */
MxAccord *mx_accord_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_accord_serialize(const MxAccord *m, xmlNodePtr parent, const char *tag);
void mx_accord_free(MxAccord *m);

#endif /* MX_ACCORD_H_INCLUDED */
