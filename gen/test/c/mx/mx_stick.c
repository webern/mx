/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_stick.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxStick *mx_stick_parse(xmlNodePtr el) {
    MxStick *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "tip") == 0) {
            m->has_tip = true;
            m->tip = mx_tip_direction_parse(s);
        } else if (strcmp(aname, "parentheses") == 0) {
            m->has_parentheses = true;
            m->parentheses = mx_yes_no_parse(s);
        } else if (strcmp(aname, "dashed-circle") == 0) {
            m->has_dashed_circle = true;
            m->dashed_circle = mx_yes_no_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_stick_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxStickChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "stick-type") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->stick_type = malloc(sizeof(*ch->stick_type));
            if (!ch->stick_type)
                abort();
            *ch->stick_type = mx_stick_type_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "stick-material") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->stick_material = malloc(sizeof(*ch->stick_material));
            if (!ch->stick_material)
                abort();
            *ch->stick_material = mx_stick_material_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_stick_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_stick_serialize(const MxStick *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_tip) {
        xmlSetProp(el, BAD_CAST "tip", BAD_CAST mx_tip_direction_to_string(m->tip));
    }
    if (m->has_parentheses) {
        xmlSetProp(el, BAD_CAST "parentheses", BAD_CAST mx_yes_no_to_string(m->parentheses));
    }
    if (m->has_dashed_circle) {
        xmlSetProp(el, BAD_CAST "dashed-circle", BAD_CAST mx_yes_no_to_string(m->dashed_circle));
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxStickChild *ch = &m->children[i];
        if (ch->stick_type) {
            xmlNewTextChild(el, NULL, BAD_CAST "stick-type", BAD_CAST mx_stick_type_to_string((*ch->stick_type)));
        } else if (ch->stick_material) {
            xmlNewTextChild(el, NULL, BAD_CAST "stick-material", BAD_CAST mx_stick_material_to_string((*ch->stick_material)));
        }
    }
    return el;
}

void mx_stick_free(MxStick *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxStickChild *ch = &m->children[i];
        free(ch->stick_type);
        free(ch->stick_material);
    }
    free(m->children);
    free(m);
}
