/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_notations.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxNotations *mx_notations_parse(xmlNodePtr el) {
    MxNotations *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "print-object") == 0) {
            m->has_print_object = true;
            m->print_object = mx_yes_no_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_notations_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxNotationsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tied") == 0) {
            ch->tied = mx_tied_parse(c);
            if (!ch->tied) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "slur") == 0) {
            ch->slur = mx_slur_parse(c);
            if (!ch->slur) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tuplet") == 0) {
            ch->tuplet = mx_tuplet_parse(c);
            if (!ch->tuplet) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "glissando") == 0) {
            ch->glissando = mx_glissando_parse(c);
            if (!ch->glissando) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "slide") == 0) {
            ch->slide = mx_slide_parse(c);
            if (!ch->slide) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "ornaments") == 0) {
            ch->ornaments = mx_ornaments_parse(c);
            if (!ch->ornaments) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "technical") == 0) {
            ch->technical = mx_technical_parse(c);
            if (!ch->technical) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "articulations") == 0) {
            ch->articulations = mx_articulations_parse(c);
            if (!ch->articulations) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "dynamics") == 0) {
            ch->dynamics = mx_dynamics_parse(c);
            if (!ch->dynamics) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "fermata") == 0) {
            ch->fermata = mx_fermata_parse(c);
            if (!ch->fermata) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "arpeggiate") == 0) {
            ch->arpeggiate = mx_arpeggiate_parse(c);
            if (!ch->arpeggiate) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "non-arpeggiate") == 0) {
            ch->non_arpeggiate = mx_non_arpeggiate_parse(c);
            if (!ch->non_arpeggiate) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "accidental-mark") == 0) {
            ch->accidental_mark = mx_accidental_mark_parse(c);
            if (!ch->accidental_mark) {
                mx_notations_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-notation") == 0) {
            ch->other_notation = mx_other_notation_parse(c);
            if (!ch->other_notation) {
                mx_notations_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_notations_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_notations_serialize(const MxNotations *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_print_object) {
        xmlSetProp(el, BAD_CAST "print-object", BAD_CAST mx_yes_no_to_string(m->print_object));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxNotationsChild *ch = &m->children[i];
        if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        } else if (ch->tied) {
            mx_tied_serialize(ch->tied, el, "tied");
        } else if (ch->slur) {
            mx_slur_serialize(ch->slur, el, "slur");
        } else if (ch->tuplet) {
            mx_tuplet_serialize(ch->tuplet, el, "tuplet");
        } else if (ch->glissando) {
            mx_glissando_serialize(ch->glissando, el, "glissando");
        } else if (ch->slide) {
            mx_slide_serialize(ch->slide, el, "slide");
        } else if (ch->ornaments) {
            mx_ornaments_serialize(ch->ornaments, el, "ornaments");
        } else if (ch->technical) {
            mx_technical_serialize(ch->technical, el, "technical");
        } else if (ch->articulations) {
            mx_articulations_serialize(ch->articulations, el, "articulations");
        } else if (ch->dynamics) {
            mx_dynamics_serialize(ch->dynamics, el, "dynamics");
        } else if (ch->fermata) {
            mx_fermata_serialize(ch->fermata, el, "fermata");
        } else if (ch->arpeggiate) {
            mx_arpeggiate_serialize(ch->arpeggiate, el, "arpeggiate");
        } else if (ch->non_arpeggiate) {
            mx_non_arpeggiate_serialize(ch->non_arpeggiate, el, "non-arpeggiate");
        } else if (ch->accidental_mark) {
            mx_accidental_mark_serialize(ch->accidental_mark, el, "accidental-mark");
        } else if (ch->other_notation) {
            mx_other_notation_serialize(ch->other_notation, el, "other-notation");
        }
    }
    return el;
}

void mx_notations_free(MxNotations *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxNotationsChild *ch = &m->children[i];
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
        if (ch->tied)
            mx_tied_free(ch->tied);
        if (ch->slur)
            mx_slur_free(ch->slur);
        if (ch->tuplet)
            mx_tuplet_free(ch->tuplet);
        if (ch->glissando)
            mx_glissando_free(ch->glissando);
        if (ch->slide)
            mx_slide_free(ch->slide);
        if (ch->ornaments)
            mx_ornaments_free(ch->ornaments);
        if (ch->technical)
            mx_technical_free(ch->technical);
        if (ch->articulations)
            mx_articulations_free(ch->articulations);
        if (ch->dynamics)
            mx_dynamics_free(ch->dynamics);
        if (ch->fermata)
            mx_fermata_free(ch->fermata);
        if (ch->arpeggiate)
            mx_arpeggiate_free(ch->arpeggiate);
        if (ch->non_arpeggiate)
            mx_non_arpeggiate_free(ch->non_arpeggiate);
        if (ch->accidental_mark)
            mx_accidental_mark_free(ch->accidental_mark);
        if (ch->other_notation)
            mx_other_notation_free(ch->other_notation);
    }
    free(m->children);
    free(m);
}
