/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_mute.h"

#include <string.h>

static const char *const mx_mute_values[] = {
    "on",
    "off",
    "straight",
    "cup",
    "harmon-no-stem",
    "harmon-stem",
    "bucket",
    "plunger",
    "hat",
    "solotone",
    "practice",
    "stop-mute",
    "stop-hand",
    "echo",
    "palm",
};

bool mx_mute_try_parse(const char *s, MxMute *out) {
    if (!s)
        s = "";
    for (size_t i = 0; i < sizeof(mx_mute_values) / sizeof(mx_mute_values[0]); i++) {
        if (strcmp(s, mx_mute_values[i]) == 0) {
            *out = (MxMute)i;
            return true;
        }
    }
    *out = (MxMute)0;
    return false;
}

MxMute mx_mute_parse(const char *s) {
    MxMute v;
    mx_mute_try_parse(s, &v);
    return v;
}

const char *mx_mute_to_string(MxMute v) {
    if ((size_t)v >= sizeof(mx_mute_values) / sizeof(mx_mute_values[0]))
        return mx_mute_values[0];
    return mx_mute_values[v];
}
