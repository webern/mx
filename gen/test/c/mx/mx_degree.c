/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_degree.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxDegree *mx_degree_parse(xmlNodePtr el) {
    MxDegree *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "print-object") == 0) {
            m->has_print_object = true;
            m->print_object = mx_yes_no_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_degree_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxDegreeChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "degree-value") == 0) {
            ch->degree_value = mx_degree_value_parse(c);
            if (!ch->degree_value) {
                mx_degree_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "degree-alter") == 0) {
            ch->degree_alter = mx_degree_alter_parse(c);
            if (!ch->degree_alter) {
                mx_degree_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "degree-type") == 0) {
            ch->degree_type = mx_degree_type_parse(c);
            if (!ch->degree_type) {
                mx_degree_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_degree_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_degree_serialize(const MxDegree *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_print_object) {
        xmlSetProp(el, BAD_CAST "print-object", BAD_CAST mx_yes_no_to_string(m->print_object));
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxDegreeChild *ch = &m->children[i];
        if (ch->degree_value) {
            mx_degree_value_serialize(ch->degree_value, el, "degree-value");
        } else if (ch->degree_alter) {
            mx_degree_alter_serialize(ch->degree_alter, el, "degree-alter");
        } else if (ch->degree_type) {
            mx_degree_type_serialize(ch->degree_type, el, "degree-type");
        }
    }
    return el;
}

void mx_degree_free(MxDegree *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxDegreeChild *ch = &m->children[i];
        if (ch->degree_value)
            mx_degree_value_free(ch->degree_value);
        if (ch->degree_alter)
            mx_degree_alter_free(ch->degree_alter);
        if (ch->degree_type)
            mx_degree_type_free(ch->degree_type);
    }
    free(m->children);
    free(m);
}
