/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BARRE_H_INCLUDED
#define MX_BARRE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_start_stop.h"

/*
 * The barre element indicates placing a finger over multiple strings on a single fret. The type is
 * "start" for the lowest pitched string (e.g., the string with the highest MusicXML number) and is
 * "stop" for the highest pitched string.
 */
typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_color;
    MxColor color; /* attribute color */
} MxBarre;

/* NULL on error; the message is in mx_error(). */
MxBarre *mx_barre_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_barre_serialize(const MxBarre *m, xmlNodePtr parent, const char *tag);
void mx_barre_free(MxBarre *m);

#endif /* MX_BARRE_H_INCLUDED */
