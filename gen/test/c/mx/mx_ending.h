/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ENDING_H_INCLUDED
#define MX_ENDING_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_ending_number.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_start_stop_discontinue.h"
#include "mx_tenths.h"
#include "mx_yes_no.h"

/*
 * The ending type represents multiple (e.g. first and second) endings. Typically, the start type is
 * associated with the left barline of the first measure in an ending. The stop and discontinue
 * types are associated with the right barline of the last measure in an ending. Stop is used when
 * the ending mark concludes with a downward jog, as is typical for first endings. Discontinue is
 * used when there is no downward jog, as is typical for second endings that do not conclude a
 * piece. The length of the jog can be specified using the end-length attribute. The text-x and
 * text-y attributes are offsets that specify where the baseline of the start of the ending text
 * appears, relative to the start of the ending line. The number attribute reflects the numeric
 * values of what is under the ending line. Single endings such as "1" or comma-separated multiple
 * endings such as "1,2" may be used. The ending element text is used when the text displayed in the
 * ending is different than what appears in the number attribute. The print-object element is used
 * to indicate when an ending is present but not printed, as is often the case for many parts in a
 * full score.
 */
typedef struct {
    bool has_number;
    MxEndingNumber number; /* attribute number */
    bool has_type;
    MxStartStopDiscontinue type; /* attribute type */
    bool has_end_length;
    MxTenths end_length; /* attribute end-length */
    bool has_text_x;
    MxTenths text_x; /* attribute text-x */
    bool has_text_y;
    MxTenths text_y; /* attribute text-y */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    char *value; /* text content */
} MxEnding;

/* NULL on error; the message is in mx_error(). */
MxEnding *mx_ending_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_ending_serialize(const MxEnding *m, xmlNodePtr parent, const char *tag);
void mx_ending_free(MxEnding *m);

#endif /* MX_ENDING_H_INCLUDED */
