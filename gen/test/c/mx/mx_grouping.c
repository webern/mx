/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_grouping.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxGrouping *mx_grouping_parse(xmlNodePtr el) {
    MxGrouping *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "type") == 0) {
            m->has_type = true;
            m->type = mx_start_stop_single_parse(s);
        } else if (strcmp(aname, "number") == 0) {
            m->has_number = true;
            m->number = mx_strdup(s);
        } else if (strcmp(aname, "member-of") == 0) {
            m->has_member_of = true;
            m->member_of = mx_strdup(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_grouping_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxGroupingChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "feature") == 0) {
            ch->feature = mx_feature_parse(c);
            if (!ch->feature) {
                mx_grouping_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_grouping_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_grouping_serialize(const MxGrouping *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_type) {
        xmlSetProp(el, BAD_CAST "type", BAD_CAST mx_start_stop_single_to_string(m->type));
    }
    if (m->has_number) {
        xmlSetProp(el, BAD_CAST "number", BAD_CAST m->number);
    }
    if (m->has_member_of) {
        xmlSetProp(el, BAD_CAST "member-of", BAD_CAST m->member_of);
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxGroupingChild *ch = &m->children[i];
        if (ch->feature) {
            mx_feature_serialize(ch->feature, el, "feature");
        }
    }
    return el;
}

void mx_grouping_free(MxGrouping *m) {
    if (!m)
        return;
    if (m->has_number)
        free(m->number);
    if (m->has_member_of)
        free(m->member_of);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxGroupingChild *ch = &m->children[i];
        if (ch->feature)
            mx_feature_free(ch->feature);
    }
    free(m->children);
    free(m);
}
