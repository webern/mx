/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_METRONOME_TUPLET_H_INCLUDED
#define MX_METRONOME_TUPLET_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_note_type_value.h"
#include "mx_show_tuplet.h"
#include "mx_start_stop.h"
#include "mx_time_modification.h"
#include "mx_yes_no.h"

/*
 * The metronome-tuplet type uses the same element structure as the time-modification element along
 * with some attributes from the tuplet element.
 */
/* One child element of MxMetronomeTuplet: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    long *actual_notes;
    long *normal_notes;
    MxNoteTypeValue *normal_type;
    MxEmpty *normal_dot;
} MxMetronomeTupletChild;

typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_bracket;
    MxYesNo bracket; /* attribute bracket */
    bool has_show_number;
    MxShowTuplet show_number; /* attribute show-number */
    MxMetronomeTupletChild *children; /* child elements in document order */
    size_t children_count;
} MxMetronomeTuplet;

/* NULL on error; the message is in mx_error(). */
MxMetronomeTuplet *mx_metronome_tuplet_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_metronome_tuplet_serialize(const MxMetronomeTuplet *m, xmlNodePtr parent, const char *tag);
void mx_metronome_tuplet_free(MxMetronomeTuplet *m);

#endif /* MX_METRONOME_TUPLET_H_INCLUDED */
