/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_tuplet_portion.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxTupletPortion *mx_tuplet_portion_parse(xmlNodePtr el) {
    MxTupletPortion *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_tuplet_portion_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxTupletPortionChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "tuplet-number") == 0) {
            ch->tuplet_number = mx_tuplet_number_parse(c);
            if (!ch->tuplet_number) {
                mx_tuplet_portion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tuplet-type") == 0) {
            ch->tuplet_type = mx_tuplet_type_parse(c);
            if (!ch->tuplet_type) {
                mx_tuplet_portion_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "tuplet-dot") == 0) {
            ch->tuplet_dot = mx_tuplet_dot_parse(c);
            if (!ch->tuplet_dot) {
                mx_tuplet_portion_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_tuplet_portion_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_tuplet_portion_serialize(const MxTupletPortion *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxTupletPortionChild *ch = &m->children[i];
        if (ch->tuplet_number) {
            mx_tuplet_number_serialize(ch->tuplet_number, el, "tuplet-number");
        } else if (ch->tuplet_type) {
            mx_tuplet_type_serialize(ch->tuplet_type, el, "tuplet-type");
        } else if (ch->tuplet_dot) {
            mx_tuplet_dot_serialize(ch->tuplet_dot, el, "tuplet-dot");
        }
    }
    return el;
}

void mx_tuplet_portion_free(MxTupletPortion *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxTupletPortionChild *ch = &m->children[i];
        if (ch->tuplet_number)
            mx_tuplet_number_free(ch->tuplet_number);
        if (ch->tuplet_type)
            mx_tuplet_type_free(ch->tuplet_type);
        if (ch->tuplet_dot)
            mx_tuplet_dot_free(ch->tuplet_dot);
    }
    free(m->children);
    free(m);
}
