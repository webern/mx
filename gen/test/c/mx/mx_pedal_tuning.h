/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_PEDAL_TUNING_H_INCLUDED
#define MX_PEDAL_TUNING_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_semitones.h"
#include "mx_step.h"

/*
 * The pedal-tuning type specifies the tuning of a single harp pedal.
 */
/* One child element of MxPedalTuning: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxStep *pedal_step;
    MxSemitones *pedal_alter;
} MxPedalTuningChild;

typedef struct {
    MxPedalTuningChild *children; /* child elements in document order */
    size_t children_count;
} MxPedalTuning;

/* NULL on error; the message is in mx_error(). */
MxPedalTuning *mx_pedal_tuning_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_pedal_tuning_serialize(const MxPedalTuning *m, xmlNodePtr parent, const char *tag);
void mx_pedal_tuning_free(MxPedalTuning *m);

#endif /* MX_PEDAL_TUNING_H_INCLUDED */
