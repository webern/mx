/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_virtual_instrument.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxVirtualInstrument *mx_virtual_instrument_parse(xmlNodePtr el) {
    MxVirtualInstrument *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_virtual_instrument_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxVirtualInstrumentChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "virtual-library") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->virtual_library = mx_strdup(s);
            xmlFree(text);
        } else if (strcmp(tag, "virtual-name") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->virtual_name = mx_strdup(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_virtual_instrument_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_virtual_instrument_serialize(const MxVirtualInstrument *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    for (size_t i = 0; i < m->children_count; i++) {
        const MxVirtualInstrumentChild *ch = &m->children[i];
        if (ch->virtual_library) {
            xmlNewTextChild(el, NULL, BAD_CAST "virtual-library", BAD_CAST ch->virtual_library);
        } else if (ch->virtual_name) {
            xmlNewTextChild(el, NULL, BAD_CAST "virtual-name", BAD_CAST ch->virtual_name);
        }
    }
    return el;
}

void mx_virtual_instrument_free(MxVirtualInstrument *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxVirtualInstrumentChild *ch = &m->children[i];
        free(ch->virtual_library);
        free(ch->virtual_name);
    }
    free(m->children);
    free(m);
}
