/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_accidental_text.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxAccidentalText *mx_accidental_text_parse(xmlNodePtr el) {
    MxAccidentalText *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "smufl") == 0) {
            m->has_smufl = true;
            m->smufl = mx_smufl_accidental_glyph_name_parse(s);
        } else if (strcmp(aname, "xml:lang") == 0) {
            m->has_xml_lang = true;
            m->xml_lang = mx_strdup(s);
        } else if (strcmp(aname, "xml:space") == 0) {
            m->has_xml_space = true;
            m->xml_space = mx_strdup(s);
        } else if (strcmp(aname, "justify") == 0) {
            m->has_justify = true;
            m->justify = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "halign") == 0) {
            m->has_halign = true;
            m->halign = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "valign") == 0) {
            m->has_valign = true;
            m->valign = mx_valign_parse(s);
        } else if (strcmp(aname, "underline") == 0) {
            m->has_underline = true;
            m->underline = mx_number_of_lines_parse(s);
        } else if (strcmp(aname, "overline") == 0) {
            m->has_overline = true;
            m->overline = mx_number_of_lines_parse(s);
        } else if (strcmp(aname, "line-through") == 0) {
            m->has_line_through = true;
            m->line_through = mx_number_of_lines_parse(s);
        } else if (strcmp(aname, "rotation") == 0) {
            m->has_rotation = true;
            m->rotation = mx_rotation_degrees_parse(s);
        } else if (strcmp(aname, "letter-spacing") == 0) {
            m->has_letter_spacing = true;
            m->letter_spacing = mx_number_or_normal_parse(s);
        } else if (strcmp(aname, "line-height") == 0) {
            m->has_line_height = true;
            m->line_height = mx_number_or_normal_parse(s);
        } else if (strcmp(aname, "dir") == 0) {
            m->has_dir = true;
            m->dir = mx_text_direction_parse(s);
        } else if (strcmp(aname, "enclosure") == 0) {
            m->has_enclosure = true;
            m->enclosure = mx_enclosure_shape_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_accidental_text_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    {
        xmlChar *text = xmlNodeGetContent(el);
        const char *s = text ? (const char *)text : "";
        m->value = mx_accidental_value_parse(s);
        xmlFree(text);
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_accidental_text_free(m);
            return NULL;
        }
    }
    return m;
}

xmlNodePtr mx_accidental_text_serialize(const MxAccidentalText *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    xmlAddChild(el, xmlNewText(BAD_CAST mx_accidental_value_to_string(m->value)));
    if (m->has_smufl) {
        xmlSetProp(el, BAD_CAST "smufl", BAD_CAST m->smufl);
    }
    if (m->has_xml_lang) {
        xmlSetProp(el, BAD_CAST "xml:lang", BAD_CAST m->xml_lang);
    }
    if (m->has_xml_space) {
        xmlSetProp(el, BAD_CAST "xml:space", BAD_CAST m->xml_space);
    }
    if (m->has_justify) {
        xmlSetProp(el, BAD_CAST "justify", BAD_CAST mx_left_center_right_to_string(m->justify));
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_halign) {
        xmlSetProp(el, BAD_CAST "halign", BAD_CAST mx_left_center_right_to_string(m->halign));
    }
    if (m->has_valign) {
        xmlSetProp(el, BAD_CAST "valign", BAD_CAST mx_valign_to_string(m->valign));
    }
    if (m->has_underline) {
        char *s = mx_number_of_lines_to_string(m->underline);
        xmlSetProp(el, BAD_CAST "underline", BAD_CAST s);
        free(s);
    }
    if (m->has_overline) {
        char *s = mx_number_of_lines_to_string(m->overline);
        xmlSetProp(el, BAD_CAST "overline", BAD_CAST s);
        free(s);
    }
    if (m->has_line_through) {
        char *s = mx_number_of_lines_to_string(m->line_through);
        xmlSetProp(el, BAD_CAST "line-through", BAD_CAST s);
        free(s);
    }
    if (m->has_rotation) {
        char *s = mx_rotation_degrees_to_string(m->rotation);
        xmlSetProp(el, BAD_CAST "rotation", BAD_CAST s);
        free(s);
    }
    if (m->has_letter_spacing) {
        char *s = mx_number_or_normal_to_string(m->letter_spacing);
        xmlSetProp(el, BAD_CAST "letter-spacing", BAD_CAST s);
        free(s);
    }
    if (m->has_line_height) {
        char *s = mx_number_or_normal_to_string(m->line_height);
        xmlSetProp(el, BAD_CAST "line-height", BAD_CAST s);
        free(s);
    }
    if (m->has_dir) {
        xmlSetProp(el, BAD_CAST "dir", BAD_CAST mx_text_direction_to_string(m->dir));
    }
    if (m->has_enclosure) {
        xmlSetProp(el, BAD_CAST "enclosure", BAD_CAST mx_enclosure_shape_to_string(m->enclosure));
    }
    return el;
}

void mx_accidental_text_free(MxAccidentalText *m) {
    if (!m)
        return;
    if (m->has_smufl)
        free(m->smufl);
    if (m->has_xml_lang)
        free(m->xml_lang);
    if (m->has_xml_space)
        free(m->xml_space);
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_letter_spacing)
        mx_number_or_normal_free(&m->letter_spacing);
    if (m->has_line_height)
        mx_number_or_normal_free(&m->line_height);
    free(m);
}
