/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_MIDI_INSTRUMENT_H_INCLUDED
#define MX_MIDI_INSTRUMENT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_midi_128.h"
#include "mx_midi_16.h"
#include "mx_midi_16384.h"
#include "mx_percent.h"
#include "mx_rotation_degrees.h"

/*
 * The midi-instrument type defines MIDI 1.0 instrument playback. The midi-instrument element can be
 * a part of either the score-instrument element at the start of a part, or the sound element within
 * a part. The id attribute refers to the score-instrument affected by the change.
 */
/* One child element of MxMIDIInstrument: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxMIDI16 *midi_channel;
    char *midi_name;
    MxMIDI16384 *midi_bank;
    MxMIDI128 *midi_program;
    MxMIDI128 *midi_unpitched;
    MxPercent *volume;
    MxRotationDegrees *pan;
    MxRotationDegrees *elevation;
} MxMIDIInstrumentChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxMIDIInstrumentChild *children; /* child elements in document order */
    size_t children_count;
} MxMIDIInstrument;

/* NULL on error; the message is in mx_error(). */
MxMIDIInstrument *mx_midi_instrument_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_midi_instrument_serialize(const MxMIDIInstrument *m, xmlNodePtr parent, const char *tag);
void mx_midi_instrument_free(MxMIDIInstrument *m);

#endif /* MX_MIDI_INSTRUMENT_H_INCLUDED */
