/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_ORNAMENTS_H_INCLUDED
#define MX_ORNAMENTS_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_accidental_mark.h"
#include "mx_empty_placement.h"
#include "mx_empty_trill_sound.h"
#include "mx_horizontal_turn.h"
#include "mx_mordent.h"
#include "mx_other_placement_text.h"
#include "mx_tremolo.h"
#include "mx_wavy_line.h"

/*
 * Ornaments can be any of several types, followed optionally by accidentals. The accidental-mark
 * element's content is represented the same as an accidental element, but with a different name to
 * reflect the different musical meaning.
 */
/* One child element of MxOrnaments: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxEmptyTrillSound *trill_mark;
    MxHorizontalTurn *turn;
    MxHorizontalTurn *delayed_turn;
    MxHorizontalTurn *inverted_turn;
    MxHorizontalTurn *delayed_inverted_turn;
    MxEmptyTrillSound *vertical_turn;
    MxEmptyTrillSound *inverted_vertical_turn;
    MxEmptyTrillSound *shake;
    MxWavyLine *wavy_line;
    MxMordent *mordent;
    MxMordent *inverted_mordent;
    MxEmptyPlacement *schleifer;
    MxTremolo *tremolo;
    MxEmptyTrillSound *haydn;
    MxOtherPlacementText *other_ornament;
    MxAccidentalMark *accidental_mark;
} MxOrnamentsChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxOrnamentsChild *children; /* child elements in document order */
    size_t children_count;
} MxOrnaments;

/* NULL on error; the message is in mx_error(). */
MxOrnaments *mx_ornaments_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_ornaments_serialize(const MxOrnaments *m, xmlNodePtr parent, const char *tag);
void mx_ornaments_free(MxOrnaments *m);

#endif /* MX_ORNAMENTS_H_INCLUDED */
