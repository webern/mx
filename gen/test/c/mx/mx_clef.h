/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_CLEF_H_INCLUDED
#define MX_CLEF_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_clef_sign.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_staff_line.h"
#include "mx_staff_number.h"
#include "mx_symbol_size.h"
#include "mx_tenths.h"
#include "mx_yes_no.h"

/*
 * Clefs are represented by a combination of sign, line, and clef-octave-change elements. The
 * optional number attribute refers to staff numbers within the part. A value of 1 is assumed if not
 * present. Sometimes clefs are added to the staff in non-standard line positions, either to
 * indicate cue passages, or when there are multiple clefs present simultaneously on one staff. In
 * this situation, the additional attribute is set to "yes" and the line value is ignored. The size
 * attribute is used for clefs where the additional attribute is "yes". It is typically used to
 * indicate cue clefs. Sometimes clefs at the start of a measure need to appear after the barline
 * rather than before, as for cues or for use after a repeated section. The after-barline attribute
 * is set to "yes" in this situation. The attribute is ignored for mid-measure clefs. Clefs appear
 * at the start of each system unless the print-object attribute has been set to "no" or the
 * additional attribute has been set to "yes".
 */
/* One child element of MxClef: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxClefSign *sign;
    MxStaffLine *line;
    long *clef_octave_change;
} MxClefChild;

typedef struct {
    bool has_number;
    MxStaffNumber number; /* attribute number */
    bool has_additional;
    MxYesNo additional; /* attribute additional */
    bool has_size;
    MxSymbolSize size; /* attribute size */
    bool has_after_barline;
    MxYesNo after_barline; /* attribute after-barline */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_print_object;
    MxYesNo print_object; /* attribute print-object */
    bool has_id;
    char *id; /* attribute id */
    MxClefChild *children; /* child elements in document order */
    size_t children_count;
} MxClef;

/* NULL on error; the message is in mx_error(). */
MxClef *mx_clef_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_clef_serialize(const MxClef *m, xmlNodePtr parent, const char *tag);
void mx_clef_free(MxClef *m);

#endif /* MX_CLEF_H_INCLUDED */
