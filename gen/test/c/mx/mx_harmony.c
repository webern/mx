/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_harmony.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxHarmony *mx_harmony_parse(xmlNodePtr el) {
    MxHarmony *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "type") == 0) {
            m->has_type = true;
            m->type = mx_harmony_type_parse(s);
        } else if (strcmp(aname, "print-frame") == 0) {
            m->has_print_frame = true;
            m->print_frame = mx_yes_no_parse(s);
        } else if (strcmp(aname, "print-object") == 0) {
            m->has_print_object = true;
            m->print_object = mx_yes_no_parse(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "font-family") == 0) {
            m->has_font_family = true;
            m->font_family = mx_comma_separated_text_parse(s);
        } else if (strcmp(aname, "font-style") == 0) {
            m->has_font_style = true;
            m->font_style = mx_font_style_parse(s);
        } else if (strcmp(aname, "font-size") == 0) {
            m->has_font_size = true;
            m->font_size = mx_font_size_parse(s);
        } else if (strcmp(aname, "font-weight") == 0) {
            m->has_font_weight = true;
            m->font_weight = mx_font_weight_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "placement") == 0) {
            m->has_placement = true;
            m->placement = mx_above_below_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_harmony_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxHarmonyChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "root") == 0) {
            ch->root = mx_root_parse(c);
            if (!ch->root) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "function") == 0) {
            ch->function = mx_style_text_parse(c);
            if (!ch->function) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "kind") == 0) {
            ch->kind = mx_kind_parse(c);
            if (!ch->kind) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "inversion") == 0) {
            ch->inversion = mx_inversion_parse(c);
            if (!ch->inversion) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "bass") == 0) {
            ch->bass = mx_bass_parse(c);
            if (!ch->bass) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "degree") == 0) {
            ch->degree = mx_degree_parse(c);
            if (!ch->degree) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "frame") == 0) {
            ch->frame = mx_frame_parse(c);
            if (!ch->frame) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "offset") == 0) {
            ch->offset = mx_offset_parse(c);
            if (!ch->offset) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "footnote") == 0) {
            ch->footnote = mx_formatted_text_parse(c);
            if (!ch->footnote) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "level") == 0) {
            ch->level = mx_level_parse(c);
            if (!ch->level) {
                mx_harmony_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staff") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->staff = malloc(sizeof(*ch->staff));
            if (!ch->staff)
                abort();
            *ch->staff = mx_parse_int(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_harmony_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_harmony_serialize(const MxHarmony *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_type) {
        xmlSetProp(el, BAD_CAST "type", BAD_CAST mx_harmony_type_to_string(m->type));
    }
    if (m->has_print_frame) {
        xmlSetProp(el, BAD_CAST "print-frame", BAD_CAST mx_yes_no_to_string(m->print_frame));
    }
    if (m->has_print_object) {
        xmlSetProp(el, BAD_CAST "print-object", BAD_CAST mx_yes_no_to_string(m->print_object));
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_font_family) {
        xmlSetProp(el, BAD_CAST "font-family", BAD_CAST m->font_family);
    }
    if (m->has_font_style) {
        xmlSetProp(el, BAD_CAST "font-style", BAD_CAST mx_font_style_to_string(m->font_style));
    }
    if (m->has_font_size) {
        char *s = mx_font_size_to_string(m->font_size);
        xmlSetProp(el, BAD_CAST "font-size", BAD_CAST s);
        free(s);
    }
    if (m->has_font_weight) {
        xmlSetProp(el, BAD_CAST "font-weight", BAD_CAST mx_font_weight_to_string(m->font_weight));
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_placement) {
        xmlSetProp(el, BAD_CAST "placement", BAD_CAST mx_above_below_to_string(m->placement));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxHarmonyChild *ch = &m->children[i];
        if (ch->root) {
            mx_root_serialize(ch->root, el, "root");
        } else if (ch->function) {
            mx_style_text_serialize(ch->function, el, "function");
        } else if (ch->kind) {
            mx_kind_serialize(ch->kind, el, "kind");
        } else if (ch->inversion) {
            mx_inversion_serialize(ch->inversion, el, "inversion");
        } else if (ch->bass) {
            mx_bass_serialize(ch->bass, el, "bass");
        } else if (ch->degree) {
            mx_degree_serialize(ch->degree, el, "degree");
        } else if (ch->frame) {
            mx_frame_serialize(ch->frame, el, "frame");
        } else if (ch->offset) {
            mx_offset_serialize(ch->offset, el, "offset");
        } else if (ch->footnote) {
            mx_formatted_text_serialize(ch->footnote, el, "footnote");
        } else if (ch->level) {
            mx_level_serialize(ch->level, el, "level");
        } else if (ch->staff) {
            char *s = mx_format_int((*ch->staff));
            xmlNewTextChild(el, NULL, BAD_CAST "staff", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_harmony_free(MxHarmony *m) {
    if (!m)
        return;
    if (m->has_font_family)
        free(m->font_family);
    if (m->has_font_size)
        mx_font_size_free(&m->font_size);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxHarmonyChild *ch = &m->children[i];
        if (ch->root)
            mx_root_free(ch->root);
        if (ch->function)
            mx_style_text_free(ch->function);
        if (ch->kind)
            mx_kind_free(ch->kind);
        if (ch->inversion)
            mx_inversion_free(ch->inversion);
        if (ch->bass)
            mx_bass_free(ch->bass);
        if (ch->degree)
            mx_degree_free(ch->degree);
        if (ch->frame)
            mx_frame_free(ch->frame);
        if (ch->offset)
            mx_offset_free(ch->offset);
        if (ch->footnote)
            mx_formatted_text_free(ch->footnote);
        if (ch->level)
            mx_level_free(ch->level);
        free(ch->staff);
    }
    free(m->children);
    free(m);
}
