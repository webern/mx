/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_WEDGE_H_INCLUDED
#define MX_WEDGE_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_color.h"
#include "mx_line_type.h"
#include "mx_number_level.h"
#include "mx_tenths.h"
#include "mx_wedge_type.h"
#include "mx_yes_no.h"

/*
 * The wedge type represents crescendo and diminuendo wedge symbols. The type attribute is crescendo
 * for the start of a wedge that is closed at the left side, and diminuendo for the start of a wedge
 * that is closed on the right side. Spread values are measured in tenths; those at the start of a
 * crescendo wedge or end of a diminuendo wedge are ignored. The niente attribute is yes if a circle
 * appears at the point of the wedge, indicating a crescendo from nothing or diminuendo to nothing.
 * It is no by default, and used only when the type is crescendo, or the type is stop for a wedge
 * that began with a diminuendo type. The line-type is solid by default.
 */
typedef struct {
    bool has_type;
    MxWedgeType type; /* attribute type */
    bool has_number;
    MxNumberLevel number; /* attribute number */
    bool has_spread;
    MxTenths spread; /* attribute spread */
    bool has_niente;
    MxYesNo niente; /* attribute niente */
    bool has_line_type;
    MxLineType line_type; /* attribute line-type */
    bool has_dash_length;
    MxTenths dash_length; /* attribute dash-length */
    bool has_space_length;
    MxTenths space_length; /* attribute space-length */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_id;
    char *id; /* attribute id */
} MxWedge;

/* NULL on error; the message is in mx_error(). */
MxWedge *mx_wedge_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_wedge_serialize(const MxWedge *m, xmlNodePtr parent, const char *tag);
void mx_wedge_free(MxWedge *m);

#endif /* MX_WEDGE_H_INCLUDED */
