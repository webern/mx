/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_BEAT_REPEAT_H_INCLUDED
#define MX_BEAT_REPEAT_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_note_type_value.h"
#include "mx_start_stop.h"
#include "mx_yes_no.h"

/*
 * The beat-repeat type is used to indicate that a single beat (but possibly many notes) is
 * repeated. Both the start and stop of the beat being repeated should be specified. The slashes
 * attribute specifies the number of slashes to use in the symbol. The use-dots attribute indicates
 * whether or not to use dots as well (for instance, with mixed rhythm patterns). By default, the
 * value for slashes is 1 and the value for use-dots is no. The beat-repeat element specifies a
 * notation style for repetitions. The actual music being repeated needs to be repeated within the
 * MusicXML file. This element specifies the notation that indicates the repeat.
 */
/* One child element of MxBeatRepeat: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxNoteTypeValue *slash_type;
    MxEmpty *slash_dot;
    char *except_voice;
} MxBeatRepeatChild;

typedef struct {
    bool has_type;
    MxStartStop type; /* attribute type */
    bool has_slashes;
    long slashes; /* attribute slashes */
    bool has_use_dots;
    MxYesNo use_dots; /* attribute use-dots */
    MxBeatRepeatChild *children; /* child elements in document order */
    size_t children_count;
} MxBeatRepeat;

/* NULL on error; the message is in mx_error(). */
MxBeatRepeat *mx_beat_repeat_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_beat_repeat_serialize(const MxBeatRepeat *m, xmlNodePtr parent, const char *tag);
void mx_beat_repeat_free(MxBeatRepeat *m);

#endif /* MX_BEAT_REPEAT_H_INCLUDED */
