/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_direction_type.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxDirectionType *mx_direction_type_parse(xmlNodePtr el) {
    MxDirectionType *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_direction_type_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxDirectionTypeChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "rehearsal") == 0) {
            ch->rehearsal = mx_formatted_text_id_parse(c);
            if (!ch->rehearsal) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "segno") == 0) {
            ch->segno = mx_segno_parse(c);
            if (!ch->segno) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "coda") == 0) {
            ch->coda = mx_coda_parse(c);
            if (!ch->coda) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "words") == 0) {
            ch->words = mx_formatted_text_id_parse(c);
            if (!ch->words) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "symbol") == 0) {
            ch->symbol = mx_formatted_symbol_id_parse(c);
            if (!ch->symbol) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "wedge") == 0) {
            ch->wedge = mx_wedge_parse(c);
            if (!ch->wedge) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "dynamics") == 0) {
            ch->dynamics = mx_dynamics_parse(c);
            if (!ch->dynamics) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "dashes") == 0) {
            ch->dashes = mx_dashes_parse(c);
            if (!ch->dashes) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "bracket") == 0) {
            ch->bracket = mx_bracket_parse(c);
            if (!ch->bracket) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "pedal") == 0) {
            ch->pedal = mx_pedal_parse(c);
            if (!ch->pedal) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "metronome") == 0) {
            ch->metronome = mx_metronome_parse(c);
            if (!ch->metronome) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "octave-shift") == 0) {
            ch->octave_shift = mx_octave_shift_parse(c);
            if (!ch->octave_shift) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "harp-pedals") == 0) {
            ch->harp_pedals = mx_harp_pedals_parse(c);
            if (!ch->harp_pedals) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "damp") == 0) {
            ch->damp = mx_empty_print_style_align_id_parse(c);
            if (!ch->damp) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "damp-all") == 0) {
            ch->damp_all = mx_empty_print_style_align_id_parse(c);
            if (!ch->damp_all) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "eyeglasses") == 0) {
            ch->eyeglasses = mx_empty_print_style_align_id_parse(c);
            if (!ch->eyeglasses) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "string-mute") == 0) {
            ch->string_mute = mx_string_mute_parse(c);
            if (!ch->string_mute) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "scordatura") == 0) {
            ch->scordatura = mx_scordatura_parse(c);
            if (!ch->scordatura) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "image") == 0) {
            ch->image = mx_image_parse(c);
            if (!ch->image) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "principal-voice") == 0) {
            ch->principal_voice = mx_principal_voice_parse(c);
            if (!ch->principal_voice) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "percussion") == 0) {
            ch->percussion = mx_percussion_parse(c);
            if (!ch->percussion) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "accordion-registration") == 0) {
            ch->accordion_registration = mx_accordion_registration_parse(c);
            if (!ch->accordion_registration) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "staff-divide") == 0) {
            ch->staff_divide = mx_staff_divide_parse(c);
            if (!ch->staff_divide) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "other-direction") == 0) {
            ch->other_direction = mx_other_direction_parse(c);
            if (!ch->other_direction) {
                mx_direction_type_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_direction_type_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_direction_type_serialize(const MxDirectionType *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxDirectionTypeChild *ch = &m->children[i];
        if (ch->rehearsal) {
            mx_formatted_text_id_serialize(ch->rehearsal, el, "rehearsal");
        } else if (ch->segno) {
            mx_segno_serialize(ch->segno, el, "segno");
        } else if (ch->coda) {
            mx_coda_serialize(ch->coda, el, "coda");
        } else if (ch->words) {
            mx_formatted_text_id_serialize(ch->words, el, "words");
        } else if (ch->symbol) {
            mx_formatted_symbol_id_serialize(ch->symbol, el, "symbol");
        } else if (ch->wedge) {
            mx_wedge_serialize(ch->wedge, el, "wedge");
        } else if (ch->dynamics) {
            mx_dynamics_serialize(ch->dynamics, el, "dynamics");
        } else if (ch->dashes) {
            mx_dashes_serialize(ch->dashes, el, "dashes");
        } else if (ch->bracket) {
            mx_bracket_serialize(ch->bracket, el, "bracket");
        } else if (ch->pedal) {
            mx_pedal_serialize(ch->pedal, el, "pedal");
        } else if (ch->metronome) {
            mx_metronome_serialize(ch->metronome, el, "metronome");
        } else if (ch->octave_shift) {
            mx_octave_shift_serialize(ch->octave_shift, el, "octave-shift");
        } else if (ch->harp_pedals) {
            mx_harp_pedals_serialize(ch->harp_pedals, el, "harp-pedals");
        } else if (ch->damp) {
            mx_empty_print_style_align_id_serialize(ch->damp, el, "damp");
        } else if (ch->damp_all) {
            mx_empty_print_style_align_id_serialize(ch->damp_all, el, "damp-all");
        } else if (ch->eyeglasses) {
            mx_empty_print_style_align_id_serialize(ch->eyeglasses, el, "eyeglasses");
        } else if (ch->string_mute) {
            mx_string_mute_serialize(ch->string_mute, el, "string-mute");
        } else if (ch->scordatura) {
            mx_scordatura_serialize(ch->scordatura, el, "scordatura");
        } else if (ch->image) {
            mx_image_serialize(ch->image, el, "image");
        } else if (ch->principal_voice) {
            mx_principal_voice_serialize(ch->principal_voice, el, "principal-voice");
        } else if (ch->percussion) {
            mx_percussion_serialize(ch->percussion, el, "percussion");
        } else if (ch->accordion_registration) {
            mx_accordion_registration_serialize(ch->accordion_registration, el, "accordion-registration");
        } else if (ch->staff_divide) {
            mx_staff_divide_serialize(ch->staff_divide, el, "staff-divide");
        } else if (ch->other_direction) {
            mx_other_direction_serialize(ch->other_direction, el, "other-direction");
        }
    }
    return el;
}

void mx_direction_type_free(MxDirectionType *m) {
    if (!m)
        return;
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxDirectionTypeChild *ch = &m->children[i];
        if (ch->rehearsal)
            mx_formatted_text_id_free(ch->rehearsal);
        if (ch->segno)
            mx_segno_free(ch->segno);
        if (ch->coda)
            mx_coda_free(ch->coda);
        if (ch->words)
            mx_formatted_text_id_free(ch->words);
        if (ch->symbol)
            mx_formatted_symbol_id_free(ch->symbol);
        if (ch->wedge)
            mx_wedge_free(ch->wedge);
        if (ch->dynamics)
            mx_dynamics_free(ch->dynamics);
        if (ch->dashes)
            mx_dashes_free(ch->dashes);
        if (ch->bracket)
            mx_bracket_free(ch->bracket);
        if (ch->pedal)
            mx_pedal_free(ch->pedal);
        if (ch->metronome)
            mx_metronome_free(ch->metronome);
        if (ch->octave_shift)
            mx_octave_shift_free(ch->octave_shift);
        if (ch->harp_pedals)
            mx_harp_pedals_free(ch->harp_pedals);
        if (ch->damp)
            mx_empty_print_style_align_id_free(ch->damp);
        if (ch->damp_all)
            mx_empty_print_style_align_id_free(ch->damp_all);
        if (ch->eyeglasses)
            mx_empty_print_style_align_id_free(ch->eyeglasses);
        if (ch->string_mute)
            mx_string_mute_free(ch->string_mute);
        if (ch->scordatura)
            mx_scordatura_free(ch->scordatura);
        if (ch->image)
            mx_image_free(ch->image);
        if (ch->principal_voice)
            mx_principal_voice_free(ch->principal_voice);
        if (ch->percussion)
            mx_percussion_free(ch->percussion);
        if (ch->accordion_registration)
            mx_accordion_registration_free(ch->accordion_registration);
        if (ch->staff_divide)
            mx_staff_divide_free(ch->staff_divide);
        if (ch->other_direction)
            mx_other_direction_free(ch->other_direction);
    }
    free(m->children);
    free(m);
}
