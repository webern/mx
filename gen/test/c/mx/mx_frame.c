/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_frame.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxFrame *mx_frame_parse(xmlNodePtr el) {
    MxFrame *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "height") == 0) {
            m->has_height = true;
            m->height = mx_tenths_parse(s);
        } else if (strcmp(aname, "width") == 0) {
            m->has_width = true;
            m->width = mx_tenths_parse(s);
        } else if (strcmp(aname, "unplayed") == 0) {
            m->has_unplayed = true;
            m->unplayed = mx_strdup(s);
        } else if (strcmp(aname, "default-x") == 0) {
            m->has_default_x = true;
            m->default_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "default-y") == 0) {
            m->has_default_y = true;
            m->default_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-x") == 0) {
            m->has_relative_x = true;
            m->relative_x = mx_tenths_parse(s);
        } else if (strcmp(aname, "relative-y") == 0) {
            m->has_relative_y = true;
            m->relative_y = mx_tenths_parse(s);
        } else if (strcmp(aname, "color") == 0) {
            m->has_color = true;
            m->color = mx_color_parse(s);
        } else if (strcmp(aname, "halign") == 0) {
            m->has_halign = true;
            m->halign = mx_left_center_right_parse(s);
        } else if (strcmp(aname, "valign") == 0) {
            m->has_valign = true;
            m->valign = mx_valign_image_parse(s);
        } else if (strcmp(aname, "id") == 0) {
            m->has_id = true;
            m->id = mx_strdup(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_frame_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxFrameChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "frame-strings") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->frame_strings = malloc(sizeof(*ch->frame_strings));
            if (!ch->frame_strings)
                abort();
            *ch->frame_strings = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "frame-frets") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->frame_frets = malloc(sizeof(*ch->frame_frets));
            if (!ch->frame_frets)
                abort();
            *ch->frame_frets = mx_parse_int(s);
            xmlFree(text);
        } else if (strcmp(tag, "first-fret") == 0) {
            ch->first_fret = mx_first_fret_parse(c);
            if (!ch->first_fret) {
                mx_frame_free(m);
                return NULL;
            }
        } else if (strcmp(tag, "frame-note") == 0) {
            ch->frame_note = mx_frame_note_parse(c);
            if (!ch->frame_note) {
                mx_frame_free(m);
                return NULL;
            }
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_frame_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_frame_serialize(const MxFrame *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_height) {
        char *s = mx_tenths_to_string(m->height);
        xmlSetProp(el, BAD_CAST "height", BAD_CAST s);
        free(s);
    }
    if (m->has_width) {
        char *s = mx_tenths_to_string(m->width);
        xmlSetProp(el, BAD_CAST "width", BAD_CAST s);
        free(s);
    }
    if (m->has_unplayed) {
        xmlSetProp(el, BAD_CAST "unplayed", BAD_CAST m->unplayed);
    }
    if (m->has_default_x) {
        char *s = mx_tenths_to_string(m->default_x);
        xmlSetProp(el, BAD_CAST "default-x", BAD_CAST s);
        free(s);
    }
    if (m->has_default_y) {
        char *s = mx_tenths_to_string(m->default_y);
        xmlSetProp(el, BAD_CAST "default-y", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_x) {
        char *s = mx_tenths_to_string(m->relative_x);
        xmlSetProp(el, BAD_CAST "relative-x", BAD_CAST s);
        free(s);
    }
    if (m->has_relative_y) {
        char *s = mx_tenths_to_string(m->relative_y);
        xmlSetProp(el, BAD_CAST "relative-y", BAD_CAST s);
        free(s);
    }
    if (m->has_color) {
        xmlSetProp(el, BAD_CAST "color", BAD_CAST m->color);
    }
    if (m->has_halign) {
        xmlSetProp(el, BAD_CAST "halign", BAD_CAST mx_left_center_right_to_string(m->halign));
    }
    if (m->has_valign) {
        xmlSetProp(el, BAD_CAST "valign", BAD_CAST mx_valign_image_to_string(m->valign));
    }
    if (m->has_id) {
        xmlSetProp(el, BAD_CAST "id", BAD_CAST m->id);
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxFrameChild *ch = &m->children[i];
        if (ch->frame_strings) {
            char *s = mx_format_int((*ch->frame_strings));
            xmlNewTextChild(el, NULL, BAD_CAST "frame-strings", BAD_CAST s);
            free(s);
        } else if (ch->frame_frets) {
            char *s = mx_format_int((*ch->frame_frets));
            xmlNewTextChild(el, NULL, BAD_CAST "frame-frets", BAD_CAST s);
            free(s);
        } else if (ch->first_fret) {
            mx_first_fret_serialize(ch->first_fret, el, "first-fret");
        } else if (ch->frame_note) {
            mx_frame_note_serialize(ch->frame_note, el, "frame-note");
        }
    }
    return el;
}

void mx_frame_free(MxFrame *m) {
    if (!m)
        return;
    if (m->has_unplayed)
        free(m->unplayed);
    if (m->has_color)
        free(m->color);
    if (m->has_id)
        free(m->id);
    for (size_t i = 0; i < m->children_count; i++) {
        MxFrameChild *ch = &m->children[i];
        free(ch->frame_strings);
        free(ch->frame_frets);
        if (ch->first_fret)
            mx_first_fret_free(ch->first_fret);
        if (ch->frame_note)
            mx_frame_note_free(ch->frame_note);
    }
    free(m->children);
    free(m);
}
