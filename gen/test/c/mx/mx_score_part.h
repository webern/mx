/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_SCORE_PART_H_INCLUDED
#define MX_SCORE_PART_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_identification.h"
#include "mx_midi_device.h"
#include "mx_midi_instrument.h"
#include "mx_name_display.h"
#include "mx_part_name.h"
#include "mx_score_instrument.h"

/*
 * Each MusicXML part corresponds to a track in a Standard MIDI Format 1 file. The score-instrument
 * elements are used when there are multiple instruments per track. The midi-device element is used
 * to make a MIDI device or port assignment for the given track or specific MIDI instruments.
 * Initial midi-instrument assignments may be made here as well.
 */
/* One child element of MxScorePart: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxIdentification *identification;
    MxPartName *part_name;
    MxNameDisplay *part_name_display;
    MxPartName *part_abbreviation;
    MxNameDisplay *part_abbreviation_display;
    char *group;
    MxScoreInstrument *score_instrument;
    MxMIDIDevice *midi_device;
    MxMIDIInstrument *midi_instrument;
} MxScorePartChild;

typedef struct {
    bool has_id;
    char *id; /* attribute id */
    MxScorePartChild *children; /* child elements in document order */
    size_t children_count;
} MxScorePart;

/* NULL on error; the message is in mx_error(). */
MxScorePart *mx_score_part_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_score_part_serialize(const MxScorePart *m, xmlNodePtr parent, const char *tag);
void mx_score_part_free(MxScorePart *m);

#endif /* MX_SCORE_PART_H_INCLUDED */
