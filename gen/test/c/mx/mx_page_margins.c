/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#include "mx_page_margins.h"

#include "mx_runtime.h"
#include <stdlib.h>
#include <string.h>

MxPageMargins *mx_page_margins_parse(xmlNodePtr el) {
    MxPageMargins *m = calloc(1, sizeof(*m));
    if (!m)
        abort();
    for (xmlAttrPtr a = el->properties; a; a = a->next) {
        char aname[128];
        if (a->ns && a->ns->prefix)
            snprintf(aname, sizeof(aname), "%s:%s",
                     (const char *)a->ns->prefix, (const char *)a->name);
        else
            snprintf(aname, sizeof(aname), "%s", (const char *)a->name);
        xmlChar *avalue = xmlNodeListGetString(el->doc, a->children, 1);
        const char *s = avalue ? (const char *)avalue : "";
        (void)s;
        if (strcmp(aname, "type") == 0) {
            m->has_type = true;
            m->type = mx_margin_type_parse(s);
        } else {
            mx_error_set("unknown attribute \"%s\" on <%s>", aname,
                         (const char *)el->name);
            xmlFree(avalue);
            mx_page_margins_free(m);
            return NULL;
        }
        xmlFree(avalue);
    }
    size_t n = 0;
    for (xmlNodePtr c = el->children; c; c = c->next)
        if (c->type == XML_ELEMENT_NODE)
            n++;
    if (n) {
        m->children = calloc(n, sizeof(*m->children));
        if (!m->children)
            abort();
    }
    for (xmlNodePtr c = el->children; c; c = c->next) {
        if (c->type != XML_ELEMENT_NODE)
            continue;
        const char *tag = (const char *)c->name;
        MxPageMarginsChild *ch = &m->children[m->children_count];
        if (strcmp(tag, "left-margin") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->left_margin = malloc(sizeof(*ch->left_margin));
            if (!ch->left_margin)
                abort();
            *ch->left_margin = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "right-margin") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->right_margin = malloc(sizeof(*ch->right_margin));
            if (!ch->right_margin)
                abort();
            *ch->right_margin = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "top-margin") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->top_margin = malloc(sizeof(*ch->top_margin));
            if (!ch->top_margin)
                abort();
            *ch->top_margin = mx_tenths_parse(s);
            xmlFree(text);
        } else if (strcmp(tag, "bottom-margin") == 0) {
            xmlChar *text = xmlNodeGetContent(c);
            const char *s = text ? (const char *)text : "";
            ch->bottom_margin = malloc(sizeof(*ch->bottom_margin));
            if (!ch->bottom_margin)
                abort();
            *ch->bottom_margin = mx_tenths_parse(s);
            xmlFree(text);
        } else {
            mx_error_set("unknown element <%s> in <%s>", tag,
                         (const char *)el->name);
            mx_page_margins_free(m);
            return NULL;
        }
        m->children_count++;
    }
    return m;
}

xmlNodePtr mx_page_margins_serialize(const MxPageMargins *m, xmlNodePtr parent, const char *tag) {
    xmlNodePtr el = parent ? xmlNewChild(parent, NULL, BAD_CAST tag, NULL)
                           : xmlNewNode(NULL, BAD_CAST tag);
    if (!el)
        abort(); /* OOM policy: abort, matching the runtime's allocators */
    if (m->has_type) {
        xmlSetProp(el, BAD_CAST "type", BAD_CAST mx_margin_type_to_string(m->type));
    }
    for (size_t i = 0; i < m->children_count; i++) {
        const MxPageMarginsChild *ch = &m->children[i];
        if (ch->left_margin) {
            char *s = mx_tenths_to_string((*ch->left_margin));
            xmlNewTextChild(el, NULL, BAD_CAST "left-margin", BAD_CAST s);
            free(s);
        } else if (ch->right_margin) {
            char *s = mx_tenths_to_string((*ch->right_margin));
            xmlNewTextChild(el, NULL, BAD_CAST "right-margin", BAD_CAST s);
            free(s);
        } else if (ch->top_margin) {
            char *s = mx_tenths_to_string((*ch->top_margin));
            xmlNewTextChild(el, NULL, BAD_CAST "top-margin", BAD_CAST s);
            free(s);
        } else if (ch->bottom_margin) {
            char *s = mx_tenths_to_string((*ch->bottom_margin));
            xmlNewTextChild(el, NULL, BAD_CAST "bottom-margin", BAD_CAST s);
            free(s);
        }
    }
    return el;
}

void mx_page_margins_free(MxPageMargins *m) {
    if (!m)
        return;
    for (size_t i = 0; i < m->children_count; i++) {
        MxPageMarginsChild *ch = &m->children[i];
        free(ch->left_margin);
        free(ch->right_margin);
        free(ch->top_margin);
        free(ch->bottom_margin);
    }
    free(m->children);
    free(m);
}
