/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_TIME_MODIFICATION_H_INCLUDED
#define MX_TIME_MODIFICATION_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_empty.h"
#include "mx_note_type_value.h"

/*
 * Time modification indicates tuplets, double-note tremolos, and other durational changes. A
 * time-modification element shows how the cumulative, sounding effect of tuplets and double-note
 * tremolos compare to the written note type represented by the type and dot elements. Nested
 * tuplets and other notations that use more detailed information need both the time-modification
 * and tuplet elements to be represented accurately.
 */
/* One child element of MxTimeModification: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    long *actual_notes;
    long *normal_notes;
    MxNoteTypeValue *normal_type;
    MxEmpty *normal_dot;
} MxTimeModificationChild;

typedef struct {
    MxTimeModificationChild *children; /* child elements in document order */
    size_t children_count;
} MxTimeModification;

/* NULL on error; the message is in mx_error(). */
MxTimeModification *mx_time_modification_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_time_modification_serialize(const MxTimeModification *m, xmlNodePtr parent, const char *tag);
void mx_time_modification_free(MxTimeModification *m);

#endif /* MX_TIME_MODIFICATION_H_INCLUDED */
