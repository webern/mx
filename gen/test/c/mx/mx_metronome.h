/* Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT. */

#ifndef MX_METRONOME_H_INCLUDED
#define MX_METRONOME_H_INCLUDED

#include <libxml/tree.h>
#include <stdbool.h>
#include <stddef.h>
#include "mx_beat_unit_tied.h"
#include "mx_color.h"
#include "mx_comma_separated_text.h"
#include "mx_empty.h"
#include "mx_font_size.h"
#include "mx_font_style.h"
#include "mx_font_weight.h"
#include "mx_left_center_right.h"
#include "mx_metronome_note.h"
#include "mx_note_type_value.h"
#include "mx_per_minute.h"
#include "mx_tenths.h"
#include "mx_valign.h"
#include "mx_yes_no.h"

/*
 * The metronome type represents metronome marks and other metric relationships. The beat-unit group
 * and per-minute element specify regular metronome marks. The metronome-note and metronome-relation
 * elements allow for the specification of metric modulations and other metric relationships, such
 * as swing tempo marks where two eighths are equated to a quarter note / eighth note triplet. Tied
 * notes can be represented in both types of metronome marks by using the beat-unit-tied and
 * metronome-tied elements. The parentheses attribute indicates whether or not to put the metronome
 * mark in parentheses; its value is no if not specified.
 */
/* One child element of MxMetronome: exactly one field is non-NULL,
   and that pointer says which element this is. Document order is
   the array order on the owning struct. Zero or multiple fields
   set is undefined: serialization writes the first non-NULL field
   in schema order and nothing when all are NULL. */
typedef struct {
    MxNoteTypeValue *beat_unit;
    MxEmpty *beat_unit_dot;
    MxBeatUnitTied *beat_unit_tied;
    MxPerMinute *per_minute;
    MxEmpty *metronome_arrows;
    MxMetronomeNote *metronome_note;
    char *metronome_relation;
} MxMetronomeChild;

typedef struct {
    bool has_parentheses;
    MxYesNo parentheses; /* attribute parentheses */
    bool has_default_x;
    MxTenths default_x; /* attribute default-x */
    bool has_default_y;
    MxTenths default_y; /* attribute default-y */
    bool has_relative_x;
    MxTenths relative_x; /* attribute relative-x */
    bool has_relative_y;
    MxTenths relative_y; /* attribute relative-y */
    bool has_font_family;
    MxCommaSeparatedText font_family; /* attribute font-family */
    bool has_font_style;
    MxFontStyle font_style; /* attribute font-style */
    bool has_font_size;
    MxFontSize font_size; /* attribute font-size */
    bool has_font_weight;
    MxFontWeight font_weight; /* attribute font-weight */
    bool has_color;
    MxColor color; /* attribute color */
    bool has_halign;
    MxLeftCenterRight halign; /* attribute halign */
    bool has_valign;
    MxValign valign; /* attribute valign */
    bool has_justify;
    MxLeftCenterRight justify; /* attribute justify */
    bool has_id;
    char *id; /* attribute id */
    MxMetronomeChild *children; /* child elements in document order */
    size_t children_count;
} MxMetronome;

/* NULL on error; the message is in mx_error(). */
MxMetronome *mx_metronome_parse(xmlNodePtr el);
/* Appends under parent, or creates a free node when parent is NULL. */
xmlNodePtr mx_metronome_serialize(const MxMetronome *m, xmlNodePtr parent, const char *tag);
void mx_metronome_free(MxMetronome *m);

#endif /* MX_METRONOME_H_INCLUDED */
