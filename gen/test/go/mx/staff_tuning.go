// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The staff-tuning type specifies the open, non-capo tuning of the lines on a tablature staff.
type StaffTuning struct {
	Line     *StaffLine         // attribute "line"
	Children []StaffTuningChild // child elements in document order
}

// StaffTuningChild is one child element of StaffTuning: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type StaffTuningChild struct {
	TuningStep   *Step
	TuningAlter  *Semitones
	TuningOctave *Octave
}

func parseStaffTuning(el *etree.Element) (*StaffTuning, error) {
	m := &StaffTuning{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "line":
			v := ParseStaffLine(a.Value)
			m.Line = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "tuning-step":
			v := ParseStep(c.Text())
			m.Children = append(m.Children, StaffTuningChild{TuningStep: &v})
		case "tuning-alter":
			v := ParseSemitones(c.Text())
			m.Children = append(m.Children, StaffTuningChild{TuningAlter: &v})
		case "tuning-octave":
			v := ParseOctave(c.Text())
			m.Children = append(m.Children, StaffTuningChild{TuningOctave: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeStaffTuning(m *StaffTuning, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Line != nil {
		el.CreateAttr("line", (*m.Line).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.TuningStep != nil:
			el.CreateElement("tuning-step").SetText((*ch.TuningStep).String())
		case ch.TuningAlter != nil:
			el.CreateElement("tuning-alter").SetText((*ch.TuningAlter).String())
		case ch.TuningOctave != nil:
			el.CreateElement("tuning-octave").SetText((*ch.TuningOctave).String())
		}
	}
}
