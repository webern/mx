// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Scordatura string tunings are represented by a series of accord elements, similar to the
// staff-tuning elements. Strings are numbered from high to low.
type Scordatura struct {
	ID       *string           // attribute "id"
	Children []ScordaturaChild // child elements in document order
}

// ScordaturaChild is one child element of Scordatura: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type ScordaturaChild struct {
	Accord *Accord
}

func parseScordatura(el *etree.Element) (*Scordatura, error) {
	m := &Scordatura{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "accord":
			v, err := parseAccord(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScordaturaChild{Accord: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeScordatura(m *Scordatura, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.Accord != nil:
			serializeAccord(ch.Accord, el, "accord")
		}
	}
}
