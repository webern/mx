// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The measure-layout type includes the horizontal distance from the previous measure.
type MeasureLayout struct {
	Children []MeasureLayoutChild // child elements in document order
}

// MeasureLayoutChild is one child element of MeasureLayout: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type MeasureLayoutChild struct {
	MeasureDistance *Tenths
}

func parseMeasureLayout(el *etree.Element) (*MeasureLayout, error) {
	m := &MeasureLayout{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "measure-distance":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, MeasureLayoutChild{MeasureDistance: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeMeasureLayout(m *MeasureLayout, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.MeasureDistance != nil:
			el.CreateElement("measure-distance").SetText((*ch.MeasureDistance).String())
		}
	}
}
