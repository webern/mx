// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Time modification indicates tuplets, double-note tremolos, and other durational changes. A
// time-modification element shows how the cumulative, sounding effect of tuplets and double-note
// tremolos compare to the written note type represented by the type and dot elements. Nested
// tuplets and other notations that use more detailed information need both the time-modification
// and tuplet elements to be represented accurately.
type TimeModification struct {
	Children []TimeModificationChild // child elements in document order
}

// TimeModificationChild is one child element of TimeModification: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type TimeModificationChild struct {
	ActualNotes *int
	NormalNotes *int
	NormalType  *NoteTypeValue
	NormalDot   *Empty
}

func parseTimeModification(el *etree.Element) (*TimeModification, error) {
	m := &TimeModification{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "actual-notes":
			v := parseInt(c.Text())
			m.Children = append(m.Children, TimeModificationChild{ActualNotes: &v})
		case "normal-notes":
			v := parseInt(c.Text())
			m.Children = append(m.Children, TimeModificationChild{NormalNotes: &v})
		case "normal-type":
			v := ParseNoteTypeValue(c.Text())
			m.Children = append(m.Children, TimeModificationChild{NormalType: &v})
		case "normal-dot":
			v, err := parseEmpty(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, TimeModificationChild{NormalDot: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeTimeModification(m *TimeModification, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.ActualNotes != nil:
			el.CreateElement("actual-notes").SetText(formatInt((*ch.ActualNotes)))
		case ch.NormalNotes != nil:
			el.CreateElement("normal-notes").SetText(formatInt((*ch.NormalNotes)))
		case ch.NormalType != nil:
			el.CreateElement("normal-type").SetText((*ch.NormalType).String())
		case ch.NormalDot != nil:
			serializeEmpty(ch.NormalDot, el, "normal-dot")
		}
	}
}
