// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// A direction is a musical indication that is not necessarily attached to a specific note. Two or
// more may be combined to indicate starts and stops of wedges, dashes, etc. For applications where
// a specific direction is indeed attached to a specific note, the direction element can be
// associated with the note element that follows it in score order that is not in a different voice.
// By default, a series of direction-type elements and a series of child elements of a
// direction-type within a single direction element follow one another in sequence visually. For a
// series of direction-type children, non-positional formatting attributes are carried over from the
// previous element by default.
type Direction struct {
	Placement *AboveBelow      // attribute "placement"
	Directive *YesNo           // attribute "directive"
	ID        *string          // attribute "id"
	Children  []DirectionChild // child elements in document order
}

// DirectionChild is one child element of Direction: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type DirectionChild struct {
	DirectionType *DirectionType
	Offset        *Offset
	Footnote      *FormattedText
	Level         *Level
	Voice         *string
	Staff         *int
	Sound         *Sound
}

func parseDirection(el *etree.Element) (*Direction, error) {
	m := &Direction{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "placement":
			v := ParseAboveBelow(a.Value)
			m.Placement = &v
		case "directive":
			v := ParseYesNo(a.Value)
			m.Directive = &v
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "direction-type":
			v, err := parseDirectionType(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DirectionChild{DirectionType: v})
		case "offset":
			v, err := parseOffset(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DirectionChild{Offset: v})
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DirectionChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DirectionChild{Level: v})
		case "voice":
			v := c.Text()
			m.Children = append(m.Children, DirectionChild{Voice: &v})
		case "staff":
			v := parseInt(c.Text())
			m.Children = append(m.Children, DirectionChild{Staff: &v})
		case "sound":
			v, err := parseSound(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DirectionChild{Sound: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeDirection(m *Direction, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Placement != nil {
		el.CreateAttr("placement", (*m.Placement).String())
	}
	if m.Directive != nil {
		el.CreateAttr("directive", (*m.Directive).String())
	}
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.DirectionType != nil:
			serializeDirectionType(ch.DirectionType, el, "direction-type")
		case ch.Offset != nil:
			serializeOffset(ch.Offset, el, "offset")
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		case ch.Voice != nil:
			el.CreateElement("voice").SetText((*ch.Voice))
		case ch.Staff != nil:
			el.CreateElement("staff").SetText(formatInt((*ch.Staff)))
		case ch.Sound != nil:
			serializeSound(ch.Sound, el, "sound")
		}
	}
}
