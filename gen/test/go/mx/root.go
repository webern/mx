// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The root type indicates a pitch like C, D, E vs. a function indication like I, II, III. It is
// used with chord symbols in popular music. The root element has a root-step and optional
// root-alter element similar to the step and alter elements, but renamed to distinguish the
// different musical meanings.
type Root struct {
	Children []RootChild // child elements in document order
}

// RootChild is one child element of Root: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type RootChild struct {
	RootStep  *RootStep
	RootAlter *RootAlter
}

func parseRoot(el *etree.Element) (*Root, error) {
	m := &Root{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "root-step":
			v, err := parseRootStep(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, RootChild{RootStep: v})
		case "root-alter":
			v, err := parseRootAlter(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, RootChild{RootAlter: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeRoot(m *Root, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.RootStep != nil:
			serializeRootStep(ch.RootStep, el, "root-step")
		case ch.RootAlter != nil:
			serializeRootAlter(ch.RootAlter, el, "root-alter")
		}
	}
}
