// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The degree type is used to add, alter, or subtract individual notes in the chord. The
// print-object attribute can be used to keep the degree from printing separately when it has
// already taken into account in the text attribute of the kind element. The degree-value and
// degree-type text attributes specify how the value and type of the degree should be displayed. A
// harmony of kind "other" can be spelled explicitly by using a series of degree elements together
// with a root.
type Degree struct {
	PrintObject *YesNo        // attribute "print-object"
	Children    []DegreeChild // child elements in document order
}

// DegreeChild is one child element of Degree: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type DegreeChild struct {
	DegreeValue *DegreeValue
	DegreeAlter *DegreeAlter
	DegreeType  *DegreeType
}

func parseDegree(el *etree.Element) (*Degree, error) {
	m := &Degree{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "print-object":
			v := ParseYesNo(a.Value)
			m.PrintObject = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "degree-value":
			v, err := parseDegreeValue(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DegreeChild{DegreeValue: v})
		case "degree-alter":
			v, err := parseDegreeAlter(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DegreeChild{DegreeAlter: v})
		case "degree-type":
			v, err := parseDegreeType(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DegreeChild{DegreeType: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeDegree(m *Degree, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.PrintObject != nil {
		el.CreateAttr("print-object", (*m.PrintObject).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.DegreeValue != nil:
			serializeDegreeValue(ch.DegreeValue, el, "degree-value")
		case ch.DegreeAlter != nil:
			serializeDegreeAlter(ch.DegreeAlter, el, "degree-alter")
		case ch.DegreeType != nil:
			serializeDegreeType(ch.DegreeType, el, "degree-type")
		}
	}
}
