// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The score-instrument type represents a single instrument within a score-part. As with the
// score-part type, each score-instrument has a required ID attribute, a name, and an optional
// abbreviation. A score-instrument type is also required if the score specifies MIDI 1.0 channels,
// banks, or programs. An initial midi-instrument assignment can also be made here. MusicXML
// software should be able to automatically assign reasonable channels and instruments without these
// elements in simple cases, such as where part names match General MIDI instrument names.
type ScoreInstrument struct {
	ID       *string                // attribute "id"
	Children []ScoreInstrumentChild // child elements in document order
}

// ScoreInstrumentChild is one child element of ScoreInstrument: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type ScoreInstrumentChild struct {
	InstrumentName         *string
	InstrumentAbbreviation *string
	InstrumentSound        *string
	Solo                   *Empty
	Ensemble               *PositiveIntegerOrEmpty
	VirtualInstrument      *VirtualInstrument
}

func parseScoreInstrument(el *etree.Element) (*ScoreInstrument, error) {
	m := &ScoreInstrument{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "instrument-name":
			v := c.Text()
			m.Children = append(m.Children, ScoreInstrumentChild{InstrumentName: &v})
		case "instrument-abbreviation":
			v := c.Text()
			m.Children = append(m.Children, ScoreInstrumentChild{InstrumentAbbreviation: &v})
		case "instrument-sound":
			v := c.Text()
			m.Children = append(m.Children, ScoreInstrumentChild{InstrumentSound: &v})
		case "solo":
			v, err := parseEmpty(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreInstrumentChild{Solo: v})
		case "ensemble":
			v := ParsePositiveIntegerOrEmpty(c.Text())
			m.Children = append(m.Children, ScoreInstrumentChild{Ensemble: &v})
		case "virtual-instrument":
			v, err := parseVirtualInstrument(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreInstrumentChild{VirtualInstrument: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeScoreInstrument(m *ScoreInstrument, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.InstrumentName != nil:
			el.CreateElement("instrument-name").SetText((*ch.InstrumentName))
		case ch.InstrumentAbbreviation != nil:
			el.CreateElement("instrument-abbreviation").SetText((*ch.InstrumentAbbreviation))
		case ch.InstrumentSound != nil:
			el.CreateElement("instrument-sound").SetText((*ch.InstrumentSound))
		case ch.Solo != nil:
			serializeEmpty(ch.Solo, el, "solo")
		case ch.Ensemble != nil:
			el.CreateElement("ensemble").SetText((*ch.Ensemble).String())
		case ch.VirtualInstrument != nil:
			serializeVirtualInstrument(ch.VirtualInstrument, el, "virtual-instrument")
		}
	}
}
