// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The backup and forward elements are required to coordinate multiple voices in one part, including
// music on multiple staves. The forward element is generally used within voices and staves.
// Duration values should always be positive, and should not cross measure boundaries or mid-measure
// changes in the divisions value.
type Forward struct {
	Children []ForwardChild // child elements in document order
}

// ForwardChild is one child element of Forward: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type ForwardChild struct {
	Duration *PositiveDivisions
	Footnote *FormattedText
	Level    *Level
	Voice    *string
	Staff    *int
}

func parseForward(el *etree.Element) (*Forward, error) {
	m := &Forward{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "duration":
			v := ParsePositiveDivisions(c.Text())
			m.Children = append(m.Children, ForwardChild{Duration: &v})
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ForwardChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ForwardChild{Level: v})
		case "voice":
			v := c.Text()
			m.Children = append(m.Children, ForwardChild{Voice: &v})
		case "staff":
			v := parseInt(c.Text())
			m.Children = append(m.Children, ForwardChild{Staff: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeForward(m *Forward, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Duration != nil:
			el.CreateElement("duration").SetText((*ch.Duration).String())
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		case ch.Voice != nil:
			el.CreateElement("voice").SetText((*ch.Voice))
		case ch.Staff != nil:
			el.CreateElement("staff").SetText(formatInt((*ch.Staff)))
		}
	}
}
