// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The part-list identifies the different musical parts in this movement. Each part has an ID that
// is used later within the musical data. Since parts may be encoded separately and combined later,
// identification elements are present at both the score and score-part levels. There must be at
// least one score-part, combined as desired with part-group elements that indicate braces and
// brackets. Parts are ordered from top to bottom in a score based on the order in which they appear
// in the part-list.
type PartList struct {
	Children []PartListChild // child elements in document order
}

// PartListChild is one child element of PartList: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PartListChild struct {
	PartGroup *PartGroup
	ScorePart *ScorePart
}

func parsePartList(el *etree.Element) (*PartList, error) {
	m := &PartList{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "part-group":
			v, err := parsePartGroup(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartListChild{PartGroup: v})
		case "score-part":
			v, err := parseScorePart(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartListChild{ScorePart: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePartList(m *PartList, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.PartGroup != nil:
			serializePartGroup(ch.PartGroup, el, "part-group")
		case ch.ScorePart != nil:
			serializeScorePart(ch.ScorePart, el, "score-part")
		}
	}
}
