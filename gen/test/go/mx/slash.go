// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The slash type is used to indicate that slash notation is to be used. If the slash is on every
// beat, use-stems is no (the default). To indicate rhythms but not pitches, use-stems is set to
// yes. The type attribute indicates whether this is the start or stop of a slash notation style.
// The use-dots attribute works as for the beat-repeat element, and only has effect if use-stems is
// no.
type Slash struct {
	Type     *StartStop   // attribute "type"
	UseDots  *YesNo       // attribute "use-dots"
	UseStems *YesNo       // attribute "use-stems"
	Children []SlashChild // child elements in document order
}

// SlashChild is one child element of Slash: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type SlashChild struct {
	SlashType   *NoteTypeValue
	SlashDot    *Empty
	ExceptVoice *string
}

func parseSlash(el *etree.Element) (*Slash, error) {
	m := &Slash{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "type":
			v := ParseStartStop(a.Value)
			m.Type = &v
		case "use-dots":
			v := ParseYesNo(a.Value)
			m.UseDots = &v
		case "use-stems":
			v := ParseYesNo(a.Value)
			m.UseStems = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "slash-type":
			v := ParseNoteTypeValue(c.Text())
			m.Children = append(m.Children, SlashChild{SlashType: &v})
		case "slash-dot":
			v, err := parseEmpty(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, SlashChild{SlashDot: v})
		case "except-voice":
			v := c.Text()
			m.Children = append(m.Children, SlashChild{ExceptVoice: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeSlash(m *Slash, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Type != nil {
		el.CreateAttr("type", (*m.Type).String())
	}
	if m.UseDots != nil {
		el.CreateAttr("use-dots", (*m.UseDots).String())
	}
	if m.UseStems != nil {
		el.CreateAttr("use-stems", (*m.UseStems).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.SlashType != nil:
			el.CreateElement("slash-type").SetText((*ch.SlashType).String())
		case ch.SlashDot != nil:
			serializeEmpty(ch.SlashDot, el, "slash-dot")
		case ch.ExceptVoice != nil:
			el.CreateElement("except-voice").SetText((*ch.ExceptVoice))
		}
	}
}
