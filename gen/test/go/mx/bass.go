// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The bass type is used to indicate a bass note in popular music chord symbols, e.g. G/C. It is
// generally not used in functional harmony, as inversion is generally not used in pop chord
// symbols. As with root, it is divided into step and alter elements, similar to pitches.
type Bass struct {
	Children []BassChild // child elements in document order
}

// BassChild is one child element of Bass: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type BassChild struct {
	BassStep  *BassStep
	BassAlter *BassAlter
}

func parseBass(el *etree.Element) (*Bass, error) {
	m := &Bass{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "bass-step":
			v, err := parseBassStep(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BassChild{BassStep: v})
		case "bass-alter":
			v, err := parseBassAlter(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BassChild{BassAlter: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeBass(m *Bass, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.BassStep != nil:
			serializeBassStep(ch.BassStep, el, "bass-step")
		case ch.BassAlter != nil:
			serializeBassAlter(ch.BassAlter, el, "bass-alter")
		}
	}
}
