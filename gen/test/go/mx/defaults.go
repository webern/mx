// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The defaults type specifies score-wide defaults for scaling, layout, and appearance.
type Defaults struct {
	Children []DefaultsChild // child elements in document order
}

// DefaultsChild is one child element of Defaults: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type DefaultsChild struct {
	Scaling       *Scaling
	PageLayout    *PageLayout
	SystemLayout  *SystemLayout
	StaffLayout   *StaffLayout
	Appearance    *Appearance
	MusicFont     *EmptyFont
	WordFont      *EmptyFont
	LyricFont     *LyricFont
	LyricLanguage *LyricLanguage
}

func parseDefaults(el *etree.Element) (*Defaults, error) {
	m := &Defaults{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "scaling":
			v, err := parseScaling(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{Scaling: v})
		case "page-layout":
			v, err := parsePageLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{PageLayout: v})
		case "system-layout":
			v, err := parseSystemLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{SystemLayout: v})
		case "staff-layout":
			v, err := parseStaffLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{StaffLayout: v})
		case "appearance":
			v, err := parseAppearance(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{Appearance: v})
		case "music-font":
			v, err := parseEmptyFont(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{MusicFont: v})
		case "word-font":
			v, err := parseEmptyFont(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{WordFont: v})
		case "lyric-font":
			v, err := parseLyricFont(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{LyricFont: v})
		case "lyric-language":
			v, err := parseLyricLanguage(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, DefaultsChild{LyricLanguage: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeDefaults(m *Defaults, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Scaling != nil:
			serializeScaling(ch.Scaling, el, "scaling")
		case ch.PageLayout != nil:
			serializePageLayout(ch.PageLayout, el, "page-layout")
		case ch.SystemLayout != nil:
			serializeSystemLayout(ch.SystemLayout, el, "system-layout")
		case ch.StaffLayout != nil:
			serializeStaffLayout(ch.StaffLayout, el, "staff-layout")
		case ch.Appearance != nil:
			serializeAppearance(ch.Appearance, el, "appearance")
		case ch.MusicFont != nil:
			serializeEmptyFont(ch.MusicFont, el, "music-font")
		case ch.WordFont != nil:
			serializeEmptyFont(ch.WordFont, el, "word-font")
		case ch.LyricFont != nil:
			serializeLyricFont(ch.LyricFont, el, "lyric-font")
		case ch.LyricLanguage != nil:
			serializeLyricLanguage(ch.LyricLanguage, el, "lyric-language")
		}
	}
}
