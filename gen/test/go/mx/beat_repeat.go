// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The beat-repeat type is used to indicate that a single beat (but possibly many notes) is
// repeated. Both the start and stop of the beat being repeated should be specified. The slashes
// attribute specifies the number of slashes to use in the symbol. The use-dots attribute indicates
// whether or not to use dots as well (for instance, with mixed rhythm patterns). By default, the
// value for slashes is 1 and the value for use-dots is no. The beat-repeat element specifies a
// notation style for repetitions. The actual music being repeated needs to be repeated within the
// MusicXML file. This element specifies the notation that indicates the repeat.
type BeatRepeat struct {
	Type     *StartStop        // attribute "type"
	Slashes  *int              // attribute "slashes"
	UseDots  *YesNo            // attribute "use-dots"
	Children []BeatRepeatChild // child elements in document order
}

// BeatRepeatChild is one child element of BeatRepeat: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type BeatRepeatChild struct {
	SlashType   *NoteTypeValue
	SlashDot    *Empty
	ExceptVoice *string
}

func parseBeatRepeat(el *etree.Element) (*BeatRepeat, error) {
	m := &BeatRepeat{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "type":
			v := ParseStartStop(a.Value)
			m.Type = &v
		case "slashes":
			v := parseInt(a.Value)
			m.Slashes = &v
		case "use-dots":
			v := ParseYesNo(a.Value)
			m.UseDots = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "slash-type":
			v := ParseNoteTypeValue(c.Text())
			m.Children = append(m.Children, BeatRepeatChild{SlashType: &v})
		case "slash-dot":
			v, err := parseEmpty(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BeatRepeatChild{SlashDot: v})
		case "except-voice":
			v := c.Text()
			m.Children = append(m.Children, BeatRepeatChild{ExceptVoice: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeBeatRepeat(m *BeatRepeat, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Type != nil {
		el.CreateAttr("type", (*m.Type).String())
	}
	if m.Slashes != nil {
		el.CreateAttr("slashes", formatInt((*m.Slashes)))
	}
	if m.UseDots != nil {
		el.CreateAttr("use-dots", (*m.UseDots).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.SlashType != nil:
			el.CreateElement("slash-type").SetText((*ch.SlashType).String())
		case ch.SlashDot != nil:
			serializeEmpty(ch.SlashDot, el, "slash-dot")
		case ch.ExceptVoice != nil:
			el.CreateElement("except-voice").SetText((*ch.ExceptVoice))
		}
	}
}
