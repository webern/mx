// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The accord type represents the tuning of a single string in the scordatura element. It uses the
// same group of elements as the staff-tuning element. Strings are numbered from high to low.
type Accord struct {
	String   *StringNumber // attribute "string"
	Children []AccordChild // child elements in document order
}

// AccordChild is one child element of Accord: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type AccordChild struct {
	TuningStep   *Step
	TuningAlter  *Semitones
	TuningOctave *Octave
}

func parseAccord(el *etree.Element) (*Accord, error) {
	m := &Accord{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "string":
			v := ParseStringNumber(a.Value)
			m.String = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "tuning-step":
			v := ParseStep(c.Text())
			m.Children = append(m.Children, AccordChild{TuningStep: &v})
		case "tuning-alter":
			v := ParseSemitones(c.Text())
			m.Children = append(m.Children, AccordChild{TuningAlter: &v})
		case "tuning-octave":
			v := ParseOctave(c.Text())
			m.Children = append(m.Children, AccordChild{TuningOctave: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeAccord(m *Accord, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.String != nil {
		el.CreateAttr("string", (*m.String).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.TuningStep != nil:
			el.CreateElement("tuning-step").SetText((*ch.TuningStep).String())
		case ch.TuningAlter != nil:
			el.CreateElement("tuning-alter").SetText((*ch.TuningAlter).String())
		case ch.TuningOctave != nil:
			el.CreateElement("tuning-octave").SetText((*ch.TuningOctave).String())
		}
	}
}
