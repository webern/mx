// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The midi-instrument type defines MIDI 1.0 instrument playback. The midi-instrument element can be
// a part of either the score-instrument element at the start of a part, or the sound element within
// a part. The id attribute refers to the score-instrument affected by the change.
type MIDIInstrument struct {
	ID       *string               // attribute "id"
	Children []MIDIInstrumentChild // child elements in document order
}

// MIDIInstrumentChild is one child element of MIDIInstrument: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type MIDIInstrumentChild struct {
	MIDIChannel   *MIDI16
	MIDIName      *string
	MIDIBank      *MIDI16384
	MIDIProgram   *MIDI128
	MIDIUnpitched *MIDI128
	Volume        *Percent
	Pan           *RotationDegrees
	Elevation     *RotationDegrees
}

func parseMIDIInstrument(el *etree.Element) (*MIDIInstrument, error) {
	m := &MIDIInstrument{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "midi-channel":
			v := ParseMIDI16(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{MIDIChannel: &v})
		case "midi-name":
			v := c.Text()
			m.Children = append(m.Children, MIDIInstrumentChild{MIDIName: &v})
		case "midi-bank":
			v := ParseMIDI16384(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{MIDIBank: &v})
		case "midi-program":
			v := ParseMIDI128(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{MIDIProgram: &v})
		case "midi-unpitched":
			v := ParseMIDI128(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{MIDIUnpitched: &v})
		case "volume":
			v := ParsePercent(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{Volume: &v})
		case "pan":
			v := ParseRotationDegrees(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{Pan: &v})
		case "elevation":
			v := ParseRotationDegrees(c.Text())
			m.Children = append(m.Children, MIDIInstrumentChild{Elevation: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeMIDIInstrument(m *MIDIInstrument, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.MIDIChannel != nil:
			el.CreateElement("midi-channel").SetText((*ch.MIDIChannel).String())
		case ch.MIDIName != nil:
			el.CreateElement("midi-name").SetText((*ch.MIDIName))
		case ch.MIDIBank != nil:
			el.CreateElement("midi-bank").SetText((*ch.MIDIBank).String())
		case ch.MIDIProgram != nil:
			el.CreateElement("midi-program").SetText((*ch.MIDIProgram).String())
		case ch.MIDIUnpitched != nil:
			el.CreateElement("midi-unpitched").SetText((*ch.MIDIUnpitched).String())
		case ch.Volume != nil:
			el.CreateElement("volume").SetText((*ch.Volume).String())
		case ch.Pan != nil:
			el.CreateElement("pan").SetText((*ch.Pan).String())
		case ch.Elevation != nil:
			el.CreateElement("elevation").SetText((*ch.Elevation).String())
		}
	}
}
