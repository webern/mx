// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The notehead-text type represents text that is displayed inside a notehead, as is done in some
// educational music. It is not needed for the numbers used in tablature or jianpu notation. The
// presence of a TAB or jianpu clefs is sufficient to indicate that numbers are used. The
// display-text and accidental-text elements allow display of fully formatted text and accidentals.
type NoteheadText struct {
	Children []NoteheadTextChild // child elements in document order
}

// NoteheadTextChild is one child element of NoteheadText: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type NoteheadTextChild struct {
	DisplayText    *FormattedText
	AccidentalText *AccidentalText
}

func parseNoteheadText(el *etree.Element) (*NoteheadText, error) {
	m := &NoteheadText{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "display-text":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, NoteheadTextChild{DisplayText: v})
		case "accidental-text":
			v, err := parseAccidentalText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, NoteheadTextChild{AccidentalText: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeNoteheadText(m *NoteheadText, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.DisplayText != nil:
			serializeFormattedText(ch.DisplayText, el, "display-text")
		case ch.AccidentalText != nil:
			serializeAccidentalText(ch.AccidentalText, el, "accidental-text")
		}
	}
}
