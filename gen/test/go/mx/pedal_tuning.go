// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The pedal-tuning type specifies the tuning of a single harp pedal.
type PedalTuning struct {
	Children []PedalTuningChild // child elements in document order
}

// PedalTuningChild is one child element of PedalTuning: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PedalTuningChild struct {
	PedalStep  *Step
	PedalAlter *Semitones
}

func parsePedalTuning(el *etree.Element) (*PedalTuning, error) {
	m := &PedalTuning{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "pedal-step":
			v := ParseStep(c.Text())
			m.Children = append(m.Children, PedalTuningChild{PedalStep: &v})
		case "pedal-alter":
			v := ParseSemitones(c.Text())
			m.Children = append(m.Children, PedalTuningChild{PedalAlter: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePedalTuning(m *PedalTuning, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.PedalStep != nil:
			el.CreateElement("pedal-step").SetText((*ch.PedalStep).String())
		case ch.PedalAlter != nil:
			el.CreateElement("pedal-alter").SetText((*ch.PedalAlter).String())
		}
	}
}
