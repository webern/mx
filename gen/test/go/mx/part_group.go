// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The part-group element indicates groupings of parts in the score, usually indicated by braces and
// brackets. Braces that are used for multi-staff parts should be defined in the attributes element
// for that part. The part-group start element appears before the first score-part in the group. The
// part-group stop element appears after the last score-part in the group. The number attribute is
// used to distinguish overlapping and nested part-groups, not the sequence of groups. As with
// parts, groups can have a name and abbreviation. Values for the child elements are ignored at the
// stop of a group. A part-group element is not needed for a single multi-staff part. By default,
// multi-staff parts include a brace symbol and (if appropriate given the bar-style) common
// barlines. The symbol formatting for a multi-staff part can be more fully specified using the
// part-symbol element.
type PartGroup struct {
	Type     *StartStop       // attribute "type"
	Number   *string          // attribute "number"
	Children []PartGroupChild // child elements in document order
}

// PartGroupChild is one child element of PartGroup: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PartGroupChild struct {
	GroupName                *GroupName
	GroupNameDisplay         *NameDisplay
	GroupAbbreviation        *GroupName
	GroupAbbreviationDisplay *NameDisplay
	GroupSymbol              *GroupSymbol
	GroupBarline             *GroupBarline
	GroupTime                *Empty
	Footnote                 *FormattedText
	Level                    *Level
}

func parsePartGroup(el *etree.Element) (*PartGroup, error) {
	m := &PartGroup{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "type":
			v := ParseStartStop(a.Value)
			m.Type = &v
		case "number":
			v := a.Value
			m.Number = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "group-name":
			v, err := parseGroupName(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupName: v})
		case "group-name-display":
			v, err := parseNameDisplay(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupNameDisplay: v})
		case "group-abbreviation":
			v, err := parseGroupName(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupAbbreviation: v})
		case "group-abbreviation-display":
			v, err := parseNameDisplay(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupAbbreviationDisplay: v})
		case "group-symbol":
			v, err := parseGroupSymbol(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupSymbol: v})
		case "group-barline":
			v, err := parseGroupBarline(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupBarline: v})
		case "group-time":
			v, err := parseEmpty(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{GroupTime: v})
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PartGroupChild{Level: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePartGroup(m *PartGroup, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Type != nil {
		el.CreateAttr("type", (*m.Type).String())
	}
	if m.Number != nil {
		el.CreateAttr("number", (*m.Number))
	}
	for _, ch := range m.Children {
		switch {
		case ch.GroupName != nil:
			serializeGroupName(ch.GroupName, el, "group-name")
		case ch.GroupNameDisplay != nil:
			serializeNameDisplay(ch.GroupNameDisplay, el, "group-name-display")
		case ch.GroupAbbreviation != nil:
			serializeGroupName(ch.GroupAbbreviation, el, "group-abbreviation")
		case ch.GroupAbbreviationDisplay != nil:
			serializeNameDisplay(ch.GroupAbbreviationDisplay, el, "group-abbreviation-display")
		case ch.GroupSymbol != nil:
			serializeGroupSymbol(ch.GroupSymbol, el, "group-symbol")
		case ch.GroupBarline != nil:
			serializeGroupBarline(ch.GroupBarline, el, "group-barline")
		case ch.GroupTime != nil:
			serializeEmpty(ch.GroupTime, el, "group-time")
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		}
	}
}
