// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Identification contains basic metadata about the score. It includes the information in MuseData
// headers that may apply at a score-wide, movement-wide, or part-wide level. The creator, rights,
// source, and relation elements are based on Dublin Core.
type Identification struct {
	Children []IdentificationChild // child elements in document order
}

// IdentificationChild is one child element of Identification: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type IdentificationChild struct {
	Creator       *TypedText
	Rights        *TypedText
	Encoding      *Encoding
	Source        *string
	Relation      *TypedText
	Miscellaneous *Miscellaneous
}

func parseIdentification(el *etree.Element) (*Identification, error) {
	m := &Identification{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "creator":
			v, err := parseTypedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, IdentificationChild{Creator: v})
		case "rights":
			v, err := parseTypedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, IdentificationChild{Rights: v})
		case "encoding":
			v, err := parseEncoding(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, IdentificationChild{Encoding: v})
		case "source":
			v := c.Text()
			m.Children = append(m.Children, IdentificationChild{Source: &v})
		case "relation":
			v, err := parseTypedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, IdentificationChild{Relation: v})
		case "miscellaneous":
			v, err := parseMiscellaneous(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, IdentificationChild{Miscellaneous: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeIdentification(m *Identification, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Creator != nil:
			serializeTypedText(ch.Creator, el, "creator")
		case ch.Rights != nil:
			serializeTypedText(ch.Rights, el, "rights")
		case ch.Encoding != nil:
			serializeEncoding(ch.Encoding, el, "encoding")
		case ch.Source != nil:
			el.CreateElement("source").SetText((*ch.Source))
		case ch.Relation != nil:
			serializeTypedText(ch.Relation, el, "relation")
		case ch.Miscellaneous != nil:
			serializeMiscellaneous(ch.Miscellaneous, el, "miscellaneous")
		}
	}
}
