// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The play type, new in Version 3.0, specifies playback techniques to be used in conjunction with
// the instrument-sound element. When used as part of a sound element, it applies to all notes going
// forward in score order. In multi-instrument parts, the affected instrument should be specified
// using the id attribute. When used as part of a note element, it applies to the current note only.
type Play struct {
	ID       *string     // attribute "id"
	Children []PlayChild // child elements in document order
}

// PlayChild is one child element of Play: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PlayChild struct {
	Ipa         *string
	Mute        *Mute
	SemiPitched *SemiPitched
	OtherPlay   *OtherPlay
}

func parsePlay(el *etree.Element) (*Play, error) {
	m := &Play{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "ipa":
			v := c.Text()
			m.Children = append(m.Children, PlayChild{Ipa: &v})
		case "mute":
			v := ParseMute(c.Text())
			m.Children = append(m.Children, PlayChild{Mute: &v})
		case "semi-pitched":
			v := ParseSemiPitched(c.Text())
			m.Children = append(m.Children, PlayChild{SemiPitched: &v})
		case "other-play":
			v, err := parseOtherPlay(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PlayChild{OtherPlay: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePlay(m *Play, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.Ipa != nil:
			el.CreateElement("ipa").SetText((*ch.Ipa))
		case ch.Mute != nil:
			el.CreateElement("mute").SetText((*ch.Mute).String())
		case ch.SemiPitched != nil:
			el.CreateElement("semi-pitched").SetText((*ch.SemiPitched).String())
		case ch.OtherPlay != nil:
			serializeOtherPlay(ch.OtherPlay, el, "other-play")
		}
	}
}
