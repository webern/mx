// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// A system is a group of staves that are read and played simultaneously. System layout includes
// left and right margins and the vertical distance from the previous system. The system distance is
// measured from the bottom line of the previous system to the top line of the current system. It is
// ignored for the first system on a page. The top system distance is measured from the page's top
// margin to the top line of the first system. It is ignored for all but the first system on a page.
// Sometimes the sum of measure widths in a system may not equal the system width specified by the
// layout elements due to roundoff or other errors. The behavior when reading MusicXML files in
// these cases is application-dependent. For instance, applications may find that the system layout
// data is more reliable than the sum of the measure widths, and adjust the measure widths
// accordingly.
type SystemLayout struct {
	Children []SystemLayoutChild // child elements in document order
}

// SystemLayoutChild is one child element of SystemLayout: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type SystemLayoutChild struct {
	SystemMargins     *SystemMargins
	SystemDistance    *Tenths
	TopSystemDistance *Tenths
	SystemDividers    *SystemDividers
}

func parseSystemLayout(el *etree.Element) (*SystemLayout, error) {
	m := &SystemLayout{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "system-margins":
			v, err := parseSystemMargins(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, SystemLayoutChild{SystemMargins: v})
		case "system-distance":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, SystemLayoutChild{SystemDistance: &v})
		case "top-system-distance":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, SystemLayoutChild{TopSystemDistance: &v})
		case "system-dividers":
			v, err := parseSystemDividers(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, SystemLayoutChild{SystemDividers: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeSystemLayout(m *SystemLayout, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.SystemMargins != nil:
			serializeSystemMargins(ch.SystemMargins, el, "system-margins")
		case ch.SystemDistance != nil:
			el.CreateElement("system-distance").SetText((*ch.SystemDistance).String())
		case ch.TopSystemDistance != nil:
			el.CreateElement("top-system-distance").SetText((*ch.TopSystemDistance).String())
		case ch.SystemDividers != nil:
			serializeSystemDividers(ch.SystemDividers, el, "system-dividers")
		}
	}
}
