// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Works are optionally identified by number and title. The work type also may indicate a link to
// the opus document that composes multiple scores into a collection.
type Work struct {
	Children []WorkChild // child elements in document order
}

// WorkChild is one child element of Work: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type WorkChild struct {
	WorkNumber *string
	WorkTitle  *string
	Opus       *Opus
}

func parseWork(el *etree.Element) (*Work, error) {
	m := &Work{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "work-number":
			v := c.Text()
			m.Children = append(m.Children, WorkChild{WorkNumber: &v})
		case "work-title":
			v := c.Text()
			m.Children = append(m.Children, WorkChild{WorkTitle: &v})
		case "opus":
			v, err := parseOpus(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, WorkChild{Opus: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeWork(m *Work, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.WorkNumber != nil:
			el.CreateElement("work-number").SetText((*ch.WorkNumber))
		case ch.WorkTitle != nil:
			el.CreateElement("work-title").SetText((*ch.WorkTitle))
		case ch.Opus != nil:
			serializeOpus(ch.Opus, el, "opus")
		}
	}
}
