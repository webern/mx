// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The backup and forward elements are required to coordinate multiple voices in one part, including
// music on multiple staves. The backup type is generally used to move between voices and staves.
// Thus the backup element does not include voice or staff elements. Duration values should always
// be positive, and should not cross measure boundaries or mid-measure changes in the divisions
// value.
type Backup struct {
	Children []BackupChild // child elements in document order
}

// BackupChild is one child element of Backup: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type BackupChild struct {
	Duration *PositiveDivisions
	Footnote *FormattedText
	Level    *Level
}

func parseBackup(el *etree.Element) (*Backup, error) {
	m := &Backup{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "duration":
			v := ParsePositiveDivisions(c.Text())
			m.Children = append(m.Children, BackupChild{Duration: &v})
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BackupChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BackupChild{Level: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeBackup(m *Backup, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Duration != nil:
			el.CreateElement("duration").SetText((*ch.Duration).String())
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		}
	}
}
