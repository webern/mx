// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Pitch is represented as a combination of the step of the diatonic scale, the chromatic
// alteration, and the octave.
type Pitch struct {
	Children []PitchChild // child elements in document order
}

// PitchChild is one child element of Pitch: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PitchChild struct {
	Step   *Step
	Alter  *Semitones
	Octave *Octave
}

func parsePitch(el *etree.Element) (*Pitch, error) {
	m := &Pitch{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "step":
			v := ParseStep(c.Text())
			m.Children = append(m.Children, PitchChild{Step: &v})
		case "alter":
			v := ParseSemitones(c.Text())
			m.Children = append(m.Children, PitchChild{Alter: &v})
		case "octave":
			v := ParseOctave(c.Text())
			m.Children = append(m.Children, PitchChild{Octave: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePitch(m *Pitch, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Step != nil:
			el.CreateElement("step").SetText((*ch.Step).String())
		case ch.Alter != nil:
			el.CreateElement("alter").SetText((*ch.Alter).String())
		case ch.Octave != nil:
			el.CreateElement("octave").SetText((*ch.Octave).String())
		}
	}
}
