// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// If a barline is other than a normal single barline, it should be represented by a barline type
// that describes it. This includes information about repeats and multiple endings, as well as line
// style. Barline data is on the same level as the other musical data in a score - a child of a
// measure in a partwise score, or a part in a timewise score. This allows for barlines within
// measures, as in dotted barlines that subdivide measures in complex meters. The two fermata
// elements allow for fermatas on both sides of the barline (the lower one inverted). Barlines have
// a location attribute to make it easier to process barlines independently of the other musical
// data in a score. It is often easier to set up measures separately from entering notes. The
// location attribute must match where the barline element occurs within the rest of the musical
// data in the score. If location is left, it should be the first element in the measure, aside from
// the print, bookmark, and link elements. If location is right, it should be the last element,
// again with the possible exception of the print, bookmark, and link elements. If no location is
// specified, the right barline is the default. The segno, coda, and divisions attributes work the
// same way as in the sound element. They are used for playback when barline elements contain segno
// or coda child elements.
type Barline struct {
	Location   *RightLeftMiddle // attribute "location"
	SegnoSound *string          // attribute "segno"
	CodaSound  *string          // attribute "coda"
	Divisions  *Divisions       // attribute "divisions"
	ID         *string          // attribute "id"
	Children   []BarlineChild   // child elements in document order
}

// BarlineChild is one child element of Barline: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type BarlineChild struct {
	BarStyle *BarStyleColor
	Footnote *FormattedText
	Level    *Level
	WavyLine *WavyLine
	Segno    *Segno
	Coda     *Coda
	Fermata  *Fermata
	Ending   *Ending
	Repeat   *Repeat
}

func parseBarline(el *etree.Element) (*Barline, error) {
	m := &Barline{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "location":
			v := ParseRightLeftMiddle(a.Value)
			m.Location = &v
		case "segno":
			v := a.Value
			m.SegnoSound = &v
		case "coda":
			v := a.Value
			m.CodaSound = &v
		case "divisions":
			v := ParseDivisions(a.Value)
			m.Divisions = &v
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "bar-style":
			v, err := parseBarStyleColor(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{BarStyle: v})
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Level: v})
		case "wavy-line":
			v, err := parseWavyLine(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{WavyLine: v})
		case "segno":
			v, err := parseSegno(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Segno: v})
		case "coda":
			v, err := parseCoda(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Coda: v})
		case "fermata":
			v, err := parseFermata(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Fermata: v})
		case "ending":
			v, err := parseEnding(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Ending: v})
		case "repeat":
			v, err := parseRepeat(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BarlineChild{Repeat: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeBarline(m *Barline, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Location != nil {
		el.CreateAttr("location", (*m.Location).String())
	}
	if m.SegnoSound != nil {
		el.CreateAttr("segno", (*m.SegnoSound))
	}
	if m.CodaSound != nil {
		el.CreateAttr("coda", (*m.CodaSound))
	}
	if m.Divisions != nil {
		el.CreateAttr("divisions", (*m.Divisions).String())
	}
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.BarStyle != nil:
			serializeBarStyleColor(ch.BarStyle, el, "bar-style")
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		case ch.WavyLine != nil:
			serializeWavyLine(ch.WavyLine, el, "wavy-line")
		case ch.Segno != nil:
			serializeSegno(ch.Segno, el, "segno")
		case ch.Coda != nil:
			serializeCoda(ch.Coda, el, "coda")
		case ch.Fermata != nil:
			serializeFermata(ch.Fermata, el, "fermata")
		case ch.Ending != nil:
			serializeEnding(ch.Ending, el, "ending")
		case ch.Repeat != nil:
			serializeRepeat(ch.Repeat, el, "repeat")
		}
	}
}
