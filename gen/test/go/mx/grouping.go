// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The grouping type is used for musical analysis. When the type attribute is "start" or "single",
// it usually contains one or more feature elements. The number attribute is used for distinguishing
// between overlapping and hierarchical groupings. The member-of attribute allows for easy
// distinguishing of what grouping elements are in what hierarchy. Feature elements contained within
// a "stop" type of grouping may be ignored. This element is flexible to allow for different types
// of analyses. Future versions of the MusicXML format may add elements that can represent more
// standardized categories of analysis data, allowing for easier data sharing.
type Grouping struct {
	Type     *StartStopSingle // attribute "type"
	Number   *string          // attribute "number"
	MemberOf *string          // attribute "member-of"
	ID       *string          // attribute "id"
	Children []GroupingChild  // child elements in document order
}

// GroupingChild is one child element of Grouping: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type GroupingChild struct {
	Feature *Feature
}

func parseGrouping(el *etree.Element) (*Grouping, error) {
	m := &Grouping{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "type":
			v := ParseStartStopSingle(a.Value)
			m.Type = &v
		case "number":
			v := a.Value
			m.Number = &v
		case "member-of":
			v := a.Value
			m.MemberOf = &v
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "feature":
			v, err := parseFeature(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, GroupingChild{Feature: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeGrouping(m *Grouping, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Type != nil {
		el.CreateAttr("type", (*m.Type).String())
	}
	if m.Number != nil {
		el.CreateAttr("number", (*m.Number))
	}
	if m.MemberOf != nil {
		el.CreateAttr("member-of", (*m.MemberOf))
	}
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.Feature != nil:
			serializeFeature(ch.Feature, el, "feature")
		}
	}
}
