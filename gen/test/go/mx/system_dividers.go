// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The system-dividers element indicates the presence or absence of system dividers (also known as
// system separation marks) between systems displayed on the same page. Dividers on the left and
// right side of the page are controlled by the left-divider and right-divider elements
// respectively. The default vertical position is half the system-distance value from the top of the
// system that is below the divider. The default horizontal position is the left and right system
// margin, respectively. When used in the print element, the system-dividers element affects the
// dividers that would appear between the current system and the previous system.
type SystemDividers struct {
	Children []SystemDividersChild // child elements in document order
}

// SystemDividersChild is one child element of SystemDividers: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type SystemDividersChild struct {
	LeftDivider  *EmptyPrintObjectStyleAlign
	RightDivider *EmptyPrintObjectStyleAlign
}

func parseSystemDividers(el *etree.Element) (*SystemDividers, error) {
	m := &SystemDividers{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "left-divider":
			v, err := parseEmptyPrintObjectStyleAlign(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, SystemDividersChild{LeftDivider: v})
		case "right-divider":
			v, err := parseEmptyPrintObjectStyleAlign(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, SystemDividersChild{RightDivider: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeSystemDividers(m *SystemDividers, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.LeftDivider != nil:
			serializeEmptyPrintObjectStyleAlign(ch.LeftDivider, el, "left-divider")
		case ch.RightDivider != nil:
			serializeEmptyPrintObjectStyleAlign(ch.RightDivider, el, "right-divider")
		}
	}
}
