// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The attributes element contains musical information that typically changes on measure boundaries.
// This includes key and time signatures, clefs, transpositions, and staving. When attributes are
// changed mid-measure, it affects the music in score order, not in MusicXML document order.
type Attributes struct {
	Children []AttributesChild // child elements in document order
}

// AttributesChild is one child element of Attributes: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type AttributesChild struct {
	Footnote     *FormattedText
	Level        *Level
	Divisions    *PositiveDivisions
	Key          *Key
	Time         *Time
	Staves       *int
	PartSymbol   *PartSymbol
	Instruments  *int
	Clef         *Clef
	StaffDetails *StaffDetails
	Transpose    *Transpose
	Directive    *Directive
	MeasureStyle *MeasureStyle
}

func parseAttributes(el *etree.Element) (*Attributes, error) {
	m := &Attributes{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Level: v})
		case "divisions":
			v := ParsePositiveDivisions(c.Text())
			m.Children = append(m.Children, AttributesChild{Divisions: &v})
		case "key":
			v, err := parseKey(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Key: v})
		case "time":
			v, err := parseTime(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Time: v})
		case "staves":
			v := parseInt(c.Text())
			m.Children = append(m.Children, AttributesChild{Staves: &v})
		case "part-symbol":
			v, err := parsePartSymbol(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{PartSymbol: v})
		case "instruments":
			v := parseInt(c.Text())
			m.Children = append(m.Children, AttributesChild{Instruments: &v})
		case "clef":
			v, err := parseClef(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Clef: v})
		case "staff-details":
			v, err := parseStaffDetails(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{StaffDetails: v})
		case "transpose":
			v, err := parseTranspose(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Transpose: v})
		case "directive":
			v, err := parseDirective(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{Directive: v})
		case "measure-style":
			v, err := parseMeasureStyle(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AttributesChild{MeasureStyle: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeAttributes(m *Attributes, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		case ch.Divisions != nil:
			el.CreateElement("divisions").SetText((*ch.Divisions).String())
		case ch.Key != nil:
			serializeKey(ch.Key, el, "key")
		case ch.Time != nil:
			serializeTime(ch.Time, el, "time")
		case ch.Staves != nil:
			el.CreateElement("staves").SetText(formatInt((*ch.Staves)))
		case ch.PartSymbol != nil:
			serializePartSymbol(ch.PartSymbol, el, "part-symbol")
		case ch.Instruments != nil:
			el.CreateElement("instruments").SetText(formatInt((*ch.Instruments)))
		case ch.Clef != nil:
			serializeClef(ch.Clef, el, "clef")
		case ch.StaffDetails != nil:
			serializeStaffDetails(ch.StaffDetails, el, "staff-details")
		case ch.Transpose != nil:
			serializeTranspose(ch.Transpose, el, "transpose")
		case ch.Directive != nil:
			serializeDirective(ch.Directive, el, "directive")
		case ch.MeasureStyle != nil:
			serializeMeasureStyle(ch.MeasureStyle, el, "measure-style")
		}
	}
}
