// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The grace type indicates the presence of a grace note. The slash attribute for a grace note is
// yes for slashed eighth notes. The other grace note attributes come from MuseData sound
// suggestions. The steal-time-previous attribute indicates the percentage of time to steal from the
// previous note for the grace note. The steal-time-following attribute indicates the percentage of
// time to steal from the following note for the grace note, as for appoggiaturas. The make-time
// attribute indicates to make time, not steal time; the units are in real-time divisions for the
// grace note.
type Grace struct {
	StealTimePrevious  *Percent   // attribute "steal-time-previous"
	StealTimeFollowing *Percent   // attribute "steal-time-following"
	MakeTime           *Divisions // attribute "make-time"
	Slash              *YesNo     // attribute "slash"
}

func parseGrace(el *etree.Element) (*Grace, error) {
	m := &Grace{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "steal-time-previous":
			v := ParsePercent(a.Value)
			m.StealTimePrevious = &v
		case "steal-time-following":
			v := ParsePercent(a.Value)
			m.StealTimeFollowing = &v
		case "make-time":
			v := ParseDivisions(a.Value)
			m.MakeTime = &v
		case "slash":
			v := ParseYesNo(a.Value)
			m.Slash = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
	}
	return m, nil
}

func serializeGrace(m *Grace, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.StealTimePrevious != nil {
		el.CreateAttr("steal-time-previous", (*m.StealTimePrevious).String())
	}
	if m.StealTimeFollowing != nil {
		el.CreateAttr("steal-time-following", (*m.StealTimeFollowing).String())
	}
	if m.MakeTime != nil {
		el.CreateAttr("make-time", (*m.MakeTime).String())
	}
	if m.Slash != nil {
		el.CreateAttr("slash", (*m.Slash).String())
	}
}
