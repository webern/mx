// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Page layout can be defined both in score-wide defaults and in the print element. Page margins are
// specified either for both even and odd pages, or via separate odd and even page number values.
// The type is not needed when used as part of a print element. If omitted when used in the defaults
// element, "both" is the default.
type PageLayout struct {
	Children []PageLayoutChild // child elements in document order
}

// PageLayoutChild is one child element of PageLayout: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PageLayoutChild struct {
	PageHeight  *Tenths
	PageWidth   *Tenths
	PageMargins *PageMargins
}

func parsePageLayout(el *etree.Element) (*PageLayout, error) {
	m := &PageLayout{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "page-height":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, PageLayoutChild{PageHeight: &v})
		case "page-width":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, PageLayoutChild{PageWidth: &v})
		case "page-margins":
			v, err := parsePageMargins(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PageLayoutChild{PageMargins: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePageLayout(m *PageLayout, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.PageHeight != nil:
			el.CreateElement("page-height").SetText((*ch.PageHeight).String())
		case ch.PageWidth != nil:
			el.CreateElement("page-width").SetText((*ch.PageWidth).String())
		case ch.PageMargins != nil:
			serializePageMargins(ch.PageMargins, el, "page-margins")
		}
	}
}
