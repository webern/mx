// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The staff-details element is used to indicate different types of staves. The optional number
// attribute specifies the staff number from top to bottom on the system, as with clef. The
// print-object attribute is used to indicate when a staff is not printed in a part, usually in
// large scores where empty parts are omitted. It is yes by default. If print-spacing is yes while
// print-object is no, the score is printed in cutaway format where vertical space is left for the
// empty part.
type StaffDetails struct {
	Number       *StaffNumber        // attribute "number"
	ShowFrets    *ShowFrets          // attribute "show-frets"
	PrintObject  *YesNo              // attribute "print-object"
	PrintSpacing *YesNo              // attribute "print-spacing"
	Children     []StaffDetailsChild // child elements in document order
}

// StaffDetailsChild is one child element of StaffDetails: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type StaffDetailsChild struct {
	StaffType   *StaffType
	StaffLines  *int
	StaffTuning *StaffTuning
	Capo        *int
	StaffSize   *NonNegativeDecimal
}

func parseStaffDetails(el *etree.Element) (*StaffDetails, error) {
	m := &StaffDetails{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "number":
			v := ParseStaffNumber(a.Value)
			m.Number = &v
		case "show-frets":
			v := ParseShowFrets(a.Value)
			m.ShowFrets = &v
		case "print-object":
			v := ParseYesNo(a.Value)
			m.PrintObject = &v
		case "print-spacing":
			v := ParseYesNo(a.Value)
			m.PrintSpacing = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "staff-type":
			v := ParseStaffType(c.Text())
			m.Children = append(m.Children, StaffDetailsChild{StaffType: &v})
		case "staff-lines":
			v := parseInt(c.Text())
			m.Children = append(m.Children, StaffDetailsChild{StaffLines: &v})
		case "staff-tuning":
			v, err := parseStaffTuning(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, StaffDetailsChild{StaffTuning: v})
		case "capo":
			v := parseInt(c.Text())
			m.Children = append(m.Children, StaffDetailsChild{Capo: &v})
		case "staff-size":
			v := ParseNonNegativeDecimal(c.Text())
			m.Children = append(m.Children, StaffDetailsChild{StaffSize: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeStaffDetails(m *StaffDetails, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Number != nil {
		el.CreateAttr("number", (*m.Number).String())
	}
	if m.ShowFrets != nil {
		el.CreateAttr("show-frets", (*m.ShowFrets).String())
	}
	if m.PrintObject != nil {
		el.CreateAttr("print-object", (*m.PrintObject).String())
	}
	if m.PrintSpacing != nil {
		el.CreateAttr("print-spacing", (*m.PrintSpacing).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.StaffType != nil:
			el.CreateElement("staff-type").SetText((*ch.StaffType).String())
		case ch.StaffLines != nil:
			el.CreateElement("staff-lines").SetText(formatInt((*ch.StaffLines)))
		case ch.StaffTuning != nil:
			serializeStaffTuning(ch.StaffTuning, el, "staff-tuning")
		case ch.Capo != nil:
			el.CreateElement("capo").SetText(formatInt((*ch.Capo)))
		case ch.StaffSize != nil:
			el.CreateElement("staff-size").SetText((*ch.StaffSize).String())
		}
	}
}
