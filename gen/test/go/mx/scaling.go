// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Margins, page sizes, and distances are all measured in tenths to keep MusicXML data in a
// consistent coordinate system as much as possible. The translation to absolute units is done with
// the scaling type, which specifies how many millimeters are equal to how many tenths. For a staff
// height of 7 mm, millimeters would be set to 7 while tenths is set to 40. The ability to set a
// formula rather than a single scaling factor helps avoid roundoff errors.
type Scaling struct {
	Children []ScalingChild // child elements in document order
}

// ScalingChild is one child element of Scaling: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type ScalingChild struct {
	Millimeters *Millimeters
	Tenths      *Tenths
}

func parseScaling(el *etree.Element) (*Scaling, error) {
	m := &Scaling{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "millimeters":
			v := ParseMillimeters(c.Text())
			m.Children = append(m.Children, ScalingChild{Millimeters: &v})
		case "tenths":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, ScalingChild{Tenths: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeScaling(m *Scaling, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Millimeters != nil:
			el.CreateElement("millimeters").SetText((*ch.Millimeters).String())
		case ch.Tenths != nil:
			el.CreateElement("tenths").SetText((*ch.Tenths).String())
		}
	}
}
