// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// An offset is represented in terms of divisions, and indicates where the direction will appear
// relative to the current musical location. This affects the visual appearance of the direction. If
// the sound attribute is "yes", then the offset affects playback too. If the sound attribute is
// "no", then any sound associated with the direction takes effect at the current location. The
// sound attribute is "no" by default for compatibility with earlier versions of the MusicXML
// format. If an element within a direction includes a default-x attribute, the offset value will be
// ignored when determining the appearance of that element.
type Offset struct {
	Sound *YesNo    // attribute "sound"
	Value Divisions // text content
}

func parseOffset(el *etree.Element) (*Offset, error) {
	m := &Offset{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "sound":
			v := ParseYesNo(a.Value)
			m.Sound = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	m.Value = ParseDivisions(el.Text())
	for _, c := range el.ChildElements() {
		return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
	}
	return m, nil
}

func serializeOffset(m *Offset, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	el.SetText(m.Value.String())
	if m.Sound != nil {
		el.CreateAttr("sound", (*m.Sound).String())
	}
}
