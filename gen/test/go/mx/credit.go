// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The credit type represents the appearance of the title, composer, arranger, lyricist, copyright,
// dedication, and other text, symbols, and graphics that commonly appear on the first page of a
// score. The credit-words, credit-symbol, and credit-image elements are similar to the words,
// symbol, and image elements for directions. However, since the credit is not part of a measure,
// the default-x and default-y attributes adjust the origin relative to the bottom left-hand corner
// of the page. The enclosure for credit-words and credit-symbol is none by default. By default, a
// series of credit-words and credit-symbol elements within a single credit element follow one
// another in sequence visually. Non-positional formatting attributes are carried over from the
// previous element by default. The page attribute for the credit element specifies the page number
// where the credit should appear. This is an integer value that starts with 1 for the first page.
// Its value is 1 by default. Since credits occur before the music, these page numbers do not refer
// to the page numbering specified by the print element's page-number attribute. The credit-type
// element indicates the purpose behind a credit. Multiple types of data may be combined in a single
// credit, so multiple elements may be used. Standard values include page number, title, subtitle,
// composer, arranger, lyricist, and rights.
type Credit struct {
	Page     *int          // attribute "page"
	ID       *string       // attribute "id"
	Children []CreditChild // child elements in document order
}

// CreditChild is one child element of Credit: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type CreditChild struct {
	CreditType   *string
	Link         *Link
	Bookmark     *Bookmark
	CreditImage  *Image
	CreditWords  *FormattedTextID
	CreditSymbol *FormattedSymbolID
}

func parseCredit(el *etree.Element) (*Credit, error) {
	m := &Credit{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "page":
			v := parseInt(a.Value)
			m.Page = &v
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "credit-type":
			v := c.Text()
			m.Children = append(m.Children, CreditChild{CreditType: &v})
		case "link":
			v, err := parseLink(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, CreditChild{Link: v})
		case "bookmark":
			v, err := parseBookmark(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, CreditChild{Bookmark: v})
		case "credit-image":
			v, err := parseImage(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, CreditChild{CreditImage: v})
		case "credit-words":
			v, err := parseFormattedTextID(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, CreditChild{CreditWords: v})
		case "credit-symbol":
			v, err := parseFormattedSymbolID(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, CreditChild{CreditSymbol: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeCredit(m *Credit, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Page != nil {
		el.CreateAttr("page", formatInt((*m.Page)))
	}
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.CreditType != nil:
			el.CreateElement("credit-type").SetText((*ch.CreditType))
		case ch.Link != nil:
			serializeLink(ch.Link, el, "link")
		case ch.Bookmark != nil:
			serializeBookmark(ch.Bookmark, el, "bookmark")
		case ch.CreditImage != nil:
			serializeImage(ch.CreditImage, el, "credit-image")
		case ch.CreditWords != nil:
			serializeFormattedTextID(ch.CreditWords, el, "credit-words")
		case ch.CreditSymbol != nil:
			serializeFormattedSymbolID(ch.CreditSymbol, el, "credit-symbol")
		}
	}
}
