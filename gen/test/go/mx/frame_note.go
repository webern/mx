// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The frame-note type represents each note included in the frame. An open string will have a fret
// value of 0, while a muted string will not be associated with a frame-note element.
type FrameNote struct {
	Children []FrameNoteChild // child elements in document order
}

// FrameNoteChild is one child element of FrameNote: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type FrameNoteChild struct {
	String    *String
	Fret      *Fret
	Fingering *Fingering
	Barre     *Barre
}

func parseFrameNote(el *etree.Element) (*FrameNote, error) {
	m := &FrameNote{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "string":
			v, err := parseString(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FrameNoteChild{String: v})
		case "fret":
			v, err := parseFret(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FrameNoteChild{Fret: v})
		case "fingering":
			v, err := parseFingering(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FrameNoteChild{Fingering: v})
		case "barre":
			v, err := parseBarre(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FrameNoteChild{Barre: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeFrameNote(m *FrameNote, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.String != nil:
			serializeString(ch.String, el, "string")
		case ch.Fret != nil:
			serializeFret(ch.Fret, el, "fret")
		case ch.Fingering != nil:
			serializeFingering(ch.Fingering, el, "fingering")
		case ch.Barre != nil:
			serializeBarre(ch.Barre, el, "barre")
		}
	}
}
