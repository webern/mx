// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The rest element indicates notated rests or silences. Rest elements are usually empty, but
// placement on the staff can be specified using display-step and display-octave elements. If the
// measure attribute is set to yes, this indicates this is a complete measure rest.
type Rest struct {
	Measure  *YesNo      // attribute "measure"
	Children []RestChild // child elements in document order
}

// RestChild is one child element of Rest: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type RestChild struct {
	DisplayStep   *Step
	DisplayOctave *Octave
}

func parseRest(el *etree.Element) (*Rest, error) {
	m := &Rest{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "measure":
			v := ParseYesNo(a.Value)
			m.Measure = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "display-step":
			v := ParseStep(c.Text())
			m.Children = append(m.Children, RestChild{DisplayStep: &v})
		case "display-octave":
			v := ParseOctave(c.Text())
			m.Children = append(m.Children, RestChild{DisplayOctave: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeRest(m *Rest, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Measure != nil {
		el.CreateAttr("measure", (*m.Measure).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.DisplayStep != nil:
			el.CreateElement("display-step").SetText((*ch.DisplayStep).String())
		case ch.DisplayOctave != nil:
			el.CreateElement("display-octave").SetText((*ch.DisplayOctave).String())
		}
	}
}
