// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Ornaments can be any of several types, followed optionally by accidentals. The accidental-mark
// element's content is represented the same as an accidental element, but with a different name to
// reflect the different musical meaning.
type Ornaments struct {
	ID       *string          // attribute "id"
	Children []OrnamentsChild // child elements in document order
}

// OrnamentsChild is one child element of Ornaments: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type OrnamentsChild struct {
	TrillMark            *EmptyTrillSound
	Turn                 *HorizontalTurn
	DelayedTurn          *HorizontalTurn
	InvertedTurn         *HorizontalTurn
	DelayedInvertedTurn  *HorizontalTurn
	VerticalTurn         *EmptyTrillSound
	InvertedVerticalTurn *EmptyTrillSound
	Shake                *EmptyTrillSound
	WavyLine             *WavyLine
	Mordent              *Mordent
	InvertedMordent      *Mordent
	Schleifer            *EmptyPlacement
	Tremolo              *Tremolo
	Haydn                *EmptyTrillSound
	OtherOrnament        *OtherPlacementText
	AccidentalMark       *AccidentalMark
}

func parseOrnaments(el *etree.Element) (*Ornaments, error) {
	m := &Ornaments{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "trill-mark":
			v, err := parseEmptyTrillSound(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{TrillMark: v})
		case "turn":
			v, err := parseHorizontalTurn(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{Turn: v})
		case "delayed-turn":
			v, err := parseHorizontalTurn(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{DelayedTurn: v})
		case "inverted-turn":
			v, err := parseHorizontalTurn(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{InvertedTurn: v})
		case "delayed-inverted-turn":
			v, err := parseHorizontalTurn(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{DelayedInvertedTurn: v})
		case "vertical-turn":
			v, err := parseEmptyTrillSound(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{VerticalTurn: v})
		case "inverted-vertical-turn":
			v, err := parseEmptyTrillSound(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{InvertedVerticalTurn: v})
		case "shake":
			v, err := parseEmptyTrillSound(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{Shake: v})
		case "wavy-line":
			v, err := parseWavyLine(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{WavyLine: v})
		case "mordent":
			v, err := parseMordent(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{Mordent: v})
		case "inverted-mordent":
			v, err := parseMordent(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{InvertedMordent: v})
		case "schleifer":
			v, err := parseEmptyPlacement(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{Schleifer: v})
		case "tremolo":
			v, err := parseTremolo(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{Tremolo: v})
		case "haydn":
			v, err := parseEmptyTrillSound(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{Haydn: v})
		case "other-ornament":
			v, err := parseOtherPlacementText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{OtherOrnament: v})
		case "accidental-mark":
			v, err := parseAccidentalMark(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, OrnamentsChild{AccidentalMark: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeOrnaments(m *Ornaments, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.TrillMark != nil:
			serializeEmptyTrillSound(ch.TrillMark, el, "trill-mark")
		case ch.Turn != nil:
			serializeHorizontalTurn(ch.Turn, el, "turn")
		case ch.DelayedTurn != nil:
			serializeHorizontalTurn(ch.DelayedTurn, el, "delayed-turn")
		case ch.InvertedTurn != nil:
			serializeHorizontalTurn(ch.InvertedTurn, el, "inverted-turn")
		case ch.DelayedInvertedTurn != nil:
			serializeHorizontalTurn(ch.DelayedInvertedTurn, el, "delayed-inverted-turn")
		case ch.VerticalTurn != nil:
			serializeEmptyTrillSound(ch.VerticalTurn, el, "vertical-turn")
		case ch.InvertedVerticalTurn != nil:
			serializeEmptyTrillSound(ch.InvertedVerticalTurn, el, "inverted-vertical-turn")
		case ch.Shake != nil:
			serializeEmptyTrillSound(ch.Shake, el, "shake")
		case ch.WavyLine != nil:
			serializeWavyLine(ch.WavyLine, el, "wavy-line")
		case ch.Mordent != nil:
			serializeMordent(ch.Mordent, el, "mordent")
		case ch.InvertedMordent != nil:
			serializeMordent(ch.InvertedMordent, el, "inverted-mordent")
		case ch.Schleifer != nil:
			serializeEmptyPlacement(ch.Schleifer, el, "schleifer")
		case ch.Tremolo != nil:
			serializeTremolo(ch.Tremolo, el, "tremolo")
		case ch.Haydn != nil:
			serializeEmptyTrillSound(ch.Haydn, el, "haydn")
		case ch.OtherOrnament != nil:
			serializeOtherPlacementText(ch.OtherOrnament, el, "other-ornament")
		case ch.AccidentalMark != nil:
			serializeAccidentalMark(ch.AccidentalMark, el, "accidental-mark")
		}
	}
}
