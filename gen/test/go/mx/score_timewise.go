// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

type ScoreTimewise struct {
	Version  *string              // attribute "version"
	Children []ScoreTimewiseChild // child elements in document order
}

// ScoreTimewiseChild is one child element of ScoreTimewise: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type ScoreTimewiseChild struct {
	Work           *Work
	MovementNumber *string
	MovementTitle  *string
	Identification *Identification
	Defaults       *Defaults
	Credit         *Credit
	PartList       *PartList
	Measure        *TimewiseMeasure
}

func parseScoreTimewise(el *etree.Element) (*ScoreTimewise, error) {
	m := &ScoreTimewise{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "version":
			v := a.Value
			m.Version = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "work":
			v, err := parseWork(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreTimewiseChild{Work: v})
		case "movement-number":
			v := c.Text()
			m.Children = append(m.Children, ScoreTimewiseChild{MovementNumber: &v})
		case "movement-title":
			v := c.Text()
			m.Children = append(m.Children, ScoreTimewiseChild{MovementTitle: &v})
		case "identification":
			v, err := parseIdentification(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreTimewiseChild{Identification: v})
		case "defaults":
			v, err := parseDefaults(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreTimewiseChild{Defaults: v})
		case "credit":
			v, err := parseCredit(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreTimewiseChild{Credit: v})
		case "part-list":
			v, err := parsePartList(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreTimewiseChild{PartList: v})
		case "measure":
			v, err := parseTimewiseMeasure(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, ScoreTimewiseChild{Measure: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeScoreTimewise(m *ScoreTimewise, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Version != nil {
		el.CreateAttr("version", (*m.Version))
	}
	for _, ch := range m.Children {
		switch {
		case ch.Work != nil:
			serializeWork(ch.Work, el, "work")
		case ch.MovementNumber != nil:
			el.CreateElement("movement-number").SetText((*ch.MovementNumber))
		case ch.MovementTitle != nil:
			el.CreateElement("movement-title").SetText((*ch.MovementTitle))
		case ch.Identification != nil:
			serializeIdentification(ch.Identification, el, "identification")
		case ch.Defaults != nil:
			serializeDefaults(ch.Defaults, el, "defaults")
		case ch.Credit != nil:
			serializeCredit(ch.Credit, el, "credit")
		case ch.PartList != nil:
			serializePartList(ch.PartList, el, "part-list")
		case ch.Measure != nil:
			serializeTimewiseMeasure(ch.Measure, el, "measure")
		}
	}
}
