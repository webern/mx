// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Page margins are specified either for both even and odd pages, or via separate odd and even page
// number values. The type attribute is not needed when used as part of a print element. If omitted
// when the page-margins type is used in the defaults element, "both" is the default value.
type PageMargins struct {
	Type     *MarginType        // attribute "type"
	Children []PageMarginsChild // child elements in document order
}

// PageMarginsChild is one child element of PageMargins: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PageMarginsChild struct {
	LeftMargin   *Tenths
	RightMargin  *Tenths
	TopMargin    *Tenths
	BottomMargin *Tenths
}

func parsePageMargins(el *etree.Element) (*PageMargins, error) {
	m := &PageMargins{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "type":
			v := ParseMarginType(a.Value)
			m.Type = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "left-margin":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, PageMarginsChild{LeftMargin: &v})
		case "right-margin":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, PageMarginsChild{RightMargin: &v})
		case "top-margin":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, PageMarginsChild{TopMargin: &v})
		case "bottom-margin":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, PageMarginsChild{BottomMargin: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePageMargins(m *PageMargins, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Type != nil {
		el.CreateAttr("type", (*m.Type).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.LeftMargin != nil:
			el.CreateElement("left-margin").SetText((*ch.LeftMargin).String())
		case ch.RightMargin != nil:
			el.CreateElement("right-margin").SetText((*ch.RightMargin).String())
		case ch.TopMargin != nil:
			el.CreateElement("top-margin").SetText((*ch.TopMargin).String())
		case ch.BottomMargin != nil:
			el.CreateElement("bottom-margin").SetText((*ch.BottomMargin).String())
		}
	}
}
