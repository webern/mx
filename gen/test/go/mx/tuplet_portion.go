// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The tuplet-portion type provides optional full control over tuplet specifications. It allows the
// number and note type (including dots) to be set for the actual and normal portions of a single
// tuplet. If any of these elements are absent, their values are based on the time-modification
// element.
type TupletPortion struct {
	Children []TupletPortionChild // child elements in document order
}

// TupletPortionChild is one child element of TupletPortion: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type TupletPortionChild struct {
	TupletNumber *TupletNumber
	TupletType   *TupletType
	TupletDot    *TupletDot
}

func parseTupletPortion(el *etree.Element) (*TupletPortion, error) {
	m := &TupletPortion{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "tuplet-number":
			v, err := parseTupletNumber(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, TupletPortionChild{TupletNumber: v})
		case "tuplet-type":
			v, err := parseTupletType(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, TupletPortionChild{TupletType: v})
		case "tuplet-dot":
			v, err := parseTupletDot(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, TupletPortionChild{TupletDot: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeTupletPortion(m *TupletPortion, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.TupletNumber != nil:
			serializeTupletNumber(ch.TupletNumber, el, "tuplet-number")
		case ch.TupletType != nil:
			serializeTupletType(ch.TupletType, el, "tuplet-type")
		case ch.TupletDot != nil:
			serializeTupletDot(ch.TupletDot, el, "tuplet-dot")
		}
	}
}
