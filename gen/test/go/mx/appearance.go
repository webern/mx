// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The appearance type controls general graphical settings for the music's final form appearance on
// a printed page of display. This includes support for line widths, definitions for note sizes, and
// standard distances between notation elements, plus an extension element for other aspects of
// appearance.
type Appearance struct {
	Children []AppearanceChild // child elements in document order
}

// AppearanceChild is one child element of Appearance: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type AppearanceChild struct {
	LineWidth       *LineWidth
	NoteSize        *NoteSize
	Distance        *Distance
	Glyph           *Glyph
	OtherAppearance *OtherAppearance
}

func parseAppearance(el *etree.Element) (*Appearance, error) {
	m := &Appearance{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "line-width":
			v, err := parseLineWidth(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AppearanceChild{LineWidth: v})
		case "note-size":
			v, err := parseNoteSize(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AppearanceChild{NoteSize: v})
		case "distance":
			v, err := parseDistance(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AppearanceChild{Distance: v})
		case "glyph":
			v, err := parseGlyph(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AppearanceChild{Glyph: v})
		case "other-appearance":
			v, err := parseOtherAppearance(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, AppearanceChild{OtherAppearance: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeAppearance(m *Appearance, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.LineWidth != nil:
			serializeLineWidth(ch.LineWidth, el, "line-width")
		case ch.NoteSize != nil:
			serializeNoteSize(ch.NoteSize, el, "note-size")
		case ch.Distance != nil:
			serializeDistance(ch.Distance, el, "distance")
		case ch.Glyph != nil:
			serializeGlyph(ch.Glyph, el, "glyph")
		case ch.OtherAppearance != nil:
			serializeOtherAppearance(ch.OtherAppearance, el, "other-appearance")
		}
	}
}
