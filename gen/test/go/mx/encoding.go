// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The encoding element contains information about who did the digital encoding, when, with what
// software, and in what aspects. Standard type values for the encoder element are music, words, and
// arrangement, but other types may be used. The type attribute is only needed when there are
// multiple encoder elements.
type Encoding struct {
	Children []EncodingChild // child elements in document order
}

// EncodingChild is one child element of Encoding: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type EncodingChild struct {
	EncodingDate        *YyyyMmDd
	Encoder             *TypedText
	Software            *string
	EncodingDescription *string
	Supports            *Supports
}

func parseEncoding(el *etree.Element) (*Encoding, error) {
	m := &Encoding{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "encoding-date":
			v := ParseYyyyMmDd(c.Text())
			m.Children = append(m.Children, EncodingChild{EncodingDate: &v})
		case "encoder":
			v, err := parseTypedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, EncodingChild{Encoder: v})
		case "software":
			v := c.Text()
			m.Children = append(m.Children, EncodingChild{Software: &v})
		case "encoding-description":
			v := c.Text()
			m.Children = append(m.Children, EncodingChild{EncodingDescription: &v})
		case "supports":
			v, err := parseSupports(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, EncodingChild{Supports: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeEncoding(m *Encoding, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.EncodingDate != nil:
			el.CreateElement("encoding-date").SetText((*ch.EncodingDate).String())
		case ch.Encoder != nil:
			serializeTypedText(ch.Encoder, el, "encoder")
		case ch.Software != nil:
			el.CreateElement("software").SetText((*ch.Software))
		case ch.EncodingDescription != nil:
			el.CreateElement("encoding-description").SetText((*ch.EncodingDescription))
		case ch.Supports != nil:
			serializeSupports(ch.Supports, el, "supports")
		}
	}
}
