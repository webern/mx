// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The beat-unit-tied type indicates a beat-unit within a metronome mark that is tied to the
// preceding beat-unit. This allows or two or more tied notes to be associated with a per-minute
// value in a metronome mark, whereas the metronome-tied element is restricted to metric
// relationship marks.
type BeatUnitTied struct {
	Children []BeatUnitTiedChild // child elements in document order
}

// BeatUnitTiedChild is one child element of BeatUnitTied: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type BeatUnitTiedChild struct {
	BeatUnit    *NoteTypeValue
	BeatUnitDot *Empty
}

func parseBeatUnitTied(el *etree.Element) (*BeatUnitTied, error) {
	m := &BeatUnitTied{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "beat-unit":
			v := ParseNoteTypeValue(c.Text())
			m.Children = append(m.Children, BeatUnitTiedChild{BeatUnit: &v})
		case "beat-unit-dot":
			v, err := parseEmpty(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, BeatUnitTiedChild{BeatUnitDot: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeBeatUnitTied(m *BeatUnitTied, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.BeatUnit != nil:
			el.CreateElement("beat-unit").SetText((*ch.BeatUnit).String())
		case ch.BeatUnitDot != nil:
			serializeEmpty(ch.BeatUnitDot, el, "beat-unit-dot")
		}
	}
}
