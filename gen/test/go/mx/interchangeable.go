// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The interchangeable type is used to represent the second in a pair of interchangeable dual time
// signatures, such as the 6/8 in 3/4 (6/8). A separate symbol attribute value is available compared
// to the time element's symbol attribute, which applies to the first of the dual time signatures.
type Interchangeable struct {
	Symbol    *TimeSymbol            // attribute "symbol"
	Separator *TimeSeparator         // attribute "separator"
	Children  []InterchangeableChild // child elements in document order
}

// InterchangeableChild is one child element of Interchangeable: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type InterchangeableChild struct {
	TimeRelation *TimeRelation
	Beats        *string
	BeatType     *string
}

func parseInterchangeable(el *etree.Element) (*Interchangeable, error) {
	m := &Interchangeable{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "symbol":
			v := ParseTimeSymbol(a.Value)
			m.Symbol = &v
		case "separator":
			v := ParseTimeSeparator(a.Value)
			m.Separator = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "time-relation":
			v := ParseTimeRelation(c.Text())
			m.Children = append(m.Children, InterchangeableChild{TimeRelation: &v})
		case "beats":
			v := c.Text()
			m.Children = append(m.Children, InterchangeableChild{Beats: &v})
		case "beat-type":
			v := c.Text()
			m.Children = append(m.Children, InterchangeableChild{BeatType: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeInterchangeable(m *Interchangeable, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Symbol != nil {
		el.CreateAttr("symbol", (*m.Symbol).String())
	}
	if m.Separator != nil {
		el.CreateAttr("separator", (*m.Separator).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.TimeRelation != nil:
			el.CreateElement("time-relation").SetText((*ch.TimeRelation).String())
		case ch.Beats != nil:
			el.CreateElement("beats").SetText((*ch.Beats))
		case ch.BeatType != nil:
			el.CreateElement("beat-type").SetText((*ch.BeatType))
		}
	}
}
