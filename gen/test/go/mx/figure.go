// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The figure type represents a single figure within a figured-bass element.
type Figure struct {
	Children []FigureChild // child elements in document order
}

// FigureChild is one child element of Figure: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type FigureChild struct {
	Prefix       *StyleText
	FigureNumber *StyleText
	Suffix       *StyleText
	Extend       *Extend
	Footnote     *FormattedText
	Level        *Level
}

func parseFigure(el *etree.Element) (*Figure, error) {
	m := &Figure{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "prefix":
			v, err := parseStyleText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FigureChild{Prefix: v})
		case "figure-number":
			v, err := parseStyleText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FigureChild{FigureNumber: v})
		case "suffix":
			v, err := parseStyleText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FigureChild{Suffix: v})
		case "extend":
			v, err := parseExtend(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FigureChild{Extend: v})
		case "footnote":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FigureChild{Footnote: v})
		case "level":
			v, err := parseLevel(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, FigureChild{Level: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeFigure(m *Figure, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.Prefix != nil:
			serializeStyleText(ch.Prefix, el, "prefix")
		case ch.FigureNumber != nil:
			serializeStyleText(ch.FigureNumber, el, "figure-number")
		case ch.Suffix != nil:
			serializeStyleText(ch.Suffix, el, "suffix")
		case ch.Extend != nil:
			serializeExtend(ch.Extend, el, "extend")
		case ch.Footnote != nil:
			serializeFormattedText(ch.Footnote, el, "footnote")
		case ch.Level != nil:
			serializeLevel(ch.Level, el, "level")
		}
	}
}
