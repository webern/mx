// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The name-display type is used for exact formatting of multi-font text in part and group names to
// the left of the system. The print-object attribute can be used to determine what, if anything, is
// printed at the start of each system. Enclosure for the display-text element is none by default.
// Language for the display-text element is Italian ("it") by default.
type NameDisplay struct {
	PrintObject *YesNo             // attribute "print-object"
	Children    []NameDisplayChild // child elements in document order
}

// NameDisplayChild is one child element of NameDisplay: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type NameDisplayChild struct {
	DisplayText    *FormattedText
	AccidentalText *AccidentalText
}

func parseNameDisplay(el *etree.Element) (*NameDisplay, error) {
	m := &NameDisplay{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "print-object":
			v := ParseYesNo(a.Value)
			m.PrintObject = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "display-text":
			v, err := parseFormattedText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, NameDisplayChild{DisplayText: v})
		case "accidental-text":
			v, err := parseAccidentalText(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, NameDisplayChild{AccidentalText: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeNameDisplay(m *NameDisplay, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.PrintObject != nil {
		el.CreateAttr("print-object", (*m.PrintObject).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.DisplayText != nil:
			serializeFormattedText(ch.DisplayText, el, "display-text")
		case ch.AccidentalText != nil:
			serializeAccidentalText(ch.AccidentalText, el, "accidental-text")
		}
	}
}
