// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// A measure-style indicates a special way to print partial to multiple measures within a part. This
// includes multiple rests over several measures, repeats of beats, single, or multiple measures,
// and use of slash notation. The multiple-rest and measure-repeat symbols indicate the number of
// measures covered in the element content. The beat-repeat and slash elements can cover partial
// measures. All but the multiple-rest element use a type attribute to indicate starting and
// stopping the use of the style. The optional number attribute specifies the staff number from top
// to bottom on the system, as with clef.
type MeasureStyle struct {
	Number     *StaffNumber        // attribute "number"
	FontFamily *CommaSeparatedText // attribute "font-family"
	FontStyle  *FontStyle          // attribute "font-style"
	FontSize   *FontSize           // attribute "font-size"
	FontWeight *FontWeight         // attribute "font-weight"
	Color      *Color              // attribute "color"
	ID         *string             // attribute "id"
	Children   []MeasureStyleChild // child elements in document order
}

// MeasureStyleChild is one child element of MeasureStyle: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type MeasureStyleChild struct {
	MultipleRest  *MultipleRest
	MeasureRepeat *MeasureRepeat
	BeatRepeat    *BeatRepeat
	Slash         *Slash
}

func parseMeasureStyle(el *etree.Element) (*MeasureStyle, error) {
	m := &MeasureStyle{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "number":
			v := ParseStaffNumber(a.Value)
			m.Number = &v
		case "font-family":
			v := ParseCommaSeparatedText(a.Value)
			m.FontFamily = &v
		case "font-style":
			v := ParseFontStyle(a.Value)
			m.FontStyle = &v
		case "font-size":
			v := ParseFontSize(a.Value)
			m.FontSize = &v
		case "font-weight":
			v := ParseFontWeight(a.Value)
			m.FontWeight = &v
		case "color":
			v := ParseColor(a.Value)
			m.Color = &v
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "multiple-rest":
			v, err := parseMultipleRest(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, MeasureStyleChild{MultipleRest: v})
		case "measure-repeat":
			v, err := parseMeasureRepeat(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, MeasureStyleChild{MeasureRepeat: v})
		case "beat-repeat":
			v, err := parseBeatRepeat(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, MeasureStyleChild{BeatRepeat: v})
		case "slash":
			v, err := parseSlash(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, MeasureStyleChild{Slash: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeMeasureStyle(m *MeasureStyle, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Number != nil {
		el.CreateAttr("number", (*m.Number).String())
	}
	if m.FontFamily != nil {
		el.CreateAttr("font-family", (*m.FontFamily).String())
	}
	if m.FontStyle != nil {
		el.CreateAttr("font-style", (*m.FontStyle).String())
	}
	if m.FontSize != nil {
		el.CreateAttr("font-size", (*m.FontSize).String())
	}
	if m.FontWeight != nil {
		el.CreateAttr("font-weight", (*m.FontWeight).String())
	}
	if m.Color != nil {
		el.CreateAttr("color", (*m.Color).String())
	}
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.MultipleRest != nil:
			serializeMultipleRest(ch.MultipleRest, el, "multiple-rest")
		case ch.MeasureRepeat != nil:
			serializeMeasureRepeat(ch.MeasureRepeat, el, "measure-repeat")
		case ch.BeatRepeat != nil:
			serializeBeatRepeat(ch.BeatRepeat, el, "beat-repeat")
		case ch.Slash != nil:
			serializeSlash(ch.Slash, el, "slash")
		}
	}
}
