// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// The print type contains general printing parameters, including the layout elements defined in the
// layout.mod file. The part-name-display and part-abbreviation-display elements used in the
// score.mod file may also be used here to change how a part name or abbreviation is displayed over
// the course of a piece. They take effect when the current measure or a succeeding measure starts a
// new system. Layout elements in a print statement only apply to the current page, system, staff,
// or measure. Music that follows continues to take the default values from the layout included in
// the defaults element.
type Print struct {
	StaffSpacing *Tenths      // attribute "staff-spacing"
	NewSystem    *YesNo       // attribute "new-system"
	NewPage      *YesNo       // attribute "new-page"
	BlankPage    *int         // attribute "blank-page"
	PageNumber   *string      // attribute "page-number"
	ID           *string      // attribute "id"
	Children     []PrintChild // child elements in document order
}

// PrintChild is one child element of Print: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type PrintChild struct {
	PageLayout              *PageLayout
	SystemLayout            *SystemLayout
	StaffLayout             *StaffLayout
	MeasureLayout           *MeasureLayout
	MeasureNumbering        *MeasureNumbering
	PartNameDisplay         *NameDisplay
	PartAbbreviationDisplay *NameDisplay
}

func parsePrint(el *etree.Element) (*Print, error) {
	m := &Print{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "staff-spacing":
			v := ParseTenths(a.Value)
			m.StaffSpacing = &v
		case "new-system":
			v := ParseYesNo(a.Value)
			m.NewSystem = &v
		case "new-page":
			v := ParseYesNo(a.Value)
			m.NewPage = &v
		case "blank-page":
			v := parseInt(a.Value)
			m.BlankPage = &v
		case "page-number":
			v := a.Value
			m.PageNumber = &v
		case "id":
			v := a.Value
			m.ID = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "page-layout":
			v, err := parsePageLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{PageLayout: v})
		case "system-layout":
			v, err := parseSystemLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{SystemLayout: v})
		case "staff-layout":
			v, err := parseStaffLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{StaffLayout: v})
		case "measure-layout":
			v, err := parseMeasureLayout(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{MeasureLayout: v})
		case "measure-numbering":
			v, err := parseMeasureNumbering(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{MeasureNumbering: v})
		case "part-name-display":
			v, err := parseNameDisplay(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{PartNameDisplay: v})
		case "part-abbreviation-display":
			v, err := parseNameDisplay(c)
			if err != nil {
				return nil, err
			}
			m.Children = append(m.Children, PrintChild{PartAbbreviationDisplay: v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializePrint(m *Print, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.StaffSpacing != nil {
		el.CreateAttr("staff-spacing", (*m.StaffSpacing).String())
	}
	if m.NewSystem != nil {
		el.CreateAttr("new-system", (*m.NewSystem).String())
	}
	if m.NewPage != nil {
		el.CreateAttr("new-page", (*m.NewPage).String())
	}
	if m.BlankPage != nil {
		el.CreateAttr("blank-page", formatInt((*m.BlankPage)))
	}
	if m.PageNumber != nil {
		el.CreateAttr("page-number", (*m.PageNumber))
	}
	if m.ID != nil {
		el.CreateAttr("id", (*m.ID))
	}
	for _, ch := range m.Children {
		switch {
		case ch.PageLayout != nil:
			serializePageLayout(ch.PageLayout, el, "page-layout")
		case ch.SystemLayout != nil:
			serializeSystemLayout(ch.SystemLayout, el, "system-layout")
		case ch.StaffLayout != nil:
			serializeStaffLayout(ch.StaffLayout, el, "staff-layout")
		case ch.MeasureLayout != nil:
			serializeMeasureLayout(ch.MeasureLayout, el, "measure-layout")
		case ch.MeasureNumbering != nil:
			serializeMeasureNumbering(ch.MeasureNumbering, el, "measure-numbering")
		case ch.PartNameDisplay != nil:
			serializeNameDisplay(ch.PartNameDisplay, el, "part-name-display")
		case ch.PartAbbreviationDisplay != nil:
			serializeNameDisplay(ch.PartAbbreviationDisplay, el, "part-abbreviation-display")
		}
	}
}
