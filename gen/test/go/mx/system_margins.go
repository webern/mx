// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// System margins are relative to the page margins. Positive values indent and negative values
// reduce the margin size.
type SystemMargins struct {
	Children []SystemMarginsChild // child elements in document order
}

// SystemMarginsChild is one child element of SystemMargins: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type SystemMarginsChild struct {
	LeftMargin  *Tenths
	RightMargin *Tenths
}

func parseSystemMargins(el *etree.Element) (*SystemMargins, error) {
	m := &SystemMargins{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "left-margin":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, SystemMarginsChild{LeftMargin: &v})
		case "right-margin":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, SystemMarginsChild{RightMargin: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeSystemMargins(m *SystemMargins, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	for _, ch := range m.Children {
		switch {
		case ch.LeftMargin != nil:
			el.CreateElement("left-margin").SetText((*ch.LeftMargin).String())
		case ch.RightMargin != nil:
			el.CreateElement("right-margin").SetText((*ch.RightMargin).String())
		}
	}
}
