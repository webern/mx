// Code generated by gen from musicxml-3.1-8bbe8e5. DO NOT EDIT.

package mx

import (
	"fmt"
	"github.com/beevik/etree"
)

// Staff layout includes the vertical distance from the bottom line of the previous staff in this
// system to the top line of the staff specified by the number attribute. The optional number
// attribute refers to staff numbers within the part, from top to bottom on the system. A value of 1
// is assumed if not present. When used in the defaults element, the values apply to all parts. This
// value is ignored for the first staff in a system.
type StaffLayout struct {
	Number   *StaffNumber       // attribute "number"
	Children []StaffLayoutChild // child elements in document order
}

// StaffLayoutChild is one child element of StaffLayout: exactly one field
// is non-nil, and that pointer says which element this is. (No kind
// discriminator: schema element names like harmony's <kind> would
// collide with a synthetic field.) Constructing a child with zero or
// multiple fields set is undefined: serialization writes the first
// non-nil field in schema order and nothing when all are nil.
type StaffLayoutChild struct {
	StaffDistance *Tenths
}

func parseStaffLayout(el *etree.Element) (*StaffLayout, error) {
	m := &StaffLayout{}
	for _, a := range el.Attr {
		if a.Space == "xmlns" || (a.Space == "" && a.Key == "xmlns") {
			continue
		}
		switch a.FullKey() {
		case "number":
			v := ParseStaffNumber(a.Value)
			m.Number = &v
		default:
			return nil, fmt.Errorf("unknown attribute %q on <%s>", a.FullKey(), el.Tag)
		}
	}
	for _, c := range el.ChildElements() {
		switch c.Tag {
		case "staff-distance":
			v := ParseTenths(c.Text())
			m.Children = append(m.Children, StaffLayoutChild{StaffDistance: &v})
		default:
			return nil, fmt.Errorf("unknown element <%s> in <%s>", c.Tag, el.Tag)
		}
	}
	return m, nil
}

func serializeStaffLayout(m *StaffLayout, parent *etree.Element, tag string) {
	el := parent.CreateElement(tag)
	if m.Number != nil {
		el.CreateAttr("number", (*m.Number).String())
	}
	for _, ch := range m.Children {
		switch {
		case ch.StaffDistance != nil:
			el.CreateElement("staff-distance").SetText((*ch.StaffDistance).String())
		}
	}
}
