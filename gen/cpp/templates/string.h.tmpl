// {{generated_banner}}

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace {{vars.namespace}}
{

{{#doc_lines}}
/// {{value}}
{{/doc_lines}}
/// Open vocabulary: a distinct type for schema fidelity and overload
/// identity{{#min_length}}, with the schema's minimum length repaired on
/// construction{{/min_length}}.
class {{ident}} final
{
public:
    {{ident}}(){{^min_length}} = default{{/min_length}};

    explicit {{ident}}(std::string value);

    const std::string &value() const noexcept
    {
        return m_value;
    }

    void setValue(std::string value);

    std::string toString() const
    {
        return m_value;
    }

    static bool tryParse(std::string_view text, {{ident}} &out);

    static {{ident}} parse(std::string_view text);

    /// Says whether the text had to be repaired.
    static {{ident}} parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const {{ident}} &other) const noexcept = default;

private:
    std::string m_value;
};

} // namespace {{vars.namespace}}
