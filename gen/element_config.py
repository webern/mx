#!/usr/bin/env python3
"""Element dispatch configuration for the code generator.

Controls which elements are generated by which path (skip, bespoke family,
tree-based, choice, simple-value, standard), plus class-name and value-type
overrides. This is the file to edit when adding new element-specific behavior
or when migrating elements between generation strategies.
"""
from parse import pascal

OVERWRITE_FILE_STEMS = {
    "Direction", "DirectionType", "DirectionAttributes",
}

ELEMENT_CLASS_NAME_OVERRIDE = {
    "attributes": "Properties",
}

ELEMENT_VALUE_TYPE_OVERRIDE = {
    "instrument-sound": {
        "cpp_type": "PlaybackSoundType",
        "header": "mx/core/PlaybackSoundType.h",
        "default": "PlaybackSoundType{}",
    },
}


def element_class_name(elem_name: str) -> str:
    """Return the C++ class name for an element, consulting overrides first."""
    return ELEMENT_CLASS_NAME_OVERRIDE.get(elem_name, pascal(elem_name))


SKIP_ELEMENTS = set()

BESPOKE_FAMILY_OWNED = {
    "part",
    "measure",
}

TREE_ELEMENTS = {
    "bend",
    "group-abbreviation-display",
    "group-name-display",
    "harmonic",
    "key",
    "metronome",
    "notations",
    "notehead-text",
    "ornaments",
    "part-abbreviation-display",
    "part-name-display",
    "play",
    "score-instrument",
}
