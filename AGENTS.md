# mx

We are implementing a generator from the MusicXML XSD specification.

Ignore git history prior to `b01288`. Reading anthing before that commit will only confuse you and
degrade your performance.
