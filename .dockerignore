# Keep the build context small and fast. The quality gates only need the
# source tree plus the build configuration; tests are not run in Docker, so
# the (large) MusicXML corpus and unrelated trees are excluded.
build/
.git/
.github/
Xcode/
Documents/
DevScripts/
Resources/
*.md
.DS_Store
.idea/
