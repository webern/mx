// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The staff-line-position type indicates the line position on a given staff. Staff lines are
/// numbered from bottom to top, with 1 being the bottom line on a staff. A staff-line-position value
/// can extend beyond the range of the lines on the current staff.
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class StaffLinePosition final
{
  public:
    /// The natural zero: the default value clamped into range.
    StaffLinePosition();

    explicit StaffLinePosition(int value);

    int value() const noexcept
    {
        return m_value;
    }

    void setValue(int value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, StaffLinePosition &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static StaffLinePosition parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static StaffLinePosition parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const StaffLinePosition &other) const noexcept = default;

  private:
    int m_value{};
};

} // namespace mx::core
