// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Play.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &Play::id() const noexcept
{
    return m_id;
}

void Play::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const PlayChoice> Play::choice() const noexcept
{
    return m_choice;
}

void Play::addChoice(PlayChoice value)
{
    m_choice.push_back(std::move(value));
}

void Play::setChoice(std::vector<PlayChoice> value)
{
    m_choice = std::move(value);
}

Play parsePlay(pugi::xml_node el, const ParseContext &context)
{
    Play out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parsePlayContent(out, el, context);
    return out;
}

void parsePlayContent(Play &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor && (cursorIs(cursor, "ipa") || cursorIs(cursor, "mute") || cursorIs(cursor, "semi-pitched") ||
                      cursorIs(cursor, "other-play")))
    {
        out.addChoice(parsePlayChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "ipa") || cursorIs(cursor, "mute") || cursorIs(cursor, "semi-pitched") ||
            cursorIs(cursor, "other-play"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePlay(const Play &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializePlayContent(v, el);
}

void serializePlayContent(const Play &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializePlayChoice(item, el);
    }
}

} // namespace mx::core
