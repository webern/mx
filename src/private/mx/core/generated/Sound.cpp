// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Sound.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<NonNegativeDecimal> &Sound::tempo() const noexcept
{
    return m_tempo;
}

void Sound::setTempo(std::optional<NonNegativeDecimal> value)
{
    m_tempo = std::move(value);
}

const std::optional<NonNegativeDecimal> &Sound::dynamics() const noexcept
{
    return m_dynamics;
}

void Sound::setDynamics(std::optional<NonNegativeDecimal> value)
{
    m_dynamics = std::move(value);
}

const std::optional<YesNo> &Sound::dacapo() const noexcept
{
    return m_dacapo;
}

void Sound::setDacapo(std::optional<YesNo> value)
{
    m_dacapo = std::move(value);
}

const std::optional<std::string> &Sound::segno() const noexcept
{
    return m_segno;
}

void Sound::setSegno(std::optional<std::string> value)
{
    m_segno = std::move(value);
}

const std::optional<std::string> &Sound::dalsegno() const noexcept
{
    return m_dalsegno;
}

void Sound::setDalsegno(std::optional<std::string> value)
{
    m_dalsegno = std::move(value);
}

const std::optional<std::string> &Sound::coda() const noexcept
{
    return m_coda;
}

void Sound::setCoda(std::optional<std::string> value)
{
    m_coda = std::move(value);
}

const std::optional<std::string> &Sound::tocoda() const noexcept
{
    return m_tocoda;
}

void Sound::setTocoda(std::optional<std::string> value)
{
    m_tocoda = std::move(value);
}

const std::optional<Divisions> &Sound::divisions() const noexcept
{
    return m_divisions;
}

void Sound::setDivisions(std::optional<Divisions> value)
{
    m_divisions = std::move(value);
}

const std::optional<YesNo> &Sound::forwardRepeat() const noexcept
{
    return m_forwardRepeat;
}

void Sound::setForwardRepeat(std::optional<YesNo> value)
{
    m_forwardRepeat = std::move(value);
}

const std::optional<std::string> &Sound::fine() const noexcept
{
    return m_fine;
}

void Sound::setFine(std::optional<std::string> value)
{
    m_fine = std::move(value);
}

const std::optional<TimeOnly> &Sound::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Sound::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

const std::optional<YesNo> &Sound::pizzicato() const noexcept
{
    return m_pizzicato;
}

void Sound::setPizzicato(std::optional<YesNo> value)
{
    m_pizzicato = std::move(value);
}

const std::optional<RotationDegrees> &Sound::pan() const noexcept
{
    return m_pan;
}

void Sound::setPan(std::optional<RotationDegrees> value)
{
    m_pan = std::move(value);
}

const std::optional<RotationDegrees> &Sound::elevation() const noexcept
{
    return m_elevation;
}

void Sound::setElevation(std::optional<RotationDegrees> value)
{
    m_elevation = std::move(value);
}

const std::optional<YesNoNumber> &Sound::damperPedal() const noexcept
{
    return m_damperPedal;
}

void Sound::setDamperPedal(std::optional<YesNoNumber> value)
{
    m_damperPedal = std::move(value);
}

const std::optional<YesNoNumber> &Sound::softPedal() const noexcept
{
    return m_softPedal;
}

void Sound::setSoftPedal(std::optional<YesNoNumber> value)
{
    m_softPedal = std::move(value);
}

const std::optional<YesNoNumber> &Sound::sostenutoPedal() const noexcept
{
    return m_sostenutoPedal;
}

void Sound::setSostenutoPedal(std::optional<YesNoNumber> value)
{
    m_sostenutoPedal = std::move(value);
}

const std::optional<Token> &Sound::id() const noexcept
{
    return m_id;
}

void Sound::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const SoundGroup> Sound::group() const noexcept
{
    return m_group;
}

void Sound::addGroup(SoundGroup value)
{
    m_group.push_back(std::move(value));
}

void Sound::setGroup(std::vector<SoundGroup> value)
{
    m_group = std::move(value);
}

const std::optional<Swing> &Sound::swing() const noexcept
{
    return m_swing;
}

void Sound::setSwing(std::optional<Swing> value)
{
    m_swing = std::move(value);
}

const std::optional<Offset> &Sound::offset() const noexcept
{
    return m_offset;
}

void Sound::setOffset(std::optional<Offset> value)
{
    m_offset = std::move(value);
}

Sound parseSound(pugi::xml_node el)
{
    return parseSound(el, ParseContext{});
}

Sound parseSound(pugi::xml_node el, const ParseContext &context)
{
    Sound out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "tempo")
        {
            out.setTempo(parseValue<NonNegativeDecimal>(a.value(), context, el, "tempo"));
        }
        else if (aname == "dynamics")
        {
            out.setDynamics(parseValue<NonNegativeDecimal>(a.value(), context, el, "dynamics"));
        }
        else if (aname == "dacapo")
        {
            out.setDacapo(parseValue<YesNo>(a.value(), context, el, "dacapo"));
        }
        else if (aname == "segno")
        {
            out.setSegno(std::string{a.value()});
        }
        else if (aname == "dalsegno")
        {
            out.setDalsegno(std::string{a.value()});
        }
        else if (aname == "coda")
        {
            out.setCoda(std::string{a.value()});
        }
        else if (aname == "tocoda")
        {
            out.setTocoda(std::string{a.value()});
        }
        else if (aname == "divisions")
        {
            out.setDivisions(parseValue<Divisions>(a.value(), context, el, "divisions"));
        }
        else if (aname == "forward-repeat")
        {
            out.setForwardRepeat(parseValue<YesNo>(a.value(), context, el, "forward-repeat"));
        }
        else if (aname == "fine")
        {
            out.setFine(std::string{a.value()});
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(parseValue<TimeOnly>(a.value(), context, el, "time-only"));
        }
        else if (aname == "pizzicato")
        {
            out.setPizzicato(parseValue<YesNo>(a.value(), context, el, "pizzicato"));
        }
        else if (aname == "pan")
        {
            out.setPan(parseValue<RotationDegrees>(a.value(), context, el, "pan"));
        }
        else if (aname == "elevation")
        {
            out.setElevation(parseValue<RotationDegrees>(a.value(), context, el, "elevation"));
        }
        else if (aname == "damper-pedal")
        {
            out.setDamperPedal(parseValue<YesNoNumber>(a.value(), context, el, "damper-pedal"));
        }
        else if (aname == "soft-pedal")
        {
            out.setSoftPedal(parseValue<YesNoNumber>(a.value(), context, el, "soft-pedal"));
        }
        else if (aname == "sostenuto-pedal")
        {
            out.setSostenutoPedal(parseValue<YesNoNumber>(a.value(), context, el, "sostenuto-pedal"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseSoundContent(out, el, context);
    return out;
}

void parseSoundContent(Sound &out, pugi::xml_node el)
{
    parseSoundContent(out, el, ParseContext{});
}

void parseSoundContent(Sound &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor && (cursorIs(cursor, "instrument-change") || cursorIs(cursor, "midi-device") ||
                      cursorIs(cursor, "midi-instrument") || cursorIs(cursor, "play")))
    {
        out.addGroup(parseSoundGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "swing"))
    {
        out.setSwing(parseSwing(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "offset"))
    {
        out.setOffset(parseOffset(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "instrument-change") || cursorIs(cursor, "midi-device") ||
            cursorIs(cursor, "midi-instrument") || cursorIs(cursor, "play") || cursorIs(cursor, "swing") ||
            cursorIs(cursor, "offset"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeSound(const Sound &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.tempo())
    {
        setAttribute(el, "tempo", v.tempo()->toString());
    }
    if (v.dynamics())
    {
        setAttribute(el, "dynamics", v.dynamics()->toString());
    }
    if (v.dacapo())
    {
        setAttribute(el, "dacapo", v.dacapo()->toString());
    }
    if (v.segno())
    {
        setAttribute(el, "segno", *v.segno());
    }
    if (v.dalsegno())
    {
        setAttribute(el, "dalsegno", *v.dalsegno());
    }
    if (v.coda())
    {
        setAttribute(el, "coda", *v.coda());
    }
    if (v.tocoda())
    {
        setAttribute(el, "tocoda", *v.tocoda());
    }
    if (v.divisions())
    {
        setAttribute(el, "divisions", v.divisions()->toString());
    }
    if (v.forwardRepeat())
    {
        setAttribute(el, "forward-repeat", v.forwardRepeat()->toString());
    }
    if (v.fine())
    {
        setAttribute(el, "fine", *v.fine());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    if (v.pizzicato())
    {
        setAttribute(el, "pizzicato", v.pizzicato()->toString());
    }
    if (v.pan())
    {
        setAttribute(el, "pan", v.pan()->toString());
    }
    if (v.elevation())
    {
        setAttribute(el, "elevation", v.elevation()->toString());
    }
    if (v.damperPedal())
    {
        setAttribute(el, "damper-pedal", v.damperPedal()->toString());
    }
    if (v.softPedal())
    {
        setAttribute(el, "soft-pedal", v.softPedal()->toString());
    }
    if (v.sostenutoPedal())
    {
        setAttribute(el, "sostenuto-pedal", v.sostenutoPedal()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeSoundContent(v, el);
}

void serializeSoundContent(const Sound &v, pugi::xml_node el)
{
    for (const auto &item : v.group())
    {
        serializeSoundGroup(item, el);
    }
    if (v.swing())
    {
        serializeSwing(*v.swing(), el, "swing");
    }
    if (v.offset())
    {
        serializeOffset(*v.offset(), el, "offset");
    }
}

} // namespace mx::core
