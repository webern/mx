// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Directive.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Language> &Directive::xmlLang() const noexcept
{
    return m_xmlLang;
}

void Directive::setXMLLang(std::optional<Language> value)
{
    m_xmlLang = std::move(value);
}

const std::optional<Tenths> &Directive::defaultX() const noexcept
{
    return m_defaultX;
}

void Directive::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Directive::defaultY() const noexcept
{
    return m_defaultY;
}

void Directive::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Directive::relativeX() const noexcept
{
    return m_relativeX;
}

void Directive::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Directive::relativeY() const noexcept
{
    return m_relativeY;
}

void Directive::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Directive::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Directive::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Directive::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Directive::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Directive::fontSize() const noexcept
{
    return m_fontSize;
}

void Directive::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Directive::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Directive::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Directive::color() const noexcept
{
    return m_color;
}

void Directive::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::string &Directive::value() const noexcept
{
    return m_value;
}

void Directive::setValue(std::string value)
{
    m_value = std::move(value);
}

Directive parseDirective(pugi::xml_node el, const ParseContext &context)
{
    Directive out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "xml:lang")
        {
            out.setXMLLang(parseValue<Language>(a.value(), context, el, "xml:lang"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseDirectiveContent(out, el, context);
    return out;
}

void parseDirectiveContent(Directive &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeDirective(const Directive &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.xmlLang())
    {
        setAttribute(el, "xml:lang", v.xmlLang()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeDirectiveContent(v, el);
}

void serializeDirectiveContent(const Directive &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
