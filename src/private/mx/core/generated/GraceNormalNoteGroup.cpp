// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/GraceNormalNoteGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const FullNoteGroup &GraceNormalNoteGroup::fullNote() const noexcept
{
    return m_fullNote;
}

void GraceNormalNoteGroup::setFullNote(FullNoteGroup value)
{
    m_fullNote = std::move(value);
}

std::span<const Tie> GraceNormalNoteGroup::tie() const noexcept
{
    return m_tie;
}

Result<void> GraceNormalNoteGroup::addTie(Tie value)
{
    if (m_tie.size() >= 2)
    {
        return Error{ErrorCode::tooManyElements, {}, "at most 2 tie occurrences"};
    }
    m_tie.push_back(std::move(value));
    return {};
}

void GraceNormalNoteGroup::clearTie() noexcept
{
    m_tie.clear();
}

GraceNormalNoteGroup parseGraceNormalNoteGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    GraceNormalNoteGroup out;
    if (cursor && (cursorIs(cursor, "chord") || cursorIs(cursor, "pitch") || cursorIs(cursor, "unpitched") ||
                   cursorIs(cursor, "rest")))
    {
        out.setFullNote(parseFullNoteGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "chord");
    }
    while (cursorIs(cursor, "tie"))
    {
        if (!out.addTie(parseTie(cursor, context)))
        {
            throwTooManyElements(cursor);
        }
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeGraceNormalNoteGroup(const GraceNormalNoteGroup &v, pugi::xml_node el)
{
    serializeFullNoteGroup(v.fullNote(), el);
    for (const auto &item : v.tie())
    {
        serializeTie(item, el, "tie");
    }
}

} // namespace mx::core
