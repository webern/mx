// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Decimal.h"
#include "mx/core/Lexical.h"
#include "mx/core/generated/CSSFontSize.h"

#include <cassert>
#include <string>
#include <string_view>
#include <variant>

namespace mx::core
{

/// The font-size can be one of the CSS font sizes (xx-small, x-small, small, medium, large, x-large,
/// xx-large) or a numeric point size.
/// Tagged union over the schema's member types: exactly one alternative by
/// construction. Alternatives are positional; dispatch is by Kind/index.
class FontSize final
{
  public:
    enum class Kind
    {
        decimal,
        cssFontSize,
    };

    /// The natural zero: the first member's natural zero.
    FontSize();

    static FontSize decimal(Decimal value);
    static FontSize cssFontSize(CSSFontSize value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isDecimal() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isDecimal().
    const Decimal &asDecimal() const noexcept
    {
        assert(isDecimal());
        return *std::get_if<0>(&m_value);
    }

    bool isCSSFontSize() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isCSSFontSize().
    const CSSFontSize &asCSSFontSize() const noexcept
    {
        assert(isCSSFontSize());
        return *std::get_if<1>(&m_value);
    }

    std::string toString() const;

    /// Strict: members tried in schema order.
    static bool tryParse(std::string_view text, FontSize &out);

    /// Lenient: unparseable text falls back to the first member's lenient
    /// parse (never produces an invalid value).
    static FontSize parse(std::string_view text);

    /// Lenient, and says whether no member matched or a matched number was
    /// clamped.
    static FontSize parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const FontSize &other) const = default;

  private:
    using Storage = std::variant<Decimal, CSSFontSize>;

    explicit FontSize(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

} // namespace mx::core
