// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/StaffTuning.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StaffLine &StaffTuning::line() const noexcept
{
    return m_line;
}

void StaffTuning::setLine(StaffLine value)
{
    m_line = std::move(value);
}

const TuningGroup &StaffTuning::tuning() const noexcept
{
    return m_tuning;
}

void StaffTuning::setTuning(TuningGroup value)
{
    m_tuning = std::move(value);
}

StaffTuning parseStaffTuning(pugi::xml_node el, const ParseContext &context)
{
    StaffTuning out;
    bool seen_line = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "line")
        {
            seen_line = true;
            out.setLine(parseValue<StaffLine>(a.value(), context, el, "line"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_line)
    {
        throwMissingAttribute(el, "line");
    }
    parseStaffTuningContent(out, el, context);
    return out;
}

void parseStaffTuningContent(StaffTuning &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "tuning-step")))
    {
        out.setTuning(parseTuningGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "tuning-step");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "tuning-step") || cursorIs(cursor, "tuning-alter") || cursorIs(cursor, "tuning-octave"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeStaffTuning(const StaffTuning &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "line", v.line().toString());
    serializeStaffTuningContent(v, el);
}

void serializeStaffTuningContent(const StaffTuning &v, pugi::xml_node el)
{
    serializeTuningGroup(v.tuning(), el);
}

} // namespace mx::core
