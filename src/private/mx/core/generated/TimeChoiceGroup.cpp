// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TimeChoiceGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const TimeSignatureGroup> TimeChoiceGroup::timeSignature() const noexcept
{
    return m_timeSignature.items();
}

void TimeChoiceGroup::addTimeSignature(TimeSignatureGroup value)
{
    m_timeSignature.add(std::move(value));
}

void TimeChoiceGroup::setTimeSignature(OneOrMore<TimeSignatureGroup> value)
{
    m_timeSignature = std::move(value);
}

const std::optional<Interchangeable> &TimeChoiceGroup::interchangeable() const noexcept
{
    return m_interchangeable;
}

void TimeChoiceGroup::setInterchangeable(std::optional<Interchangeable> value)
{
    m_interchangeable = std::move(value);
}

TimeChoiceGroup parseTimeChoiceGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseTimeChoiceGroup(el, cursor, ParseContext{});
}

TimeChoiceGroup parseTimeChoiceGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    TimeChoiceGroup out;
    if (!(cursor && (cursorIs(cursor, "beats"))))
    {
        throwMissingElement(el, "beats");
    }
    out.setTimeSignature(OneOrMore<TimeSignatureGroup>{parseTimeSignatureGroup(el, cursor, context)});
    while (cursor && (cursorIs(cursor, "beats")))
    {
        out.addTimeSignature(parseTimeSignatureGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "interchangeable"))
    {
        out.setInterchangeable(parseInterchangeable(cursor, context));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeTimeChoiceGroup(const TimeChoiceGroup &v, pugi::xml_node el)
{
    for (const auto &item : v.timeSignature())
    {
        serializeTimeSignatureGroup(item, el);
    }
    if (v.interchangeable())
    {
        serializeInterchangeable(*v.interchangeable(), el, "interchangeable");
    }
}

} // namespace mx::core
