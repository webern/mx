// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Swing.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const SwingChoice &Swing::choice() const noexcept
{
    return m_choice;
}

void Swing::setChoice(SwingChoice value)
{
    m_choice = std::move(value);
}

const std::optional<std::string> &Swing::swingStyle() const noexcept
{
    return m_swingStyle;
}

void Swing::setSwingStyle(std::optional<std::string> value)
{
    m_swingStyle = std::move(value);
}

Swing parseSwing(pugi::xml_node el, const ParseContext &context)
{
    Swing out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseSwingContent(out, el, context);
    return out;
}

void parseSwingContent(Swing &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "straight") || cursorIs(cursor, "first")))
    {
        out.setChoice(parseSwingChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "straight");
    }
    if (cursorIs(cursor, "swing-style"))
    {
        out.setSwingStyle(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "straight") || cursorIs(cursor, "first") || cursorIs(cursor, "second") ||
            cursorIs(cursor, "swing-type") || cursorIs(cursor, "swing-style"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeSwing(const Swing &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeSwingContent(v, el);
}

void serializeSwingContent(const Swing &v, pugi::xml_node el)
{
    serializeSwingChoice(v.choice(), el);
    if (v.swingStyle())
    {
        writeTextElement(el, "swing-style", *v.swingStyle());
    }
}

} // namespace mx::core
