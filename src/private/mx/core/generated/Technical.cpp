// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Technical.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &Technical::id() const noexcept
{
    return m_id;
}

void Technical::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const TechnicalChoice> Technical::choice() const noexcept
{
    return m_choice;
}

void Technical::addChoice(TechnicalChoice value)
{
    m_choice.push_back(std::move(value));
}

void Technical::setChoice(std::vector<TechnicalChoice> value)
{
    m_choice = std::move(value);
}

Technical parseTechnical(pugi::xml_node el, const ParseContext &context)
{
    Technical out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseTechnicalContent(out, el, context);
    return out;
}

void parseTechnicalContent(Technical &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor &&
           (cursorIs(cursor, "up-bow") || cursorIs(cursor, "down-bow") || cursorIs(cursor, "harmonic") ||
            cursorIs(cursor, "open-string") || cursorIs(cursor, "thumb-position") || cursorIs(cursor, "fingering") ||
            cursorIs(cursor, "pluck") || cursorIs(cursor, "double-tongue") || cursorIs(cursor, "triple-tongue") ||
            cursorIs(cursor, "stopped") || cursorIs(cursor, "snap-pizzicato") || cursorIs(cursor, "fret") ||
            cursorIs(cursor, "string") || cursorIs(cursor, "hammer-on") || cursorIs(cursor, "pull-off") ||
            cursorIs(cursor, "bend") || cursorIs(cursor, "tap") || cursorIs(cursor, "heel") ||
            cursorIs(cursor, "toe") || cursorIs(cursor, "fingernails") || cursorIs(cursor, "hole") ||
            cursorIs(cursor, "arrow") || cursorIs(cursor, "handbell") || cursorIs(cursor, "brass-bend") ||
            cursorIs(cursor, "flip") || cursorIs(cursor, "smear") || cursorIs(cursor, "open") ||
            cursorIs(cursor, "half-muted") || cursorIs(cursor, "harmon-mute") || cursorIs(cursor, "golpe") ||
            cursorIs(cursor, "other-technical")))
    {
        out.addChoice(parseTechnicalChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "up-bow") || cursorIs(cursor, "down-bow") || cursorIs(cursor, "harmonic") ||
            cursorIs(cursor, "open-string") || cursorIs(cursor, "thumb-position") || cursorIs(cursor, "fingering") ||
            cursorIs(cursor, "pluck") || cursorIs(cursor, "double-tongue") || cursorIs(cursor, "triple-tongue") ||
            cursorIs(cursor, "stopped") || cursorIs(cursor, "snap-pizzicato") || cursorIs(cursor, "fret") ||
            cursorIs(cursor, "string") || cursorIs(cursor, "hammer-on") || cursorIs(cursor, "pull-off") ||
            cursorIs(cursor, "bend") || cursorIs(cursor, "tap") || cursorIs(cursor, "heel") ||
            cursorIs(cursor, "toe") || cursorIs(cursor, "fingernails") || cursorIs(cursor, "hole") ||
            cursorIs(cursor, "arrow") || cursorIs(cursor, "handbell") || cursorIs(cursor, "brass-bend") ||
            cursorIs(cursor, "flip") || cursorIs(cursor, "smear") || cursorIs(cursor, "open") ||
            cursorIs(cursor, "half-muted") || cursorIs(cursor, "harmon-mute") || cursorIs(cursor, "golpe") ||
            cursorIs(cursor, "other-technical"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTechnical(const Technical &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeTechnicalContent(v, el);
}

void serializeTechnicalContent(const Technical &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializeTechnicalChoice(item, el);
    }
}

} // namespace mx::core
