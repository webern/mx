// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ScorePart.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &ScorePart::id() const noexcept
{
    return m_id;
}

void ScorePart::setID(Token value)
{
    m_id = std::move(value);
}

const std::optional<Identification> &ScorePart::identification() const noexcept
{
    return m_identification;
}

void ScorePart::setIdentification(std::optional<Identification> value)
{
    m_identification = std::move(value);
}

std::span<const PartLink> ScorePart::partLink() const noexcept
{
    return m_partLink;
}

void ScorePart::addPartLink(PartLink value)
{
    m_partLink.push_back(std::move(value));
}

void ScorePart::setPartLink(std::vector<PartLink> value)
{
    m_partLink = std::move(value);
}

const PartName &ScorePart::partName() const noexcept
{
    return m_partName;
}

void ScorePart::setPartName(PartName value)
{
    m_partName = std::move(value);
}

const std::optional<NameDisplay> &ScorePart::partNameDisplay() const noexcept
{
    return m_partNameDisplay;
}

void ScorePart::setPartNameDisplay(std::optional<NameDisplay> value)
{
    m_partNameDisplay = std::move(value);
}

const std::optional<PartName> &ScorePart::partAbbreviation() const noexcept
{
    return m_partAbbreviation;
}

void ScorePart::setPartAbbreviation(std::optional<PartName> value)
{
    m_partAbbreviation = std::move(value);
}

const std::optional<NameDisplay> &ScorePart::partAbbreviationDisplay() const noexcept
{
    return m_partAbbreviationDisplay;
}

void ScorePart::setPartAbbreviationDisplay(std::optional<NameDisplay> value)
{
    m_partAbbreviationDisplay = std::move(value);
}

std::span<const std::string> ScorePart::group() const noexcept
{
    return m_group;
}

void ScorePart::addGroup(std::string value)
{
    m_group.push_back(std::move(value));
}

void ScorePart::setGroup(std::vector<std::string> value)
{
    m_group = std::move(value);
}

std::span<const ScoreInstrument> ScorePart::scoreInstrument() const noexcept
{
    return m_scoreInstrument;
}

void ScorePart::addScoreInstrument(ScoreInstrument value)
{
    m_scoreInstrument.push_back(std::move(value));
}

void ScorePart::setScoreInstrument(std::vector<ScoreInstrument> value)
{
    m_scoreInstrument = std::move(value);
}

std::span<const Player> ScorePart::player() const noexcept
{
    return m_player;
}

void ScorePart::addPlayer(Player value)
{
    m_player.push_back(std::move(value));
}

void ScorePart::setPlayer(std::vector<Player> value)
{
    m_player = std::move(value);
}

std::span<const ScorePartMIDIGroup> ScorePart::midiGroup() const noexcept
{
    return m_midiGroup;
}

void ScorePart::addMIDIGroup(ScorePartMIDIGroup value)
{
    m_midiGroup.push_back(std::move(value));
}

void ScorePart::setMIDIGroup(std::vector<ScorePartMIDIGroup> value)
{
    m_midiGroup = std::move(value);
}

ScorePart parseScorePart(pugi::xml_node el)
{
    return parseScorePart(el, ParseContext{});
}

ScorePart parseScorePart(pugi::xml_node el, const ParseContext &context)
{
    ScorePart out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseScorePartContent(out, el, context);
    return out;
}

void parseScorePartContent(ScorePart &out, pugi::xml_node el)
{
    parseScorePartContent(out, el, ParseContext{});
}

void parseScorePartContent(ScorePart &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "identification"))
    {
        out.setIdentification(parseIdentification(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "part-link"))
    {
        out.addPartLink(parsePartLink(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-name"))
    {
        out.setPartName(parsePartName(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "part-name");
    }
    if (cursorIs(cursor, "part-name-display"))
    {
        out.setPartNameDisplay(parseNameDisplay(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-abbreviation"))
    {
        out.setPartAbbreviation(parsePartName(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-abbreviation-display"))
    {
        out.setPartAbbreviationDisplay(parseNameDisplay(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "group"))
    {
        out.addGroup(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "score-instrument"))
    {
        out.addScoreInstrument(parseScoreInstrument(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "player"))
    {
        out.addPlayer(parsePlayer(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursor && (cursorIs(cursor, "midi-device") || cursorIs(cursor, "midi-instrument")))
    {
        out.addMIDIGroup(parseScorePartMIDIGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "identification") || cursorIs(cursor, "part-link") || cursorIs(cursor, "part-name") ||
            cursorIs(cursor, "part-name-display") || cursorIs(cursor, "part-abbreviation") ||
            cursorIs(cursor, "part-abbreviation-display") || cursorIs(cursor, "group") ||
            cursorIs(cursor, "score-instrument") || cursorIs(cursor, "player") || cursorIs(cursor, "midi-device") ||
            cursorIs(cursor, "midi-instrument"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeScorePart(const ScorePart &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializeScorePartContent(v, el);
}

void serializeScorePartContent(const ScorePart &v, pugi::xml_node el)
{
    if (v.identification())
    {
        serializeIdentification(*v.identification(), el, "identification");
    }
    for (const auto &item : v.partLink())
    {
        serializePartLink(item, el, "part-link");
    }
    serializePartName(v.partName(), el, "part-name");
    if (v.partNameDisplay())
    {
        serializeNameDisplay(*v.partNameDisplay(), el, "part-name-display");
    }
    if (v.partAbbreviation())
    {
        serializePartName(*v.partAbbreviation(), el, "part-abbreviation");
    }
    if (v.partAbbreviationDisplay())
    {
        serializeNameDisplay(*v.partAbbreviationDisplay(), el, "part-abbreviation-display");
    }
    for (const auto &item : v.group())
    {
        writeTextElement(el, "group", item);
    }
    for (const auto &item : v.scoreInstrument())
    {
        serializeScoreInstrument(item, el, "score-instrument");
    }
    for (const auto &item : v.player())
    {
        serializePlayer(item, el, "player");
    }
    for (const auto &item : v.midiGroup())
    {
        serializeScorePartMIDIGroup(item, el);
    }
}

} // namespace mx::core
