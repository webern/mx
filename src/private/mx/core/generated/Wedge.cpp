// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Wedge.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const WedgeType &Wedge::type() const noexcept
{
    return m_type;
}

void Wedge::setType(WedgeType value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &Wedge::number() const noexcept
{
    return m_number;
}

void Wedge::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<Tenths> &Wedge::spread() const noexcept
{
    return m_spread;
}

void Wedge::setSpread(std::optional<Tenths> value)
{
    m_spread = std::move(value);
}

const std::optional<YesNo> &Wedge::niente() const noexcept
{
    return m_niente;
}

void Wedge::setNiente(std::optional<YesNo> value)
{
    m_niente = std::move(value);
}

const std::optional<LineType> &Wedge::lineType() const noexcept
{
    return m_lineType;
}

void Wedge::setLineType(std::optional<LineType> value)
{
    m_lineType = std::move(value);
}

const std::optional<Tenths> &Wedge::dashLength() const noexcept
{
    return m_dashLength;
}

void Wedge::setDashLength(std::optional<Tenths> value)
{
    m_dashLength = std::move(value);
}

const std::optional<Tenths> &Wedge::spaceLength() const noexcept
{
    return m_spaceLength;
}

void Wedge::setSpaceLength(std::optional<Tenths> value)
{
    m_spaceLength = std::move(value);
}

const std::optional<Tenths> &Wedge::defaultX() const noexcept
{
    return m_defaultX;
}

void Wedge::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Wedge::defaultY() const noexcept
{
    return m_defaultY;
}

void Wedge::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Wedge::relativeX() const noexcept
{
    return m_relativeX;
}

void Wedge::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Wedge::relativeY() const noexcept
{
    return m_relativeY;
}

void Wedge::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<Color> &Wedge::color() const noexcept
{
    return m_color;
}

void Wedge::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &Wedge::id() const noexcept
{
    return m_id;
}

void Wedge::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

Wedge parseWedge(pugi::xml_node el, const ParseContext &context)
{
    Wedge out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<WedgeType>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(parseValue<NumberLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "spread")
        {
            out.setSpread(parseValue<Tenths>(a.value(), context, el, "spread"));
        }
        else if (aname == "niente")
        {
            out.setNiente(parseValue<YesNo>(a.value(), context, el, "niente"));
        }
        else if (aname == "line-type")
        {
            out.setLineType(parseValue<LineType>(a.value(), context, el, "line-type"));
        }
        else if (aname == "dash-length")
        {
            out.setDashLength(parseValue<Tenths>(a.value(), context, el, "dash-length"));
        }
        else if (aname == "space-length")
        {
            out.setSpaceLength(parseValue<Tenths>(a.value(), context, el, "space-length"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseWedgeContent(out, el, context);
    return out;
}

void parseWedgeContent(Wedge &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeWedge(const Wedge &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.spread())
    {
        setAttribute(el, "spread", v.spread()->toString());
    }
    if (v.niente())
    {
        setAttribute(el, "niente", v.niente()->toString());
    }
    if (v.lineType())
    {
        setAttribute(el, "line-type", v.lineType()->toString());
    }
    if (v.dashLength())
    {
        setAttribute(el, "dash-length", v.dashLength()->toString());
    }
    if (v.spaceLength())
    {
        setAttribute(el, "space-length", v.spaceLength()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeWedgeContent(v, el);
}

void serializeWedgeContent(const Wedge &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
