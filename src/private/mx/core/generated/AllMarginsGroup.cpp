// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/AllMarginsGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const LeftRightMarginsGroup &AllMarginsGroup::leftRightMargins() const noexcept
{
    return m_leftRightMargins;
}

void AllMarginsGroup::setLeftRightMargins(LeftRightMarginsGroup value)
{
    m_leftRightMargins = std::move(value);
}

const Tenths &AllMarginsGroup::topMargin() const noexcept
{
    return m_topMargin;
}

void AllMarginsGroup::setTopMargin(Tenths value)
{
    m_topMargin = std::move(value);
}

const Tenths &AllMarginsGroup::bottomMargin() const noexcept
{
    return m_bottomMargin;
}

void AllMarginsGroup::setBottomMargin(Tenths value)
{
    m_bottomMargin = std::move(value);
}

AllMarginsGroup parseAllMarginsGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    AllMarginsGroup out;
    if (cursor && (cursorIs(cursor, "left-margin")))
    {
        out.setLeftRightMargins(parseLeftRightMarginsGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "left-margin");
    }
    if (cursorIs(cursor, "top-margin"))
    {
        out.setTopMargin(parseValue<Tenths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "top-margin");
    }
    if (cursorIs(cursor, "bottom-margin"))
    {
        out.setBottomMargin(parseValue<Tenths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "bottom-margin");
    }
    return out;
}

void serializeAllMarginsGroup(const AllMarginsGroup &v, pugi::xml_node el)
{
    serializeLeftRightMarginsGroup(v.leftRightMargins(), el);
    writeTextElement(el, "top-margin", v.topMargin().toString());
    writeTextElement(el, "bottom-margin", v.bottomMargin().toString());
}

} // namespace mx::core
