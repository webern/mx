// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/SlashGroup.h"
#include "mx/core/generated/StartStop.h"
#include "mx/core/generated/YesNo.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The beat-repeat type is used to indicate that a single beat (but possibly many notes) is
/// repeated. The slashes attribute specifies the number of slashes to use in the symbol. The
/// use-dots attribute indicates whether or not to use dots as well (for instance, with mixed rhythm
/// patterns). The value for slashes is 1 and the value for use-dots is no if not specified. The stop
/// type indicates the first beat where the repeats are no longer displayed. Both the start and stop
/// of the beat being repeated should be specified unless the repeats are displayed through the end
/// of the part. The beat-repeat element specifies a notation style for repetitions. The actual music
/// being repeated needs to be repeated within the MusicXML file. This element specifies the notation
/// that indicates the repeat.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class BeatRepeat final
{
  public:
    const StartStop &type() const noexcept;
    void setType(StartStop value);
    const std::optional<int> &slashes() const noexcept;
    void setSlashes(std::optional<int> value);
    const std::optional<YesNo> &useDots() const noexcept;
    void setUseDots(std::optional<YesNo> value);
    const std::optional<SlashGroup> &slash() const noexcept;
    void setSlash(std::optional<SlashGroup> value);
    bool operator==(const BeatRepeat &other) const = default;

  private:
    StartStop m_type{};
    std::optional<int> m_slashes;
    std::optional<YesNo> m_useDots;
    std::optional<SlashGroup> m_slash;
};

BeatRepeat parseBeatRepeat(pugi::xml_node el);

BeatRepeat parseBeatRepeat(pugi::xml_node el, const ParseContext &context);

void parseBeatRepeatContent(BeatRepeat &out, pugi::xml_node el);

void parseBeatRepeatContent(BeatRepeat &out, pugi::xml_node el, const ParseContext &context);

void serializeBeatRepeat(const BeatRepeat &v, pugi::xml_node parent, const char *tag);

void serializeBeatRepeatContent(const BeatRepeat &v, pugi::xml_node el);

} // namespace mx::core
