// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Defaults.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Scaling> &Defaults::scaling() const noexcept
{
    return m_scaling;
}

void Defaults::setScaling(std::optional<Scaling> value)
{
    m_scaling = std::move(value);
}

bool Defaults::concertScore() const noexcept
{
    return m_concertScore;
}

void Defaults::setConcertScore(bool value) noexcept
{
    m_concertScore = value;
}

const LayoutGroup &Defaults::layout() const noexcept
{
    return m_layout;
}

void Defaults::setLayout(LayoutGroup value)
{
    m_layout = std::move(value);
}

const std::optional<Appearance> &Defaults::appearance() const noexcept
{
    return m_appearance;
}

void Defaults::setAppearance(std::optional<Appearance> value)
{
    m_appearance = std::move(value);
}

const std::optional<EmptyFont> &Defaults::musicFont() const noexcept
{
    return m_musicFont;
}

void Defaults::setMusicFont(std::optional<EmptyFont> value)
{
    m_musicFont = std::move(value);
}

const std::optional<EmptyFont> &Defaults::wordFont() const noexcept
{
    return m_wordFont;
}

void Defaults::setWordFont(std::optional<EmptyFont> value)
{
    m_wordFont = std::move(value);
}

std::span<const LyricFont> Defaults::lyricFont() const noexcept
{
    return m_lyricFont;
}

void Defaults::addLyricFont(LyricFont value)
{
    m_lyricFont.push_back(std::move(value));
}

void Defaults::setLyricFont(std::vector<LyricFont> value)
{
    m_lyricFont = std::move(value);
}

std::span<const LyricLanguage> Defaults::lyricLanguage() const noexcept
{
    return m_lyricLanguage;
}

void Defaults::addLyricLanguage(LyricLanguage value)
{
    m_lyricLanguage.push_back(std::move(value));
}

void Defaults::setLyricLanguage(std::vector<LyricLanguage> value)
{
    m_lyricLanguage = std::move(value);
}

Defaults parseDefaults(pugi::xml_node el)
{
    return parseDefaults(el, ParseContext{});
}

Defaults parseDefaults(pugi::xml_node el, const ParseContext &context)
{
    Defaults out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseDefaultsContent(out, el, context);
    return out;
}

void parseDefaultsContent(Defaults &out, pugi::xml_node el)
{
    parseDefaultsContent(out, el, ParseContext{});
}

void parseDefaultsContent(Defaults &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "scaling"))
    {
        out.setScaling(parseScaling(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "concert-score"))
    {
        parseEmpty(cursor, context);
        out.setConcertScore(true);
        cursor = nextElement(cursor);
    }
    if (cursor &&
        (cursorIs(cursor, "page-layout") || cursorIs(cursor, "system-layout") || cursorIs(cursor, "staff-layout")))
    {
        out.setLayout(parseLayoutGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "appearance"))
    {
        out.setAppearance(parseAppearance(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "music-font"))
    {
        out.setMusicFont(parseEmptyFont(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "word-font"))
    {
        out.setWordFont(parseEmptyFont(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "lyric-font"))
    {
        out.addLyricFont(parseLyricFont(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "lyric-language"))
    {
        out.addLyricLanguage(parseLyricLanguage(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "scaling") || cursorIs(cursor, "concert-score") || cursorIs(cursor, "page-layout") ||
            cursorIs(cursor, "system-layout") || cursorIs(cursor, "staff-layout") || cursorIs(cursor, "appearance") ||
            cursorIs(cursor, "music-font") || cursorIs(cursor, "word-font") || cursorIs(cursor, "lyric-font") ||
            cursorIs(cursor, "lyric-language"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeDefaults(const Defaults &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeDefaultsContent(v, el);
}

void serializeDefaultsContent(const Defaults &v, pugi::xml_node el)
{
    if (v.scaling())
    {
        serializeScaling(*v.scaling(), el, "scaling");
    }
    if (v.concertScore())
    {
        el.append_child("concert-score");
    }
    serializeLayoutGroup(v.layout(), el);
    if (v.appearance())
    {
        serializeAppearance(*v.appearance(), el, "appearance");
    }
    if (v.musicFont())
    {
        serializeEmptyFont(*v.musicFont(), el, "music-font");
    }
    if (v.wordFont())
    {
        serializeEmptyFont(*v.wordFont(), el, "word-font");
    }
    for (const auto &item : v.lyricFont())
    {
        serializeLyricFont(item, el, "lyric-font");
    }
    for (const auto &item : v.lyricLanguage())
    {
        serializeLyricLanguage(item, el, "lyric-language");
    }
}

} // namespace mx::core
