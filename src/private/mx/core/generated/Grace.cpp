// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Grace.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Percent> &Grace::stealTimePrevious() const noexcept
{
    return m_stealTimePrevious;
}

void Grace::setStealTimePrevious(std::optional<Percent> value)
{
    m_stealTimePrevious = std::move(value);
}

const std::optional<Percent> &Grace::stealTimeFollowing() const noexcept
{
    return m_stealTimeFollowing;
}

void Grace::setStealTimeFollowing(std::optional<Percent> value)
{
    m_stealTimeFollowing = std::move(value);
}

const std::optional<Divisions> &Grace::makeTime() const noexcept
{
    return m_makeTime;
}

void Grace::setMakeTime(std::optional<Divisions> value)
{
    m_makeTime = std::move(value);
}

const std::optional<YesNo> &Grace::slash() const noexcept
{
    return m_slash;
}

void Grace::setSlash(std::optional<YesNo> value)
{
    m_slash = std::move(value);
}

Grace parseGrace(pugi::xml_node el, const ParseContext &context)
{
    Grace out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "steal-time-previous")
        {
            out.setStealTimePrevious(parseValue<Percent>(a.value(), context, el, "steal-time-previous"));
        }
        else if (aname == "steal-time-following")
        {
            out.setStealTimeFollowing(parseValue<Percent>(a.value(), context, el, "steal-time-following"));
        }
        else if (aname == "make-time")
        {
            out.setMakeTime(parseValue<Divisions>(a.value(), context, el, "make-time"));
        }
        else if (aname == "slash")
        {
            out.setSlash(parseValue<YesNo>(a.value(), context, el, "slash"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseGraceContent(out, el, context);
    return out;
}

void parseGraceContent(Grace &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeGrace(const Grace &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.stealTimePrevious())
    {
        setAttribute(el, "steal-time-previous", v.stealTimePrevious()->toString());
    }
    if (v.stealTimeFollowing())
    {
        setAttribute(el, "steal-time-following", v.stealTimeFollowing()->toString());
    }
    if (v.makeTime())
    {
        setAttribute(el, "make-time", v.makeTime()->toString());
    }
    if (v.slash())
    {
        setAttribute(el, "slash", v.slash()->toString());
    }
    serializeGraceContent(v, el);
}

void serializeGraceContent(const Grace &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
