// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/CueNoteGroup.h"
#include "mx/core/generated/GraceNoteGroup.h"
#include "mx/core/generated/NormalNoteGroup.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class NoteChoice final
{
  public:
    enum class Kind
    {
        graceNoteGroup,
        cueNoteGroup,
        normalNoteGroup,
    };

    /// The natural zero: the first alternative.
    NoteChoice();

    static NoteChoice graceNoteGroup(GraceNoteGroup value);
    static NoteChoice cueNoteGroup(CueNoteGroup value);
    static NoteChoice normalNoteGroup(NormalNoteGroup value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isGraceNoteGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isGraceNoteGroup().
    const GraceNoteGroup &asGraceNoteGroup() const noexcept
    {
        assert(isGraceNoteGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isCueNoteGroup() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isCueNoteGroup().
    const CueNoteGroup &asCueNoteGroup() const noexcept
    {
        assert(isCueNoteGroup());
        return *std::get_if<1>(&m_value);
    }

    bool isNormalNoteGroup() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isNormalNoteGroup().
    const NormalNoteGroup &asNormalNoteGroup() const noexcept
    {
        assert(isNormalNoteGroup());
        return *std::get_if<2>(&m_value);
    }

    bool operator==(const NoteChoice &other) const = default;

  private:
    using Storage = std::variant<GraceNoteGroup, CueNoteGroup, NormalNoteGroup>;

    explicit NoteChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
NoteChoice parseNoteChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeNoteChoice(const NoteChoice &v, pugi::xml_node el);

} // namespace mx::core
