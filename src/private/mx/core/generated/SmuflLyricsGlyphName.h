// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The smufl-lyrics-glyph-name type is used to reference a specific Standard Music Font Layout
/// (SMuFL) lyrics elision character. The value is a SMuFL canonical glyph name that starts with
/// lyrics.
/// Structural representation (plan §2.2): the glyph-name prefix is
/// structural and emitted by the serializer; only the suffix is stored.
/// Construction repairs the suffix to name characters
/// (and to non-empty, the placeholder being "X"), so a
/// pattern-invalid name cannot exist.
class SmuflLyricsGlyphName final
{
  public:
    /// The natural zero: the prefix plus the placeholder suffix.
    SmuflLyricsGlyphName();

    explicit SmuflLyricsGlyphName(std::string suffix);

    const std::string &suffix() const noexcept
    {
        return m_suffix;
    }

    void setSuffix(std::string value);

    std::string toString() const;

    /// Strict: a known prefix followed by a pattern-valid suffix.
    static bool tryParse(std::string_view text, SmuflLyricsGlyphName &out);

    /// Lenient: repairs into the nearest valid glyph name.
    static SmuflLyricsGlyphName parse(std::string_view text);

    /// Lenient, and says whether the text had to be repaired.
    static SmuflLyricsGlyphName parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const SmuflLyricsGlyphName &other) const noexcept = default;

  private:
    void repair();

    std::string m_suffix;
};

} // namespace mx::core
