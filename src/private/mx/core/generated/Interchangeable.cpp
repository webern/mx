// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Interchangeable.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<TimeSymbol> &Interchangeable::symbol() const noexcept
{
    return m_symbol;
}

void Interchangeable::setSymbol(std::optional<TimeSymbol> value)
{
    m_symbol = std::move(value);
}

const std::optional<TimeSeparator> &Interchangeable::separator() const noexcept
{
    return m_separator;
}

void Interchangeable::setSeparator(std::optional<TimeSeparator> value)
{
    m_separator = std::move(value);
}

const std::optional<TimeRelation> &Interchangeable::timeRelation() const noexcept
{
    return m_timeRelation;
}

void Interchangeable::setTimeRelation(std::optional<TimeRelation> value)
{
    m_timeRelation = std::move(value);
}

std::span<const TimeSignatureGroup> Interchangeable::timeSignature() const noexcept
{
    return m_timeSignature.items();
}

void Interchangeable::addTimeSignature(TimeSignatureGroup value)
{
    m_timeSignature.add(std::move(value));
}

void Interchangeable::setTimeSignature(OneOrMore<TimeSignatureGroup> value)
{
    m_timeSignature = std::move(value);
}

Interchangeable parseInterchangeable(pugi::xml_node el)
{
    return parseInterchangeable(el, ParseContext{});
}

Interchangeable parseInterchangeable(pugi::xml_node el, const ParseContext &context)
{
    Interchangeable out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "symbol")
        {
            out.setSymbol(parseValue<TimeSymbol>(a.value(), context, el, "symbol"));
        }
        else if (aname == "separator")
        {
            out.setSeparator(parseValue<TimeSeparator>(a.value(), context, el, "separator"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseInterchangeableContent(out, el, context);
    return out;
}

void parseInterchangeableContent(Interchangeable &out, pugi::xml_node el)
{
    parseInterchangeableContent(out, el, ParseContext{});
}

void parseInterchangeableContent(Interchangeable &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "time-relation"))
    {
        out.setTimeRelation(parseValue<TimeRelation>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (!(cursor && (cursorIs(cursor, "beats"))))
    {
        throwMissingElement(el, "beats");
    }
    out.setTimeSignature(OneOrMore<TimeSignatureGroup>{parseTimeSignatureGroup(el, cursor, context)});
    while (cursor && (cursorIs(cursor, "beats")))
    {
        out.addTimeSignature(parseTimeSignatureGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "time-relation") || cursorIs(cursor, "beats") || cursorIs(cursor, "beat-type"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeInterchangeable(const Interchangeable &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.symbol())
    {
        setAttribute(el, "symbol", v.symbol()->toString());
    }
    if (v.separator())
    {
        setAttribute(el, "separator", v.separator()->toString());
    }
    serializeInterchangeableContent(v, el);
}

void serializeInterchangeableContent(const Interchangeable &v, pugi::xml_node el)
{
    if (v.timeRelation())
    {
        writeTextElement(el, "time-relation", v.timeRelation()->toString());
    }
    for (const auto &item : v.timeSignature())
    {
        serializeTimeSignatureGroup(item, el);
    }
}

} // namespace mx::core
