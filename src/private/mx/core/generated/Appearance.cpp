// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Appearance.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const LineWidth> Appearance::lineWidth() const noexcept
{
    return m_lineWidth;
}

void Appearance::addLineWidth(LineWidth value)
{
    m_lineWidth.push_back(std::move(value));
}

void Appearance::setLineWidth(std::vector<LineWidth> value)
{
    m_lineWidth = std::move(value);
}

std::span<const NoteSize> Appearance::noteSize() const noexcept
{
    return m_noteSize;
}

void Appearance::addNoteSize(NoteSize value)
{
    m_noteSize.push_back(std::move(value));
}

void Appearance::setNoteSize(std::vector<NoteSize> value)
{
    m_noteSize = std::move(value);
}

std::span<const Distance> Appearance::distance() const noexcept
{
    return m_distance;
}

void Appearance::addDistance(Distance value)
{
    m_distance.push_back(std::move(value));
}

void Appearance::setDistance(std::vector<Distance> value)
{
    m_distance = std::move(value);
}

std::span<const Glyph> Appearance::glyph() const noexcept
{
    return m_glyph;
}

void Appearance::addGlyph(Glyph value)
{
    m_glyph.push_back(std::move(value));
}

void Appearance::setGlyph(std::vector<Glyph> value)
{
    m_glyph = std::move(value);
}

std::span<const OtherAppearance> Appearance::otherAppearance() const noexcept
{
    return m_otherAppearance;
}

void Appearance::addOtherAppearance(OtherAppearance value)
{
    m_otherAppearance.push_back(std::move(value));
}

void Appearance::setOtherAppearance(std::vector<OtherAppearance> value)
{
    m_otherAppearance = std::move(value);
}

Appearance parseAppearance(pugi::xml_node el)
{
    return parseAppearance(el, ParseContext{});
}

Appearance parseAppearance(pugi::xml_node el, const ParseContext &context)
{
    Appearance out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseAppearanceContent(out, el, context);
    return out;
}

void parseAppearanceContent(Appearance &out, pugi::xml_node el)
{
    parseAppearanceContent(out, el, ParseContext{});
}

void parseAppearanceContent(Appearance &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursorIs(cursor, "line-width"))
    {
        out.addLineWidth(parseLineWidth(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "note-size"))
    {
        out.addNoteSize(parseNoteSize(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "distance"))
    {
        out.addDistance(parseDistance(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "glyph"))
    {
        out.addGlyph(parseGlyph(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "other-appearance"))
    {
        out.addOtherAppearance(parseOtherAppearance(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "line-width") || cursorIs(cursor, "note-size") || cursorIs(cursor, "distance") ||
            cursorIs(cursor, "glyph") || cursorIs(cursor, "other-appearance"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeAppearance(const Appearance &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeAppearanceContent(v, el);
}

void serializeAppearanceContent(const Appearance &v, pugi::xml_node el)
{
    for (const auto &item : v.lineWidth())
    {
        serializeLineWidth(item, el, "line-width");
    }
    for (const auto &item : v.noteSize())
    {
        serializeNoteSize(item, el, "note-size");
    }
    for (const auto &item : v.distance())
    {
        serializeDistance(item, el, "distance");
    }
    for (const auto &item : v.glyph())
    {
        serializeGlyph(item, el, "glyph");
    }
    for (const auto &item : v.otherAppearance())
    {
        serializeOtherAppearance(item, el, "other-appearance");
    }
}

} // namespace mx::core
