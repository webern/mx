// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The stem-value type represents the notated stem direction.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StemValue final
{
  public:
    enum class Tag
    {
        down,
        up,
        double_,
        none,
    };

    /// The natural zero: the first variant.
    StemValue() noexcept = default;

    static StemValue down() noexcept;
    static StemValue up() noexcept;
    static StemValue double_() noexcept;
    static StemValue none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StemValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StemValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StemValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StemValue &other) const noexcept = default;

  private:
    explicit StemValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::down};
};

} // namespace mx::core
