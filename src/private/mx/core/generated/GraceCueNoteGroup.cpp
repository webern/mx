// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/GraceCueNoteGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Empty &GraceCueNoteGroup::cue() const noexcept
{
    return m_cue;
}

void GraceCueNoteGroup::setCue(Empty value)
{
    m_cue = std::move(value);
}

const FullNoteGroup &GraceCueNoteGroup::fullNote() const noexcept
{
    return m_fullNote;
}

void GraceCueNoteGroup::setFullNote(FullNoteGroup value)
{
    m_fullNote = std::move(value);
}

GraceCueNoteGroup parseGraceCueNoteGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseGraceCueNoteGroup(el, cursor, ParseContext{});
}

GraceCueNoteGroup parseGraceCueNoteGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    GraceCueNoteGroup out;
    if (cursorIs(cursor, "cue"))
    {
        out.setCue(parseEmpty(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "cue");
    }
    if (cursor && (cursorIs(cursor, "chord") || cursorIs(cursor, "pitch") || cursorIs(cursor, "unpitched") ||
                   cursorIs(cursor, "rest")))
    {
        out.setFullNote(parseFullNoteGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "chord");
    }
    return out;
}

void serializeGraceCueNoteGroup(const GraceCueNoteGroup &v, pugi::xml_node el)
{
    serializeEmpty(v.cue(), el, "cue");
    serializeFullNoteGroup(v.fullNote(), el);
}

} // namespace mx::core
