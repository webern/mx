// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/SlashGroup.h"
#include "mx/core/generated/StartStop.h"
#include "mx/core/generated/YesNo.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The slash type is used to indicate that slash notation is to be used. If the slash is on every
/// beat, use-stems is no (the default). To indicate rhythms but not pitches, use-stems is set to
/// yes. The type attribute indicates whether this is the start or stop of a slash notation style.
/// The use-dots attribute works as for the beat-repeat element, and only has effect if use-stems is
/// no.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Slash final
{
  public:
    const StartStop &type() const noexcept;
    void setType(StartStop value);
    const std::optional<YesNo> &useDots() const noexcept;
    void setUseDots(std::optional<YesNo> value);
    const std::optional<YesNo> &useStems() const noexcept;
    void setUseStems(std::optional<YesNo> value);
    const std::optional<SlashGroup> &slash() const noexcept;
    void setSlash(std::optional<SlashGroup> value);
    bool operator==(const Slash &other) const = default;

  private:
    StartStop m_type{};
    std::optional<YesNo> m_useDots;
    std::optional<YesNo> m_useStems;
    std::optional<SlashGroup> m_slash;
};

Slash parseSlash(pugi::xml_node el);

Slash parseSlash(pugi::xml_node el, const ParseContext &context);

void parseSlashContent(Slash &out, pugi::xml_node el);

void parseSlashContent(Slash &out, pugi::xml_node el, const ParseContext &context);

void serializeSlash(const Slash &v, pugi::xml_node parent, const char *tag);

void serializeSlashContent(const Slash &v, pugi::xml_node el);

} // namespace mx::core
