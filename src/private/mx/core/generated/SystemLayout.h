// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/SystemDividers.h"
#include "mx/core/generated/SystemMargins.h"
#include "mx/core/generated/Tenths.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A system is a group of staves that are read and played simultaneously. System layout includes
/// left and right margins and the vertical distance from the previous system. The system distance is
/// measured from the bottom line of the previous system to the top line of the current system. It is
/// ignored for the first system on a page. The top system distance is measured from the page's top
/// margin to the top line of the first system. It is ignored for all but the first system on a page.
/// Sometimes the sum of measure widths in a system may not equal the system width specified by the
/// layout elements due to roundoff or other errors. The behavior when reading MusicXML files in
/// these cases is application-dependent. For instance, applications may find that the system layout
/// data is more reliable than the sum of the measure widths, and adjust the measure widths
/// accordingly. When used in the defaults element, the system-layout element defines a default
/// appearance for all systems in the score. If no system-layout element is present in the defaults
/// element, default system layout values are chosen by the application. When used in the print
/// element, the system-layout element affects the appearance of the current system only. All other
/// systems use the default values as determined by the defaults element. If any child elements are
/// missing from the system-layout element in a print element, the values determined by the defaults
/// element are used there as well. This type of system-layout element need only be read from or
/// written to the first visible part in the score.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class SystemLayout final
{
  public:
    const std::optional<SystemMargins> &systemMargins() const noexcept;
    void setSystemMargins(std::optional<SystemMargins> value);
    const std::optional<Tenths> &systemDistance() const noexcept;
    void setSystemDistance(std::optional<Tenths> value);
    const std::optional<Tenths> &topSystemDistance() const noexcept;
    void setTopSystemDistance(std::optional<Tenths> value);
    const std::optional<SystemDividers> &systemDividers() const noexcept;
    void setSystemDividers(std::optional<SystemDividers> value);
    bool operator==(const SystemLayout &other) const = default;

  private:
    std::optional<SystemMargins> m_systemMargins;
    std::optional<Tenths> m_systemDistance;
    std::optional<Tenths> m_topSystemDistance;
    std::optional<SystemDividers> m_systemDividers;
};

SystemLayout parseSystemLayout(pugi::xml_node el);

SystemLayout parseSystemLayout(pugi::xml_node el, const ParseContext &context);

void parseSystemLayoutContent(SystemLayout &out, pugi::xml_node el);

void parseSystemLayoutContent(SystemLayout &out, pugi::xml_node el, const ParseContext &context);

void serializeSystemLayout(const SystemLayout &v, pugi::xml_node parent, const char *tag);

void serializeSystemLayoutContent(const SystemLayout &v, pugi::xml_node el);

} // namespace mx::core
