// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Hole.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &Hole::defaultX() const noexcept
{
    return m_defaultX;
}

void Hole::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Hole::defaultY() const noexcept
{
    return m_defaultY;
}

void Hole::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Hole::relativeX() const noexcept
{
    return m_relativeX;
}

void Hole::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Hole::relativeY() const noexcept
{
    return m_relativeY;
}

void Hole::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Hole::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Hole::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Hole::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Hole::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Hole::fontSize() const noexcept
{
    return m_fontSize;
}

void Hole::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Hole::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Hole::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Hole::color() const noexcept
{
    return m_color;
}

void Hole::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<AboveBelow> &Hole::placement() const noexcept
{
    return m_placement;
}

void Hole::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<std::string> &Hole::holeType() const noexcept
{
    return m_holeType;
}

void Hole::setHoleType(std::optional<std::string> value)
{
    m_holeType = std::move(value);
}

const HoleClosed &Hole::holeClosed() const noexcept
{
    return m_holeClosed;
}

void Hole::setHoleClosed(HoleClosed value)
{
    m_holeClosed = std::move(value);
}

const std::optional<std::string> &Hole::holeShape() const noexcept
{
    return m_holeShape;
}

void Hole::setHoleShape(std::optional<std::string> value)
{
    m_holeShape = std::move(value);
}

Hole parseHole(pugi::xml_node el)
{
    return parseHole(el, ParseContext{});
}

Hole parseHole(pugi::xml_node el, const ParseContext &context)
{
    Hole out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHoleContent(out, el, context);
    return out;
}

void parseHoleContent(Hole &out, pugi::xml_node el)
{
    parseHoleContent(out, el, ParseContext{});
}

void parseHoleContent(Hole &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "hole-type"))
    {
        out.setHoleType(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "hole-closed"))
    {
        out.setHoleClosed(parseHoleClosed(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "hole-closed");
    }
    if (cursorIs(cursor, "hole-shape"))
    {
        out.setHoleShape(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "hole-type") || cursorIs(cursor, "hole-closed") || cursorIs(cursor, "hole-shape"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeHole(const Hole &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    serializeHoleContent(v, el);
}

void serializeHoleContent(const Hole &v, pugi::xml_node el)
{
    if (v.holeType())
    {
        writeTextElement(el, "hole-type", *v.holeType());
    }
    serializeHoleClosed(v.holeClosed(), el, "hole-closed");
    if (v.holeShape())
    {
        writeTextElement(el, "hole-shape", *v.holeShape());
    }
}

} // namespace mx::core
