// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The line-end type specifies if there is a jog up or down (or both), an arrow, or nothing at the
/// start or end of a bracket.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class LineEnd final
{
  public:
    enum class Tag
    {
        up,
        down,
        both,
        arrow,
        none,
    };

    /// The natural zero: the first variant.
    LineEnd() noexcept = default;

    static LineEnd up() noexcept;
    static LineEnd down() noexcept;
    static LineEnd both() noexcept;
    static LineEnd arrow() noexcept;
    static LineEnd none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, LineEnd &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static LineEnd parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static LineEnd parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const LineEnd &other) const noexcept = default;

  private:
    explicit LineEnd(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::up};
};

} // namespace mx::core
