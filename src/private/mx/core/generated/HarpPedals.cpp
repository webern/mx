// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/HarpPedals.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &HarpPedals::defaultX() const noexcept
{
    return m_defaultX;
}

void HarpPedals::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &HarpPedals::defaultY() const noexcept
{
    return m_defaultY;
}

void HarpPedals::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &HarpPedals::relativeX() const noexcept
{
    return m_relativeX;
}

void HarpPedals::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &HarpPedals::relativeY() const noexcept
{
    return m_relativeY;
}

void HarpPedals::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &HarpPedals::fontFamily() const noexcept
{
    return m_fontFamily;
}

void HarpPedals::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &HarpPedals::fontStyle() const noexcept
{
    return m_fontStyle;
}

void HarpPedals::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &HarpPedals::fontSize() const noexcept
{
    return m_fontSize;
}

void HarpPedals::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &HarpPedals::fontWeight() const noexcept
{
    return m_fontWeight;
}

void HarpPedals::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &HarpPedals::color() const noexcept
{
    return m_color;
}

void HarpPedals::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &HarpPedals::halign() const noexcept
{
    return m_halign;
}

void HarpPedals::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &HarpPedals::valign() const noexcept
{
    return m_valign;
}

void HarpPedals::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const std::optional<Token> &HarpPedals::id() const noexcept
{
    return m_id;
}

void HarpPedals::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const PedalTuning> HarpPedals::pedalTuning() const noexcept
{
    return m_pedalTuning.items();
}

void HarpPedals::addPedalTuning(PedalTuning value)
{
    m_pedalTuning.add(std::move(value));
}

void HarpPedals::setPedalTuning(OneOrMore<PedalTuning> value)
{
    m_pedalTuning = std::move(value);
}

HarpPedals parseHarpPedals(pugi::xml_node el, const ParseContext &context)
{
    HarpPedals out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "halign")
        {
            out.setHalign(parseValue<LeftCenterRight>(a.value(), context, el, "halign"));
        }
        else if (aname == "valign")
        {
            out.setValign(parseValue<Valign>(a.value(), context, el, "valign"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHarpPedalsContent(out, el, context);
    return out;
}

void parseHarpPedalsContent(HarpPedals &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!cursorIs(cursor, "pedal-tuning"))
    {
        throwMissingOrMisplaced(el, cursor, "pedal-tuning");
    }
    out.setPedalTuning(OneOrMore<PedalTuning>{parsePedalTuning(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "pedal-tuning"))
    {
        out.addPedalTuning(parsePedalTuning(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "pedal-tuning"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeHarpPedals(const HarpPedals &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeHarpPedalsContent(v, el);
}

void serializeHarpPedalsContent(const HarpPedals &v, pugi::xml_node el)
{
    for (const auto &item : v.pedalTuning())
    {
        serializePedalTuning(item, el, "pedal-tuning");
    }
}

} // namespace mx::core
