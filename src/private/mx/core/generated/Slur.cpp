// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Slur.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStopContinue &Slur::type() const noexcept
{
    return m_type;
}

void Slur::setType(StartStopContinue value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &Slur::number() const noexcept
{
    return m_number;
}

void Slur::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<LineType> &Slur::lineType() const noexcept
{
    return m_lineType;
}

void Slur::setLineType(std::optional<LineType> value)
{
    m_lineType = std::move(value);
}

const std::optional<Tenths> &Slur::dashLength() const noexcept
{
    return m_dashLength;
}

void Slur::setDashLength(std::optional<Tenths> value)
{
    m_dashLength = std::move(value);
}

const std::optional<Tenths> &Slur::spaceLength() const noexcept
{
    return m_spaceLength;
}

void Slur::setSpaceLength(std::optional<Tenths> value)
{
    m_spaceLength = std::move(value);
}

const std::optional<Tenths> &Slur::defaultX() const noexcept
{
    return m_defaultX;
}

void Slur::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Slur::defaultY() const noexcept
{
    return m_defaultY;
}

void Slur::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Slur::relativeX() const noexcept
{
    return m_relativeX;
}

void Slur::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Slur::relativeY() const noexcept
{
    return m_relativeY;
}

void Slur::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<AboveBelow> &Slur::placement() const noexcept
{
    return m_placement;
}

void Slur::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<OverUnder> &Slur::orientation() const noexcept
{
    return m_orientation;
}

void Slur::setOrientation(std::optional<OverUnder> value)
{
    m_orientation = std::move(value);
}

const std::optional<Tenths> &Slur::bezierX() const noexcept
{
    return m_bezierX;
}

void Slur::setBezierX(std::optional<Tenths> value)
{
    m_bezierX = std::move(value);
}

const std::optional<Tenths> &Slur::bezierY() const noexcept
{
    return m_bezierY;
}

void Slur::setBezierY(std::optional<Tenths> value)
{
    m_bezierY = std::move(value);
}

const std::optional<Tenths> &Slur::bezierX2() const noexcept
{
    return m_bezierX2;
}

void Slur::setBezierX2(std::optional<Tenths> value)
{
    m_bezierX2 = std::move(value);
}

const std::optional<Tenths> &Slur::bezierY2() const noexcept
{
    return m_bezierY2;
}

void Slur::setBezierY2(std::optional<Tenths> value)
{
    m_bezierY2 = std::move(value);
}

const std::optional<Divisions> &Slur::bezierOffset() const noexcept
{
    return m_bezierOffset;
}

void Slur::setBezierOffset(std::optional<Divisions> value)
{
    m_bezierOffset = std::move(value);
}

const std::optional<Divisions> &Slur::bezierOffset2() const noexcept
{
    return m_bezierOffset2;
}

void Slur::setBezierOffset2(std::optional<Divisions> value)
{
    m_bezierOffset2 = std::move(value);
}

const std::optional<Color> &Slur::color() const noexcept
{
    return m_color;
}

void Slur::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &Slur::id() const noexcept
{
    return m_id;
}

void Slur::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

Slur parseSlur(pugi::xml_node el, const ParseContext &context)
{
    Slur out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStopContinue>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(parseValue<NumberLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "line-type")
        {
            out.setLineType(parseValue<LineType>(a.value(), context, el, "line-type"));
        }
        else if (aname == "dash-length")
        {
            out.setDashLength(parseValue<Tenths>(a.value(), context, el, "dash-length"));
        }
        else if (aname == "space-length")
        {
            out.setSpaceLength(parseValue<Tenths>(a.value(), context, el, "space-length"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "orientation")
        {
            out.setOrientation(parseValue<OverUnder>(a.value(), context, el, "orientation"));
        }
        else if (aname == "bezier-x")
        {
            out.setBezierX(parseValue<Tenths>(a.value(), context, el, "bezier-x"));
        }
        else if (aname == "bezier-y")
        {
            out.setBezierY(parseValue<Tenths>(a.value(), context, el, "bezier-y"));
        }
        else if (aname == "bezier-x2")
        {
            out.setBezierX2(parseValue<Tenths>(a.value(), context, el, "bezier-x2"));
        }
        else if (aname == "bezier-y2")
        {
            out.setBezierY2(parseValue<Tenths>(a.value(), context, el, "bezier-y2"));
        }
        else if (aname == "bezier-offset")
        {
            out.setBezierOffset(parseValue<Divisions>(a.value(), context, el, "bezier-offset"));
        }
        else if (aname == "bezier-offset2")
        {
            out.setBezierOffset2(parseValue<Divisions>(a.value(), context, el, "bezier-offset2"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseSlurContent(out, el, context);
    return out;
}

void parseSlurContent(Slur &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeSlur(const Slur &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.lineType())
    {
        setAttribute(el, "line-type", v.lineType()->toString());
    }
    if (v.dashLength())
    {
        setAttribute(el, "dash-length", v.dashLength()->toString());
    }
    if (v.spaceLength())
    {
        setAttribute(el, "space-length", v.spaceLength()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.orientation())
    {
        setAttribute(el, "orientation", v.orientation()->toString());
    }
    if (v.bezierX())
    {
        setAttribute(el, "bezier-x", v.bezierX()->toString());
    }
    if (v.bezierY())
    {
        setAttribute(el, "bezier-y", v.bezierY()->toString());
    }
    if (v.bezierX2())
    {
        setAttribute(el, "bezier-x2", v.bezierX2()->toString());
    }
    if (v.bezierY2())
    {
        setAttribute(el, "bezier-y2", v.bezierY2()->toString());
    }
    if (v.bezierOffset())
    {
        setAttribute(el, "bezier-offset", v.bezierOffset()->toString());
    }
    if (v.bezierOffset2())
    {
        setAttribute(el, "bezier-offset2", v.bezierOffset2()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeSlurContent(v, el);
}

void serializeSlurContent(const Slur &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
