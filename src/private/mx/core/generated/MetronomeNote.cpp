// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MetronomeNote.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const NoteTypeValue &MetronomeNote::metronomeType() const noexcept
{
    return m_metronomeType;
}

void MetronomeNote::setMetronomeType(NoteTypeValue value)
{
    m_metronomeType = std::move(value);
}

std::span<const Empty> MetronomeNote::metronomeDot() const noexcept
{
    return m_metronomeDot;
}

void MetronomeNote::addMetronomeDot(Empty value)
{
    m_metronomeDot.push_back(std::move(value));
}

void MetronomeNote::setMetronomeDot(std::vector<Empty> value)
{
    m_metronomeDot = std::move(value);
}

std::span<const MetronomeBeam> MetronomeNote::metronomeBeam() const noexcept
{
    return m_metronomeBeam;
}

void MetronomeNote::addMetronomeBeam(MetronomeBeam value)
{
    m_metronomeBeam.push_back(std::move(value));
}

void MetronomeNote::setMetronomeBeam(std::vector<MetronomeBeam> value)
{
    m_metronomeBeam = std::move(value);
}

const std::optional<MetronomeTied> &MetronomeNote::metronomeTied() const noexcept
{
    return m_metronomeTied;
}

void MetronomeNote::setMetronomeTied(std::optional<MetronomeTied> value)
{
    m_metronomeTied = std::move(value);
}

const std::optional<MetronomeTuplet> &MetronomeNote::metronomeTuplet() const noexcept
{
    return m_metronomeTuplet;
}

void MetronomeNote::setMetronomeTuplet(std::optional<MetronomeTuplet> value)
{
    m_metronomeTuplet = std::move(value);
}

MetronomeNote parseMetronomeNote(pugi::xml_node el, const ParseContext &context)
{
    MetronomeNote out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseMetronomeNoteContent(out, el, context);
    return out;
}

void parseMetronomeNoteContent(MetronomeNote &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "metronome-type"))
    {
        out.setMetronomeType(parseValue<NoteTypeValue>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "metronome-type");
    }
    while (cursorIs(cursor, "metronome-dot"))
    {
        out.addMetronomeDot(parseEmpty(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "metronome-beam"))
    {
        out.addMetronomeBeam(parseMetronomeBeam(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "metronome-tied"))
    {
        out.setMetronomeTied(parseMetronomeTied(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "metronome-tuplet"))
    {
        out.setMetronomeTuplet(parseMetronomeTuplet(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "metronome-type") || cursorIs(cursor, "metronome-dot") ||
            cursorIs(cursor, "metronome-beam") || cursorIs(cursor, "metronome-tied") ||
            cursorIs(cursor, "metronome-tuplet"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeMetronomeNote(const MetronomeNote &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeMetronomeNoteContent(v, el);
}

void serializeMetronomeNoteContent(const MetronomeNote &v, pugi::xml_node el)
{
    writeTextElement(el, "metronome-type", v.metronomeType().toString());
    for (const auto &item : v.metronomeDot())
    {
        serializeEmpty(item, el, "metronome-dot");
    }
    for (const auto &item : v.metronomeBeam())
    {
        serializeMetronomeBeam(item, el, "metronome-beam");
    }
    if (v.metronomeTied())
    {
        serializeMetronomeTied(*v.metronomeTied(), el, "metronome-tied");
    }
    if (v.metronomeTuplet())
    {
        serializeMetronomeTuplet(*v.metronomeTuplet(), el, "metronome-tuplet");
    }
}

} // namespace mx::core
