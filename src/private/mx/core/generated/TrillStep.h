// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The trill-step type describes the alternating note of trills and mordents for playback, relative
/// to the current note.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TrillStep final
{
  public:
    enum class Tag
    {
        whole,
        half,
        unison,
    };

    /// The natural zero: the first variant.
    TrillStep() noexcept = default;

    static TrillStep whole() noexcept;
    static TrillStep half() noexcept;
    static TrillStep unison() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TrillStep &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TrillStep parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TrillStep parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TrillStep &other) const noexcept = default;

  private:
    explicit TrillStep(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::whole};
};

} // namespace mx::core
