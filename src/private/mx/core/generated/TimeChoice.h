// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/TimeChoiceGroup.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class TimeChoice final
{
  public:
    enum class Kind
    {
        group,
        senzaMisura,
    };

    /// The natural zero: the first alternative.
    TimeChoice();

    static TimeChoice group(TimeChoiceGroup value);
    static TimeChoice senzaMisura(std::string value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isGroup().
    const TimeChoiceGroup &asGroup() const noexcept
    {
        assert(isGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isSenzaMisura() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isSenzaMisura().
    const std::string &asSenzaMisura() const noexcept
    {
        assert(isSenzaMisura());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const TimeChoice &other) const = default;

  private:
    using Storage = std::variant<TimeChoiceGroup, std::string>;

    explicit TimeChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
TimeChoice parseTimeChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeTimeChoice(const TimeChoice &v, pugi::xml_node el);

} // namespace mx::core
