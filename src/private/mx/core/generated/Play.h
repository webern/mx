// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/NameToken.h"
#include "mx/core/Token.h"
#include "mx/core/generated/PlayChoice.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The play type specifies playback techniques to be used in conjunction with the instrument-sound
/// element. When used as part of a sound element, it applies to all notes going forward in score
/// order. In multi-instrument parts, the affected instrument should be specified using the id
/// attribute. When used as part of a note element, it applies to the current note only.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Play final
{
  public:
    const std::optional<Token> &id() const noexcept;
    void setID(std::optional<Token> value);
    std::span<const PlayChoice> choice() const noexcept;
    void addChoice(PlayChoice value);
    void setChoice(std::vector<PlayChoice> value);
    bool operator==(const Play &other) const = default;

  private:
    std::optional<Token> m_id;
    std::vector<PlayChoice> m_choice;
};

Play parsePlay(pugi::xml_node el);

Play parsePlay(pugi::xml_node el, const ParseContext &context);

void parsePlayContent(Play &out, pugi::xml_node el);

void parsePlayContent(Play &out, pugi::xml_node el, const ParseContext &context);

void serializePlay(const Play &v, pugi::xml_node parent, const char *tag);

void serializePlayContent(const Play &v, pugi::xml_node el);

} // namespace mx::core
