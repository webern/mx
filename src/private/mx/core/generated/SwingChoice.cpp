// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/SwingChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

SwingChoice::SwingChoice()
{
    *this = SwingChoice::straight(Empty{});
}

SwingChoice SwingChoice::straight(Empty value)
{
    return SwingChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

SwingChoice SwingChoice::group(SwingChoiceGroup value)
{
    return SwingChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

SwingChoice parseSwingChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "straight")))
    {
        Empty value = parseEmpty(cursor, context);
        cursor = nextElement(cursor);
        return SwingChoice::straight(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "first")))
    {
        return SwingChoice::group(parseSwingChoiceGroup(el, cursor, context));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "straight");
}

void serializeSwingChoice(const SwingChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case SwingChoice::Kind::straight:
        serializeEmpty(v.asStraight(), el, "straight");
        break;
    case SwingChoice::Kind::group:
        serializeSwingChoiceGroup(v.asGroup(), el);
        break;
    }
}

} // namespace mx::core
