// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/NumeralKey.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &NumeralKey::printObject() const noexcept
{
    return m_printObject;
}

void NumeralKey::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const Fifths &NumeralKey::numeralFifths() const noexcept
{
    return m_numeralFifths;
}

void NumeralKey::setNumeralFifths(Fifths value)
{
    m_numeralFifths = std::move(value);
}

const NumeralMode &NumeralKey::numeralMode() const noexcept
{
    return m_numeralMode;
}

void NumeralKey::setNumeralMode(NumeralMode value)
{
    m_numeralMode = std::move(value);
}

NumeralKey parseNumeralKey(pugi::xml_node el, const ParseContext &context)
{
    NumeralKey out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseNumeralKeyContent(out, el, context);
    return out;
}

void parseNumeralKeyContent(NumeralKey &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "numeral-fifths"))
    {
        out.setNumeralFifths(parseValue<Fifths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "numeral-fifths");
    }
    if (cursorIs(cursor, "numeral-mode"))
    {
        out.setNumeralMode(parseValue<NumeralMode>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "numeral-mode");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "numeral-fifths") || cursorIs(cursor, "numeral-mode"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeNumeralKey(const NumeralKey &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    serializeNumeralKeyContent(v, el);
}

void serializeNumeralKeyContent(const NumeralKey &v, pugi::xml_node el)
{
    writeTextElement(el, "numeral-fifths", v.numeralFifths().toString());
    writeTextElement(el, "numeral-mode", v.numeralMode().toString());
}

} // namespace mx::core
