// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ScoreHeaderGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Work> &ScoreHeaderGroup::work() const noexcept
{
    return m_work;
}

void ScoreHeaderGroup::setWork(std::optional<Work> value)
{
    m_work = std::move(value);
}

const std::optional<std::string> &ScoreHeaderGroup::movementNumber() const noexcept
{
    return m_movementNumber;
}

void ScoreHeaderGroup::setMovementNumber(std::optional<std::string> value)
{
    m_movementNumber = std::move(value);
}

const std::optional<std::string> &ScoreHeaderGroup::movementTitle() const noexcept
{
    return m_movementTitle;
}

void ScoreHeaderGroup::setMovementTitle(std::optional<std::string> value)
{
    m_movementTitle = std::move(value);
}

const std::optional<Identification> &ScoreHeaderGroup::identification() const noexcept
{
    return m_identification;
}

void ScoreHeaderGroup::setIdentification(std::optional<Identification> value)
{
    m_identification = std::move(value);
}

const std::optional<Defaults> &ScoreHeaderGroup::defaults() const noexcept
{
    return m_defaults;
}

void ScoreHeaderGroup::setDefaults(std::optional<Defaults> value)
{
    m_defaults = std::move(value);
}

std::span<const Credit> ScoreHeaderGroup::credit() const noexcept
{
    return m_credit;
}

void ScoreHeaderGroup::addCredit(Credit value)
{
    m_credit.push_back(std::move(value));
}

void ScoreHeaderGroup::setCredit(std::vector<Credit> value)
{
    m_credit = std::move(value);
}

const PartList &ScoreHeaderGroup::partList() const noexcept
{
    return m_partList;
}

void ScoreHeaderGroup::setPartList(PartList value)
{
    m_partList = std::move(value);
}

ScoreHeaderGroup parseScoreHeaderGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    ScoreHeaderGroup out;
    if (cursorIs(cursor, "work"))
    {
        out.setWork(parseWork(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "movement-number"))
    {
        out.setMovementNumber(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "movement-title"))
    {
        out.setMovementTitle(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "identification"))
    {
        out.setIdentification(parseIdentification(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "defaults"))
    {
        out.setDefaults(parseDefaults(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "credit"))
    {
        out.addCredit(parseCredit(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-list"))
    {
        out.setPartList(parsePartList(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "part-list");
    }
    return out;
}

void serializeScoreHeaderGroup(const ScoreHeaderGroup &v, pugi::xml_node el)
{
    if (v.work())
    {
        serializeWork(*v.work(), el, "work");
    }
    if (v.movementNumber())
    {
        writeTextElement(el, "movement-number", *v.movementNumber());
    }
    if (v.movementTitle())
    {
        writeTextElement(el, "movement-title", *v.movementTitle());
    }
    if (v.identification())
    {
        serializeIdentification(*v.identification(), el, "identification");
    }
    if (v.defaults())
    {
        serializeDefaults(*v.defaults(), el, "defaults");
    }
    for (const auto &item : v.credit())
    {
        serializeCredit(item, el, "credit");
    }
    serializePartList(v.partList(), el, "part-list");
}

} // namespace mx::core
