// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Barre.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &Barre::type() const noexcept
{
    return m_type;
}

void Barre::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<Color> &Barre::color() const noexcept
{
    return m_color;
}

void Barre::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

Barre parseBarre(pugi::xml_node el, const ParseContext &context)
{
    Barre out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStop>(a.value(), context, el, "type"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseBarreContent(out, el, context);
    return out;
}

void parseBarreContent(Barre &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeBarre(const Barre &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeBarreContent(v, el);
}

void serializeBarreContent(const Barre &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
