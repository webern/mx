// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/HarmonyChordGroupChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

HarmonyChordGroupChoice::HarmonyChordGroupChoice()
{
    *this = HarmonyChordGroupChoice::root(Root{});
}

HarmonyChordGroupChoice HarmonyChordGroupChoice::root(Root value)
{
    return HarmonyChordGroupChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

HarmonyChordGroupChoice HarmonyChordGroupChoice::numeral(Numeral value)
{
    return HarmonyChordGroupChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

HarmonyChordGroupChoice HarmonyChordGroupChoice::function(StyleText value)
{
    return HarmonyChordGroupChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

HarmonyChordGroupChoice parseHarmonyChordGroupChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseHarmonyChordGroupChoice(el, cursor, ParseContext{});
}

HarmonyChordGroupChoice parseHarmonyChordGroupChoice(pugi::xml_node el, pugi::xml_node &cursor,
                                                     const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "root")))
    {
        Root value = parseRoot(cursor, context);
        cursor = nextElement(cursor);
        return HarmonyChordGroupChoice::root(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "numeral")))
    {
        Numeral value = parseNumeral(cursor, context);
        cursor = nextElement(cursor);
        return HarmonyChordGroupChoice::numeral(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "function")))
    {
        StyleText value = parseStyleText(cursor, context);
        cursor = nextElement(cursor);
        return HarmonyChordGroupChoice::function(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "root");
}

void serializeHarmonyChordGroupChoice(const HarmonyChordGroupChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case HarmonyChordGroupChoice::Kind::root:
        serializeRoot(v.asRoot(), el, "root");
        break;
    case HarmonyChordGroupChoice::Kind::numeral:
        serializeNumeral(v.asNumeral(), el, "numeral");
        break;
    case HarmonyChordGroupChoice::Kind::function:
        serializeStyleText(v.asFunction(), el, "function");
        break;
    }
}

} // namespace mx::core
