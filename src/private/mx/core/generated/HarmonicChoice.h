// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class HarmonicChoice final
{
  public:
    enum class Kind
    {
        natural,
        artificial,
    };

    /// The natural zero: the first alternative.
    HarmonicChoice();

    static HarmonicChoice natural(Empty value);
    static HarmonicChoice artificial(Empty value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isNatural() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isNatural().
    const Empty &asNatural() const noexcept
    {
        assert(isNatural());
        return *std::get_if<0>(&m_value);
    }

    bool isArtificial() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isArtificial().
    const Empty &asArtificial() const noexcept
    {
        assert(isArtificial());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const HarmonicChoice &other) const = default;

  private:
    using Storage = std::variant<Empty, Empty>;

    explicit HarmonicChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
HarmonicChoice parseHarmonicChoice(pugi::xml_node el, pugi::xml_node &cursor);

HarmonicChoice parseHarmonicChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeHarmonicChoice(const HarmonicChoice &v, pugi::xml_node el);

} // namespace mx::core
