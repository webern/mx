// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The step type represents a step of the diatonic scale, represented using the English letters A
/// through G.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class Step final
{
  public:
    enum class Tag
    {
        a,
        b,
        c,
        d,
        e,
        f,
        g,
    };

    /// The natural zero: the first variant.
    Step() noexcept = default;

    static Step a() noexcept;
    static Step b() noexcept;
    static Step c() noexcept;
    static Step d() noexcept;
    static Step e() noexcept;
    static Step f() noexcept;
    static Step g() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, Step &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static Step parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static Step parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Step &other) const noexcept = default;

  private:
    explicit Step(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::a};
};

} // namespace mx::core
