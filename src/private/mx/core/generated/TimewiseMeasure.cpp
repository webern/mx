// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TimewiseMeasure.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::string &TimewiseMeasure::number() const noexcept
{
    return m_number;
}

void TimewiseMeasure::setNumber(std::string value)
{
    m_number = std::move(value);
}

const std::optional<MeasureText> &TimewiseMeasure::text() const noexcept
{
    return m_text;
}

void TimewiseMeasure::setText(std::optional<MeasureText> value)
{
    m_text = std::move(value);
}

const std::optional<YesNo> &TimewiseMeasure::implicit() const noexcept
{
    return m_implicit;
}

void TimewiseMeasure::setImplicit(std::optional<YesNo> value)
{
    m_implicit = std::move(value);
}

const std::optional<YesNo> &TimewiseMeasure::nonControlling() const noexcept
{
    return m_nonControlling;
}

void TimewiseMeasure::setNonControlling(std::optional<YesNo> value)
{
    m_nonControlling = std::move(value);
}

const std::optional<Tenths> &TimewiseMeasure::width() const noexcept
{
    return m_width;
}

void TimewiseMeasure::setWidth(std::optional<Tenths> value)
{
    m_width = std::move(value);
}

const std::optional<Token> &TimewiseMeasure::id() const noexcept
{
    return m_id;
}

void TimewiseMeasure::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const TimewisePart> TimewiseMeasure::part() const noexcept
{
    return m_part.items();
}

void TimewiseMeasure::addPart(TimewisePart value)
{
    m_part.add(std::move(value));
}

void TimewiseMeasure::setPart(OneOrMore<TimewisePart> value)
{
    m_part = std::move(value);
}

TimewiseMeasure parseTimewiseMeasure(pugi::xml_node el)
{
    return parseTimewiseMeasure(el, ParseContext{});
}

TimewiseMeasure parseTimewiseMeasure(pugi::xml_node el, const ParseContext &context)
{
    TimewiseMeasure out;
    bool seen_number = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            seen_number = true;
            out.setNumber(std::string{a.value()});
        }
        else if (aname == "text")
        {
            out.setText(parseValue<MeasureText>(a.value(), context, el, "text"));
        }
        else if (aname == "implicit")
        {
            out.setImplicit(parseValue<YesNo>(a.value(), context, el, "implicit"));
        }
        else if (aname == "non-controlling")
        {
            out.setNonControlling(parseValue<YesNo>(a.value(), context, el, "non-controlling"));
        }
        else if (aname == "width")
        {
            out.setWidth(parseValue<Tenths>(a.value(), context, el, "width"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_number)
    {
        throwMissingAttribute(el, "number");
    }
    parseTimewiseMeasureContent(out, el, context);
    return out;
}

void parseTimewiseMeasureContent(TimewiseMeasure &out, pugi::xml_node el)
{
    parseTimewiseMeasureContent(out, el, ParseContext{});
}

void parseTimewiseMeasureContent(TimewiseMeasure &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!cursorIs(cursor, "part"))
    {
        throwMissingOrMisplaced(el, cursor, "part");
    }
    out.setPart(OneOrMore<TimewisePart>{parseTimewisePart(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "part"))
    {
        out.addPart(parseTimewisePart(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "part"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTimewiseMeasure(const TimewiseMeasure &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "number", v.number());
    if (v.text())
    {
        setAttribute(el, "text", v.text()->toString());
    }
    if (v.implicit())
    {
        setAttribute(el, "implicit", v.implicit()->toString());
    }
    if (v.nonControlling())
    {
        setAttribute(el, "non-controlling", v.nonControlling()->toString());
    }
    if (v.width())
    {
        setAttribute(el, "width", v.width()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeTimewiseMeasureContent(v, el);
}

void serializeTimewiseMeasureContent(const TimewiseMeasure &v, pugi::xml_node el)
{
    for (const auto &item : v.part())
    {
        serializeTimewisePart(item, el, "part");
    }
}

} // namespace mx::core
