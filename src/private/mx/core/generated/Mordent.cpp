// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Mordent.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Mordent::long_() const noexcept
{
    return m_long_;
}

void Mordent::setLong(std::optional<YesNo> value)
{
    m_long_ = std::move(value);
}

const std::optional<AboveBelow> &Mordent::approach() const noexcept
{
    return m_approach;
}

void Mordent::setApproach(std::optional<AboveBelow> value)
{
    m_approach = std::move(value);
}

const std::optional<AboveBelow> &Mordent::departure() const noexcept
{
    return m_departure;
}

void Mordent::setDeparture(std::optional<AboveBelow> value)
{
    m_departure = std::move(value);
}

Mordent parseMordent(pugi::xml_node el, const ParseContext &context)
{
    Mordent out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "start-note")
        {
            out.setStartNote(parseValue<StartNote>(a.value(), context, el, "start-note"));
        }
        else if (aname == "trill-step")
        {
            out.setTrillStep(parseValue<TrillStep>(a.value(), context, el, "trill-step"));
        }
        else if (aname == "two-note-turn")
        {
            out.setTwoNoteTurn(parseValue<TwoNoteTurn>(a.value(), context, el, "two-note-turn"));
        }
        else if (aname == "accelerate")
        {
            out.setAccelerate(parseValue<YesNo>(a.value(), context, el, "accelerate"));
        }
        else if (aname == "beats")
        {
            out.setBeats(parseValue<TrillBeats>(a.value(), context, el, "beats"));
        }
        else if (aname == "second-beat")
        {
            out.setSecondBeat(parseValue<Percent>(a.value(), context, el, "second-beat"));
        }
        else if (aname == "last-beat")
        {
            out.setLastBeat(parseValue<Percent>(a.value(), context, el, "last-beat"));
        }
        else if (aname == "long")
        {
            out.setLong(parseValue<YesNo>(a.value(), context, el, "long"));
        }
        else if (aname == "approach")
        {
            out.setApproach(parseValue<AboveBelow>(a.value(), context, el, "approach"));
        }
        else if (aname == "departure")
        {
            out.setDeparture(parseValue<AboveBelow>(a.value(), context, el, "departure"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseEmptyTrillSoundContent(out, el, context);
    return out;
}

void serializeMordent(const Mordent &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.startNote())
    {
        setAttribute(el, "start-note", v.startNote()->toString());
    }
    if (v.trillStep())
    {
        setAttribute(el, "trill-step", v.trillStep()->toString());
    }
    if (v.twoNoteTurn())
    {
        setAttribute(el, "two-note-turn", v.twoNoteTurn()->toString());
    }
    if (v.accelerate())
    {
        setAttribute(el, "accelerate", v.accelerate()->toString());
    }
    if (v.beats())
    {
        setAttribute(el, "beats", v.beats()->toString());
    }
    if (v.secondBeat())
    {
        setAttribute(el, "second-beat", v.secondBeat()->toString());
    }
    if (v.lastBeat())
    {
        setAttribute(el, "last-beat", v.lastBeat()->toString());
    }
    if (v.long_())
    {
        setAttribute(el, "long", v.long_()->toString());
    }
    if (v.approach())
    {
        setAttribute(el, "approach", v.approach()->toString());
    }
    if (v.departure())
    {
        setAttribute(el, "departure", v.departure()->toString());
    }
    serializeEmptyTrillSoundContent(v, el);
}

} // namespace mx::core
