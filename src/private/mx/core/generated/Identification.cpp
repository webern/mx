// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Identification.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const TypedText> Identification::creator() const noexcept
{
    return m_creator;
}

void Identification::addCreator(TypedText value)
{
    m_creator.push_back(std::move(value));
}

void Identification::setCreator(std::vector<TypedText> value)
{
    m_creator = std::move(value);
}

std::span<const TypedText> Identification::rights() const noexcept
{
    return m_rights;
}

void Identification::addRights(TypedText value)
{
    m_rights.push_back(std::move(value));
}

void Identification::setRights(std::vector<TypedText> value)
{
    m_rights = std::move(value);
}

const std::optional<Encoding> &Identification::encoding() const noexcept
{
    return m_encoding;
}

void Identification::setEncoding(std::optional<Encoding> value)
{
    m_encoding = std::move(value);
}

const std::optional<std::string> &Identification::source() const noexcept
{
    return m_source;
}

void Identification::setSource(std::optional<std::string> value)
{
    m_source = std::move(value);
}

std::span<const TypedText> Identification::relation() const noexcept
{
    return m_relation;
}

void Identification::addRelation(TypedText value)
{
    m_relation.push_back(std::move(value));
}

void Identification::setRelation(std::vector<TypedText> value)
{
    m_relation = std::move(value);
}

const std::optional<Miscellaneous> &Identification::miscellaneous() const noexcept
{
    return m_miscellaneous;
}

void Identification::setMiscellaneous(std::optional<Miscellaneous> value)
{
    m_miscellaneous = std::move(value);
}

Identification parseIdentification(pugi::xml_node el, const ParseContext &context)
{
    Identification out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseIdentificationContent(out, el, context);
    return out;
}

void parseIdentificationContent(Identification &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursorIs(cursor, "creator"))
    {
        out.addCreator(parseTypedText(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "rights"))
    {
        out.addRights(parseTypedText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "encoding"))
    {
        out.setEncoding(parseEncoding(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "source"))
    {
        out.setSource(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "relation"))
    {
        out.addRelation(parseTypedText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "miscellaneous"))
    {
        out.setMiscellaneous(parseMiscellaneous(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "creator") || cursorIs(cursor, "rights") || cursorIs(cursor, "encoding") ||
            cursorIs(cursor, "source") || cursorIs(cursor, "relation") || cursorIs(cursor, "miscellaneous"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeIdentification(const Identification &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeIdentificationContent(v, el);
}

void serializeIdentificationContent(const Identification &v, pugi::xml_node el)
{
    for (const auto &item : v.creator())
    {
        serializeTypedText(item, el, "creator");
    }
    for (const auto &item : v.rights())
    {
        serializeTypedText(item, el, "rights");
    }
    if (v.encoding())
    {
        serializeEncoding(*v.encoding(), el, "encoding");
    }
    if (v.source())
    {
        writeTextElement(el, "source", *v.source());
    }
    for (const auto &item : v.relation())
    {
        serializeTypedText(item, el, "relation");
    }
    if (v.miscellaneous())
    {
        serializeMiscellaneous(*v.miscellaneous(), el, "miscellaneous");
    }
}

} // namespace mx::core
