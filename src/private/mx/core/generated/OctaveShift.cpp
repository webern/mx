// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/OctaveShift.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const UpDownStopContinue &OctaveShift::type() const noexcept
{
    return m_type;
}

void OctaveShift::setType(UpDownStopContinue value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &OctaveShift::number() const noexcept
{
    return m_number;
}

void OctaveShift::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<int> &OctaveShift::size() const noexcept
{
    return m_size;
}

void OctaveShift::setSize(std::optional<int> value)
{
    m_size = std::move(value);
}

const std::optional<Tenths> &OctaveShift::dashLength() const noexcept
{
    return m_dashLength;
}

void OctaveShift::setDashLength(std::optional<Tenths> value)
{
    m_dashLength = std::move(value);
}

const std::optional<Tenths> &OctaveShift::spaceLength() const noexcept
{
    return m_spaceLength;
}

void OctaveShift::setSpaceLength(std::optional<Tenths> value)
{
    m_spaceLength = std::move(value);
}

const std::optional<Tenths> &OctaveShift::defaultX() const noexcept
{
    return m_defaultX;
}

void OctaveShift::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &OctaveShift::defaultY() const noexcept
{
    return m_defaultY;
}

void OctaveShift::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &OctaveShift::relativeX() const noexcept
{
    return m_relativeX;
}

void OctaveShift::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &OctaveShift::relativeY() const noexcept
{
    return m_relativeY;
}

void OctaveShift::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &OctaveShift::fontFamily() const noexcept
{
    return m_fontFamily;
}

void OctaveShift::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &OctaveShift::fontStyle() const noexcept
{
    return m_fontStyle;
}

void OctaveShift::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &OctaveShift::fontSize() const noexcept
{
    return m_fontSize;
}

void OctaveShift::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &OctaveShift::fontWeight() const noexcept
{
    return m_fontWeight;
}

void OctaveShift::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &OctaveShift::color() const noexcept
{
    return m_color;
}

void OctaveShift::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &OctaveShift::id() const noexcept
{
    return m_id;
}

void OctaveShift::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

OctaveShift parseOctaveShift(pugi::xml_node el)
{
    return parseOctaveShift(el, ParseContext{});
}

OctaveShift parseOctaveShift(pugi::xml_node el, const ParseContext &context)
{
    OctaveShift out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<UpDownStopContinue>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(parseValue<NumberLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "size")
        {
            out.setSize(parseIntegerValue(a.value(), context, el, "size"));
        }
        else if (aname == "dash-length")
        {
            out.setDashLength(parseValue<Tenths>(a.value(), context, el, "dash-length"));
        }
        else if (aname == "space-length")
        {
            out.setSpaceLength(parseValue<Tenths>(a.value(), context, el, "space-length"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseOctaveShiftContent(out, el, context);
    return out;
}

void parseOctaveShiftContent(OctaveShift &out, pugi::xml_node el)
{
    parseOctaveShiftContent(out, el, ParseContext{});
}

void parseOctaveShiftContent(OctaveShift &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeOctaveShift(const OctaveShift &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.size())
    {
        setAttribute(el, "size", formatInt(*v.size()));
    }
    if (v.dashLength())
    {
        setAttribute(el, "dash-length", v.dashLength()->toString());
    }
    if (v.spaceLength())
    {
        setAttribute(el, "space-length", v.spaceLength()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeOctaveShiftContent(v, el);
}

void serializeOctaveShiftContent(const OctaveShift &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
