// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/SystemDividers.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const EmptyPrintObjectStyleAlign &SystemDividers::leftDivider() const noexcept
{
    return m_leftDivider;
}

void SystemDividers::setLeftDivider(EmptyPrintObjectStyleAlign value)
{
    m_leftDivider = std::move(value);
}

const EmptyPrintObjectStyleAlign &SystemDividers::rightDivider() const noexcept
{
    return m_rightDivider;
}

void SystemDividers::setRightDivider(EmptyPrintObjectStyleAlign value)
{
    m_rightDivider = std::move(value);
}

SystemDividers parseSystemDividers(pugi::xml_node el, const ParseContext &context)
{
    SystemDividers out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseSystemDividersContent(out, el, context);
    return out;
}

void parseSystemDividersContent(SystemDividers &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "left-divider"))
    {
        out.setLeftDivider(parseEmptyPrintObjectStyleAlign(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "left-divider");
    }
    if (cursorIs(cursor, "right-divider"))
    {
        out.setRightDivider(parseEmptyPrintObjectStyleAlign(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "right-divider");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "left-divider") || cursorIs(cursor, "right-divider"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeSystemDividers(const SystemDividers &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeSystemDividersContent(v, el);
}

void serializeSystemDividersContent(const SystemDividers &v, pugi::xml_node el)
{
    serializeEmptyPrintObjectStyleAlign(v.leftDivider(), el, "left-divider");
    serializeEmptyPrintObjectStyleAlign(v.rightDivider(), el, "right-divider");
}

} // namespace mx::core
