// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The line-type type distinguishes between solid, dashed, dotted, and wavy lines.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class LineType final
{
  public:
    enum class Tag
    {
        solid,
        dashed,
        dotted,
        wavy,
    };

    /// The natural zero: the first variant.
    LineType() noexcept = default;

    static LineType solid() noexcept;
    static LineType dashed() noexcept;
    static LineType dotted() noexcept;
    static LineType wavy() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, LineType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static LineType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static LineType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const LineType &other) const noexcept = default;

  private:
    explicit LineType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::solid};
};

} // namespace mx::core
