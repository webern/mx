// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The left-right type is used to indicate whether one element appears to the left or the right of
/// another element.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class LeftRight final
{
  public:
    enum class Tag
    {
        left,
        right,
    };

    /// The natural zero: the first variant.
    LeftRight() noexcept = default;

    static LeftRight left() noexcept;
    static LeftRight right() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, LeftRight &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static LeftRight parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static LeftRight parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const LeftRight &other) const noexcept = default;

  private:
    explicit LeftRight(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::left};
};

} // namespace mx::core
