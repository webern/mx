// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The clef-sign type represents the different clef symbols. The jianpu sign indicates that the
/// music that follows should be in jianpu numbered notation, just as the TAB sign indicates that the
/// music that follows should be in tablature notation. Unlike TAB, a jianpu sign does not correspond
/// to a visual clef notation. The none sign is deprecated as of MusicXML 4.0. Use the clef element's
/// print-object attribute instead. When the none sign is used, notes should be displayed as if in
/// treble clef.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ClefSign final
{
  public:
    enum class Tag
    {
        g,
        f,
        c,
        percussion,
        tab,
        jianpu,
        none,
    };

    /// The natural zero: the first variant.
    ClefSign() noexcept = default;

    static ClefSign g() noexcept;
    static ClefSign f() noexcept;
    static ClefSign c() noexcept;
    static ClefSign percussion() noexcept;
    static ClefSign tab() noexcept;
    static ClefSign jianpu() noexcept;
    static ClefSign none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ClefSign &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ClefSign parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ClefSign parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ClefSign &other) const noexcept = default;

  private:
    explicit ClefSign(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::g};
};

} // namespace mx::core
