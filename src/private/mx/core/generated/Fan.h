// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The fan type represents the type of beam fanning present on a note, used to represent
/// accelerandos and ritardandos.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class Fan final
{
  public:
    enum class Tag
    {
        accel,
        rit,
        none,
    };

    /// The natural zero: the first variant.
    Fan() noexcept = default;

    static Fan accel() noexcept;
    static Fan rit() noexcept;
    static Fan none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, Fan &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static Fan parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static Fan parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Fan &other) const noexcept = default;

  private:
    explicit Fan(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::accel};
};

} // namespace mx::core
