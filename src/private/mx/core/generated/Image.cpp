// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Image.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::string &Image::source() const noexcept
{
    return m_source;
}

void Image::setSource(std::string value)
{
    m_source = std::move(value);
}

const std::string &Image::type() const noexcept
{
    return m_type;
}

void Image::setType(std::string value)
{
    m_type = std::move(value);
}

const std::optional<Tenths> &Image::height() const noexcept
{
    return m_height;
}

void Image::setHeight(std::optional<Tenths> value)
{
    m_height = std::move(value);
}

const std::optional<Tenths> &Image::width() const noexcept
{
    return m_width;
}

void Image::setWidth(std::optional<Tenths> value)
{
    m_width = std::move(value);
}

const std::optional<Tenths> &Image::defaultX() const noexcept
{
    return m_defaultX;
}

void Image::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Image::defaultY() const noexcept
{
    return m_defaultY;
}

void Image::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Image::relativeX() const noexcept
{
    return m_relativeX;
}

void Image::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Image::relativeY() const noexcept
{
    return m_relativeY;
}

void Image::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<LeftCenterRight> &Image::halign() const noexcept
{
    return m_halign;
}

void Image::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<ValignImage> &Image::valign() const noexcept
{
    return m_valign;
}

void Image::setValign(std::optional<ValignImage> value)
{
    m_valign = std::move(value);
}

const std::optional<Token> &Image::id() const noexcept
{
    return m_id;
}

void Image::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

Image parseImage(pugi::xml_node el)
{
    return parseImage(el, ParseContext{});
}

Image parseImage(pugi::xml_node el, const ParseContext &context)
{
    Image out;
    bool seen_source = false;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "source")
        {
            seen_source = true;
            out.setSource(std::string{a.value()});
        }
        else if (aname == "type")
        {
            seen_type = true;
            out.setType(std::string{a.value()});
        }
        else if (aname == "height")
        {
            out.setHeight(parseValue<Tenths>(a.value(), context, el, "height"));
        }
        else if (aname == "width")
        {
            out.setWidth(parseValue<Tenths>(a.value(), context, el, "width"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "halign")
        {
            out.setHalign(parseValue<LeftCenterRight>(a.value(), context, el, "halign"));
        }
        else if (aname == "valign")
        {
            out.setValign(parseValue<ValignImage>(a.value(), context, el, "valign"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_source)
    {
        throwMissingAttribute(el, "source");
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseImageContent(out, el, context);
    return out;
}

void parseImageContent(Image &out, pugi::xml_node el)
{
    parseImageContent(out, el, ParseContext{});
}

void parseImageContent(Image &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeImage(const Image &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "source", v.source());
    setAttribute(el, "type", v.type());
    if (v.height())
    {
        setAttribute(el, "height", v.height()->toString());
    }
    if (v.width())
    {
        setAttribute(el, "width", v.width()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeImageContent(v, el);
}

void serializeImageContent(const Image &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
