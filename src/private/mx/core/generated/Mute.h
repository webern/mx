// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The mute type represents muting for different instruments, including brass, winds, and strings.
/// The on and off values are used for undifferentiated mutes. The remaining values represent
/// specific mutes.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class Mute final
{
  public:
    enum class Tag
    {
        on,
        off,
        straight,
        cup,
        harmonNoStem,
        harmonStem,
        bucket,
        plunger,
        hat,
        solotone,
        practice,
        stopMute,
        stopHand,
        echo,
        palm,
    };

    /// The natural zero: the first variant.
    Mute() noexcept = default;

    static Mute on() noexcept;
    static Mute off() noexcept;
    static Mute straight() noexcept;
    static Mute cup() noexcept;
    static Mute harmonNoStem() noexcept;
    static Mute harmonStem() noexcept;
    static Mute bucket() noexcept;
    static Mute plunger() noexcept;
    static Mute hat() noexcept;
    static Mute solotone() noexcept;
    static Mute practice() noexcept;
    static Mute stopMute() noexcept;
    static Mute stopHand() noexcept;
    static Mute echo() noexcept;
    static Mute palm() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, Mute &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static Mute parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static Mute parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Mute &other) const noexcept = default;

  private:
    explicit Mute(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::on};
};

} // namespace mx::core
