// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The group-barline-value type indicates if the group should have common barlines.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class GroupBarlineValue final
{
  public:
    enum class Tag
    {
        yes,
        no,
        mensurstrich,
    };

    /// The natural zero: the first variant.
    GroupBarlineValue() noexcept = default;

    static GroupBarlineValue yes() noexcept;
    static GroupBarlineValue no() noexcept;
    static GroupBarlineValue mensurstrich() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, GroupBarlineValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static GroupBarlineValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static GroupBarlineValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const GroupBarlineValue &other) const noexcept = default;

  private:
    explicit GroupBarlineValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::yes};
};

} // namespace mx::core
