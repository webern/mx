// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <span>
#include <string>
#include <string_view>
#include <vector>

namespace mx::core
{

/// The time-only type is used to indicate that a particular playback- or listening-related element
/// only applies particular times through a repeated section. The value is a comma-separated list of
/// positive integers arranged in ascending order, indicating which times through the repeated
/// section that the element applies.
/// Structural representation (plan §2.2): a never-empty list of positive
/// integers (the time-through-repeat indices), serialized comma-separated.
/// Construction clamps each value to >= 1; an empty list becomes [1].
class TimeOnly final
{
  public:
    /// The natural zero: the single value 1.
    TimeOnly();

    explicit TimeOnly(std::vector<int> values);

    std::span<const int> values() const noexcept
    {
        return m_values;
    }

    void setValues(std::vector<int> values);

    void addValue(int value);

    std::string toString() const;

    /// Strict: one or more comma-separated positive integers.
    static bool tryParse(std::string_view text, TimeOnly &out);

    /// Lenient: repairs into the nearest valid list.
    static TimeOnly parse(std::string_view text);

    /// Lenient, and says whether the text had to be repaired.
    static TimeOnly parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const TimeOnly &other) const noexcept
    {
        return m_values == other.m_values;
    }

    bool operator!=(const TimeOnly &other) const noexcept
    {
        return !(*this == other);
    }

  private:
    void repair();

    std::vector<int> m_values;
    std::string m_text; // preserved wire spelling; empty = none
};

} // namespace mx::core
