// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/OneOrMore.h"
#include "mx/core/generated/ListeningChoice.h"
#include "mx/core/generated/Offset.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The listen and listening types, new in Version 4.0, specify different ways that a score following
/// or machine listening application can interact with a performer. The listening type handles
/// interactions that change the state of the listening application from the specified point in the
/// performance onward. If multiple child elements of the same type are present, they should have
/// distinct player and/or time-only attributes. The offset element is used to indicate that the
/// listening change takes place offset from the current score position. If the listening element is
/// a child of a direction element, the listening offset element overrides the direction offset
/// element if both elements are present. Note that the offset reflects the intended musical position
/// for the change in state. It should not be used to compensate for latency issues in particular
/// hardware configurations.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Listening final
{
  public:
    std::span<const ListeningChoice> choice() const noexcept;
    void addChoice(ListeningChoice value);
    void setChoice(OneOrMore<ListeningChoice> value);
    const std::optional<Offset> &offset() const noexcept;
    void setOffset(std::optional<Offset> value);
    bool operator==(const Listening &other) const = default;

  private:
    OneOrMore<ListeningChoice> m_choice;
    std::optional<Offset> m_offset;
};

Listening parseListening(pugi::xml_node el);

Listening parseListening(pugi::xml_node el, const ParseContext &context);

void parseListeningContent(Listening &out, pugi::xml_node el);

void parseListeningContent(Listening &out, pugi::xml_node el, const ParseContext &context);

void serializeListening(const Listening &v, pugi::xml_node parent, const char *tag);

void serializeListeningContent(const Listening &v, pugi::xml_node el);

} // namespace mx::core
