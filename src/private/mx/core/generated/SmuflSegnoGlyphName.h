// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The smufl-segno-glyph-name type is used to reference a specific Standard Music Font Layout
/// (SMuFL) segno character. The value is a SMuFL canonical glyph name that starts with segno.
/// Structural representation (plan §2.2): the glyph-name prefix is
/// structural and emitted by the serializer; only the suffix is stored.
/// Construction repairs the suffix to name characters, so a
/// pattern-invalid name cannot exist.
class SmuflSegnoGlyphName final
{
  public:
    /// The natural zero: the prefix.
    SmuflSegnoGlyphName();

    explicit SmuflSegnoGlyphName(std::string suffix);

    const std::string &suffix() const noexcept
    {
        return m_suffix;
    }

    void setSuffix(std::string value);

    std::string toString() const;

    /// Strict: a known prefix followed by a pattern-valid suffix.
    static bool tryParse(std::string_view text, SmuflSegnoGlyphName &out);

    /// Lenient: repairs into the nearest valid glyph name.
    static SmuflSegnoGlyphName parse(std::string_view text);

    /// Lenient, and says whether the text had to be repaired.
    static SmuflSegnoGlyphName parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const SmuflSegnoGlyphName &other) const noexcept = default;

  private:
    void repair();

    std::string m_suffix;
};

} // namespace mx::core
