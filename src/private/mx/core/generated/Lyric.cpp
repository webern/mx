// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Lyric.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<NameToken> &Lyric::number() const noexcept
{
    return m_number;
}

void Lyric::setNumber(std::optional<NameToken> value)
{
    m_number = std::move(value);
}

const std::optional<std::string> &Lyric::name() const noexcept
{
    return m_name;
}

void Lyric::setName(std::optional<std::string> value)
{
    m_name = std::move(value);
}

const std::optional<TimeOnly> &Lyric::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Lyric::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

const std::optional<LeftCenterRight> &Lyric::justify() const noexcept
{
    return m_justify;
}

void Lyric::setJustify(std::optional<LeftCenterRight> value)
{
    m_justify = std::move(value);
}

const std::optional<Tenths> &Lyric::defaultX() const noexcept
{
    return m_defaultX;
}

void Lyric::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Lyric::defaultY() const noexcept
{
    return m_defaultY;
}

void Lyric::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Lyric::relativeX() const noexcept
{
    return m_relativeX;
}

void Lyric::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Lyric::relativeY() const noexcept
{
    return m_relativeY;
}

void Lyric::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<AboveBelow> &Lyric::placement() const noexcept
{
    return m_placement;
}

void Lyric::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<Color> &Lyric::color() const noexcept
{
    return m_color;
}

void Lyric::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<YesNo> &Lyric::printObject() const noexcept
{
    return m_printObject;
}

void Lyric::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Token> &Lyric::id() const noexcept
{
    return m_id;
}

void Lyric::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const LyricChoice &Lyric::choice() const noexcept
{
    return m_choice;
}

void Lyric::setChoice(LyricChoice value)
{
    m_choice = std::move(value);
}

bool Lyric::endLine() const noexcept
{
    return m_endLine;
}

void Lyric::setEndLine(bool value) noexcept
{
    m_endLine = value;
}

bool Lyric::endParagraph() const noexcept
{
    return m_endParagraph;
}

void Lyric::setEndParagraph(bool value) noexcept
{
    m_endParagraph = value;
}

const EditorialGroup &Lyric::editorial() const noexcept
{
    return m_editorial;
}

void Lyric::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

Lyric parseLyric(pugi::xml_node el, const ParseContext &context)
{
    Lyric out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<NameToken>(a.value(), context, el, "number"));
        }
        else if (aname == "name")
        {
            out.setName(std::string{a.value()});
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(parseValue<TimeOnly>(a.value(), context, el, "time-only"));
        }
        else if (aname == "justify")
        {
            out.setJustify(parseValue<LeftCenterRight>(a.value(), context, el, "justify"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseLyricContent(out, el, context);
    return out;
}

void parseLyricContent(Lyric &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "syllabic") || cursorIs(cursor, "text") || cursorIs(cursor, "extend") ||
                   cursorIs(cursor, "laughing") || cursorIs(cursor, "humming")))
    {
        out.setChoice(parseLyricChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "syllabic");
    }
    if (cursorIs(cursor, "end-line"))
    {
        parseEmpty(cursor, context);
        out.setEndLine(true);
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "end-paragraph"))
    {
        parseEmpty(cursor, context);
        out.setEndParagraph(true);
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "syllabic") || cursorIs(cursor, "text") || cursorIs(cursor, "elision") ||
            cursorIs(cursor, "extend") || cursorIs(cursor, "laughing") || cursorIs(cursor, "humming") ||
            cursorIs(cursor, "end-line") || cursorIs(cursor, "end-paragraph") || cursorIs(cursor, "footnote") ||
            cursorIs(cursor, "level"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeLyric(const Lyric &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.name())
    {
        setAttribute(el, "name", *v.name());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    if (v.justify())
    {
        setAttribute(el, "justify", v.justify()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeLyricContent(v, el);
}

void serializeLyricContent(const Lyric &v, pugi::xml_node el)
{
    serializeLyricChoice(v.choice(), el);
    if (v.endLine())
    {
        el.append_child("end-line");
    }
    if (v.endParagraph())
    {
        el.append_child("end-paragraph");
    }
    serializeEditorialGroup(v.editorial(), el);
}

} // namespace mx::core
