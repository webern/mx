// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Grouping.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStopSingle &Grouping::type() const noexcept
{
    return m_type;
}

void Grouping::setType(StartStopSingle value)
{
    m_type = std::move(value);
}

const std::optional<std::string> &Grouping::number() const noexcept
{
    return m_number;
}

void Grouping::setNumber(std::optional<std::string> value)
{
    m_number = std::move(value);
}

const std::optional<std::string> &Grouping::memberOf() const noexcept
{
    return m_memberOf;
}

void Grouping::setMemberOf(std::optional<std::string> value)
{
    m_memberOf = std::move(value);
}

const std::optional<Token> &Grouping::id() const noexcept
{
    return m_id;
}

void Grouping::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const Feature> Grouping::feature() const noexcept
{
    return m_feature;
}

void Grouping::addFeature(Feature value)
{
    m_feature.push_back(std::move(value));
}

void Grouping::setFeature(std::vector<Feature> value)
{
    m_feature = std::move(value);
}

Grouping parseGrouping(pugi::xml_node el, const ParseContext &context)
{
    Grouping out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStopSingle>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(std::string{a.value()});
        }
        else if (aname == "member-of")
        {
            out.setMemberOf(std::string{a.value()});
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseGroupingContent(out, el, context);
    return out;
}

void parseGroupingContent(Grouping &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursorIs(cursor, "feature"))
    {
        out.addFeature(parseFeature(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "feature"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeGrouping(const Grouping &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", *v.number());
    }
    if (v.memberOf())
    {
        setAttribute(el, "member-of", *v.memberOf());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeGroupingContent(v, el);
}

void serializeGroupingContent(const Grouping &v, pugi::xml_node el)
{
    for (const auto &item : v.feature())
    {
        serializeFeature(item, el, "feature");
    }
}

} // namespace mx::core
