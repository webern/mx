// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/StaffDetails.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &StaffDetails::number() const noexcept
{
    return m_number;
}

void StaffDetails::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<ShowFrets> &StaffDetails::showFrets() const noexcept
{
    return m_showFrets;
}

void StaffDetails::setShowFrets(std::optional<ShowFrets> value)
{
    m_showFrets = std::move(value);
}

const std::optional<YesNo> &StaffDetails::printObject() const noexcept
{
    return m_printObject;
}

void StaffDetails::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<YesNo> &StaffDetails::printSpacing() const noexcept
{
    return m_printSpacing;
}

void StaffDetails::setPrintSpacing(std::optional<YesNo> value)
{
    m_printSpacing = std::move(value);
}

const std::optional<StaffType> &StaffDetails::staffType() const noexcept
{
    return m_staffType;
}

void StaffDetails::setStaffType(std::optional<StaffType> value)
{
    m_staffType = std::move(value);
}

const std::optional<StaffDetailsGroup> &StaffDetails::group() const noexcept
{
    return m_group;
}

void StaffDetails::setGroup(std::optional<StaffDetailsGroup> value)
{
    m_group = std::move(value);
}

std::span<const StaffTuning> StaffDetails::staffTuning() const noexcept
{
    return m_staffTuning;
}

void StaffDetails::addStaffTuning(StaffTuning value)
{
    m_staffTuning.push_back(std::move(value));
}

void StaffDetails::setStaffTuning(std::vector<StaffTuning> value)
{
    m_staffTuning = std::move(value);
}

const std::optional<int> &StaffDetails::capo() const noexcept
{
    return m_capo;
}

void StaffDetails::setCapo(std::optional<int> value)
{
    m_capo = std::move(value);
}

const std::optional<StaffSize> &StaffDetails::staffSize() const noexcept
{
    return m_staffSize;
}

void StaffDetails::setStaffSize(std::optional<StaffSize> value)
{
    m_staffSize = std::move(value);
}

StaffDetails parseStaffDetails(pugi::xml_node el, const ParseContext &context)
{
    StaffDetails out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "show-frets")
        {
            out.setShowFrets(parseValue<ShowFrets>(a.value(), context, el, "show-frets"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "print-spacing")
        {
            out.setPrintSpacing(parseValue<YesNo>(a.value(), context, el, "print-spacing"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseStaffDetailsContent(out, el, context);
    return out;
}

void parseStaffDetailsContent(StaffDetails &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "staff-type"))
    {
        out.setStaffType(parseValue<StaffType>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "staff-lines")))
    {
        out.setGroup(parseStaffDetailsGroup(el, cursor, context));
    }
    while (cursorIs(cursor, "staff-tuning"))
    {
        out.addStaffTuning(parseStaffTuning(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "capo"))
    {
        out.setCapo(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "staff-size"))
    {
        out.setStaffSize(parseStaffSize(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "staff-type") || cursorIs(cursor, "staff-lines") || cursorIs(cursor, "line-detail") ||
            cursorIs(cursor, "staff-tuning") || cursorIs(cursor, "capo") || cursorIs(cursor, "staff-size"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeStaffDetails(const StaffDetails &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.showFrets())
    {
        setAttribute(el, "show-frets", v.showFrets()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.printSpacing())
    {
        setAttribute(el, "print-spacing", v.printSpacing()->toString());
    }
    serializeStaffDetailsContent(v, el);
}

void serializeStaffDetailsContent(const StaffDetails &v, pugi::xml_node el)
{
    if (v.staffType())
    {
        writeTextElement(el, "staff-type", v.staffType()->toString());
    }
    if (v.group())
    {
        serializeStaffDetailsGroup(*v.group(), el);
    }
    for (const auto &item : v.staffTuning())
    {
        serializeStaffTuning(item, el, "staff-tuning");
    }
    if (v.capo())
    {
        writeTextElement(el, "capo", formatInt(*v.capo()));
    }
    if (v.staffSize())
    {
        serializeStaffSize(*v.staffSize(), el, "staff-size");
    }
}

} // namespace mx::core
