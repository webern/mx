// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The fifths type represents the number of flats or sharps in a traditional key signature. Negative
/// numbers are used for flats and positive numbers for sharps, reflecting the key's placement within
/// the circle of fifths (hence the type name).
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class Fifths final
{
  public:
    /// The natural zero: the default value clamped into range.
    Fifths();

    explicit Fifths(int value);

    int value() const noexcept
    {
        return m_value;
    }

    void setValue(int value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, Fifths &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static Fifths parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static Fifths parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const Fifths &other) const noexcept = default;

  private:
    int m_value{};
};

} // namespace mx::core
