// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The line-shape type distinguishes between straight and curved lines.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class LineShape final
{
  public:
    enum class Tag
    {
        straight,
        curved,
    };

    /// The natural zero: the first variant.
    LineShape() noexcept = default;

    static LineShape straight() noexcept;
    static LineShape curved() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, LineShape &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static LineShape parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static LineShape parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const LineShape &other) const noexcept = default;

  private:
    explicit LineShape(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::straight};
};

} // namespace mx::core
