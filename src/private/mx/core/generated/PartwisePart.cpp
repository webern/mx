// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PartwisePart.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &PartwisePart::id() const noexcept
{
    return m_id;
}

void PartwisePart::setID(Token value)
{
    m_id = std::move(value);
}

std::span<const PartwiseMeasure> PartwisePart::measure() const noexcept
{
    return m_measure.items();
}

void PartwisePart::addMeasure(PartwiseMeasure value)
{
    m_measure.add(std::move(value));
}

void PartwisePart::setMeasure(OneOrMore<PartwiseMeasure> value)
{
    m_measure = std::move(value);
}

PartwisePart parsePartwisePart(pugi::xml_node el)
{
    return parsePartwisePart(el, ParseContext{});
}

PartwisePart parsePartwisePart(pugi::xml_node el, const ParseContext &context)
{
    PartwisePart out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parsePartwisePartContent(out, el, context);
    return out;
}

void parsePartwisePartContent(PartwisePart &out, pugi::xml_node el)
{
    parsePartwisePartContent(out, el, ParseContext{});
}

void parsePartwisePartContent(PartwisePart &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!cursorIs(cursor, "measure"))
    {
        throwMissingOrMisplaced(el, cursor, "measure");
    }
    out.setMeasure(OneOrMore<PartwiseMeasure>{parsePartwiseMeasure(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "measure"))
    {
        out.addMeasure(parsePartwiseMeasure(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "measure"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePartwisePart(const PartwisePart &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializePartwisePartContent(v, el);
}

void serializePartwisePartContent(const PartwisePart &v, pugi::xml_node el)
{
    for (const auto &item : v.measure())
    {
        serializePartwiseMeasure(item, el, "measure");
    }
}

} // namespace mx::core
