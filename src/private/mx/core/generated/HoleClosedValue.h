// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The hole-closed-value type represents whether the hole is closed, open, or half-open.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class HoleClosedValue final
{
  public:
    enum class Tag
    {
        yes,
        no,
        half,
    };

    /// The natural zero: the first variant.
    HoleClosedValue() noexcept = default;

    static HoleClosedValue yes() noexcept;
    static HoleClosedValue no() noexcept;
    static HoleClosedValue half() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, HoleClosedValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static HoleClosedValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static HoleClosedValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const HoleClosedValue &other) const noexcept = default;

  private:
    explicit HoleClosedValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::yes};
};

} // namespace mx::core
