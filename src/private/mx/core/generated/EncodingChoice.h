// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Supports.h"
#include "mx/core/generated/TypedText.h"
#include "mx/core/generated/YyyyMmDd.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class EncodingChoice final
{
  public:
    enum class Kind
    {
        encodingDate,
        encoder,
        software,
        encodingDescription,
        supports,
    };

    /// The natural zero: the first alternative.
    EncodingChoice();

    static EncodingChoice encodingDate(YyyyMmDd value);
    static EncodingChoice encoder(TypedText value);
    static EncodingChoice software(std::string value);
    static EncodingChoice encodingDescription(std::string value);
    static EncodingChoice supports(Supports value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isEncodingDate() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isEncodingDate().
    const YyyyMmDd &asEncodingDate() const noexcept
    {
        assert(isEncodingDate());
        return *std::get_if<0>(&m_value);
    }

    bool isEncoder() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isEncoder().
    const TypedText &asEncoder() const noexcept
    {
        assert(isEncoder());
        return *std::get_if<1>(&m_value);
    }

    bool isSoftware() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isSoftware().
    const std::string &asSoftware() const noexcept
    {
        assert(isSoftware());
        return *std::get_if<2>(&m_value);
    }

    bool isEncodingDescription() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isEncodingDescription().
    const std::string &asEncodingDescription() const noexcept
    {
        assert(isEncodingDescription());
        return *std::get_if<3>(&m_value);
    }

    bool isSupports() const noexcept
    {
        return m_value.index() == 4;
    }

    /// Precondition: isSupports().
    const Supports &asSupports() const noexcept
    {
        assert(isSupports());
        return *std::get_if<4>(&m_value);
    }

    bool operator==(const EncodingChoice &other) const = default;

  private:
    using Storage = std::variant<YyyyMmDd, TypedText, std::string, std::string, Supports>;

    explicit EncodingChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
EncodingChoice parseEncodingChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeEncodingChoice(const EncodingChoice &v, pugi::xml_node el);

} // namespace mx::core
