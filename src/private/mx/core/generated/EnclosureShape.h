// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The enclosure-shape type describes the shape and presence / absence of an enclosure around text
/// or symbols. A bracket enclosure is similar to a rectangle with the bottom line missing, as is
/// common in jazz notation. An inverted-bracket enclosure is similar to a rectangle with the top
/// line missing.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class EnclosureShape final
{
  public:
    enum class Tag
    {
        rectangle,
        square,
        oval,
        circle,
        bracket,
        invertedBracket,
        triangle,
        diamond,
        pentagon,
        hexagon,
        heptagon,
        octagon,
        nonagon,
        decagon,
        none,
    };

    /// The natural zero: the first variant.
    EnclosureShape() noexcept = default;

    static EnclosureShape rectangle() noexcept;
    static EnclosureShape square() noexcept;
    static EnclosureShape oval() noexcept;
    static EnclosureShape circle() noexcept;
    static EnclosureShape bracket() noexcept;
    static EnclosureShape invertedBracket() noexcept;
    static EnclosureShape triangle() noexcept;
    static EnclosureShape diamond() noexcept;
    static EnclosureShape pentagon() noexcept;
    static EnclosureShape hexagon() noexcept;
    static EnclosureShape heptagon() noexcept;
    static EnclosureShape octagon() noexcept;
    static EnclosureShape nonagon() noexcept;
    static EnclosureShape decagon() noexcept;
    static EnclosureShape none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, EnclosureShape &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static EnclosureShape parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static EnclosureShape parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const EnclosureShape &other) const noexcept = default;

  private:
    explicit EnclosureShape(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::rectangle};
};

} // namespace mx::core
