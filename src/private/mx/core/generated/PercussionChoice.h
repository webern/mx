// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Beater.h"
#include "mx/core/generated/Effect.h"
#include "mx/core/generated/Glass.h"
#include "mx/core/generated/Membrane.h"
#include "mx/core/generated/Metal.h"
#include "mx/core/generated/OtherText.h"
#include "mx/core/generated/Pitched.h"
#include "mx/core/generated/Stick.h"
#include "mx/core/generated/StickLocation.h"
#include "mx/core/generated/Timpani.h"
#include "mx/core/generated/Wood.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class PercussionChoice final
{
  public:
    enum class Kind
    {
        glass,
        metal,
        wood,
        pitched,
        membrane,
        effect,
        timpani,
        beater,
        stick,
        stickLocation,
        otherPercussion,
    };

    /// The natural zero: the first alternative.
    PercussionChoice();

    static PercussionChoice glass(Glass value);
    static PercussionChoice metal(Metal value);
    static PercussionChoice wood(Wood value);
    static PercussionChoice pitched(Pitched value);
    static PercussionChoice membrane(Membrane value);
    static PercussionChoice effect(Effect value);
    static PercussionChoice timpani(Timpani value);
    static PercussionChoice beater(Beater value);
    static PercussionChoice stick(Stick value);
    static PercussionChoice stickLocation(StickLocation value);
    static PercussionChoice otherPercussion(OtherText value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isGlass() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isGlass().
    const Glass &asGlass() const noexcept
    {
        assert(isGlass());
        return *std::get_if<0>(&m_value);
    }

    bool isMetal() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isMetal().
    const Metal &asMetal() const noexcept
    {
        assert(isMetal());
        return *std::get_if<1>(&m_value);
    }

    bool isWood() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isWood().
    const Wood &asWood() const noexcept
    {
        assert(isWood());
        return *std::get_if<2>(&m_value);
    }

    bool isPitched() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isPitched().
    const Pitched &asPitched() const noexcept
    {
        assert(isPitched());
        return *std::get_if<3>(&m_value);
    }

    bool isMembrane() const noexcept
    {
        return m_value.index() == 4;
    }

    /// Precondition: isMembrane().
    const Membrane &asMembrane() const noexcept
    {
        assert(isMembrane());
        return *std::get_if<4>(&m_value);
    }

    bool isEffect() const noexcept
    {
        return m_value.index() == 5;
    }

    /// Precondition: isEffect().
    const Effect &asEffect() const noexcept
    {
        assert(isEffect());
        return *std::get_if<5>(&m_value);
    }

    bool isTimpani() const noexcept
    {
        return m_value.index() == 6;
    }

    /// Precondition: isTimpani().
    const Timpani &asTimpani() const noexcept
    {
        assert(isTimpani());
        return *std::get_if<6>(&m_value);
    }

    bool isBeater() const noexcept
    {
        return m_value.index() == 7;
    }

    /// Precondition: isBeater().
    const Beater &asBeater() const noexcept
    {
        assert(isBeater());
        return *std::get_if<7>(&m_value);
    }

    bool isStick() const noexcept
    {
        return m_value.index() == 8;
    }

    /// Precondition: isStick().
    const Stick &asStick() const noexcept
    {
        assert(isStick());
        return *std::get_if<8>(&m_value);
    }

    bool isStickLocation() const noexcept
    {
        return m_value.index() == 9;
    }

    /// Precondition: isStickLocation().
    const StickLocation &asStickLocation() const noexcept
    {
        assert(isStickLocation());
        return *std::get_if<9>(&m_value);
    }

    bool isOtherPercussion() const noexcept
    {
        return m_value.index() == 10;
    }

    /// Precondition: isOtherPercussion().
    const OtherText &asOtherPercussion() const noexcept
    {
        assert(isOtherPercussion());
        return *std::get_if<10>(&m_value);
    }

    bool operator==(const PercussionChoice &other) const = default;

  private:
    using Storage =
        std::variant<Glass, Metal, Wood, Pitched, Membrane, Effect, Timpani, Beater, Stick, StickLocation, OtherText>;

    explicit PercussionChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
PercussionChoice parsePercussionChoice(pugi::xml_node el, pugi::xml_node &cursor);

PercussionChoice parsePercussionChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializePercussionChoice(const PercussionChoice &v, pugi::xml_node el);

} // namespace mx::core
