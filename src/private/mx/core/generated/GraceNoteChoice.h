// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/GraceCueNoteGroup.h"
#include "mx/core/generated/GraceNormalNoteGroup.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class GraceNoteChoice final
{
  public:
    enum class Kind
    {
        graceNormalNoteGroup,
        graceCueNoteGroup,
    };

    /// The natural zero: the first alternative.
    GraceNoteChoice();

    static GraceNoteChoice graceNormalNoteGroup(GraceNormalNoteGroup value);
    static GraceNoteChoice graceCueNoteGroup(GraceCueNoteGroup value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isGraceNormalNoteGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isGraceNormalNoteGroup().
    const GraceNormalNoteGroup &asGraceNormalNoteGroup() const noexcept
    {
        assert(isGraceNormalNoteGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isGraceCueNoteGroup() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isGraceCueNoteGroup().
    const GraceCueNoteGroup &asGraceCueNoteGroup() const noexcept
    {
        assert(isGraceCueNoteGroup());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const GraceNoteChoice &other) const = default;

  private:
    using Storage = std::variant<GraceNormalNoteGroup, GraceCueNoteGroup>;

    explicit GraceNoteChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
GraceNoteChoice parseGraceNoteChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeGraceNoteChoice(const GraceNoteChoice &v, pugi::xml_node el);

} // namespace mx::core
