// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/MetronomeChoiceGroupChoiceGroup.h"
#include "mx/core/generated/PerMinute.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class MetronomeChoiceGroupChoice final
{
  public:
    enum class Kind
    {
        perMinute,
        group,
    };

    /// The natural zero: the first alternative.
    MetronomeChoiceGroupChoice();

    static MetronomeChoiceGroupChoice perMinute(PerMinute value);
    static MetronomeChoiceGroupChoice group(MetronomeChoiceGroupChoiceGroup value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isPerMinute() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isPerMinute().
    const PerMinute &asPerMinute() const noexcept
    {
        assert(isPerMinute());
        return *std::get_if<0>(&m_value);
    }

    bool isGroup() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isGroup().
    const MetronomeChoiceGroupChoiceGroup &asGroup() const noexcept
    {
        assert(isGroup());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const MetronomeChoiceGroupChoice &other) const = default;

  private:
    using Storage = std::variant<PerMinute, MetronomeChoiceGroupChoiceGroup>;

    explicit MetronomeChoiceGroupChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
MetronomeChoiceGroupChoice parseMetronomeChoiceGroupChoice(pugi::xml_node el, pugi::xml_node &cursor);

MetronomeChoiceGroupChoice parseMetronomeChoiceGroupChoice(pugi::xml_node el, pugi::xml_node &cursor,
                                                           const ParseContext &context);

void serializeMetronomeChoiceGroupChoice(const MetronomeChoiceGroupChoice &v, pugi::xml_node el);

} // namespace mx::core
