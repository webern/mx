// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PageLayoutGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Tenths &PageLayoutGroup::pageHeight() const noexcept
{
    return m_pageHeight;
}

void PageLayoutGroup::setPageHeight(Tenths value)
{
    m_pageHeight = std::move(value);
}

const Tenths &PageLayoutGroup::pageWidth() const noexcept
{
    return m_pageWidth;
}

void PageLayoutGroup::setPageWidth(Tenths value)
{
    m_pageWidth = std::move(value);
}

PageLayoutGroup parsePageLayoutGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    PageLayoutGroup out;
    if (cursorIs(cursor, "page-height"))
    {
        out.setPageHeight(parseValue<Tenths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "page-height");
    }
    if (cursorIs(cursor, "page-width"))
    {
        out.setPageWidth(parseValue<Tenths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "page-width");
    }
    return out;
}

void serializePageLayoutGroup(const PageLayoutGroup &v, pugi::xml_node el)
{
    writeTextElement(el, "page-height", v.pageHeight().toString());
    writeTextElement(el, "page-width", v.pageWidth().toString());
}

} // namespace mx::core
