// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/CreditChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

CreditChoice::CreditChoice()
{
    *this = CreditChoice::creditImage(Image{});
}

CreditChoice CreditChoice::creditImage(Image value)
{
    return CreditChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

CreditChoice CreditChoice::group(CreditChoiceGroup value)
{
    return CreditChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

CreditChoice parseCreditChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "credit-image")))
    {
        Image value = parseImage(cursor, context);
        cursor = nextElement(cursor);
        return CreditChoice::creditImage(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "credit-words") || cursorIs(cursor, "credit-symbol")))
    {
        return CreditChoice::group(parseCreditChoiceGroup(el, cursor, context));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "credit-image");
}

void serializeCreditChoice(const CreditChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case CreditChoice::Kind::creditImage:
        serializeImage(v.asCreditImage(), el, "credit-image");
        break;
    case CreditChoice::Kind::group:
        serializeCreditChoiceGroup(v.asGroup(), el);
        break;
    }
}

} // namespace mx::core
