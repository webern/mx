// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/OtherListening.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::string &OtherListening::type() const noexcept
{
    return m_type;
}

void OtherListening::setType(std::string value)
{
    m_type = std::move(value);
}

const std::optional<Token> &OtherListening::player() const noexcept
{
    return m_player;
}

void OtherListening::setPlayer(std::optional<Token> value)
{
    m_player = std::move(value);
}

const std::optional<TimeOnly> &OtherListening::timeOnly() const noexcept
{
    return m_timeOnly;
}

void OtherListening::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

const std::string &OtherListening::value() const noexcept
{
    return m_value;
}

void OtherListening::setValue(std::string value)
{
    m_value = std::move(value);
}

OtherListening parseOtherListening(pugi::xml_node el, const ParseContext &context)
{
    OtherListening out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(std::string{a.value()});
        }
        else if (aname == "player")
        {
            out.setPlayer(parseValue<Token>(a.value(), context, el, "player"));
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(parseValue<TimeOnly>(a.value(), context, el, "time-only"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseOtherListeningContent(out, el, context);
    return out;
}

void parseOtherListeningContent(OtherListening &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeOtherListening(const OtherListening &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type());
    if (v.player())
    {
        setAttribute(el, "player", v.player()->toString());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    serializeOtherListeningContent(v, el);
}

void serializeOtherListeningContent(const OtherListening &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
