// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/SoundGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<InstrumentChange> &SoundGroup::instrumentChange() const noexcept
{
    return m_instrumentChange;
}

void SoundGroup::setInstrumentChange(std::optional<InstrumentChange> value)
{
    m_instrumentChange = std::move(value);
}

const std::optional<MIDIDevice> &SoundGroup::midiDevice() const noexcept
{
    return m_midiDevice;
}

void SoundGroup::setMIDIDevice(std::optional<MIDIDevice> value)
{
    m_midiDevice = std::move(value);
}

const std::optional<MIDIInstrument> &SoundGroup::midiInstrument() const noexcept
{
    return m_midiInstrument;
}

void SoundGroup::setMIDIInstrument(std::optional<MIDIInstrument> value)
{
    m_midiInstrument = std::move(value);
}

const std::optional<Play> &SoundGroup::play() const noexcept
{
    return m_play;
}

void SoundGroup::setPlay(std::optional<Play> value)
{
    m_play = std::move(value);
}

SoundGroup parseSoundGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseSoundGroup(el, cursor, ParseContext{});
}

SoundGroup parseSoundGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    SoundGroup out;
    if (cursorIs(cursor, "instrument-change"))
    {
        out.setInstrumentChange(parseInstrumentChange(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "midi-device"))
    {
        out.setMIDIDevice(parseMIDIDevice(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "midi-instrument"))
    {
        out.setMIDIInstrument(parseMIDIInstrument(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "play"))
    {
        out.setPlay(parsePlay(cursor, context));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeSoundGroup(const SoundGroup &v, pugi::xml_node el)
{
    if (v.instrumentChange())
    {
        serializeInstrumentChange(*v.instrumentChange(), el, "instrument-change");
    }
    if (v.midiDevice())
    {
        serializeMIDIDevice(*v.midiDevice(), el, "midi-device");
    }
    if (v.midiInstrument())
    {
        serializeMIDIInstrument(*v.midiInstrument(), el, "midi-instrument");
    }
    if (v.play())
    {
        serializePlay(*v.play(), el, "play");
    }
}

} // namespace mx::core
