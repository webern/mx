// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Octave.h"
#include "mx/core/generated/Semitones.h"
#include "mx/core/generated/Step.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// Pitch is represented as a combination of the step of the diatonic scale, the chromatic
/// alteration, and the octave.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Pitch final
{
  public:
    const Step &step() const noexcept;
    void setStep(Step value);
    const std::optional<Semitones> &alter() const noexcept;
    void setAlter(std::optional<Semitones> value);
    const Octave &octave() const noexcept;
    void setOctave(Octave value);
    bool operator==(const Pitch &other) const = default;

  private:
    Step m_step{};
    std::optional<Semitones> m_alter;
    Octave m_octave{};
};

Pitch parsePitch(pugi::xml_node el);

Pitch parsePitch(pugi::xml_node el, const ParseContext &context);

void parsePitchContent(Pitch &out, pugi::xml_node el);

void parsePitchContent(Pitch &out, pugi::xml_node el, const ParseContext &context);

void serializePitch(const Pitch &v, pugi::xml_node parent, const char *tag);

void serializePitchContent(const Pitch &v, pugi::xml_node el);

} // namespace mx::core
