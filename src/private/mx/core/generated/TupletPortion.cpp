// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TupletPortion.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<TupletNumber> &TupletPortion::tupletNumber() const noexcept
{
    return m_tupletNumber;
}

void TupletPortion::setTupletNumber(std::optional<TupletNumber> value)
{
    m_tupletNumber = std::move(value);
}

const std::optional<TupletType> &TupletPortion::tupletType() const noexcept
{
    return m_tupletType;
}

void TupletPortion::setTupletType(std::optional<TupletType> value)
{
    m_tupletType = std::move(value);
}

std::span<const TupletDot> TupletPortion::tupletDot() const noexcept
{
    return m_tupletDot;
}

void TupletPortion::addTupletDot(TupletDot value)
{
    m_tupletDot.push_back(std::move(value));
}

void TupletPortion::setTupletDot(std::vector<TupletDot> value)
{
    m_tupletDot = std::move(value);
}

TupletPortion parseTupletPortion(pugi::xml_node el)
{
    return parseTupletPortion(el, ParseContext{});
}

TupletPortion parseTupletPortion(pugi::xml_node el, const ParseContext &context)
{
    TupletPortion out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseTupletPortionContent(out, el, context);
    return out;
}

void parseTupletPortionContent(TupletPortion &out, pugi::xml_node el)
{
    parseTupletPortionContent(out, el, ParseContext{});
}

void parseTupletPortionContent(TupletPortion &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "tuplet-number"))
    {
        out.setTupletNumber(parseTupletNumber(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "tuplet-type"))
    {
        out.setTupletType(parseTupletType(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "tuplet-dot"))
    {
        out.addTupletDot(parseTupletDot(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "tuplet-number") || cursorIs(cursor, "tuplet-type") || cursorIs(cursor, "tuplet-dot"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTupletPortion(const TupletPortion &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeTupletPortionContent(v, el);
}

void serializeTupletPortionContent(const TupletPortion &v, pugi::xml_node el)
{
    if (v.tupletNumber())
    {
        serializeTupletNumber(*v.tupletNumber(), el, "tuplet-number");
    }
    if (v.tupletType())
    {
        serializeTupletType(*v.tupletType(), el, "tuplet-type");
    }
    for (const auto &item : v.tupletDot())
    {
        serializeTupletDot(item, el, "tuplet-dot");
    }
}

} // namespace mx::core
