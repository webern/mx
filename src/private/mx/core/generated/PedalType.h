// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The pedal-type simple type is used to distinguish types of pedal directions. The start value
/// indicates the start of a damper pedal, while the sostenuto value indicates the start of a
/// sostenuto pedal. The other values can be used with either the damper or sostenuto pedal. The soft
/// pedal is not included here because there is no special symbol or graphic used for it beyond what
/// can be specified with words and bracket elements. The change, continue, discontinue, and resume
/// types are used when the line attribute is yes. The change type indicates a pedal lift and retake
/// indicated with an inverted V marking. The continue type allows more precise formatting across
/// system breaks and for more complex pedaling lines. The discontinue type indicates the end of a
/// pedal line that does not include the explicit lift represented by the stop type. The resume type
/// indicates the start of a pedal line that does not include the downstroke represented by the start
/// type. It can be used when a line resumes after being discontinued, or to start a pedal line that
/// is preceded by a text or symbol representation of the pedal.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class PedalType final
{
  public:
    enum class Tag
    {
        start,
        stop,
        sostenuto,
        change,
        continue_,
        discontinue,
        resume,
    };

    /// The natural zero: the first variant.
    PedalType() noexcept = default;

    static PedalType start() noexcept;
    static PedalType stop() noexcept;
    static PedalType sostenuto() noexcept;
    static PedalType change() noexcept;
    static PedalType continue_() noexcept;
    static PedalType discontinue() noexcept;
    static PedalType resume() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, PedalType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static PedalType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static PedalType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const PedalType &other) const noexcept = default;

  private:
    explicit PedalType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::start};
};

} // namespace mx::core
