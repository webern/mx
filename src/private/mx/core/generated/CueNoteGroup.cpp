// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/CueNoteGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Empty &CueNoteGroup::cue() const noexcept
{
    return m_cue;
}

void CueNoteGroup::setCue(Empty value)
{
    m_cue = std::move(value);
}

const FullNoteGroup &CueNoteGroup::fullNote() const noexcept
{
    return m_fullNote;
}

void CueNoteGroup::setFullNote(FullNoteGroup value)
{
    m_fullNote = std::move(value);
}

const PositiveDivisions &CueNoteGroup::duration() const noexcept
{
    return m_duration;
}

void CueNoteGroup::setDuration(PositiveDivisions value)
{
    m_duration = std::move(value);
}

CueNoteGroup parseCueNoteGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseCueNoteGroup(el, cursor, ParseContext{});
}

CueNoteGroup parseCueNoteGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    CueNoteGroup out;
    if (cursorIs(cursor, "cue"))
    {
        out.setCue(parseEmpty(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "cue");
    }
    if (cursor && (cursorIs(cursor, "chord") || cursorIs(cursor, "pitch") || cursorIs(cursor, "unpitched") ||
                   cursorIs(cursor, "rest")))
    {
        out.setFullNote(parseFullNoteGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "chord");
    }
    if (cursorIs(cursor, "duration"))
    {
        out.setDuration(parseValue<PositiveDivisions>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "duration");
    }
    return out;
}

void serializeCueNoteGroup(const CueNoteGroup &v, pugi::xml_node el)
{
    serializeEmpty(v.cue(), el, "cue");
    serializeFullNoteGroup(v.fullNote(), el);
    writeTextElement(el, "duration", v.duration().toString());
}

} // namespace mx::core
