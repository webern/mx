// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Listening.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const ListeningChoice> Listening::choice() const noexcept
{
    return m_choice.items();
}

void Listening::addChoice(ListeningChoice value)
{
    m_choice.add(std::move(value));
}

void Listening::setChoice(OneOrMore<ListeningChoice> value)
{
    m_choice = std::move(value);
}

const std::optional<Offset> &Listening::offset() const noexcept
{
    return m_offset;
}

void Listening::setOffset(std::optional<Offset> value)
{
    m_offset = std::move(value);
}

Listening parseListening(pugi::xml_node el, const ParseContext &context)
{
    Listening out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseListeningContent(out, el, context);
    return out;
}

void parseListeningContent(Listening &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!(cursor && (cursorIs(cursor, "sync") || cursorIs(cursor, "other-listening"))))
    {
        throwMissingElement(el, "sync");
    }
    out.setChoice(OneOrMore<ListeningChoice>{parseListeningChoice(el, cursor, context)});
    while (cursor && (cursorIs(cursor, "sync") || cursorIs(cursor, "other-listening")))
    {
        out.addChoice(parseListeningChoice(el, cursor, context));
    }
    if (cursorIs(cursor, "offset"))
    {
        out.setOffset(parseOffset(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "sync") || cursorIs(cursor, "other-listening") || cursorIs(cursor, "offset"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeListening(const Listening &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeListeningContent(v, el);
}

void serializeListeningContent(const Listening &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializeListeningChoice(item, el);
    }
    if (v.offset())
    {
        serializeOffset(*v.offset(), el, "offset");
    }
}

} // namespace mx::core
