// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/FrameNote.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const String &FrameNote::string() const noexcept
{
    return m_string;
}

void FrameNote::setString(String value)
{
    m_string = std::move(value);
}

const Fret &FrameNote::fret() const noexcept
{
    return m_fret;
}

void FrameNote::setFret(Fret value)
{
    m_fret = std::move(value);
}

const std::optional<Fingering> &FrameNote::fingering() const noexcept
{
    return m_fingering;
}

void FrameNote::setFingering(std::optional<Fingering> value)
{
    m_fingering = std::move(value);
}

const std::optional<Barre> &FrameNote::barre() const noexcept
{
    return m_barre;
}

void FrameNote::setBarre(std::optional<Barre> value)
{
    m_barre = std::move(value);
}

FrameNote parseFrameNote(pugi::xml_node el)
{
    return parseFrameNote(el, ParseContext{});
}

FrameNote parseFrameNote(pugi::xml_node el, const ParseContext &context)
{
    FrameNote out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseFrameNoteContent(out, el, context);
    return out;
}

void parseFrameNoteContent(FrameNote &out, pugi::xml_node el)
{
    parseFrameNoteContent(out, el, ParseContext{});
}

void parseFrameNoteContent(FrameNote &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "string"))
    {
        out.setString(parseString(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "string");
    }
    if (cursorIs(cursor, "fret"))
    {
        out.setFret(parseFret(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "fret");
    }
    if (cursorIs(cursor, "fingering"))
    {
        out.setFingering(parseFingering(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "barre"))
    {
        out.setBarre(parseBarre(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "string") || cursorIs(cursor, "fret") || cursorIs(cursor, "fingering") ||
            cursorIs(cursor, "barre"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeFrameNote(const FrameNote &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeFrameNoteContent(v, el);
}

void serializeFrameNoteContent(const FrameNote &v, pugi::xml_node el)
{
    serializeString(v.string(), el, "string");
    serializeFret(v.fret(), el, "fret");
    if (v.fingering())
    {
        serializeFingering(*v.fingering(), el, "fingering");
    }
    if (v.barre())
    {
        serializeBarre(*v.barre(), el, "barre");
    }
}

} // namespace mx::core
