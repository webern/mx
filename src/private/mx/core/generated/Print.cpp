// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Print.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &Print::staffSpacing() const noexcept
{
    return m_staffSpacing;
}

void Print::setStaffSpacing(std::optional<Tenths> value)
{
    m_staffSpacing = std::move(value);
}

const std::optional<YesNo> &Print::newSystem() const noexcept
{
    return m_newSystem;
}

void Print::setNewSystem(std::optional<YesNo> value)
{
    m_newSystem = std::move(value);
}

const std::optional<YesNo> &Print::newPage() const noexcept
{
    return m_newPage;
}

void Print::setNewPage(std::optional<YesNo> value)
{
    m_newPage = std::move(value);
}

const std::optional<int> &Print::blankPage() const noexcept
{
    return m_blankPage;
}

void Print::setBlankPage(std::optional<int> value)
{
    m_blankPage = std::move(value);
}

const std::optional<std::string> &Print::pageNumber() const noexcept
{
    return m_pageNumber;
}

void Print::setPageNumber(std::optional<std::string> value)
{
    m_pageNumber = std::move(value);
}

const std::optional<Token> &Print::id() const noexcept
{
    return m_id;
}

void Print::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const LayoutGroup &Print::layout() const noexcept
{
    return m_layout;
}

void Print::setLayout(LayoutGroup value)
{
    m_layout = std::move(value);
}

const std::optional<MeasureLayout> &Print::measureLayout() const noexcept
{
    return m_measureLayout;
}

void Print::setMeasureLayout(std::optional<MeasureLayout> value)
{
    m_measureLayout = std::move(value);
}

const std::optional<MeasureNumbering> &Print::measureNumbering() const noexcept
{
    return m_measureNumbering;
}

void Print::setMeasureNumbering(std::optional<MeasureNumbering> value)
{
    m_measureNumbering = std::move(value);
}

const std::optional<NameDisplay> &Print::partNameDisplay() const noexcept
{
    return m_partNameDisplay;
}

void Print::setPartNameDisplay(std::optional<NameDisplay> value)
{
    m_partNameDisplay = std::move(value);
}

const std::optional<NameDisplay> &Print::partAbbreviationDisplay() const noexcept
{
    return m_partAbbreviationDisplay;
}

void Print::setPartAbbreviationDisplay(std::optional<NameDisplay> value)
{
    m_partAbbreviationDisplay = std::move(value);
}

Print parsePrint(pugi::xml_node el, const ParseContext &context)
{
    Print out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "staff-spacing")
        {
            out.setStaffSpacing(parseValue<Tenths>(a.value(), context, el, "staff-spacing"));
        }
        else if (aname == "new-system")
        {
            out.setNewSystem(parseValue<YesNo>(a.value(), context, el, "new-system"));
        }
        else if (aname == "new-page")
        {
            out.setNewPage(parseValue<YesNo>(a.value(), context, el, "new-page"));
        }
        else if (aname == "blank-page")
        {
            out.setBlankPage(parseIntegerValue(a.value(), context, el, "blank-page"));
        }
        else if (aname == "page-number")
        {
            out.setPageNumber(std::string{a.value()});
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parsePrintContent(out, el, context);
    return out;
}

void parsePrintContent(Print &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor &&
        (cursorIs(cursor, "page-layout") || cursorIs(cursor, "system-layout") || cursorIs(cursor, "staff-layout")))
    {
        out.setLayout(parseLayoutGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "measure-layout"))
    {
        out.setMeasureLayout(parseMeasureLayout(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "measure-numbering"))
    {
        out.setMeasureNumbering(parseMeasureNumbering(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-name-display"))
    {
        out.setPartNameDisplay(parseNameDisplay(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-abbreviation-display"))
    {
        out.setPartAbbreviationDisplay(parseNameDisplay(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "page-layout") || cursorIs(cursor, "system-layout") || cursorIs(cursor, "staff-layout") ||
            cursorIs(cursor, "measure-layout") || cursorIs(cursor, "measure-numbering") ||
            cursorIs(cursor, "part-name-display") || cursorIs(cursor, "part-abbreviation-display"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePrint(const Print &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.staffSpacing())
    {
        setAttribute(el, "staff-spacing", v.staffSpacing()->toString());
    }
    if (v.newSystem())
    {
        setAttribute(el, "new-system", v.newSystem()->toString());
    }
    if (v.newPage())
    {
        setAttribute(el, "new-page", v.newPage()->toString());
    }
    if (v.blankPage())
    {
        setAttribute(el, "blank-page", formatInt(*v.blankPage()));
    }
    if (v.pageNumber())
    {
        setAttribute(el, "page-number", *v.pageNumber());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializePrintContent(v, el);
}

void serializePrintContent(const Print &v, pugi::xml_node el)
{
    serializeLayoutGroup(v.layout(), el);
    if (v.measureLayout())
    {
        serializeMeasureLayout(*v.measureLayout(), el, "measure-layout");
    }
    if (v.measureNumbering())
    {
        serializeMeasureNumbering(*v.measureNumbering(), el, "measure-numbering");
    }
    if (v.partNameDisplay())
    {
        serializeNameDisplay(*v.partNameDisplay(), el, "part-name-display");
    }
    if (v.partAbbreviationDisplay())
    {
        serializeNameDisplay(*v.partAbbreviationDisplay(), el, "part-abbreviation-display");
    }
}

} // namespace mx::core
