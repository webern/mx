// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"
#include "mx/core/generated/PositiveIntegerOrEmpty.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class VirtualInstrumentDataGroupChoice final
{
  public:
    enum class Kind
    {
        solo,
        ensemble,
    };

    /// The natural zero: the first alternative.
    VirtualInstrumentDataGroupChoice();

    static VirtualInstrumentDataGroupChoice solo(Empty value);
    static VirtualInstrumentDataGroupChoice ensemble(PositiveIntegerOrEmpty value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isSolo() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isSolo().
    const Empty &asSolo() const noexcept
    {
        assert(isSolo());
        return *std::get_if<0>(&m_value);
    }

    bool isEnsemble() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isEnsemble().
    const PositiveIntegerOrEmpty &asEnsemble() const noexcept
    {
        assert(isEnsemble());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const VirtualInstrumentDataGroupChoice &other) const = default;

  private:
    using Storage = std::variant<Empty, PositiveIntegerOrEmpty>;

    explicit VirtualInstrumentDataGroupChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
VirtualInstrumentDataGroupChoice parseVirtualInstrumentDataGroupChoice(pugi::xml_node el, pugi::xml_node &cursor);

VirtualInstrumentDataGroupChoice parseVirtualInstrumentDataGroupChoice(pugi::xml_node el, pugi::xml_node &cursor,
                                                                       const ParseContext &context);

void serializeVirtualInstrumentDataGroupChoice(const VirtualInstrumentDataGroupChoice &v, pugi::xml_node el);

} // namespace mx::core
