// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The note-size-type type indicates the type of note being defined by a note-size element. The
/// grace-cue type is used for notes of grace-cue size. The grace type is used for notes of cue size
/// that include a grace element. The cue type is used for all other notes with cue size, whether
/// defined explicitly or implicitly via a cue element. The large type is used for notes of large
/// size.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class NoteSizeType final
{
  public:
    enum class Tag
    {
        cue,
        grace,
        graceCue,
        large,
    };

    /// The natural zero: the first variant.
    NoteSizeType() noexcept = default;

    static NoteSizeType cue() noexcept;
    static NoteSizeType grace() noexcept;
    static NoteSizeType graceCue() noexcept;
    static NoteSizeType large() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, NoteSizeType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static NoteSizeType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static NoteSizeType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const NoteSizeType &other) const noexcept = default;

  private:
    explicit NoteSizeType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::cue};
};

} // namespace mx::core
