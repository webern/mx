// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/HarmonMute.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &HarmonMute::defaultX() const noexcept
{
    return m_defaultX;
}

void HarmonMute::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &HarmonMute::defaultY() const noexcept
{
    return m_defaultY;
}

void HarmonMute::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &HarmonMute::relativeX() const noexcept
{
    return m_relativeX;
}

void HarmonMute::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &HarmonMute::relativeY() const noexcept
{
    return m_relativeY;
}

void HarmonMute::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &HarmonMute::fontFamily() const noexcept
{
    return m_fontFamily;
}

void HarmonMute::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &HarmonMute::fontStyle() const noexcept
{
    return m_fontStyle;
}

void HarmonMute::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &HarmonMute::fontSize() const noexcept
{
    return m_fontSize;
}

void HarmonMute::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &HarmonMute::fontWeight() const noexcept
{
    return m_fontWeight;
}

void HarmonMute::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &HarmonMute::color() const noexcept
{
    return m_color;
}

void HarmonMute::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<AboveBelow> &HarmonMute::placement() const noexcept
{
    return m_placement;
}

void HarmonMute::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const HarmonClosed &HarmonMute::harmonClosed() const noexcept
{
    return m_harmonClosed;
}

void HarmonMute::setHarmonClosed(HarmonClosed value)
{
    m_harmonClosed = std::move(value);
}

HarmonMute parseHarmonMute(pugi::xml_node el)
{
    return parseHarmonMute(el, ParseContext{});
}

HarmonMute parseHarmonMute(pugi::xml_node el, const ParseContext &context)
{
    HarmonMute out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHarmonMuteContent(out, el, context);
    return out;
}

void parseHarmonMuteContent(HarmonMute &out, pugi::xml_node el)
{
    parseHarmonMuteContent(out, el, ParseContext{});
}

void parseHarmonMuteContent(HarmonMute &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "harmon-closed"))
    {
        out.setHarmonClosed(parseHarmonClosed(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "harmon-closed");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "harmon-closed"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeHarmonMute(const HarmonMute &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    serializeHarmonMuteContent(v, el);
}

void serializeHarmonMuteContent(const HarmonMute &v, pugi::xml_node el)
{
    serializeHarmonClosed(v.harmonClosed(), el, "harmon-closed");
}

} // namespace mx::core
