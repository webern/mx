// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The glass-value type represents pictograms for glass percussion instruments.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class GlassValue final
{
  public:
    enum class Tag
    {
        glassHarmonica,
        glassHarp,
        windChimes,
    };

    /// The natural zero: the first variant.
    GlassValue() noexcept = default;

    static GlassValue glassHarmonica() noexcept;
    static GlassValue glassHarp() noexcept;
    static GlassValue windChimes() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, GlassValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static GlassValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static GlassValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const GlassValue &other) const noexcept = default;

  private:
    explicit GlassValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::glassHarmonica};
};

} // namespace mx::core
