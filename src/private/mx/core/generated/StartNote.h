// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The start-note type describes the starting note of trills and mordents for playback, relative to
/// the current note.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StartNote final
{
  public:
    enum class Tag
    {
        upper,
        main,
        below,
    };

    /// The natural zero: the first variant.
    StartNote() noexcept = default;

    static StartNote upper() noexcept;
    static StartNote main() noexcept;
    static StartNote below() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StartNote &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StartNote parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StartNote parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StartNote &other) const noexcept = default;

  private:
    explicit StartNote(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::upper};
};

} // namespace mx::core
