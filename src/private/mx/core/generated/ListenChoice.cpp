// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ListenChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

ListenChoice::ListenChoice()
{
    *this = ListenChoice::assess(Assess{});
}

ListenChoice ListenChoice::assess(Assess value)
{
    return ListenChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

ListenChoice ListenChoice::wait(Wait value)
{
    return ListenChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

ListenChoice ListenChoice::otherListen(OtherListening value)
{
    return ListenChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

ListenChoice parseListenChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "assess")))
    {
        Assess value = parseAssess(cursor, context);
        cursor = nextElement(cursor);
        return ListenChoice::assess(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "wait")))
    {
        Wait value = parseWait(cursor, context);
        cursor = nextElement(cursor);
        return ListenChoice::wait(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-listen")))
    {
        OtherListening value = parseOtherListening(cursor, context);
        cursor = nextElement(cursor);
        return ListenChoice::otherListen(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "assess");
}

void serializeListenChoice(const ListenChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case ListenChoice::Kind::assess:
        serializeAssess(v.asAssess(), el, "assess");
        break;
    case ListenChoice::Kind::wait:
        serializeWait(v.asWait(), el, "wait");
        break;
    case ListenChoice::Kind::otherListen:
        serializeOtherListening(v.asOtherListen(), el, "other-listen");
        break;
    }
}

} // namespace mx::core
