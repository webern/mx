// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The principal-voice-symbol type represents the type of symbol used to indicate a principal or
/// secondary voice. The "plain" value represents a plain square bracket. The value of "none" is used
/// for analysis markup when the principal-voice element does not have a corresponding appearance in
/// the score.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class PrincipalVoiceSymbol final
{
  public:
    enum class Tag
    {
        hauptstimme,
        nebenstimme,
        plain,
        none,
    };

    /// The natural zero: the first variant.
    PrincipalVoiceSymbol() noexcept = default;

    static PrincipalVoiceSymbol hauptstimme() noexcept;
    static PrincipalVoiceSymbol nebenstimme() noexcept;
    static PrincipalVoiceSymbol plain() noexcept;
    static PrincipalVoiceSymbol none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, PrincipalVoiceSymbol &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static PrincipalVoiceSymbol parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static PrincipalVoiceSymbol parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const PrincipalVoiceSymbol &other) const noexcept = default;

  private:
    explicit PrincipalVoiceSymbol(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::hauptstimme};
};

} // namespace mx::core
