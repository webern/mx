// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/LyricChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

LyricChoice::LyricChoice()
{
    *this = LyricChoice::lyricTextGroup(LyricTextGroup{});
}

LyricChoice LyricChoice::lyricTextGroup(LyricTextGroup value)
{
    return LyricChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

LyricChoice LyricChoice::extend(Extend value)
{
    return LyricChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

LyricChoice LyricChoice::laughing(Empty value)
{
    return LyricChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

LyricChoice LyricChoice::humming(Empty value)
{
    return LyricChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

LyricChoice parseLyricChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseLyricChoice(el, cursor, ParseContext{});
}

LyricChoice parseLyricChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "syllabic") || cursorIs(cursor, "text")))
    {
        return LyricChoice::lyricTextGroup(parseLyricTextGroup(el, cursor, context));
    }
    if (cursor && (cursorIs(cursor, "extend")))
    {
        Extend value = parseExtend(cursor, context);
        cursor = nextElement(cursor);
        return LyricChoice::extend(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "laughing")))
    {
        Empty value = parseEmpty(cursor, context);
        cursor = nextElement(cursor);
        return LyricChoice::laughing(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "humming")))
    {
        Empty value = parseEmpty(cursor, context);
        cursor = nextElement(cursor);
        return LyricChoice::humming(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "syllabic");
}

void serializeLyricChoice(const LyricChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case LyricChoice::Kind::lyricTextGroup:
        serializeLyricTextGroup(v.asLyricTextGroup(), el);
        break;
    case LyricChoice::Kind::extend:
        serializeExtend(v.asExtend(), el, "extend");
        break;
    case LyricChoice::Kind::laughing:
        serializeEmpty(v.asLaughing(), el, "laughing");
        break;
    case LyricChoice::Kind::humming:
        serializeEmpty(v.asHumming(), el, "humming");
        break;
    }
}

} // namespace mx::core
