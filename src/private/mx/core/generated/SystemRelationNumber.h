// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The system-relation-number type distinguishes measure numbers that are associated with a system
/// rather than the particular part where the element appears. A value of only-top or only-bottom
/// indicates that the number should appear only on the top or bottom part of the current system,
/// respectively. A value of also-top or also-bottom indicates that the number should appear on both
/// the current part and the top or bottom part of the current system, respectively. If these values
/// appear in a score, when parts are created the number should only appear once in this part, not
/// twice. A value of none indicates that the number is associated only with the current part, not
/// with the system.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class SystemRelationNumber final
{
  public:
    enum class Tag
    {
        onlyTop,
        onlyBottom,
        alsoTop,
        alsoBottom,
        none,
    };

    /// The natural zero: the first variant.
    SystemRelationNumber() noexcept = default;

    static SystemRelationNumber onlyTop() noexcept;
    static SystemRelationNumber onlyBottom() noexcept;
    static SystemRelationNumber alsoTop() noexcept;
    static SystemRelationNumber alsoBottom() noexcept;
    static SystemRelationNumber none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, SystemRelationNumber &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static SystemRelationNumber parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static SystemRelationNumber parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const SystemRelationNumber &other) const noexcept = default;

  private:
    explicit SystemRelationNumber(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::onlyTop};
};

} // namespace mx::core
