// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The stick-material type represents the material being displayed in a stick pictogram.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StickMaterial final
{
  public:
    enum class Tag
    {
        soft,
        medium,
        hard,
        shaded,
        x,
    };

    /// The natural zero: the first variant.
    StickMaterial() noexcept = default;

    static StickMaterial soft() noexcept;
    static StickMaterial medium() noexcept;
    static StickMaterial hard() noexcept;
    static StickMaterial shaded() noexcept;
    static StickMaterial x() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StickMaterial &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StickMaterial parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StickMaterial parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StickMaterial &other) const noexcept = default;

  private:
    explicit StickMaterial(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::soft};
};

} // namespace mx::core
