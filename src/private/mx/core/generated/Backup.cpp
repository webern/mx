// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Backup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const PositiveDivisions &Backup::duration() const noexcept
{
    return m_duration;
}

void Backup::setDuration(PositiveDivisions value)
{
    m_duration = std::move(value);
}

const EditorialGroup &Backup::editorial() const noexcept
{
    return m_editorial;
}

void Backup::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

Backup parseBackup(pugi::xml_node el, const ParseContext &context)
{
    Backup out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseBackupContent(out, el, context);
    return out;
}

void parseBackupContent(Backup &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "duration"))
    {
        out.setDuration(parseValue<PositiveDivisions>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "duration");
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "duration") || cursorIs(cursor, "footnote") || cursorIs(cursor, "level"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeBackup(const Backup &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeBackupContent(v, el);
}

void serializeBackupContent(const Backup &v, pugi::xml_node el)
{
    writeTextElement(el, "duration", v.duration().toString());
    serializeEditorialGroup(v.editorial(), el);
}

} // namespace mx::core
