// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/StaffNumber.h"

#include "mx/core/Lexical.h"

#include <utility>

namespace mx::core
{

int clampedStaffNumber(int v)
{
    if (v < 1)
    {
        v = 1;
    }
    return v;
}

StaffNumber::StaffNumber() : m_value{clampedStaffNumber(int{})}
{
}

StaffNumber::StaffNumber(int value) : m_value{clampedStaffNumber(std::move(value))}
{
}

void StaffNumber::setValue(int value)
{
    m_value = clampedStaffNumber(std::move(value));
}

std::string StaffNumber::toString() const
{
    return formatInt(m_value);
}

bool StaffNumber::tryParse(std::string_view text, StaffNumber &out)
{
    ValueParseOutcome outcome = ValueParseOutcome::valid;
    StaffNumber parsed = parse(text, outcome);
    if (outcome == ValueParseOutcome::invalid)
    {
        return false;
    }
    out = std::move(parsed);
    return true;
}

StaffNumber StaffNumber::parse(std::string_view text)
{
    ValueParseOutcome outcome = ValueParseOutcome::valid;
    return parse(text, outcome);
}

StaffNumber StaffNumber::parse(std::string_view text, ValueParseOutcome &outcome)
{
    int v{};
    if (!tryParseInt(text, v))
    {
        outcome = ValueParseOutcome::invalid;
        return StaffNumber{};
    }
    StaffNumber out{v};
    outcome = out.value() == v ? ValueParseOutcome::valid : ValueParseOutcome::adjusted;
    return out;
}

} // namespace mx::core
