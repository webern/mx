// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The bar-style type represents barline style information. Choices are regular, dotted, dashed,
/// heavy, light-light, light-heavy, heavy-light, heavy-heavy, tick (a short stroke through the top
/// line), short (a partial barline between the 2nd and 4th lines), and none.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class BarStyle final
{
  public:
    enum class Tag
    {
        regular,
        dotted,
        dashed,
        heavy,
        lightLight,
        lightHeavy,
        heavyLight,
        heavyHeavy,
        tick,
        short_,
        none,
    };

    /// The natural zero: the first variant.
    BarStyle() noexcept = default;

    static BarStyle regular() noexcept;
    static BarStyle dotted() noexcept;
    static BarStyle dashed() noexcept;
    static BarStyle heavy() noexcept;
    static BarStyle lightLight() noexcept;
    static BarStyle lightHeavy() noexcept;
    static BarStyle heavyLight() noexcept;
    static BarStyle heavyHeavy() noexcept;
    static BarStyle tick() noexcept;
    static BarStyle short_() noexcept;
    static BarStyle none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, BarStyle &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static BarStyle parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static BarStyle parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const BarStyle &other) const noexcept = default;

  private:
    explicit BarStyle(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::regular};
};

} // namespace mx::core
