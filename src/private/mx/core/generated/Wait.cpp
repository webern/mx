// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Wait.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &Wait::player() const noexcept
{
    return m_player;
}

void Wait::setPlayer(std::optional<Token> value)
{
    m_player = std::move(value);
}

const std::optional<TimeOnly> &Wait::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Wait::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

Wait parseWait(pugi::xml_node el, const ParseContext &context)
{
    Wait out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "player")
        {
            out.setPlayer(parseValue<Token>(a.value(), context, el, "player"));
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(parseValue<TimeOnly>(a.value(), context, el, "time-only"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseWaitContent(out, el, context);
    return out;
}

void parseWaitContent(Wait &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeWait(const Wait &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.player())
    {
        setAttribute(el, "player", v.player()->toString());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    serializeWaitContent(v, el);
}

void serializeWaitContent(const Wait &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
