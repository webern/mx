// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/GroupBarline.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Color> &GroupBarline::color() const noexcept
{
    return m_color;
}

void GroupBarline::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const GroupBarlineValue &GroupBarline::value() const noexcept
{
    return m_value;
}

void GroupBarline::setValue(GroupBarlineValue value)
{
    m_value = std::move(value);
}

GroupBarline parseGroupBarline(pugi::xml_node el, const ParseContext &context)
{
    GroupBarline out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseGroupBarlineContent(out, el, context);
    return out;
}

void parseGroupBarlineContent(GroupBarline &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<GroupBarlineValue>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeGroupBarline(const GroupBarline &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeGroupBarlineContent(v, el);
}

void serializeGroupBarlineContent(const GroupBarline &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
