// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The beater-value type represents pictograms for beaters, mallets, and sticks that do not have
/// different materials represented in the pictogram. The finger and hammer values are in addition to
/// Stone's list.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class BeaterValue final
{
  public:
    enum class Tag
    {
        bow,
        chimeHammer,
        coin,
        drumStick,
        finger,
        fingernail,
        fist,
        guiroScraper,
        hammer,
        hand,
        jazzStick,
        knittingNeedle,
        metalHammer,
        slideBrushOnGong,
        snareStick,
        spoonMallet,
        superball,
        triangleBeater,
        triangleBeaterPlain,
        wireBrush,
    };

    /// The natural zero: the first variant.
    BeaterValue() noexcept = default;

    static BeaterValue bow() noexcept;
    static BeaterValue chimeHammer() noexcept;
    static BeaterValue coin() noexcept;
    static BeaterValue drumStick() noexcept;
    static BeaterValue finger() noexcept;
    static BeaterValue fingernail() noexcept;
    static BeaterValue fist() noexcept;
    static BeaterValue guiroScraper() noexcept;
    static BeaterValue hammer() noexcept;
    static BeaterValue hand() noexcept;
    static BeaterValue jazzStick() noexcept;
    static BeaterValue knittingNeedle() noexcept;
    static BeaterValue metalHammer() noexcept;
    static BeaterValue slideBrushOnGong() noexcept;
    static BeaterValue snareStick() noexcept;
    static BeaterValue spoonMallet() noexcept;
    static BeaterValue superball() noexcept;
    static BeaterValue triangleBeater() noexcept;
    static BeaterValue triangleBeaterPlain() noexcept;
    static BeaterValue wireBrush() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, BeaterValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static BeaterValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static BeaterValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const BeaterValue &other) const noexcept = default;

  private:
    explicit BeaterValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::bow};
};

} // namespace mx::core
