// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ShowType final
{
  public:
    enum class Tag
    {
        new_,
        replace,
        embed,
        other,
        none,
    };

    /// The natural zero: the first variant.
    ShowType() noexcept = default;

    static ShowType new_() noexcept;
    static ShowType replace() noexcept;
    static ShowType embed() noexcept;
    static ShowType other() noexcept;
    static ShowType none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ShowType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ShowType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ShowType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ShowType &other) const noexcept = default;

  private:
    explicit ShowType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::new_};
};

} // namespace mx::core
