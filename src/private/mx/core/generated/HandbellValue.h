// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The handbell-value type represents the type of handbell technique being notated.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class HandbellValue final
{
  public:
    enum class Tag
    {
        belltree,
        damp,
        echo,
        gyro,
        handMartellato,
        malletLift,
        malletTable,
        martellato,
        martellatoLift,
        mutedMartellato,
        pluckLift,
        swing,
    };

    /// The natural zero: the first variant.
    HandbellValue() noexcept = default;

    static HandbellValue belltree() noexcept;
    static HandbellValue damp() noexcept;
    static HandbellValue echo() noexcept;
    static HandbellValue gyro() noexcept;
    static HandbellValue handMartellato() noexcept;
    static HandbellValue malletLift() noexcept;
    static HandbellValue malletTable() noexcept;
    static HandbellValue martellato() noexcept;
    static HandbellValue martellatoLift() noexcept;
    static HandbellValue mutedMartellato() noexcept;
    static HandbellValue pluckLift() noexcept;
    static HandbellValue swing() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, HandbellValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static HandbellValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static HandbellValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const HandbellValue &other) const noexcept = default;

  private:
    explicit HandbellValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::belltree};
};

} // namespace mx::core
