// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MeasureStyleChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

MeasureStyleChoice::MeasureStyleChoice()
{
    *this = MeasureStyleChoice::multipleRest(MultipleRest{});
}

MeasureStyleChoice MeasureStyleChoice::multipleRest(MultipleRest value)
{
    return MeasureStyleChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

MeasureStyleChoice MeasureStyleChoice::measureRepeat(MeasureRepeat value)
{
    return MeasureStyleChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

MeasureStyleChoice MeasureStyleChoice::beatRepeat(BeatRepeat value)
{
    return MeasureStyleChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

MeasureStyleChoice MeasureStyleChoice::slash(Slash value)
{
    return MeasureStyleChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

MeasureStyleChoice parseMeasureStyleChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "multiple-rest")))
    {
        MultipleRest value = parseMultipleRest(cursor, context);
        cursor = nextElement(cursor);
        return MeasureStyleChoice::multipleRest(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "measure-repeat")))
    {
        MeasureRepeat value = parseMeasureRepeat(cursor, context);
        cursor = nextElement(cursor);
        return MeasureStyleChoice::measureRepeat(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "beat-repeat")))
    {
        BeatRepeat value = parseBeatRepeat(cursor, context);
        cursor = nextElement(cursor);
        return MeasureStyleChoice::beatRepeat(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "slash")))
    {
        Slash value = parseSlash(cursor, context);
        cursor = nextElement(cursor);
        return MeasureStyleChoice::slash(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "multiple-rest");
}

void serializeMeasureStyleChoice(const MeasureStyleChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case MeasureStyleChoice::Kind::multipleRest:
        serializeMultipleRest(v.asMultipleRest(), el, "multiple-rest");
        break;
    case MeasureStyleChoice::Kind::measureRepeat:
        serializeMeasureRepeat(v.asMeasureRepeat(), el, "measure-repeat");
        break;
    case MeasureStyleChoice::Kind::beatRepeat:
        serializeBeatRepeat(v.asBeatRepeat(), el, "beat-repeat");
        break;
    case MeasureStyleChoice::Kind::slash:
        serializeSlash(v.asSlash(), el, "slash");
        break;
    }
}

} // namespace mx::core
