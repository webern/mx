// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The show-tuplet type indicates whether to show a part of a tuplet relating to the tuplet-actual
/// element, both the tuplet-actual and tuplet-normal elements, or neither.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ShowTuplet final
{
  public:
    enum class Tag
    {
        actual,
        both,
        none,
    };

    /// The natural zero: the first variant.
    ShowTuplet() noexcept = default;

    static ShowTuplet actual() noexcept;
    static ShowTuplet both() noexcept;
    static ShowTuplet none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ShowTuplet &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ShowTuplet parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ShowTuplet parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ShowTuplet &other) const noexcept = default;

  private:
    explicit ShowTuplet(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::actual};
};

} // namespace mx::core
