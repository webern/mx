// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The system-relation type distinguishes elements that are associated with a system rather than the
/// particular part where the element appears. A value of only-top indicates that the element should
/// appear only on the top part of the current system. A value of also-top indicates that the element
/// should appear on both the current part and the top part of the current system. If this value
/// appears in a score, when parts are created the element should only appear once in this part, not
/// twice. A value of none indicates that the element is associated only with the current part, not
/// with the system.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class SystemRelation final
{
  public:
    enum class Tag
    {
        onlyTop,
        alsoTop,
        none,
    };

    /// The natural zero: the first variant.
    SystemRelation() noexcept = default;

    static SystemRelation onlyTop() noexcept;
    static SystemRelation alsoTop() noexcept;
    static SystemRelation none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, SystemRelation &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static SystemRelation parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static SystemRelation parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const SystemRelation &other) const noexcept = default;

  private:
    explicit SystemRelation(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::onlyTop};
};

} // namespace mx::core
