// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Bend.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<BendShape> &Bend::shape() const noexcept
{
    return m_shape;
}

void Bend::setShape(std::optional<BendShape> value)
{
    m_shape = std::move(value);
}

const std::optional<Tenths> &Bend::defaultX() const noexcept
{
    return m_defaultX;
}

void Bend::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Bend::defaultY() const noexcept
{
    return m_defaultY;
}

void Bend::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Bend::relativeX() const noexcept
{
    return m_relativeX;
}

void Bend::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Bend::relativeY() const noexcept
{
    return m_relativeY;
}

void Bend::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Bend::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Bend::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Bend::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Bend::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Bend::fontSize() const noexcept
{
    return m_fontSize;
}

void Bend::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Bend::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Bend::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Bend::color() const noexcept
{
    return m_color;
}

void Bend::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<YesNo> &Bend::accelerate() const noexcept
{
    return m_accelerate;
}

void Bend::setAccelerate(std::optional<YesNo> value)
{
    m_accelerate = std::move(value);
}

const std::optional<TrillBeats> &Bend::beats() const noexcept
{
    return m_beats;
}

void Bend::setBeats(std::optional<TrillBeats> value)
{
    m_beats = std::move(value);
}

const std::optional<Percent> &Bend::firstBeat() const noexcept
{
    return m_firstBeat;
}

void Bend::setFirstBeat(std::optional<Percent> value)
{
    m_firstBeat = std::move(value);
}

const std::optional<Percent> &Bend::lastBeat() const noexcept
{
    return m_lastBeat;
}

void Bend::setLastBeat(std::optional<Percent> value)
{
    m_lastBeat = std::move(value);
}

const Semitones &Bend::bendAlter() const noexcept
{
    return m_bendAlter;
}

void Bend::setBendAlter(Semitones value)
{
    m_bendAlter = std::move(value);
}

const std::optional<BendChoice> &Bend::choice() const noexcept
{
    return m_choice;
}

void Bend::setChoice(std::optional<BendChoice> value)
{
    m_choice = std::move(value);
}

const std::optional<PlacementText> &Bend::withBar() const noexcept
{
    return m_withBar;
}

void Bend::setWithBar(std::optional<PlacementText> value)
{
    m_withBar = std::move(value);
}

Bend parseBend(pugi::xml_node el, const ParseContext &context)
{
    Bend out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "shape")
        {
            out.setShape(parseValue<BendShape>(a.value(), context, el, "shape"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "accelerate")
        {
            out.setAccelerate(parseValue<YesNo>(a.value(), context, el, "accelerate"));
        }
        else if (aname == "beats")
        {
            out.setBeats(parseValue<TrillBeats>(a.value(), context, el, "beats"));
        }
        else if (aname == "first-beat")
        {
            out.setFirstBeat(parseValue<Percent>(a.value(), context, el, "first-beat"));
        }
        else if (aname == "last-beat")
        {
            out.setLastBeat(parseValue<Percent>(a.value(), context, el, "last-beat"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseBendContent(out, el, context);
    return out;
}

void parseBendContent(Bend &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "bend-alter"))
    {
        out.setBendAlter(parseValue<Semitones>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "bend-alter");
    }
    if (cursor && (cursorIs(cursor, "pre-bend") || cursorIs(cursor, "release")))
    {
        out.setChoice(parseBendChoice(el, cursor, context));
    }
    if (cursorIs(cursor, "with-bar"))
    {
        out.setWithBar(parsePlacementText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "bend-alter") || cursorIs(cursor, "pre-bend") || cursorIs(cursor, "release") ||
            cursorIs(cursor, "with-bar"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeBend(const Bend &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.shape())
    {
        setAttribute(el, "shape", v.shape()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.accelerate())
    {
        setAttribute(el, "accelerate", v.accelerate()->toString());
    }
    if (v.beats())
    {
        setAttribute(el, "beats", v.beats()->toString());
    }
    if (v.firstBeat())
    {
        setAttribute(el, "first-beat", v.firstBeat()->toString());
    }
    if (v.lastBeat())
    {
        setAttribute(el, "last-beat", v.lastBeat()->toString());
    }
    serializeBendContent(v, el);
}

void serializeBendContent(const Bend &v, pugi::xml_node el)
{
    writeTextElement(el, "bend-alter", v.bendAlter().toString());
    if (v.choice())
    {
        serializeBendChoice(*v.choice(), el);
    }
    if (v.withBar())
    {
        serializePlacementText(*v.withBar(), el, "with-bar");
    }
}

} // namespace mx::core
