// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/HarmonyAlter.h"
#include "mx/core/generated/RootStep.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The root type indicates a pitch like C, D, E vs. a scale degree like 1, 2, 3. It is used with
/// chord symbols in popular music. The root element has a root-step and optional root-alter element
/// similar to the step and alter elements, but renamed to distinguish the different musical
/// meanings.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Root final
{
  public:
    const RootStep &rootStep() const noexcept;
    void setRootStep(RootStep value);
    const std::optional<HarmonyAlter> &rootAlter() const noexcept;
    void setRootAlter(std::optional<HarmonyAlter> value);
    bool operator==(const Root &other) const = default;

  private:
    RootStep m_rootStep{};
    std::optional<HarmonyAlter> m_rootAlter;
};

Root parseRoot(pugi::xml_node el, const ParseContext &context);

void parseRootContent(Root &out, pugi::xml_node el, const ParseContext &context);

void serializeRoot(const Root &v, pugi::xml_node parent, const char *tag);

void serializeRootContent(const Root &v, pugi::xml_node el);

} // namespace mx::core
