// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The above-below type is used to indicate whether one element appears above or below another
/// element.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class AboveBelow final
{
  public:
    enum class Tag
    {
        above,
        below,
    };

    /// The natural zero: the first variant.
    AboveBelow() noexcept = default;

    static AboveBelow above() noexcept;
    static AboveBelow below() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, AboveBelow &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static AboveBelow parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static AboveBelow parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const AboveBelow &other) const noexcept = default;

  private:
    explicit AboveBelow(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::above};
};

} // namespace mx::core
