// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The tip-direction type represents the direction in which the tip of a stick or beater points,
/// using Unicode arrow terminology.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TipDirection final
{
  public:
    enum class Tag
    {
        up,
        down,
        left,
        right,
        northwest,
        northeast,
        southeast,
        southwest,
    };

    /// The natural zero: the first variant.
    TipDirection() noexcept = default;

    static TipDirection up() noexcept;
    static TipDirection down() noexcept;
    static TipDirection left() noexcept;
    static TipDirection right() noexcept;
    static TipDirection northwest() noexcept;
    static TipDirection northeast() noexcept;
    static TipDirection southeast() noexcept;
    static TipDirection southwest() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TipDirection &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TipDirection parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TipDirection parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TipDirection &other) const noexcept = default;

  private:
    explicit TipDirection(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::up};
};

} // namespace mx::core
