// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/LineWidth.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const LineWidthType &LineWidth::type() const noexcept
{
    return m_type;
}

void LineWidth::setType(LineWidthType value)
{
    m_type = std::move(value);
}

const Tenths &LineWidth::value() const noexcept
{
    return m_value;
}

void LineWidth::setValue(Tenths value)
{
    m_value = std::move(value);
}

LineWidth parseLineWidth(pugi::xml_node el)
{
    return parseLineWidth(el, ParseContext{});
}

LineWidth parseLineWidth(pugi::xml_node el, const ParseContext &context)
{
    LineWidth out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<LineWidthType>(a.value(), context, el, "type"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseLineWidthContent(out, el, context);
    return out;
}

void parseLineWidthContent(LineWidth &out, pugi::xml_node el)
{
    parseLineWidthContent(out, el, ParseContext{});
}

void parseLineWidthContent(LineWidth &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<Tenths>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeLineWidth(const LineWidth &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    serializeLineWidthContent(v, el);
}

void serializeLineWidthContent(const LineWidth &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
