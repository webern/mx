// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Feature.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &Feature::type() const noexcept
{
    return m_type;
}

void Feature::setType(std::optional<std::string> value)
{
    m_type = std::move(value);
}

const std::string &Feature::value() const noexcept
{
    return m_value;
}

void Feature::setValue(std::string value)
{
    m_value = std::move(value);
}

Feature parseFeature(pugi::xml_node el)
{
    return parseFeature(el, ParseContext{});
}

Feature parseFeature(pugi::xml_node el, const ParseContext &context)
{
    Feature out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            out.setType(std::string{a.value()});
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseFeatureContent(out, el, context);
    return out;
}

void parseFeatureContent(Feature &out, pugi::xml_node el)
{
    parseFeatureContent(out, el, ParseContext{});
}

void parseFeatureContent(Feature &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeFeature(const Feature &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.type())
    {
        setAttribute(el, "type", *v.type());
    }
    serializeFeatureContent(v, el);
}

void serializeFeatureContent(const Feature &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
