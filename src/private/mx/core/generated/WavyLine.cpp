// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/WavyLine.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStopContinue &WavyLine::type() const noexcept
{
    return m_type;
}

void WavyLine::setType(StartStopContinue value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &WavyLine::number() const noexcept
{
    return m_number;
}

void WavyLine::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<SmuflWavyLineGlyphName> &WavyLine::smufl() const noexcept
{
    return m_smufl;
}

void WavyLine::setSmufl(std::optional<SmuflWavyLineGlyphName> value)
{
    m_smufl = std::move(value);
}

const std::optional<Tenths> &WavyLine::defaultX() const noexcept
{
    return m_defaultX;
}

void WavyLine::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &WavyLine::defaultY() const noexcept
{
    return m_defaultY;
}

void WavyLine::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &WavyLine::relativeX() const noexcept
{
    return m_relativeX;
}

void WavyLine::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &WavyLine::relativeY() const noexcept
{
    return m_relativeY;
}

void WavyLine::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<AboveBelow> &WavyLine::placement() const noexcept
{
    return m_placement;
}

void WavyLine::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<Color> &WavyLine::color() const noexcept
{
    return m_color;
}

void WavyLine::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<StartNote> &WavyLine::startNote() const noexcept
{
    return m_startNote;
}

void WavyLine::setStartNote(std::optional<StartNote> value)
{
    m_startNote = std::move(value);
}

const std::optional<TrillStep> &WavyLine::trillStep() const noexcept
{
    return m_trillStep;
}

void WavyLine::setTrillStep(std::optional<TrillStep> value)
{
    m_trillStep = std::move(value);
}

const std::optional<TwoNoteTurn> &WavyLine::twoNoteTurn() const noexcept
{
    return m_twoNoteTurn;
}

void WavyLine::setTwoNoteTurn(std::optional<TwoNoteTurn> value)
{
    m_twoNoteTurn = std::move(value);
}

const std::optional<YesNo> &WavyLine::accelerate() const noexcept
{
    return m_accelerate;
}

void WavyLine::setAccelerate(std::optional<YesNo> value)
{
    m_accelerate = std::move(value);
}

const std::optional<TrillBeats> &WavyLine::beats() const noexcept
{
    return m_beats;
}

void WavyLine::setBeats(std::optional<TrillBeats> value)
{
    m_beats = std::move(value);
}

const std::optional<Percent> &WavyLine::secondBeat() const noexcept
{
    return m_secondBeat;
}

void WavyLine::setSecondBeat(std::optional<Percent> value)
{
    m_secondBeat = std::move(value);
}

const std::optional<Percent> &WavyLine::lastBeat() const noexcept
{
    return m_lastBeat;
}

void WavyLine::setLastBeat(std::optional<Percent> value)
{
    m_lastBeat = std::move(value);
}

WavyLine parseWavyLine(pugi::xml_node el, const ParseContext &context)
{
    WavyLine out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStopContinue>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(parseValue<NumberLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "smufl")
        {
            out.setSmufl(parseValue<SmuflWavyLineGlyphName>(a.value(), context, el, "smufl"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "start-note")
        {
            out.setStartNote(parseValue<StartNote>(a.value(), context, el, "start-note"));
        }
        else if (aname == "trill-step")
        {
            out.setTrillStep(parseValue<TrillStep>(a.value(), context, el, "trill-step"));
        }
        else if (aname == "two-note-turn")
        {
            out.setTwoNoteTurn(parseValue<TwoNoteTurn>(a.value(), context, el, "two-note-turn"));
        }
        else if (aname == "accelerate")
        {
            out.setAccelerate(parseValue<YesNo>(a.value(), context, el, "accelerate"));
        }
        else if (aname == "beats")
        {
            out.setBeats(parseValue<TrillBeats>(a.value(), context, el, "beats"));
        }
        else if (aname == "second-beat")
        {
            out.setSecondBeat(parseValue<Percent>(a.value(), context, el, "second-beat"));
        }
        else if (aname == "last-beat")
        {
            out.setLastBeat(parseValue<Percent>(a.value(), context, el, "last-beat"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseWavyLineContent(out, el, context);
    return out;
}

void parseWavyLineContent(WavyLine &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeWavyLine(const WavyLine &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.smufl())
    {
        setAttribute(el, "smufl", v.smufl()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.startNote())
    {
        setAttribute(el, "start-note", v.startNote()->toString());
    }
    if (v.trillStep())
    {
        setAttribute(el, "trill-step", v.trillStep()->toString());
    }
    if (v.twoNoteTurn())
    {
        setAttribute(el, "two-note-turn", v.twoNoteTurn()->toString());
    }
    if (v.accelerate())
    {
        setAttribute(el, "accelerate", v.accelerate()->toString());
    }
    if (v.beats())
    {
        setAttribute(el, "beats", v.beats()->toString());
    }
    if (v.secondBeat())
    {
        setAttribute(el, "second-beat", v.secondBeat()->toString());
    }
    if (v.lastBeat())
    {
        setAttribute(el, "last-beat", v.lastBeat()->toString());
    }
    serializeWavyLineContent(v, el);
}

void serializeWavyLineContent(const WavyLine &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
