// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Divisions.h"
#include "mx/core/generated/YesNo.h"

#include <optional>
#include <string>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// An offset is represented in terms of divisions, and indicates where the direction will appear
/// relative to the current musical location. The current musical location is always within the
/// current measure, even at the end of a measure. The offset affects the visual appearance of the
/// direction. If the sound attribute is "yes", then the offset affects playback and listening too.
/// If the sound attribute is "no", then any sound or listening associated with the direction takes
/// effect at the current location. The sound attribute is "no" by default for compatibility with
/// earlier versions of the MusicXML format. If an element within a direction includes a default-x
/// attribute, the offset value will be ignored when determining the appearance of that element.
class Offset final
{
  public:
    const std::optional<YesNo> &sound() const noexcept;
    void setSound(std::optional<YesNo> value);
    const Divisions &value() const noexcept;
    void setValue(Divisions value);
    bool operator==(const Offset &other) const = default;

  private:
    std::optional<YesNo> m_sound;
    Divisions m_value{};
};

Offset parseOffset(pugi::xml_node el, const ParseContext &context);

void parseOffsetContent(Offset &out, pugi::xml_node el, const ParseContext &context);

void serializeOffset(const Offset &v, pugi::xml_node parent, const char *tag);

void serializeOffsetContent(const Offset &v, pugi::xml_node el);

} // namespace mx::core
