// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Work.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &Work::workNumber() const noexcept
{
    return m_workNumber;
}

void Work::setWorkNumber(std::optional<std::string> value)
{
    m_workNumber = std::move(value);
}

const std::optional<std::string> &Work::workTitle() const noexcept
{
    return m_workTitle;
}

void Work::setWorkTitle(std::optional<std::string> value)
{
    m_workTitle = std::move(value);
}

const std::optional<Opus> &Work::opus() const noexcept
{
    return m_opus;
}

void Work::setOpus(std::optional<Opus> value)
{
    m_opus = std::move(value);
}

Work parseWork(pugi::xml_node el)
{
    return parseWork(el, ParseContext{});
}

Work parseWork(pugi::xml_node el, const ParseContext &context)
{
    Work out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseWorkContent(out, el, context);
    return out;
}

void parseWorkContent(Work &out, pugi::xml_node el)
{
    parseWorkContent(out, el, ParseContext{});
}

void parseWorkContent(Work &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "work-number"))
    {
        out.setWorkNumber(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "work-title"))
    {
        out.setWorkTitle(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "opus"))
    {
        out.setOpus(parseOpus(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "work-number") || cursorIs(cursor, "work-title") || cursorIs(cursor, "opus"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeWork(const Work &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeWorkContent(v, el);
}

void serializeWorkContent(const Work &v, pugi::xml_node el)
{
    if (v.workNumber())
    {
        writeTextElement(el, "work-number", *v.workNumber());
    }
    if (v.workTitle())
    {
        writeTextElement(el, "work-title", *v.workTitle());
    }
    if (v.opus())
    {
        serializeOpus(*v.opus(), el, "opus");
    }
}

} // namespace mx::core
