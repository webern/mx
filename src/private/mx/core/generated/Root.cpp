// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Root.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const RootStep &Root::rootStep() const noexcept
{
    return m_rootStep;
}

void Root::setRootStep(RootStep value)
{
    m_rootStep = std::move(value);
}

const std::optional<HarmonyAlter> &Root::rootAlter() const noexcept
{
    return m_rootAlter;
}

void Root::setRootAlter(std::optional<HarmonyAlter> value)
{
    m_rootAlter = std::move(value);
}

Root parseRoot(pugi::xml_node el)
{
    return parseRoot(el, ParseContext{});
}

Root parseRoot(pugi::xml_node el, const ParseContext &context)
{
    Root out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseRootContent(out, el, context);
    return out;
}

void parseRootContent(Root &out, pugi::xml_node el)
{
    parseRootContent(out, el, ParseContext{});
}

void parseRootContent(Root &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "root-step"))
    {
        out.setRootStep(parseRootStep(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "root-step");
    }
    if (cursorIs(cursor, "root-alter"))
    {
        out.setRootAlter(parseHarmonyAlter(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "root-step") || cursorIs(cursor, "root-alter"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeRoot(const Root &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeRootContent(v, el);
}

void serializeRootContent(const Root &v, pugi::xml_node el)
{
    serializeRootStep(v.rootStep(), el, "root-step");
    if (v.rootAlter())
    {
        serializeHarmonyAlter(*v.rootAlter(), el, "root-alter");
    }
}

} // namespace mx::core
