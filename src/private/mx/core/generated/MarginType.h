// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The margin-type type specifies whether margins apply to even page, odd pages, or both.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class MarginType final
{
  public:
    enum class Tag
    {
        odd,
        even,
        both,
    };

    /// The natural zero: the first variant.
    MarginType() noexcept = default;

    static MarginType odd() noexcept;
    static MarginType even() noexcept;
    static MarginType both() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, MarginType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static MarginType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static MarginType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const MarginType &other) const noexcept = default;

  private:
    explicit MarginType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::odd};
};

} // namespace mx::core
