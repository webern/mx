// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class HarmonicChoice2 final
{
  public:
    enum class Kind
    {
        basePitch,
        touchingPitch,
        soundingPitch,
    };

    /// The natural zero: the first alternative.
    HarmonicChoice2();

    static HarmonicChoice2 basePitch(Empty value);
    static HarmonicChoice2 touchingPitch(Empty value);
    static HarmonicChoice2 soundingPitch(Empty value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isBasePitch() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isBasePitch().
    const Empty &asBasePitch() const noexcept
    {
        assert(isBasePitch());
        return *std::get_if<0>(&m_value);
    }

    bool isTouchingPitch() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isTouchingPitch().
    const Empty &asTouchingPitch() const noexcept
    {
        assert(isTouchingPitch());
        return *std::get_if<1>(&m_value);
    }

    bool isSoundingPitch() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isSoundingPitch().
    const Empty &asSoundingPitch() const noexcept
    {
        assert(isSoundingPitch());
        return *std::get_if<2>(&m_value);
    }

    bool operator==(const HarmonicChoice2 &other) const = default;

  private:
    using Storage = std::variant<Empty, Empty, Empty>;

    explicit HarmonicChoice2(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
HarmonicChoice2 parseHarmonicChoice2(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeHarmonicChoice2(const HarmonicChoice2 &v, pugi::xml_node el);

} // namespace mx::core
