// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The wedge type is crescendo for the start of a wedge that is closed at the left side, diminuendo
/// for the start of a wedge that is closed on the right side, and stop for the end of a wedge. The
/// continue type is used for formatting wedges over a system break, or for other situations where a
/// single wedge is divided into multiple segments.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class WedgeType final
{
  public:
    enum class Tag
    {
        crescendo,
        diminuendo,
        stop,
        continue_,
    };

    /// The natural zero: the first variant.
    WedgeType() noexcept = default;

    static WedgeType crescendo() noexcept;
    static WedgeType diminuendo() noexcept;
    static WedgeType stop() noexcept;
    static WedgeType continue_() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, WedgeType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static WedgeType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static WedgeType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const WedgeType &other) const noexcept = default;

  private:
    explicit WedgeType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::crescendo};
};

} // namespace mx::core
