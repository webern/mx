// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Mute.h"
#include "mx/core/generated/OtherPlay.h"
#include "mx/core/generated/SemiPitched.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class PlayChoice final
{
  public:
    enum class Kind
    {
        ipa,
        mute,
        semiPitched,
        otherPlay,
    };

    /// The natural zero: the first alternative.
    PlayChoice();

    static PlayChoice ipa(std::string value);
    static PlayChoice mute(Mute value);
    static PlayChoice semiPitched(SemiPitched value);
    static PlayChoice otherPlay(OtherPlay value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isIpa() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isIpa().
    const std::string &asIpa() const noexcept
    {
        assert(isIpa());
        return *std::get_if<0>(&m_value);
    }

    bool isMute() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isMute().
    const Mute &asMute() const noexcept
    {
        assert(isMute());
        return *std::get_if<1>(&m_value);
    }

    bool isSemiPitched() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isSemiPitched().
    const SemiPitched &asSemiPitched() const noexcept
    {
        assert(isSemiPitched());
        return *std::get_if<2>(&m_value);
    }

    bool isOtherPlay() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isOtherPlay().
    const OtherPlay &asOtherPlay() const noexcept
    {
        assert(isOtherPlay());
        return *std::get_if<3>(&m_value);
    }

    bool operator==(const PlayChoice &other) const = default;

  private:
    using Storage = std::variant<std::string, Mute, SemiPitched, OtherPlay>;

    explicit PlayChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
PlayChoice parsePlayChoice(pugi::xml_node el, pugi::xml_node &cursor);

PlayChoice parsePlayChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializePlayChoice(const PlayChoice &v, pugi::xml_node el);

} // namespace mx::core
