// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MetronomeTuplet.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &MetronomeTuplet::type() const noexcept
{
    return m_type;
}

void MetronomeTuplet::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<YesNo> &MetronomeTuplet::bracket() const noexcept
{
    return m_bracket;
}

void MetronomeTuplet::setBracket(std::optional<YesNo> value)
{
    m_bracket = std::move(value);
}

const std::optional<ShowTuplet> &MetronomeTuplet::showNumber() const noexcept
{
    return m_showNumber;
}

void MetronomeTuplet::setShowNumber(std::optional<ShowTuplet> value)
{
    m_showNumber = std::move(value);
}

MetronomeTuplet parseMetronomeTuplet(pugi::xml_node el)
{
    return parseMetronomeTuplet(el, ParseContext{});
}

MetronomeTuplet parseMetronomeTuplet(pugi::xml_node el, const ParseContext &context)
{
    MetronomeTuplet out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStop>(a.value(), context, el, "type"));
        }
        else if (aname == "bracket")
        {
            out.setBracket(parseValue<YesNo>(a.value(), context, el, "bracket"));
        }
        else if (aname == "show-number")
        {
            out.setShowNumber(parseValue<ShowTuplet>(a.value(), context, el, "show-number"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseTimeModificationContent(out, el, context);
    return out;
}

void serializeMetronomeTuplet(const MetronomeTuplet &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.bracket())
    {
        setAttribute(el, "bracket", v.bracket()->toString());
    }
    if (v.showNumber())
    {
        setAttribute(el, "show-number", v.showNumber()->toString());
    }
    serializeTimeModificationContent(v, el);
}

} // namespace mx::core
