// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Arpeggiate.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<NumberLevel> &Arpeggiate::number() const noexcept
{
    return m_number;
}

void Arpeggiate::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<UpDown> &Arpeggiate::direction() const noexcept
{
    return m_direction;
}

void Arpeggiate::setDirection(std::optional<UpDown> value)
{
    m_direction = std::move(value);
}

const std::optional<YesNo> &Arpeggiate::unbroken() const noexcept
{
    return m_unbroken;
}

void Arpeggiate::setUnbroken(std::optional<YesNo> value)
{
    m_unbroken = std::move(value);
}

const std::optional<Tenths> &Arpeggiate::defaultX() const noexcept
{
    return m_defaultX;
}

void Arpeggiate::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Arpeggiate::defaultY() const noexcept
{
    return m_defaultY;
}

void Arpeggiate::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Arpeggiate::relativeX() const noexcept
{
    return m_relativeX;
}

void Arpeggiate::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Arpeggiate::relativeY() const noexcept
{
    return m_relativeY;
}

void Arpeggiate::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<AboveBelow> &Arpeggiate::placement() const noexcept
{
    return m_placement;
}

void Arpeggiate::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<Color> &Arpeggiate::color() const noexcept
{
    return m_color;
}

void Arpeggiate::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &Arpeggiate::id() const noexcept
{
    return m_id;
}

void Arpeggiate::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

Arpeggiate parseArpeggiate(pugi::xml_node el, const ParseContext &context)
{
    Arpeggiate out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<NumberLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "direction")
        {
            out.setDirection(parseValue<UpDown>(a.value(), context, el, "direction"));
        }
        else if (aname == "unbroken")
        {
            out.setUnbroken(parseValue<YesNo>(a.value(), context, el, "unbroken"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseArpeggiateContent(out, el, context);
    return out;
}

void parseArpeggiateContent(Arpeggiate &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeArpeggiate(const Arpeggiate &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.direction())
    {
        setAttribute(el, "direction", v.direction()->toString());
    }
    if (v.unbroken())
    {
        setAttribute(el, "unbroken", v.unbroken()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeArpeggiateContent(v, el);
}

void serializeArpeggiateContent(const Arpeggiate &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
