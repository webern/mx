// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Key.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &Key::number() const noexcept
{
    return m_number;
}

void Key::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<Tenths> &Key::defaultX() const noexcept
{
    return m_defaultX;
}

void Key::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Key::defaultY() const noexcept
{
    return m_defaultY;
}

void Key::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Key::relativeX() const noexcept
{
    return m_relativeX;
}

void Key::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Key::relativeY() const noexcept
{
    return m_relativeY;
}

void Key::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Key::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Key::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Key::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Key::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Key::fontSize() const noexcept
{
    return m_fontSize;
}

void Key::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Key::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Key::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Key::color() const noexcept
{
    return m_color;
}

void Key::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<YesNo> &Key::printObject() const noexcept
{
    return m_printObject;
}

void Key::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Token> &Key::id() const noexcept
{
    return m_id;
}

void Key::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const KeyChoice &Key::choice() const noexcept
{
    return m_choice;
}

void Key::setChoice(KeyChoice value)
{
    m_choice = std::move(value);
}

std::span<const KeyOctave> Key::keyOctave() const noexcept
{
    return m_keyOctave;
}

void Key::addKeyOctave(KeyOctave value)
{
    m_keyOctave.push_back(std::move(value));
}

void Key::setKeyOctave(std::vector<KeyOctave> value)
{
    m_keyOctave = std::move(value);
}

Key parseKey(pugi::xml_node el, const ParseContext &context)
{
    Key out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseKeyContent(out, el, context);
    return out;
}

void parseKeyContent(Key &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "cancel") || cursorIs(cursor, "fifths") || cursorIs(cursor, "key-step")))
    {
        out.setChoice(parseKeyChoice(el, cursor, context));
    }
    while (cursorIs(cursor, "key-octave"))
    {
        out.addKeyOctave(parseKeyOctave(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "cancel") || cursorIs(cursor, "fifths") || cursorIs(cursor, "mode") ||
            cursorIs(cursor, "key-step") || cursorIs(cursor, "key-alter") || cursorIs(cursor, "key-accidental") ||
            cursorIs(cursor, "key-octave"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeKey(const Key &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeKeyContent(v, el);
}

void serializeKeyContent(const Key &v, pugi::xml_node el)
{
    serializeKeyChoice(v.choice(), el);
    for (const auto &item : v.keyOctave())
    {
        serializeKeyOctave(item, el, "key-octave");
    }
}

} // namespace mx::core
