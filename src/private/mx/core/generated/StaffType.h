// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The staff-type value can be ossia, editorial, cue, alternate, or regular. An ossia staff
/// represents music that can be played instead of what appears on the regular staff. An editorial
/// staff also represents musical alternatives, but is created by an editor rather than the composer.
/// It can be used for suggested interpretations or alternatives from other sources. A cue staff
/// represents music from another part. An alternate staff shares the same music as the prior staff,
/// but displayed differently (e.g., treble and bass clef, standard notation and tablature). It is
/// not included in playback. An alternate staff provides more information to an application reading
/// a file than encoding the same music in separate parts, so its use is preferred in this situation
/// if feasible. A regular staff is the standard default staff-type.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StaffType final
{
  public:
    enum class Tag
    {
        ossia,
        editorial,
        cue,
        alternate,
        regular,
    };

    /// The natural zero: the first variant.
    StaffType() noexcept = default;

    static StaffType ossia() noexcept;
    static StaffType editorial() noexcept;
    static StaffType cue() noexcept;
    static StaffType alternate() noexcept;
    static StaffType regular() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StaffType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StaffType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StaffType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StaffType &other) const noexcept = default;

  private:
    explicit StaffType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::ossia};
};

} // namespace mx::core
