// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Articulations.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &Articulations::id() const noexcept
{
    return m_id;
}

void Articulations::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const ArticulationsChoice> Articulations::choice() const noexcept
{
    return m_choice;
}

void Articulations::addChoice(ArticulationsChoice value)
{
    m_choice.push_back(std::move(value));
}

void Articulations::setChoice(std::vector<ArticulationsChoice> value)
{
    m_choice = std::move(value);
}

Articulations parseArticulations(pugi::xml_node el, const ParseContext &context)
{
    Articulations out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseArticulationsContent(out, el, context);
    return out;
}

void parseArticulationsContent(Articulations &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor &&
           (cursorIs(cursor, "accent") || cursorIs(cursor, "strong-accent") || cursorIs(cursor, "staccato") ||
            cursorIs(cursor, "tenuto") || cursorIs(cursor, "detached-legato") || cursorIs(cursor, "staccatissimo") ||
            cursorIs(cursor, "spiccato") || cursorIs(cursor, "scoop") || cursorIs(cursor, "plop") ||
            cursorIs(cursor, "doit") || cursorIs(cursor, "falloff") || cursorIs(cursor, "breath-mark") ||
            cursorIs(cursor, "caesura") || cursorIs(cursor, "stress") || cursorIs(cursor, "unstress") ||
            cursorIs(cursor, "soft-accent") || cursorIs(cursor, "other-articulation")))
    {
        out.addChoice(parseArticulationsChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "accent") || cursorIs(cursor, "strong-accent") || cursorIs(cursor, "staccato") ||
            cursorIs(cursor, "tenuto") || cursorIs(cursor, "detached-legato") || cursorIs(cursor, "staccatissimo") ||
            cursorIs(cursor, "spiccato") || cursorIs(cursor, "scoop") || cursorIs(cursor, "plop") ||
            cursorIs(cursor, "doit") || cursorIs(cursor, "falloff") || cursorIs(cursor, "breath-mark") ||
            cursorIs(cursor, "caesura") || cursorIs(cursor, "stress") || cursorIs(cursor, "unstress") ||
            cursorIs(cursor, "soft-accent") || cursorIs(cursor, "other-articulation"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeArticulations(const Articulations &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeArticulationsContent(v, el);
}

void serializeArticulationsContent(const Articulations &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializeArticulationsChoice(item, el);
    }
}

} // namespace mx::core
