// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The hole-closed-location type indicates which portion of the hole is filled in when the
/// corresponding hole-closed-value is half.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class HoleClosedLocation final
{
  public:
    enum class Tag
    {
        right,
        bottom,
        left,
        top,
    };

    /// The natural zero: the first variant.
    HoleClosedLocation() noexcept = default;

    static HoleClosedLocation right() noexcept;
    static HoleClosedLocation bottom() noexcept;
    static HoleClosedLocation left() noexcept;
    static HoleClosedLocation top() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, HoleClosedLocation &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static HoleClosedLocation parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static HoleClosedLocation parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const HoleClosedLocation &other) const noexcept = default;

  private:
    explicit HoleClosedLocation(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::right};
};

} // namespace mx::core
