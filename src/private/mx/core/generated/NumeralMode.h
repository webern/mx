// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The numeral-mode type specifies the mode similar to the mode type, but with a restricted set of
/// values. The different minor values are used to interpret numeral-root values of 6 and 7 when
/// present in a minor key. The harmonic minor value sharpens the 7 and the melodic minor value
/// sharpens both 6 and 7. If a minor mode is used without qualification, either in the mode or
/// numeral-mode elements, natural minor is used.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class NumeralMode final
{
  public:
    enum class Tag
    {
        major,
        minor,
        naturalMinor,
        melodicMinor,
        harmonicMinor,
    };

    /// The natural zero: the first variant.
    NumeralMode() noexcept = default;

    static NumeralMode major() noexcept;
    static NumeralMode minor() noexcept;
    static NumeralMode naturalMinor() noexcept;
    static NumeralMode melodicMinor() noexcept;
    static NumeralMode harmonicMinor() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, NumeralMode &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static NumeralMode parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static NumeralMode parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const NumeralMode &other) const noexcept = default;

  private:
    explicit NumeralMode(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::major};
};

} // namespace mx::core
