// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Barline.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<RightLeftMiddle> &Barline::location() const noexcept
{
    return m_location;
}

void Barline::setLocation(std::optional<RightLeftMiddle> value)
{
    m_location = std::move(value);
}

const std::optional<std::string> &Barline::segnoSound() const noexcept
{
    return m_segnoSound;
}

void Barline::setSegnoSound(std::optional<std::string> value)
{
    m_segnoSound = std::move(value);
}

const std::optional<std::string> &Barline::codaSound() const noexcept
{
    return m_codaSound;
}

void Barline::setCodaSound(std::optional<std::string> value)
{
    m_codaSound = std::move(value);
}

const std::optional<Divisions> &Barline::divisions() const noexcept
{
    return m_divisions;
}

void Barline::setDivisions(std::optional<Divisions> value)
{
    m_divisions = std::move(value);
}

const std::optional<Token> &Barline::id() const noexcept
{
    return m_id;
}

void Barline::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const std::optional<BarStyleColor> &Barline::barStyle() const noexcept
{
    return m_barStyle;
}

void Barline::setBarStyle(std::optional<BarStyleColor> value)
{
    m_barStyle = std::move(value);
}

const EditorialGroup &Barline::editorial() const noexcept
{
    return m_editorial;
}

void Barline::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

const std::optional<WavyLine> &Barline::wavyLine() const noexcept
{
    return m_wavyLine;
}

void Barline::setWavyLine(std::optional<WavyLine> value)
{
    m_wavyLine = std::move(value);
}

const std::optional<Segno> &Barline::segno() const noexcept
{
    return m_segno;
}

void Barline::setSegno(std::optional<Segno> value)
{
    m_segno = std::move(value);
}

const std::optional<Coda> &Barline::coda() const noexcept
{
    return m_coda;
}

void Barline::setCoda(std::optional<Coda> value)
{
    m_coda = std::move(value);
}

std::span<const Fermata> Barline::fermata() const noexcept
{
    return m_fermata;
}

Result<void> Barline::addFermata(Fermata value)
{
    if (m_fermata.size() >= 2)
    {
        return Error{ErrorCode::tooManyElements, {}, "at most 2 fermata occurrences"};
    }
    m_fermata.push_back(std::move(value));
    return {};
}

void Barline::clearFermata() noexcept
{
    m_fermata.clear();
}

const std::optional<Ending> &Barline::ending() const noexcept
{
    return m_ending;
}

void Barline::setEnding(std::optional<Ending> value)
{
    m_ending = std::move(value);
}

const std::optional<Repeat> &Barline::repeat() const noexcept
{
    return m_repeat;
}

void Barline::setRepeat(std::optional<Repeat> value)
{
    m_repeat = std::move(value);
}

Barline parseBarline(pugi::xml_node el)
{
    return parseBarline(el, ParseContext{});
}

Barline parseBarline(pugi::xml_node el, const ParseContext &context)
{
    Barline out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "location")
        {
            out.setLocation(parseValue<RightLeftMiddle>(a.value(), context, el, "location"));
        }
        else if (aname == "segno")
        {
            out.setSegnoSound(std::string{a.value()});
        }
        else if (aname == "coda")
        {
            out.setCodaSound(std::string{a.value()});
        }
        else if (aname == "divisions")
        {
            out.setDivisions(parseValue<Divisions>(a.value(), context, el, "divisions"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseBarlineContent(out, el, context);
    return out;
}

void parseBarlineContent(Barline &out, pugi::xml_node el)
{
    parseBarlineContent(out, el, ParseContext{});
}

void parseBarlineContent(Barline &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "bar-style"))
    {
        out.setBarStyle(parseBarStyleColor(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "wavy-line"))
    {
        out.setWavyLine(parseWavyLine(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "segno"))
    {
        out.setSegno(parseSegno(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "coda"))
    {
        out.setCoda(parseCoda(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "fermata"))
    {
        if (!out.addFermata(parseFermata(cursor, context)))
        {
            throwTooManyElements(cursor);
        }
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "ending"))
    {
        out.setEnding(parseEnding(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "repeat"))
    {
        out.setRepeat(parseRepeat(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "bar-style") || cursorIs(cursor, "footnote") || cursorIs(cursor, "level") ||
            cursorIs(cursor, "wavy-line") || cursorIs(cursor, "segno") || cursorIs(cursor, "coda") ||
            cursorIs(cursor, "fermata") || cursorIs(cursor, "ending") || cursorIs(cursor, "repeat"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeBarline(const Barline &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.location())
    {
        setAttribute(el, "location", v.location()->toString());
    }
    if (v.segnoSound())
    {
        setAttribute(el, "segno", *v.segnoSound());
    }
    if (v.codaSound())
    {
        setAttribute(el, "coda", *v.codaSound());
    }
    if (v.divisions())
    {
        setAttribute(el, "divisions", v.divisions()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeBarlineContent(v, el);
}

void serializeBarlineContent(const Barline &v, pugi::xml_node el)
{
    if (v.barStyle())
    {
        serializeBarStyleColor(*v.barStyle(), el, "bar-style");
    }
    serializeEditorialGroup(v.editorial(), el);
    if (v.wavyLine())
    {
        serializeWavyLine(*v.wavyLine(), el, "wavy-line");
    }
    if (v.segno())
    {
        serializeSegno(*v.segno(), el, "segno");
    }
    if (v.coda())
    {
        serializeCoda(*v.coda(), el, "coda");
    }
    for (const auto &item : v.fermata())
    {
        serializeFermata(item, el, "fermata");
    }
    if (v.ending())
    {
        serializeEnding(*v.ending(), el, "ending");
    }
    if (v.repeat())
    {
        serializeRepeat(*v.repeat(), el, "repeat");
    }
}

} // namespace mx::core
