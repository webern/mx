// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/PositiveIntegerOrEmpty.h"
#include "mx/core/generated/StartStop.h"

#include <optional>
#include <string>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The measure-repeat type is used for both single and multiple measure repeats. The text of the
/// element indicates the number of measures to be repeated in a single pattern. The slashes
/// attribute specifies the number of slashes to use in the repeat sign. It is 1 if not specified.
/// The text of the element is ignored when the type is stop. The stop type indicates the first
/// measure where the repeats are no longer displayed. Both the start and the stop of the
/// measure-repeat should be specified unless the repeats are displayed through the end of the part.
/// The measure-repeat element specifies a notation style for repetitions. The actual music being
/// repeated needs to be repeated within each measure of the MusicXML file. This element specifies
/// the notation that indicates the repeat.
class MeasureRepeat final
{
  public:
    const StartStop &type() const noexcept;
    void setType(StartStop value);
    const std::optional<int> &slashes() const noexcept;
    void setSlashes(std::optional<int> value);
    const PositiveIntegerOrEmpty &value() const noexcept;
    void setValue(PositiveIntegerOrEmpty value);
    bool operator==(const MeasureRepeat &other) const = default;

  private:
    StartStop m_type{};
    std::optional<int> m_slashes;
    PositiveIntegerOrEmpty m_value{};
};

MeasureRepeat parseMeasureRepeat(pugi::xml_node el, const ParseContext &context);

void parseMeasureRepeatContent(MeasureRepeat &out, pugi::xml_node el, const ParseContext &context);

void serializeMeasureRepeat(const MeasureRepeat &v, pugi::xml_node parent, const char *tag);

void serializeMeasureRepeatContent(const MeasureRepeat &v, pugi::xml_node el);

} // namespace mx::core
