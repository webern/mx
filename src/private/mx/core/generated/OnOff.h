// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The on-off type is used for notation elements such as string mutes.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class OnOff final
{
  public:
    enum class Tag
    {
        on,
        off,
    };

    /// The natural zero: the first variant.
    OnOff() noexcept = default;

    static OnOff on() noexcept;
    static OnOff off() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, OnOff &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static OnOff parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static OnOff parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const OnOff &other) const noexcept = default;

  private:
    explicit OnOff(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::on};
};

} // namespace mx::core
