// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"
#include "mx/core/generated/Extend.h"
#include "mx/core/generated/LyricTextGroup.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class LyricChoice final
{
  public:
    enum class Kind
    {
        lyricTextGroup,
        extend,
        laughing,
        humming,
    };

    /// The natural zero: the first alternative.
    LyricChoice();

    static LyricChoice lyricTextGroup(LyricTextGroup value);
    static LyricChoice extend(Extend value);
    static LyricChoice laughing(Empty value);
    static LyricChoice humming(Empty value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isLyricTextGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isLyricTextGroup().
    const LyricTextGroup &asLyricTextGroup() const noexcept
    {
        assert(isLyricTextGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isExtend() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isExtend().
    const Extend &asExtend() const noexcept
    {
        assert(isExtend());
        return *std::get_if<1>(&m_value);
    }

    bool isLaughing() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isLaughing().
    const Empty &asLaughing() const noexcept
    {
        assert(isLaughing());
        return *std::get_if<2>(&m_value);
    }

    bool isHumming() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isHumming().
    const Empty &asHumming() const noexcept
    {
        assert(isHumming());
        return *std::get_if<3>(&m_value);
    }

    bool operator==(const LyricChoice &other) const = default;

  private:
    using Storage = std::variant<LyricTextGroup, Extend, Empty, Empty>;

    explicit LyricChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
LyricChoice parseLyricChoice(pugi::xml_node el, pugi::xml_node &cursor);

LyricChoice parseLyricChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeLyricChoice(const LyricChoice &v, pugi::xml_node el);

} // namespace mx::core
