// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PercussionChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

PercussionChoice::PercussionChoice()
{
    *this = PercussionChoice::glass(Glass{});
}

PercussionChoice PercussionChoice::glass(Glass value)
{
    return PercussionChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

PercussionChoice PercussionChoice::metal(Metal value)
{
    return PercussionChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

PercussionChoice PercussionChoice::wood(Wood value)
{
    return PercussionChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

PercussionChoice PercussionChoice::pitched(Pitched value)
{
    return PercussionChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

PercussionChoice PercussionChoice::membrane(Membrane value)
{
    return PercussionChoice{Storage{std::in_place_index<4>, std::move(value)}};
}

PercussionChoice PercussionChoice::effect(Effect value)
{
    return PercussionChoice{Storage{std::in_place_index<5>, std::move(value)}};
}

PercussionChoice PercussionChoice::timpani(Timpani value)
{
    return PercussionChoice{Storage{std::in_place_index<6>, std::move(value)}};
}

PercussionChoice PercussionChoice::beater(Beater value)
{
    return PercussionChoice{Storage{std::in_place_index<7>, std::move(value)}};
}

PercussionChoice PercussionChoice::stick(Stick value)
{
    return PercussionChoice{Storage{std::in_place_index<8>, std::move(value)}};
}

PercussionChoice PercussionChoice::stickLocation(StickLocation value)
{
    return PercussionChoice{Storage{std::in_place_index<9>, std::move(value)}};
}

PercussionChoice PercussionChoice::otherPercussion(OtherText value)
{
    return PercussionChoice{Storage{std::in_place_index<10>, std::move(value)}};
}

PercussionChoice parsePercussionChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parsePercussionChoice(el, cursor, ParseContext{});
}

PercussionChoice parsePercussionChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "glass")))
    {
        Glass value = parseGlass(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::glass(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "metal")))
    {
        Metal value = parseMetal(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::metal(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "wood")))
    {
        Wood value = parseWood(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::wood(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "pitched")))
    {
        Pitched value = parsePitched(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::pitched(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "membrane")))
    {
        Membrane value = parseMembrane(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::membrane(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "effect")))
    {
        Effect value = parseEffect(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::effect(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "timpani")))
    {
        Timpani value = parseTimpani(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::timpani(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "beater")))
    {
        Beater value = parseBeater(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::beater(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "stick")))
    {
        Stick value = parseStick(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::stick(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "stick-location")))
    {
        StickLocation value = parseValue<StickLocation>(childText(cursor), context, cursor, nullptr);
        cursor = nextElement(cursor);
        return PercussionChoice::stickLocation(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-percussion")))
    {
        OtherText value = parseOtherText(cursor, context);
        cursor = nextElement(cursor);
        return PercussionChoice::otherPercussion(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "glass");
}

void serializePercussionChoice(const PercussionChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case PercussionChoice::Kind::glass:
        serializeGlass(v.asGlass(), el, "glass");
        break;
    case PercussionChoice::Kind::metal:
        serializeMetal(v.asMetal(), el, "metal");
        break;
    case PercussionChoice::Kind::wood:
        serializeWood(v.asWood(), el, "wood");
        break;
    case PercussionChoice::Kind::pitched:
        serializePitched(v.asPitched(), el, "pitched");
        break;
    case PercussionChoice::Kind::membrane:
        serializeMembrane(v.asMembrane(), el, "membrane");
        break;
    case PercussionChoice::Kind::effect:
        serializeEffect(v.asEffect(), el, "effect");
        break;
    case PercussionChoice::Kind::timpani:
        serializeTimpani(v.asTimpani(), el, "timpani");
        break;
    case PercussionChoice::Kind::beater:
        serializeBeater(v.asBeater(), el, "beater");
        break;
    case PercussionChoice::Kind::stick:
        serializeStick(v.asStick(), el, "stick");
        break;
    case PercussionChoice::Kind::stickLocation:
        writeTextElement(el, "stick-location", v.asStickLocation().toString());
        break;
    case PercussionChoice::Kind::otherPercussion:
        serializeOtherText(v.asOtherPercussion(), el, "other-percussion");
        break;
    }
}

} // namespace mx::core
