// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Rest.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Rest::measure() const noexcept
{
    return m_measure;
}

void Rest::setMeasure(std::optional<YesNo> value)
{
    m_measure = std::move(value);
}

const std::optional<DisplayStepOctaveGroup> &Rest::displayStepOctave() const noexcept
{
    return m_displayStepOctave;
}

void Rest::setDisplayStepOctave(std::optional<DisplayStepOctaveGroup> value)
{
    m_displayStepOctave = std::move(value);
}

Rest parseRest(pugi::xml_node el, const ParseContext &context)
{
    Rest out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "measure")
        {
            out.setMeasure(parseValue<YesNo>(a.value(), context, el, "measure"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseRestContent(out, el, context);
    return out;
}

void parseRestContent(Rest &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "display-step")))
    {
        out.setDisplayStepOctave(parseDisplayStepOctaveGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "display-step") || cursorIs(cursor, "display-octave"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeRest(const Rest &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.measure())
    {
        setAttribute(el, "measure", v.measure()->toString());
    }
    serializeRestContent(v, el);
}

void serializeRestContent(const Rest &v, pugi::xml_node el)
{
    if (v.displayStepOctave())
    {
        serializeDisplayStepOctaveGroup(*v.displayStepOctave(), el);
    }
}

} // namespace mx::core
