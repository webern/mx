// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Release.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Divisions> &Release::offset() const noexcept
{
    return m_offset;
}

void Release::setOffset(std::optional<Divisions> value)
{
    m_offset = std::move(value);
}

Release parseRelease(pugi::xml_node el, const ParseContext &context)
{
    Release out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "offset")
        {
            out.setOffset(parseValue<Divisions>(a.value(), context, el, "offset"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseEmptyContent(out, el, context);
    return out;
}

void serializeRelease(const Release &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.offset())
    {
        setAttribute(el, "offset", v.offset()->toString());
    }
    serializeEmptyContent(v, el);
}

} // namespace mx::core
