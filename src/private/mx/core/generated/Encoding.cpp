// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Encoding.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const EncodingChoice> Encoding::choice() const noexcept
{
    return m_choice;
}

void Encoding::addChoice(EncodingChoice value)
{
    m_choice.push_back(std::move(value));
}

void Encoding::setChoice(std::vector<EncodingChoice> value)
{
    m_choice = std::move(value);
}

Encoding parseEncoding(pugi::xml_node el, const ParseContext &context)
{
    Encoding out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseEncodingContent(out, el, context);
    return out;
}

void parseEncodingContent(Encoding &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor &&
           (cursorIs(cursor, "encoding-date") || cursorIs(cursor, "encoder") || cursorIs(cursor, "software") ||
            cursorIs(cursor, "encoding-description") || cursorIs(cursor, "supports")))
    {
        out.addChoice(parseEncodingChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "encoding-date") || cursorIs(cursor, "encoder") || cursorIs(cursor, "software") ||
            cursorIs(cursor, "encoding-description") || cursorIs(cursor, "supports"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeEncoding(const Encoding &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeEncodingContent(v, el);
}

void serializeEncodingContent(const Encoding &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializeEncodingChoice(item, el);
    }
}

} // namespace mx::core
