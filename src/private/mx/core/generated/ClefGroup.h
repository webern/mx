// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/ClefSign.h"
#include "mx/core/generated/StaffLinePosition.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// Clefs are represented by a combination of sign, line, and clef-octave-change elements.
/// A shared content group: transparent on the wire, its
/// fields serialize directly into the referencing element (plan §2.3).
class ClefGroup final
{
  public:
    const ClefSign &sign() const noexcept;
    void setSign(ClefSign value);
    const std::optional<StaffLinePosition> &line() const noexcept;
    void setLine(std::optional<StaffLinePosition> value);
    const std::optional<int> &clefOctaveChange() const noexcept;
    void setClefOctaveChange(std::optional<int> value);
    bool operator==(const ClefGroup &other) const = default;

  private:
    ClefSign m_sign{};
    std::optional<StaffLinePosition> m_line;
    std::optional<int> m_clefOctaveChange;
};

/// Consumes matching siblings starting at `cursor` (which advances); `el`
/// is the enclosing element, for error paths.
ClefGroup parseClefGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeClefGroup(const ClefGroup &v, pugi::xml_node el);

} // namespace mx::core
