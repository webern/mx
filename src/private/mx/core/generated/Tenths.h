// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Decimal.h"
#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The tenths type is a number representing tenths of interline staff space (positive or negative).
/// Both integer and decimal values are allowed, such as 5 for a half space and 2.5 for a quarter
/// space. Interline space is measured from the middle of a staff line. Distances in a MusicXML file
/// are measured in tenths of staff space. Tenths are then scaled to millimeters within the scaling
/// element, used in the defaults element at the start of a score. Individual staves can apply a
/// scaling factor to adjust staff size. When a MusicXML element or attribute refers to tenths, it
/// means the global tenths defined by the scaling element, not the local tenths as adjusted by the
/// staff-size element.
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class Tenths final
{
  public:
    /// The natural zero: the default value clamped into range.
    Tenths();

    explicit Tenths(Decimal value);

    const Decimal &value() const noexcept
    {
        return m_value;
    }

    void setValue(Decimal value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, Tenths &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static Tenths parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static Tenths parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const Tenths &other) const noexcept = default;

  private:
    Decimal m_value{};
};

} // namespace mx::core
