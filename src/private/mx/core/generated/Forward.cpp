// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Forward.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const PositiveDivisions &Forward::duration() const noexcept
{
    return m_duration;
}

void Forward::setDuration(PositiveDivisions value)
{
    m_duration = std::move(value);
}

const EditorialVoiceGroup &Forward::editorialVoice() const noexcept
{
    return m_editorialVoice;
}

void Forward::setEditorialVoice(EditorialVoiceGroup value)
{
    m_editorialVoice = std::move(value);
}

const std::optional<int> &Forward::staff() const noexcept
{
    return m_staff;
}

void Forward::setStaff(std::optional<int> value)
{
    m_staff = std::move(value);
}

Forward parseForward(pugi::xml_node el, const ParseContext &context)
{
    Forward out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseForwardContent(out, el, context);
    return out;
}

void parseForwardContent(Forward &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "duration"))
    {
        out.setDuration(parseValue<PositiveDivisions>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "duration");
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "voice")))
    {
        out.setEditorialVoice(parseEditorialVoiceGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "staff"))
    {
        out.setStaff(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "duration") || cursorIs(cursor, "footnote") || cursorIs(cursor, "level") ||
            cursorIs(cursor, "voice") || cursorIs(cursor, "staff"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeForward(const Forward &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeForwardContent(v, el);
}

void serializeForwardContent(const Forward &v, pugi::xml_node el)
{
    writeTextElement(el, "duration", v.duration().toString());
    serializeEditorialVoiceGroup(v.editorialVoice(), el);
    if (v.staff())
    {
        writeTextElement(el, "staff", formatInt(*v.staff()));
    }
}

} // namespace mx::core
