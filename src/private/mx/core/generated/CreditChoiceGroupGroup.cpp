// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/CreditChoiceGroupGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const Link> CreditChoiceGroupGroup::link() const noexcept
{
    return m_link;
}

void CreditChoiceGroupGroup::addLink(Link value)
{
    m_link.push_back(std::move(value));
}

void CreditChoiceGroupGroup::setLink(std::vector<Link> value)
{
    m_link = std::move(value);
}

std::span<const Bookmark> CreditChoiceGroupGroup::bookmark() const noexcept
{
    return m_bookmark;
}

void CreditChoiceGroupGroup::addBookmark(Bookmark value)
{
    m_bookmark.push_back(std::move(value));
}

void CreditChoiceGroupGroup::setBookmark(std::vector<Bookmark> value)
{
    m_bookmark = std::move(value);
}

const CreditChoiceGroupGroupChoice &CreditChoiceGroupGroup::choice() const noexcept
{
    return m_choice;
}

void CreditChoiceGroupGroup::setChoice(CreditChoiceGroupGroupChoice value)
{
    m_choice = std::move(value);
}

CreditChoiceGroupGroup parseCreditChoiceGroupGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseCreditChoiceGroupGroup(el, cursor, ParseContext{});
}

CreditChoiceGroupGroup parseCreditChoiceGroupGroup(pugi::xml_node el, pugi::xml_node &cursor,
                                                   const ParseContext &context)
{
    CreditChoiceGroupGroup out;
    while (cursorIs(cursor, "link"))
    {
        out.addLink(parseLink(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "bookmark"))
    {
        out.addBookmark(parseBookmark(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "credit-words") || cursorIs(cursor, "credit-symbol")))
    {
        out.setChoice(parseCreditChoiceGroupGroupChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "credit-words");
    }
    return out;
}

void serializeCreditChoiceGroupGroup(const CreditChoiceGroupGroup &v, pugi::xml_node el)
{
    for (const auto &item : v.link())
    {
        serializeLink(item, el, "link");
    }
    for (const auto &item : v.bookmark())
    {
        serializeBookmark(item, el, "bookmark");
    }
    serializeCreditChoiceGroupGroupChoice(v.choice(), el);
}

} // namespace mx::core
