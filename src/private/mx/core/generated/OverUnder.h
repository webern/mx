// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The over-under type is used to indicate whether the tips of curved lines such as slurs and ties
/// are overhand (tips down) or underhand (tips up).
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class OverUnder final
{
  public:
    enum class Tag
    {
        over,
        under,
    };

    /// The natural zero: the first variant.
    OverUnder() noexcept = default;

    static OverUnder over() noexcept;
    static OverUnder under() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, OverUnder &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static OverUnder parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static OverUnder parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const OverUnder &other) const noexcept = default;

  private:
    explicit OverUnder(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::over};
};

} // namespace mx::core
