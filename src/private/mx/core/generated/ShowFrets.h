// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The show-frets type indicates whether to show tablature frets as numbers (0, 1, 2) or letters (a,
/// b, c). The default choice is numbers.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ShowFrets final
{
  public:
    enum class Tag
    {
        numbers,
        letters,
    };

    /// The natural zero: the first variant.
    ShowFrets() noexcept = default;

    static ShowFrets numbers() noexcept;
    static ShowFrets letters() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ShowFrets &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ShowFrets parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ShowFrets parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ShowFrets &other) const noexcept = default;

  private:
    explicit ShowFrets(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::numbers};
};

} // namespace mx::core
