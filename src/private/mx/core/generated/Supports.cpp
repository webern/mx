// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Supports.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const YesNo &Supports::type() const noexcept
{
    return m_type;
}

void Supports::setType(YesNo value)
{
    m_type = std::move(value);
}

const NameToken &Supports::element() const noexcept
{
    return m_element;
}

void Supports::setElement(NameToken value)
{
    m_element = std::move(value);
}

const std::optional<NameToken> &Supports::attribute() const noexcept
{
    return m_attribute;
}

void Supports::setAttribute(std::optional<NameToken> value)
{
    m_attribute = std::move(value);
}

const std::optional<std::string> &Supports::value() const noexcept
{
    return m_value;
}

void Supports::setValue(std::optional<std::string> value)
{
    m_value = std::move(value);
}

Supports parseSupports(pugi::xml_node el, const ParseContext &context)
{
    Supports out;
    bool seen_type = false;
    bool seen_element = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<YesNo>(a.value(), context, el, "type"));
        }
        else if (aname == "element")
        {
            seen_element = true;
            out.setElement(parseValue<NameToken>(a.value(), context, el, "element"));
        }
        else if (aname == "attribute")
        {
            out.setAttribute(parseValue<NameToken>(a.value(), context, el, "attribute"));
        }
        else if (aname == "value")
        {
            out.setValue(std::string{a.value()});
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    if (!seen_element)
    {
        throwMissingAttribute(el, "element");
    }
    parseSupportsContent(out, el, context);
    return out;
}

void parseSupportsContent(Supports &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeSupports(const Supports &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    setAttribute(el, "element", v.element().toString());
    if (v.attribute())
    {
        setAttribute(el, "attribute", v.attribute()->toString());
    }
    if (v.value())
    {
        setAttribute(el, "value", *v.value());
    }
    serializeSupportsContent(v, el);
}

void serializeSupportsContent(const Supports &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
