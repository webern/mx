// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TupletDot.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<FontFamily> &TupletDot::fontFamily() const noexcept
{
    return m_fontFamily;
}

void TupletDot::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &TupletDot::fontStyle() const noexcept
{
    return m_fontStyle;
}

void TupletDot::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &TupletDot::fontSize() const noexcept
{
    return m_fontSize;
}

void TupletDot::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &TupletDot::fontWeight() const noexcept
{
    return m_fontWeight;
}

void TupletDot::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &TupletDot::color() const noexcept
{
    return m_color;
}

void TupletDot::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

TupletDot parseTupletDot(pugi::xml_node el)
{
    return parseTupletDot(el, ParseContext{});
}

TupletDot parseTupletDot(pugi::xml_node el, const ParseContext &context)
{
    TupletDot out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseTupletDotContent(out, el, context);
    return out;
}

void parseTupletDotContent(TupletDot &out, pugi::xml_node el)
{
    parseTupletDotContent(out, el, ParseContext{});
}

void parseTupletDotContent(TupletDot &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeTupletDot(const TupletDot &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeTupletDotContent(v, el);
}

void serializeTupletDotContent(const TupletDot &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
