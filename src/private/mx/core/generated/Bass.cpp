// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Bass.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<HarmonyArrangement> &Bass::arrangement() const noexcept
{
    return m_arrangement;
}

void Bass::setArrangement(std::optional<HarmonyArrangement> value)
{
    m_arrangement = std::move(value);
}

const std::optional<StyleText> &Bass::bassSeparator() const noexcept
{
    return m_bassSeparator;
}

void Bass::setBassSeparator(std::optional<StyleText> value)
{
    m_bassSeparator = std::move(value);
}

const BassStep &Bass::bassStep() const noexcept
{
    return m_bassStep;
}

void Bass::setBassStep(BassStep value)
{
    m_bassStep = std::move(value);
}

const std::optional<HarmonyAlter> &Bass::bassAlter() const noexcept
{
    return m_bassAlter;
}

void Bass::setBassAlter(std::optional<HarmonyAlter> value)
{
    m_bassAlter = std::move(value);
}

Bass parseBass(pugi::xml_node el)
{
    return parseBass(el, ParseContext{});
}

Bass parseBass(pugi::xml_node el, const ParseContext &context)
{
    Bass out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "arrangement")
        {
            out.setArrangement(parseValue<HarmonyArrangement>(a.value(), context, el, "arrangement"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseBassContent(out, el, context);
    return out;
}

void parseBassContent(Bass &out, pugi::xml_node el)
{
    parseBassContent(out, el, ParseContext{});
}

void parseBassContent(Bass &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "bass-separator"))
    {
        out.setBassSeparator(parseStyleText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "bass-step"))
    {
        out.setBassStep(parseBassStep(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "bass-step");
    }
    if (cursorIs(cursor, "bass-alter"))
    {
        out.setBassAlter(parseHarmonyAlter(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "bass-separator") || cursorIs(cursor, "bass-step") || cursorIs(cursor, "bass-alter"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeBass(const Bass &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.arrangement())
    {
        setAttribute(el, "arrangement", v.arrangement()->toString());
    }
    serializeBassContent(v, el);
}

void serializeBassContent(const Bass &v, pugi::xml_node el)
{
    if (v.bassSeparator())
    {
        serializeStyleText(*v.bassSeparator(), el, "bass-separator");
    }
    serializeBassStep(v.bassStep(), el, "bass-step");
    if (v.bassAlter())
    {
        serializeHarmonyAlter(*v.bassAlter(), el, "bass-alter");
    }
}

} // namespace mx::core
