// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/DirectionType.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &DirectionType::id() const noexcept
{
    return m_id;
}

void DirectionType::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const DirectionTypeChoice &DirectionType::choice() const noexcept
{
    return m_choice;
}

void DirectionType::setChoice(DirectionTypeChoice value)
{
    m_choice = std::move(value);
}

DirectionType parseDirectionType(pugi::xml_node el, const ParseContext &context)
{
    DirectionType out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseDirectionTypeContent(out, el, context);
    return out;
}

void parseDirectionTypeContent(DirectionType &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "rehearsal") || cursorIs(cursor, "segno") || cursorIs(cursor, "coda") ||
                   cursorIs(cursor, "words") || cursorIs(cursor, "symbol") || cursorIs(cursor, "wedge") ||
                   cursorIs(cursor, "dynamics") || cursorIs(cursor, "dashes") || cursorIs(cursor, "bracket") ||
                   cursorIs(cursor, "pedal") || cursorIs(cursor, "metronome") || cursorIs(cursor, "octave-shift") ||
                   cursorIs(cursor, "harp-pedals") || cursorIs(cursor, "damp") || cursorIs(cursor, "damp-all") ||
                   cursorIs(cursor, "eyeglasses") || cursorIs(cursor, "string-mute") ||
                   cursorIs(cursor, "scordatura") || cursorIs(cursor, "image") || cursorIs(cursor, "principal-voice") ||
                   cursorIs(cursor, "percussion") || cursorIs(cursor, "accordion-registration") ||
                   cursorIs(cursor, "staff-divide") || cursorIs(cursor, "other-direction")))
    {
        out.setChoice(parseDirectionTypeChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "rehearsal");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "rehearsal") || cursorIs(cursor, "segno") || cursorIs(cursor, "coda") ||
            cursorIs(cursor, "words") || cursorIs(cursor, "symbol") || cursorIs(cursor, "wedge") ||
            cursorIs(cursor, "dynamics") || cursorIs(cursor, "dashes") || cursorIs(cursor, "bracket") ||
            cursorIs(cursor, "pedal") || cursorIs(cursor, "metronome") || cursorIs(cursor, "octave-shift") ||
            cursorIs(cursor, "harp-pedals") || cursorIs(cursor, "damp") || cursorIs(cursor, "damp-all") ||
            cursorIs(cursor, "eyeglasses") || cursorIs(cursor, "string-mute") || cursorIs(cursor, "scordatura") ||
            cursorIs(cursor, "image") || cursorIs(cursor, "principal-voice") || cursorIs(cursor, "percussion") ||
            cursorIs(cursor, "accordion-registration") || cursorIs(cursor, "staff-divide") ||
            cursorIs(cursor, "other-direction"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeDirectionType(const DirectionType &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeDirectionTypeContent(v, el);
}

void serializeDirectionTypeContent(const DirectionType &v, pugi::xml_node el)
{
    serializeDirectionTypeChoice(v.choice(), el);
}

} // namespace mx::core
