// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MIDIInstrument.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &MIDIInstrument::id() const noexcept
{
    return m_id;
}

void MIDIInstrument::setID(Token value)
{
    m_id = std::move(value);
}

const std::optional<MIDI16> &MIDIInstrument::midiChannel() const noexcept
{
    return m_midiChannel;
}

void MIDIInstrument::setMIDIChannel(std::optional<MIDI16> value)
{
    m_midiChannel = std::move(value);
}

const std::optional<std::string> &MIDIInstrument::midiName() const noexcept
{
    return m_midiName;
}

void MIDIInstrument::setMIDIName(std::optional<std::string> value)
{
    m_midiName = std::move(value);
}

const std::optional<MIDI16384> &MIDIInstrument::midiBank() const noexcept
{
    return m_midiBank;
}

void MIDIInstrument::setMIDIBank(std::optional<MIDI16384> value)
{
    m_midiBank = std::move(value);
}

const std::optional<MIDI128> &MIDIInstrument::midiProgram() const noexcept
{
    return m_midiProgram;
}

void MIDIInstrument::setMIDIProgram(std::optional<MIDI128> value)
{
    m_midiProgram = std::move(value);
}

const std::optional<MIDI128> &MIDIInstrument::midiUnpitched() const noexcept
{
    return m_midiUnpitched;
}

void MIDIInstrument::setMIDIUnpitched(std::optional<MIDI128> value)
{
    m_midiUnpitched = std::move(value);
}

const std::optional<Percent> &MIDIInstrument::volume() const noexcept
{
    return m_volume;
}

void MIDIInstrument::setVolume(std::optional<Percent> value)
{
    m_volume = std::move(value);
}

const std::optional<RotationDegrees> &MIDIInstrument::pan() const noexcept
{
    return m_pan;
}

void MIDIInstrument::setPan(std::optional<RotationDegrees> value)
{
    m_pan = std::move(value);
}

const std::optional<RotationDegrees> &MIDIInstrument::elevation() const noexcept
{
    return m_elevation;
}

void MIDIInstrument::setElevation(std::optional<RotationDegrees> value)
{
    m_elevation = std::move(value);
}

MIDIInstrument parseMIDIInstrument(pugi::xml_node el, const ParseContext &context)
{
    MIDIInstrument out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(parseValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseMIDIInstrumentContent(out, el, context);
    return out;
}

void parseMIDIInstrumentContent(MIDIInstrument &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "midi-channel"))
    {
        out.setMIDIChannel(parseValue<MIDI16>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "midi-name"))
    {
        out.setMIDIName(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "midi-bank"))
    {
        out.setMIDIBank(parseValue<MIDI16384>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "midi-program"))
    {
        out.setMIDIProgram(parseValue<MIDI128>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "midi-unpitched"))
    {
        out.setMIDIUnpitched(parseValue<MIDI128>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "volume"))
    {
        out.setVolume(parseValue<Percent>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "pan"))
    {
        out.setPan(parseValue<RotationDegrees>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "elevation"))
    {
        out.setElevation(parseValue<RotationDegrees>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "midi-channel") || cursorIs(cursor, "midi-name") || cursorIs(cursor, "midi-bank") ||
            cursorIs(cursor, "midi-program") || cursorIs(cursor, "midi-unpitched") || cursorIs(cursor, "volume") ||
            cursorIs(cursor, "pan") || cursorIs(cursor, "elevation"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeMIDIInstrument(const MIDIInstrument &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializeMIDIInstrumentContent(v, el);
}

void serializeMIDIInstrumentContent(const MIDIInstrument &v, pugi::xml_node el)
{
    if (v.midiChannel())
    {
        writeTextElement(el, "midi-channel", v.midiChannel()->toString());
    }
    if (v.midiName())
    {
        writeTextElement(el, "midi-name", *v.midiName());
    }
    if (v.midiBank())
    {
        writeTextElement(el, "midi-bank", v.midiBank()->toString());
    }
    if (v.midiProgram())
    {
        writeTextElement(el, "midi-program", v.midiProgram()->toString());
    }
    if (v.midiUnpitched())
    {
        writeTextElement(el, "midi-unpitched", v.midiUnpitched()->toString());
    }
    if (v.volume())
    {
        writeTextElement(el, "volume", v.volume()->toString());
    }
    if (v.pan())
    {
        writeTextElement(el, "pan", v.pan()->toString());
    }
    if (v.elevation())
    {
        writeTextElement(el, "elevation", v.elevation()->toString());
    }
}

} // namespace mx::core
