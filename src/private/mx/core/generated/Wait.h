// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/NameToken.h"
#include "mx/core/Token.h"
#include "mx/core/generated/TimeOnly.h"

#include <optional>
#include <string>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The wait type specifies a point where the accompaniment should wait for a performer event before
/// continuing. This typically happens at the start of new sections or after a held note or
/// indeterminate music. These waiting points cannot always be inferred reliably from the contents of
/// the displayed score. The optional player and time-only attributes restrict the type to apply to a
/// single player or set of times through a repeated section, respectively.
class Wait final
{
  public:
    const std::optional<Token> &player() const noexcept;
    void setPlayer(std::optional<Token> value);
    const std::optional<TimeOnly> &timeOnly() const noexcept;
    void setTimeOnly(std::optional<TimeOnly> value);
    bool operator==(const Wait &other) const = default;

  private:
    std::optional<Token> m_player;
    std::optional<TimeOnly> m_timeOnly;
};

Wait parseWait(pugi::xml_node el, const ParseContext &context);

void parseWaitContent(Wait &out, pugi::xml_node el, const ParseContext &context);

void serializeWait(const Wait &v, pugi::xml_node parent, const char *tag);

void serializeWaitContent(const Wait &v, pugi::xml_node el);

} // namespace mx::core
