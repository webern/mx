// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/EncodingChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

EncodingChoice::EncodingChoice()
{
    *this = EncodingChoice::encodingDate(YyyyMmDd{});
}

EncodingChoice EncodingChoice::encodingDate(YyyyMmDd value)
{
    return EncodingChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

EncodingChoice EncodingChoice::encoder(TypedText value)
{
    return EncodingChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

EncodingChoice EncodingChoice::software(std::string value)
{
    return EncodingChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

EncodingChoice EncodingChoice::encodingDescription(std::string value)
{
    return EncodingChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

EncodingChoice EncodingChoice::supports(Supports value)
{
    return EncodingChoice{Storage{std::in_place_index<4>, std::move(value)}};
}

EncodingChoice parseEncodingChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "encoding-date")))
    {
        YyyyMmDd value = parseValue<YyyyMmDd>(childText(cursor), context, cursor, nullptr);
        cursor = nextElement(cursor);
        return EncodingChoice::encodingDate(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "encoder")))
    {
        TypedText value = parseTypedText(cursor, context);
        cursor = nextElement(cursor);
        return EncodingChoice::encoder(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "software")))
    {
        std::string value = std::string{childText(cursor)};
        cursor = nextElement(cursor);
        return EncodingChoice::software(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "encoding-description")))
    {
        std::string value = std::string{childText(cursor)};
        cursor = nextElement(cursor);
        return EncodingChoice::encodingDescription(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "supports")))
    {
        Supports value = parseSupports(cursor, context);
        cursor = nextElement(cursor);
        return EncodingChoice::supports(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "encoding-date");
}

void serializeEncodingChoice(const EncodingChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case EncodingChoice::Kind::encodingDate:
        writeTextElement(el, "encoding-date", v.asEncodingDate().toString());
        break;
    case EncodingChoice::Kind::encoder:
        serializeTypedText(v.asEncoder(), el, "encoder");
        break;
    case EncodingChoice::Kind::software:
        writeTextElement(el, "software", v.asSoftware());
        break;
    case EncodingChoice::Kind::encodingDescription:
        writeTextElement(el, "encoding-description", v.asEncodingDescription());
        break;
    case EncodingChoice::Kind::supports:
        serializeSupports(v.asSupports(), el, "supports");
        break;
    }
}

} // namespace mx::core
