// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The tremolo-type is used to distinguish double-note, single-note, and unmeasured tremolos.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TremoloType final
{
  public:
    enum class Tag
    {
        start,
        stop,
        single,
        unmeasured,
    };

    /// The natural zero: the first variant.
    TremoloType() noexcept = default;

    static TremoloType start() noexcept;
    static TremoloType stop() noexcept;
    static TremoloType single() noexcept;
    static TremoloType unmeasured() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TremoloType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TremoloType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TremoloType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TremoloType &other) const noexcept = default;

  private:
    explicit TremoloType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::start};
};

} // namespace mx::core
