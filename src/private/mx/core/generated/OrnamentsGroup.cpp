// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/OrnamentsGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const OrnamentsGroupChoice &OrnamentsGroup::choice() const noexcept
{
    return m_choice;
}

void OrnamentsGroup::setChoice(OrnamentsGroupChoice value)
{
    m_choice = std::move(value);
}

std::span<const AccidentalMark> OrnamentsGroup::accidentalMark() const noexcept
{
    return m_accidentalMark;
}

void OrnamentsGroup::addAccidentalMark(AccidentalMark value)
{
    m_accidentalMark.push_back(std::move(value));
}

void OrnamentsGroup::setAccidentalMark(std::vector<AccidentalMark> value)
{
    m_accidentalMark = std::move(value);
}

OrnamentsGroup parseOrnamentsGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    OrnamentsGroup out;
    if (cursor && (cursorIs(cursor, "trill-mark") || cursorIs(cursor, "turn") || cursorIs(cursor, "delayed-turn") ||
                   cursorIs(cursor, "inverted-turn") || cursorIs(cursor, "delayed-inverted-turn") ||
                   cursorIs(cursor, "vertical-turn") || cursorIs(cursor, "inverted-vertical-turn") ||
                   cursorIs(cursor, "shake") || cursorIs(cursor, "wavy-line") || cursorIs(cursor, "mordent") ||
                   cursorIs(cursor, "inverted-mordent") || cursorIs(cursor, "schleifer") ||
                   cursorIs(cursor, "tremolo") || cursorIs(cursor, "haydn") || cursorIs(cursor, "other-ornament")))
    {
        out.setChoice(parseOrnamentsGroupChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "trill-mark");
    }
    while (cursorIs(cursor, "accidental-mark"))
    {
        out.addAccidentalMark(parseAccidentalMark(cursor, context));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeOrnamentsGroup(const OrnamentsGroup &v, pugi::xml_node el)
{
    serializeOrnamentsGroupChoice(v.choice(), el);
    for (const auto &item : v.accidentalMark())
    {
        serializeAccidentalMark(item, el, "accidental-mark");
    }
}

} // namespace mx::core
