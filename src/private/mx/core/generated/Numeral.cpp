// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Numeral.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const NumeralRoot &Numeral::numeralRoot() const noexcept
{
    return m_numeralRoot;
}

void Numeral::setNumeralRoot(NumeralRoot value)
{
    m_numeralRoot = std::move(value);
}

const std::optional<HarmonyAlter> &Numeral::numeralAlter() const noexcept
{
    return m_numeralAlter;
}

void Numeral::setNumeralAlter(std::optional<HarmonyAlter> value)
{
    m_numeralAlter = std::move(value);
}

const std::optional<NumeralKey> &Numeral::numeralKey() const noexcept
{
    return m_numeralKey;
}

void Numeral::setNumeralKey(std::optional<NumeralKey> value)
{
    m_numeralKey = std::move(value);
}

Numeral parseNumeral(pugi::xml_node el)
{
    return parseNumeral(el, ParseContext{});
}

Numeral parseNumeral(pugi::xml_node el, const ParseContext &context)
{
    Numeral out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseNumeralContent(out, el, context);
    return out;
}

void parseNumeralContent(Numeral &out, pugi::xml_node el)
{
    parseNumeralContent(out, el, ParseContext{});
}

void parseNumeralContent(Numeral &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "numeral-root"))
    {
        out.setNumeralRoot(parseNumeralRoot(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "numeral-root");
    }
    if (cursorIs(cursor, "numeral-alter"))
    {
        out.setNumeralAlter(parseHarmonyAlter(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "numeral-key"))
    {
        out.setNumeralKey(parseNumeralKey(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "numeral-root") || cursorIs(cursor, "numeral-alter") || cursorIs(cursor, "numeral-key"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeNumeral(const Numeral &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeNumeralContent(v, el);
}

void serializeNumeralContent(const Numeral &v, pugi::xml_node el)
{
    serializeNumeralRoot(v.numeralRoot(), el, "numeral-root");
    if (v.numeralAlter())
    {
        serializeHarmonyAlter(*v.numeralAlter(), el, "numeral-alter");
    }
    if (v.numeralKey())
    {
        serializeNumeralKey(*v.numeralKey(), el, "numeral-key");
    }
}

} // namespace mx::core
