// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The harmon-closed-location type indicates which portion of the symbol is filled in when the
/// corresponding harmon-closed-value is half.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class HarmonClosedLocation final
{
  public:
    enum class Tag
    {
        right,
        bottom,
        left,
        top,
    };

    /// The natural zero: the first variant.
    HarmonClosedLocation() noexcept = default;

    static HarmonClosedLocation right() noexcept;
    static HarmonClosedLocation bottom() noexcept;
    static HarmonClosedLocation left() noexcept;
    static HarmonClosedLocation top() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, HarmonClosedLocation &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static HarmonClosedLocation parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static HarmonClosedLocation parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const HarmonClosedLocation &other) const noexcept = default;

  private:
    explicit HarmonClosedLocation(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::right};
};

} // namespace mx::core
