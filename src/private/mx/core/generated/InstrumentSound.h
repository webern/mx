// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"
#include "mx/core/generated/SoundID.h"

#include <cassert>
#include <string>
#include <string_view>
#include <variant>

namespace mx::core
{

/// The instrument-sound value: one of the standard sound-id identifiers, or any other string. The
/// schema leaves the content open (xs:string), so the string member is intrinsic, not a fallback.
/// Tagged union over the schema's member types: exactly one alternative by
/// construction. Alternatives are positional; dispatch is by Kind/index.
class InstrumentSound final
{
  public:
    enum class Kind
    {
        soundID,
        string,
    };

    /// The natural zero: the first member's natural zero.
    InstrumentSound();

    static InstrumentSound soundID(SoundID value);
    static InstrumentSound string(std::string value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isSoundID() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isSoundID().
    const SoundID &asSoundID() const noexcept
    {
        assert(isSoundID());
        return *std::get_if<0>(&m_value);
    }

    bool isString() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isString().
    const std::string &asString() const noexcept
    {
        assert(isString());
        return *std::get_if<1>(&m_value);
    }

    std::string toString() const;

    /// Strict: members tried in schema order.
    static bool tryParse(std::string_view text, InstrumentSound &out);

    /// Lenient: unparseable text falls back to the first member's lenient
    /// parse (never produces an invalid value).
    static InstrumentSound parse(std::string_view text);

    /// Lenient, and says whether no member matched or a matched number was
    /// clamped.
    static InstrumentSound parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const InstrumentSound &other) const = default;

  private:
    using Storage = std::variant<SoundID, std::string>;

    explicit InstrumentSound(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

} // namespace mx::core
