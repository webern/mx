// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PartTranspose.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const TransposeGroup &PartTranspose::transpose() const noexcept
{
    return m_transpose;
}

void PartTranspose::setTranspose(TransposeGroup value)
{
    m_transpose = std::move(value);
}

PartTranspose parsePartTranspose(pugi::xml_node el)
{
    return parsePartTranspose(el, ParseContext{});
}

PartTranspose parsePartTranspose(pugi::xml_node el, const ParseContext &context)
{
    PartTranspose out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parsePartTransposeContent(out, el, context);
    return out;
}

void parsePartTransposeContent(PartTranspose &out, pugi::xml_node el)
{
    parsePartTransposeContent(out, el, ParseContext{});
}

void parsePartTransposeContent(PartTranspose &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "diatonic") || cursorIs(cursor, "chromatic")))
    {
        out.setTranspose(parseTransposeGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "diatonic");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "diatonic") || cursorIs(cursor, "chromatic") || cursorIs(cursor, "octave-change") ||
            cursorIs(cursor, "double"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePartTranspose(const PartTranspose &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializePartTransposeContent(v, el);
}

void serializePartTransposeContent(const PartTranspose &v, pugi::xml_node el)
{
    serializeTransposeGroup(v.transpose(), el);
}

} // namespace mx::core
