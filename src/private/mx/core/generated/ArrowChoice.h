// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/ArrowChoiceGroup.h"
#include "mx/core/generated/CircularArrow.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class ArrowChoice final
{
  public:
    enum class Kind
    {
        group,
        circularArrow,
    };

    /// The natural zero: the first alternative.
    ArrowChoice();

    static ArrowChoice group(ArrowChoiceGroup value);
    static ArrowChoice circularArrow(CircularArrow value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isGroup().
    const ArrowChoiceGroup &asGroup() const noexcept
    {
        assert(isGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isCircularArrow() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isCircularArrow().
    const CircularArrow &asCircularArrow() const noexcept
    {
        assert(isCircularArrow());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const ArrowChoice &other) const = default;

  private:
    using Storage = std::variant<ArrowChoiceGroup, CircularArrow>;

    explicit ArrowChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
ArrowChoice parseArrowChoice(pugi::xml_node el, pugi::xml_node &cursor);

ArrowChoice parseArrowChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeArrowChoice(const ArrowChoice &v, pugi::xml_node el);

} // namespace mx::core
