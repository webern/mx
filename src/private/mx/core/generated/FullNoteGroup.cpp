// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/FullNoteGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

bool FullNoteGroup::chord() const noexcept
{
    return m_chord;
}

void FullNoteGroup::setChord(bool value) noexcept
{
    m_chord = value;
}

const FullNoteGroupChoice &FullNoteGroup::choice() const noexcept
{
    return m_choice;
}

void FullNoteGroup::setChoice(FullNoteGroupChoice value)
{
    m_choice = std::move(value);
}

FullNoteGroup parseFullNoteGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    FullNoteGroup out;
    if (cursorIs(cursor, "chord"))
    {
        parseEmpty(cursor, context);
        out.setChord(true);
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "pitch") || cursorIs(cursor, "unpitched") || cursorIs(cursor, "rest")))
    {
        out.setChoice(parseFullNoteGroupChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "pitch");
    }
    return out;
}

void serializeFullNoteGroup(const FullNoteGroup &v, pugi::xml_node el)
{
    if (v.chord())
    {
        el.append_child("chord");
    }
    serializeFullNoteGroupChoice(v.choice(), el);
}

} // namespace mx::core
