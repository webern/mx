// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/RotationDegrees.h"

#include "mx/core/Lexical.h"

#include <utility>

namespace mx::core
{

Decimal clampedRotationDegrees(Decimal v)
{
    if (v.value() < -180.0)
    {
        v.setValue(-180.0);
    }
    if (v.value() > 180.0)
    {
        v.setValue(180.0);
    }
    return v;
}

RotationDegrees::RotationDegrees() : m_value{clampedRotationDegrees(Decimal{})}
{
}

RotationDegrees::RotationDegrees(Decimal value) : m_value{clampedRotationDegrees(std::move(value))}
{
}

void RotationDegrees::setValue(Decimal value)
{
    m_value = clampedRotationDegrees(std::move(value));
}

std::string RotationDegrees::toString() const
{
    return m_value.toString();
}

bool RotationDegrees::tryParse(std::string_view text, RotationDegrees &out)
{
    ValueParseOutcome outcome = ValueParseOutcome::valid;
    RotationDegrees parsed = parse(text, outcome);
    if (outcome == ValueParseOutcome::invalid)
    {
        return false;
    }
    out = std::move(parsed);
    return true;
}

RotationDegrees RotationDegrees::parse(std::string_view text)
{
    ValueParseOutcome outcome = ValueParseOutcome::valid;
    return parse(text, outcome);
}

RotationDegrees RotationDegrees::parse(std::string_view text, ValueParseOutcome &outcome)
{
    Decimal v;
    if (!Decimal::tryParse(text, v))
    {
        outcome = ValueParseOutcome::invalid;
        return RotationDegrees{};
    }
    RotationDegrees out{v};
    outcome = out.value() == v ? ValueParseOutcome::valid : ValueParseOutcome::adjusted;
    return out;
}

} // namespace mx::core
