// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/EmptyPrintObjectStyleAlign.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The system-dividers element indicates the presence or absence of system dividers (also known as
/// system separation marks) between systems displayed on the same page. Dividers on the left and
/// right side of the page are controlled by the left-divider and right-divider elements
/// respectively. The default vertical position is half the system-distance value from the top of the
/// system that is below the divider. The default horizontal position is the left and right system
/// margin, respectively. When used in the print element, the system-dividers element affects the
/// dividers that would appear between the current system and the previous system.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class SystemDividers final
{
  public:
    const EmptyPrintObjectStyleAlign &leftDivider() const noexcept;
    void setLeftDivider(EmptyPrintObjectStyleAlign value);
    const EmptyPrintObjectStyleAlign &rightDivider() const noexcept;
    void setRightDivider(EmptyPrintObjectStyleAlign value);
    bool operator==(const SystemDividers &other) const = default;

  private:
    EmptyPrintObjectStyleAlign m_leftDivider{};
    EmptyPrintObjectStyleAlign m_rightDivider{};
};

SystemDividers parseSystemDividers(pugi::xml_node el, const ParseContext &context);

void parseSystemDividersContent(SystemDividers &out, pugi::xml_node el, const ParseContext &context);

void serializeSystemDividers(const SystemDividers &v, pugi::xml_node parent, const char *tag);

void serializeSystemDividersContent(const SystemDividers &v, pugi::xml_node el);

} // namespace mx::core
