// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PartwiseMeasure.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::string &PartwiseMeasure::number() const noexcept
{
    return m_number;
}

void PartwiseMeasure::setNumber(std::string value)
{
    m_number = std::move(value);
}

const std::optional<MeasureText> &PartwiseMeasure::text() const noexcept
{
    return m_text;
}

void PartwiseMeasure::setText(std::optional<MeasureText> value)
{
    m_text = std::move(value);
}

const std::optional<YesNo> &PartwiseMeasure::implicit() const noexcept
{
    return m_implicit;
}

void PartwiseMeasure::setImplicit(std::optional<YesNo> value)
{
    m_implicit = std::move(value);
}

const std::optional<YesNo> &PartwiseMeasure::nonControlling() const noexcept
{
    return m_nonControlling;
}

void PartwiseMeasure::setNonControlling(std::optional<YesNo> value)
{
    m_nonControlling = std::move(value);
}

const std::optional<Tenths> &PartwiseMeasure::width() const noexcept
{
    return m_width;
}

void PartwiseMeasure::setWidth(std::optional<Tenths> value)
{
    m_width = std::move(value);
}

const std::optional<Token> &PartwiseMeasure::id() const noexcept
{
    return m_id;
}

void PartwiseMeasure::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const MusicDataChoice> PartwiseMeasure::musicData() const noexcept
{
    return m_musicData;
}

void PartwiseMeasure::addMusicData(MusicDataChoice value)
{
    m_musicData.push_back(std::move(value));
}

void PartwiseMeasure::setMusicData(std::vector<MusicDataChoice> value)
{
    m_musicData = std::move(value);
}

PartwiseMeasure parsePartwiseMeasure(pugi::xml_node el, const ParseContext &context)
{
    PartwiseMeasure out;
    bool seen_number = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            seen_number = true;
            out.setNumber(std::string{a.value()});
        }
        else if (aname == "text")
        {
            out.setText(parseValue<MeasureText>(a.value(), context, el, "text"));
        }
        else if (aname == "implicit")
        {
            out.setImplicit(parseValue<YesNo>(a.value(), context, el, "implicit"));
        }
        else if (aname == "non-controlling")
        {
            out.setNonControlling(parseValue<YesNo>(a.value(), context, el, "non-controlling"));
        }
        else if (aname == "width")
        {
            out.setWidth(parseValue<Tenths>(a.value(), context, el, "width"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_number)
    {
        throwMissingAttribute(el, "number");
    }
    parsePartwiseMeasureContent(out, el, context);
    return out;
}

void parsePartwiseMeasureContent(PartwiseMeasure &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor && (cursorIs(cursor, "note") || cursorIs(cursor, "backup") || cursorIs(cursor, "forward") ||
                      cursorIs(cursor, "direction") || cursorIs(cursor, "attributes") || cursorIs(cursor, "harmony") ||
                      cursorIs(cursor, "figured-bass") || cursorIs(cursor, "print") || cursorIs(cursor, "sound") ||
                      cursorIs(cursor, "listening") || cursorIs(cursor, "barline") || cursorIs(cursor, "grouping") ||
                      cursorIs(cursor, "link") || cursorIs(cursor, "bookmark")))
    {
        out.addMusicData(parseMusicDataChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "note") || cursorIs(cursor, "backup") || cursorIs(cursor, "forward") ||
            cursorIs(cursor, "direction") || cursorIs(cursor, "attributes") || cursorIs(cursor, "harmony") ||
            cursorIs(cursor, "figured-bass") || cursorIs(cursor, "print") || cursorIs(cursor, "sound") ||
            cursorIs(cursor, "listening") || cursorIs(cursor, "barline") || cursorIs(cursor, "grouping") ||
            cursorIs(cursor, "link") || cursorIs(cursor, "bookmark"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePartwiseMeasure(const PartwiseMeasure &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "number", v.number());
    if (v.text())
    {
        setAttribute(el, "text", v.text()->toString());
    }
    if (v.implicit())
    {
        setAttribute(el, "implicit", v.implicit()->toString());
    }
    if (v.nonControlling())
    {
        setAttribute(el, "non-controlling", v.nonControlling()->toString());
    }
    if (v.width())
    {
        setAttribute(el, "width", v.width()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializePartwiseMeasureContent(v, el);
}

void serializePartwiseMeasureContent(const PartwiseMeasure &v, pugi::xml_node el)
{
    for (const auto &item : v.musicData())
    {
        serializeMusicDataChoice(item, el);
    }
}

} // namespace mx::core
