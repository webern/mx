// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Player.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &Player::id() const noexcept
{
    return m_id;
}

void Player::setID(Token value)
{
    m_id = std::move(value);
}

const std::string &Player::playerName() const noexcept
{
    return m_playerName;
}

void Player::setPlayerName(std::string value)
{
    m_playerName = std::move(value);
}

Player parsePlayer(pugi::xml_node el)
{
    return parsePlayer(el, ParseContext{});
}

Player parsePlayer(pugi::xml_node el, const ParseContext &context)
{
    Player out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parsePlayerContent(out, el, context);
    return out;
}

void parsePlayerContent(Player &out, pugi::xml_node el)
{
    parsePlayerContent(out, el, ParseContext{});
}

void parsePlayerContent(Player &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "player-name"))
    {
        out.setPlayerName(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "player-name");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "player-name"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePlayer(const Player &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializePlayerContent(v, el);
}

void serializePlayerContent(const Player &v, pugi::xml_node el)
{
    writeTextElement(el, "player-name", v.playerName());
}

} // namespace mx::core
