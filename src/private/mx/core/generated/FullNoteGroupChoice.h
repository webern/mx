// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Pitch.h"
#include "mx/core/generated/Rest.h"
#include "mx/core/generated/Unpitched.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class FullNoteGroupChoice final
{
  public:
    enum class Kind
    {
        pitch,
        unpitched,
        rest,
    };

    /// The natural zero: the first alternative.
    FullNoteGroupChoice();

    static FullNoteGroupChoice pitch(Pitch value);
    static FullNoteGroupChoice unpitched(Unpitched value);
    static FullNoteGroupChoice rest(Rest value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isPitch() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isPitch().
    const Pitch &asPitch() const noexcept
    {
        assert(isPitch());
        return *std::get_if<0>(&m_value);
    }

    bool isUnpitched() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isUnpitched().
    const Unpitched &asUnpitched() const noexcept
    {
        assert(isUnpitched());
        return *std::get_if<1>(&m_value);
    }

    bool isRest() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isRest().
    const Rest &asRest() const noexcept
    {
        assert(isRest());
        return *std::get_if<2>(&m_value);
    }

    bool operator==(const FullNoteGroupChoice &other) const = default;

  private:
    using Storage = std::variant<Pitch, Unpitched, Rest>;

    explicit FullNoteGroupChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
FullNoteGroupChoice parseFullNoteGroupChoice(pugi::xml_node el, pugi::xml_node &cursor);

FullNoteGroupChoice parseFullNoteGroupChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeFullNoteGroupChoice(const FullNoteGroupChoice &v, pugi::xml_node el);

} // namespace mx::core
