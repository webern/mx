// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PlayChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

PlayChoice::PlayChoice()
{
    *this = PlayChoice::ipa(std::string{});
}

PlayChoice PlayChoice::ipa(std::string value)
{
    return PlayChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

PlayChoice PlayChoice::mute(Mute value)
{
    return PlayChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

PlayChoice PlayChoice::semiPitched(SemiPitched value)
{
    return PlayChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

PlayChoice PlayChoice::otherPlay(OtherPlay value)
{
    return PlayChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

PlayChoice parsePlayChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "ipa")))
    {
        std::string value = std::string{childText(cursor)};
        cursor = nextElement(cursor);
        return PlayChoice::ipa(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "mute")))
    {
        Mute value = parseValue<Mute>(childText(cursor), context, cursor, nullptr);
        cursor = nextElement(cursor);
        return PlayChoice::mute(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "semi-pitched")))
    {
        SemiPitched value = parseValue<SemiPitched>(childText(cursor), context, cursor, nullptr);
        cursor = nextElement(cursor);
        return PlayChoice::semiPitched(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-play")))
    {
        OtherPlay value = parseOtherPlay(cursor, context);
        cursor = nextElement(cursor);
        return PlayChoice::otherPlay(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "ipa");
}

void serializePlayChoice(const PlayChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case PlayChoice::Kind::ipa:
        writeTextElement(el, "ipa", v.asIpa());
        break;
    case PlayChoice::Kind::mute:
        writeTextElement(el, "mute", v.asMute().toString());
        break;
    case PlayChoice::Kind::semiPitched:
        writeTextElement(el, "semi-pitched", v.asSemiPitched().toString());
        break;
    case PlayChoice::Kind::otherPlay:
        serializeOtherPlay(v.asOtherPlay(), el, "other-play");
        break;
    }
}

} // namespace mx::core
