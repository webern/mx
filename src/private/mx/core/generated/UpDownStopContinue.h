// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The up-down-stop-continue type is used for octave-shift elements, indicating the direction of the
/// shift from their true pitched values because of printing difficulty.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class UpDownStopContinue final
{
  public:
    enum class Tag
    {
        up,
        down,
        stop,
        continue_,
    };

    /// The natural zero: the first variant.
    UpDownStopContinue() noexcept = default;

    static UpDownStopContinue up() noexcept;
    static UpDownStopContinue down() noexcept;
    static UpDownStopContinue stop() noexcept;
    static UpDownStopContinue continue_() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, UpDownStopContinue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static UpDownStopContinue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static UpDownStopContinue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const UpDownStopContinue &other) const noexcept = default;

  private:
    explicit UpDownStopContinue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::up};
};

} // namespace mx::core
