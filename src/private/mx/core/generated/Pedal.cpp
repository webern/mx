// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Pedal.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const PedalType &Pedal::type() const noexcept
{
    return m_type;
}

void Pedal::setType(PedalType value)
{
    m_type = std::move(value);
}

const std::optional<NumberLevel> &Pedal::number() const noexcept
{
    return m_number;
}

void Pedal::setNumber(std::optional<NumberLevel> value)
{
    m_number = std::move(value);
}

const std::optional<YesNo> &Pedal::line() const noexcept
{
    return m_line;
}

void Pedal::setLine(std::optional<YesNo> value)
{
    m_line = std::move(value);
}

const std::optional<YesNo> &Pedal::sign() const noexcept
{
    return m_sign;
}

void Pedal::setSign(std::optional<YesNo> value)
{
    m_sign = std::move(value);
}

const std::optional<YesNo> &Pedal::abbreviated() const noexcept
{
    return m_abbreviated;
}

void Pedal::setAbbreviated(std::optional<YesNo> value)
{
    m_abbreviated = std::move(value);
}

const std::optional<Tenths> &Pedal::defaultX() const noexcept
{
    return m_defaultX;
}

void Pedal::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Pedal::defaultY() const noexcept
{
    return m_defaultY;
}

void Pedal::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Pedal::relativeX() const noexcept
{
    return m_relativeX;
}

void Pedal::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Pedal::relativeY() const noexcept
{
    return m_relativeY;
}

void Pedal::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Pedal::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Pedal::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Pedal::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Pedal::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Pedal::fontSize() const noexcept
{
    return m_fontSize;
}

void Pedal::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Pedal::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Pedal::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Pedal::color() const noexcept
{
    return m_color;
}

void Pedal::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Pedal::halign() const noexcept
{
    return m_halign;
}

void Pedal::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &Pedal::valign() const noexcept
{
    return m_valign;
}

void Pedal::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const std::optional<Token> &Pedal::id() const noexcept
{
    return m_id;
}

void Pedal::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

Pedal parsePedal(pugi::xml_node el, const ParseContext &context)
{
    Pedal out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<PedalType>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(parseValue<NumberLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "line")
        {
            out.setLine(parseValue<YesNo>(a.value(), context, el, "line"));
        }
        else if (aname == "sign")
        {
            out.setSign(parseValue<YesNo>(a.value(), context, el, "sign"));
        }
        else if (aname == "abbreviated")
        {
            out.setAbbreviated(parseValue<YesNo>(a.value(), context, el, "abbreviated"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "halign")
        {
            out.setHalign(parseValue<LeftCenterRight>(a.value(), context, el, "halign"));
        }
        else if (aname == "valign")
        {
            out.setValign(parseValue<Valign>(a.value(), context, el, "valign"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parsePedalContent(out, el, context);
    return out;
}

void parsePedalContent(Pedal &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializePedal(const Pedal &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.line())
    {
        setAttribute(el, "line", v.line()->toString());
    }
    if (v.sign())
    {
        setAttribute(el, "sign", v.sign()->toString());
    }
    if (v.abbreviated())
    {
        setAttribute(el, "abbreviated", v.abbreviated()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializePedalContent(v, el);
}

void serializePedalContent(const Pedal &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
