// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Stem.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &Stem::defaultX() const noexcept
{
    return m_defaultX;
}

void Stem::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Stem::defaultY() const noexcept
{
    return m_defaultY;
}

void Stem::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Stem::relativeX() const noexcept
{
    return m_relativeX;
}

void Stem::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Stem::relativeY() const noexcept
{
    return m_relativeY;
}

void Stem::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<Color> &Stem::color() const noexcept
{
    return m_color;
}

void Stem::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const StemValue &Stem::value() const noexcept
{
    return m_value;
}

void Stem::setValue(StemValue value)
{
    m_value = std::move(value);
}

Stem parseStem(pugi::xml_node el)
{
    return parseStem(el, ParseContext{});
}

Stem parseStem(pugi::xml_node el, const ParseContext &context)
{
    Stem out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseStemContent(out, el, context);
    return out;
}

void parseStemContent(Stem &out, pugi::xml_node el)
{
    parseStemContent(out, el, ParseContext{});
}

void parseStemContent(Stem &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<StemValue>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeStem(const Stem &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeStemContent(v, el);
}

void serializeStemContent(const Stem &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
