// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/PartGroup.h"
#include "mx/core/generated/ScorePart.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class PartListChoice final
{
  public:
    enum class Kind
    {
        partGroup,
        scorePart,
    };

    /// The natural zero: the first alternative.
    PartListChoice();

    static PartListChoice partGroup(PartGroup value);
    static PartListChoice scorePart(ScorePart value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isPartGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isPartGroup().
    const PartGroup &asPartGroup() const noexcept
    {
        assert(isPartGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isScorePart() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isScorePart().
    const ScorePart &asScorePart() const noexcept
    {
        assert(isScorePart());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const PartListChoice &other) const = default;

  private:
    using Storage = std::variant<PartGroup, ScorePart>;

    explicit PartListChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
PartListChoice parsePartListChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializePartListChoice(const PartListChoice &v, pugi::xml_node el);

} // namespace mx::core
