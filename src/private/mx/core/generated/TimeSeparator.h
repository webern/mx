// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The time-separator type indicates how to display the arrangement between the beats and beat-type
/// values in a time signature. The default value is none. The horizontal, diagonal, and vertical
/// values represent horizontal, diagonal lower-left to upper-right, and vertical lines respectively.
/// For these values, the beats and beat-type values are arranged on either side of the separator
/// line. The none value represents no separator with the beats and beat-type arranged vertically.
/// The adjacent value represents no separator with the beats and beat-type arranged horizontally.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TimeSeparator final
{
  public:
    enum class Tag
    {
        none,
        horizontal,
        diagonal,
        vertical,
        adjacent,
    };

    /// The natural zero: the first variant.
    TimeSeparator() noexcept = default;

    static TimeSeparator none() noexcept;
    static TimeSeparator horizontal() noexcept;
    static TimeSeparator diagonal() noexcept;
    static TimeSeparator vertical() noexcept;
    static TimeSeparator adjacent() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TimeSeparator &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TimeSeparator parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TimeSeparator parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TimeSeparator &other) const noexcept = default;

  private:
    explicit TimeSeparator(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::none};
};

} // namespace mx::core
