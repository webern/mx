// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Harmonic.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Harmonic::printObject() const noexcept
{
    return m_printObject;
}

void Harmonic::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Tenths> &Harmonic::defaultX() const noexcept
{
    return m_defaultX;
}

void Harmonic::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Harmonic::defaultY() const noexcept
{
    return m_defaultY;
}

void Harmonic::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Harmonic::relativeX() const noexcept
{
    return m_relativeX;
}

void Harmonic::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Harmonic::relativeY() const noexcept
{
    return m_relativeY;
}

void Harmonic::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Harmonic::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Harmonic::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Harmonic::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Harmonic::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Harmonic::fontSize() const noexcept
{
    return m_fontSize;
}

void Harmonic::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Harmonic::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Harmonic::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Harmonic::color() const noexcept
{
    return m_color;
}

void Harmonic::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<AboveBelow> &Harmonic::placement() const noexcept
{
    return m_placement;
}

void Harmonic::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<HarmonicChoice> &Harmonic::choice() const noexcept
{
    return m_choice;
}

void Harmonic::setChoice(std::optional<HarmonicChoice> value)
{
    m_choice = std::move(value);
}

const std::optional<HarmonicChoice2> &Harmonic::choice2() const noexcept
{
    return m_choice2;
}

void Harmonic::setChoice2(std::optional<HarmonicChoice2> value)
{
    m_choice2 = std::move(value);
}

Harmonic parseHarmonic(pugi::xml_node el, const ParseContext &context)
{
    Harmonic out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHarmonicContent(out, el, context);
    return out;
}

void parseHarmonicContent(Harmonic &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "natural") || cursorIs(cursor, "artificial")))
    {
        out.setChoice(parseHarmonicChoice(el, cursor, context));
    }
    if (cursor &&
        (cursorIs(cursor, "base-pitch") || cursorIs(cursor, "touching-pitch") || cursorIs(cursor, "sounding-pitch")))
    {
        out.setChoice2(parseHarmonicChoice2(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "natural") || cursorIs(cursor, "artificial") || cursorIs(cursor, "base-pitch") ||
            cursorIs(cursor, "touching-pitch") || cursorIs(cursor, "sounding-pitch"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeHarmonic(const Harmonic &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    serializeHarmonicContent(v, el);
}

void serializeHarmonicContent(const Harmonic &v, pugi::xml_node el)
{
    if (v.choice())
    {
        serializeHarmonicChoice(*v.choice(), el);
    }
    if (v.choice2())
    {
        serializeHarmonicChoice2(*v.choice2(), el);
    }
}

} // namespace mx::core
