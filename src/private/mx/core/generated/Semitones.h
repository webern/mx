// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Decimal.h"
#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The semitones type is a number representing semitones, used for chromatic alteration. A value of
/// -1 corresponds to a flat and a value of 1 to a sharp. Decimal values like 0.5 (quarter tone
/// sharp) are used for microtones.
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class Semitones final
{
  public:
    /// The natural zero: the default value clamped into range.
    Semitones();

    explicit Semitones(Decimal value);

    const Decimal &value() const noexcept
    {
        return m_value;
    }

    void setValue(Decimal value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, Semitones &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static Semitones parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static Semitones parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const Semitones &other) const noexcept = default;

  private:
    Decimal m_value{};
};

} // namespace mx::core
