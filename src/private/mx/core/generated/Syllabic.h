// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// Lyric hyphenation is indicated by the syllabic type. The single, begin, end, and middle values
/// represent single-syllable words, word-beginning syllables, word-ending syllables, and mid-word
/// syllables, respectively.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class Syllabic final
{
  public:
    enum class Tag
    {
        single,
        begin,
        end,
        middle,
    };

    /// The natural zero: the first variant.
    Syllabic() noexcept = default;

    static Syllabic single() noexcept;
    static Syllabic begin() noexcept;
    static Syllabic end() noexcept;
    static Syllabic middle() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, Syllabic &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static Syllabic parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static Syllabic parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Syllabic &other) const noexcept = default;

  private:
    explicit Syllabic(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::single};
};

} // namespace mx::core
