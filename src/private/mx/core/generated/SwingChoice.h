// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"
#include "mx/core/generated/SwingChoiceGroup.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class SwingChoice final
{
  public:
    enum class Kind
    {
        straight,
        group,
    };

    /// The natural zero: the first alternative.
    SwingChoice();

    static SwingChoice straight(Empty value);
    static SwingChoice group(SwingChoiceGroup value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isStraight() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isStraight().
    const Empty &asStraight() const noexcept
    {
        assert(isStraight());
        return *std::get_if<0>(&m_value);
    }

    bool isGroup() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isGroup().
    const SwingChoiceGroup &asGroup() const noexcept
    {
        assert(isGroup());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const SwingChoice &other) const = default;

  private:
    using Storage = std::variant<Empty, SwingChoiceGroup>;

    explicit SwingChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
SwingChoice parseSwingChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeSwingChoice(const SwingChoice &v, pugi::xml_node el);

} // namespace mx::core
