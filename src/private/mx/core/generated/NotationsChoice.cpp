// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/NotationsChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

NotationsChoice::NotationsChoice()
{
    *this = NotationsChoice::tied(Tied{});
}

NotationsChoice NotationsChoice::tied(Tied value)
{
    return NotationsChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

NotationsChoice NotationsChoice::slur(Slur value)
{
    return NotationsChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

NotationsChoice NotationsChoice::tuplet(Tuplet value)
{
    return NotationsChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

NotationsChoice NotationsChoice::glissando(Glissando value)
{
    return NotationsChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

NotationsChoice NotationsChoice::slide(Slide value)
{
    return NotationsChoice{Storage{std::in_place_index<4>, std::move(value)}};
}

NotationsChoice NotationsChoice::ornaments(Ornaments value)
{
    return NotationsChoice{Storage{std::in_place_index<5>, std::move(value)}};
}

NotationsChoice NotationsChoice::technical(Technical value)
{
    return NotationsChoice{Storage{std::in_place_index<6>, std::move(value)}};
}

NotationsChoice NotationsChoice::articulations(Articulations value)
{
    return NotationsChoice{Storage{std::in_place_index<7>, std::move(value)}};
}

NotationsChoice NotationsChoice::dynamics(Dynamics value)
{
    return NotationsChoice{Storage{std::in_place_index<8>, std::move(value)}};
}

NotationsChoice NotationsChoice::fermata(Fermata value)
{
    return NotationsChoice{Storage{std::in_place_index<9>, std::move(value)}};
}

NotationsChoice NotationsChoice::arpeggiate(Arpeggiate value)
{
    return NotationsChoice{Storage{std::in_place_index<10>, std::move(value)}};
}

NotationsChoice NotationsChoice::nonArpeggiate(NonArpeggiate value)
{
    return NotationsChoice{Storage{std::in_place_index<11>, std::move(value)}};
}

NotationsChoice NotationsChoice::accidentalMark(AccidentalMark value)
{
    return NotationsChoice{Storage{std::in_place_index<12>, std::move(value)}};
}

NotationsChoice NotationsChoice::otherNotation(OtherNotation value)
{
    return NotationsChoice{Storage{std::in_place_index<13>, std::move(value)}};
}

NotationsChoice parseNotationsChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseNotationsChoice(el, cursor, ParseContext{});
}

NotationsChoice parseNotationsChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "tied")))
    {
        Tied value = parseTied(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::tied(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "slur")))
    {
        Slur value = parseSlur(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::slur(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "tuplet")))
    {
        Tuplet value = parseTuplet(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::tuplet(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "glissando")))
    {
        Glissando value = parseGlissando(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::glissando(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "slide")))
    {
        Slide value = parseSlide(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::slide(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "ornaments")))
    {
        Ornaments value = parseOrnaments(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::ornaments(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "technical")))
    {
        Technical value = parseTechnical(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::technical(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "articulations")))
    {
        Articulations value = parseArticulations(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::articulations(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "dynamics")))
    {
        Dynamics value = parseDynamics(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::dynamics(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "fermata")))
    {
        Fermata value = parseFermata(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::fermata(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "arpeggiate")))
    {
        Arpeggiate value = parseArpeggiate(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::arpeggiate(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "non-arpeggiate")))
    {
        NonArpeggiate value = parseNonArpeggiate(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::nonArpeggiate(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "accidental-mark")))
    {
        AccidentalMark value = parseAccidentalMark(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::accidentalMark(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-notation")))
    {
        OtherNotation value = parseOtherNotation(cursor, context);
        cursor = nextElement(cursor);
        return NotationsChoice::otherNotation(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "tied");
}

void serializeNotationsChoice(const NotationsChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case NotationsChoice::Kind::tied:
        serializeTied(v.asTied(), el, "tied");
        break;
    case NotationsChoice::Kind::slur:
        serializeSlur(v.asSlur(), el, "slur");
        break;
    case NotationsChoice::Kind::tuplet:
        serializeTuplet(v.asTuplet(), el, "tuplet");
        break;
    case NotationsChoice::Kind::glissando:
        serializeGlissando(v.asGlissando(), el, "glissando");
        break;
    case NotationsChoice::Kind::slide:
        serializeSlide(v.asSlide(), el, "slide");
        break;
    case NotationsChoice::Kind::ornaments:
        serializeOrnaments(v.asOrnaments(), el, "ornaments");
        break;
    case NotationsChoice::Kind::technical:
        serializeTechnical(v.asTechnical(), el, "technical");
        break;
    case NotationsChoice::Kind::articulations:
        serializeArticulations(v.asArticulations(), el, "articulations");
        break;
    case NotationsChoice::Kind::dynamics:
        serializeDynamics(v.asDynamics(), el, "dynamics");
        break;
    case NotationsChoice::Kind::fermata:
        serializeFermata(v.asFermata(), el, "fermata");
        break;
    case NotationsChoice::Kind::arpeggiate:
        serializeArpeggiate(v.asArpeggiate(), el, "arpeggiate");
        break;
    case NotationsChoice::Kind::nonArpeggiate:
        serializeNonArpeggiate(v.asNonArpeggiate(), el, "non-arpeggiate");
        break;
    case NotationsChoice::Kind::accidentalMark:
        serializeAccidentalMark(v.asAccidentalMark(), el, "accidental-mark");
        break;
    case NotationsChoice::Kind::otherNotation:
        serializeOtherNotation(v.asOtherNotation(), el, "other-notation");
        break;
    }
}

} // namespace mx::core
