// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Elision.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<SmuflLyricsGlyphName> &Elision::smufl() const noexcept
{
    return m_smufl;
}

void Elision::setSmufl(std::optional<SmuflLyricsGlyphName> value)
{
    m_smufl = std::move(value);
}

const std::optional<FontFamily> &Elision::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Elision::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Elision::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Elision::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Elision::fontSize() const noexcept
{
    return m_fontSize;
}

void Elision::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Elision::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Elision::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Elision::color() const noexcept
{
    return m_color;
}

void Elision::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::string &Elision::value() const noexcept
{
    return m_value;
}

void Elision::setValue(std::string value)
{
    m_value = std::move(value);
}

Elision parseElision(pugi::xml_node el)
{
    return parseElision(el, ParseContext{});
}

Elision parseElision(pugi::xml_node el, const ParseContext &context)
{
    Elision out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "smufl")
        {
            out.setSmufl(parseValue<SmuflLyricsGlyphName>(a.value(), context, el, "smufl"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseElisionContent(out, el, context);
    return out;
}

void parseElisionContent(Elision &out, pugi::xml_node el)
{
    parseElisionContent(out, el, ParseContext{});
}

void parseElisionContent(Elision &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeElision(const Elision &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.smufl())
    {
        setAttribute(el, "smufl", v.smufl()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeElisionContent(v, el);
}

void serializeElisionContent(const Elision &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
