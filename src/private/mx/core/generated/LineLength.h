// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The line-length type distinguishes between different line lengths for doit, falloff, plop, and
/// scoop articulations.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class LineLength final
{
  public:
    enum class Tag
    {
        short_,
        medium,
        long_,
    };

    /// The natural zero: the first variant.
    LineLength() noexcept = default;

    static LineLength short_() noexcept;
    static LineLength medium() noexcept;
    static LineLength long_() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, LineLength &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static LineLength parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static LineLength parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const LineLength &other) const noexcept = default;

  private:
    explicit LineLength(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::short_};
};

} // namespace mx::core
