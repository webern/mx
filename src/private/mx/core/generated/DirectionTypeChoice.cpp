// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/DirectionTypeChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

DirectionTypeChoice::DirectionTypeChoice()
{
    *this = DirectionTypeChoice::rehearsal(OneOrMore<FormattedTextID>{});
}

DirectionTypeChoice DirectionTypeChoice::rehearsal(OneOrMore<FormattedTextID> value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::segno(OneOrMore<Segno> value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::coda(OneOrMore<Coda> value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::choice(OneOrMore<DirectionTypeChoiceChoice> value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::wedge(Wedge value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<4>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::dynamics(OneOrMore<Dynamics> value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<5>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::dashes(Dashes value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<6>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::bracket(Bracket value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<7>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::pedal(Pedal value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<8>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::metronome(Metronome value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<9>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::octaveShift(OctaveShift value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<10>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::harpPedals(HarpPedals value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<11>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::damp(EmptyPrintStyleAlignID value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<12>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::dampAll(EmptyPrintStyleAlignID value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<13>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::eyeglasses(EmptyPrintStyleAlignID value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<14>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::stringMute(StringMute value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<15>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::scordatura(Scordatura value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<16>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::image(Image value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<17>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::principalVoice(PrincipalVoice value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<18>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::percussion(OneOrMore<Percussion> value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<19>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::accordionRegistration(AccordionRegistration value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<20>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::staffDivide(StaffDivide value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<21>, std::move(value)}};
}

DirectionTypeChoice DirectionTypeChoice::otherDirection(OtherDirection value)
{
    return DirectionTypeChoice{Storage{std::in_place_index<22>, std::move(value)}};
}

DirectionTypeChoice parseDirectionTypeChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "rehearsal")))
    {
        OneOrMore<FormattedTextID> items{parseFormattedTextID(cursor, context)};
        cursor = nextElement(cursor);
        while (cursor && (cursorIs(cursor, "rehearsal")))
        {
            items.add(parseFormattedTextID(cursor, context));
            cursor = nextElement(cursor);
        }
        return DirectionTypeChoice::rehearsal(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "segno")))
    {
        OneOrMore<Segno> items{parseSegno(cursor, context)};
        cursor = nextElement(cursor);
        while (cursor && (cursorIs(cursor, "segno")))
        {
            items.add(parseSegno(cursor, context));
            cursor = nextElement(cursor);
        }
        return DirectionTypeChoice::segno(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "coda")))
    {
        OneOrMore<Coda> items{parseCoda(cursor, context)};
        cursor = nextElement(cursor);
        while (cursor && (cursorIs(cursor, "coda")))
        {
            items.add(parseCoda(cursor, context));
            cursor = nextElement(cursor);
        }
        return DirectionTypeChoice::coda(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "words") || cursorIs(cursor, "symbol")))
    {
        OneOrMore<DirectionTypeChoiceChoice> items{parseDirectionTypeChoiceChoice(el, cursor, context)};
        while (cursor && (cursorIs(cursor, "words") || cursorIs(cursor, "symbol")))
        {
            items.add(parseDirectionTypeChoiceChoice(el, cursor, context));
        }
        return DirectionTypeChoice::choice(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "wedge")))
    {
        Wedge value = parseWedge(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::wedge(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "dynamics")))
    {
        OneOrMore<Dynamics> items{parseDynamics(cursor, context)};
        cursor = nextElement(cursor);
        while (cursor && (cursorIs(cursor, "dynamics")))
        {
            items.add(parseDynamics(cursor, context));
            cursor = nextElement(cursor);
        }
        return DirectionTypeChoice::dynamics(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "dashes")))
    {
        Dashes value = parseDashes(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::dashes(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "bracket")))
    {
        Bracket value = parseBracket(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::bracket(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "pedal")))
    {
        Pedal value = parsePedal(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::pedal(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "metronome")))
    {
        Metronome value = parseMetronome(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::metronome(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "octave-shift")))
    {
        OctaveShift value = parseOctaveShift(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::octaveShift(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "harp-pedals")))
    {
        HarpPedals value = parseHarpPedals(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::harpPedals(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "damp")))
    {
        EmptyPrintStyleAlignID value = parseEmptyPrintStyleAlignID(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::damp(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "damp-all")))
    {
        EmptyPrintStyleAlignID value = parseEmptyPrintStyleAlignID(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::dampAll(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "eyeglasses")))
    {
        EmptyPrintStyleAlignID value = parseEmptyPrintStyleAlignID(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::eyeglasses(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "string-mute")))
    {
        StringMute value = parseStringMute(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::stringMute(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "scordatura")))
    {
        Scordatura value = parseScordatura(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::scordatura(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "image")))
    {
        Image value = parseImage(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::image(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "principal-voice")))
    {
        PrincipalVoice value = parsePrincipalVoice(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::principalVoice(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "percussion")))
    {
        OneOrMore<Percussion> items{parsePercussion(cursor, context)};
        cursor = nextElement(cursor);
        while (cursor && (cursorIs(cursor, "percussion")))
        {
            items.add(parsePercussion(cursor, context));
            cursor = nextElement(cursor);
        }
        return DirectionTypeChoice::percussion(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "accordion-registration")))
    {
        AccordionRegistration value = parseAccordionRegistration(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::accordionRegistration(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "staff-divide")))
    {
        StaffDivide value = parseStaffDivide(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::staffDivide(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-direction")))
    {
        OtherDirection value = parseOtherDirection(cursor, context);
        cursor = nextElement(cursor);
        return DirectionTypeChoice::otherDirection(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "rehearsal");
}

void serializeDirectionTypeChoice(const DirectionTypeChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case DirectionTypeChoice::Kind::rehearsal:
        for (const auto &item : v.asRehearsal().items())
        {
            serializeFormattedTextID(item, el, "rehearsal");
        }
        break;
    case DirectionTypeChoice::Kind::segno:
        for (const auto &item : v.asSegno().items())
        {
            serializeSegno(item, el, "segno");
        }
        break;
    case DirectionTypeChoice::Kind::coda:
        for (const auto &item : v.asCoda().items())
        {
            serializeCoda(item, el, "coda");
        }
        break;
    case DirectionTypeChoice::Kind::choice:
        for (const auto &item : v.asChoice().items())
        {
            serializeDirectionTypeChoiceChoice(item, el);
        }
        break;
    case DirectionTypeChoice::Kind::wedge:
        serializeWedge(v.asWedge(), el, "wedge");
        break;
    case DirectionTypeChoice::Kind::dynamics:
        for (const auto &item : v.asDynamics().items())
        {
            serializeDynamics(item, el, "dynamics");
        }
        break;
    case DirectionTypeChoice::Kind::dashes:
        serializeDashes(v.asDashes(), el, "dashes");
        break;
    case DirectionTypeChoice::Kind::bracket:
        serializeBracket(v.asBracket(), el, "bracket");
        break;
    case DirectionTypeChoice::Kind::pedal:
        serializePedal(v.asPedal(), el, "pedal");
        break;
    case DirectionTypeChoice::Kind::metronome:
        serializeMetronome(v.asMetronome(), el, "metronome");
        break;
    case DirectionTypeChoice::Kind::octaveShift:
        serializeOctaveShift(v.asOctaveShift(), el, "octave-shift");
        break;
    case DirectionTypeChoice::Kind::harpPedals:
        serializeHarpPedals(v.asHarpPedals(), el, "harp-pedals");
        break;
    case DirectionTypeChoice::Kind::damp:
        serializeEmptyPrintStyleAlignID(v.asDamp(), el, "damp");
        break;
    case DirectionTypeChoice::Kind::dampAll:
        serializeEmptyPrintStyleAlignID(v.asDampAll(), el, "damp-all");
        break;
    case DirectionTypeChoice::Kind::eyeglasses:
        serializeEmptyPrintStyleAlignID(v.asEyeglasses(), el, "eyeglasses");
        break;
    case DirectionTypeChoice::Kind::stringMute:
        serializeStringMute(v.asStringMute(), el, "string-mute");
        break;
    case DirectionTypeChoice::Kind::scordatura:
        serializeScordatura(v.asScordatura(), el, "scordatura");
        break;
    case DirectionTypeChoice::Kind::image:
        serializeImage(v.asImage(), el, "image");
        break;
    case DirectionTypeChoice::Kind::principalVoice:
        serializePrincipalVoice(v.asPrincipalVoice(), el, "principal-voice");
        break;
    case DirectionTypeChoice::Kind::percussion:
        for (const auto &item : v.asPercussion().items())
        {
            serializePercussion(item, el, "percussion");
        }
        break;
    case DirectionTypeChoice::Kind::accordionRegistration:
        serializeAccordionRegistration(v.asAccordionRegistration(), el, "accordion-registration");
        break;
    case DirectionTypeChoice::Kind::staffDivide:
        serializeStaffDivide(v.asStaffDivide(), el, "staff-divide");
        break;
    case DirectionTypeChoice::Kind::otherDirection:
        serializeOtherDirection(v.asOtherDirection(), el, "other-direction");
        break;
    }
}

} // namespace mx::core
