// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Numeral.h"
#include "mx/core/generated/Root.h"
#include "mx/core/generated/StyleText.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class HarmonyChordGroupChoice final
{
  public:
    enum class Kind
    {
        root,
        numeral,
        function,
    };

    /// The natural zero: the first alternative.
    HarmonyChordGroupChoice();

    static HarmonyChordGroupChoice root(Root value);
    static HarmonyChordGroupChoice numeral(Numeral value);
    static HarmonyChordGroupChoice function(StyleText value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isRoot() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isRoot().
    const Root &asRoot() const noexcept
    {
        assert(isRoot());
        return *std::get_if<0>(&m_value);
    }

    bool isNumeral() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isNumeral().
    const Numeral &asNumeral() const noexcept
    {
        assert(isNumeral());
        return *std::get_if<1>(&m_value);
    }

    bool isFunction() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isFunction().
    const StyleText &asFunction() const noexcept
    {
        assert(isFunction());
        return *std::get_if<2>(&m_value);
    }

    bool operator==(const HarmonyChordGroupChoice &other) const = default;

  private:
    using Storage = std::variant<Root, Numeral, StyleText>;

    explicit HarmonyChordGroupChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
HarmonyChordGroupChoice parseHarmonyChordGroupChoice(pugi::xml_node el, pugi::xml_node &cursor,
                                                     const ParseContext &context);

void serializeHarmonyChordGroupChoice(const HarmonyChordGroupChoice &v, pugi::xml_node el);

} // namespace mx::core
