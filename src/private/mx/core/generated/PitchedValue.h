// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The pitched-value type represents pictograms for pitched percussion instruments. The chimes and
/// tubular chimes values distinguish the single-line and double-line versions of the pictogram.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class PitchedValue final
{
  public:
    enum class Tag
    {
        celesta,
        chimes,
        glockenspiel,
        lithophone,
        mallet,
        marimba,
        steelDrums,
        tubaphone,
        tubularChimes,
        vibraphone,
        xylophone,
    };

    /// The natural zero: the first variant.
    PitchedValue() noexcept = default;

    static PitchedValue celesta() noexcept;
    static PitchedValue chimes() noexcept;
    static PitchedValue glockenspiel() noexcept;
    static PitchedValue lithophone() noexcept;
    static PitchedValue mallet() noexcept;
    static PitchedValue marimba() noexcept;
    static PitchedValue steelDrums() noexcept;
    static PitchedValue tubaphone() noexcept;
    static PitchedValue tubularChimes() noexcept;
    static PitchedValue vibraphone() noexcept;
    static PitchedValue xylophone() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, PitchedValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static PitchedValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static PitchedValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const PitchedValue &other) const noexcept = default;

  private:
    explicit PitchedValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::celesta};
};

} // namespace mx::core
