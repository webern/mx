// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The smufl-wavy-line-glyph-name type is used to reference a specific Standard Music Font Layout
/// (SMuFL) wavy line character. The value is a SMuFL canonical glyph name that either starts with
/// wiggle, or begins with guitar and ends with VibratoStroke. This includes all the glyphs in the
/// Multi-segment lines range, excluding the beam glyphs.
/// Structural representation (plan §2.2) of the two branches the pattern
/// allows: wiggle<suffix> (suffix non-empty), or guitar<middle>VibratoStroke
/// (middle possibly empty). The literals are structural; only the variable
/// part is stored, so a pattern-invalid name cannot exist.
class SmuflWavyLineGlyphName final
{
  public:
    enum class Kind
    {
        wiggle,
        guitarVibratoStroke,
    };

    /// The natural zero: wiggle with the placeholder suffix "X".
    SmuflWavyLineGlyphName();

    SmuflWavyLineGlyphName(Kind kind, std::string part);

    Kind kind() const noexcept
    {
        return m_kind;
    }

    /// The variable part: the wiggle suffix, or the guitar middle.
    const std::string &part() const noexcept
    {
        return m_part;
    }

    void setKind(Kind value);

    void setPart(std::string value);

    std::string toString() const;

    /// Strict: exactly one of the pattern's two branches.
    static bool tryParse(std::string_view text, SmuflWavyLineGlyphName &out);

    /// Lenient: repairs into the nearest valid glyph name.
    static SmuflWavyLineGlyphName parse(std::string_view text);

    /// Lenient, and says whether the text had to be repaired.
    static SmuflWavyLineGlyphName parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const SmuflWavyLineGlyphName &other) const noexcept = default;

  private:
    void repair();

    Kind m_kind{Kind::wiggle};
    std::string m_part;
};

} // namespace mx::core
