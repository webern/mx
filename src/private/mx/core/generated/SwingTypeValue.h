// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The swing-type-value type specifies the note type, either eighth or 16th, to which the ratio
/// defined in the swing element is applied.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class SwingTypeValue final
{
  public:
    enum class Tag
    {
        _16th,
        eighth,
    };

    /// The natural zero: the first variant.
    SwingTypeValue() noexcept = default;

    static SwingTypeValue _16th() noexcept;
    static SwingTypeValue eighth() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, SwingTypeValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static SwingTypeValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static SwingTypeValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const SwingTypeValue &other) const noexcept = default;

  private:
    explicit SwingTypeValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::_16th};
};

} // namespace mx::core
