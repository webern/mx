// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/FullNoteGroupChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

FullNoteGroupChoice::FullNoteGroupChoice()
{
    *this = FullNoteGroupChoice::pitch(Pitch{});
}

FullNoteGroupChoice FullNoteGroupChoice::pitch(Pitch value)
{
    return FullNoteGroupChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

FullNoteGroupChoice FullNoteGroupChoice::unpitched(Unpitched value)
{
    return FullNoteGroupChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

FullNoteGroupChoice FullNoteGroupChoice::rest(Rest value)
{
    return FullNoteGroupChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

FullNoteGroupChoice parseFullNoteGroupChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseFullNoteGroupChoice(el, cursor, ParseContext{});
}

FullNoteGroupChoice parseFullNoteGroupChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "pitch")))
    {
        Pitch value = parsePitch(cursor, context);
        cursor = nextElement(cursor);
        return FullNoteGroupChoice::pitch(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "unpitched")))
    {
        Unpitched value = parseUnpitched(cursor, context);
        cursor = nextElement(cursor);
        return FullNoteGroupChoice::unpitched(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "rest")))
    {
        Rest value = parseRest(cursor, context);
        cursor = nextElement(cursor);
        return FullNoteGroupChoice::rest(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "pitch");
}

void serializeFullNoteGroupChoice(const FullNoteGroupChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case FullNoteGroupChoice::Kind::pitch:
        serializePitch(v.asPitch(), el, "pitch");
        break;
    case FullNoteGroupChoice::Kind::unpitched:
        serializeUnpitched(v.asUnpitched(), el, "unpitched");
        break;
    case FullNoteGroupChoice::Kind::rest:
        serializeRest(v.asRest(), el, "rest");
        break;
    }
}

} // namespace mx::core
