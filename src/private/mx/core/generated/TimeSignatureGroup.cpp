// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TimeSignatureGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::string &TimeSignatureGroup::beats() const noexcept
{
    return m_beats;
}

void TimeSignatureGroup::setBeats(std::string value)
{
    m_beats = std::move(value);
}

const std::string &TimeSignatureGroup::beatType() const noexcept
{
    return m_beatType;
}

void TimeSignatureGroup::setBeatType(std::string value)
{
    m_beatType = std::move(value);
}

TimeSignatureGroup parseTimeSignatureGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseTimeSignatureGroup(el, cursor, ParseContext{});
}

TimeSignatureGroup parseTimeSignatureGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    TimeSignatureGroup out;
    if (cursorIs(cursor, "beats"))
    {
        out.setBeats(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "beats");
    }
    if (cursorIs(cursor, "beat-type"))
    {
        out.setBeatType(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "beat-type");
    }
    return out;
}

void serializeTimeSignatureGroup(const TimeSignatureGroup &v, pugi::xml_node el)
{
    writeTextElement(el, "beats", v.beats());
    writeTextElement(el, "beat-type", v.beatType());
}

} // namespace mx::core
