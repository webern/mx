// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PartList.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

std::span<const PartGroup> PartList::partGroup() const noexcept
{
    return m_partGroup;
}

void PartList::addPartGroup(PartGroup value)
{
    m_partGroup.push_back(std::move(value));
}

void PartList::setPartGroup(std::vector<PartGroup> value)
{
    m_partGroup = std::move(value);
}

const ScorePart &PartList::scorePart() const noexcept
{
    return m_scorePart;
}

void PartList::setScorePart(ScorePart value)
{
    m_scorePart = std::move(value);
}

std::span<const PartListChoice> PartList::choice() const noexcept
{
    return m_choice;
}

void PartList::addChoice(PartListChoice value)
{
    m_choice.push_back(std::move(value));
}

void PartList::setChoice(std::vector<PartListChoice> value)
{
    m_choice = std::move(value);
}

PartList parsePartList(pugi::xml_node el, const ParseContext &context)
{
    PartList out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parsePartListContent(out, el, context);
    return out;
}

void parsePartListContent(PartList &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursorIs(cursor, "part-group"))
    {
        out.addPartGroup(parsePartGroup(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "score-part"))
    {
        out.setScorePart(parseScorePart(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "score-part");
    }
    while (cursor && (cursorIs(cursor, "part-group") || cursorIs(cursor, "score-part")))
    {
        out.addChoice(parsePartListChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "part-group") || cursorIs(cursor, "score-part"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePartList(const PartList &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializePartListContent(v, el);
}

void serializePartListContent(const PartList &v, pugi::xml_node el)
{
    for (const auto &item : v.partGroup())
    {
        serializePartGroup(item, el, "part-group");
    }
    serializeScorePart(v.scorePart(), el, "score-part");
    for (const auto &item : v.choice())
    {
        serializePartListChoice(item, el);
    }
}

} // namespace mx::core
