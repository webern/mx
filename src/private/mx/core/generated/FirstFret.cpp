// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/FirstFret.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &FirstFret::text() const noexcept
{
    return m_text;
}

void FirstFret::setText(std::optional<std::string> value)
{
    m_text = std::move(value);
}

const std::optional<LeftRight> &FirstFret::location() const noexcept
{
    return m_location;
}

void FirstFret::setLocation(std::optional<LeftRight> value)
{
    m_location = std::move(value);
}

const int &FirstFret::value() const noexcept
{
    return m_value;
}

void FirstFret::setValue(int value)
{
    m_value = std::move(value);
}

FirstFret parseFirstFret(pugi::xml_node el, const ParseContext &context)
{
    FirstFret out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "text")
        {
            out.setText(std::string{a.value()});
        }
        else if (aname == "location")
        {
            out.setLocation(parseValue<LeftRight>(a.value(), context, el, "location"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseFirstFretContent(out, el, context);
    return out;
}

void parseFirstFretContent(FirstFret &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseIntegerValue(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeFirstFret(const FirstFret &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.text())
    {
        setAttribute(el, "text", *v.text());
    }
    if (v.location())
    {
        setAttribute(el, "location", v.location()->toString());
    }
    serializeFirstFretContent(v, el);
}

void serializeFirstFretContent(const FirstFret &v, pugi::xml_node el)
{
    setText(el, formatInt(v.value()));
}

} // namespace mx::core
