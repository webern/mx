// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The harmony-type type differentiates different types of harmonies when alternate harmonies are
/// possible. Explicit harmonies have all note present in the music; implied have some notes missing
/// but implied; alternate represents alternate analyses.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class HarmonyType final
{
  public:
    enum class Tag
    {
        explicit_,
        implied,
        alternate,
    };

    /// The natural zero: the first variant.
    HarmonyType() noexcept = default;

    static HarmonyType explicit_() noexcept;
    static HarmonyType implied() noexcept;
    static HarmonyType alternate() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, HarmonyType &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static HarmonyType parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static HarmonyType parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const HarmonyType &other) const noexcept = default;

  private:
    explicit HarmonyType(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::explicit_};
};

} // namespace mx::core
