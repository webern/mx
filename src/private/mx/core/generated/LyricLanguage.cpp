// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/LyricLanguage.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<NameToken> &LyricLanguage::number() const noexcept
{
    return m_number;
}

void LyricLanguage::setNumber(std::optional<NameToken> value)
{
    m_number = std::move(value);
}

const std::optional<std::string> &LyricLanguage::name() const noexcept
{
    return m_name;
}

void LyricLanguage::setName(std::optional<std::string> value)
{
    m_name = std::move(value);
}

const std::string &LyricLanguage::xmlLang() const noexcept
{
    return m_xmlLang;
}

void LyricLanguage::setXMLLang(std::string value)
{
    m_xmlLang = std::move(value);
}

LyricLanguage parseLyricLanguage(pugi::xml_node el)
{
    return parseLyricLanguage(el, ParseContext{});
}

LyricLanguage parseLyricLanguage(pugi::xml_node el, const ParseContext &context)
{
    LyricLanguage out;
    bool seen_xmlLang = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(NameToken::parse(a.value()));
        }
        else if (aname == "name")
        {
            out.setName(std::string{a.value()});
        }
        else if (aname == "xml:lang")
        {
            seen_xmlLang = true;
            out.setXMLLang(std::string{a.value()});
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_xmlLang)
    {
        // Configured import repair (plan §2.4): inject the default.
        out.setXMLLang(std::string("und"));
        reportAttributeDefaulted(context, el, "xml:lang", "und");
    }
    parseLyricLanguageContent(out, el, context);
    return out;
}

void parseLyricLanguageContent(LyricLanguage &out, pugi::xml_node el)
{
    parseLyricLanguageContent(out, el, ParseContext{});
}

void parseLyricLanguageContent(LyricLanguage &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeLyricLanguage(const LyricLanguage &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.name())
    {
        setAttribute(el, "name", *v.name());
    }
    setAttribute(el, "xml:lang", v.xmlLang());
    serializeLyricLanguageContent(v, el);
}

void serializeLyricLanguageContent(const LyricLanguage &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
