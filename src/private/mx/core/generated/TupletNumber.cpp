// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TupletNumber.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<FontFamily> &TupletNumber::fontFamily() const noexcept
{
    return m_fontFamily;
}

void TupletNumber::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &TupletNumber::fontStyle() const noexcept
{
    return m_fontStyle;
}

void TupletNumber::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &TupletNumber::fontSize() const noexcept
{
    return m_fontSize;
}

void TupletNumber::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &TupletNumber::fontWeight() const noexcept
{
    return m_fontWeight;
}

void TupletNumber::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &TupletNumber::color() const noexcept
{
    return m_color;
}

void TupletNumber::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const int &TupletNumber::value() const noexcept
{
    return m_value;
}

void TupletNumber::setValue(int value)
{
    m_value = std::move(value);
}

TupletNumber parseTupletNumber(pugi::xml_node el, const ParseContext &context)
{
    TupletNumber out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseTupletNumberContent(out, el, context);
    return out;
}

void parseTupletNumberContent(TupletNumber &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseIntegerValue(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeTupletNumber(const TupletNumber &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeTupletNumberContent(v, el);
}

void serializeTupletNumberContent(const TupletNumber &v, pugi::xml_node el)
{
    setText(el, formatInt(v.value()));
}

} // namespace mx::core
