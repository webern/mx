// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/BassStep.h"
#include "mx/core/generated/HarmonyAlter.h"
#include "mx/core/generated/HarmonyArrangement.h"
#include "mx/core/generated/StyleText.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The bass type is used to indicate a bass note in popular music chord symbols, e.g. G/C. It is
/// generally not used in functional harmony, as inversion is generally not used in pop chord
/// symbols. As with root, it is divided into step and alter elements, similar to pitches. The
/// arrangement attribute specifies where the bass is displayed relative to what precedes it.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Bass final
{
  public:
    const std::optional<HarmonyArrangement> &arrangement() const noexcept;
    void setArrangement(std::optional<HarmonyArrangement> value);
    const std::optional<StyleText> &bassSeparator() const noexcept;
    void setBassSeparator(std::optional<StyleText> value);
    const BassStep &bassStep() const noexcept;
    void setBassStep(BassStep value);
    const std::optional<HarmonyAlter> &bassAlter() const noexcept;
    void setBassAlter(std::optional<HarmonyAlter> value);
    bool operator==(const Bass &other) const = default;

  private:
    std::optional<HarmonyArrangement> m_arrangement;
    std::optional<StyleText> m_bassSeparator;
    BassStep m_bassStep{};
    std::optional<HarmonyAlter> m_bassAlter;
};

Bass parseBass(pugi::xml_node el, const ParseContext &context);

void parseBassContent(Bass &out, pugi::xml_node el, const ParseContext &context);

void serializeBass(const Bass &v, pugi::xml_node parent, const char *tag);

void serializeBassContent(const Bass &v, pugi::xml_node el);

} // namespace mx::core
