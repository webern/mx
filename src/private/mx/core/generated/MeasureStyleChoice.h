// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/BeatRepeat.h"
#include "mx/core/generated/MeasureRepeat.h"
#include "mx/core/generated/MultipleRest.h"
#include "mx/core/generated/Slash.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class MeasureStyleChoice final
{
  public:
    enum class Kind
    {
        multipleRest,
        measureRepeat,
        beatRepeat,
        slash,
    };

    /// The natural zero: the first alternative.
    MeasureStyleChoice();

    static MeasureStyleChoice multipleRest(MultipleRest value);
    static MeasureStyleChoice measureRepeat(MeasureRepeat value);
    static MeasureStyleChoice beatRepeat(BeatRepeat value);
    static MeasureStyleChoice slash(Slash value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isMultipleRest() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isMultipleRest().
    const MultipleRest &asMultipleRest() const noexcept
    {
        assert(isMultipleRest());
        return *std::get_if<0>(&m_value);
    }

    bool isMeasureRepeat() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isMeasureRepeat().
    const MeasureRepeat &asMeasureRepeat() const noexcept
    {
        assert(isMeasureRepeat());
        return *std::get_if<1>(&m_value);
    }

    bool isBeatRepeat() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isBeatRepeat().
    const BeatRepeat &asBeatRepeat() const noexcept
    {
        assert(isBeatRepeat());
        return *std::get_if<2>(&m_value);
    }

    bool isSlash() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isSlash().
    const Slash &asSlash() const noexcept
    {
        assert(isSlash());
        return *std::get_if<3>(&m_value);
    }

    bool operator==(const MeasureStyleChoice &other) const = default;

  private:
    using Storage = std::variant<MultipleRest, MeasureRepeat, BeatRepeat, Slash>;

    explicit MeasureStyleChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
MeasureStyleChoice parseMeasureStyleChoice(pugi::xml_node el, pugi::xml_node &cursor);

MeasureStyleChoice parseMeasureStyleChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeMeasureStyleChoice(const MeasureStyleChoice &v, pugi::xml_node el);

} // namespace mx::core
