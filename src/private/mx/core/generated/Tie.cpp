// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Tie.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &Tie::type() const noexcept
{
    return m_type;
}

void Tie::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<TimeOnly> &Tie::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Tie::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

Tie parseTie(pugi::xml_node el, const ParseContext &context)
{
    Tie out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStop>(a.value(), context, el, "type"));
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(parseValue<TimeOnly>(a.value(), context, el, "time-only"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseTieContent(out, el, context);
    return out;
}

void parseTieContent(Tie &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeTie(const Tie &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    serializeTieContent(v, el);
}

void serializeTieContent(const Tie &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
