// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/PartGroup.h"
#include "mx/core/generated/PartListChoice.h"
#include "mx/core/generated/ScorePart.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The part-list identifies the different musical parts in this document. Each part has an ID that
/// is used later within the musical data. Since parts may be encoded separately and combined later,
/// identification elements are present at both the score and score-part levels. There must be at
/// least one score-part, combined as desired with part-group elements that indicate braces and
/// brackets. Parts are ordered from top to bottom in a score based on the order in which they appear
/// in the part-list.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class PartList final
{
  public:
    std::span<const PartGroup> partGroup() const noexcept;
    void addPartGroup(PartGroup value);
    void setPartGroup(std::vector<PartGroup> value);
    const ScorePart &scorePart() const noexcept;
    void setScorePart(ScorePart value);
    std::span<const PartListChoice> choice() const noexcept;
    void addChoice(PartListChoice value);
    void setChoice(std::vector<PartListChoice> value);
    bool operator==(const PartList &other) const = default;

  private:
    std::vector<PartGroup> m_partGroup;
    ScorePart m_scorePart{};
    std::vector<PartListChoice> m_choice;
};

PartList parsePartList(pugi::xml_node el, const ParseContext &context);

void parsePartListContent(PartList &out, pugi::xml_node el, const ParseContext &context);

void serializePartList(const PartList &v, pugi::xml_node parent, const char *tag);

void serializePartListContent(const PartList &v, pugi::xml_node el);

} // namespace mx::core
