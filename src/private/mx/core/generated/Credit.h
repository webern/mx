// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/NameToken.h"
#include "mx/core/Token.h"
#include "mx/core/generated/Bookmark.h"
#include "mx/core/generated/CreditChoice.h"
#include "mx/core/generated/Link.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The credit type represents the appearance of the title, composer, arranger, lyricist, copyright,
/// dedication, and other text, symbols, and graphics that commonly appear on the first page of a
/// score. The credit-words, credit-symbol, and credit-image elements are similar to the words,
/// symbol, and image elements for directions. However, since the credit is not part of a measure,
/// the default-x and default-y attributes adjust the origin relative to the bottom left-hand corner
/// of the page. The enclosure for credit-words and credit-symbol is none by default. By default, a
/// series of credit-words and credit-symbol elements within a single credit element follow one
/// another in sequence visually. Non-positional formatting attributes are carried over from the
/// previous element by default. The page attribute for the credit element specifies the page number
/// where the credit should appear. This is an integer value that starts with 1 for the first page.
/// Its value is 1 by default. Since credits occur before the music, these page numbers do not refer
/// to the page numbering specified by the print element's page-number attribute. The credit-type
/// element indicates the purpose behind a credit. Multiple types of data may be combined in a single
/// credit, so multiple elements may be used. Standard values include page number, title, subtitle,
/// composer, arranger, lyricist, rights, and part name.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Credit final
{
  public:
    const std::optional<int> &page() const noexcept;
    void setPage(std::optional<int> value);
    const std::optional<Token> &id() const noexcept;
    void setID(std::optional<Token> value);
    std::span<const std::string> creditType() const noexcept;
    void addCreditType(std::string value);
    void setCreditType(std::vector<std::string> value);
    std::span<const Link> link() const noexcept;
    void addLink(Link value);
    void setLink(std::vector<Link> value);
    std::span<const Bookmark> bookmark() const noexcept;
    void addBookmark(Bookmark value);
    void setBookmark(std::vector<Bookmark> value);
    const CreditChoice &choice() const noexcept;
    void setChoice(CreditChoice value);
    bool operator==(const Credit &other) const = default;

  private:
    std::optional<int> m_page;
    std::optional<Token> m_id;
    std::vector<std::string> m_creditType;
    std::vector<Link> m_link;
    std::vector<Bookmark> m_bookmark;
    CreditChoice m_choice{};
};

Credit parseCredit(pugi::xml_node el, const ParseContext &context);

void parseCreditContent(Credit &out, pugi::xml_node el, const ParseContext &context);

void serializeCredit(const Credit &v, pugi::xml_node parent, const char *tag);

void serializeCreditContent(const Credit &v, pugi::xml_node el);

} // namespace mx::core
