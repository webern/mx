// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The harmony-arrangement type indicates how stacked chords and bass notes are displayed within a
/// harmony element. The vertical value specifies that the second element appears below the first.
/// The horizontal value specifies that the second element appears to the right of the first. The
/// diagonal value specifies that the second element appears both below and to the right of the
/// first.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class HarmonyArrangement final
{
  public:
    enum class Tag
    {
        vertical,
        horizontal,
        diagonal,
    };

    /// The natural zero: the first variant.
    HarmonyArrangement() noexcept = default;

    static HarmonyArrangement vertical() noexcept;
    static HarmonyArrangement horizontal() noexcept;
    static HarmonyArrangement diagonal() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, HarmonyArrangement &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static HarmonyArrangement parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static HarmonyArrangement parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const HarmonyArrangement &other) const noexcept = default;

  private:
    explicit HarmonyArrangement(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::vertical};
};

} // namespace mx::core
