// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The time-relation type indicates the symbol used to represent the interchangeable aspect of dual
/// time signatures.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TimeRelation final
{
  public:
    enum class Tag
    {
        parentheses,
        bracket,
        equals,
        slash,
        space,
        hyphen,
    };

    /// The natural zero: the first variant.
    TimeRelation() noexcept = default;

    static TimeRelation parentheses() noexcept;
    static TimeRelation bracket() noexcept;
    static TimeRelation equals() noexcept;
    static TimeRelation slash() noexcept;
    static TimeRelation space() noexcept;
    static TimeRelation hyphen() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TimeRelation &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TimeRelation parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TimeRelation parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TimeRelation &other) const noexcept = default;

  private:
    explicit TimeRelation(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::parentheses};
};

} // namespace mx::core
