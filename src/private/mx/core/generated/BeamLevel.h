// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The MusicXML format supports six levels of beaming, up to 1024th notes. Unlike the number-level
/// type, the beam-level type identifies concurrent beams in a beam group. It does not distinguish
/// overlapping beams such as grace notes within regular notes, or beams used in different voices.
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class BeamLevel final
{
  public:
    /// The natural zero: the default value clamped into range.
    BeamLevel();

    explicit BeamLevel(int value);

    int value() const noexcept
    {
        return m_value;
    }

    void setValue(int value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, BeamLevel &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static BeamLevel parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static BeamLevel parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const BeamLevel &other) const noexcept = default;

  private:
    int m_value{};
};

} // namespace mx::core
