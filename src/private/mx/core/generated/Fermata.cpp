// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Fermata.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<UprightInverted> &Fermata::type() const noexcept
{
    return m_type;
}

void Fermata::setType(std::optional<UprightInverted> value)
{
    m_type = std::move(value);
}

const std::optional<Tenths> &Fermata::defaultX() const noexcept
{
    return m_defaultX;
}

void Fermata::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Fermata::defaultY() const noexcept
{
    return m_defaultY;
}

void Fermata::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Fermata::relativeX() const noexcept
{
    return m_relativeX;
}

void Fermata::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Fermata::relativeY() const noexcept
{
    return m_relativeY;
}

void Fermata::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Fermata::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Fermata::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Fermata::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Fermata::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Fermata::fontSize() const noexcept
{
    return m_fontSize;
}

void Fermata::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Fermata::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Fermata::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Fermata::color() const noexcept
{
    return m_color;
}

void Fermata::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &Fermata::id() const noexcept
{
    return m_id;
}

void Fermata::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const FermataShape &Fermata::value() const noexcept
{
    return m_value;
}

void Fermata::setValue(FermataShape value)
{
    m_value = std::move(value);
}

Fermata parseFermata(pugi::xml_node el)
{
    return parseFermata(el, ParseContext{});
}

Fermata parseFermata(pugi::xml_node el, const ParseContext &context)
{
    Fermata out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            out.setType(parseValue<UprightInverted>(a.value(), context, el, "type"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseFermataContent(out, el, context);
    return out;
}

void parseFermataContent(Fermata &out, pugi::xml_node el)
{
    parseFermataContent(out, el, ParseContext{});
}

void parseFermataContent(Fermata &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<FermataShape>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeFermata(const Fermata &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.type())
    {
        setAttribute(el, "type", v.type()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeFermataContent(v, el);
}

void serializeFermataContent(const Fermata &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
