// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Distance.h"
#include "mx/core/generated/Glyph.h"
#include "mx/core/generated/LineWidth.h"
#include "mx/core/generated/NoteSize.h"
#include "mx/core/generated/OtherAppearance.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The appearance type controls general graphical settings for the music's final form appearance on
/// a printed page of display. This includes support for line widths, definitions for note sizes, and
/// standard distances between notation elements, plus an extension element for other aspects of
/// appearance.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Appearance final
{
  public:
    std::span<const LineWidth> lineWidth() const noexcept;
    void addLineWidth(LineWidth value);
    void setLineWidth(std::vector<LineWidth> value);
    std::span<const NoteSize> noteSize() const noexcept;
    void addNoteSize(NoteSize value);
    void setNoteSize(std::vector<NoteSize> value);
    std::span<const Distance> distance() const noexcept;
    void addDistance(Distance value);
    void setDistance(std::vector<Distance> value);
    std::span<const Glyph> glyph() const noexcept;
    void addGlyph(Glyph value);
    void setGlyph(std::vector<Glyph> value);
    std::span<const OtherAppearance> otherAppearance() const noexcept;
    void addOtherAppearance(OtherAppearance value);
    void setOtherAppearance(std::vector<OtherAppearance> value);
    bool operator==(const Appearance &other) const = default;

  private:
    std::vector<LineWidth> m_lineWidth;
    std::vector<NoteSize> m_noteSize;
    std::vector<Distance> m_distance;
    std::vector<Glyph> m_glyph;
    std::vector<OtherAppearance> m_otherAppearance;
};

Appearance parseAppearance(pugi::xml_node el);

Appearance parseAppearance(pugi::xml_node el, const ParseContext &context);

void parseAppearanceContent(Appearance &out, pugi::xml_node el);

void parseAppearanceContent(Appearance &out, pugi::xml_node el, const ParseContext &context);

void serializeAppearance(const Appearance &v, pugi::xml_node parent, const char *tag);

void serializeAppearanceContent(const Appearance &v, pugi::xml_node el);

} // namespace mx::core
