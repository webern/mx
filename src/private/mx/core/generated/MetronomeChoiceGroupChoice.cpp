// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MetronomeChoiceGroupChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

MetronomeChoiceGroupChoice::MetronomeChoiceGroupChoice()
{
    *this = MetronomeChoiceGroupChoice::perMinute(PerMinute{});
}

MetronomeChoiceGroupChoice MetronomeChoiceGroupChoice::perMinute(PerMinute value)
{
    return MetronomeChoiceGroupChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

MetronomeChoiceGroupChoice MetronomeChoiceGroupChoice::group(MetronomeChoiceGroupChoiceGroup value)
{
    return MetronomeChoiceGroupChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

MetronomeChoiceGroupChoice parseMetronomeChoiceGroupChoice(pugi::xml_node el, pugi::xml_node &cursor,
                                                           const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "per-minute")))
    {
        PerMinute value = parsePerMinute(cursor, context);
        cursor = nextElement(cursor);
        return MetronomeChoiceGroupChoice::perMinute(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "beat-unit")))
    {
        return MetronomeChoiceGroupChoice::group(parseMetronomeChoiceGroupChoiceGroup(el, cursor, context));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "per-minute");
}

void serializeMetronomeChoiceGroupChoice(const MetronomeChoiceGroupChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case MetronomeChoiceGroupChoice::Kind::perMinute:
        serializePerMinute(v.asPerMinute(), el, "per-minute");
        break;
    case MetronomeChoiceGroupChoice::Kind::group:
        serializeMetronomeChoiceGroupChoiceGroup(v.asGroup(), el);
        break;
    }
}

} // namespace mx::core
