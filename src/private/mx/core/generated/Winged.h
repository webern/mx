// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The winged attribute indicates whether the repeat has winged extensions that appear above and
/// below the barline. The straight and curved values represent single wings, while the
/// double-straight and double-curved values represent double wings. The none value indicates no
/// wings and is the default.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class Winged final
{
  public:
    enum class Tag
    {
        none,
        straight,
        curved,
        doubleStraight,
        doubleCurved,
    };

    /// The natural zero: the first variant.
    Winged() noexcept = default;

    static Winged none() noexcept;
    static Winged straight() noexcept;
    static Winged curved() noexcept;
    static Winged doubleStraight() noexcept;
    static Winged doubleCurved() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, Winged &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static Winged parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static Winged parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Winged &other) const noexcept = default;

  private:
    explicit Winged(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::none};
};

} // namespace mx::core
