// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Transpose.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &Transpose::number() const noexcept
{
    return m_number;
}

void Transpose::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<Token> &Transpose::id() const noexcept
{
    return m_id;
}

void Transpose::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const TransposeGroup &Transpose::transpose() const noexcept
{
    return m_transpose;
}

void Transpose::setTranspose(TransposeGroup value)
{
    m_transpose = std::move(value);
}

Transpose parseTranspose(pugi::xml_node el)
{
    return parseTranspose(el, ParseContext{});
}

Transpose parseTranspose(pugi::xml_node el, const ParseContext &context)
{
    Transpose out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseTransposeContent(out, el, context);
    return out;
}

void parseTransposeContent(Transpose &out, pugi::xml_node el)
{
    parseTransposeContent(out, el, ParseContext{});
}

void parseTransposeContent(Transpose &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "diatonic") || cursorIs(cursor, "chromatic")))
    {
        out.setTranspose(parseTransposeGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "diatonic");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "diatonic") || cursorIs(cursor, "chromatic") || cursorIs(cursor, "octave-change") ||
            cursorIs(cursor, "double"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTranspose(const Transpose &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeTransposeContent(v, el);
}

void serializeTransposeContent(const Transpose &v, pugi::xml_node el)
{
    serializeTransposeGroup(v.transpose(), el);
}

} // namespace mx::core
