// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Scordatura.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &Scordatura::id() const noexcept
{
    return m_id;
}

void Scordatura::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const Accord> Scordatura::accord() const noexcept
{
    return m_accord.items();
}

void Scordatura::addAccord(Accord value)
{
    m_accord.add(std::move(value));
}

void Scordatura::setAccord(OneOrMore<Accord> value)
{
    m_accord = std::move(value);
}

Scordatura parseScordatura(pugi::xml_node el, const ParseContext &context)
{
    Scordatura out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseScordaturaContent(out, el, context);
    return out;
}

void parseScordaturaContent(Scordatura &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!cursorIs(cursor, "accord"))
    {
        throwMissingOrMisplaced(el, cursor, "accord");
    }
    out.setAccord(OneOrMore<Accord>{parseAccord(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "accord"))
    {
        out.addAccord(parseAccord(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "accord"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeScordatura(const Scordatura &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeScordaturaContent(v, el);
}

void serializeScordaturaContent(const Scordatura &v, pugi::xml_node el)
{
    for (const auto &item : v.accord())
    {
        serializeAccord(item, el, "accord");
    }
}

} // namespace mx::core
