// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The valign type is used to indicate vertical alignment to the top, middle, bottom, or baseline of
/// the text. If the text is on multiple lines, baseline alignment refers to the baseline of the
/// lowest line of text. Defaults are implementation-dependent.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class Valign final
{
  public:
    enum class Tag
    {
        top,
        middle,
        bottom,
        baseline,
    };

    /// The natural zero: the first variant.
    Valign() noexcept = default;

    static Valign top() noexcept;
    static Valign middle() noexcept;
    static Valign bottom() noexcept;
    static Valign baseline() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, Valign &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static Valign parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static Valign parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Valign &other) const noexcept = default;

  private:
    explicit Valign(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::top};
};

} // namespace mx::core
