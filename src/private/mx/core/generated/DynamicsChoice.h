// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"
#include "mx/core/generated/OtherText.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class DynamicsChoice final
{
  public:
    enum class Kind
    {
        p,
        pp,
        ppp,
        pppp,
        ppppp,
        pppppp,
        f,
        ff,
        fff,
        ffff,
        fffff,
        ffffff,
        mp,
        mf,
        sf,
        sfp,
        sfpp,
        fp,
        rf,
        rfz,
        sfz,
        sffz,
        fz,
        n,
        pf,
        sfzp,
        otherDynamics,
    };

    /// The natural zero: the first alternative.
    DynamicsChoice();

    static DynamicsChoice p(Empty value);
    static DynamicsChoice pp(Empty value);
    static DynamicsChoice ppp(Empty value);
    static DynamicsChoice pppp(Empty value);
    static DynamicsChoice ppppp(Empty value);
    static DynamicsChoice pppppp(Empty value);
    static DynamicsChoice f(Empty value);
    static DynamicsChoice ff(Empty value);
    static DynamicsChoice fff(Empty value);
    static DynamicsChoice ffff(Empty value);
    static DynamicsChoice fffff(Empty value);
    static DynamicsChoice ffffff(Empty value);
    static DynamicsChoice mp(Empty value);
    static DynamicsChoice mf(Empty value);
    static DynamicsChoice sf(Empty value);
    static DynamicsChoice sfp(Empty value);
    static DynamicsChoice sfpp(Empty value);
    static DynamicsChoice fp(Empty value);
    static DynamicsChoice rf(Empty value);
    static DynamicsChoice rfz(Empty value);
    static DynamicsChoice sfz(Empty value);
    static DynamicsChoice sffz(Empty value);
    static DynamicsChoice fz(Empty value);
    static DynamicsChoice n(Empty value);
    static DynamicsChoice pf(Empty value);
    static DynamicsChoice sfzp(Empty value);
    static DynamicsChoice otherDynamics(OtherText value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isP() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isP().
    const Empty &asP() const noexcept
    {
        assert(isP());
        return *std::get_if<0>(&m_value);
    }

    bool isPp() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isPp().
    const Empty &asPp() const noexcept
    {
        assert(isPp());
        return *std::get_if<1>(&m_value);
    }

    bool isPpp() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isPpp().
    const Empty &asPpp() const noexcept
    {
        assert(isPpp());
        return *std::get_if<2>(&m_value);
    }

    bool isPppp() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isPppp().
    const Empty &asPppp() const noexcept
    {
        assert(isPppp());
        return *std::get_if<3>(&m_value);
    }

    bool isPpppp() const noexcept
    {
        return m_value.index() == 4;
    }

    /// Precondition: isPpppp().
    const Empty &asPpppp() const noexcept
    {
        assert(isPpppp());
        return *std::get_if<4>(&m_value);
    }

    bool isPppppp() const noexcept
    {
        return m_value.index() == 5;
    }

    /// Precondition: isPppppp().
    const Empty &asPppppp() const noexcept
    {
        assert(isPppppp());
        return *std::get_if<5>(&m_value);
    }

    bool isF() const noexcept
    {
        return m_value.index() == 6;
    }

    /// Precondition: isF().
    const Empty &asF() const noexcept
    {
        assert(isF());
        return *std::get_if<6>(&m_value);
    }

    bool isFf() const noexcept
    {
        return m_value.index() == 7;
    }

    /// Precondition: isFf().
    const Empty &asFf() const noexcept
    {
        assert(isFf());
        return *std::get_if<7>(&m_value);
    }

    bool isFff() const noexcept
    {
        return m_value.index() == 8;
    }

    /// Precondition: isFff().
    const Empty &asFff() const noexcept
    {
        assert(isFff());
        return *std::get_if<8>(&m_value);
    }

    bool isFfff() const noexcept
    {
        return m_value.index() == 9;
    }

    /// Precondition: isFfff().
    const Empty &asFfff() const noexcept
    {
        assert(isFfff());
        return *std::get_if<9>(&m_value);
    }

    bool isFffff() const noexcept
    {
        return m_value.index() == 10;
    }

    /// Precondition: isFffff().
    const Empty &asFffff() const noexcept
    {
        assert(isFffff());
        return *std::get_if<10>(&m_value);
    }

    bool isFfffff() const noexcept
    {
        return m_value.index() == 11;
    }

    /// Precondition: isFfffff().
    const Empty &asFfffff() const noexcept
    {
        assert(isFfffff());
        return *std::get_if<11>(&m_value);
    }

    bool isMp() const noexcept
    {
        return m_value.index() == 12;
    }

    /// Precondition: isMp().
    const Empty &asMp() const noexcept
    {
        assert(isMp());
        return *std::get_if<12>(&m_value);
    }

    bool isMf() const noexcept
    {
        return m_value.index() == 13;
    }

    /// Precondition: isMf().
    const Empty &asMf() const noexcept
    {
        assert(isMf());
        return *std::get_if<13>(&m_value);
    }

    bool isSf() const noexcept
    {
        return m_value.index() == 14;
    }

    /// Precondition: isSf().
    const Empty &asSf() const noexcept
    {
        assert(isSf());
        return *std::get_if<14>(&m_value);
    }

    bool isSfp() const noexcept
    {
        return m_value.index() == 15;
    }

    /// Precondition: isSfp().
    const Empty &asSfp() const noexcept
    {
        assert(isSfp());
        return *std::get_if<15>(&m_value);
    }

    bool isSfpp() const noexcept
    {
        return m_value.index() == 16;
    }

    /// Precondition: isSfpp().
    const Empty &asSfpp() const noexcept
    {
        assert(isSfpp());
        return *std::get_if<16>(&m_value);
    }

    bool isFp() const noexcept
    {
        return m_value.index() == 17;
    }

    /// Precondition: isFp().
    const Empty &asFp() const noexcept
    {
        assert(isFp());
        return *std::get_if<17>(&m_value);
    }

    bool isRf() const noexcept
    {
        return m_value.index() == 18;
    }

    /// Precondition: isRf().
    const Empty &asRf() const noexcept
    {
        assert(isRf());
        return *std::get_if<18>(&m_value);
    }

    bool isRfz() const noexcept
    {
        return m_value.index() == 19;
    }

    /// Precondition: isRfz().
    const Empty &asRfz() const noexcept
    {
        assert(isRfz());
        return *std::get_if<19>(&m_value);
    }

    bool isSfz() const noexcept
    {
        return m_value.index() == 20;
    }

    /// Precondition: isSfz().
    const Empty &asSfz() const noexcept
    {
        assert(isSfz());
        return *std::get_if<20>(&m_value);
    }

    bool isSffz() const noexcept
    {
        return m_value.index() == 21;
    }

    /// Precondition: isSffz().
    const Empty &asSffz() const noexcept
    {
        assert(isSffz());
        return *std::get_if<21>(&m_value);
    }

    bool isFz() const noexcept
    {
        return m_value.index() == 22;
    }

    /// Precondition: isFz().
    const Empty &asFz() const noexcept
    {
        assert(isFz());
        return *std::get_if<22>(&m_value);
    }

    bool isN() const noexcept
    {
        return m_value.index() == 23;
    }

    /// Precondition: isN().
    const Empty &asN() const noexcept
    {
        assert(isN());
        return *std::get_if<23>(&m_value);
    }

    bool isPf() const noexcept
    {
        return m_value.index() == 24;
    }

    /// Precondition: isPf().
    const Empty &asPf() const noexcept
    {
        assert(isPf());
        return *std::get_if<24>(&m_value);
    }

    bool isSfzp() const noexcept
    {
        return m_value.index() == 25;
    }

    /// Precondition: isSfzp().
    const Empty &asSfzp() const noexcept
    {
        assert(isSfzp());
        return *std::get_if<25>(&m_value);
    }

    bool isOtherDynamics() const noexcept
    {
        return m_value.index() == 26;
    }

    /// Precondition: isOtherDynamics().
    const OtherText &asOtherDynamics() const noexcept
    {
        assert(isOtherDynamics());
        return *std::get_if<26>(&m_value);
    }

    bool operator==(const DynamicsChoice &other) const = default;

  private:
    using Storage =
        std::variant<Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty,
                     Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, Empty, OtherText>;

    explicit DynamicsChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
DynamicsChoice parseDynamicsChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeDynamicsChoice(const DynamicsChoice &v, pugi::xml_node el);

} // namespace mx::core
