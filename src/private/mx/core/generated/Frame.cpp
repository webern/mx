// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Frame.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Tenths> &Frame::height() const noexcept
{
    return m_height;
}

void Frame::setHeight(std::optional<Tenths> value)
{
    m_height = std::move(value);
}

const std::optional<Tenths> &Frame::width() const noexcept
{
    return m_width;
}

void Frame::setWidth(std::optional<Tenths> value)
{
    m_width = std::move(value);
}

const std::optional<std::string> &Frame::unplayed() const noexcept
{
    return m_unplayed;
}

void Frame::setUnplayed(std::optional<std::string> value)
{
    m_unplayed = std::move(value);
}

const std::optional<Tenths> &Frame::defaultX() const noexcept
{
    return m_defaultX;
}

void Frame::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Frame::defaultY() const noexcept
{
    return m_defaultY;
}

void Frame::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Frame::relativeX() const noexcept
{
    return m_relativeX;
}

void Frame::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Frame::relativeY() const noexcept
{
    return m_relativeY;
}

void Frame::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<Color> &Frame::color() const noexcept
{
    return m_color;
}

void Frame::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Frame::halign() const noexcept
{
    return m_halign;
}

void Frame::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<ValignImage> &Frame::valign() const noexcept
{
    return m_valign;
}

void Frame::setValign(std::optional<ValignImage> value)
{
    m_valign = std::move(value);
}

const std::optional<Token> &Frame::id() const noexcept
{
    return m_id;
}

void Frame::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const int &Frame::frameStrings() const noexcept
{
    return m_frameStrings;
}

void Frame::setFrameStrings(int value)
{
    m_frameStrings = std::move(value);
}

const int &Frame::frameFrets() const noexcept
{
    return m_frameFrets;
}

void Frame::setFrameFrets(int value)
{
    m_frameFrets = std::move(value);
}

const std::optional<FirstFret> &Frame::firstFret() const noexcept
{
    return m_firstFret;
}

void Frame::setFirstFret(std::optional<FirstFret> value)
{
    m_firstFret = std::move(value);
}

std::span<const FrameNote> Frame::frameNote() const noexcept
{
    return m_frameNote.items();
}

void Frame::addFrameNote(FrameNote value)
{
    m_frameNote.add(std::move(value));
}

void Frame::setFrameNote(OneOrMore<FrameNote> value)
{
    m_frameNote = std::move(value);
}

Frame parseFrame(pugi::xml_node el, const ParseContext &context)
{
    Frame out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "height")
        {
            out.setHeight(parseValue<Tenths>(a.value(), context, el, "height"));
        }
        else if (aname == "width")
        {
            out.setWidth(parseValue<Tenths>(a.value(), context, el, "width"));
        }
        else if (aname == "unplayed")
        {
            out.setUnplayed(std::string{a.value()});
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "halign")
        {
            out.setHalign(parseValue<LeftCenterRight>(a.value(), context, el, "halign"));
        }
        else if (aname == "valign")
        {
            out.setValign(parseValue<ValignImage>(a.value(), context, el, "valign"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseFrameContent(out, el, context);
    return out;
}

void parseFrameContent(Frame &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "frame-strings"))
    {
        out.setFrameStrings(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "frame-strings");
    }
    if (cursorIs(cursor, "frame-frets"))
    {
        out.setFrameFrets(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "frame-frets");
    }
    if (cursorIs(cursor, "first-fret"))
    {
        out.setFirstFret(parseFirstFret(cursor, context));
        cursor = nextElement(cursor);
    }
    if (!cursorIs(cursor, "frame-note"))
    {
        throwMissingOrMisplaced(el, cursor, "frame-note");
    }
    out.setFrameNote(OneOrMore<FrameNote>{parseFrameNote(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "frame-note"))
    {
        out.addFrameNote(parseFrameNote(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "frame-strings") || cursorIs(cursor, "frame-frets") || cursorIs(cursor, "first-fret") ||
            cursorIs(cursor, "frame-note"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeFrame(const Frame &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.height())
    {
        setAttribute(el, "height", v.height()->toString());
    }
    if (v.width())
    {
        setAttribute(el, "width", v.width()->toString());
    }
    if (v.unplayed())
    {
        setAttribute(el, "unplayed", *v.unplayed());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeFrameContent(v, el);
}

void serializeFrameContent(const Frame &v, pugi::xml_node el)
{
    writeTextElement(el, "frame-strings", formatInt(v.frameStrings()));
    writeTextElement(el, "frame-frets", formatInt(v.frameFrets()));
    if (v.firstFret())
    {
        serializeFirstFret(*v.firstFret(), el, "first-fret");
    }
    for (const auto &item : v.frameNote())
    {
        serializeFrameNote(item, el, "frame-note");
    }
}

} // namespace mx::core
