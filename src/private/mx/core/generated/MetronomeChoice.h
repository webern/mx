// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/MetronomeChoiceGroup.h"
#include "mx/core/generated/MetronomeChoiceGroup2.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class MetronomeChoice final
{
  public:
    enum class Kind
    {
        group,
        group2,
    };

    /// The natural zero: the first alternative.
    MetronomeChoice();

    static MetronomeChoice group(MetronomeChoiceGroup value);
    static MetronomeChoice group2(MetronomeChoiceGroup2 value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isGroup() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isGroup().
    const MetronomeChoiceGroup &asGroup() const noexcept
    {
        assert(isGroup());
        return *std::get_if<0>(&m_value);
    }

    bool isGroup2() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isGroup2().
    const MetronomeChoiceGroup2 &asGroup2() const noexcept
    {
        assert(isGroup2());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const MetronomeChoice &other) const = default;

  private:
    using Storage = std::variant<MetronomeChoiceGroup, MetronomeChoiceGroup2>;

    explicit MetronomeChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
MetronomeChoice parseMetronomeChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeMetronomeChoice(const MetronomeChoice &v, pugi::xml_node el);

} // namespace mx::core
