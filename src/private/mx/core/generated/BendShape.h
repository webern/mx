// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The bend-shape type distinguishes between the angled bend symbols commonly used in standard
/// notation and the curved bend symbols commonly used in both tablature and standard notation.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class BendShape final
{
  public:
    enum class Tag
    {
        angled,
        curved,
    };

    /// The natural zero: the first variant.
    BendShape() noexcept = default;

    static BendShape angled() noexcept;
    static BendShape curved() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, BendShape &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static BendShape parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static BendShape parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const BendShape &other) const noexcept = default;

  private:
    explicit BendShape(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::angled};
};

} // namespace mx::core
