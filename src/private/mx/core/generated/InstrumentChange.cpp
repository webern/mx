// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/InstrumentChange.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &InstrumentChange::id() const noexcept
{
    return m_id;
}

void InstrumentChange::setID(Token value)
{
    m_id = std::move(value);
}

const VirtualInstrumentDataGroup &InstrumentChange::virtualInstrumentData() const noexcept
{
    return m_virtualInstrumentData;
}

void InstrumentChange::setVirtualInstrumentData(VirtualInstrumentDataGroup value)
{
    m_virtualInstrumentData = std::move(value);
}

InstrumentChange parseInstrumentChange(pugi::xml_node el)
{
    return parseInstrumentChange(el, ParseContext{});
}

InstrumentChange parseInstrumentChange(pugi::xml_node el, const ParseContext &context)
{
    InstrumentChange out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseInstrumentChangeContent(out, el, context);
    return out;
}

void parseInstrumentChangeContent(InstrumentChange &out, pugi::xml_node el)
{
    parseInstrumentChangeContent(out, el, ParseContext{});
}

void parseInstrumentChangeContent(InstrumentChange &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "instrument-sound") || cursorIs(cursor, "solo") || cursorIs(cursor, "ensemble") ||
                   cursorIs(cursor, "virtual-instrument")))
    {
        out.setVirtualInstrumentData(parseVirtualInstrumentDataGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "instrument-sound") || cursorIs(cursor, "solo") || cursorIs(cursor, "ensemble") ||
            cursorIs(cursor, "virtual-instrument"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeInstrumentChange(const InstrumentChange &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializeInstrumentChangeContent(v, el);
}

void serializeInstrumentChangeContent(const InstrumentChange &v, pugi::xml_node el)
{
    serializeVirtualInstrumentDataGroup(v.virtualInstrumentData(), el);
}

} // namespace mx::core
