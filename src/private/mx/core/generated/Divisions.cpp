// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Divisions.h"

#include "mx/core/Lexical.h"

#include <utility>

namespace mx::core
{

Decimal clampedDivisions(Decimal v)
{
    return v;
}

Divisions::Divisions() : m_value{clampedDivisions(Decimal{})}
{
}

Divisions::Divisions(Decimal value) : m_value{clampedDivisions(std::move(value))}
{
}

void Divisions::setValue(Decimal value)
{
    m_value = clampedDivisions(std::move(value));
}

std::string Divisions::toString() const
{
    return m_value.toString();
}

bool Divisions::tryParse(std::string_view text, Divisions &out)
{
    ValueParseOutcome outcome = ValueParseOutcome::valid;
    Divisions parsed = parse(text, outcome);
    if (outcome == ValueParseOutcome::invalid)
    {
        return false;
    }
    out = std::move(parsed);
    return true;
}

Divisions Divisions::parse(std::string_view text)
{
    ValueParseOutcome outcome = ValueParseOutcome::valid;
    return parse(text, outcome);
}

Divisions Divisions::parse(std::string_view text, ValueParseOutcome &outcome)
{
    Decimal v;
    if (!Decimal::tryParse(text, v))
    {
        outcome = ValueParseOutcome::invalid;
        return Divisions{};
    }
    Divisions out{v};
    outcome = out.value() == v ? ValueParseOutcome::valid : ValueParseOutcome::adjusted;
    return out;
}

} // namespace mx::core
