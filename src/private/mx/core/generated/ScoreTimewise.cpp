// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ScoreTimewise.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &ScoreTimewise::version() const noexcept
{
    return m_version;
}

void ScoreTimewise::setVersion(std::optional<std::string> value)
{
    m_version = std::move(value);
}

const ScoreHeaderGroup &ScoreTimewise::scoreHeader() const noexcept
{
    return m_scoreHeader;
}

void ScoreTimewise::setScoreHeader(ScoreHeaderGroup value)
{
    m_scoreHeader = std::move(value);
}

std::span<const TimewiseMeasure> ScoreTimewise::measure() const noexcept
{
    return m_measure.items();
}

void ScoreTimewise::addMeasure(TimewiseMeasure value)
{
    m_measure.add(std::move(value));
}

void ScoreTimewise::setMeasure(OneOrMore<TimewiseMeasure> value)
{
    m_measure = std::move(value);
}

ScoreTimewise parseScoreTimewise(pugi::xml_node el, const ParseContext &context)
{
    ScoreTimewise out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "version")
        {
            out.setVersion(std::string{a.value()});
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseScoreTimewiseContent(out, el, context);
    return out;
}

void parseScoreTimewiseContent(ScoreTimewise &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "work") || cursorIs(cursor, "movement-number") ||
                   cursorIs(cursor, "movement-title") || cursorIs(cursor, "identification") ||
                   cursorIs(cursor, "defaults") || cursorIs(cursor, "credit") || cursorIs(cursor, "part-list")))
    {
        out.setScoreHeader(parseScoreHeaderGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "work");
    }
    if (!cursorIs(cursor, "measure"))
    {
        throwMissingOrMisplaced(el, cursor, "measure");
    }
    out.setMeasure(OneOrMore<TimewiseMeasure>{parseTimewiseMeasure(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "measure"))
    {
        out.addMeasure(parseTimewiseMeasure(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "work") || cursorIs(cursor, "movement-number") || cursorIs(cursor, "movement-title") ||
            cursorIs(cursor, "identification") || cursorIs(cursor, "defaults") || cursorIs(cursor, "credit") ||
            cursorIs(cursor, "part-list") || cursorIs(cursor, "measure"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeScoreTimewise(const ScoreTimewise &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.version())
    {
        setAttribute(el, "version", *v.version());
    }
    serializeScoreTimewiseContent(v, el);
}

void serializeScoreTimewiseContent(const ScoreTimewise &v, pugi::xml_node el)
{
    serializeScoreHeaderGroup(v.scoreHeader(), el);
    for (const auto &item : v.measure())
    {
        serializeTimewiseMeasure(item, el, "measure");
    }
}

} // namespace mx::core
