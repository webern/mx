// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The arrow-direction type represents the direction in which an arrow points, using Unicode arrow
/// terminology.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ArrowDirection final
{
  public:
    enum class Tag
    {
        left,
        up,
        right,
        down,
        northwest,
        northeast,
        southeast,
        southwest,
        leftRight,
        upDown,
        northwestSoutheast,
        northeastSouthwest,
        other,
    };

    /// The natural zero: the first variant.
    ArrowDirection() noexcept = default;

    static ArrowDirection left() noexcept;
    static ArrowDirection up() noexcept;
    static ArrowDirection right() noexcept;
    static ArrowDirection down() noexcept;
    static ArrowDirection northwest() noexcept;
    static ArrowDirection northeast() noexcept;
    static ArrowDirection southeast() noexcept;
    static ArrowDirection southwest() noexcept;
    static ArrowDirection leftRight() noexcept;
    static ArrowDirection upDown() noexcept;
    static ArrowDirection northwestSoutheast() noexcept;
    static ArrowDirection northeastSouthwest() noexcept;
    static ArrowDirection other() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ArrowDirection &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ArrowDirection parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ArrowDirection parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ArrowDirection &other) const noexcept = default;

  private:
    explicit ArrowDirection(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::left};
};

} // namespace mx::core
