// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/TupletDot.h"
#include "mx/core/generated/TupletNumber.h"
#include "mx/core/generated/TupletType.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The tuplet-portion type provides optional full control over tuplet specifications. It allows the
/// number and note type (including dots) to be set for the actual and normal portions of a single
/// tuplet. If any of these elements are absent, their values are based on the time-modification
/// element.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class TupletPortion final
{
  public:
    const std::optional<TupletNumber> &tupletNumber() const noexcept;
    void setTupletNumber(std::optional<TupletNumber> value);
    const std::optional<TupletType> &tupletType() const noexcept;
    void setTupletType(std::optional<TupletType> value);
    std::span<const TupletDot> tupletDot() const noexcept;
    void addTupletDot(TupletDot value);
    void setTupletDot(std::vector<TupletDot> value);
    bool operator==(const TupletPortion &other) const = default;

  private:
    std::optional<TupletNumber> m_tupletNumber;
    std::optional<TupletType> m_tupletType;
    std::vector<TupletDot> m_tupletDot;
};

TupletPortion parseTupletPortion(pugi::xml_node el, const ParseContext &context);

void parseTupletPortionContent(TupletPortion &out, pugi::xml_node el, const ParseContext &context);

void serializeTupletPortion(const TupletPortion &v, pugi::xml_node parent, const char *tag);

void serializeTupletPortionContent(const TupletPortion &v, pugi::xml_node el);

} // namespace mx::core
