// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Result.h"
#include "mx/core/generated/PageLayoutGroup.h"
#include "mx/core/generated/PageMargins.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// Page layout can be defined both in score-wide defaults and in the print element. Page margins are
/// specified either for both even and odd pages, or via separate odd and even page number values.
/// The type is not needed when used as part of a print element. If omitted when used in the defaults
/// element, "both" is the default. If no page-layout element is present in the defaults element,
/// default page layout values are chosen by the application. When used in the print element, the
/// page-layout element affects the appearance of the current page only. All other pages use the
/// default values as determined by the defaults element. If any child elements are missing from the
/// page-layout element in a print element, the values determined by the defaults element are used
/// there as well.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class PageLayout final
{
  public:
    const std::optional<PageLayoutGroup> &group() const noexcept;
    void setGroup(std::optional<PageLayoutGroup> value);
    std::span<const PageMargins> pageMargins() const noexcept;
    /// Bounded by the schema: at most 2 occurrences. An over-cap
    /// append is rejected (plan §1.2).
    [[nodiscard]] Result<void> addPageMargins(PageMargins value);
    void clearPageMargins() noexcept;
    bool operator==(const PageLayout &other) const = default;

  private:
    std::optional<PageLayoutGroup> m_group;
    std::vector<PageMargins> m_pageMargins;
};

PageLayout parsePageLayout(pugi::xml_node el, const ParseContext &context);

void parsePageLayoutContent(PageLayout &out, pugi::xml_node el, const ParseContext &context);

void serializePageLayout(const PageLayout &v, pugi::xml_node parent, const char *tag);

void serializePageLayoutContent(const PageLayout &v, pugi::xml_node el);

} // namespace mx::core
