// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The number-of-lines type is used to specify the number of lines in text decoration attributes.
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class NumberOfLines final
{
  public:
    /// The natural zero: the default value clamped into range.
    NumberOfLines();

    explicit NumberOfLines(int value);

    int value() const noexcept
    {
        return m_value;
    }

    void setValue(int value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, NumberOfLines &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static NumberOfLines parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static NumberOfLines parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const NumberOfLines &other) const noexcept = default;

  private:
    int m_value{};
};

} // namespace mx::core
