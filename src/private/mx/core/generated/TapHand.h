// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The tap-hand type represents the symbol to use for a tap element. The left and right values refer
/// to the SMuFL guitarLeftHandTapping and guitarRightHandTapping glyphs respectively.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TapHand final
{
  public:
    enum class Tag
    {
        left,
        right,
    };

    /// The natural zero: the first variant.
    TapHand() noexcept = default;

    static TapHand left() noexcept;
    static TapHand right() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TapHand &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TapHand parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TapHand parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TapHand &other) const noexcept = default;

  private:
    explicit TapHand(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::left};
};

} // namespace mx::core
