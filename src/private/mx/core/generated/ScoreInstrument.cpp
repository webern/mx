// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ScoreInstrument.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &ScoreInstrument::id() const noexcept
{
    return m_id;
}

void ScoreInstrument::setID(Token value)
{
    m_id = std::move(value);
}

const std::string &ScoreInstrument::instrumentName() const noexcept
{
    return m_instrumentName;
}

void ScoreInstrument::setInstrumentName(std::string value)
{
    m_instrumentName = std::move(value);
}

const std::optional<std::string> &ScoreInstrument::instrumentAbbreviation() const noexcept
{
    return m_instrumentAbbreviation;
}

void ScoreInstrument::setInstrumentAbbreviation(std::optional<std::string> value)
{
    m_instrumentAbbreviation = std::move(value);
}

const VirtualInstrumentDataGroup &ScoreInstrument::virtualInstrumentData() const noexcept
{
    return m_virtualInstrumentData;
}

void ScoreInstrument::setVirtualInstrumentData(VirtualInstrumentDataGroup value)
{
    m_virtualInstrumentData = std::move(value);
}

ScoreInstrument parseScoreInstrument(pugi::xml_node el, const ParseContext &context)
{
    ScoreInstrument out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseScoreInstrumentContent(out, el, context);
    return out;
}

void parseScoreInstrumentContent(ScoreInstrument &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "instrument-name"))
    {
        out.setInstrumentName(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "instrument-name");
    }
    if (cursorIs(cursor, "instrument-abbreviation"))
    {
        out.setInstrumentAbbreviation(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "instrument-sound") || cursorIs(cursor, "solo") || cursorIs(cursor, "ensemble") ||
                   cursorIs(cursor, "virtual-instrument")))
    {
        out.setVirtualInstrumentData(parseVirtualInstrumentDataGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "instrument-name") || cursorIs(cursor, "instrument-abbreviation") ||
            cursorIs(cursor, "instrument-sound") || cursorIs(cursor, "solo") || cursorIs(cursor, "ensemble") ||
            cursorIs(cursor, "virtual-instrument"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeScoreInstrument(const ScoreInstrument &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializeScoreInstrumentContent(v, el);
}

void serializeScoreInstrumentContent(const ScoreInstrument &v, pugi::xml_node el)
{
    writeTextElement(el, "instrument-name", v.instrumentName());
    if (v.instrumentAbbreviation())
    {
        writeTextElement(el, "instrument-abbreviation", *v.instrumentAbbreviation());
    }
    serializeVirtualInstrumentDataGroup(v.virtualInstrumentData(), el);
}

} // namespace mx::core
