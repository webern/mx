// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/BreathMark.h"
#include "mx/core/generated/Caesura.h"
#include "mx/core/generated/EmptyLine.h"
#include "mx/core/generated/EmptyPlacement.h"
#include "mx/core/generated/OtherPlacementText.h"
#include "mx/core/generated/StrongAccent.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class ArticulationsChoice final
{
  public:
    enum class Kind
    {
        accent,
        strongAccent,
        staccato,
        tenuto,
        detachedLegato,
        staccatissimo,
        spiccato,
        scoop,
        plop,
        doit,
        falloff,
        breathMark,
        caesura,
        stress,
        unstress,
        softAccent,
        otherArticulation,
    };

    /// The natural zero: the first alternative.
    ArticulationsChoice();

    static ArticulationsChoice accent(EmptyPlacement value);
    static ArticulationsChoice strongAccent(StrongAccent value);
    static ArticulationsChoice staccato(EmptyPlacement value);
    static ArticulationsChoice tenuto(EmptyPlacement value);
    static ArticulationsChoice detachedLegato(EmptyPlacement value);
    static ArticulationsChoice staccatissimo(EmptyPlacement value);
    static ArticulationsChoice spiccato(EmptyPlacement value);
    static ArticulationsChoice scoop(EmptyLine value);
    static ArticulationsChoice plop(EmptyLine value);
    static ArticulationsChoice doit(EmptyLine value);
    static ArticulationsChoice falloff(EmptyLine value);
    static ArticulationsChoice breathMark(BreathMark value);
    static ArticulationsChoice caesura(Caesura value);
    static ArticulationsChoice stress(EmptyPlacement value);
    static ArticulationsChoice unstress(EmptyPlacement value);
    static ArticulationsChoice softAccent(EmptyPlacement value);
    static ArticulationsChoice otherArticulation(OtherPlacementText value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isAccent() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isAccent().
    const EmptyPlacement &asAccent() const noexcept
    {
        assert(isAccent());
        return *std::get_if<0>(&m_value);
    }

    bool isStrongAccent() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isStrongAccent().
    const StrongAccent &asStrongAccent() const noexcept
    {
        assert(isStrongAccent());
        return *std::get_if<1>(&m_value);
    }

    bool isStaccato() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isStaccato().
    const EmptyPlacement &asStaccato() const noexcept
    {
        assert(isStaccato());
        return *std::get_if<2>(&m_value);
    }

    bool isTenuto() const noexcept
    {
        return m_value.index() == 3;
    }

    /// Precondition: isTenuto().
    const EmptyPlacement &asTenuto() const noexcept
    {
        assert(isTenuto());
        return *std::get_if<3>(&m_value);
    }

    bool isDetachedLegato() const noexcept
    {
        return m_value.index() == 4;
    }

    /// Precondition: isDetachedLegato().
    const EmptyPlacement &asDetachedLegato() const noexcept
    {
        assert(isDetachedLegato());
        return *std::get_if<4>(&m_value);
    }

    bool isStaccatissimo() const noexcept
    {
        return m_value.index() == 5;
    }

    /// Precondition: isStaccatissimo().
    const EmptyPlacement &asStaccatissimo() const noexcept
    {
        assert(isStaccatissimo());
        return *std::get_if<5>(&m_value);
    }

    bool isSpiccato() const noexcept
    {
        return m_value.index() == 6;
    }

    /// Precondition: isSpiccato().
    const EmptyPlacement &asSpiccato() const noexcept
    {
        assert(isSpiccato());
        return *std::get_if<6>(&m_value);
    }

    bool isScoop() const noexcept
    {
        return m_value.index() == 7;
    }

    /// Precondition: isScoop().
    const EmptyLine &asScoop() const noexcept
    {
        assert(isScoop());
        return *std::get_if<7>(&m_value);
    }

    bool isPlop() const noexcept
    {
        return m_value.index() == 8;
    }

    /// Precondition: isPlop().
    const EmptyLine &asPlop() const noexcept
    {
        assert(isPlop());
        return *std::get_if<8>(&m_value);
    }

    bool isDoit() const noexcept
    {
        return m_value.index() == 9;
    }

    /// Precondition: isDoit().
    const EmptyLine &asDoit() const noexcept
    {
        assert(isDoit());
        return *std::get_if<9>(&m_value);
    }

    bool isFalloff() const noexcept
    {
        return m_value.index() == 10;
    }

    /// Precondition: isFalloff().
    const EmptyLine &asFalloff() const noexcept
    {
        assert(isFalloff());
        return *std::get_if<10>(&m_value);
    }

    bool isBreathMark() const noexcept
    {
        return m_value.index() == 11;
    }

    /// Precondition: isBreathMark().
    const BreathMark &asBreathMark() const noexcept
    {
        assert(isBreathMark());
        return *std::get_if<11>(&m_value);
    }

    bool isCaesura() const noexcept
    {
        return m_value.index() == 12;
    }

    /// Precondition: isCaesura().
    const Caesura &asCaesura() const noexcept
    {
        assert(isCaesura());
        return *std::get_if<12>(&m_value);
    }

    bool isStress() const noexcept
    {
        return m_value.index() == 13;
    }

    /// Precondition: isStress().
    const EmptyPlacement &asStress() const noexcept
    {
        assert(isStress());
        return *std::get_if<13>(&m_value);
    }

    bool isUnstress() const noexcept
    {
        return m_value.index() == 14;
    }

    /// Precondition: isUnstress().
    const EmptyPlacement &asUnstress() const noexcept
    {
        assert(isUnstress());
        return *std::get_if<14>(&m_value);
    }

    bool isSoftAccent() const noexcept
    {
        return m_value.index() == 15;
    }

    /// Precondition: isSoftAccent().
    const EmptyPlacement &asSoftAccent() const noexcept
    {
        assert(isSoftAccent());
        return *std::get_if<15>(&m_value);
    }

    bool isOtherArticulation() const noexcept
    {
        return m_value.index() == 16;
    }

    /// Precondition: isOtherArticulation().
    const OtherPlacementText &asOtherArticulation() const noexcept
    {
        assert(isOtherArticulation());
        return *std::get_if<16>(&m_value);
    }

    bool operator==(const ArticulationsChoice &other) const = default;

  private:
    using Storage = std::variant<EmptyPlacement, StrongAccent, EmptyPlacement, EmptyPlacement, EmptyPlacement,
                                 EmptyPlacement, EmptyPlacement, EmptyLine, EmptyLine, EmptyLine, EmptyLine, BreathMark,
                                 Caesura, EmptyPlacement, EmptyPlacement, EmptyPlacement, OtherPlacementText>;

    explicit ArticulationsChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
ArticulationsChoice parseArticulationsChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeArticulationsChoice(const ArticulationsChoice &v, pugi::xml_node el);

} // namespace mx::core
