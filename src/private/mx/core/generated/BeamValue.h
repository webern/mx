// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The beam-value type represents the type of beam associated with each of 8 beam levels (up to
/// 1024th notes) available for each note.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class BeamValue final
{
  public:
    enum class Tag
    {
        begin,
        continue_,
        end,
        forwardHook,
        backwardHook,
    };

    /// The natural zero: the first variant.
    BeamValue() noexcept = default;

    static BeamValue begin() noexcept;
    static BeamValue continue_() noexcept;
    static BeamValue end() noexcept;
    static BeamValue forwardHook() noexcept;
    static BeamValue backwardHook() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, BeamValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static BeamValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static BeamValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const BeamValue &other) const noexcept = default;

  private:
    explicit BeamValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::begin};
};

} // namespace mx::core
