// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/CreditChoiceGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const CreditChoiceGroupChoice &CreditChoiceGroup::choice() const noexcept
{
    return m_choice;
}

void CreditChoiceGroup::setChoice(CreditChoiceGroupChoice value)
{
    m_choice = std::move(value);
}

std::span<const CreditChoiceGroupGroup> CreditChoiceGroup::group() const noexcept
{
    return m_group;
}

void CreditChoiceGroup::addGroup(CreditChoiceGroupGroup value)
{
    m_group.push_back(std::move(value));
}

void CreditChoiceGroup::setGroup(std::vector<CreditChoiceGroupGroup> value)
{
    m_group = std::move(value);
}

CreditChoiceGroup parseCreditChoiceGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    CreditChoiceGroup out;
    if (cursor && (cursorIs(cursor, "credit-words") || cursorIs(cursor, "credit-symbol")))
    {
        out.setChoice(parseCreditChoiceGroupChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "credit-words");
    }
    while (cursor && (cursorIs(cursor, "link") || cursorIs(cursor, "bookmark") || cursorIs(cursor, "credit-words") ||
                      cursorIs(cursor, "credit-symbol")))
    {
        out.addGroup(parseCreditChoiceGroupGroup(el, cursor, context));
    }
    return out;
}

void serializeCreditChoiceGroup(const CreditChoiceGroup &v, pugi::xml_node el)
{
    serializeCreditChoiceGroupChoice(v.choice(), el);
    for (const auto &item : v.group())
    {
        serializeCreditChoiceGroupGroup(item, el);
    }
}

} // namespace mx::core
