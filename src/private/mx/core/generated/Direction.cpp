// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Direction.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<AboveBelow> &Direction::placement() const noexcept
{
    return m_placement;
}

void Direction::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<YesNo> &Direction::directive() const noexcept
{
    return m_directive;
}

void Direction::setDirective(std::optional<YesNo> value)
{
    m_directive = std::move(value);
}

const std::optional<SystemRelation> &Direction::system() const noexcept
{
    return m_system;
}

void Direction::setSystem(std::optional<SystemRelation> value)
{
    m_system = std::move(value);
}

const std::optional<Token> &Direction::id() const noexcept
{
    return m_id;
}

void Direction::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const DirectionType> Direction::directionType() const noexcept
{
    return m_directionType.items();
}

void Direction::addDirectionType(DirectionType value)
{
    m_directionType.add(std::move(value));
}

void Direction::setDirectionType(OneOrMore<DirectionType> value)
{
    m_directionType = std::move(value);
}

const std::optional<Offset> &Direction::offset() const noexcept
{
    return m_offset;
}

void Direction::setOffset(std::optional<Offset> value)
{
    m_offset = std::move(value);
}

const EditorialVoiceDirectionGroup &Direction::editorialVoiceDirection() const noexcept
{
    return m_editorialVoiceDirection;
}

void Direction::setEditorialVoiceDirection(EditorialVoiceDirectionGroup value)
{
    m_editorialVoiceDirection = std::move(value);
}

const std::optional<int> &Direction::staff() const noexcept
{
    return m_staff;
}

void Direction::setStaff(std::optional<int> value)
{
    m_staff = std::move(value);
}

const std::optional<Sound> &Direction::sound() const noexcept
{
    return m_sound;
}

void Direction::setSound(std::optional<Sound> value)
{
    m_sound = std::move(value);
}

const std::optional<Listening> &Direction::listening() const noexcept
{
    return m_listening;
}

void Direction::setListening(std::optional<Listening> value)
{
    m_listening = std::move(value);
}

Direction parseDirection(pugi::xml_node el)
{
    return parseDirection(el, ParseContext{});
}

Direction parseDirection(pugi::xml_node el, const ParseContext &context)
{
    Direction out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "directive")
        {
            out.setDirective(parseValue<YesNo>(a.value(), context, el, "directive"));
        }
        else if (aname == "system")
        {
            out.setSystem(parseValue<SystemRelation>(a.value(), context, el, "system"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseDirectionContent(out, el, context);
    return out;
}

void parseDirectionContent(Direction &out, pugi::xml_node el)
{
    parseDirectionContent(out, el, ParseContext{});
}

void parseDirectionContent(Direction &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!cursorIs(cursor, "direction-type"))
    {
        throwMissingOrMisplaced(el, cursor, "direction-type");
    }
    out.setDirectionType(OneOrMore<DirectionType>{parseDirectionType(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "direction-type"))
    {
        out.addDirectionType(parseDirectionType(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "offset"))
    {
        out.setOffset(parseOffset(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "voice")))
    {
        out.setEditorialVoiceDirection(parseEditorialVoiceDirectionGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "staff"))
    {
        out.setStaff(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "sound"))
    {
        out.setSound(parseSound(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "listening"))
    {
        out.setListening(parseListening(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "direction-type") || cursorIs(cursor, "offset") || cursorIs(cursor, "footnote") ||
            cursorIs(cursor, "level") || cursorIs(cursor, "voice") || cursorIs(cursor, "staff") ||
            cursorIs(cursor, "sound") || cursorIs(cursor, "listening"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeDirection(const Direction &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.directive())
    {
        setAttribute(el, "directive", v.directive()->toString());
    }
    if (v.system())
    {
        setAttribute(el, "system", v.system()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeDirectionContent(v, el);
}

void serializeDirectionContent(const Direction &v, pugi::xml_node el)
{
    for (const auto &item : v.directionType())
    {
        serializeDirectionType(item, el, "direction-type");
    }
    if (v.offset())
    {
        serializeOffset(*v.offset(), el, "offset");
    }
    serializeEditorialVoiceDirectionGroup(v.editorialVoiceDirection(), el);
    if (v.staff())
    {
        writeTextElement(el, "staff", formatInt(*v.staff()));
    }
    if (v.sound())
    {
        serializeSound(*v.sound(), el, "sound");
    }
    if (v.listening())
    {
        serializeListening(*v.listening(), el, "listening");
    }
}

} // namespace mx::core
