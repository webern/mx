// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The font-weight type represents a simplified version of the CSS font-weight property.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class FontWeight final
{
  public:
    enum class Tag
    {
        normal,
        bold,
    };

    /// The natural zero: the first variant.
    FontWeight() noexcept = default;

    static FontWeight normal() noexcept;
    static FontWeight bold() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, FontWeight &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static FontWeight parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static FontWeight parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const FontWeight &other) const noexcept = default;

  private:
    explicit FontWeight(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::normal};
};

} // namespace mx::core
