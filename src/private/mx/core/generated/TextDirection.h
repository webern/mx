// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The text-direction type is used to adjust and override the Unicode bidirectional text algorithm,
/// similar to the Directionality data category in the W3C Internationalization Tag Set
/// recommendation. Values are ltr (left-to-right embed), rtl (right-to-left embed), lro
/// (left-to-right bidi-override), and rlo (right-to-left bidi-override). The default value is ltr.
/// This type is typically used by applications that store text in left-to-right visual order rather
/// than logical order. Such applications can use the lro value to better communicate with other
/// applications that more fully support bidirectional text.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TextDirection final
{
  public:
    enum class Tag
    {
        ltr,
        rtl,
        lro,
        rlo,
    };

    /// The natural zero: the first variant.
    TextDirection() noexcept = default;

    static TextDirection ltr() noexcept;
    static TextDirection rtl() noexcept;
    static TextDirection lro() noexcept;
    static TextDirection rlo() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TextDirection &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TextDirection parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TextDirection parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TextDirection &other) const noexcept = default;

  private:
    explicit TextDirection(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::ltr};
};

} // namespace mx::core
