// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Effect.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<SmuflPictogramGlyphName> &Effect::smufl() const noexcept
{
    return m_smufl;
}

void Effect::setSmufl(std::optional<SmuflPictogramGlyphName> value)
{
    m_smufl = std::move(value);
}

const EffectValue &Effect::value() const noexcept
{
    return m_value;
}

void Effect::setValue(EffectValue value)
{
    m_value = std::move(value);
}

Effect parseEffect(pugi::xml_node el)
{
    return parseEffect(el, ParseContext{});
}

Effect parseEffect(pugi::xml_node el, const ParseContext &context)
{
    Effect out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "smufl")
        {
            out.setSmufl(parseValue<SmuflPictogramGlyphName>(a.value(), context, el, "smufl"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseEffectContent(out, el, context);
    return out;
}

void parseEffectContent(Effect &out, pugi::xml_node el)
{
    parseEffectContent(out, el, ParseContext{});
}

void parseEffectContent(Effect &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<EffectValue>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeEffect(const Effect &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.smufl())
    {
        setAttribute(el, "smufl", v.smufl()->toString());
    }
    serializeEffectContent(v, el);
}

void serializeEffectContent(const Effect &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
