// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TimeChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

TimeChoice::TimeChoice()
{
    *this = TimeChoice::group(TimeChoiceGroup{});
}

TimeChoice TimeChoice::group(TimeChoiceGroup value)
{
    return TimeChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

TimeChoice TimeChoice::senzaMisura(std::string value)
{
    return TimeChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

TimeChoice parseTimeChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "beats")))
    {
        return TimeChoice::group(parseTimeChoiceGroup(el, cursor, context));
    }
    if (cursor && (cursorIs(cursor, "senza-misura")))
    {
        std::string value = std::string{childText(cursor)};
        cursor = nextElement(cursor);
        return TimeChoice::senzaMisura(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "beats");
}

void serializeTimeChoice(const TimeChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case TimeChoice::Kind::group:
        serializeTimeChoiceGroup(v.asGroup(), el);
        break;
    case TimeChoice::Kind::senzaMisura:
        writeTextElement(el, "senza-misura", v.asSenzaMisura());
        break;
    }
}

} // namespace mx::core
