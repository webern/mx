// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Slash.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &Slash::type() const noexcept
{
    return m_type;
}

void Slash::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<YesNo> &Slash::useDots() const noexcept
{
    return m_useDots;
}

void Slash::setUseDots(std::optional<YesNo> value)
{
    m_useDots = std::move(value);
}

const std::optional<YesNo> &Slash::useStems() const noexcept
{
    return m_useStems;
}

void Slash::setUseStems(std::optional<YesNo> value)
{
    m_useStems = std::move(value);
}

const std::optional<SlashGroup> &Slash::slash() const noexcept
{
    return m_slash;
}

void Slash::setSlash(std::optional<SlashGroup> value)
{
    m_slash = std::move(value);
}

Slash parseSlash(pugi::xml_node el, const ParseContext &context)
{
    Slash out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStop>(a.value(), context, el, "type"));
        }
        else if (aname == "use-dots")
        {
            out.setUseDots(parseValue<YesNo>(a.value(), context, el, "use-dots"));
        }
        else if (aname == "use-stems")
        {
            out.setUseStems(parseValue<YesNo>(a.value(), context, el, "use-stems"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseSlashContent(out, el, context);
    return out;
}

void parseSlashContent(Slash &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "slash-type") || cursorIs(cursor, "except-voice")))
    {
        out.setSlash(parseSlashGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "slash-type") || cursorIs(cursor, "slash-dot") || cursorIs(cursor, "except-voice"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeSlash(const Slash &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.useDots())
    {
        setAttribute(el, "use-dots", v.useDots()->toString());
    }
    if (v.useStems())
    {
        setAttribute(el, "use-stems", v.useStems()->toString());
    }
    serializeSlashContent(v, el);
}

void serializeSlashContent(const Slash &v, pugi::xml_node el)
{
    if (v.slash())
    {
        serializeSlashGroup(*v.slash(), el);
    }
}

} // namespace mx::core
