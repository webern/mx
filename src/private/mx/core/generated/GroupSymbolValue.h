// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The group-symbol-value type indicates how the symbol for a group or multi-staff part is indicated
/// in the score.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class GroupSymbolValue final
{
  public:
    enum class Tag
    {
        none,
        brace,
        line,
        bracket,
        square,
    };

    /// The natural zero: the first variant.
    GroupSymbolValue() noexcept = default;

    static GroupSymbolValue none() noexcept;
    static GroupSymbolValue brace() noexcept;
    static GroupSymbolValue line() noexcept;
    static GroupSymbolValue bracket() noexcept;
    static GroupSymbolValue square() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, GroupSymbolValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static GroupSymbolValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static GroupSymbolValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const GroupSymbolValue &other) const noexcept = default;

  private:
    explicit GroupSymbolValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::none};
};

} // namespace mx::core
