// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The symbol-size type is used to distinguish between full, cue sized, grace cue sized, and
/// oversized symbols.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class SymbolSize final
{
  public:
    enum class Tag
    {
        full,
        cue,
        graceCue,
        large,
    };

    /// The natural zero: the first variant.
    SymbolSize() noexcept = default;

    static SymbolSize full() noexcept;
    static SymbolSize cue() noexcept;
    static SymbolSize graceCue() noexcept;
    static SymbolSize large() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, SymbolSize &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static SymbolSize parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static SymbolSize parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const SymbolSize &other) const noexcept = default;

  private:
    explicit SymbolSize(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::full};
};

} // namespace mx::core
