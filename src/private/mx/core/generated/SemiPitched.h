// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The semi-pitched type represents categories of indefinite pitch for percussion instruments.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class SemiPitched final
{
  public:
    enum class Tag
    {
        high,
        mediumHigh,
        medium,
        mediumLow,
        low,
        veryLow,
    };

    /// The natural zero: the first variant.
    SemiPitched() noexcept = default;

    static SemiPitched high() noexcept;
    static SemiPitched mediumHigh() noexcept;
    static SemiPitched medium() noexcept;
    static SemiPitched mediumLow() noexcept;
    static SemiPitched low() noexcept;
    static SemiPitched veryLow() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, SemiPitched &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static SemiPitched parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static SemiPitched parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const SemiPitched &other) const noexcept = default;

  private:
    explicit SemiPitched(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::high};
};

} // namespace mx::core
