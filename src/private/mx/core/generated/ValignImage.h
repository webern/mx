// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The valign-image type is used to indicate vertical alignment for images and graphics, so it does
/// not include a baseline value. Defaults are implementation-dependent.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ValignImage final
{
  public:
    enum class Tag
    {
        top,
        middle,
        bottom,
    };

    /// The natural zero: the first variant.
    ValignImage() noexcept = default;

    static ValignImage top() noexcept;
    static ValignImage middle() noexcept;
    static ValignImage bottom() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ValignImage &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ValignImage parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ValignImage parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ValignImage &other) const noexcept = default;

  private:
    explicit ValignImage(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::top};
};

} // namespace mx::core
