// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Ornaments.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Token> &Ornaments::id() const noexcept
{
    return m_id;
}

void Ornaments::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const OrnamentsGroup> Ornaments::group() const noexcept
{
    return m_group;
}

void Ornaments::addGroup(OrnamentsGroup value)
{
    m_group.push_back(std::move(value));
}

void Ornaments::setGroup(std::vector<OrnamentsGroup> value)
{
    m_group = std::move(value);
}

Ornaments parseOrnaments(pugi::xml_node el)
{
    return parseOrnaments(el, ParseContext{});
}

Ornaments parseOrnaments(pugi::xml_node el, const ParseContext &context)
{
    Ornaments out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseOrnamentsContent(out, el, context);
    return out;
}

void parseOrnamentsContent(Ornaments &out, pugi::xml_node el)
{
    parseOrnamentsContent(out, el, ParseContext{});
}

void parseOrnamentsContent(Ornaments &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor && (cursorIs(cursor, "trill-mark") || cursorIs(cursor, "turn") || cursorIs(cursor, "delayed-turn") ||
                      cursorIs(cursor, "inverted-turn") || cursorIs(cursor, "delayed-inverted-turn") ||
                      cursorIs(cursor, "vertical-turn") || cursorIs(cursor, "inverted-vertical-turn") ||
                      cursorIs(cursor, "shake") || cursorIs(cursor, "wavy-line") || cursorIs(cursor, "mordent") ||
                      cursorIs(cursor, "inverted-mordent") || cursorIs(cursor, "schleifer") ||
                      cursorIs(cursor, "tremolo") || cursorIs(cursor, "haydn") || cursorIs(cursor, "other-ornament")))
    {
        out.addGroup(parseOrnamentsGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "trill-mark") || cursorIs(cursor, "turn") || cursorIs(cursor, "delayed-turn") ||
            cursorIs(cursor, "inverted-turn") || cursorIs(cursor, "delayed-inverted-turn") ||
            cursorIs(cursor, "vertical-turn") || cursorIs(cursor, "inverted-vertical-turn") ||
            cursorIs(cursor, "shake") || cursorIs(cursor, "wavy-line") || cursorIs(cursor, "mordent") ||
            cursorIs(cursor, "inverted-mordent") || cursorIs(cursor, "schleifer") || cursorIs(cursor, "tremolo") ||
            cursorIs(cursor, "haydn") || cursorIs(cursor, "other-ornament") || cursorIs(cursor, "accidental-mark"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeOrnaments(const Ornaments &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeOrnamentsContent(v, el);
}

void serializeOrnamentsContent(const Ornaments &v, pugi::xml_node el)
{
    for (const auto &item : v.group())
    {
        serializeOrnamentsGroup(item, el);
    }
}

} // namespace mx::core
