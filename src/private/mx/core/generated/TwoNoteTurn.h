// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The two-note-turn type describes the ending notes of trills and mordents for playback, relative
/// to the current note.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TwoNoteTurn final
{
  public:
    enum class Tag
    {
        whole,
        half,
        none,
    };

    /// The natural zero: the first variant.
    TwoNoteTurn() noexcept = default;

    static TwoNoteTurn whole() noexcept;
    static TwoNoteTurn half() noexcept;
    static TwoNoteTurn none() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TwoNoteTurn &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TwoNoteTurn parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TwoNoteTurn parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TwoNoteTurn &other) const noexcept = default;

  private:
    explicit TwoNoteTurn(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::whole};
};

} // namespace mx::core
