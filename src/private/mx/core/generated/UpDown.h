// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The up-down type is used for the direction of arrows and other pointed symbols like vertical
/// accents, indicating which way the tip is pointing.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class UpDown final
{
  public:
    enum class Tag
    {
        up,
        down,
    };

    /// The natural zero: the first variant.
    UpDown() noexcept = default;

    static UpDown up() noexcept;
    static UpDown down() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, UpDown &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static UpDown parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static UpDown parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const UpDown &other) const noexcept = default;

  private:
    explicit UpDown(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::up};
};

} // namespace mx::core
