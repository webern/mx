// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The staff-number type indicates staff numbers within a multi-staff part. Staves are numbered from
/// top to bottom, with 1 being the top staff on a part.
/// Construction and mutation clamp to the schema's range (one policy, one
/// spelling, identical to import); an out-of-range value is unrepresentable.
class StaffNumber final
{
  public:
    /// The natural zero: the default value clamped into range.
    StaffNumber();

    explicit StaffNumber(int value);

    int value() const noexcept
    {
        return m_value;
    }

    void setValue(int value);

    std::string toString() const;

    /// Strict lexical parse, then range-clamp (the import leniency policy
    /// for numbers, plan §2.4).
    static bool tryParse(std::string_view text, StaffNumber &out);

    /// Lenient: non-numeric text yields the clamped zero.
    static StaffNumber parse(std::string_view text);

    /// Lenient, and says whether the text was non-numeric or clamped.
    static StaffNumber parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const StaffNumber &other) const noexcept = default;

  private:
    int m_value{};
};

} // namespace mx::core
