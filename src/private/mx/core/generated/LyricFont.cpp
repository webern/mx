// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/LyricFont.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<NameToken> &LyricFont::number() const noexcept
{
    return m_number;
}

void LyricFont::setNumber(std::optional<NameToken> value)
{
    m_number = std::move(value);
}

const std::optional<std::string> &LyricFont::name() const noexcept
{
    return m_name;
}

void LyricFont::setName(std::optional<std::string> value)
{
    m_name = std::move(value);
}

const std::optional<FontFamily> &LyricFont::fontFamily() const noexcept
{
    return m_fontFamily;
}

void LyricFont::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &LyricFont::fontStyle() const noexcept
{
    return m_fontStyle;
}

void LyricFont::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &LyricFont::fontSize() const noexcept
{
    return m_fontSize;
}

void LyricFont::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &LyricFont::fontWeight() const noexcept
{
    return m_fontWeight;
}

void LyricFont::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

LyricFont parseLyricFont(pugi::xml_node el, const ParseContext &context)
{
    LyricFont out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(NameToken::parse(a.value()));
        }
        else if (aname == "name")
        {
            out.setName(std::string{a.value()});
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseLyricFontContent(out, el, context);
    return out;
}

void parseLyricFontContent(LyricFont &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeLyricFont(const LyricFont &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.name())
    {
        setAttribute(el, "name", *v.name());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    serializeLyricFontContent(v, el);
}

void serializeLyricFontContent(const LyricFont &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
