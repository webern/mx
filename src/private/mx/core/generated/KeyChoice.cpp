// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/KeyChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

KeyChoice::KeyChoice()
{
    *this = KeyChoice::nonTraditionalKey(std::vector<NonTraditionalKeyGroup>{});
}

KeyChoice KeyChoice::traditionalKey(TraditionalKeyGroup value)
{
    return KeyChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

KeyChoice KeyChoice::nonTraditionalKey(std::vector<NonTraditionalKeyGroup> value)
{
    return KeyChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

KeyChoice parseKeyChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseKeyChoice(el, cursor, ParseContext{});
}

KeyChoice parseKeyChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "cancel") || cursorIs(cursor, "fifths")))
    {
        return KeyChoice::traditionalKey(parseTraditionalKeyGroup(el, cursor, context));
    }
    if (cursor && (cursorIs(cursor, "key-step")))
    {
        std::vector<NonTraditionalKeyGroup> items;
        while (cursor && (cursorIs(cursor, "key-step")))
        {
            items.push_back(parseNonTraditionalKeyGroup(el, cursor, context));
        }
        return KeyChoice::nonTraditionalKey(std::move(items));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "cancel");
}

void serializeKeyChoice(const KeyChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case KeyChoice::Kind::traditionalKey:
        serializeTraditionalKeyGroup(v.asTraditionalKey(), el);
        break;
    case KeyChoice::Kind::nonTraditionalKey:
        for (const auto &item : v.asNonTraditionalKey())
        {
            serializeNonTraditionalKeyGroup(item, el);
        }
        break;
    }
}

} // namespace mx::core
