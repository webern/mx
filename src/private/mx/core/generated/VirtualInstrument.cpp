// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/VirtualInstrument.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &VirtualInstrument::virtualLibrary() const noexcept
{
    return m_virtualLibrary;
}

void VirtualInstrument::setVirtualLibrary(std::optional<std::string> value)
{
    m_virtualLibrary = std::move(value);
}

const std::optional<std::string> &VirtualInstrument::virtualName() const noexcept
{
    return m_virtualName;
}

void VirtualInstrument::setVirtualName(std::optional<std::string> value)
{
    m_virtualName = std::move(value);
}

VirtualInstrument parseVirtualInstrument(pugi::xml_node el, const ParseContext &context)
{
    VirtualInstrument out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseVirtualInstrumentContent(out, el, context);
    return out;
}

void parseVirtualInstrumentContent(VirtualInstrument &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "virtual-library"))
    {
        out.setVirtualLibrary(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "virtual-name"))
    {
        out.setVirtualName(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "virtual-library") || cursorIs(cursor, "virtual-name"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeVirtualInstrument(const VirtualInstrument &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeVirtualInstrumentContent(v, el);
}

void serializeVirtualInstrumentContent(const VirtualInstrument &v, pugi::xml_node el)
{
    if (v.virtualLibrary())
    {
        writeTextElement(el, "virtual-library", *v.virtualLibrary());
    }
    if (v.virtualName())
    {
        writeTextElement(el, "virtual-name", *v.virtualName());
    }
}

} // namespace mx::core
