// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MetronomeChoiceGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const BeatUnitGroup &MetronomeChoiceGroup::beatUnit() const noexcept
{
    return m_beatUnit;
}

void MetronomeChoiceGroup::setBeatUnit(BeatUnitGroup value)
{
    m_beatUnit = std::move(value);
}

std::span<const BeatUnitTied> MetronomeChoiceGroup::beatUnitTied() const noexcept
{
    return m_beatUnitTied;
}

void MetronomeChoiceGroup::addBeatUnitTied(BeatUnitTied value)
{
    m_beatUnitTied.push_back(std::move(value));
}

void MetronomeChoiceGroup::setBeatUnitTied(std::vector<BeatUnitTied> value)
{
    m_beatUnitTied = std::move(value);
}

const MetronomeChoiceGroupChoice &MetronomeChoiceGroup::choice() const noexcept
{
    return m_choice;
}

void MetronomeChoiceGroup::setChoice(MetronomeChoiceGroupChoice value)
{
    m_choice = std::move(value);
}

MetronomeChoiceGroup parseMetronomeChoiceGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseMetronomeChoiceGroup(el, cursor, ParseContext{});
}

MetronomeChoiceGroup parseMetronomeChoiceGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    MetronomeChoiceGroup out;
    if (cursor && (cursorIs(cursor, "beat-unit")))
    {
        out.setBeatUnit(parseBeatUnitGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "beat-unit");
    }
    while (cursorIs(cursor, "beat-unit-tied"))
    {
        out.addBeatUnitTied(parseBeatUnitTied(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "per-minute") || cursorIs(cursor, "beat-unit")))
    {
        out.setChoice(parseMetronomeChoiceGroupChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "per-minute");
    }
    return out;
}

void serializeMetronomeChoiceGroup(const MetronomeChoiceGroup &v, pugi::xml_node el)
{
    serializeBeatUnitGroup(v.beatUnit(), el);
    for (const auto &item : v.beatUnitTied())
    {
        serializeBeatUnitTied(item, el, "beat-unit-tied");
    }
    serializeMetronomeChoiceGroupChoice(v.choice(), el);
}

} // namespace mx::core
