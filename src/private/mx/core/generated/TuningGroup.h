// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Octave.h"
#include "mx/core/generated/Semitones.h"
#include "mx/core/generated/Step.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The tuning group contains the sequence of elements common to the staff-tuning and accord
/// elements.
/// A shared content group: transparent on the wire, its
/// fields serialize directly into the referencing element (plan §2.3).
class TuningGroup final
{
  public:
    const Step &tuningStep() const noexcept;
    void setTuningStep(Step value);
    const std::optional<Semitones> &tuningAlter() const noexcept;
    void setTuningAlter(std::optional<Semitones> value);
    const Octave &tuningOctave() const noexcept;
    void setTuningOctave(Octave value);
    bool operator==(const TuningGroup &other) const = default;

  private:
    Step m_tuningStep{};
    std::optional<Semitones> m_tuningAlter;
    Octave m_tuningOctave{};
};

/// Consumes matching siblings starting at `cursor` (which advances); `el`
/// is the enclosing element, for error paths.
TuningGroup parseTuningGroup(pugi::xml_node el, pugi::xml_node &cursor);

TuningGroup parseTuningGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeTuningGroup(const TuningGroup &v, pugi::xml_node el);

} // namespace mx::core
