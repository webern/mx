// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/NoteChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

NoteChoice::NoteChoice()
{
    *this = NoteChoice::graceNoteGroup(GraceNoteGroup{});
}

NoteChoice NoteChoice::graceNoteGroup(GraceNoteGroup value)
{
    return NoteChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

NoteChoice NoteChoice::cueNoteGroup(CueNoteGroup value)
{
    return NoteChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

NoteChoice NoteChoice::normalNoteGroup(NormalNoteGroup value)
{
    return NoteChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

NoteChoice parseNoteChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseNoteChoice(el, cursor, ParseContext{});
}

NoteChoice parseNoteChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "grace")))
    {
        return NoteChoice::graceNoteGroup(parseGraceNoteGroup(el, cursor, context));
    }
    if (cursor && (cursorIs(cursor, "cue")))
    {
        return NoteChoice::cueNoteGroup(parseCueNoteGroup(el, cursor, context));
    }
    if (cursor && (cursorIs(cursor, "chord") || cursorIs(cursor, "pitch") || cursorIs(cursor, "unpitched") ||
                   cursorIs(cursor, "rest")))
    {
        return NoteChoice::normalNoteGroup(parseNormalNoteGroup(el, cursor, context));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "grace");
}

void serializeNoteChoice(const NoteChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case NoteChoice::Kind::graceNoteGroup:
        serializeGraceNoteGroup(v.asGraceNoteGroup(), el);
        break;
    case NoteChoice::Kind::cueNoteGroup:
        serializeCueNoteGroup(v.asCueNoteGroup(), el);
        break;
    case NoteChoice::Kind::normalNoteGroup:
        serializeNormalNoteGroup(v.asNormalNoteGroup(), el);
        break;
    }
}

} // namespace mx::core
