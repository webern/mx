// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Beam.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<BeamLevel> &Beam::number() const noexcept
{
    return m_number;
}

void Beam::setNumber(std::optional<BeamLevel> value)
{
    m_number = std::move(value);
}

const std::optional<YesNo> &Beam::repeater() const noexcept
{
    return m_repeater;
}

void Beam::setRepeater(std::optional<YesNo> value)
{
    m_repeater = std::move(value);
}

const std::optional<Fan> &Beam::fan() const noexcept
{
    return m_fan;
}

void Beam::setFan(std::optional<Fan> value)
{
    m_fan = std::move(value);
}

const std::optional<Color> &Beam::color() const noexcept
{
    return m_color;
}

void Beam::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &Beam::id() const noexcept
{
    return m_id;
}

void Beam::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const BeamValue &Beam::value() const noexcept
{
    return m_value;
}

void Beam::setValue(BeamValue value)
{
    m_value = std::move(value);
}

Beam parseBeam(pugi::xml_node el)
{
    return parseBeam(el, ParseContext{});
}

Beam parseBeam(pugi::xml_node el, const ParseContext &context)
{
    Beam out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<BeamLevel>(a.value(), context, el, "number"));
        }
        else if (aname == "repeater")
        {
            out.setRepeater(parseValue<YesNo>(a.value(), context, el, "repeater"));
        }
        else if (aname == "fan")
        {
            out.setFan(parseValue<Fan>(a.value(), context, el, "fan"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseBeamContent(out, el, context);
    return out;
}

void parseBeamContent(Beam &out, pugi::xml_node el)
{
    parseBeamContent(out, el, ParseContext{});
}

void parseBeamContent(Beam &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<BeamValue>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeBeam(const Beam &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.repeater())
    {
        setAttribute(el, "repeater", v.repeater()->toString());
    }
    if (v.fan())
    {
        setAttribute(el, "fan", v.fan()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeBeamContent(v, el);
}

void serializeBeamContent(const Beam &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
