// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Decimal.h"
#include "mx/core/Lexical.h"
#include "mx/core/generated/YesNo.h"

#include <cassert>
#include <string>
#include <string_view>
#include <variant>

namespace mx::core
{

/// The yes-no-number type is used for attributes that can be either boolean or numeric values.
/// Tagged union over the schema's member types: exactly one alternative by
/// construction. Alternatives are positional; dispatch is by Kind/index.
class YesNoNumber final
{
  public:
    enum class Kind
    {
        yesNo,
        decimal,
    };

    /// The natural zero: the first member's natural zero.
    YesNoNumber();

    static YesNoNumber yesNo(YesNo value);
    static YesNoNumber decimal(Decimal value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isYesNo() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isYesNo().
    const YesNo &asYesNo() const noexcept
    {
        assert(isYesNo());
        return *std::get_if<0>(&m_value);
    }

    bool isDecimal() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isDecimal().
    const Decimal &asDecimal() const noexcept
    {
        assert(isDecimal());
        return *std::get_if<1>(&m_value);
    }

    std::string toString() const;

    /// Strict: members tried in schema order.
    static bool tryParse(std::string_view text, YesNoNumber &out);

    /// Lenient: unparseable text falls back to the first member's lenient
    /// parse (never produces an invalid value).
    static YesNoNumber parse(std::string_view text);

    /// Lenient, and says whether no member matched or a matched number was
    /// clamped.
    static YesNoNumber parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const YesNoNumber &other) const = default;

  private:
    using Storage = std::variant<YesNo, Decimal>;

    explicit YesNoNumber(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

} // namespace mx::core
