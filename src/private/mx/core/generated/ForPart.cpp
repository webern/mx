// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ForPart.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &ForPart::number() const noexcept
{
    return m_number;
}

void ForPart::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<Token> &ForPart::id() const noexcept
{
    return m_id;
}

void ForPart::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const std::optional<PartClef> &ForPart::partClef() const noexcept
{
    return m_partClef;
}

void ForPart::setPartClef(std::optional<PartClef> value)
{
    m_partClef = std::move(value);
}

const PartTranspose &ForPart::partTranspose() const noexcept
{
    return m_partTranspose;
}

void ForPart::setPartTranspose(PartTranspose value)
{
    m_partTranspose = std::move(value);
}

ForPart parseForPart(pugi::xml_node el, const ParseContext &context)
{
    ForPart out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseForPartContent(out, el, context);
    return out;
}

void parseForPartContent(ForPart &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "part-clef"))
    {
        out.setPartClef(parsePartClef(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-transpose"))
    {
        out.setPartTranspose(parsePartTranspose(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "part-transpose");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "part-clef") || cursorIs(cursor, "part-transpose"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeForPart(const ForPart &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeForPartContent(v, el);
}

void serializeForPartContent(const ForPart &v, pugi::xml_node el)
{
    if (v.partClef())
    {
        serializePartClef(*v.partClef(), el, "part-clef");
    }
    serializePartTranspose(v.partTranspose(), el, "part-transpose");
}

} // namespace mx::core
