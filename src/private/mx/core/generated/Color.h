// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <cstdint>
#include <optional>
#include <string>
#include <string_view>

namespace mx::core
{

/// The color type indicates the color of an element. Color may be represented as hexadecimal RGB
/// triples, as in HTML, or as hexadecimal ARGB tuples, with the A indicating alpha of transparency.
/// An alpha value of 00 is totally transparent; FF is totally opaque. If RGB is used, the A value is
/// assumed to be FF. For instance, the RGB value "#800080" represents purple. An ARGB value of
/// "#40800080" would be a transparent purple. As in SVG 1.1, colors are defined in terms of the sRGB
/// color space (IEC 61966).
/// Structural representation (plan §2.2): numeric RGB components plus an
/// optional alpha; serialization formats #RRGGBB / #AARRGGBB. A malformed
/// hex string cannot exist.
class Color final
{
  public:
    /// The natural zero: opaque black, #000000.
    Color() noexcept = default;

    Color(std::uint8_t red, std::uint8_t green, std::uint8_t blue) noexcept : m_red{red}, m_green{green}, m_blue{blue}
    {
    }

    Color(std::uint8_t alpha, std::uint8_t red, std::uint8_t green, std::uint8_t blue) noexcept
        : m_alpha{alpha}, m_red{red}, m_green{green}, m_blue{blue}
    {
    }

    std::uint8_t red() const noexcept
    {
        return m_red;
    }

    std::uint8_t green() const noexcept
    {
        return m_green;
    }

    std::uint8_t blue() const noexcept
    {
        return m_blue;
    }

    const std::optional<std::uint8_t> &alpha() const noexcept
    {
        return m_alpha;
    }

    void setRed(std::uint8_t value) noexcept
    {
        m_red = value;
    }

    void setGreen(std::uint8_t value) noexcept
    {
        m_green = value;
    }

    void setBlue(std::uint8_t value) noexcept
    {
        m_blue = value;
    }

    void setAlpha(std::optional<std::uint8_t> value) noexcept
    {
        m_alpha = value;
    }

    /// #RRGGBB, or #AARRGGBB when alpha is engaged (uppercase hex).
    std::string toString() const;

    /// Strict: exactly the schema's #RRGGBB / #AARRGGBB shape.
    static bool tryParse(std::string_view text, Color &out) noexcept;

    /// Lenient: malformed text yields opaque black.
    static Color parse(std::string_view text) noexcept;

    /// Lenient, and says whether the text had to be repaired.
    static Color parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const Color &other) const noexcept = default;

  private:
    std::optional<std::uint8_t> m_alpha;
    std::uint8_t m_red{0};
    std::uint8_t m_green{0};
    std::uint8_t m_blue{0};
};

} // namespace mx::core
