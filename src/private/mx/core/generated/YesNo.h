// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The yes-no type is used for boolean-like attributes. We cannot use W3C XML Schema booleans due to
/// their restrictions on expression of boolean values.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class YesNo final
{
  public:
    enum class Tag
    {
        yes,
        no,
    };

    /// The natural zero: the first variant.
    YesNo() noexcept = default;

    static YesNo yes() noexcept;
    static YesNo no() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, YesNo &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static YesNo parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static YesNo parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const YesNo &other) const noexcept = default;

  private:
    explicit YesNo(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::yes};
};

} // namespace mx::core
