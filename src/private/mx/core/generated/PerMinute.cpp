// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PerMinute.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<FontFamily> &PerMinute::fontFamily() const noexcept
{
    return m_fontFamily;
}

void PerMinute::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &PerMinute::fontStyle() const noexcept
{
    return m_fontStyle;
}

void PerMinute::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &PerMinute::fontSize() const noexcept
{
    return m_fontSize;
}

void PerMinute::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &PerMinute::fontWeight() const noexcept
{
    return m_fontWeight;
}

void PerMinute::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::string &PerMinute::value() const noexcept
{
    return m_value;
}

void PerMinute::setValue(std::string value)
{
    m_value = std::move(value);
}

PerMinute parsePerMinute(pugi::xml_node el, const ParseContext &context)
{
    PerMinute out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parsePerMinuteContent(out, el, context);
    return out;
}

void parsePerMinuteContent(PerMinute &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(std::string{childText(el)});
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializePerMinute(const PerMinute &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    serializePerMinuteContent(v, el);
}

void serializePerMinuteContent(const PerMinute &v, pugi::xml_node el)
{
    setText(el, v.value());
}

} // namespace mx::core
