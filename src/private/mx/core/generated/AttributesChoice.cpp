// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/AttributesChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

AttributesChoice::AttributesChoice()
{
    *this = AttributesChoice::transpose(std::vector<Transpose>{});
}

AttributesChoice AttributesChoice::transpose(std::vector<Transpose> value)
{
    return AttributesChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

AttributesChoice AttributesChoice::forPart(std::vector<ForPart> value)
{
    return AttributesChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

AttributesChoice parseAttributesChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseAttributesChoice(el, cursor, ParseContext{});
}

AttributesChoice parseAttributesChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "transpose")))
    {
        std::vector<Transpose> items;
        while (cursor && (cursorIs(cursor, "transpose")))
        {
            items.push_back(parseTranspose(cursor, context));
            cursor = nextElement(cursor);
        }
        return AttributesChoice::transpose(std::move(items));
    }
    if (cursor && (cursorIs(cursor, "for-part")))
    {
        std::vector<ForPart> items;
        while (cursor && (cursorIs(cursor, "for-part")))
        {
            items.push_back(parseForPart(cursor, context));
            cursor = nextElement(cursor);
        }
        return AttributesChoice::forPart(std::move(items));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "transpose");
}

void serializeAttributesChoice(const AttributesChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case AttributesChoice::Kind::transpose:
        for (const auto &item : v.asTranspose())
        {
            serializeTranspose(item, el, "transpose");
        }
        break;
    case AttributesChoice::Kind::forPart:
        for (const auto &item : v.asForPart())
        {
            serializeForPart(item, el, "for-part");
        }
        break;
    }
}

} // namespace mx::core
