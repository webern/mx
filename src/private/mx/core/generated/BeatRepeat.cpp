// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/BeatRepeat.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &BeatRepeat::type() const noexcept
{
    return m_type;
}

void BeatRepeat::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<int> &BeatRepeat::slashes() const noexcept
{
    return m_slashes;
}

void BeatRepeat::setSlashes(std::optional<int> value)
{
    m_slashes = std::move(value);
}

const std::optional<YesNo> &BeatRepeat::useDots() const noexcept
{
    return m_useDots;
}

void BeatRepeat::setUseDots(std::optional<YesNo> value)
{
    m_useDots = std::move(value);
}

const std::optional<SlashGroup> &BeatRepeat::slash() const noexcept
{
    return m_slash;
}

void BeatRepeat::setSlash(std::optional<SlashGroup> value)
{
    m_slash = std::move(value);
}

BeatRepeat parseBeatRepeat(pugi::xml_node el)
{
    return parseBeatRepeat(el, ParseContext{});
}

BeatRepeat parseBeatRepeat(pugi::xml_node el, const ParseContext &context)
{
    BeatRepeat out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStop>(a.value(), context, el, "type"));
        }
        else if (aname == "slashes")
        {
            out.setSlashes(parseIntegerValue(a.value(), context, el, "slashes"));
        }
        else if (aname == "use-dots")
        {
            out.setUseDots(parseValue<YesNo>(a.value(), context, el, "use-dots"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseBeatRepeatContent(out, el, context);
    return out;
}

void parseBeatRepeatContent(BeatRepeat &out, pugi::xml_node el)
{
    parseBeatRepeatContent(out, el, ParseContext{});
}

void parseBeatRepeatContent(BeatRepeat &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "slash-type") || cursorIs(cursor, "except-voice")))
    {
        out.setSlash(parseSlashGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "slash-type") || cursorIs(cursor, "slash-dot") || cursorIs(cursor, "except-voice"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeBeatRepeat(const BeatRepeat &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.slashes())
    {
        setAttribute(el, "slashes", formatInt(*v.slashes()));
    }
    if (v.useDots())
    {
        setAttribute(el, "use-dots", v.useDots()->toString());
    }
    serializeBeatRepeatContent(v, el);
}

void serializeBeatRepeatContent(const BeatRepeat &v, pugi::xml_node el)
{
    if (v.slash())
    {
        serializeSlashGroup(*v.slash(), el);
    }
}

} // namespace mx::core
