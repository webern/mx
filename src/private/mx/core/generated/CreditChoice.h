// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/CreditChoiceGroup.h"
#include "mx/core/generated/Image.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class CreditChoice final
{
  public:
    enum class Kind
    {
        creditImage,
        group,
    };

    /// The natural zero: the first alternative.
    CreditChoice();

    static CreditChoice creditImage(Image value);
    static CreditChoice group(CreditChoiceGroup value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isCreditImage() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isCreditImage().
    const Image &asCreditImage() const noexcept
    {
        assert(isCreditImage());
        return *std::get_if<0>(&m_value);
    }

    bool isGroup() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isGroup().
    const CreditChoiceGroup &asGroup() const noexcept
    {
        assert(isGroup());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const CreditChoice &other) const = default;

  private:
    using Storage = std::variant<Image, CreditChoiceGroup>;

    explicit CreditChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
CreditChoice parseCreditChoice(pugi::xml_node el, pugi::xml_node &cursor);

CreditChoice parseCreditChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeCreditChoice(const CreditChoice &v, pugi::xml_node el);

} // namespace mx::core
