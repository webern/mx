// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Assess.h"
#include "mx/core/generated/OtherListening.h"
#include "mx/core/generated/Wait.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class ListenChoice final
{
  public:
    enum class Kind
    {
        assess,
        wait,
        otherListen,
    };

    /// The natural zero: the first alternative.
    ListenChoice();

    static ListenChoice assess(Assess value);
    static ListenChoice wait(Wait value);
    static ListenChoice otherListen(OtherListening value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isAssess() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isAssess().
    const Assess &asAssess() const noexcept
    {
        assert(isAssess());
        return *std::get_if<0>(&m_value);
    }

    bool isWait() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isWait().
    const Wait &asWait() const noexcept
    {
        assert(isWait());
        return *std::get_if<1>(&m_value);
    }

    bool isOtherListen() const noexcept
    {
        return m_value.index() == 2;
    }

    /// Precondition: isOtherListen().
    const OtherListening &asOtherListen() const noexcept
    {
        assert(isOtherListen());
        return *std::get_if<2>(&m_value);
    }

    bool operator==(const ListenChoice &other) const = default;

  private:
    using Storage = std::variant<Assess, Wait, OtherListening>;

    explicit ListenChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
ListenChoice parseListenChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeListenChoice(const ListenChoice &v, pugi::xml_node el);

} // namespace mx::core
