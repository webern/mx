// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Credit.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<int> &Credit::page() const noexcept
{
    return m_page;
}

void Credit::setPage(std::optional<int> value)
{
    m_page = std::move(value);
}

const std::optional<Token> &Credit::id() const noexcept
{
    return m_id;
}

void Credit::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const std::string> Credit::creditType() const noexcept
{
    return m_creditType;
}

void Credit::addCreditType(std::string value)
{
    m_creditType.push_back(std::move(value));
}

void Credit::setCreditType(std::vector<std::string> value)
{
    m_creditType = std::move(value);
}

std::span<const Link> Credit::link() const noexcept
{
    return m_link;
}

void Credit::addLink(Link value)
{
    m_link.push_back(std::move(value));
}

void Credit::setLink(std::vector<Link> value)
{
    m_link = std::move(value);
}

std::span<const Bookmark> Credit::bookmark() const noexcept
{
    return m_bookmark;
}

void Credit::addBookmark(Bookmark value)
{
    m_bookmark.push_back(std::move(value));
}

void Credit::setBookmark(std::vector<Bookmark> value)
{
    m_bookmark = std::move(value);
}

const CreditChoice &Credit::choice() const noexcept
{
    return m_choice;
}

void Credit::setChoice(CreditChoice value)
{
    m_choice = std::move(value);
}

Credit parseCredit(pugi::xml_node el, const ParseContext &context)
{
    Credit out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "page")
        {
            out.setPage(parseIntegerValue(a.value(), context, el, "page"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseCreditContent(out, el, context);
    return out;
}

void parseCreditContent(Credit &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursorIs(cursor, "credit-type"))
    {
        out.addCreditType(std::string{childText(cursor)});
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "link"))
    {
        out.addLink(parseLink(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "bookmark"))
    {
        out.addBookmark(parseBookmark(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor &&
        (cursorIs(cursor, "credit-image") || cursorIs(cursor, "credit-words") || cursorIs(cursor, "credit-symbol")))
    {
        out.setChoice(parseCreditChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "credit-image");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "credit-type") || cursorIs(cursor, "link") || cursorIs(cursor, "bookmark") ||
            cursorIs(cursor, "credit-image") || cursorIs(cursor, "credit-words") || cursorIs(cursor, "credit-symbol"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeCredit(const Credit &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.page())
    {
        setAttribute(el, "page", formatInt(*v.page()));
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeCreditContent(v, el);
}

void serializeCreditContent(const Credit &v, pugi::xml_node el)
{
    for (const auto &item : v.creditType())
    {
        writeTextElement(el, "credit-type", item);
    }
    for (const auto &item : v.link())
    {
        serializeLink(item, el, "link");
    }
    for (const auto &item : v.bookmark())
    {
        serializeBookmark(item, el, "bookmark");
    }
    serializeCreditChoice(v.choice(), el);
}

} // namespace mx::core
