// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TransposeGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<int> &TransposeGroup::diatonic() const noexcept
{
    return m_diatonic;
}

void TransposeGroup::setDiatonic(std::optional<int> value)
{
    m_diatonic = std::move(value);
}

const Semitones &TransposeGroup::chromatic() const noexcept
{
    return m_chromatic;
}

void TransposeGroup::setChromatic(Semitones value)
{
    m_chromatic = std::move(value);
}

const std::optional<int> &TransposeGroup::octaveChange() const noexcept
{
    return m_octaveChange;
}

void TransposeGroup::setOctaveChange(std::optional<int> value)
{
    m_octaveChange = std::move(value);
}

const std::optional<Double> &TransposeGroup::double_() const noexcept
{
    return m_double_;
}

void TransposeGroup::setDouble(std::optional<Double> value)
{
    m_double_ = std::move(value);
}

TransposeGroup parseTransposeGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseTransposeGroup(el, cursor, ParseContext{});
}

TransposeGroup parseTransposeGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    TransposeGroup out;
    if (cursorIs(cursor, "diatonic"))
    {
        out.setDiatonic(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "chromatic"))
    {
        out.setChromatic(parseValue<Semitones>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "chromatic");
    }
    if (cursorIs(cursor, "octave-change"))
    {
        out.setOctaveChange(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "double"))
    {
        out.setDouble(parseDouble(cursor, context));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeTransposeGroup(const TransposeGroup &v, pugi::xml_node el)
{
    if (v.diatonic())
    {
        writeTextElement(el, "diatonic", formatInt(*v.diatonic()));
    }
    writeTextElement(el, "chromatic", v.chromatic().toString());
    if (v.octaveChange())
    {
        writeTextElement(el, "octave-change", formatInt(*v.octaveChange()));
    }
    if (v.double_())
    {
        serializeDouble(*v.double_(), el, "double");
    }
}

} // namespace mx::core
