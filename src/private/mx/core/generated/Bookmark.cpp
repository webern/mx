// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Bookmark.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &Bookmark::id() const noexcept
{
    return m_id;
}

void Bookmark::setID(Token value)
{
    m_id = std::move(value);
}

const std::optional<std::string> &Bookmark::name() const noexcept
{
    return m_name;
}

void Bookmark::setName(std::optional<std::string> value)
{
    m_name = std::move(value);
}

const std::optional<NameToken> &Bookmark::element() const noexcept
{
    return m_element;
}

void Bookmark::setElement(std::optional<NameToken> value)
{
    m_element = std::move(value);
}

const std::optional<int> &Bookmark::position() const noexcept
{
    return m_position;
}

void Bookmark::setPosition(std::optional<int> value)
{
    m_position = std::move(value);
}

Bookmark parseBookmark(pugi::xml_node el, const ParseContext &context)
{
    Bookmark out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else if (aname == "name")
        {
            out.setName(std::string{a.value()});
        }
        else if (aname == "element")
        {
            out.setElement(parseValue<NameToken>(a.value(), context, el, "element"));
        }
        else if (aname == "position")
        {
            out.setPosition(parseIntegerValue(a.value(), context, el, "position"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseBookmarkContent(out, el, context);
    return out;
}

void parseBookmarkContent(Bookmark &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeBookmark(const Bookmark &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    if (v.name())
    {
        setAttribute(el, "name", *v.name());
    }
    if (v.element())
    {
        setAttribute(el, "element", v.element()->toString());
    }
    if (v.position())
    {
        setAttribute(el, "position", formatInt(*v.position()));
    }
    serializeBookmarkContent(v, el);
}

void serializeBookmarkContent(const Bookmark &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
