// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Time.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &Time::number() const noexcept
{
    return m_number;
}

void Time::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<TimeSymbol> &Time::symbol() const noexcept
{
    return m_symbol;
}

void Time::setSymbol(std::optional<TimeSymbol> value)
{
    m_symbol = std::move(value);
}

const std::optional<TimeSeparator> &Time::separator() const noexcept
{
    return m_separator;
}

void Time::setSeparator(std::optional<TimeSeparator> value)
{
    m_separator = std::move(value);
}

const std::optional<Tenths> &Time::defaultX() const noexcept
{
    return m_defaultX;
}

void Time::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Time::defaultY() const noexcept
{
    return m_defaultY;
}

void Time::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Time::relativeX() const noexcept
{
    return m_relativeX;
}

void Time::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Time::relativeY() const noexcept
{
    return m_relativeY;
}

void Time::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Time::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Time::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Time::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Time::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Time::fontSize() const noexcept
{
    return m_fontSize;
}

void Time::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Time::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Time::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Time::color() const noexcept
{
    return m_color;
}

void Time::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Time::halign() const noexcept
{
    return m_halign;
}

void Time::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &Time::valign() const noexcept
{
    return m_valign;
}

void Time::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const std::optional<YesNo> &Time::printObject() const noexcept
{
    return m_printObject;
}

void Time::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Token> &Time::id() const noexcept
{
    return m_id;
}

void Time::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const TimeChoice &Time::choice() const noexcept
{
    return m_choice;
}

void Time::setChoice(TimeChoice value)
{
    m_choice = std::move(value);
}

Time parseTime(pugi::xml_node el, const ParseContext &context)
{
    Time out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "symbol")
        {
            out.setSymbol(parseValue<TimeSymbol>(a.value(), context, el, "symbol"));
        }
        else if (aname == "separator")
        {
            out.setSeparator(parseValue<TimeSeparator>(a.value(), context, el, "separator"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "halign")
        {
            out.setHalign(parseValue<LeftCenterRight>(a.value(), context, el, "halign"));
        }
        else if (aname == "valign")
        {
            out.setValign(parseValue<Valign>(a.value(), context, el, "valign"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseTimeContent(out, el, context);
    return out;
}

void parseTimeContent(Time &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "beats") || cursorIs(cursor, "senza-misura")))
    {
        out.setChoice(parseTimeChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "beats");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "beats") || cursorIs(cursor, "beat-type") || cursorIs(cursor, "interchangeable") ||
            cursorIs(cursor, "senza-misura"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTime(const Time &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.symbol())
    {
        setAttribute(el, "symbol", v.symbol()->toString());
    }
    if (v.separator())
    {
        setAttribute(el, "separator", v.separator()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeTimeContent(v, el);
}

void serializeTimeContent(const Time &v, pugi::xml_node el)
{
    serializeTimeChoice(v.choice(), el);
}

} // namespace mx::core
