// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TraditionalKeyGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<Cancel> &TraditionalKeyGroup::cancel() const noexcept
{
    return m_cancel;
}

void TraditionalKeyGroup::setCancel(std::optional<Cancel> value)
{
    m_cancel = std::move(value);
}

const Fifths &TraditionalKeyGroup::fifths() const noexcept
{
    return m_fifths;
}

void TraditionalKeyGroup::setFifths(Fifths value)
{
    m_fifths = std::move(value);
}

const std::optional<Mode> &TraditionalKeyGroup::mode() const noexcept
{
    return m_mode;
}

void TraditionalKeyGroup::setMode(std::optional<Mode> value)
{
    m_mode = std::move(value);
}

TraditionalKeyGroup parseTraditionalKeyGroup(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseTraditionalKeyGroup(el, cursor, ParseContext{});
}

TraditionalKeyGroup parseTraditionalKeyGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    TraditionalKeyGroup out;
    if (cursorIs(cursor, "cancel"))
    {
        out.setCancel(parseCancel(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "fifths"))
    {
        out.setFifths(parseValue<Fifths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "fifths");
    }
    if (cursorIs(cursor, "mode"))
    {
        out.setMode(parseValue<Mode>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeTraditionalKeyGroup(const TraditionalKeyGroup &v, pugi::xml_node el)
{
    if (v.cancel())
    {
        serializeCancel(*v.cancel(), el, "cancel");
    }
    writeTextElement(el, "fifths", v.fifths().toString());
    if (v.mode())
    {
        writeTextElement(el, "mode", v.mode()->toString());
    }
}

} // namespace mx::core
