// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The start-stop-discontinue type is used to specify ending types. Typically, the start type is
/// associated with the left barline of the first measure in an ending. The stop and discontinue
/// types are associated with the right barline of the last measure in an ending. Stop is used when
/// the ending mark concludes with a downward jog, as is typical for first endings. Discontinue is
/// used when there is no downward jog, as is typical for second endings that do not conclude a
/// piece.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StartStopDiscontinue final
{
  public:
    enum class Tag
    {
        start,
        stop,
        discontinue,
    };

    /// The natural zero: the first variant.
    StartStopDiscontinue() noexcept = default;

    static StartStopDiscontinue start() noexcept;
    static StartStopDiscontinue stop() noexcept;
    static StartStopDiscontinue discontinue() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StartStopDiscontinue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StartStopDiscontinue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StartStopDiscontinue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StartStopDiscontinue &other) const noexcept = default;

  private:
    explicit StartStopDiscontinue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::start};
};

} // namespace mx::core
