// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/AccidentalText.h"
#include "mx/core/generated/FormattedText.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class NameDisplayChoice final
{
  public:
    enum class Kind
    {
        displayText,
        accidentalText,
    };

    /// The natural zero: the first alternative.
    NameDisplayChoice();

    static NameDisplayChoice displayText(FormattedText value);
    static NameDisplayChoice accidentalText(AccidentalText value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isDisplayText() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isDisplayText().
    const FormattedText &asDisplayText() const noexcept
    {
        assert(isDisplayText());
        return *std::get_if<0>(&m_value);
    }

    bool isAccidentalText() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isAccidentalText().
    const AccidentalText &asAccidentalText() const noexcept
    {
        assert(isAccidentalText());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const NameDisplayChoice &other) const = default;

  private:
    using Storage = std::variant<FormattedText, AccidentalText>;

    explicit NameDisplayChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
NameDisplayChoice parseNameDisplayChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeNameDisplayChoice(const NameDisplayChoice &v, pugi::xml_node el);

} // namespace mx::core
