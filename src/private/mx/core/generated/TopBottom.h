// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The top-bottom type is used to indicate the top or bottom part of a vertical shape like
/// non-arpeggiate.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TopBottom final
{
  public:
    enum class Tag
    {
        top,
        bottom,
    };

    /// The natural zero: the first variant.
    TopBottom() noexcept = default;

    static TopBottom top() noexcept;
    static TopBottom bottom() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TopBottom &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TopBottom parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TopBottom parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TopBottom &other) const noexcept = default;

  private:
    explicit TopBottom(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::top};
};

} // namespace mx::core
