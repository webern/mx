// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/SmuflCodaGlyphName.h"

#include <cstddef>
#include <iterator>
#include <utility>

namespace mx::core
{

constexpr std::string_view kSmuflCodaGlyphNamePrefix[] = {
    "coda",
};

// The ASCII subset of XML name characters the schema's \c denotes.
bool isNameCharSmuflCodaGlyphName(char c) noexcept
{
    return (c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z') || (c >= '0' && c <= '9') || c == '-' || c == '.' ||
           c == ':' || c == '_';
}

SmuflCodaGlyphName::SmuflCodaGlyphName()
{
    repair();
}

SmuflCodaGlyphName::SmuflCodaGlyphName(std::string suffix) : m_suffix{std::move(suffix)}
{
    repair();
}

void SmuflCodaGlyphName::setSuffix(std::string value)
{
    m_suffix = std::move(value);
    repair();
}

void SmuflCodaGlyphName::repair()
{
    std::string cleaned;
    cleaned.reserve(m_suffix.size());
    for (const char c : m_suffix)
    {
        if (isNameCharSmuflCodaGlyphName(c))
        {
            cleaned.push_back(c);
        }
    }
    m_suffix = std::move(cleaned);
}

std::string SmuflCodaGlyphName::toString() const
{
    std::string out{kSmuflCodaGlyphNamePrefix[0]};
    out += m_suffix;
    return out;
}

bool SmuflCodaGlyphName::tryParse(std::string_view text, SmuflCodaGlyphName &out)
{
    for (std::size_t i = 0; i < std::size(kSmuflCodaGlyphNamePrefix); ++i)
    {
        const std::string_view prefix = kSmuflCodaGlyphNamePrefix[i];
        if (text.size() < prefix.size() || text.substr(0, prefix.size()) != prefix)
        {
            continue;
        }
        const std::string_view suffix = text.substr(prefix.size());
        bool valid = true;
        for (const char c : suffix)
        {
            if (!isNameCharSmuflCodaGlyphName(c))
            {
                valid = false;
                break;
            }
        }
        if (!valid)
        {
            continue;
        }
        out = SmuflCodaGlyphName{std::string{suffix}};
        return true;
    }
    return false;
}

SmuflCodaGlyphName SmuflCodaGlyphName::parse(std::string_view text)
{
    SmuflCodaGlyphName out;
    if (tryParse(text, out))
    {
        return out;
    }
    // Repair: keep what we can of the text as a suffix.
    return SmuflCodaGlyphName{std::string{text}};
}

} // namespace mx::core
