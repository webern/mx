// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Kind.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Kind::useSymbols() const noexcept
{
    return m_useSymbols;
}

void Kind::setUseSymbols(std::optional<YesNo> value)
{
    m_useSymbols = std::move(value);
}

const std::optional<std::string> &Kind::text() const noexcept
{
    return m_text;
}

void Kind::setText(std::optional<std::string> value)
{
    m_text = std::move(value);
}

const std::optional<YesNo> &Kind::stackDegrees() const noexcept
{
    return m_stackDegrees;
}

void Kind::setStackDegrees(std::optional<YesNo> value)
{
    m_stackDegrees = std::move(value);
}

const std::optional<YesNo> &Kind::parenthesesDegrees() const noexcept
{
    return m_parenthesesDegrees;
}

void Kind::setParenthesesDegrees(std::optional<YesNo> value)
{
    m_parenthesesDegrees = std::move(value);
}

const std::optional<YesNo> &Kind::bracketDegrees() const noexcept
{
    return m_bracketDegrees;
}

void Kind::setBracketDegrees(std::optional<YesNo> value)
{
    m_bracketDegrees = std::move(value);
}

const std::optional<Tenths> &Kind::defaultX() const noexcept
{
    return m_defaultX;
}

void Kind::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Kind::defaultY() const noexcept
{
    return m_defaultY;
}

void Kind::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Kind::relativeX() const noexcept
{
    return m_relativeX;
}

void Kind::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Kind::relativeY() const noexcept
{
    return m_relativeY;
}

void Kind::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Kind::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Kind::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Kind::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Kind::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Kind::fontSize() const noexcept
{
    return m_fontSize;
}

void Kind::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Kind::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Kind::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Kind::color() const noexcept
{
    return m_color;
}

void Kind::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<LeftCenterRight> &Kind::halign() const noexcept
{
    return m_halign;
}

void Kind::setHalign(std::optional<LeftCenterRight> value)
{
    m_halign = std::move(value);
}

const std::optional<Valign> &Kind::valign() const noexcept
{
    return m_valign;
}

void Kind::setValign(std::optional<Valign> value)
{
    m_valign = std::move(value);
}

const KindValue &Kind::value() const noexcept
{
    return m_value;
}

void Kind::setValue(KindValue value)
{
    m_value = std::move(value);
}

Kind parseKind(pugi::xml_node el)
{
    return parseKind(el, ParseContext{});
}

Kind parseKind(pugi::xml_node el, const ParseContext &context)
{
    Kind out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "use-symbols")
        {
            out.setUseSymbols(parseValue<YesNo>(a.value(), context, el, "use-symbols"));
        }
        else if (aname == "text")
        {
            out.setText(std::string{a.value()});
        }
        else if (aname == "stack-degrees")
        {
            out.setStackDegrees(parseValue<YesNo>(a.value(), context, el, "stack-degrees"));
        }
        else if (aname == "parentheses-degrees")
        {
            out.setParenthesesDegrees(parseValue<YesNo>(a.value(), context, el, "parentheses-degrees"));
        }
        else if (aname == "bracket-degrees")
        {
            out.setBracketDegrees(parseValue<YesNo>(a.value(), context, el, "bracket-degrees"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "halign")
        {
            out.setHalign(parseValue<LeftCenterRight>(a.value(), context, el, "halign"));
        }
        else if (aname == "valign")
        {
            out.setValign(parseValue<Valign>(a.value(), context, el, "valign"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseKindContent(out, el, context);
    return out;
}

void parseKindContent(Kind &out, pugi::xml_node el)
{
    parseKindContent(out, el, ParseContext{});
}

void parseKindContent(Kind &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<KindValue>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeKind(const Kind &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.useSymbols())
    {
        setAttribute(el, "use-symbols", v.useSymbols()->toString());
    }
    if (v.text())
    {
        setAttribute(el, "text", *v.text());
    }
    if (v.stackDegrees())
    {
        setAttribute(el, "stack-degrees", v.stackDegrees()->toString());
    }
    if (v.parenthesesDegrees())
    {
        setAttribute(el, "parentheses-degrees", v.parenthesesDegrees()->toString());
    }
    if (v.bracketDegrees())
    {
        setAttribute(el, "bracket-degrees", v.bracketDegrees()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.halign())
    {
        setAttribute(el, "halign", v.halign()->toString());
    }
    if (v.valign())
    {
        setAttribute(el, "valign", v.valign()->toString());
    }
    serializeKindContent(v, el);
}

void serializeKindContent(const Kind &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
