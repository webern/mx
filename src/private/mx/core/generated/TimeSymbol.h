// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The time-symbol type indicates how to display a time signature. The normal value is the usual
/// fractional display, and is the implied symbol type if none is specified. Other options are the
/// common and cut time symbols, as well as a single number with an implied denominator. The note
/// symbol indicates that the beat-type should be represented with the corresponding downstem note
/// rather than a number. The dotted-note symbol indicates that the beat-type should be represented
/// with a dotted downstem note that corresponds to three times the beat-type value, and a numerator
/// that is one third the beats value.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class TimeSymbol final
{
  public:
    enum class Tag
    {
        common,
        cut,
        singleNumber,
        note,
        dottedNote,
        normal,
    };

    /// The natural zero: the first variant.
    TimeSymbol() noexcept = default;

    static TimeSymbol common() noexcept;
    static TimeSymbol cut() noexcept;
    static TimeSymbol singleNumber() noexcept;
    static TimeSymbol note() noexcept;
    static TimeSymbol dottedNote() noexcept;
    static TimeSymbol normal() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, TimeSymbol &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static TimeSymbol parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static TimeSymbol parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const TimeSymbol &other) const noexcept = default;

  private:
    explicit TimeSymbol(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::common};
};

} // namespace mx::core
