// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TimeModification.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const int &TimeModification::actualNotes() const noexcept
{
    return m_actualNotes;
}

void TimeModification::setActualNotes(int value)
{
    m_actualNotes = std::move(value);
}

const int &TimeModification::normalNotes() const noexcept
{
    return m_normalNotes;
}

void TimeModification::setNormalNotes(int value)
{
    m_normalNotes = std::move(value);
}

const std::optional<TimeModificationGroup> &TimeModification::group() const noexcept
{
    return m_group;
}

void TimeModification::setGroup(std::optional<TimeModificationGroup> value)
{
    m_group = std::move(value);
}

TimeModification parseTimeModification(pugi::xml_node el)
{
    return parseTimeModification(el, ParseContext{});
}

TimeModification parseTimeModification(pugi::xml_node el, const ParseContext &context)
{
    TimeModification out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseTimeModificationContent(out, el, context);
    return out;
}

void parseTimeModificationContent(TimeModification &out, pugi::xml_node el)
{
    parseTimeModificationContent(out, el, ParseContext{});
}

void parseTimeModificationContent(TimeModification &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "actual-notes"))
    {
        out.setActualNotes(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "actual-notes");
    }
    if (cursorIs(cursor, "normal-notes"))
    {
        out.setNormalNotes(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "normal-notes");
    }
    if (cursor && (cursorIs(cursor, "normal-type")))
    {
        out.setGroup(parseTimeModificationGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "actual-notes") || cursorIs(cursor, "normal-notes") || cursorIs(cursor, "normal-type") ||
            cursorIs(cursor, "normal-dot"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTimeModification(const TimeModification &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeTimeModificationContent(v, el);
}

void serializeTimeModificationContent(const TimeModification &v, pugi::xml_node el)
{
    writeTextElement(el, "actual-notes", formatInt(v.actualNotes()));
    writeTextElement(el, "normal-notes", formatInt(v.normalNotes()));
    if (v.group())
    {
        serializeTimeModificationGroup(*v.group(), el);
    }
}

} // namespace mx::core
