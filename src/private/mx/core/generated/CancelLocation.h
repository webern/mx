// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The cancel-location type is used to indicate where a key signature cancellation appears relative
/// to a new key signature: to the left, to the right, or before the barline and to the left. It is
/// left by default. For mid-measure key elements, a cancel-location of before-barline should be
/// treated like a cancel-location of left.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class CancelLocation final
{
  public:
    enum class Tag
    {
        left,
        right,
        beforeBarline,
    };

    /// The natural zero: the first variant.
    CancelLocation() noexcept = default;

    static CancelLocation left() noexcept;
    static CancelLocation right() noexcept;
    static CancelLocation beforeBarline() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, CancelLocation &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static CancelLocation parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static CancelLocation parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const CancelLocation &other) const noexcept = default;

  private:
    explicit CancelLocation(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::left};
};

} // namespace mx::core
