// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PageMargins.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<MarginType> &PageMargins::type() const noexcept
{
    return m_type;
}

void PageMargins::setType(std::optional<MarginType> value)
{
    m_type = std::move(value);
}

const AllMarginsGroup &PageMargins::allMargins() const noexcept
{
    return m_allMargins;
}

void PageMargins::setAllMargins(AllMarginsGroup value)
{
    m_allMargins = std::move(value);
}

PageMargins parsePageMargins(pugi::xml_node el, const ParseContext &context)
{
    PageMargins out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            out.setType(parseValue<MarginType>(a.value(), context, el, "type"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parsePageMarginsContent(out, el, context);
    return out;
}

void parsePageMarginsContent(PageMargins &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "left-margin")))
    {
        out.setAllMargins(parseAllMarginsGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "left-margin");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "left-margin") || cursorIs(cursor, "right-margin") || cursorIs(cursor, "top-margin") ||
            cursorIs(cursor, "bottom-margin"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePageMargins(const PageMargins &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.type())
    {
        setAttribute(el, "type", v.type()->toString());
    }
    serializePageMarginsContent(v, el);
}

void serializePageMarginsContent(const PageMargins &v, pugi::xml_node el)
{
    serializeAllMarginsGroup(v.allMargins(), el);
}

} // namespace mx::core
