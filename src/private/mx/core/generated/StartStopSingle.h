// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The start-stop-single type is used for an attribute of musical elements that can be used for
/// either multi-note or single-note musical elements, as for groupings. When multiple elements with
/// the same tag are used within the same note, their order within the MusicXML document should match
/// the musical score order.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StartStopSingle final
{
  public:
    enum class Tag
    {
        start,
        stop,
        single,
    };

    /// The natural zero: the first variant.
    StartStopSingle() noexcept = default;

    static StartStopSingle start() noexcept;
    static StartStopSingle stop() noexcept;
    static StartStopSingle single() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StartStopSingle &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StartStopSingle parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StartStopSingle parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StartStopSingle &other) const noexcept = default;

  private:
    explicit StartStopSingle(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::start};
};

} // namespace mx::core
