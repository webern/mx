// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Instrument.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &Instrument::id() const noexcept
{
    return m_id;
}

void Instrument::setID(Token value)
{
    m_id = std::move(value);
}

Instrument parseInstrument(pugi::xml_node el)
{
    return parseInstrument(el, ParseContext{});
}

Instrument parseInstrument(pugi::xml_node el, const ParseContext &context)
{
    Instrument out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseInstrumentContent(out, el, context);
    return out;
}

void parseInstrumentContent(Instrument &out, pugi::xml_node el)
{
    parseInstrumentContent(out, el, ParseContext{});
}

void parseInstrumentContent(Instrument &out, pugi::xml_node el, const ParseContext &context)
{
    (void)out;
    (void)context;
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeInstrument(const Instrument &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializeInstrumentContent(v, el);
}

void serializeInstrumentContent(const Instrument &v, pugi::xml_node el)
{
    (void)v;
    (void)el;
}

} // namespace mx::core
