// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Millimeters.h"
#include "mx/core/generated/Tenths.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// Margins, page sizes, and distances are all measured in tenths to keep MusicXML data in a
/// consistent coordinate system as much as possible. The translation to absolute units is done with
/// the scaling type, which specifies how many millimeters are equal to how many tenths. For a staff
/// height of 7 mm, millimeters would be set to 7 while tenths is set to 40. The ability to set a
/// formula rather than a single scaling factor helps avoid roundoff errors.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Scaling final
{
  public:
    const Millimeters &millimeters() const noexcept;
    void setMillimeters(Millimeters value);
    const Tenths &tenths() const noexcept;
    void setTenths(Tenths value);
    bool operator==(const Scaling &other) const = default;

  private:
    Millimeters m_millimeters{};
    Tenths m_tenths{};
};

Scaling parseScaling(pugi::xml_node el);

Scaling parseScaling(pugi::xml_node el, const ParseContext &context);

void parseScalingContent(Scaling &out, pugi::xml_node el);

void parseScalingContent(Scaling &out, pugi::xml_node el, const ParseContext &context);

void serializeScaling(const Scaling &v, pugi::xml_node parent, const char *tag);

void serializeScalingContent(const Scaling &v, pugi::xml_node el);

} // namespace mx::core
