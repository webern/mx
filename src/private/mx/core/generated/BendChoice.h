// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"
#include "mx/core/generated/Release.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class BendChoice final
{
  public:
    enum class Kind
    {
        preBend,
        release,
    };

    /// The natural zero: the first alternative.
    BendChoice();

    static BendChoice preBend(Empty value);
    static BendChoice release(Release value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isPreBend() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isPreBend().
    const Empty &asPreBend() const noexcept
    {
        assert(isPreBend());
        return *std::get_if<0>(&m_value);
    }

    bool isRelease() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isRelease().
    const Release &asRelease() const noexcept
    {
        assert(isRelease());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const BendChoice &other) const = default;

  private:
    using Storage = std::variant<Empty, Release>;

    explicit BendChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
BendChoice parseBendChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeBendChoice(const BendChoice &v, pugi::xml_node el);

} // namespace mx::core
