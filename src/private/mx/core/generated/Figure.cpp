// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Figure.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StyleText> &Figure::prefix() const noexcept
{
    return m_prefix;
}

void Figure::setPrefix(std::optional<StyleText> value)
{
    m_prefix = std::move(value);
}

const std::optional<StyleText> &Figure::figureNumber() const noexcept
{
    return m_figureNumber;
}

void Figure::setFigureNumber(std::optional<StyleText> value)
{
    m_figureNumber = std::move(value);
}

const std::optional<StyleText> &Figure::suffix() const noexcept
{
    return m_suffix;
}

void Figure::setSuffix(std::optional<StyleText> value)
{
    m_suffix = std::move(value);
}

const std::optional<Extend> &Figure::extend() const noexcept
{
    return m_extend;
}

void Figure::setExtend(std::optional<Extend> value)
{
    m_extend = std::move(value);
}

const EditorialGroup &Figure::editorial() const noexcept
{
    return m_editorial;
}

void Figure::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

Figure parseFigure(pugi::xml_node el, const ParseContext &context)
{
    Figure out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseFigureContent(out, el, context);
    return out;
}

void parseFigureContent(Figure &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "prefix"))
    {
        out.setPrefix(parseStyleText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "figure-number"))
    {
        out.setFigureNumber(parseStyleText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "suffix"))
    {
        out.setSuffix(parseStyleText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "extend"))
    {
        out.setExtend(parseExtend(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "prefix") || cursorIs(cursor, "figure-number") || cursorIs(cursor, "suffix") ||
            cursorIs(cursor, "extend") || cursorIs(cursor, "footnote") || cursorIs(cursor, "level"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeFigure(const Figure &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeFigureContent(v, el);
}

void serializeFigureContent(const Figure &v, pugi::xml_node el)
{
    if (v.prefix())
    {
        serializeStyleText(*v.prefix(), el, "prefix");
    }
    if (v.figureNumber())
    {
        serializeStyleText(*v.figureNumber(), el, "figure-number");
    }
    if (v.suffix())
    {
        serializeStyleText(*v.suffix(), el, "suffix");
    }
    if (v.extend())
    {
        serializeExtend(*v.extend(), el, "extend");
    }
    serializeEditorialGroup(v.editorial(), el);
}

} // namespace mx::core
