// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/MeasureStyle.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &MeasureStyle::number() const noexcept
{
    return m_number;
}

void MeasureStyle::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<FontFamily> &MeasureStyle::fontFamily() const noexcept
{
    return m_fontFamily;
}

void MeasureStyle::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &MeasureStyle::fontStyle() const noexcept
{
    return m_fontStyle;
}

void MeasureStyle::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &MeasureStyle::fontSize() const noexcept
{
    return m_fontSize;
}

void MeasureStyle::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &MeasureStyle::fontWeight() const noexcept
{
    return m_fontWeight;
}

void MeasureStyle::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &MeasureStyle::color() const noexcept
{
    return m_color;
}

void MeasureStyle::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<Token> &MeasureStyle::id() const noexcept
{
    return m_id;
}

void MeasureStyle::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const MeasureStyleChoice &MeasureStyle::choice() const noexcept
{
    return m_choice;
}

void MeasureStyle::setChoice(MeasureStyleChoice value)
{
    m_choice = std::move(value);
}

MeasureStyle parseMeasureStyle(pugi::xml_node el, const ParseContext &context)
{
    MeasureStyle out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseMeasureStyleContent(out, el, context);
    return out;
}

void parseMeasureStyleContent(MeasureStyle &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "multiple-rest") || cursorIs(cursor, "measure-repeat") ||
                   cursorIs(cursor, "beat-repeat") || cursorIs(cursor, "slash")))
    {
        out.setChoice(parseMeasureStyleChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "multiple-rest");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "multiple-rest") || cursorIs(cursor, "measure-repeat") ||
            cursorIs(cursor, "beat-repeat") || cursorIs(cursor, "slash"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeMeasureStyle(const MeasureStyle &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeMeasureStyleContent(v, el);
}

void serializeMeasureStyleContent(const MeasureStyle &v, pugi::xml_node el)
{
    serializeMeasureStyleChoice(v.choice(), el);
}

} // namespace mx::core
