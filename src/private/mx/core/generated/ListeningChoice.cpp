// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ListeningChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

ListeningChoice::ListeningChoice()
{
    *this = ListeningChoice::sync(Sync{});
}

ListeningChoice ListeningChoice::sync(Sync value)
{
    return ListeningChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

ListeningChoice ListeningChoice::otherListening(OtherListening value)
{
    return ListeningChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

ListeningChoice parseListeningChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "sync")))
    {
        Sync value = parseSync(cursor, context);
        cursor = nextElement(cursor);
        return ListeningChoice::sync(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-listening")))
    {
        OtherListening value = parseOtherListening(cursor, context);
        cursor = nextElement(cursor);
        return ListeningChoice::otherListening(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "sync");
}

void serializeListeningChoice(const ListeningChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case ListeningChoice::Kind::sync:
        serializeSync(v.asSync(), el, "sync");
        break;
    case ListeningChoice::Kind::otherListening:
        serializeOtherListening(v.asOtherListening(), el, "other-listening");
        break;
    }
}

} // namespace mx::core
