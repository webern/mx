// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/SystemLayout.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<SystemMargins> &SystemLayout::systemMargins() const noexcept
{
    return m_systemMargins;
}

void SystemLayout::setSystemMargins(std::optional<SystemMargins> value)
{
    m_systemMargins = std::move(value);
}

const std::optional<Tenths> &SystemLayout::systemDistance() const noexcept
{
    return m_systemDistance;
}

void SystemLayout::setSystemDistance(std::optional<Tenths> value)
{
    m_systemDistance = std::move(value);
}

const std::optional<Tenths> &SystemLayout::topSystemDistance() const noexcept
{
    return m_topSystemDistance;
}

void SystemLayout::setTopSystemDistance(std::optional<Tenths> value)
{
    m_topSystemDistance = std::move(value);
}

const std::optional<SystemDividers> &SystemLayout::systemDividers() const noexcept
{
    return m_systemDividers;
}

void SystemLayout::setSystemDividers(std::optional<SystemDividers> value)
{
    m_systemDividers = std::move(value);
}

SystemLayout parseSystemLayout(pugi::xml_node el, const ParseContext &context)
{
    SystemLayout out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseSystemLayoutContent(out, el, context);
    return out;
}

void parseSystemLayoutContent(SystemLayout &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "system-margins"))
    {
        out.setSystemMargins(parseSystemMargins(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "system-distance"))
    {
        out.setSystemDistance(parseValue<Tenths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "top-system-distance"))
    {
        out.setTopSystemDistance(parseValue<Tenths>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "system-dividers"))
    {
        out.setSystemDividers(parseSystemDividers(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "system-margins") || cursorIs(cursor, "system-distance") ||
            cursorIs(cursor, "top-system-distance") || cursorIs(cursor, "system-dividers"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeSystemLayout(const SystemLayout &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeSystemLayoutContent(v, el);
}

void serializeSystemLayoutContent(const SystemLayout &v, pugi::xml_node el)
{
    if (v.systemMargins())
    {
        serializeSystemMargins(*v.systemMargins(), el, "system-margins");
    }
    if (v.systemDistance())
    {
        writeTextElement(el, "system-distance", v.systemDistance()->toString());
    }
    if (v.topSystemDistance())
    {
        writeTextElement(el, "top-system-distance", v.topSystemDistance()->toString());
    }
    if (v.systemDividers())
    {
        serializeSystemDividers(*v.systemDividers(), el, "system-dividers");
    }
}

} // namespace mx::core
