// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/NameToken.h"
#include "mx/core/Token.h"
#include "mx/core/generated/VirtualInstrumentDataGroup.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The score-instrument type represents a single instrument within a score-part. As with the
/// score-part type, each score-instrument has a required ID attribute, a name, and an optional
/// abbreviation. A score-instrument type is also required if the score specifies MIDI 1.0 channels,
/// banks, or programs. An initial midi-instrument assignment can also be made here. MusicXML
/// software should be able to automatically assign reasonable channels and instruments without these
/// elements in simple cases, such as where part names match General MIDI instrument names. The
/// score-instrument element can also distinguish multiple instruments of the same type that are on
/// the same part, such as Clarinet 1 and Clarinet 2 instruments within a Clarinets 1 and 2 part.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class ScoreInstrument final
{
  public:
    const Token &id() const noexcept;
    void setID(Token value);
    const std::string &instrumentName() const noexcept;
    void setInstrumentName(std::string value);
    const std::optional<std::string> &instrumentAbbreviation() const noexcept;
    void setInstrumentAbbreviation(std::optional<std::string> value);
    const VirtualInstrumentDataGroup &virtualInstrumentData() const noexcept;
    void setVirtualInstrumentData(VirtualInstrumentDataGroup value);
    bool operator==(const ScoreInstrument &other) const = default;

  private:
    Token m_id{};
    std::string m_instrumentName{};
    std::optional<std::string> m_instrumentAbbreviation;
    VirtualInstrumentDataGroup m_virtualInstrumentData{};
};

ScoreInstrument parseScoreInstrument(pugi::xml_node el);

ScoreInstrument parseScoreInstrument(pugi::xml_node el, const ParseContext &context);

void parseScoreInstrumentContent(ScoreInstrument &out, pugi::xml_node el);

void parseScoreInstrumentContent(ScoreInstrument &out, pugi::xml_node el, const ParseContext &context);

void serializeScoreInstrument(const ScoreInstrument &v, pugi::xml_node parent, const char *tag);

void serializeScoreInstrumentContent(const ScoreInstrument &v, pugi::xml_node el);

} // namespace mx::core
