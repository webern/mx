// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/OtherListening.h"
#include "mx/core/generated/Sync.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class ListeningChoice final
{
  public:
    enum class Kind
    {
        sync,
        otherListening,
    };

    /// The natural zero: the first alternative.
    ListeningChoice();

    static ListeningChoice sync(Sync value);
    static ListeningChoice otherListening(OtherListening value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isSync() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isSync().
    const Sync &asSync() const noexcept
    {
        assert(isSync());
        return *std::get_if<0>(&m_value);
    }

    bool isOtherListening() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isOtherListening().
    const OtherListening &asOtherListening() const noexcept
    {
        assert(isOtherListening());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const ListeningChoice &other) const = default;

  private:
    using Storage = std::variant<Sync, OtherListening>;

    explicit ListeningChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
ListeningChoice parseListeningChoice(pugi::xml_node el, pugi::xml_node &cursor);

ListeningChoice parseListeningChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeListeningChoice(const ListeningChoice &v, pugi::xml_node el);

} // namespace mx::core
