// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/RootStep.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &RootStep::text() const noexcept
{
    return m_text;
}

void RootStep::setText(std::optional<std::string> value)
{
    m_text = std::move(value);
}

const std::optional<Tenths> &RootStep::defaultX() const noexcept
{
    return m_defaultX;
}

void RootStep::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &RootStep::defaultY() const noexcept
{
    return m_defaultY;
}

void RootStep::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &RootStep::relativeX() const noexcept
{
    return m_relativeX;
}

void RootStep::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &RootStep::relativeY() const noexcept
{
    return m_relativeY;
}

void RootStep::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &RootStep::fontFamily() const noexcept
{
    return m_fontFamily;
}

void RootStep::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &RootStep::fontStyle() const noexcept
{
    return m_fontStyle;
}

void RootStep::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &RootStep::fontSize() const noexcept
{
    return m_fontSize;
}

void RootStep::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &RootStep::fontWeight() const noexcept
{
    return m_fontWeight;
}

void RootStep::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &RootStep::color() const noexcept
{
    return m_color;
}

void RootStep::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const Step &RootStep::value() const noexcept
{
    return m_value;
}

void RootStep::setValue(Step value)
{
    m_value = std::move(value);
}

RootStep parseRootStep(pugi::xml_node el)
{
    return parseRootStep(el, ParseContext{});
}

RootStep parseRootStep(pugi::xml_node el, const ParseContext &context)
{
    RootStep out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "text")
        {
            out.setText(std::string{a.value()});
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseRootStepContent(out, el, context);
    return out;
}

void parseRootStepContent(RootStep &out, pugi::xml_node el)
{
    parseRootStepContent(out, el, ParseContext{});
}

void parseRootStepContent(RootStep &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<Step>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeRootStep(const RootStep &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.text())
    {
        setAttribute(el, "text", *v.text());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    serializeRootStepContent(v, el);
}

void serializeRootStepContent(const RootStep &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
