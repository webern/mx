// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Octave.h"
#include "mx/core/generated/Step.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The display-step-octave group contains the sequence of elements used by both the rest and
/// unpitched elements. This group is used to place rests and unpitched elements on the staff without
/// implying that these elements have pitch. Positioning follows the current clef. If percussion clef
/// is used, the display-step and display-octave elements are interpreted as if in treble clef, with
/// a G in octave 4 on line 2.
/// A shared content group: transparent on the wire, its
/// fields serialize directly into the referencing element (plan §2.3).
class DisplayStepOctaveGroup final
{
  public:
    const Step &displayStep() const noexcept;
    void setDisplayStep(Step value);
    const Octave &displayOctave() const noexcept;
    void setDisplayOctave(Octave value);
    bool operator==(const DisplayStepOctaveGroup &other) const = default;

  private:
    Step m_displayStep{};
    Octave m_displayOctave{};
};

/// Consumes matching siblings starting at `cursor` (which advances); `el`
/// is the enclosing element, for error paths.
DisplayStepOctaveGroup parseDisplayStepOctaveGroup(pugi::xml_node el, pugi::xml_node &cursor,
                                                   const ParseContext &context);

void serializeDisplayStepOctaveGroup(const DisplayStepOctaveGroup &v, pugi::xml_node el);

} // namespace mx::core
