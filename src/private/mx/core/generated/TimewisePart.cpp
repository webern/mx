// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/TimewisePart.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Token &TimewisePart::id() const noexcept
{
    return m_id;
}

void TimewisePart::setID(Token value)
{
    m_id = std::move(value);
}

std::span<const MusicDataChoice> TimewisePart::musicData() const noexcept
{
    return m_musicData;
}

void TimewisePart::addMusicData(MusicDataChoice value)
{
    m_musicData.push_back(std::move(value));
}

void TimewisePart::setMusicData(std::vector<MusicDataChoice> value)
{
    m_musicData = std::move(value);
}

TimewisePart parseTimewisePart(pugi::xml_node el)
{
    return parseTimewisePart(el, ParseContext{});
}

TimewisePart parseTimewisePart(pugi::xml_node el, const ParseContext &context)
{
    TimewisePart out;
    bool seen_id = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "id")
        {
            seen_id = true;
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_id)
    {
        throwMissingAttribute(el, "id");
    }
    parseTimewisePartContent(out, el, context);
    return out;
}

void parseTimewisePartContent(TimewisePart &out, pugi::xml_node el)
{
    parseTimewisePartContent(out, el, ParseContext{});
}

void parseTimewisePartContent(TimewisePart &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor && (cursorIs(cursor, "note") || cursorIs(cursor, "backup") || cursorIs(cursor, "forward") ||
                      cursorIs(cursor, "direction") || cursorIs(cursor, "attributes") || cursorIs(cursor, "harmony") ||
                      cursorIs(cursor, "figured-bass") || cursorIs(cursor, "print") || cursorIs(cursor, "sound") ||
                      cursorIs(cursor, "listening") || cursorIs(cursor, "barline") || cursorIs(cursor, "grouping") ||
                      cursorIs(cursor, "link") || cursorIs(cursor, "bookmark")))
    {
        out.addMusicData(parseMusicDataChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "note") || cursorIs(cursor, "backup") || cursorIs(cursor, "forward") ||
            cursorIs(cursor, "direction") || cursorIs(cursor, "attributes") || cursorIs(cursor, "harmony") ||
            cursorIs(cursor, "figured-bass") || cursorIs(cursor, "print") || cursorIs(cursor, "sound") ||
            cursorIs(cursor, "listening") || cursorIs(cursor, "barline") || cursorIs(cursor, "grouping") ||
            cursorIs(cursor, "link") || cursorIs(cursor, "bookmark"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeTimewisePart(const TimewisePart &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "id", v.id().toString());
    serializeTimewisePartContent(v, el);
}

void serializeTimewisePartContent(const TimewisePart &v, pugi::xml_node el)
{
    for (const auto &item : v.musicData())
    {
        serializeMusicDataChoice(item, el);
    }
}

} // namespace mx::core
