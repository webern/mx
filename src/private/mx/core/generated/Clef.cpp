// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Clef.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<StaffNumber> &Clef::number() const noexcept
{
    return m_number;
}

void Clef::setNumber(std::optional<StaffNumber> value)
{
    m_number = std::move(value);
}

const std::optional<YesNo> &Clef::additional() const noexcept
{
    return m_additional;
}

void Clef::setAdditional(std::optional<YesNo> value)
{
    m_additional = std::move(value);
}

const std::optional<SymbolSize> &Clef::size() const noexcept
{
    return m_size;
}

void Clef::setSize(std::optional<SymbolSize> value)
{
    m_size = std::move(value);
}

const std::optional<YesNo> &Clef::afterBarline() const noexcept
{
    return m_afterBarline;
}

void Clef::setAfterBarline(std::optional<YesNo> value)
{
    m_afterBarline = std::move(value);
}

const std::optional<Tenths> &Clef::defaultX() const noexcept
{
    return m_defaultX;
}

void Clef::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Clef::defaultY() const noexcept
{
    return m_defaultY;
}

void Clef::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Clef::relativeX() const noexcept
{
    return m_relativeX;
}

void Clef::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Clef::relativeY() const noexcept
{
    return m_relativeY;
}

void Clef::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Clef::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Clef::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Clef::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Clef::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Clef::fontSize() const noexcept
{
    return m_fontSize;
}

void Clef::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Clef::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Clef::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Clef::color() const noexcept
{
    return m_color;
}

void Clef::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<YesNo> &Clef::printObject() const noexcept
{
    return m_printObject;
}

void Clef::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Token> &Clef::id() const noexcept
{
    return m_id;
}

void Clef::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const ClefGroup &Clef::clef() const noexcept
{
    return m_clef;
}

void Clef::setClef(ClefGroup value)
{
    m_clef = std::move(value);
}

Clef parseClef(pugi::xml_node el)
{
    return parseClef(el, ParseContext{});
}

Clef parseClef(pugi::xml_node el, const ParseContext &context)
{
    Clef out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "number")
        {
            out.setNumber(parseValue<StaffNumber>(a.value(), context, el, "number"));
        }
        else if (aname == "additional")
        {
            out.setAdditional(parseValue<YesNo>(a.value(), context, el, "additional"));
        }
        else if (aname == "size")
        {
            out.setSize(parseValue<SymbolSize>(a.value(), context, el, "size"));
        }
        else if (aname == "after-barline")
        {
            out.setAfterBarline(parseValue<YesNo>(a.value(), context, el, "after-barline"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseClefContent(out, el, context);
    return out;
}

void parseClefContent(Clef &out, pugi::xml_node el)
{
    parseClefContent(out, el, ParseContext{});
}

void parseClefContent(Clef &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "sign")))
    {
        out.setClef(parseClefGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "sign");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "sign") || cursorIs(cursor, "line") || cursorIs(cursor, "clef-octave-change"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeClef(const Clef &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.number())
    {
        setAttribute(el, "number", v.number()->toString());
    }
    if (v.additional())
    {
        setAttribute(el, "additional", v.additional()->toString());
    }
    if (v.size())
    {
        setAttribute(el, "size", v.size()->toString());
    }
    if (v.afterBarline())
    {
        setAttribute(el, "after-barline", v.afterBarline()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeClefContent(v, el);
}

void serializeClefContent(const Clef &v, pugi::xml_node el)
{
    serializeClefGroup(v.clef(), el);
}

} // namespace mx::core
