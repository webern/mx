// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/OrnamentsGroupChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

OrnamentsGroupChoice::OrnamentsGroupChoice()
{
    *this = OrnamentsGroupChoice::trillMark(EmptyTrillSound{});
}

OrnamentsGroupChoice OrnamentsGroupChoice::trillMark(EmptyTrillSound value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::turn(HorizontalTurn value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::delayedTurn(HorizontalTurn value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<2>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::invertedTurn(HorizontalTurn value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<3>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::delayedInvertedTurn(HorizontalTurn value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<4>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::verticalTurn(EmptyTrillSound value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<5>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::invertedVerticalTurn(EmptyTrillSound value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<6>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::shake(EmptyTrillSound value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<7>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::wavyLine(WavyLine value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<8>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::mordent(Mordent value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<9>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::invertedMordent(Mordent value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<10>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::schleifer(EmptyPlacement value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<11>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::tremolo(Tremolo value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<12>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::haydn(EmptyTrillSound value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<13>, std::move(value)}};
}

OrnamentsGroupChoice OrnamentsGroupChoice::otherOrnament(OtherPlacementText value)
{
    return OrnamentsGroupChoice{Storage{std::in_place_index<14>, std::move(value)}};
}

OrnamentsGroupChoice parseOrnamentsGroupChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "trill-mark")))
    {
        EmptyTrillSound value = parseEmptyTrillSound(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::trillMark(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "turn")))
    {
        HorizontalTurn value = parseHorizontalTurn(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::turn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "delayed-turn")))
    {
        HorizontalTurn value = parseHorizontalTurn(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::delayedTurn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "inverted-turn")))
    {
        HorizontalTurn value = parseHorizontalTurn(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::invertedTurn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "delayed-inverted-turn")))
    {
        HorizontalTurn value = parseHorizontalTurn(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::delayedInvertedTurn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "vertical-turn")))
    {
        EmptyTrillSound value = parseEmptyTrillSound(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::verticalTurn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "inverted-vertical-turn")))
    {
        EmptyTrillSound value = parseEmptyTrillSound(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::invertedVerticalTurn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "shake")))
    {
        EmptyTrillSound value = parseEmptyTrillSound(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::shake(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "wavy-line")))
    {
        WavyLine value = parseWavyLine(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::wavyLine(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "mordent")))
    {
        Mordent value = parseMordent(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::mordent(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "inverted-mordent")))
    {
        Mordent value = parseMordent(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::invertedMordent(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "schleifer")))
    {
        EmptyPlacement value = parseEmptyPlacement(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::schleifer(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "tremolo")))
    {
        Tremolo value = parseTremolo(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::tremolo(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "haydn")))
    {
        EmptyTrillSound value = parseEmptyTrillSound(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::haydn(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "other-ornament")))
    {
        OtherPlacementText value = parseOtherPlacementText(cursor, context);
        cursor = nextElement(cursor);
        return OrnamentsGroupChoice::otherOrnament(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "trill-mark");
}

void serializeOrnamentsGroupChoice(const OrnamentsGroupChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case OrnamentsGroupChoice::Kind::trillMark:
        serializeEmptyTrillSound(v.asTrillMark(), el, "trill-mark");
        break;
    case OrnamentsGroupChoice::Kind::turn:
        serializeHorizontalTurn(v.asTurn(), el, "turn");
        break;
    case OrnamentsGroupChoice::Kind::delayedTurn:
        serializeHorizontalTurn(v.asDelayedTurn(), el, "delayed-turn");
        break;
    case OrnamentsGroupChoice::Kind::invertedTurn:
        serializeHorizontalTurn(v.asInvertedTurn(), el, "inverted-turn");
        break;
    case OrnamentsGroupChoice::Kind::delayedInvertedTurn:
        serializeHorizontalTurn(v.asDelayedInvertedTurn(), el, "delayed-inverted-turn");
        break;
    case OrnamentsGroupChoice::Kind::verticalTurn:
        serializeEmptyTrillSound(v.asVerticalTurn(), el, "vertical-turn");
        break;
    case OrnamentsGroupChoice::Kind::invertedVerticalTurn:
        serializeEmptyTrillSound(v.asInvertedVerticalTurn(), el, "inverted-vertical-turn");
        break;
    case OrnamentsGroupChoice::Kind::shake:
        serializeEmptyTrillSound(v.asShake(), el, "shake");
        break;
    case OrnamentsGroupChoice::Kind::wavyLine:
        serializeWavyLine(v.asWavyLine(), el, "wavy-line");
        break;
    case OrnamentsGroupChoice::Kind::mordent:
        serializeMordent(v.asMordent(), el, "mordent");
        break;
    case OrnamentsGroupChoice::Kind::invertedMordent:
        serializeMordent(v.asInvertedMordent(), el, "inverted-mordent");
        break;
    case OrnamentsGroupChoice::Kind::schleifer:
        serializeEmptyPlacement(v.asSchleifer(), el, "schleifer");
        break;
    case OrnamentsGroupChoice::Kind::tremolo:
        serializeTremolo(v.asTremolo(), el, "tremolo");
        break;
    case OrnamentsGroupChoice::Kind::haydn:
        serializeEmptyTrillSound(v.asHaydn(), el, "haydn");
        break;
    case OrnamentsGroupChoice::Kind::otherOrnament:
        serializeOtherPlacementText(v.asOtherOrnament(), el, "other-ornament");
        break;
    }
}

} // namespace mx::core
