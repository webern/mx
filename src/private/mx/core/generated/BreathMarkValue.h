// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The breath-mark-value type represents the symbol used for a breath mark.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class BreathMarkValue final
{
  public:
    enum class Tag
    {
        empty,
        comma,
        tick,
        upbow,
        salzedo,
    };

    /// The natural zero: the first variant.
    BreathMarkValue() noexcept = default;

    static BreathMarkValue empty() noexcept;
    static BreathMarkValue comma() noexcept;
    static BreathMarkValue tick() noexcept;
    static BreathMarkValue upbow() noexcept;
    static BreathMarkValue salzedo() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, BreathMarkValue &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static BreathMarkValue parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static BreathMarkValue parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const BreathMarkValue &other) const noexcept = default;

  private:
    explicit BreathMarkValue(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::empty};
};

} // namespace mx::core
