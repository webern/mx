// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The smufl-pictogram-glyph-name type is used to reference a specific Standard Music Font Layout
/// (SMuFL) percussion pictogram character. The value is a SMuFL canonical glyph name that starts
/// with pict.
/// Structural representation (plan §2.2): the glyph-name prefix is
/// structural and emitted by the serializer; only the suffix is stored.
/// Construction repairs the suffix to name characters
/// (and to non-empty, the placeholder being "X"), so a
/// pattern-invalid name cannot exist.
class SmuflPictogramGlyphName final
{
  public:
    /// The natural zero: the prefix plus the placeholder suffix.
    SmuflPictogramGlyphName();

    explicit SmuflPictogramGlyphName(std::string suffix);

    const std::string &suffix() const noexcept
    {
        return m_suffix;
    }

    void setSuffix(std::string value);

    std::string toString() const;

    /// Strict: a known prefix followed by a pattern-valid suffix.
    static bool tryParse(std::string_view text, SmuflPictogramGlyphName &out);

    /// Lenient: repairs into the nearest valid glyph name.
    static SmuflPictogramGlyphName parse(std::string_view text);

    /// Lenient, and says whether the text had to be repaired.
    static SmuflPictogramGlyphName parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const SmuflPictogramGlyphName &other) const noexcept = default;

  private:
    void repair();

    std::string m_suffix;
};

} // namespace mx::core
