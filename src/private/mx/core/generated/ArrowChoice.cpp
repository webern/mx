// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ArrowChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

ArrowChoice::ArrowChoice()
{
    *this = ArrowChoice::group(ArrowChoiceGroup{});
}

ArrowChoice ArrowChoice::group(ArrowChoiceGroup value)
{
    return ArrowChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

ArrowChoice ArrowChoice::circularArrow(CircularArrow value)
{
    return ArrowChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

ArrowChoice parseArrowChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "arrow-direction")))
    {
        return ArrowChoice::group(parseArrowChoiceGroup(el, cursor, context));
    }
    if (cursor && (cursorIs(cursor, "circular-arrow")))
    {
        CircularArrow value = parseValue<CircularArrow>(childText(cursor), context, cursor, nullptr);
        cursor = nextElement(cursor);
        return ArrowChoice::circularArrow(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "arrow-direction");
}

void serializeArrowChoice(const ArrowChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case ArrowChoice::Kind::group:
        serializeArrowChoiceGroup(v.asGroup(), el);
        break;
    case ArrowChoice::Kind::circularArrow:
        writeTextElement(el, "circular-arrow", v.asCircularArrow().toString());
        break;
    }
}

} // namespace mx::core
