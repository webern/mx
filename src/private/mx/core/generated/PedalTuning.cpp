// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PedalTuning.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const Step &PedalTuning::pedalStep() const noexcept
{
    return m_pedalStep;
}

void PedalTuning::setPedalStep(Step value)
{
    m_pedalStep = std::move(value);
}

const Semitones &PedalTuning::pedalAlter() const noexcept
{
    return m_pedalAlter;
}

void PedalTuning::setPedalAlter(Semitones value)
{
    m_pedalAlter = std::move(value);
}

PedalTuning parsePedalTuning(pugi::xml_node el)
{
    return parsePedalTuning(el, ParseContext{});
}

PedalTuning parsePedalTuning(pugi::xml_node el, const ParseContext &context)
{
    PedalTuning out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parsePedalTuningContent(out, el, context);
    return out;
}

void parsePedalTuningContent(PedalTuning &out, pugi::xml_node el)
{
    parsePedalTuningContent(out, el, ParseContext{});
}

void parsePedalTuningContent(PedalTuning &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "pedal-step"))
    {
        out.setPedalStep(parseValue<Step>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "pedal-step");
    }
    if (cursorIs(cursor, "pedal-alter"))
    {
        out.setPedalAlter(parseValue<Semitones>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "pedal-alter");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "pedal-step") || cursorIs(cursor, "pedal-alter"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePedalTuning(const PedalTuning &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializePedalTuningContent(v, el);
}

void serializePedalTuningContent(const PedalTuning &v, pugi::xml_node el)
{
    writeTextElement(el, "pedal-step", v.pedalStep().toString());
    writeTextElement(el, "pedal-alter", v.pedalAlter().toString());
}

} // namespace mx::core
