// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/PartGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const StartStop &PartGroup::type() const noexcept
{
    return m_type;
}

void PartGroup::setType(StartStop value)
{
    m_type = std::move(value);
}

const std::optional<std::string> &PartGroup::number() const noexcept
{
    return m_number;
}

void PartGroup::setNumber(std::optional<std::string> value)
{
    m_number = std::move(value);
}

const std::optional<GroupName> &PartGroup::groupName() const noexcept
{
    return m_groupName;
}

void PartGroup::setGroupName(std::optional<GroupName> value)
{
    m_groupName = std::move(value);
}

const std::optional<NameDisplay> &PartGroup::groupNameDisplay() const noexcept
{
    return m_groupNameDisplay;
}

void PartGroup::setGroupNameDisplay(std::optional<NameDisplay> value)
{
    m_groupNameDisplay = std::move(value);
}

const std::optional<GroupName> &PartGroup::groupAbbreviation() const noexcept
{
    return m_groupAbbreviation;
}

void PartGroup::setGroupAbbreviation(std::optional<GroupName> value)
{
    m_groupAbbreviation = std::move(value);
}

const std::optional<NameDisplay> &PartGroup::groupAbbreviationDisplay() const noexcept
{
    return m_groupAbbreviationDisplay;
}

void PartGroup::setGroupAbbreviationDisplay(std::optional<NameDisplay> value)
{
    m_groupAbbreviationDisplay = std::move(value);
}

const std::optional<GroupSymbol> &PartGroup::groupSymbol() const noexcept
{
    return m_groupSymbol;
}

void PartGroup::setGroupSymbol(std::optional<GroupSymbol> value)
{
    m_groupSymbol = std::move(value);
}

const std::optional<GroupBarline> &PartGroup::groupBarline() const noexcept
{
    return m_groupBarline;
}

void PartGroup::setGroupBarline(std::optional<GroupBarline> value)
{
    m_groupBarline = std::move(value);
}

bool PartGroup::groupTime() const noexcept
{
    return m_groupTime;
}

void PartGroup::setGroupTime(bool value) noexcept
{
    m_groupTime = value;
}

const EditorialGroup &PartGroup::editorial() const noexcept
{
    return m_editorial;
}

void PartGroup::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

PartGroup parsePartGroup(pugi::xml_node el)
{
    return parsePartGroup(el, ParseContext{});
}

PartGroup parsePartGroup(pugi::xml_node el, const ParseContext &context)
{
    PartGroup out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<StartStop>(a.value(), context, el, "type"));
        }
        else if (aname == "number")
        {
            out.setNumber(std::string{a.value()});
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parsePartGroupContent(out, el, context);
    return out;
}

void parsePartGroupContent(PartGroup &out, pugi::xml_node el)
{
    parsePartGroupContent(out, el, ParseContext{});
}

void parsePartGroupContent(PartGroup &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "group-name"))
    {
        out.setGroupName(parseGroupName(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "group-name-display"))
    {
        out.setGroupNameDisplay(parseNameDisplay(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "group-abbreviation"))
    {
        out.setGroupAbbreviation(parseGroupName(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "group-abbreviation-display"))
    {
        out.setGroupAbbreviationDisplay(parseNameDisplay(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "group-symbol"))
    {
        out.setGroupSymbol(parseGroupSymbol(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "group-barline"))
    {
        out.setGroupBarline(parseGroupBarline(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "group-time"))
    {
        parseEmpty(cursor, context);
        out.setGroupTime(true);
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "group-name") || cursorIs(cursor, "group-name-display") ||
            cursorIs(cursor, "group-abbreviation") || cursorIs(cursor, "group-abbreviation-display") ||
            cursorIs(cursor, "group-symbol") || cursorIs(cursor, "group-barline") || cursorIs(cursor, "group-time") ||
            cursorIs(cursor, "footnote") || cursorIs(cursor, "level"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializePartGroup(const PartGroup &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    if (v.number())
    {
        setAttribute(el, "number", *v.number());
    }
    serializePartGroupContent(v, el);
}

void serializePartGroupContent(const PartGroup &v, pugi::xml_node el)
{
    if (v.groupName())
    {
        serializeGroupName(*v.groupName(), el, "group-name");
    }
    if (v.groupNameDisplay())
    {
        serializeNameDisplay(*v.groupNameDisplay(), el, "group-name-display");
    }
    if (v.groupAbbreviation())
    {
        serializeGroupName(*v.groupAbbreviation(), el, "group-abbreviation");
    }
    if (v.groupAbbreviationDisplay())
    {
        serializeNameDisplay(*v.groupAbbreviationDisplay(), el, "group-abbreviation-display");
    }
    if (v.groupSymbol())
    {
        serializeGroupSymbol(*v.groupSymbol(), el, "group-symbol");
    }
    if (v.groupBarline())
    {
        serializeGroupBarline(*v.groupBarline(), el, "group-barline");
    }
    if (v.groupTime())
    {
        el.append_child("group-time");
    }
    serializeEditorialGroup(v.editorial(), el);
}

} // namespace mx::core
