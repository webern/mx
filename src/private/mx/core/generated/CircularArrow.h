// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The circular-arrow type represents the direction in which a circular arrow points, using Unicode
/// arrow terminology.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class CircularArrow final
{
  public:
    enum class Tag
    {
        clockwise,
        anticlockwise,
    };

    /// The natural zero: the first variant.
    CircularArrow() noexcept = default;

    static CircularArrow clockwise() noexcept;
    static CircularArrow anticlockwise() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, CircularArrow &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static CircularArrow parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static CircularArrow parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const CircularArrow &other) const noexcept = default;

  private:
    explicit CircularArrow(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::clockwise};
};

} // namespace mx::core
