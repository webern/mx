// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/SlashGroupGroup.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The slash group combines elements used for more complete specification of the slash and
/// beat-repeat measure-style elements. They have the same values as the type and dot elements, and
/// define what the beat is for the display of repetition marks. If not present, the beat is based on
/// the current time signature.
/// A shared content group: transparent on the wire, its
/// fields serialize directly into the referencing element (plan §2.3).
class SlashGroup final
{
  public:
    const std::optional<SlashGroupGroup> &group() const noexcept;
    void setGroup(std::optional<SlashGroupGroup> value);
    std::span<const std::string> exceptVoice() const noexcept;
    void addExceptVoice(std::string value);
    void setExceptVoice(std::vector<std::string> value);
    bool operator==(const SlashGroup &other) const = default;

  private:
    std::optional<SlashGroupGroup> m_group;
    std::vector<std::string> m_exceptVoice;
};

/// Consumes matching siblings starting at `cursor` (which advances); `el`
/// is the enclosing element, for error paths.
SlashGroup parseSlashGroup(pugi::xml_node el, pugi::xml_node &cursor);

SlashGroup parseSlashGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeSlashGroup(const SlashGroup &v, pugi::xml_node el);

} // namespace mx::core
