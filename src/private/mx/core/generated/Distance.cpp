// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Distance.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const DistanceType &Distance::type() const noexcept
{
    return m_type;
}

void Distance::setType(DistanceType value)
{
    m_type = std::move(value);
}

const Tenths &Distance::value() const noexcept
{
    return m_value;
}

void Distance::setValue(Tenths value)
{
    m_value = std::move(value);
}

Distance parseDistance(pugi::xml_node el)
{
    return parseDistance(el, ParseContext{});
}

Distance parseDistance(pugi::xml_node el, const ParseContext &context)
{
    Distance out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<DistanceType>(a.value(), context, el, "type"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseDistanceContent(out, el, context);
    return out;
}

void parseDistanceContent(Distance &out, pugi::xml_node el)
{
    parseDistanceContent(out, el, ParseContext{});
}

void parseDistanceContent(Distance &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<Tenths>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeDistance(const Distance &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    serializeDistanceContent(v, el);
}

void serializeDistanceContent(const Distance &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
