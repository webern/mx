// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/AllMarginsGroup.h"
#include "mx/core/generated/MarginType.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// Page margins are specified either for both even and odd pages, or via separate odd and even page
/// number values. The type attribute is not needed when used as part of a print element. If omitted
/// when the page-margins type is used in the defaults element, "both" is the default value.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class PageMargins final
{
  public:
    const std::optional<MarginType> &type() const noexcept;
    void setType(std::optional<MarginType> value);
    const AllMarginsGroup &allMargins() const noexcept;
    void setAllMargins(AllMarginsGroup value);
    bool operator==(const PageMargins &other) const = default;

  private:
    std::optional<MarginType> m_type;
    AllMarginsGroup m_allMargins{};
};

PageMargins parsePageMargins(pugi::xml_node el, const ParseContext &context);

void parsePageMarginsContent(PageMargins &out, pugi::xml_node el, const ParseContext &context);

void serializePageMargins(const PageMargins &v, pugi::xml_node parent, const char *tag);

void serializePageMarginsContent(const PageMargins &v, pugi::xml_node el);

} // namespace mx::core
