// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/BendChoice.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

BendChoice::BendChoice()
{
    *this = BendChoice::preBend(Empty{});
}

BendChoice BendChoice::preBend(Empty value)
{
    return BendChoice{Storage{std::in_place_index<0>, std::move(value)}};
}

BendChoice BendChoice::release(Release value)
{
    return BendChoice{Storage{std::in_place_index<1>, std::move(value)}};
}

BendChoice parseBendChoice(pugi::xml_node el, pugi::xml_node &cursor)
{
    return parseBendChoice(el, cursor, ParseContext{});
}

BendChoice parseBendChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    if (cursor && (cursorIs(cursor, "pre-bend")))
    {
        Empty value = parseEmpty(cursor, context);
        cursor = nextElement(cursor);
        return BendChoice::preBend(std::move(value));
    }
    if (cursor && (cursorIs(cursor, "release")))
    {
        Release value = parseRelease(cursor, context);
        cursor = nextElement(cursor);
        return BendChoice::release(std::move(value));
    }
    if (cursor)
    {
        throwUnknownElement(cursor);
    }
    throwMissingElement(el, "pre-bend");
}

void serializeBendChoice(const BendChoice &v, pugi::xml_node el)
{
    switch (v.kind())
    {
    case BendChoice::Kind::preBend:
        serializeEmpty(v.asPreBend(), el, "pre-bend");
        break;
    case BendChoice::Kind::release:
        serializeRelease(v.asRelease(), el, "release");
        break;
    }
}

} // namespace mx::core
