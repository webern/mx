// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The backward-forward type is used to specify repeat directions. The start of the repeat has a
/// forward direction while the end of the repeat has a backward direction.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class BackwardForward final
{
  public:
    enum class Tag
    {
        backward,
        forward,
    };

    /// The natural zero: the first variant.
    BackwardForward() noexcept = default;

    static BackwardForward backward() noexcept;
    static BackwardForward forward() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, BackwardForward &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static BackwardForward parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static BackwardForward parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const BackwardForward &other) const noexcept = default;

  private:
    explicit BackwardForward(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::backward};
};

} // namespace mx::core
