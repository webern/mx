// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ScorePartwise.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<std::string> &ScorePartwise::version() const noexcept
{
    return m_version;
}

void ScorePartwise::setVersion(std::optional<std::string> value)
{
    m_version = std::move(value);
}

const ScoreHeaderGroup &ScorePartwise::scoreHeader() const noexcept
{
    return m_scoreHeader;
}

void ScorePartwise::setScoreHeader(ScoreHeaderGroup value)
{
    m_scoreHeader = std::move(value);
}

std::span<const PartwisePart> ScorePartwise::part() const noexcept
{
    return m_part.items();
}

void ScorePartwise::addPart(PartwisePart value)
{
    m_part.add(std::move(value));
}

void ScorePartwise::setPart(OneOrMore<PartwisePart> value)
{
    m_part = std::move(value);
}

ScorePartwise parseScorePartwise(pugi::xml_node el)
{
    return parseScorePartwise(el, ParseContext{});
}

ScorePartwise parseScorePartwise(pugi::xml_node el, const ParseContext &context)
{
    ScorePartwise out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "version")
        {
            out.setVersion(std::string{a.value()});
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseScorePartwiseContent(out, el, context);
    return out;
}

void parseScorePartwiseContent(ScorePartwise &out, pugi::xml_node el)
{
    parseScorePartwiseContent(out, el, ParseContext{});
}

void parseScorePartwiseContent(ScorePartwise &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "work") || cursorIs(cursor, "movement-number") ||
                   cursorIs(cursor, "movement-title") || cursorIs(cursor, "identification") ||
                   cursorIs(cursor, "defaults") || cursorIs(cursor, "credit") || cursorIs(cursor, "part-list")))
    {
        out.setScoreHeader(parseScoreHeaderGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "work");
    }
    if (!cursorIs(cursor, "part"))
    {
        throwMissingOrMisplaced(el, cursor, "part");
    }
    out.setPart(OneOrMore<PartwisePart>{parsePartwisePart(cursor, context)});
    cursor = nextElement(cursor);
    while (cursorIs(cursor, "part"))
    {
        out.addPart(parsePartwisePart(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "work") || cursorIs(cursor, "movement-number") || cursorIs(cursor, "movement-title") ||
            cursorIs(cursor, "identification") || cursorIs(cursor, "defaults") || cursorIs(cursor, "credit") ||
            cursorIs(cursor, "part-list") || cursorIs(cursor, "part"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeScorePartwise(const ScorePartwise &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.version())
    {
        setAttribute(el, "version", *v.version());
    }
    serializeScorePartwiseContent(v, el);
}

void serializeScorePartwiseContent(const ScorePartwise &v, pugi::xml_node el)
{
    serializeScoreHeaderGroup(v.scoreHeader(), el);
    for (const auto &item : v.part())
    {
        serializePartwisePart(item, el, "part");
    }
}

} // namespace mx::core
