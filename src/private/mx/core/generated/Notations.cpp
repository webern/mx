// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Notations.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Notations::printObject() const noexcept
{
    return m_printObject;
}

void Notations::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Token> &Notations::id() const noexcept
{
    return m_id;
}

void Notations::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const EditorialGroup &Notations::editorial() const noexcept
{
    return m_editorial;
}

void Notations::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

std::span<const NotationsChoice> Notations::choice() const noexcept
{
    return m_choice;
}

void Notations::addChoice(NotationsChoice value)
{
    m_choice.push_back(std::move(value));
}

void Notations::setChoice(std::vector<NotationsChoice> value)
{
    m_choice = std::move(value);
}

Notations parseNotations(pugi::xml_node el, const ParseContext &context)
{
    Notations out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "id")
        {
            out.setID(parseIdValue<Token>(a.value(), context, el, "id"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseNotationsContent(out, el, context);
    return out;
}

void parseNotationsContent(Notations &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    while (cursor &&
           (cursorIs(cursor, "tied") || cursorIs(cursor, "slur") || cursorIs(cursor, "tuplet") ||
            cursorIs(cursor, "glissando") || cursorIs(cursor, "slide") || cursorIs(cursor, "ornaments") ||
            cursorIs(cursor, "technical") || cursorIs(cursor, "articulations") || cursorIs(cursor, "dynamics") ||
            cursorIs(cursor, "fermata") || cursorIs(cursor, "arpeggiate") || cursorIs(cursor, "non-arpeggiate") ||
            cursorIs(cursor, "accidental-mark") || cursorIs(cursor, "other-notation")))
    {
        out.addChoice(parseNotationsChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "tied") ||
            cursorIs(cursor, "slur") || cursorIs(cursor, "tuplet") || cursorIs(cursor, "glissando") ||
            cursorIs(cursor, "slide") || cursorIs(cursor, "ornaments") || cursorIs(cursor, "technical") ||
            cursorIs(cursor, "articulations") || cursorIs(cursor, "dynamics") || cursorIs(cursor, "fermata") ||
            cursorIs(cursor, "arpeggiate") || cursorIs(cursor, "non-arpeggiate") ||
            cursorIs(cursor, "accidental-mark") || cursorIs(cursor, "other-notation"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeNotations(const Notations &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeNotationsContent(v, el);
}

void serializeNotationsContent(const Notations &v, pugi::xml_node el)
{
    serializeEditorialGroup(v.editorial(), el);
    for (const auto &item : v.choice())
    {
        serializeNotationsChoice(item, el);
    }
}

} // namespace mx::core
