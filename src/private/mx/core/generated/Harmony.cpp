// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Harmony.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<HarmonyType> &Harmony::type() const noexcept
{
    return m_type;
}

void Harmony::setType(std::optional<HarmonyType> value)
{
    m_type = std::move(value);
}

const std::optional<YesNo> &Harmony::printFrame() const noexcept
{
    return m_printFrame;
}

void Harmony::setPrintFrame(std::optional<YesNo> value)
{
    m_printFrame = std::move(value);
}

const std::optional<HarmonyArrangement> &Harmony::arrangement() const noexcept
{
    return m_arrangement;
}

void Harmony::setArrangement(std::optional<HarmonyArrangement> value)
{
    m_arrangement = std::move(value);
}

const std::optional<YesNo> &Harmony::printObject() const noexcept
{
    return m_printObject;
}

void Harmony::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<Tenths> &Harmony::defaultX() const noexcept
{
    return m_defaultX;
}

void Harmony::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Harmony::defaultY() const noexcept
{
    return m_defaultY;
}

void Harmony::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Harmony::relativeX() const noexcept
{
    return m_relativeX;
}

void Harmony::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Harmony::relativeY() const noexcept
{
    return m_relativeY;
}

void Harmony::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Harmony::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Harmony::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Harmony::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Harmony::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Harmony::fontSize() const noexcept
{
    return m_fontSize;
}

void Harmony::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Harmony::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Harmony::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Harmony::color() const noexcept
{
    return m_color;
}

void Harmony::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<AboveBelow> &Harmony::placement() const noexcept
{
    return m_placement;
}

void Harmony::setPlacement(std::optional<AboveBelow> value)
{
    m_placement = std::move(value);
}

const std::optional<SystemRelation> &Harmony::system() const noexcept
{
    return m_system;
}

void Harmony::setSystem(std::optional<SystemRelation> value)
{
    m_system = std::move(value);
}

const std::optional<Token> &Harmony::id() const noexcept
{
    return m_id;
}

void Harmony::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

std::span<const HarmonyChordGroup> Harmony::harmonyChord() const noexcept
{
    return m_harmonyChord.items();
}

void Harmony::addHarmonyChord(HarmonyChordGroup value)
{
    m_harmonyChord.add(std::move(value));
}

void Harmony::setHarmonyChord(OneOrMore<HarmonyChordGroup> value)
{
    m_harmonyChord = std::move(value);
}

const std::optional<Frame> &Harmony::frame() const noexcept
{
    return m_frame;
}

void Harmony::setFrame(std::optional<Frame> value)
{
    m_frame = std::move(value);
}

const std::optional<Offset> &Harmony::offset() const noexcept
{
    return m_offset;
}

void Harmony::setOffset(std::optional<Offset> value)
{
    m_offset = std::move(value);
}

const EditorialGroup &Harmony::editorial() const noexcept
{
    return m_editorial;
}

void Harmony::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

const std::optional<int> &Harmony::staff() const noexcept
{
    return m_staff;
}

void Harmony::setStaff(std::optional<int> value)
{
    m_staff = std::move(value);
}

Harmony parseHarmony(pugi::xml_node el)
{
    return parseHarmony(el, ParseContext{});
}

Harmony parseHarmony(pugi::xml_node el, const ParseContext &context)
{
    Harmony out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            out.setType(parseValue<HarmonyType>(a.value(), context, el, "type"));
        }
        else if (aname == "print-frame")
        {
            out.setPrintFrame(parseValue<YesNo>(a.value(), context, el, "print-frame"));
        }
        else if (aname == "arrangement")
        {
            out.setArrangement(parseValue<HarmonyArrangement>(a.value(), context, el, "arrangement"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "placement")
        {
            out.setPlacement(parseValue<AboveBelow>(a.value(), context, el, "placement"));
        }
        else if (aname == "system")
        {
            out.setSystem(parseValue<SystemRelation>(a.value(), context, el, "system"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseHarmonyContent(out, el, context);
    return out;
}

void parseHarmonyContent(Harmony &out, pugi::xml_node el)
{
    parseHarmonyContent(out, el, ParseContext{});
}

void parseHarmonyContent(Harmony &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (!(cursor && (cursorIs(cursor, "root") || cursorIs(cursor, "numeral") || cursorIs(cursor, "function"))))
    {
        throwMissingElement(el, "root");
    }
    out.setHarmonyChord(OneOrMore<HarmonyChordGroup>{parseHarmonyChordGroup(el, cursor, context)});
    while (cursor && (cursorIs(cursor, "root") || cursorIs(cursor, "numeral") || cursorIs(cursor, "function")))
    {
        out.addHarmonyChord(parseHarmonyChordGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "frame"))
    {
        out.setFrame(parseFrame(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "offset"))
    {
        out.setOffset(parseOffset(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "staff"))
    {
        out.setStaff(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "root") || cursorIs(cursor, "numeral") || cursorIs(cursor, "function") ||
            cursorIs(cursor, "kind") || cursorIs(cursor, "inversion") || cursorIs(cursor, "bass") ||
            cursorIs(cursor, "degree") || cursorIs(cursor, "frame") || cursorIs(cursor, "offset") ||
            cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "staff"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeHarmony(const Harmony &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.type())
    {
        setAttribute(el, "type", v.type()->toString());
    }
    if (v.printFrame())
    {
        setAttribute(el, "print-frame", v.printFrame()->toString());
    }
    if (v.arrangement())
    {
        setAttribute(el, "arrangement", v.arrangement()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.placement())
    {
        setAttribute(el, "placement", v.placement()->toString());
    }
    if (v.system())
    {
        setAttribute(el, "system", v.system()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeHarmonyContent(v, el);
}

void serializeHarmonyContent(const Harmony &v, pugi::xml_node el)
{
    for (const auto &item : v.harmonyChord())
    {
        serializeHarmonyChordGroup(item, el);
    }
    if (v.frame())
    {
        serializeFrame(*v.frame(), el, "frame");
    }
    if (v.offset())
    {
        serializeOffset(*v.offset(), el, "offset");
    }
    serializeEditorialGroup(v.editorial(), el);
    if (v.staff())
    {
        writeTextElement(el, "staff", formatInt(*v.staff()));
    }
}

} // namespace mx::core
