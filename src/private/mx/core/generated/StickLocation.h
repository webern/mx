// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The stick-location type represents pictograms for the location of sticks, beaters, or mallets on
/// cymbals, gongs, drums, and other instruments.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class StickLocation final
{
  public:
    enum class Tag
    {
        center,
        rim,
        cymbalBell,
        cymbalEdge,
    };

    /// The natural zero: the first variant.
    StickLocation() noexcept = default;

    static StickLocation center() noexcept;
    static StickLocation rim() noexcept;
    static StickLocation cymbalBell() noexcept;
    static StickLocation cymbalEdge() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, StickLocation &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static StickLocation parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static StickLocation parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const StickLocation &other) const noexcept = default;

  private:
    explicit StickLocation(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::center};
};

} // namespace mx::core
