// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string>
#include <string_view>

namespace mx::core
{

/// The smufl-glyph-name type is used for attributes that reference a specific Standard Music Font
/// Layout (SMuFL) character. The value is a SMuFL canonical glyph name, not a code point. For
/// instance, the value for a standard piano pedal mark would be keyboardPedalPed, not U+E650.
/// Open vocabulary over xs:NMTOKEN: construction repairs the value to name
/// characters and to non-empty (placeholder "X"), so a lexically invalid
/// token cannot exist.
class SmuflGlyphName final
{
  public:
    /// The natural zero: the placeholder "X".
    SmuflGlyphName();

    explicit SmuflGlyphName(std::string value);

    const std::string &value() const noexcept
    {
        return m_value;
    }

    void setValue(std::string value);

    std::string toString() const
    {
        return m_value;
    }

    /// Strict: a non-empty all-name-character token.
    static bool tryParse(std::string_view text, SmuflGlyphName &out);

    /// Lenient: repairs into the nearest valid token.
    static SmuflGlyphName parse(std::string_view text);

    /// Lenient, and says whether the text had to be repaired.
    static SmuflGlyphName parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const SmuflGlyphName &other) const noexcept = default;

  private:
    void repair();

    std::string m_value;
};

} // namespace mx::core
