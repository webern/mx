// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Beater.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<TipDirection> &Beater::tip() const noexcept
{
    return m_tip;
}

void Beater::setTip(std::optional<TipDirection> value)
{
    m_tip = std::move(value);
}

const BeaterValue &Beater::value() const noexcept
{
    return m_value;
}

void Beater::setValue(BeaterValue value)
{
    m_value = std::move(value);
}

Beater parseBeater(pugi::xml_node el)
{
    return parseBeater(el, ParseContext{});
}

Beater parseBeater(pugi::xml_node el, const ParseContext &context)
{
    Beater out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "tip")
        {
            out.setTip(parseValue<TipDirection>(a.value(), context, el, "tip"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseBeaterContent(out, el, context);
    return out;
}

void parseBeaterContent(Beater &out, pugi::xml_node el)
{
    parseBeaterContent(out, el, ParseContext{});
}

void parseBeaterContent(Beater &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<BeaterValue>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeBeater(const Beater &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.tip())
    {
        setAttribute(el, "tip", v.tip()->toString());
    }
    serializeBeaterContent(v, el);
}

void serializeBeaterContent(const Beater &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
