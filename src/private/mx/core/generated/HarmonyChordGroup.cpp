// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/HarmonyChordGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const HarmonyChordGroupChoice &HarmonyChordGroup::choice() const noexcept
{
    return m_choice;
}

void HarmonyChordGroup::setChoice(HarmonyChordGroupChoice value)
{
    m_choice = std::move(value);
}

const Kind &HarmonyChordGroup::kind() const noexcept
{
    return m_kind;
}

void HarmonyChordGroup::setKind(Kind value)
{
    m_kind = std::move(value);
}

const std::optional<Inversion> &HarmonyChordGroup::inversion() const noexcept
{
    return m_inversion;
}

void HarmonyChordGroup::setInversion(std::optional<Inversion> value)
{
    m_inversion = std::move(value);
}

const std::optional<Bass> &HarmonyChordGroup::bass() const noexcept
{
    return m_bass;
}

void HarmonyChordGroup::setBass(std::optional<Bass> value)
{
    m_bass = std::move(value);
}

std::span<const Degree> HarmonyChordGroup::degree() const noexcept
{
    return m_degree;
}

void HarmonyChordGroup::addDegree(Degree value)
{
    m_degree.push_back(std::move(value));
}

void HarmonyChordGroup::setDegree(std::vector<Degree> value)
{
    m_degree = std::move(value);
}

HarmonyChordGroup parseHarmonyChordGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    HarmonyChordGroup out;
    if (cursor && (cursorIs(cursor, "root") || cursorIs(cursor, "numeral") || cursorIs(cursor, "function")))
    {
        out.setChoice(parseHarmonyChordGroupChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "root");
    }
    if (cursorIs(cursor, "kind"))
    {
        out.setKind(parseKind(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "kind");
    }
    if (cursorIs(cursor, "inversion"))
    {
        out.setInversion(parseInversion(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "bass"))
    {
        out.setBass(parseBass(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "degree"))
    {
        out.addDegree(parseDegree(cursor, context));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeHarmonyChordGroup(const HarmonyChordGroup &v, pugi::xml_node el)
{
    serializeHarmonyChordGroupChoice(v.choice(), el);
    serializeKind(v.kind(), el, "kind");
    if (v.inversion())
    {
        serializeInversion(*v.inversion(), el, "inversion");
    }
    if (v.bass())
    {
        serializeBass(*v.bass(), el, "bass");
    }
    for (const auto &item : v.degree())
    {
        serializeDegree(item, el, "degree");
    }
}

} // namespace mx::core
