// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/BeatUnitTied.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const BeatUnitGroup &BeatUnitTied::beatUnit() const noexcept
{
    return m_beatUnit;
}

void BeatUnitTied::setBeatUnit(BeatUnitGroup value)
{
    m_beatUnit = std::move(value);
}

BeatUnitTied parseBeatUnitTied(pugi::xml_node el)
{
    return parseBeatUnitTied(el, ParseContext{});
}

BeatUnitTied parseBeatUnitTied(pugi::xml_node el, const ParseContext &context)
{
    BeatUnitTied out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseBeatUnitTiedContent(out, el, context);
    return out;
}

void parseBeatUnitTiedContent(BeatUnitTied &out, pugi::xml_node el)
{
    parseBeatUnitTiedContent(out, el, ParseContext{});
}

void parseBeatUnitTiedContent(BeatUnitTied &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "beat-unit")))
    {
        out.setBeatUnit(parseBeatUnitGroup(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "beat-unit");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "beat-unit") || cursorIs(cursor, "beat-unit-dot"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeBeatUnitTied(const BeatUnitTied &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeBeatUnitTiedContent(v, el);
}

void serializeBeatUnitTiedContent(const BeatUnitTied &v, pugi::xml_node el)
{
    serializeBeatUnitGroup(v.beatUnit(), el);
}

} // namespace mx::core
