// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Attributes.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const EditorialGroup &Attributes::editorial() const noexcept
{
    return m_editorial;
}

void Attributes::setEditorial(EditorialGroup value)
{
    m_editorial = std::move(value);
}

const std::optional<PositiveDivisions> &Attributes::divisions() const noexcept
{
    return m_divisions;
}

void Attributes::setDivisions(std::optional<PositiveDivisions> value)
{
    m_divisions = std::move(value);
}

std::span<const Key> Attributes::key() const noexcept
{
    return m_key;
}

void Attributes::addKey(Key value)
{
    m_key.push_back(std::move(value));
}

void Attributes::setKey(std::vector<Key> value)
{
    m_key = std::move(value);
}

std::span<const Time> Attributes::time() const noexcept
{
    return m_time;
}

void Attributes::addTime(Time value)
{
    m_time.push_back(std::move(value));
}

void Attributes::setTime(std::vector<Time> value)
{
    m_time = std::move(value);
}

const std::optional<int> &Attributes::staves() const noexcept
{
    return m_staves;
}

void Attributes::setStaves(std::optional<int> value)
{
    m_staves = std::move(value);
}

const std::optional<PartSymbol> &Attributes::partSymbol() const noexcept
{
    return m_partSymbol;
}

void Attributes::setPartSymbol(std::optional<PartSymbol> value)
{
    m_partSymbol = std::move(value);
}

const std::optional<int> &Attributes::instruments() const noexcept
{
    return m_instruments;
}

void Attributes::setInstruments(std::optional<int> value)
{
    m_instruments = std::move(value);
}

std::span<const Clef> Attributes::clef() const noexcept
{
    return m_clef;
}

void Attributes::addClef(Clef value)
{
    m_clef.push_back(std::move(value));
}

void Attributes::setClef(std::vector<Clef> value)
{
    m_clef = std::move(value);
}

std::span<const StaffDetails> Attributes::staffDetails() const noexcept
{
    return m_staffDetails;
}

void Attributes::addStaffDetails(StaffDetails value)
{
    m_staffDetails.push_back(std::move(value));
}

void Attributes::setStaffDetails(std::vector<StaffDetails> value)
{
    m_staffDetails = std::move(value);
}

const AttributesChoice &Attributes::choice() const noexcept
{
    return m_choice;
}

void Attributes::setChoice(AttributesChoice value)
{
    m_choice = std::move(value);
}

std::span<const Directive> Attributes::directive() const noexcept
{
    return m_directive;
}

void Attributes::addDirective(Directive value)
{
    m_directive.push_back(std::move(value));
}

void Attributes::setDirective(std::vector<Directive> value)
{
    m_directive = std::move(value);
}

std::span<const MeasureStyle> Attributes::measureStyle() const noexcept
{
    return m_measureStyle;
}

void Attributes::addMeasureStyle(MeasureStyle value)
{
    m_measureStyle.push_back(std::move(value));
}

void Attributes::setMeasureStyle(std::vector<MeasureStyle> value)
{
    m_measureStyle = std::move(value);
}

Attributes parseAttributes(pugi::xml_node el, const ParseContext &context)
{
    Attributes out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        throwUnknownAttribute(el, a.name());
    }
    parseAttributesContent(out, el, context);
    return out;
}

void parseAttributesContent(Attributes &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level")))
    {
        out.setEditorial(parseEditorialGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "divisions"))
    {
        out.setDivisions(parseValue<PositiveDivisions>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "key"))
    {
        out.addKey(parseKey(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "time"))
    {
        out.addTime(parseTime(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "staves"))
    {
        out.setStaves(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "part-symbol"))
    {
        out.setPartSymbol(parsePartSymbol(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "instruments"))
    {
        out.setInstruments(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "clef"))
    {
        out.addClef(parseClef(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "staff-details"))
    {
        out.addStaffDetails(parseStaffDetails(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "transpose") || cursorIs(cursor, "for-part")))
    {
        out.setChoice(parseAttributesChoice(el, cursor, context));
    }
    while (cursorIs(cursor, "directive"))
    {
        out.addDirective(parseDirective(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "measure-style"))
    {
        out.addMeasureStyle(parseMeasureStyle(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "divisions") ||
            cursorIs(cursor, "key") || cursorIs(cursor, "time") || cursorIs(cursor, "staves") ||
            cursorIs(cursor, "part-symbol") || cursorIs(cursor, "instruments") || cursorIs(cursor, "clef") ||
            cursorIs(cursor, "staff-details") || cursorIs(cursor, "transpose") || cursorIs(cursor, "for-part") ||
            cursorIs(cursor, "directive") || cursorIs(cursor, "measure-style"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeAttributes(const Attributes &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    serializeAttributesContent(v, el);
}

void serializeAttributesContent(const Attributes &v, pugi::xml_node el)
{
    serializeEditorialGroup(v.editorial(), el);
    if (v.divisions())
    {
        writeTextElement(el, "divisions", v.divisions()->toString());
    }
    for (const auto &item : v.key())
    {
        serializeKey(item, el, "key");
    }
    for (const auto &item : v.time())
    {
        serializeTime(item, el, "time");
    }
    if (v.staves())
    {
        writeTextElement(el, "staves", formatInt(*v.staves()));
    }
    if (v.partSymbol())
    {
        serializePartSymbol(*v.partSymbol(), el, "part-symbol");
    }
    if (v.instruments())
    {
        writeTextElement(el, "instruments", formatInt(*v.instruments()));
    }
    for (const auto &item : v.clef())
    {
        serializeClef(item, el, "clef");
    }
    for (const auto &item : v.staffDetails())
    {
        serializeStaffDetails(item, el, "staff-details");
    }
    serializeAttributesChoice(v.choice(), el);
    for (const auto &item : v.directive())
    {
        serializeDirective(item, el, "directive");
    }
    for (const auto &item : v.measureStyle())
    {
        serializeMeasureStyle(item, el, "measure-style");
    }
}

} // namespace mx::core
