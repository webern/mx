// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/NameDisplayChoice.h"
#include "mx/core/generated/YesNo.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The name-display type is used for exact formatting of multi-font text in part and group names to
/// the left of the system. The print-object attribute can be used to determine what, if anything, is
/// printed at the start of each system. Enclosure for the display-text element is none by default.
/// Language for the display-text element is Italian ("it") by default.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class NameDisplay final
{
  public:
    const std::optional<YesNo> &printObject() const noexcept;
    void setPrintObject(std::optional<YesNo> value);
    std::span<const NameDisplayChoice> choice() const noexcept;
    void addChoice(NameDisplayChoice value);
    void setChoice(std::vector<NameDisplayChoice> value);
    bool operator==(const NameDisplay &other) const = default;

  private:
    std::optional<YesNo> m_printObject;
    std::vector<NameDisplayChoice> m_choice;
};

NameDisplay parseNameDisplay(pugi::xml_node el, const ParseContext &context);

void parseNameDisplayContent(NameDisplay &out, pugi::xml_node el, const ParseContext &context);

void serializeNameDisplay(const NameDisplay &v, pugi::xml_node parent, const char *tag);

void serializeNameDisplayContent(const NameDisplay &v, pugi::xml_node el);

} // namespace mx::core
