// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Empty.h"
#include "mx/core/generated/FullNoteGroupChoice.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The full-note group is a sequence of the common note elements between cue/grace notes and regular
/// (full) notes: pitch, chord, and rest information, but not duration (cue and grace notes do not
/// have duration encoded). Unpitched elements are used for unpitched percussion, speaking voice, and
/// other musical elements lacking determinate pitch.
/// A shared content group: transparent on the wire, its
/// fields serialize directly into the referencing element (plan §2.3).
class FullNoteGroup final
{
  public:
    bool chord() const noexcept;
    void setChord(bool value) noexcept;
    const FullNoteGroupChoice &choice() const noexcept;
    void setChoice(FullNoteGroupChoice value);
    bool operator==(const FullNoteGroup &other) const = default;

  private:
    bool m_chord{false};
    FullNoteGroupChoice m_choice{};
};

/// Consumes matching siblings starting at `cursor` (which advances); `el`
/// is the enclosing element, for error paths.
FullNoteGroup parseFullNoteGroup(pugi::xml_node el, pugi::xml_node &cursor);

FullNoteGroup parseFullNoteGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeFullNoteGroup(const FullNoteGroup &v, pugi::xml_node el);

} // namespace mx::core
