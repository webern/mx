// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/ClefGroup.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const ClefSign &ClefGroup::sign() const noexcept
{
    return m_sign;
}

void ClefGroup::setSign(ClefSign value)
{
    m_sign = std::move(value);
}

const std::optional<StaffLinePosition> &ClefGroup::line() const noexcept
{
    return m_line;
}

void ClefGroup::setLine(std::optional<StaffLinePosition> value)
{
    m_line = std::move(value);
}

const std::optional<int> &ClefGroup::clefOctaveChange() const noexcept
{
    return m_clefOctaveChange;
}

void ClefGroup::setClefOctaveChange(std::optional<int> value)
{
    m_clefOctaveChange = std::move(value);
}

ClefGroup parseClefGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context)
{
    ClefGroup out;
    if (cursorIs(cursor, "sign"))
    {
        out.setSign(parseValue<ClefSign>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "sign");
    }
    if (cursorIs(cursor, "line"))
    {
        out.setLine(parseValue<StaffLinePosition>(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "clef-octave-change"))
    {
        out.setClefOctaveChange(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    return out;
}

void serializeClefGroup(const ClefGroup &v, pugi::xml_node el)
{
    writeTextElement(el, "sign", v.sign().toString());
    if (v.line())
    {
        writeTextElement(el, "line", v.line()->toString());
    }
    if (v.clefOctaveChange())
    {
        writeTextElement(el, "clef-octave-change", formatInt(*v.clefOctaveChange()));
    }
}

} // namespace mx::core
