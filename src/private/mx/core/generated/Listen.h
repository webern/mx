// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/OneOrMore.h"
#include "mx/core/generated/ListenChoice.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The listen and listening types, new in Version 4.0, specify different ways that a score following
/// or machine listening application can interact with a performer. The listen type handles
/// interactions that are specific to a note. If multiple child elements of the same type are
/// present, they should have distinct player and/or time-only attributes.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Listen final
{
  public:
    std::span<const ListenChoice> choice() const noexcept;
    void addChoice(ListenChoice value);
    void setChoice(OneOrMore<ListenChoice> value);
    bool operator==(const Listen &other) const = default;

  private:
    OneOrMore<ListenChoice> m_choice;
};

Listen parseListen(pugi::xml_node el);

Listen parseListen(pugi::xml_node el, const ParseContext &context);

void parseListenContent(Listen &out, pugi::xml_node el);

void parseListenContent(Listen &out, pugi::xml_node el, const ParseContext &context);

void serializeListen(const Listen &v, pugi::xml_node parent, const char *tag);

void serializeListenContent(const Listen &v, pugi::xml_node el);

} // namespace mx::core
