// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Note.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Note::printLeger() const noexcept
{
    return m_printLeger;
}

void Note::setPrintLeger(std::optional<YesNo> value)
{
    m_printLeger = std::move(value);
}

const std::optional<NonNegativeDecimal> &Note::dynamics() const noexcept
{
    return m_dynamics;
}

void Note::setDynamics(std::optional<NonNegativeDecimal> value)
{
    m_dynamics = std::move(value);
}

const std::optional<NonNegativeDecimal> &Note::endDynamics() const noexcept
{
    return m_endDynamics;
}

void Note::setEndDynamics(std::optional<NonNegativeDecimal> value)
{
    m_endDynamics = std::move(value);
}

const std::optional<Divisions> &Note::attack() const noexcept
{
    return m_attack;
}

void Note::setAttack(std::optional<Divisions> value)
{
    m_attack = std::move(value);
}

const std::optional<Divisions> &Note::release() const noexcept
{
    return m_release;
}

void Note::setRelease(std::optional<Divisions> value)
{
    m_release = std::move(value);
}

const std::optional<TimeOnly> &Note::timeOnly() const noexcept
{
    return m_timeOnly;
}

void Note::setTimeOnly(std::optional<TimeOnly> value)
{
    m_timeOnly = std::move(value);
}

const std::optional<YesNo> &Note::pizzicato() const noexcept
{
    return m_pizzicato;
}

void Note::setPizzicato(std::optional<YesNo> value)
{
    m_pizzicato = std::move(value);
}

const std::optional<Tenths> &Note::defaultX() const noexcept
{
    return m_defaultX;
}

void Note::setDefaultX(std::optional<Tenths> value)
{
    m_defaultX = std::move(value);
}

const std::optional<Tenths> &Note::defaultY() const noexcept
{
    return m_defaultY;
}

void Note::setDefaultY(std::optional<Tenths> value)
{
    m_defaultY = std::move(value);
}

const std::optional<Tenths> &Note::relativeX() const noexcept
{
    return m_relativeX;
}

void Note::setRelativeX(std::optional<Tenths> value)
{
    m_relativeX = std::move(value);
}

const std::optional<Tenths> &Note::relativeY() const noexcept
{
    return m_relativeY;
}

void Note::setRelativeY(std::optional<Tenths> value)
{
    m_relativeY = std::move(value);
}

const std::optional<FontFamily> &Note::fontFamily() const noexcept
{
    return m_fontFamily;
}

void Note::setFontFamily(std::optional<FontFamily> value)
{
    m_fontFamily = std::move(value);
}

const std::optional<FontStyle> &Note::fontStyle() const noexcept
{
    return m_fontStyle;
}

void Note::setFontStyle(std::optional<FontStyle> value)
{
    m_fontStyle = std::move(value);
}

const std::optional<FontSize> &Note::fontSize() const noexcept
{
    return m_fontSize;
}

void Note::setFontSize(std::optional<FontSize> value)
{
    m_fontSize = std::move(value);
}

const std::optional<FontWeight> &Note::fontWeight() const noexcept
{
    return m_fontWeight;
}

void Note::setFontWeight(std::optional<FontWeight> value)
{
    m_fontWeight = std::move(value);
}

const std::optional<Color> &Note::color() const noexcept
{
    return m_color;
}

void Note::setColor(std::optional<Color> value)
{
    m_color = std::move(value);
}

const std::optional<YesNo> &Note::printDot() const noexcept
{
    return m_printDot;
}

void Note::setPrintDot(std::optional<YesNo> value)
{
    m_printDot = std::move(value);
}

const std::optional<YesNo> &Note::printLyric() const noexcept
{
    return m_printLyric;
}

void Note::setPrintLyric(std::optional<YesNo> value)
{
    m_printLyric = std::move(value);
}

const std::optional<YesNo> &Note::printObject() const noexcept
{
    return m_printObject;
}

void Note::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const std::optional<YesNo> &Note::printSpacing() const noexcept
{
    return m_printSpacing;
}

void Note::setPrintSpacing(std::optional<YesNo> value)
{
    m_printSpacing = std::move(value);
}

const std::optional<Token> &Note::id() const noexcept
{
    return m_id;
}

void Note::setID(std::optional<Token> value)
{
    m_id = std::move(value);
}

const NoteChoice &Note::choice() const noexcept
{
    return m_choice;
}

void Note::setChoice(NoteChoice value)
{
    m_choice = std::move(value);
}

std::span<const Instrument> Note::instrument() const noexcept
{
    return m_instrument;
}

void Note::addInstrument(Instrument value)
{
    m_instrument.push_back(std::move(value));
}

void Note::setInstrument(std::vector<Instrument> value)
{
    m_instrument = std::move(value);
}

const EditorialVoiceGroup &Note::editorialVoice() const noexcept
{
    return m_editorialVoice;
}

void Note::setEditorialVoice(EditorialVoiceGroup value)
{
    m_editorialVoice = std::move(value);
}

const std::optional<NoteType> &Note::type() const noexcept
{
    return m_type;
}

void Note::setType(std::optional<NoteType> value)
{
    m_type = std::move(value);
}

std::span<const EmptyPlacement> Note::dot() const noexcept
{
    return m_dot;
}

void Note::addDot(EmptyPlacement value)
{
    m_dot.push_back(std::move(value));
}

void Note::setDot(std::vector<EmptyPlacement> value)
{
    m_dot = std::move(value);
}

const std::optional<Accidental> &Note::accidental() const noexcept
{
    return m_accidental;
}

void Note::setAccidental(std::optional<Accidental> value)
{
    m_accidental = std::move(value);
}

const std::optional<TimeModification> &Note::timeModification() const noexcept
{
    return m_timeModification;
}

void Note::setTimeModification(std::optional<TimeModification> value)
{
    m_timeModification = std::move(value);
}

const std::optional<Stem> &Note::stem() const noexcept
{
    return m_stem;
}

void Note::setStem(std::optional<Stem> value)
{
    m_stem = std::move(value);
}

const std::optional<Notehead> &Note::notehead() const noexcept
{
    return m_notehead;
}

void Note::setNotehead(std::optional<Notehead> value)
{
    m_notehead = std::move(value);
}

const std::optional<NoteheadText> &Note::noteheadText() const noexcept
{
    return m_noteheadText;
}

void Note::setNoteheadText(std::optional<NoteheadText> value)
{
    m_noteheadText = std::move(value);
}

const std::optional<int> &Note::staff() const noexcept
{
    return m_staff;
}

void Note::setStaff(std::optional<int> value)
{
    m_staff = std::move(value);
}

std::span<const Beam> Note::beam() const noexcept
{
    return m_beam;
}

Result<void> Note::addBeam(Beam value)
{
    if (m_beam.size() >= 8)
    {
        return Error{ErrorCode::tooManyElements, {}, "at most 8 beam occurrences"};
    }
    m_beam.push_back(std::move(value));
    return {};
}

void Note::clearBeam() noexcept
{
    m_beam.clear();
}

std::span<const Notations> Note::notations() const noexcept
{
    return m_notations;
}

void Note::addNotations(Notations value)
{
    m_notations.push_back(std::move(value));
}

void Note::setNotations(std::vector<Notations> value)
{
    m_notations = std::move(value);
}

std::span<const Lyric> Note::lyric() const noexcept
{
    return m_lyric;
}

void Note::addLyric(Lyric value)
{
    m_lyric.push_back(std::move(value));
}

void Note::setLyric(std::vector<Lyric> value)
{
    m_lyric = std::move(value);
}

const std::optional<Play> &Note::play() const noexcept
{
    return m_play;
}

void Note::setPlay(std::optional<Play> value)
{
    m_play = std::move(value);
}

const std::optional<Listen> &Note::listen() const noexcept
{
    return m_listen;
}

void Note::setListen(std::optional<Listen> value)
{
    m_listen = std::move(value);
}

Note parseNote(pugi::xml_node el)
{
    return parseNote(el, ParseContext{});
}

Note parseNote(pugi::xml_node el, const ParseContext &context)
{
    Note out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "print-leger")
        {
            out.setPrintLeger(parseValue<YesNo>(a.value(), context, el, "print-leger"));
        }
        else if (aname == "dynamics")
        {
            out.setDynamics(parseValue<NonNegativeDecimal>(a.value(), context, el, "dynamics"));
        }
        else if (aname == "end-dynamics")
        {
            out.setEndDynamics(parseValue<NonNegativeDecimal>(a.value(), context, el, "end-dynamics"));
        }
        else if (aname == "attack")
        {
            out.setAttack(parseValue<Divisions>(a.value(), context, el, "attack"));
        }
        else if (aname == "release")
        {
            out.setRelease(parseValue<Divisions>(a.value(), context, el, "release"));
        }
        else if (aname == "time-only")
        {
            out.setTimeOnly(parseValue<TimeOnly>(a.value(), context, el, "time-only"));
        }
        else if (aname == "pizzicato")
        {
            out.setPizzicato(parseValue<YesNo>(a.value(), context, el, "pizzicato"));
        }
        else if (aname == "default-x")
        {
            out.setDefaultX(parseValue<Tenths>(a.value(), context, el, "default-x"));
        }
        else if (aname == "default-y")
        {
            out.setDefaultY(parseValue<Tenths>(a.value(), context, el, "default-y"));
        }
        else if (aname == "relative-x")
        {
            out.setRelativeX(parseValue<Tenths>(a.value(), context, el, "relative-x"));
        }
        else if (aname == "relative-y")
        {
            out.setRelativeY(parseValue<Tenths>(a.value(), context, el, "relative-y"));
        }
        else if (aname == "font-family")
        {
            out.setFontFamily(parseValue<FontFamily>(a.value(), context, el, "font-family"));
        }
        else if (aname == "font-style")
        {
            out.setFontStyle(parseValue<FontStyle>(a.value(), context, el, "font-style"));
        }
        else if (aname == "font-size")
        {
            out.setFontSize(parseValue<FontSize>(a.value(), context, el, "font-size"));
        }
        else if (aname == "font-weight")
        {
            out.setFontWeight(parseValue<FontWeight>(a.value(), context, el, "font-weight"));
        }
        else if (aname == "color")
        {
            out.setColor(parseValue<Color>(a.value(), context, el, "color"));
        }
        else if (aname == "print-dot")
        {
            out.setPrintDot(parseValue<YesNo>(a.value(), context, el, "print-dot"));
        }
        else if (aname == "print-lyric")
        {
            out.setPrintLyric(parseValue<YesNo>(a.value(), context, el, "print-lyric"));
        }
        else if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else if (aname == "print-spacing")
        {
            out.setPrintSpacing(parseValue<YesNo>(a.value(), context, el, "print-spacing"));
        }
        else if (aname == "id")
        {
            out.setID(Token::parse(a.value()));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseNoteContent(out, el, context);
    return out;
}

void parseNoteContent(Note &out, pugi::xml_node el)
{
    parseNoteContent(out, el, ParseContext{});
}

void parseNoteContent(Note &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursor && (cursorIs(cursor, "grace") || cursorIs(cursor, "cue") || cursorIs(cursor, "chord") ||
                   cursorIs(cursor, "pitch") || cursorIs(cursor, "unpitched") || cursorIs(cursor, "rest")))
    {
        out.setChoice(parseNoteChoice(el, cursor, context));
    }
    else
    {
        throwMissingElement(el, "grace");
    }
    while (cursorIs(cursor, "instrument"))
    {
        out.addInstrument(parseInstrument(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor && (cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "voice")))
    {
        out.setEditorialVoice(parseEditorialVoiceGroup(el, cursor, context));
    }
    if (cursorIs(cursor, "type"))
    {
        out.setType(parseNoteType(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "dot"))
    {
        out.addDot(parseEmptyPlacement(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "accidental"))
    {
        out.setAccidental(parseAccidental(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "time-modification"))
    {
        out.setTimeModification(parseTimeModification(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "stem"))
    {
        out.setStem(parseStem(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "notehead"))
    {
        out.setNotehead(parseNotehead(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "notehead-text"))
    {
        out.setNoteheadText(parseNoteheadText(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "staff"))
    {
        out.setStaff(parseIntegerValue(childText(cursor), context, cursor, nullptr));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "beam"))
    {
        if (!out.addBeam(parseBeam(cursor, context)))
        {
            throwTooManyElements(cursor);
        }
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "notations"))
    {
        out.addNotations(parseNotations(cursor, context));
        cursor = nextElement(cursor);
    }
    while (cursorIs(cursor, "lyric"))
    {
        out.addLyric(parseLyric(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "play"))
    {
        out.setPlay(parsePlay(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursorIs(cursor, "listen"))
    {
        out.setListen(parseListen(cursor, context));
        cursor = nextElement(cursor);
    }
    if (cursor)
    {
        if (cursorIs(cursor, "grace") || cursorIs(cursor, "chord") || cursorIs(cursor, "pitch") ||
            cursorIs(cursor, "unpitched") || cursorIs(cursor, "rest") || cursorIs(cursor, "tie") ||
            cursorIs(cursor, "cue") || cursorIs(cursor, "duration") || cursorIs(cursor, "instrument") ||
            cursorIs(cursor, "footnote") || cursorIs(cursor, "level") || cursorIs(cursor, "voice") ||
            cursorIs(cursor, "type") || cursorIs(cursor, "dot") || cursorIs(cursor, "accidental") ||
            cursorIs(cursor, "time-modification") || cursorIs(cursor, "stem") || cursorIs(cursor, "notehead") ||
            cursorIs(cursor, "notehead-text") || cursorIs(cursor, "staff") || cursorIs(cursor, "beam") ||
            cursorIs(cursor, "notations") || cursorIs(cursor, "lyric") || cursorIs(cursor, "play") ||
            cursorIs(cursor, "listen"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeNote(const Note &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.printLeger())
    {
        setAttribute(el, "print-leger", v.printLeger()->toString());
    }
    if (v.dynamics())
    {
        setAttribute(el, "dynamics", v.dynamics()->toString());
    }
    if (v.endDynamics())
    {
        setAttribute(el, "end-dynamics", v.endDynamics()->toString());
    }
    if (v.attack())
    {
        setAttribute(el, "attack", v.attack()->toString());
    }
    if (v.release())
    {
        setAttribute(el, "release", v.release()->toString());
    }
    if (v.timeOnly())
    {
        setAttribute(el, "time-only", v.timeOnly()->toString());
    }
    if (v.pizzicato())
    {
        setAttribute(el, "pizzicato", v.pizzicato()->toString());
    }
    if (v.defaultX())
    {
        setAttribute(el, "default-x", v.defaultX()->toString());
    }
    if (v.defaultY())
    {
        setAttribute(el, "default-y", v.defaultY()->toString());
    }
    if (v.relativeX())
    {
        setAttribute(el, "relative-x", v.relativeX()->toString());
    }
    if (v.relativeY())
    {
        setAttribute(el, "relative-y", v.relativeY()->toString());
    }
    if (v.fontFamily())
    {
        setAttribute(el, "font-family", v.fontFamily()->toString());
    }
    if (v.fontStyle())
    {
        setAttribute(el, "font-style", v.fontStyle()->toString());
    }
    if (v.fontSize())
    {
        setAttribute(el, "font-size", v.fontSize()->toString());
    }
    if (v.fontWeight())
    {
        setAttribute(el, "font-weight", v.fontWeight()->toString());
    }
    if (v.color())
    {
        setAttribute(el, "color", v.color()->toString());
    }
    if (v.printDot())
    {
        setAttribute(el, "print-dot", v.printDot()->toString());
    }
    if (v.printLyric())
    {
        setAttribute(el, "print-lyric", v.printLyric()->toString());
    }
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    if (v.printSpacing())
    {
        setAttribute(el, "print-spacing", v.printSpacing()->toString());
    }
    if (v.id())
    {
        setAttribute(el, "id", v.id()->toString());
    }
    serializeNoteContent(v, el);
}

void serializeNoteContent(const Note &v, pugi::xml_node el)
{
    serializeNoteChoice(v.choice(), el);
    for (const auto &item : v.instrument())
    {
        serializeInstrument(item, el, "instrument");
    }
    serializeEditorialVoiceGroup(v.editorialVoice(), el);
    if (v.type())
    {
        serializeNoteType(*v.type(), el, "type");
    }
    for (const auto &item : v.dot())
    {
        serializeEmptyPlacement(item, el, "dot");
    }
    if (v.accidental())
    {
        serializeAccidental(*v.accidental(), el, "accidental");
    }
    if (v.timeModification())
    {
        serializeTimeModification(*v.timeModification(), el, "time-modification");
    }
    if (v.stem())
    {
        serializeStem(*v.stem(), el, "stem");
    }
    if (v.notehead())
    {
        serializeNotehead(*v.notehead(), el, "notehead");
    }
    if (v.noteheadText())
    {
        serializeNoteheadText(*v.noteheadText(), el, "notehead-text");
    }
    if (v.staff())
    {
        writeTextElement(el, "staff", formatInt(*v.staff()));
    }
    for (const auto &item : v.beam())
    {
        serializeBeam(item, el, "beam");
    }
    for (const auto &item : v.notations())
    {
        serializeNotations(item, el, "notations");
    }
    for (const auto &item : v.lyric())
    {
        serializeLyric(item, el, "lyric");
    }
    if (v.play())
    {
        serializePlay(*v.play(), el, "play");
    }
    if (v.listen())
    {
        serializeListen(*v.listen(), el, "listen");
    }
}

} // namespace mx::core
