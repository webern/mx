// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/NameDisplay.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &NameDisplay::printObject() const noexcept
{
    return m_printObject;
}

void NameDisplay::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

std::span<const NameDisplayChoice> NameDisplay::choice() const noexcept
{
    return m_choice;
}

void NameDisplay::addChoice(NameDisplayChoice value)
{
    m_choice.push_back(std::move(value));
}

void NameDisplay::setChoice(std::vector<NameDisplayChoice> value)
{
    m_choice = std::move(value);
}

NameDisplay parseNameDisplay(pugi::xml_node el, const ParseContext &context)
{
    NameDisplay out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseNameDisplayContent(out, el, context);
    return out;
}

void parseNameDisplayContent(NameDisplay &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    while (cursor && (cursorIs(cursor, "display-text") || cursorIs(cursor, "accidental-text")))
    {
        out.addChoice(parseNameDisplayChoice(el, cursor, context));
    }
    if (cursor)
    {
        if (cursorIs(cursor, "display-text") || cursorIs(cursor, "accidental-text"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeNameDisplay(const NameDisplay &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    serializeNameDisplayContent(v, el);
}

void serializeNameDisplayContent(const NameDisplay &v, pugi::xml_node el)
{
    for (const auto &item : v.choice())
    {
        serializeNameDisplayChoice(item, el);
    }
}

} // namespace mx::core
