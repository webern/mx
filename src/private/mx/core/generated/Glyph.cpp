// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Glyph.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const GlyphType &Glyph::type() const noexcept
{
    return m_type;
}

void Glyph::setType(GlyphType value)
{
    m_type = std::move(value);
}

const SmuflGlyphName &Glyph::value() const noexcept
{
    return m_value;
}

void Glyph::setValue(SmuflGlyphName value)
{
    m_value = std::move(value);
}

Glyph parseGlyph(pugi::xml_node el, const ParseContext &context)
{
    Glyph out;
    bool seen_type = false;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "type")
        {
            seen_type = true;
            out.setType(parseValue<GlyphType>(a.value(), context, el, "type"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    if (!seen_type)
    {
        throwMissingAttribute(el, "type");
    }
    parseGlyphContent(out, el, context);
    return out;
}

void parseGlyphContent(Glyph &out, pugi::xml_node el, const ParseContext &context)
{
    out.setValue(parseValue<SmuflGlyphName>(childText(el), context, el, nullptr));
    if (firstElement(el))
    {
        throwUnknownElement(firstElement(el));
    }
}

void serializeGlyph(const Glyph &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    setAttribute(el, "type", v.type().toString());
    serializeGlyphContent(v, el);
}

void serializeGlyphContent(const Glyph &v, pugi::xml_node el)
{
    setText(el, v.value().toString());
}

} // namespace mx::core
