// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/ForPart.h"
#include "mx/core/generated/Transpose.h"

#include <cassert>
#include <string>
#include <variant>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A schema choice (synthesized from an anonymous particle
/// of the schema): exactly one alternative by construction.
/// Alternatives are positional; dispatch is by Kind/index, never by type
/// (alternative types may repeat -- plan §2.3).
class AttributesChoice final
{
  public:
    enum class Kind
    {
        transpose,
        forPart,
    };

    /// The natural zero: the first nullable (empty)
    /// alternative.
    AttributesChoice();

    static AttributesChoice transpose(std::vector<Transpose> value);
    static AttributesChoice forPart(std::vector<ForPart> value);

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isTranspose() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isTranspose().
    const std::vector<Transpose> &asTranspose() const noexcept
    {
        assert(isTranspose());
        return *std::get_if<0>(&m_value);
    }

    bool isForPart() const noexcept
    {
        return m_value.index() == 1;
    }

    /// Precondition: isForPart().
    const std::vector<ForPart> &asForPart() const noexcept
    {
        assert(isForPart());
        return *std::get_if<1>(&m_value);
    }

    bool operator==(const AttributesChoice &other) const = default;

  private:
    using Storage = std::variant<std::vector<Transpose>, std::vector<ForPart>>;

    explicit AttributesChoice(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

/// Consumes one alternative starting at `cursor` (which advances); `el` is
/// the enclosing element, for error paths.
AttributesChoice parseAttributesChoice(pugi::xml_node el, pugi::xml_node &cursor);

AttributesChoice parseAttributesChoice(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeAttributesChoice(const AttributesChoice &v, pugi::xml_node el);

} // namespace mx::core
