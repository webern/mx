// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/generated/Bass.h"
#include "mx/core/generated/Degree.h"
#include "mx/core/generated/HarmonyChordGroupChoice.h"
#include "mx/core/generated/Inversion.h"
#include "mx/core/generated/Kind.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// A harmony element can contain many stacked chords (e.g. V of II). A sequence of harmony-chord
/// groups is used for this type of secondary function, where V of II would be represented by a
/// harmony-chord with a 5 numeral followed by a harmony-chord with a 2 numeral. A root is a pitch
/// name like C, D, E, while a numeral is a scale degree like 1, 2, 3. The root element is generally
/// used with pop chord symbols, while the numeral element is generally used with classical
/// functional harmony and Nashville numbers. It is an either/or choice to avoid data inconsistency.
/// The function element, which represents Roman numerals with roman numeral text, has been
/// deprecated as of MusicXML 4.0.
/// A shared content group: transparent on the wire, its
/// fields serialize directly into the referencing element (plan §2.3).
class HarmonyChordGroup final
{
  public:
    const HarmonyChordGroupChoice &choice() const noexcept;
    void setChoice(HarmonyChordGroupChoice value);
    const Kind &kind() const noexcept;
    void setKind(Kind value);
    const std::optional<Inversion> &inversion() const noexcept;
    void setInversion(std::optional<Inversion> value);
    const std::optional<Bass> &bass() const noexcept;
    void setBass(std::optional<Bass> value);
    std::span<const Degree> degree() const noexcept;
    void addDegree(Degree value);
    void setDegree(std::vector<Degree> value);
    bool operator==(const HarmonyChordGroup &other) const = default;

  private:
    HarmonyChordGroupChoice m_choice{};
    Kind m_kind{};
    std::optional<Inversion> m_inversion;
    std::optional<Bass> m_bass;
    std::vector<Degree> m_degree;
};

/// Consumes matching siblings starting at `cursor` (which advances); `el`
/// is the enclosing element, for error paths.
HarmonyChordGroup parseHarmonyChordGroup(pugi::xml_node el, pugi::xml_node &cursor, const ParseContext &context);

void serializeHarmonyChordGroup(const HarmonyChordGroup &v, pugi::xml_node el);

} // namespace mx::core
