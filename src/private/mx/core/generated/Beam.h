// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/NameToken.h"
#include "mx/core/Token.h"
#include "mx/core/generated/BeamLevel.h"
#include "mx/core/generated/BeamValue.h"
#include "mx/core/generated/Color.h"
#include "mx/core/generated/Fan.h"
#include "mx/core/generated/YesNo.h"

#include <optional>
#include <string>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// Beam values include begin, continue, end, forward hook, and backward hook. Up to eight concurrent
/// beams are available to cover up to 1024th notes. Each beam in a note is represented with a
/// separate beam element, starting with the eighth note beam using a number attribute of 1. Note
/// that the beam number does not distinguish sets of beams that overlap, as it does for slur and
/// other elements. Beaming groups are distinguished by being in different voices and/or the presence
/// or absence of grace and cue elements. Beams that have a begin value can also have a fan attribute
/// to indicate accelerandos and ritardandos using fanned beams. The fan attribute may also be used
/// with a continue value if the fanning direction changes on that note. The value is "none" if not
/// specified. The repeater attribute has been deprecated in MusicXML 3.0. Formerly used for
/// tremolos, it needs to be specified with a "yes" value for each beam using it.
class Beam final
{
  public:
    const std::optional<BeamLevel> &number() const noexcept;
    void setNumber(std::optional<BeamLevel> value);
    const std::optional<YesNo> &repeater() const noexcept;
    void setRepeater(std::optional<YesNo> value);
    const std::optional<Fan> &fan() const noexcept;
    void setFan(std::optional<Fan> value);
    const std::optional<Color> &color() const noexcept;
    void setColor(std::optional<Color> value);
    const std::optional<Token> &id() const noexcept;
    void setID(std::optional<Token> value);
    const BeamValue &value() const noexcept;
    void setValue(BeamValue value);
    bool operator==(const Beam &other) const = default;

  private:
    std::optional<BeamLevel> m_number;
    std::optional<YesNo> m_repeater;
    std::optional<Fan> m_fan;
    std::optional<Color> m_color;
    std::optional<Token> m_id;
    BeamValue m_value{};
};

Beam parseBeam(pugi::xml_node el, const ParseContext &context);

void parseBeamContent(Beam &out, pugi::xml_node el, const ParseContext &context);

void serializeBeam(const Beam &v, pugi::xml_node parent, const char *tag);

void serializeBeamContent(const Beam &v, pugi::xml_node el);

} // namespace mx::core
