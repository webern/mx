// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <string_view>

namespace mx::core
{

/// The arrow-style type represents the style of an arrow, using Unicode arrow terminology. Filled
/// and hollow arrows indicate polygonal single arrows. Paired arrows are duplicate single arrows in
/// the same direction. Combined arrows apply to double direction arrows like left right, indicating
/// that an arrow in one direction should be combined with an arrow in the other direction.
/// Closed vocabulary: the named factories are the only constructors, so an
/// out-of-vocabulary value is unrepresentable through the public API.
class ArrowStyle final
{
  public:
    enum class Tag
    {
        single,
        double_,
        filled,
        hollow,
        paired,
        combined,
        other,
    };

    /// The natural zero: the first variant.
    ArrowStyle() noexcept = default;

    static ArrowStyle single() noexcept;
    static ArrowStyle double_() noexcept;
    static ArrowStyle filled() noexcept;
    static ArrowStyle hollow() noexcept;
    static ArrowStyle paired() noexcept;
    static ArrowStyle combined() noexcept;
    static ArrowStyle other() noexcept;

    Tag tag() const noexcept
    {
        return m_tag;
    }

    /// The wire literal (static storage).
    std::string_view toString() const noexcept;

    /// Strict: matches a wire literal exactly.
    static bool tryParse(std::string_view text, ArrowStyle &out) noexcept;

    /// Lenient: an unrecognized literal falls back to the first variant
    /// (the import leniency policy; never produces an invalid value).
    static ArrowStyle parse(std::string_view text) noexcept;

    /// Lenient, and says whether the literal was recognized.
    static ArrowStyle parse(std::string_view text, ValueParseOutcome &outcome) noexcept;

    bool operator==(const ArrowStyle &other) const noexcept = default;

  private:
    explicit ArrowStyle(Tag tag) noexcept : m_tag{tag}
    {
    }

    Tag m_tag{Tag::single};
};

} // namespace mx::core
