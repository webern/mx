// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/NameToken.h"
#include "mx/core/Token.h"
#include "mx/core/generated/Feature.h"
#include "mx/core/generated/StartStopSingle.h"

#include <optional>
#include <span>
#include <string>
#include <vector>

namespace pugi
{
class xml_node;
}

namespace mx::core
{

class ParseContext;

/// The grouping type is used for musical analysis. When the type attribute is "start" or "single",
/// it usually contains one or more feature elements. The number attribute is used for distinguishing
/// between overlapping and hierarchical groupings. The member-of attribute allows for easy
/// distinguishing of what grouping elements are in what hierarchy. Feature elements contained within
/// a "stop" type of grouping may be ignored. This element is flexible to allow for different types
/// of analyses. Future versions of the MusicXML format may add elements that can represent more
/// standardized categories of analysis data, allowing for easier data sharing.
/// Content fields mirror the schema grammar in declaration order; the
/// serializer walks them, so a wrong order is unrepresentable (plan §2.3).
class Grouping final
{
  public:
    const StartStopSingle &type() const noexcept;
    void setType(StartStopSingle value);
    const std::optional<std::string> &number() const noexcept;
    void setNumber(std::optional<std::string> value);
    const std::optional<std::string> &memberOf() const noexcept;
    void setMemberOf(std::optional<std::string> value);
    const std::optional<Token> &id() const noexcept;
    void setID(std::optional<Token> value);
    std::span<const Feature> feature() const noexcept;
    void addFeature(Feature value);
    void setFeature(std::vector<Feature> value);
    bool operator==(const Grouping &other) const = default;

  private:
    StartStopSingle m_type{};
    std::optional<std::string> m_number;
    std::optional<std::string> m_memberOf;
    std::optional<Token> m_id;
    std::vector<Feature> m_feature;
};

Grouping parseGrouping(pugi::xml_node el);

Grouping parseGrouping(pugi::xml_node el, const ParseContext &context);

void parseGroupingContent(Grouping &out, pugi::xml_node el);

void parseGroupingContent(Grouping &out, pugi::xml_node el, const ParseContext &context);

void serializeGrouping(const Grouping &v, pugi::xml_node parent, const char *tag);

void serializeGroupingContent(const Grouping &v, pugi::xml_node el);

} // namespace mx::core
