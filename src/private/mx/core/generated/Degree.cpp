// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#include "mx/core/generated/Degree.h"

#include "mx/core/Lexical.h"
#include "mx/core/ParseContext.h"
#include "mx/core/Xml.h"

#include <utility>

namespace mx::core
{

const std::optional<YesNo> &Degree::printObject() const noexcept
{
    return m_printObject;
}

void Degree::setPrintObject(std::optional<YesNo> value)
{
    m_printObject = std::move(value);
}

const DegreeValue &Degree::degreeValue() const noexcept
{
    return m_degreeValue;
}

void Degree::setDegreeValue(DegreeValue value)
{
    m_degreeValue = std::move(value);
}

const DegreeAlter &Degree::degreeAlter() const noexcept
{
    return m_degreeAlter;
}

void Degree::setDegreeAlter(DegreeAlter value)
{
    m_degreeAlter = std::move(value);
}

const DegreeType &Degree::degreeType() const noexcept
{
    return m_degreeType;
}

void Degree::setDegreeType(DegreeType value)
{
    m_degreeType = std::move(value);
}

Degree parseDegree(pugi::xml_node el)
{
    return parseDegree(el, ParseContext{});
}

Degree parseDegree(pugi::xml_node el, const ParseContext &context)
{
    Degree out;
    for (pugi::xml_attribute a : el.attributes())
    {
        const std::string_view aname{a.name()};
        if (isNamespaceDecl(aname))
        {
            continue;
        }
        if (aname == "print-object")
        {
            out.setPrintObject(parseValue<YesNo>(a.value(), context, el, "print-object"));
        }
        else
        {
            throwUnknownAttribute(el, a.name());
        }
    }
    parseDegreeContent(out, el, context);
    return out;
}

void parseDegreeContent(Degree &out, pugi::xml_node el)
{
    parseDegreeContent(out, el, ParseContext{});
}

void parseDegreeContent(Degree &out, pugi::xml_node el, const ParseContext &context)
{
    pugi::xml_node cursor = firstElement(el);
    if (cursorIs(cursor, "degree-value"))
    {
        out.setDegreeValue(parseDegreeValue(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "degree-value");
    }
    if (cursorIs(cursor, "degree-alter"))
    {
        out.setDegreeAlter(parseDegreeAlter(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "degree-alter");
    }
    if (cursorIs(cursor, "degree-type"))
    {
        out.setDegreeType(parseDegreeType(cursor, context));
        cursor = nextElement(cursor);
    }
    else
    {
        throwMissingOrMisplaced(el, cursor, "degree-type");
    }
    if (cursor)
    {
        if (cursorIs(cursor, "degree-value") || cursorIs(cursor, "degree-alter") || cursorIs(cursor, "degree-type"))
        {
            throwMisplacedElement(cursor);
        }
        throwUnknownElement(cursor);
    }
}

void serializeDegree(const Degree &v, pugi::xml_node parent, const char *tag)
{
    pugi::xml_node el = parent.append_child(tag);
    if (v.printObject())
    {
        setAttribute(el, "print-object", v.printObject()->toString());
    }
    serializeDegreeContent(v, el);
}

void serializeDegreeContent(const Degree &v, pugi::xml_node el)
{
    serializeDegreeValue(v.degreeValue(), el, "degree-value");
    serializeDegreeAlter(v.degreeAlter(), el, "degree-alter");
    serializeDegreeType(v.degreeType(), el, "degree-type");
}

} // namespace mx::core
