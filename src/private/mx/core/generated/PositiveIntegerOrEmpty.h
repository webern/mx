// Code generated by gen from musicxml-4.0-ed15c23. DO NOT EDIT.

#pragma once

#include "mx/core/Lexical.h"

#include <cassert>
#include <string>
#include <string_view>
#include <variant>

namespace mx::core
{

/// The positive-integer-or-empty values can be either a positive integer or an empty string.
/// Tagged union over the schema's member types: exactly one alternative by
/// construction. Alternatives are positional; dispatch is by Kind/index.
class PositiveIntegerOrEmpty final
{
  public:
    /// Marker for the literal "".
    struct Empty final
    {
        bool operator==(const Empty &) const noexcept = default;
    };

    enum class Kind
    {
        positiveInteger,
        empty,
    };

    /// The natural zero: the first member's natural zero.
    PositiveIntegerOrEmpty();

    static PositiveIntegerOrEmpty positiveInteger(int value);
    static PositiveIntegerOrEmpty empty();

    Kind kind() const noexcept
    {
        return static_cast<Kind>(m_value.index());
    }

    bool isPositiveInteger() const noexcept
    {
        return m_value.index() == 0;
    }

    /// Precondition: isPositiveInteger().
    const int &asPositiveInteger() const noexcept
    {
        assert(isPositiveInteger());
        return *std::get_if<0>(&m_value);
    }

    bool isEmpty() const noexcept
    {
        return m_value.index() == 1;
    }

    std::string toString() const;

    /// Strict: members tried in schema order.
    static bool tryParse(std::string_view text, PositiveIntegerOrEmpty &out);

    /// Lenient: unparseable text falls back to the first member's lenient
    /// parse (never produces an invalid value).
    static PositiveIntegerOrEmpty parse(std::string_view text);

    /// Lenient, and says whether no member matched or a matched number was
    /// clamped.
    static PositiveIntegerOrEmpty parse(std::string_view text, ValueParseOutcome &outcome);

    bool operator==(const PositiveIntegerOrEmpty &other) const = default;

  private:
    using Storage = std::variant<int, Empty>;

    explicit PositiveIntegerOrEmpty(Storage value) : m_value{std::move(value)}
    {
    }

    Storage m_value;
};

} // namespace mx::core
